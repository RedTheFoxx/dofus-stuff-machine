---
phase: 03-parcours-simplifi-document-depuis-le-rendu-r-el
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - docs/parcours-simplifie.md
  - tests/test_docs_parcours.py
autonomous: true
requirements:
  - SIMP-03
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Les deux écrans qui exposent la sauvegarde et l'export sont rendus par le client de test Flask et leurs libellés exigés : `GET /saves` porte `SAV-01` dans la ligne d'en-tête, `CHARGEMENT DES SAUVEGARDES LOCALES…` dans le corps et `N OUVRIR | DEL N | PURGE OUI` dans la ligne de statut (`dofus_stuff/web/routes.py:1280-1295`) ; l'écran de résultat porte `SAVE [NOM]`, `SAVES` et `DB` dans sa ligne de statut (`dofus_stuff/web/routes.py:1382`) et `data-mode=\"result\"` ; la page cite ces libellés (critère 3, V8)."
    - "La limite de sauvegarde navigateur est ancrée sur **les valeurs réellement portées par `dofus_stuff/web/static/js/terminal.js`** : le test extrait `SAVES_KEY` (clé `dofus-stuff-machine.saves`, ligne 14) et `MAX_SAVES` (valeur lue, ligne 15) et exige que la page cite cette clé et **cette valeur** ; passer `MAX_SAVES` à 50 rougit sans que la page change. C'est un contrôle de **littéral source**, pas de comportement (le JS n'est pas exécutable ici : ni `localStorage`, ni `shift`) et le module le dit (V9)."
    - "La page dit la vérité de l'éviction : à la 21ᵉ sauvegarde, la plus ancienne est **remplacée silencieusement** (`shift`, `terminal.js:294-318`) — la page contient la tournure épinglée `les plus anciennes sont remplacées` et **ne dit pas** `20 maximum` tout court ; introduire cette tournure dans la section rougit (V9, ÉCR-5)."
    - "L'export Dofusbook est ancré sur la surface publique `build_dofusbook_url` (pure) : pour des emplacements connus, l'URL produite contient `dofusbook.net` et un jeton `stuff=` qui se décode en `[caracs(51), points(51), niveau, flags, counts(10), ids]` avec `niveau` respecté ; le nombre d'emplacements exportés est **calculé** depuis `_GROUP_SLOTS` (`dofus_stuff/web/dofusbook_export.py:26-37`, 16) et la `prysma` est absente des `ids` alors que son identifiant est fourni : la page cite ce nombre et dit que la prysmaradite **n'est pas exportée** (critère 3, V10, mesure M9). L'ordre des dix groupes n'est pas re-testé : il l'est déjà par `tests/test_web.py:713-750`, que le module cite en commentaire au lieu de le dupliquer (D-12)."
    - "La page cite l'URL d'import réellement portée par le module (`DOFUSBOOK_IMPORT_URL`, `dofus_stuff/web/dofusbook_export.py:21`) : le test lit l'attribut public du module et exige sa présence dans la page — changer l'URL dans le code rougit sans que la page change (D-42) — et elle est citée **entre accents graves**, jamais comme lien externe (D-01)."
    - "Aucun test de ce plan ne poste `DB` sans patcher `webbrowser.open_new_tab` : la saisie `DB` sur l'écran de résultat appelle `webbrowser.open_new_tab` **côté serveur** (`dofus_stuff/web/routes.py:1326-1337`) et lancerait un navigateur pendant la suite. Le comportement est déjà couvert par `tests/test_web.py:668` (patché) : ce plan s'y renvoie et n'exécute que des rendus (sécurité du harnais)."
  artifacts:
    - path: "docs/parcours-simplifie.md"
      provides: "La section « Sauvegarder et exporter » : commande de sauvegarde, écran des sauvegardes, limite réelle du navigateur, export Dofusbook et emplacements non exportés"
      contains: "## Sauvegarder et exporter"
      min_lines: 80
    - path: "tests/test_docs_parcours.py"
      provides: "Contrôles de sauvegarde et d'export : rendu des deux écrans, extraction des littéraux du JS, calcul des groupes exportés, décodage de l'URL, refus d'une tournure fausse sur la limite"
      contains: "MAX_SAVES"
      min_lines: 260
  key_links:
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/web/static/js/terminal.js"
      via: "extraction des littéraux SAVES_KEY et MAX_SAVES par expression régulière sur le fichier, puis exigence de leur citation par la page (contrôle de littéral nommé comme tel)"
      pattern: "MAX_SAVES"
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/web/dofusbook_export.py"
      via: "appel public de build_dofusbook_url, décodage base64 + msgpack de la charge utile, et lecture de _GROUP_SLOTS et DOFUSBOOK_IMPORT_URL comme source des valeurs citées par la page"
      pattern: "build_dofusbook_url"
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/web/routes.py"
      via: "rendu de GET /saves et de l'écran de résultat par le client de test Flask, lecture de l'en-tête, du corps et de la ligne de statut"
      pattern: "/saves"
    - from: "tests/test_web.py"
      to: "dofus_stuff/web/dofusbook_export.py"
      via: "couverture existante réutilisée : l'ordre cape-avant-chapeau et les counts sont déjà prouvés là (tests/test_web.py:713-750), le plan 03-03 ne les réécrit pas"
      pattern: "_GROUP_SLOTS"
  prohibitions:
    - statement: "La page ne doit pas présenter la sauvegarde navigateur comme un envoi au serveur ni comme une garantie de conservation : elle reste dans le navigateur, et la 21ᵉ sauvegarde remplace la plus ancienne sans message d'échec."
    - statement: "La page ne doit pas laisser croire que tout le stuff part vers Dofusbook : la prysmaradite n'est pas exportée, et au plus seize emplacements le sont."
    - statement: "Aucun test ne doit postuler `DB` sans patcher `webbrowser.open_new_tab`, ni exécuter le JavaScript de sauvegarde (aucun moteur JS n'est disponible) : l'ancrage du JS reste un contrôle de littéraux."
  assumptions:
    - "Exigence **documentaire** : la sonde d'arêtes n'a pas de prédicat pour SIMP-03, donc ce plan est traité comme non classé. Décision enregistrée : la couverture d'arêtes est écrite à la main depuis V8, V9 et V10 de `03-VALIDATION.md`, chacune avec son contrôle falsifiable."
    - "Le **comportement** JavaScript (éviction, compteur, statuts de la liste, purge) n'est pas vérifiable dans cet environnement : le plan l'annonce comme un contrôle de littéraux source, ce qui prouve que la page et le code ne divergent pas sur les constantes et sur les libellés, **pas** que le navigateur se comporte ainsi."
    - "La liste des sauvegardes affichée par l'écran `SAV-01` est **remplacée par le navigateur** au chargement (`terminal.js:622-630`) : le rendu serveur ne porte que le texte d'attente, et le plan n'exige donc rien de plus sur le contenu de cette liste."
---

<objective>
**Cadrage dérivé (non une décision produit nouvelle)** — repris du `**Goal:**` de la phase 3, de son critère de succès 3 et des décisions D-38 et D-42 de `03-CONTEXT.md` :
**En tant que** lecteur qui vient d'obtenir un stuff, **je veux** savoir comment le garder dans mon navigateur et comment l'envoyer vers Dofusbook, en connaissant la limite réelle de la sauvegarde et ce qui ne part pas, **afin de** ne pas perdre un stuff par surprise ni croire qu'un export est complet alors qu'il ne l'est pas.

Purpose : c'est SIMP-03 et le critère 3 — « la sauvegarde navigateur (20 sauvegardes maximum) et l'export Dofusbook sont décrits et **rattachés à l'écran qui les expose** » (D-38 : une **section de cette page**, pas une page dédiée, pas de report). La recherche a mesuré deux vérités désagréables que cette section dit au lieu de les contourner : la 21ᵉ sauvegarde **remplace la plus ancienne sans le dire** (ÉCR-5), et la `prysma` **n'est pas exportée** (au plus seize emplacements partent vers Dofusbook).

Output : la section `## Sauvegarder et exporter` de `docs/parcours-simplifie.md` (insérée avant `## Source de vérité`), et, dans `tests/test_docs_parcours.py`, le helper de lecture de l'en-tête, les constantes de sauvegarde/export et deux tests (`test_ecrans_de_sauvegarde_et_export`, `test_sauvegarde_navigateur_et_export_dofusbook`).

Ordonnancement déclaré : **vague 3**, `depends_on: [03-02]` — même page, même module : les plans de la phase 3 ne s'exécutent pas en parallèle.

**Ce que ce plan ne fait pas** : il ne documente aucune commande (la surface de `fetcher.py` reste dans `docs/cli.md`, D-37) ; il ne décrit pas les écrans du wizard avancé (phase 4) ; il ne réécrit pas les assertions d'ordre des groupes Dofusbook déjà portées par `tests/test_web.py:713-750` (D-12) ; il n'indique ni chemin de fichier local, ni fichier de sauvegarde sur disque — la sauvegarde est **dans le navigateur**.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-CONTEXT.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-PATTERNS.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-VALIDATION.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-01-PLAN.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-02-PLAN.md
@docs/parcours-simplifie.md
@tests/test_docs_parcours.py
@dofus_stuff/web/routes.py
@dofus_stuff/web/dofusbook_export.py
@dofus_stuff/web/static/js/terminal.js
@dofus_stuff/web/templates/screen.html
@tests/test_web.py
@tests/test_docs_code_anchor.py
</context>
<tasks>

<task type="auto">
  <name>Tâche 1 : la section « Sauvegarder et exporter » et le contrôle des deux écrans qui les exposent</name>
  <files>docs/parcours-simplifie.md, tests/test_docs_parcours.py</files>
  <read_first>
    docs/parcours-simplifie.md
    tests/test_docs_parcours.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/templates/screen.html
    tests/test_web.py
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
  </read_first>
  <action>Écrire la section qui rattache la sauvegarde et l'export **à l'écran qui les expose** (D-38) et son contrôle de rendu.

**Page** — insérer `## Sauvegarder et exporter` après `## Correspondance des libellés` et avant `## Source de vérité` :
- une phrase : sur l'**écran de résultat**, la ligne de statut annonce les commandes disponibles, dont `SAVE [NOM]` (enregistrer le stuff courant dans le navigateur), `SAVES` (ouvrir la liste des sauvegardes) et `DB` (envoyer vers Dofusbook) — libellés **verbatim** du rendu (`dofus_stuff/web/routes.py:1382`), la page ne recopie **aucune** ligne de commande de `fetcher.py` (D-37) ;
- une phrase : la sauvegarde se fait en tapant `SAVE` ou `SAVE <libellé>` ; la liste s'ouvre sur l'écran des sauvegardes (`SAV-01`, route `/saves`), dont le corps affiche d'abord `CHARGEMENT DES SAUVEGARDES LOCALES…` — le navigateur remplace ensuite ce texte par la liste (`dofus_stuff/web/static/js/terminal.js:622-630`) — et dont la ligne de statut annonce `N OUVRIR | DEL N | PURGE OUI` (ouvrir une sauvegarde, en supprimer une, tout purger) ;
- une phrase sur la **limite réelle** : les sauvegardes vivent **dans le navigateur** et rien n'est envoyé au serveur ; au-delà de la limite, **les plus anciennes sont remplacées** (l'outil n'affiche pas de refus) — la page dit cela et **ne dit pas** `20 maximum` tout court (ÉCR-5, mesure : `shift` silencieux, `terminal.js:294-318`) ;
- une section courte pour l'export : `DB` sur l'écran de résultat (ou `DB DOFUSBOOK` depuis le détail d'une sauvegarde) ouvre une page Dofusbook pré-remplie ; l'URL d'import est citée **entre accents graves** (c'est une adresse technique, pas un lien externe, D-01) ; **la prysmaradite n'est pas exportée** et au plus seize emplacements partent : la page le dit (mesure M9).

**Module** — ajouter le helper `_entete(reponse)` (le texte de `<div class="row header" id="header-row">…</div>`, `dofus_stuff/web/templates/screen.html:24`) et le test `test_ecrans_de_sauvegarde_et_export(app, docs_dir, section, normalize)` :
1. `GET /saves` (client neuf) : `_entete` contient `SAV-01` ; `_lignes_du_corps` contient `CHARGEMENT DES SAUVEGARDES LOCALES…` ; `_statut` commence par `N OUVRIR | DEL N | PURGE OUI` ; la coquille porte `data-mode="saves"` ; le champ s'appelle `cmd` avec `maxlength="40"` ; `_touches` contient (`ESC`, `Retour`) ;
2. l'écran de résultat, obtenu **sans lancer de solveur** en injectant l'état de session, exactement comme le fait déjà `tests/test_web.py:668-671` (`session_transaction()` puis `sess["optimize_result_lines"]` et `sess["optimize_result_build"]`) : `GET /optimize/result` ; `_statut` contient `SAVE [NOM]`, `SAVES` et `DB` ; la coquille porte `data-mode="result"` ;
3. côté page : la section `## Sauvegarder et exporter` existe, contient `SAVE [NOM]`, `SAVES`, `CHARGEMENT DES SAUVEGARDES LOCALES…` et `N OUVRIR | DEL N | PURGE OUI`, et ne contient aucun bloc de code balisé `console` (aucune ligne de commande recopiée, D-37).
Les constats sont accumulés et joints à **une** assertion nommant la page, le libellé attendu et `dofus_stuff/web/routes.py:1280-1295` (`/saves`) ou `:1382` (résultat). **Ne jamais** poster `DB` : le comportement d'ouverture du navigateur est déjà couvert, patché, par `tests/test_web.py:668`.</action>
  <acceptance_criteria>
    - La section `## Sauvegarder et exporter` existe dans la page, avant `## Source de vérité`, et cite `SAVE [NOM]`, `SAVES`, `CHARGEMENT DES SAUVEGARDES LOCALES…` et `N OUVRIR | DEL N | PURGE OUI`.
    - Les deux écrans sont rendus par le client de test Flask et leurs libellés exigés : `SAV-01` + le corps + le statut de `/saves`, et le statut du résultat avec `SAVE [NOM]`/`SAVES`/`DB` plus `data-mode` sur les deux.
    - La section ne contient aucun bloc de code balisé `console` ni aucune ligne de commande (contrôle de forme), et la page n'ajoute aucun lien externe.
    - Aucun test ne poste `DB` ; le rendu du résultat ne lance pas le solveur (injection de session, patron de `tests/test_web.py:668`), donc le module reste rapide et sans effet de bord.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q` est vert ; `.data/dofus.sqlite3` garde taille et `mtime_ns`.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in statut_saves statut_resultat; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in statut_saves) sed -i 's/N OUVRIR | DEL N | PURGE OUI/OUVRIR | SUPPRIMER | PURGER/' "$T/dofus_stuff/web/routes.py"; motif='PURGE OUI';; statut_resultat) sed -i 's/ID DETAIL | SAVE \[NOM\] | SAVES | EDIT | DB/ID DETAIL | SAUVER | SAVES | EDIT | DB/' "$T/dofus_stuff/web/routes.py"; motif='SAVE [NOM]';; esac; out="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 03-03 EN ECHEC"; exit 1; fi; echo "morsures 03-03 (tache 1) : 2/2 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 03-03 EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>Le critère 3 est tenu pour le rattachement aux écrans : la sauvegarde et l'export sont décrits depuis les libellés réellement rendus de `/saves` et de l'écran de résultat, aucun bloc de commandes n'est recopié, et deux mutations de la ligne de statut rougissent le module sur une copie verte avant mutation.</done>
</task>

<task type="auto">
  <name>Tâche 2 : la limite de sauvegarde ancrée sur les littéraux du JS et l'export Dofusbook ancré sur sa surface publique</name>
  <files>docs/parcours-simplifie.md, tests/test_docs_parcours.py</files>
  <read_first>
    docs/parcours-simplifie.md
    tests/test_docs_parcours.py
    dofus_stuff/web/static/js/terminal.js
    dofus_stuff/web/dofusbook_export.py
    tests/test_web.py
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
  </read_first>
  <action>Ancrer la limite de sauvegarde **sur les valeurs du fichier JS** (et non sur une valeur écrite de mémoire) et prouver l'export par la **surface publique pure** `build_dofusbook_url`.

**Module** — ajouter :
- `_litteral_js(motif)` : lecture de `dofus_stuff/web/static/js/terminal.js` en UTF-8, première capture du motif, échec nommant le fichier et le motif si rien ne correspond (« la constante a peut-être été renommée : la page doit être mise à jour »), jamais une exception brute ; constantes de motif épinglées : clé de stockage (`SAVES_KEY = "…"`, ligne 14) et `MAX_SAVES = …` (ligne 15) ;
- `test_sauvegarde_navigateur_et_export_dofusbook(app, docs_dir, section, normalize)` :
  1. **Valeur de la limite** : `_litteral_js` rend la valeur courante de `MAX_SAVES` ; la section `## Sauvegarder et exporter` doit citer **cette valeur** (mot entier) — le message d'échec la cite avec son porteur (`MAX_SAVES = <valeur>` dans `terminal.js:15`) ;
  2. **Clé de stockage** : la section cite la clé extraite, verbatim ;
  3. **Éviction honnête** : la section contient la tournure épinglée `les plus anciennes sont remplacées` et **ne contient pas** la tournure interdite `20 maximum` (ÉCR-5) ;
  4. **Libellés du JS ancrés sur leur porteur** (patron `LIBELLES_SOURCE` de `tests/test_docs_code_anchor.py:61-74`) : pour `DB DOFUSBOOK`, `BACK LISTE` et `SAUVEGARDES PURGEES`, exiger le libellé **dans la page** (normalisé) **et** le même libellé sur une ligne du fichier JS — un libellé qui disparaît du JS ou de la page rougit ;
  5. **Export, par la surface publique pure** : appeler `build_dofusbook_url` sur un dictionnaire de **17 emplacements d'identifiants distincts** (les seize exportés plus `prysma`) ; décoder le jeton `stuff=` (`base64` puis `msgpack`, comme le fait déjà `tests/test_web.py:684-712`) ; exiger la forme mesurée `[caracs(51), points(51), niveau, flags, counts(10), ids]`, `niveau` respecté, **`sum(counts) == 16`** (donc au plus seize emplacements exportés) et **l'identifiant de `prysma` absent de `ids`** ; l'ordre des dix groupes **n'est pas** re-testé (il l'est par `tests/test_web.py:713-750` : le module cite ce test en commentaire, il ne le duplique pas — D-12) ;
  6. **Côté page** : la section contient le nombre calculé (`16`, littéral lu du calcul, jamais écrit de mémoire), la mention que la `prysma` **n'est pas exportée** (tournure épinglée `n'est pas exportée` sur la même ligne que `prysma`), et l'URL d'import **réellement portée par le module** (`DOFUSBOOK_IMPORT_URL`, lue par `importlib` sur l'attribut public), citée entre accents graves ;
- `test_aucun_post_db_sans_patch()` : garde de sûreté du harnais, par `ast` sur **ce module** — aucun import de `webbrowser`, et aucun appel dont un argument nommé `data` soit un dictionnaire littéral contenant la paire `"cmd": "DB"`. Le message d'échec rappelle pourquoi : la saisie `DB` appelle `webbrowser.open_new_tab` côté serveur (`dofus_stuff/web/routes.py:1326-1337`) et lancerait un navigateur pendant la suite ; le comportement est couvert, patché, par `tests/test_web.py:668`.
Les constats sont accumulés et joints à **une** assertion nommant la page, la valeur attendue et le fichier cité. Le module doit écrire que le contrôle du JS est un contrôle de **littéraux** (aucun moteur JS ici : ni `localStorage`, ni `shift`), jamais un contrôle de comportement.</action>
  <acceptance_criteria>
    - La section cite la valeur courante de `MAX_SAVES` et la clé de stockage extraites du fichier JS ; passer `MAX_SAVES` à 50 (ou renommer la clé) rougit sans que la page soit modifiée.
    - La section contient `les plus anciennes sont remplacées` et **pas** `20 maximum` ; introduire cette tournure rougit.
    - Les libellés `DB DOFUSBOOK`, `BACK LISTE` et `SAUVEGARDES PURGEES` sont exigés dans la page **et** sur une ligne du fichier JS.
    - L'export est prouvé par `build_dofusbook_url` : charge utile décodée de forme `[caracs(51), points(51), niveau, flags, counts(10), ids]`, `sum(counts) == 16`, identifiant de `prysma` absent des `ids` ; la section cite `16`, la non-exportation de la `prysma` et l'URL d'import du module.
    - La garde `ast` du harnais passe : ce module n'importe pas `webbrowser` et ne poste jamais `DB`.
    - `./.venv/Scripts/python.exe -m pytest -q` reste vert, module compris ;`.data/dofus.sqlite3` garde taille et `mtime_ns`.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in max_saves tournure_fausse export_prysma url_import; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in max_saves) sed -i 's/var MAX_SAVES = 20;/var MAX_SAVES = 50;/' "$T/dofus_stuff/web/static/js/terminal.js"; motif='MAX_SAVES = 50';; tournure_fausse) sed -i 's/^## Sauvegarder et exporter$/## Sauvegarder et exporter\n\nLes sauvegardes : 20 maximum./' "$T/docs/parcours-simplifie.md"; motif='20 maximum';; export_prysma) sed -i 's|("pet",),  # 9 familier/monture (fa)|("pet", "prysma"),  # 9 familier/monture (fa)|' "$T/dofus_stuff/web/dofusbook_export.py"; motif='16';; url_import) sed -i 's|dofus-stuffer/objets|dofus-stuffer/objet|' "$T/dofus_stuff/web/dofusbook_export.py"; motif='DOFUSBOOK_IMPORT_URL';; esac; out="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 03-03 EN ECHEC"; exit 1; fi; echo "morsures 03-03 (tache 2) : 4/4 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 03-03 EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>Le critère 3 est tenu pour la limite et l'export : la limite de sauvegarde est ancrée sur la constante du JS et sur la tournure honnête de l'éviction, l'export est prouvé pur et testé (seize emplacements, `prysma` exclue), le harnais ne peut pas lancer de navigateur, et quatre dérives rougissent le module sur une copie verte avant mutation.</done>
</task>

</tasks>

<threat_model>
**Surface** : une page `docs/`, un module de test, une lecture du JS et de la base locale par la fixture. Aucun réseau, aucun secret, aucune dépendance ajoutée. La menace réelle de ce plan est **l'effet de bord** : un test qui déclenche une action du navigateur ou qui laisse croire à un comportement JS prouvé.

| # | Menace | Mitigation | Résidu |
|---|--------|-----------|--------|
| T-12 | **Lancer un navigateur pendant la suite** : poster `DB` sur l'écran de résultat appelle `webbrowser.open_new_tab` côté serveur | Le plan n'exécute que des rendus ; la garde `ast` interdit tout `webbrowser` et tout `post` avec `"cmd": "DB"` dans le module ; le comportement reste couvert, patché, par `tests/test_web.py:668` (patron de patch mesuré) | Si un futur plan veut tester `DB`, il devra patcher — la garde le signale |
| T-13 | **Comportement JS présenté comme prouvé** : le JS de sauvegarde n'est pas exécutable ici | La page et le module nomment la limite : contrôle de **littéraux** (`SAVES_KEY`, `MAX_SAVES`, libellés) et non de comportement | L'éviction réelle reste non testée ; elle est décrite depuis la source lue |
| T-14 | **Export présenté comme complet** : la prysmaradite ne part pas, elle était pourtant dans le stuff | Contrôle **comportemental** sur la surface publique pure (`build_dofusbook_url` + décodage `msgpack`) : `sum(counts) == 16` et identifiant de `prysma` absent ; la page le dit | Aucun : c'est mesuré à chaque exécution |
| T-15 | **Valeur figée côté page** : `20` écrit de mémoire alors que la constante peut changer | La valeur de la page est comparée à celle **extraite du JS** à chaque exécution ; changer `MAX_SAVES` fait rougir | Aucun |
| T-16 | **Écriture sous `.data/`** ou sur le disque de l'utilisateur | Aucun test n'écrit ; le rendu de `/saves` et du résultat n'écrit rien ; `mtime_ns` et taille comparés après la tâche | La base reste lue, jamais modifiée |

**Hors périmètre** : publication, déploiement, synchronisation Dofusdude, suppression de sauvegardes côté machine — aucune commande destructive n'est employée, et la purge décrite dans la page est une action **du lecteur dans son navigateur**, jamais exécutée par les tests.
</threat_model>

<verification>
**Contrôles automatisés** : `tests/test_docs_parcours.py` (V8, V9, V10), les modules des phases 1-2 inchangés, la suite complète, et six morsures sur copie verte avant mutation : statut de `/saves` reformulé, statut du résultat reformulé, `MAX_SAVES` porté à 50, tournure interdite ajoutée dans la page, `prysma` ajoutée à un groupe d'export, URL d'import modifiée dans le module.

**Non automatisable, nommé comme tel** : le comportement du navigateur (compteur de sauvegardes, éviction, purge, hydratation de la liste) — aucun moteur JS dans l'environnement ; le plan l'assume et nomme le contrôle comme un contrôle de littéraux. Aucun blocage : rien ne dépend d'un geste humain, d'un secret ou d'un réseau.

**Références de vérification de phase** : `03-VALIDATION.md` § Per-Task Verification Map assigne V8–V10 à ce plan ; `/gsd:verify-work` rejouera la suite et la mesure de non-régression `.data/`.
</verification>

<success_criteria>
- La section `## Sauvegarder et exporter` décrit la sauvegarde navigateur et l'export Dofusbook **depuis les écrans qui les exposent**, sans recopier la surface de commandes de `fetcher.py` (critère 3, V8, D-37, D-38).
- La limite de sauvegarde est celle du JS (valeur extraite), l'éviction est dite honnêtement, et la tournure fausse est interdite (V9, ÉCR-5).
- L'export est prouvé par la surface publique pure : seize emplacements au plus, `prysma` exclue, URL d'import citée depuis le module (V10, mesure M9), l'ordre des groupes restant couvert par `tests/test_web.py:713-750`.
- Le harnais ne peut ni lancer de navigateur ni écrire sous `.data/`, et six dérives épinglées le font rougir sur une copie verte avant mutation.
- La suite complète est verte et `.data/dofus.sqlite3` est intact (taille + `mtime_ns`).
</success_criteria>

## Couverture des sources (ce plan)

| Source | Élément | Consommé par |
|--------|---------|--------------|
| ROADMAP § Phase 3 | Critère 3 (sauvegarde navigateur, export Dofusbook, rattachés à l'écran qui les expose) | tâche 1 (écrans) et tâche 2 (limite et export), ÉCR-5 en tâche 2 |
| REQUIREMENTS | SIMP-03 | `requirements` du plan |
| CONTEXT | D-38 (« Sauvegarder et exporter » est une section de cette page) | tâche 1 (titre épinglé `## Sauvegarder et exporter`) |
| CONTEXT | D-37 (la surface de commandes reste dans `docs/cli.md`) | tâche 1 (aucun bloc `console`, aucun recopiage) |
| CONTEXT | D-42 (aucune constante écrite de mémoire) | tâche 2 (valeurs extraites du JS et du module) |
| CONTEXT | D-14 (ancrage par API publique, jamais d'introspection privée) | tâche 2 (`build_dofusbook_url`, `DOFUSBOOK_IMPORT_URL`) |
| CONTEXT | D-41 (une limite non adossée est retirée ou présentée comme une interprétation) | tâche 2 (prysma non exportée, éviction silencieuse) |
| CONTEXT | D-45 (conventions des phases 1-2) | tâches 1 et 2 |
| RESEARCH | M8 (écrans `OPT-03` et `SAV-01`, `MAX_SAVES`, clé de stockage, éviction silencieuse, JS non atteignable) | tâches 1 et 2 |
| RESEARCH | M9 (URL d'import, dix groupes, `prysma` exclue, canaux réels, couverture existante à réutiliser) | tâche 2 |
| RESEARCH | M12 (aucune écriture sous `.data/`) | la vérification des deux tâches |
| PATTERNS | Patron `LIBELLES_SOURCE` (libellé + porteur sur la même ligne) | tâche 2 (libellés du JS) |
| VALIDATION | V8, V9, V10 | tâche 1 (V8), tâche 2 (V9, V10) |
| VALIDATION | V14, V15 | la suite complète et la mesure locale |

**Ligne `unresolved` (assumée)** : SIMP-03 est une exigence documentaire, sans prédicat évaluable par la sonde d'arêtes ; la couverture d'arêtes de ce plan est écrite à la main depuis V8, V9 et V10 (voir `must_haves.assumptions`), et le contrôle du JS est **nommé** comme un contrôle de littéraux.

## Contributions et sondes

| Élément | État | Emplacement |
|---------|------|-------------|
| Composants, libs, capabilities, fichiers réservés | — | Absents du dépôt : rien à brancher ni à déclarer |
| Tables canon | — | Ce plan n'écrit aucune ligne de table de suivi |
| Sonde jetable | `.gsd-tmp/plan-p3/` si l'exécuteur en écrit une | Non suivie, non indexée, jamais supprimée ; les morsures travaillent dans un `mktemp -d` hors dépôt |
| Sauvegardes du lecteur | — | Le plan **n'écrit aucune** sauvegarde : ni `localStorage` (impossible sans navigateur), ni fichier de sauvegarde sur disque |

## Artifacts this phase produces (ce plan)

- **Créés** : la section `## Sauvegarder et exporter` de `docs/parcours-simplifie.md` (insérée avant `## Source de vérité`).
- **Créés dans `tests/test_docs_parcours.py`** : le helper `_entete`, `_litteral_js`, les motifs épinglés (`MOTIF_MAX_SAVES`, `MOTIF_SAVES_KEY`), les tournures épinglées (éviction, tournure interdite, non-exportation), et les tests `test_ecrans_de_sauvegarde_et_export`, `test_sauvegarde_navigateur_et_export_dofusbook`, `test_aucun_post_db_sans_patch`.
- **Non modifiés** : `dofus_stuff/**`, `tests/conftest.py`, `tests/test_web.py` (sa couverture est citée, pas réécrite), `tests/test_docs_structure.py`, `tests/test_docs_code_anchor.py`, `docs/sommaire.md`, `docs/cli.md`.

