---
phase: 03-parcours-simplifi-document-depuis-le-rendu-r-el
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - docs/parcours-simplifie.md
  - tests/test_docs_parcours.py
autonomous: true
requirements:
  - SIMP-02
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "La carte de pagination rendue est exigée dans la **ligne de statut** de l'écran de résultat (jamais dans le corps) : sur la fixture minimale (niveau 200, les quatre éléments — 3 pages mesurées), la ligne de statut de la première page contient un fragment `PAGE 1/N` et `ENTREE=VALIDER`, les attributs `data-body-page`/`data-body-total` de la coquille sont cohérents avec elle, et la page N/N rendue avec `?page=N` porte `PAGE N/N` : la cohérence est vérifiée entre le statut et les attributs, sans épingler un total de pages (mesure M6 ; le total dépend du catalogue). Un `indicators.append` retiré de `dofus_stuff/web/routes.py:145` rougit le module (V5)."
    - "La position des trois diagnostics est vérifiée **positionnellement** sur la concaténation des pages du résultat, dans l'ordre : `Méthode : `, `Score : `, `Indice de recherche : ` après le dernier `Équipement :` et avant `Greedy: `, la phrase `Recherche sur une sélection du catalogue ; optimalité globale non garantie.` juste après le bloc de diagnostics (mesures M6, insertion `dofus_stuff/optimize/api.py:433`, bloc `if simple:` en `:432` et phrase du catalogue en `:434`). Le contrôle n'assert **jamais** `page == total` : sur la fixture les diagnostics tombent sur la page 2/3, et sur la base réelle la même exécution donne 6 puis 7 pages (mesures M6 et M11) — c'est la reformulation enregistrée d'ÉCR-2 (V6)."
    - "La table « libellé technique → nom complet » de la page couvre **exactement** les libellés de `display_slots` (`dofus_stuff/optimize/api.py:362-380`, extraits par `ast`), et chaque nom complet cité par la page est encore lisible dans la source citée : les commentaires de `dofus_stuff/web/dofusbook_export.py:26-37` pour les seize emplacements exportés, `dofus_stuff/model/solver_spec.py` pour `prysma` (`Prysmaradite`). Un libellé retiré de `display_slots`, une ligne de table supprimée dans la page ou un nom complet absent de sa source rougissent le module (V7, ÉCR-1)."
    - "Aucun libellé de slot n'est présenté comme tronqué et aucun test n'asserte l'apparition de `…` : la règle de troncature (`dofus_stuff/web/screens.py:14-20`) ne se déclenche jamais dans ce flux (zéro ligne rendue contenant `…`, longueur maximale exactement 100 = `COLS`, libellés complétés par `f\"  {slot:8s} : \"` — mesures M7) ; c'est la reformulation enregistrée d'ÉCR-1, et la table porte sur le **nom complet** de l'emplacement, pas sur une troncature."
    - "La section `## Lire le résultat` ne fige aucune valeur volatile : les motifs `Score :\\s*\\d`, `Indice de recherche :\\s*\\d`, `Greedy:\\s*\\d` et `Méthode :\\s*[a-z]` n'y apparaissent pas (une méthode, un score ou un total épinglés dans la page seraient faux à la prochaine exécution : deux exécutions sur la base réelle ont donné `cpsat_feasible+local (FEASIBLE)` puis `greedy (FEASIBLE)` et 6 puis 7 pages, mesure M11), et la section ne contient pas la tournure `dernière page` (reformulation d'ÉCR-2)."
    - "La carte de pagination et les diagnostics sont décrits par la page avec les libellés réellement rendus, et la barre de touches du résultat est distinguée de celle du wizard : `F7=Page prec`, `F8=Page suiv`, `ESC=Retour` sur le résultat (mesure M6, déjà épinglés par `tests/test_docs_code_anchor.py:64-67`), contre `Precedent`/`Suivant` pour le wizard — la page ne doit pas emprunter les libellés de la phase 4."
  artifacts:
    - path: "docs/parcours-simplifie.md"
      provides: "Les sections « Lire le résultat » (carte de pagination, emplacement des diagnostics, valeurs volatiles annoncées comme telles, barre de touches) et « Correspondance des libellés » (table libellé technique → nom complet des 17 emplacements)"
      contains: "## Lire le résultat"
      min_lines: 60
    - path: "tests/test_docs_parcours.py"
      provides: "Contrôles de lecture du résultat : pagination cohérente statut/attributs, position des diagnostics, table des emplacements ancrée sur display_slots et sur les commentaires d'export, interdiction des valeurs volatiles"
      contains: "display_slots"
      min_lines: 210
  key_links:
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/web/routes.py"
      via: "rendu du résultat par le client de test Flask (injection de session puis POST de l'étape 3), lecture de la ligne de statut et des attributs data-body-page/data-body-total de la coquille"
      pattern: "optimize_result"
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/optimize/api.py"
      via: "extraction par ast de la table display_slots, et lecture des lignes rendues concaténées pour la position des diagnostics"
      pattern: "display_slots"
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/web/dofusbook_export.py"
      via: "lecture des commentaires de _GROUP_SLOTS pour exiger que chaque nom complet cité par la page soit encore lisible dans la source citée"
      pattern: "_GROUP_SLOTS"
    - from: "docs/parcours-simplifie.md"
      to: "dofus_stuff/optimize/api.py"
      via: "la table des libellés techniques de la page cite les libellés que api.py rend réellement (display_slots), jamais une liste écrite de mémoire"
      pattern: "display_slots"
  prohibitions:
    - statement: "La page ne doit pas épingler une valeur volatile du résultat (score, méthode, total de pages, indice) ni présenter « Méthode », « Score » ou « Indice de recherche » comme une qualité en combat : ce sont des diagnostics internes du solveur."
    - statement: "La page ne doit pas dire que les diagnostics sont sur la dernière page, et aucun test ne doit comparer le numéro de page au total."
    - statement: "Aucune assertion ne doit porter sur `data-stuff-payload` ni sur la réponse HTTP entière : elle porte tout le résultat sur chaque page, accents échappés, donc `assert \"Méthode\" in response.data` est faux sur une page correcte et `assert \"Score\" in response.data` est vrai sur une page qui ne l'affiche pas (mesure M6)."
  assumptions:
    - "Exigence **documentaire** : la sonde d'arêtes n'a pas de prédicat pour une exigence de rédaction, donc SIMP-02 est traitée comme non classée. Décision enregistrée : la couverture d'arêtes de ce plan est écrite à la main depuis V5, V6, V7 de `03-VALIDATION.md`, chacune avec son contrôle falsifiable."
    - "Les noms français des emplacements ne sont pas rendus par l'interface : ils sont lisibles dans le code (commentaires de `_GROUP_SLOTS`, `dofus_stuff/web/dofusbook_export.py:26-37`) et le contrôle exige que cette source les porte encore. Ce qui reste une interprétation — l'équivalence entre le libellé technique et le mot affiché à l'écran du jeu — est présentée dans la page comme une correspondance de lecture, pas comme une sortie de l'outil."
    - "Le total de pages et la page où tombent les diagnostics dépendent du catalogue : ce plan n'épingle donc **aucun** total, et la fixture minimale (3 équipements, déterminisme mesuré par empreinte SHA-256 identique sur trois exécutions) est la seule source utilisée."
---

<objective>
**Cadrage dérivé (non une décision produit nouvelle)** — repris du `**Goal:**` de la phase 3 (« … lire son résultat … »), de son critère de succès 2 et des décisions D-32, D-38 et D-42 de `03-CONTEXT.md` :
**En tant que** lecteur qui vient d'obtenir un stuff, **je veux** comprendre où lire la pagination de mon résultat, où sont les informations de calcul et ce que veut dire chaque libellé court affiché par l'outil, **afin de** interpréter l'écran sans connaître le vocabulaire technique du solveur.

Purpose : c'est SIMP-02 et le critère 2 de la phase, dans ses deux moitiés **mesurées** : la carte de pagination et l'emplacement réel de `Méthode`, `Score` et `Indice de recherche` (reformulation enregistrée d'ÉCR-2 : l'assertion est **positionnelle**, jamais un indice de page), et la correspondance libellé technique → nom complet des emplacements (reformulation enregistrée d'ÉCR-1 : dans ce flux, rien n'est tronqué — la largeur maximale d'une ligne rendue vaut exactement `COLS` = 100 et les libellés sont complétés, jamais coupés).

Output : deux sections de `docs/parcours-simplifie.md` (`## Lire le résultat`, `## Correspondance des libellés`, insérées **avant** `## Source de vérité`), et, dans `tests/test_docs_parcours.py`, le helper `_touches`, la table épinglée `SLOTS_MESURE`, l'extraction par `ast` de la table `display_slots`, et deux tests (`test_pagination_et_emplacement_du_calcul`, `test_correspondance_libelles_slots`).

Ordonnancement déclaré : **vague 2**, `depends_on: [03-01]` — dépendance de fichiers, pas de choix : ce plan étend la même page et le même module que le plan 03-01, donc ils ne peuvent pas s'exécuter en parallèle.

**Ce que ce plan ne fait pas** : il ne documente ni la saisie `SAVE`, ni la copie Dofusbook, ni l'écran `/saves` (c'est 03-03, rattaché à l'écran qui les expose), ni les hypothèses et les limites (03-04), ni la ligne `Détail … base+parcho` (03-04 lève l'ambiguïté du mot « parcho » avec l'hypothèse « zéro parchemin »), ni les options de commande (phase 2). Les valeurs numériques du résultat ne sont **jamais** recopiées : la page décrit des emplacements et des formes, et dit explicitement quelles valeurs varient d'une exécution à l'autre.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-CONTEXT.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-PATTERNS.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-VALIDATION.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-01-PLAN.md
@docs/parcours-simplifie.md
@tests/test_docs_parcours.py
@tests/conftest.py
@dofus_stuff/web/routes.py
@dofus_stuff/optimize/api.py
@dofus_stuff/web/dofusbook_export.py
@dofus_stuff/web/screens.py
@dofus_stuff/web/templates/screen.html
@tests/test_docs_code_anchor.py
@tests/test_recommend.py
</context>
<tasks>

<task type="auto">
  <name>Tâche 1 : la section « Lire le résultat » et le contrôle de la pagination et de l'emplacement des diagnostics</name>
  <files>docs/parcours-simplifie.md, tests/test_docs_parcours.py</files>
  <read_first>
    docs/parcours-simplifie.md
    tests/test_docs_parcours.py
    dofus_stuff/web/routes.py
    dofus_stuff/optimize/api.py
    dofus_stuff/web/templates/screen.html
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
  </read_first>
  <action>Écrire la moitié « emplacement et pagination » du critère 2, puis son contrôle — en gardant l'assertion **positionnelle** (reformulation d'ÉCR-2) et **jamais** un indice de page.

**Page** — insérer `## Lire le résultat` **avant** `## Source de vérité`, et rien d'autre (le plan 03-03 ajoutera ses sections avant la même ancre). Contenu :
- une phrase : le résultat s'affiche comme un écran plein ; quand il tient sur une seule page, il n'y a **aucune carte de pagination et aucune touche F7/F8** (mesure : la carte et les deux touches ne sont ajoutées que si `total > 1`, `dofus_stuff/web/routes.py:143-146`) ;
- une phrase : quand le résultat s'étale sur plusieurs pages, la **ligne de statut**, en bas de l'écran, l'annonce sous la forme `PAGE n/total`, et les touches `F7=Page prec` et `F8=Page suiv` le parcourent (`ESC=Retour` revient) — libellés **verbatim** du rendu, et **différents de ceux du wizard avancé** (`Precedent`/`Suivant`), que cette page ne documente pas (D-43) ; la page cite aussi, dans la même phrase, la fin de la ligne de statut `ENTREE=VALIDER` (le rendu la porte parce que l'écran garde un champ de saisie, `dofus_stuff/web/routes.py:146-147`) ;
- une phrase : les informations de calcul — `Méthode : `, `Score : `, `Indice de recherche : ` — sont **en fin de résultat**, après la liste des emplacements, suivies de la phrase `Recherche sur une sélection du catalogue ; optimalité globale non garantie.` (mesures M6 : bloc construit en `dofus_stuff/optimize/api.py:327-341`, inséré en `dofus_stuff/optimize/api.py:433`, phrase en `:434`). La page écrit `en fin de résultat — jusqu'à PAGE n/n` et **ne dit nulle part** qu'elles sont « sur la dernière page » (ÉCR-2) ;
- une phrase : ces trois valeurs **changent d'une exécution à l'autre** — deux exécutions sur le même niveau ont donné deux méthodes différentes et six puis sept pages (mesure M11) — et la page n'en fige donc aucune ;
- une phrase sur la liste des emplacements : elle est introduite par `Équipement :`, chaque ligne commence par le libellé technique de l'emplacement (voir la section suivante pour le nom complet), et un emplacement vide affiche `(vide)`, **sauf** les six Dofus, le familier, la prysmaradite et le bouclier, qui sont omis quand ils sont vides (mesure : `dofus_stuff/optimize/api.py:384-385`).

**Module** — réutiliser le helper `_touches(reponse)` **posé par le plan 03-01 (tâche 1)**, où il sert déjà à exiger `ESC=Retour` (couples touche ↔ libellé de la barre `id="fkey-bar"`, motif `<span class="fkey-key meta">([^<]*)</span><span class="fkey-eq meta">=</span><span class="fkey-label">([^<]*)</span>`, mesuré dans `dofus_stuff/web/templates/screen.html:69-73`) : **ne pas le redéfinir**, deux définitions divergeraient ; `test_pagination_et_emplacement_du_calcul(app, docs_dir, section, normalize)` :
1. **Injection de session, puis rendu réel** (patron de la phase : `client.session_transaction()`, déjà employé en `tests/test_web.py:553`) : un client neuf dont la session porte **`recommendation_input`** — `sess["recommendation_input"] = {"classe": "Cra", "elements": ["terre", "feu", "eau", "air"]}` : c'est **la clé que la vue lit** (`dofus_stuff/web/routes.py:940`), et injecter les mêmes clés à la racine de la session fait répondre `302 -> /optimize` au POST de l'étape 3 (**mesuré** — ni la pagination ni les diagnostics ne seraient alors rendus ; la forme est la même que celle écrite par `_client_etape` au plan 03-01) — puis `POST /optimize/quick/niveau` avec `cmd=200`, sans suivre la redirection : exiger un `302` vers `/optimize/result` (le solveur tourne **une** fois, sur la fixture : 0,29 s mesuré) ;
2. **Carte de pagination** : `GET /optimize/result` ; la ligne de statut doit contenir un fragment `PAGE 1/<total>` (capturer `<total>`) ; les attributs de la coquille `data-body-page` et `data-body-total` doivent valoir `1` et `<total>` (cohérence statut/attributs, jamais un total épinglé) ; `data-mode` doit valoir `result` ;
3. **Dernière page atteinte** : `GET /optimize/result?page=<total>` ; sa ligne de statut doit contenir `PAGE <total>/<total>` ;
4. **Barre de touches** : `_touches` doit contenir les trois couples (`F7`, `Page prec`), (`F8`, `Page suiv`), (`ESC`, `Retour`) — et **pas** (`F7`, `Precedent`) ni (`F8`, `Suivant`) ;
5. **Position des diagnostics, sur les pages concaténées dans l'ordre** : lire les pages `1..<total>` et concaténer leurs lignes du corps ; exiger que les trois marqueurs `Méthode : `, `Score : `, `Indice de recherche : ` apparaissent **après** la dernière ligne commençant par `Équipement :` et **avant** la ligne commençant par `Greedy: `, et que la phrase `Recherche sur une sélection du catalogue ; optimalité globale non garantie.` soit la ligne immédiatement après la dernière ligne de diagnostic — **aucune** comparaison entre un numéro de page et le total, nulle part (ÉCR-2) ;
6. **Emplacements vides** : la concaténation doit contenir la chaîne `(vide)` ;
7. **Côté page** : la section `## Lire le résultat` existe et contient `ENTREE=VALIDER`, `F7=Page prec`, `F8=Page suiv`, `PAGE n/total`, la phrase `en fin de résultat`, la phrase du catalogue, et `(vide)` ; elle **ne contient pas** la tournure `dernière page`, et aucun des motifs de valeur volatile (`Score :\s*\d`, `Indice de recherche :\s*\d`, `Greedy:\s*\d`, `Méthode :\s*[a-z]`) n'y apparaît — une valeur figée serait fausse à la prochaine exécution (mesure M11).
Les constats sont accumulés et joints à **une** assertion nommant la page, le fragment ou le marqueur attendu et le fichier de code (`dofus_stuff/web/routes.py:145` pour la carte, `dofus_stuff/optimize/api.py:433` pour les diagnostics). Le module doit écrire pourquoi : sur la fixture les diagnostics tombent sur la page **2/3**, alors que la même exécution sur la base réelle donne six puis sept pages — un contrôle qui exigerait la dernière page serait **faux sur une implémentation correcte**.

Ne pas documenter ici la saisie `SAVE`, la copie Dofusbook ni l'écran `/saves` (03-03), ni la ligne `Détail … base+parcho` (03-04). Ne pas citer de score, de méthode ni de total de pages.</action>
  <acceptance_criteria>
    - La section `## Lire le résultat` existe dans la page, avant `## Source de vérité`, et cite `PAGE n/total`, `ENTREE=VALIDER`, `F7=Page prec`, `F8=Page suiv`, `en fin de résultat`, la phrase du catalogue et `(vide)`.
    - Le contrôle rend réellement le résultat sur la fixture (un `POST` de l'étape 3 puis les `GET` de pages), exige la cohérence statut/attributs (`PAGE 1/<total>` contre `data-body-page`/`data-body-total`), atteint `PAGE <total>/<total>`, et vérifie la position des diagnostics sur les pages concaténées — sans jamais comparer le numéro de page au total.
    - La section ne contient ni `dernière page`, ni valeur volatile (score, indice, méthode, total) : les quatre motifs interdits ne s'y trouvent pas.
    - Les libellés de touches du résultat (`Page prec`, `Page suiv`) sont exigés, et ceux du wizard (`Precedent`, `Suivant`) sont exigés **absents** de ce rendu.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q` est vert, et le module reste sous ~1,5 s (un seul calcul de solveur sur la fixture, mesuré 0,29 s, puis des rendus à ~0,02 s).
    - Aucune assertion sur `data-stuff-payload` ni sur la réponse HTTP entière ; `.data/dofus.sqlite3` garde taille et `mtime_ns` après la tâche.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in carte diagnostics; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in carte) sed -i 's/PAGE {page}\/{total}/PAGEX {page}\/{total}/' "$T/dofus_stuff/web/routes.py"; motif='PAGE';; diagnostics) sed -i 's/lines.extend(diagnostics)/pass/' "$T/dofus_stuff/optimize/api.py"; motif='Méthode : ';; esac; out="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 03-02 EN ECHEC"; exit 1; fi; echo "morsures 03-02 (tache 1) : 2/2 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 03-02 EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>Le critère 2 est tenu pour la pagination et l'emplacement : la carte est lue dans la ligne de statut et cohérente avec les attributs de la coquille, la dernière page est atteinte, les diagnostics sont exigés en fin de résultat par leur position, et deux mutations du code (carte renommée, diagnostics retirés) rougissent le module sur une copie verte avant mutation.</done>
</task>

<task type="auto">
  <name>Tâche 2 : la table « libellé technique → nom complet » ancrée sur display_slots et sur les commentaires d'export</name>
  <files>docs/parcours-simplifie.md, tests/test_docs_parcours.py</files>
  <read_first>
    docs/parcours-simplifie.md
    tests/test_docs_parcours.py
    dofus_stuff/optimize/api.py
    dofus_stuff/web/dofusbook_export.py
    dofus_stuff/model/solver_spec.py
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-PATTERNS.md
  </read_first>
  <action>Écrire la seconde moitié du critère 2 : la correspondance entre le libellé technique affiché et le nom complet de l'emplacement — **reformulée par ÉCR-1** (dans ce flux, aucun libellé n'est tronqué : la largeur maximale d'une ligne rendue vaut exactement `COLS` = 100, les libellés sont complétés par `f"  {slot:8s} : "` et **jamais** coupés ; la règle de troncature `clip` existe, `dofus_stuff/web/screens.py:14-20`, mais ne se déclenche pas ici).

**Page** — insérer `## Correspondance des libellés` après `## Lire le résultat` et avant `## Source de vérité` : un tableau GFM à deux colonnes, en-tête en français (patron des tables de `docs/cli.md`), une ligne par emplacement, le libellé technique entre accents graves et le nom complet en clair :

| Libellé affiché | Emplacement |
|---|---|
| `amulet` | amulette (am) |
| `ring_a`, `ring_b` | anneaux (a1, a2) |
| `belt` | ceinture (ce) |
| `boots` | bottes (bo) |
| `hat` | coiffe (ch) |
| `cape` | cape (ca) |
| `weapon` | arme (ar) |
| `shield` | bouclier (br) |
| `dofus_1`, `dofus_2`, `dofus_3`, `dofus_4`, `dofus_5`, `dofus_6` | dofus |
| `pet` | familier/monture (fa) |
| `prysma` | prysmaradite |

Les noms complets ne sont **pas** écrits de mémoire : ils sont recopiés depuis la source qui les porte — les commentaires de `_GROUP_SLOTS` (`dofus_stuff/web/dofusbook_export.py:26-37` : `# 0 cape (ca)`, `# 1 coiffe (ch)`, `# 2 ceinture (ce)`, `# 3 bottes (bo)`, `# 4 amulette (am)`, `# 5 anneaux (a1, a2)`, `# 6 dofus`, `# 7 bouclier (br)`, `# 8 arme (ar)`, `# 9 familier/monture (fa)`) pour les seize emplacements exportés, et `dofus_stuff/model/solver_spec.py` (`"prysmaradite"`, également présent dans l'aide de `dofus_stuff/cli.py:119`) pour `prysma`. La page ajoute **une** phrase qui dit l'essentiel du lecteur : ces libellés sont des noms **techniques** affichés tels quels (ils ne sont pas raccourcis), et la correspondance ci-dessus est celle des emplacements du jeu ; les codes entre parenthèses (`ca`, `ch`, `ce`, `bo`, `am`, `a1`, `a2`, `br`, `ar`, `fa`) sont ceux des groupes d'import Dofusbook ; les six emplacements Dofus vont de `dofus_1` à `dofus_6` ; les six Dofus, le familier, la prysmaradite et le bouclier sont **omis de l'affichage quand ils sont vides** (mesure : `dofus_stuff/optimize/api.py:384-385`), les autres affichant `(vide)`. Elle ne doit **pas** reprendre la tournure « libellé tronqué » ni promettre une troncature qui n'existe pas.

**Module** — ajouter :
- `_libelles_display_slots()` : extraction de la table `display_slots` de `dofus_stuff/optimize/api.py` par `ast` (parcours des `ast.Assign`/`ast.AnnAssign` dont la cible s'appelle `display_slots`, valeur = `ast.Tuple` d'`ast.Constant` de type `str`), avec une garde : si l'extraction est vide ou ne rend pas un tuple de chaînes, l'échec nomme `dofus_stuff/optimize/api.py:362-380` et dit que la table a peut-être été renommée — jamais un `AttributeError` brut ;
- `SLOTS_MESURE` : la table épinglée des paires `(libellé, nom complet, fichier source, aiguille de ligne)` — l'`aiguille` est le fragment de ligne qui porte le nom dans la source (par exemple `"cape",` pour `dofus_stuff/web/dofusbook_export.py`, `"prysmaradite",` pour `dofus_stuff/model/solver_spec.py`). Chaque `nom_complet` est **recopié depuis la source citée** au moment de la rédaction (même patron que `LIBELLES_SOURCE` de `tests/test_docs_code_anchor.py:61-74`) ;
- `test_correspondance_libelles_slots(docs_dir, section, normalize)` :
  1. la vérité des libellés est `_libelles_display_slots()`, exigée non vide et de taille **17** (littéral mesuré ; si le produit ajoute ou retire un emplacement, c'est la page qui doit être mise à jour dans le même changement — la garde empêche aussi qu'une vérité vide rende le contrôle vert sans rien vérifier) ;
  2. `SLOTS_MESURE` doit couvrir exactement ces libellés (mêmes ensembles, taille 17) ;
  3. chaque entrée épinglée est **re-vérifiée dans sa source** : le fichier cité contient une ligne portant l'`aiguille`, et le `nom_complet` (normalisé) apparaît sur **cette ligne** (le libellé et son nom complet sur la même ligne, exactement ce que porte `_GROUP_SLOTS`). Les `nom_complet` sont **recopiés exactement** du commentaire qui les porte, codes Dofusbook inclus — `cape (ca)`, `coiffe (ch)`, `ceinture (ce)`, `bottes (bo)`, `amulette (am)`, `anneaux (a1, a2)`, `dofus`, `bouclier (br)`, `arme (ar)`, `familier/monture (fa)` — et jamais paraphrasés : « anneaux (les deux emplacements d'anneau) » ou « familier ou monture » ne seraient pas lisibles sur la ligne source et feraient rougir le contrôle sur une page pourtant censée ;
  4. les lignes du tableau de la section `## Correspondance des libellés` : chacun des **17** libellés de `display_slots` apparaît **exactement une fois** dans le tableau (les lignes du tableau, pas la prose ; une même ligne peut porter plusieurs libellés, comme les six `dofus_1` … `dofus_6`) ; tout libellé du tableau absent de `display_slots` est signalé comme inventé ; **un libellé de `display_slots` absent du tableau est signalé comme manquant** (l'absence ne doit pas passer en silence) ; chaque libellé apparaît sur la **même ligne** que son nom complet attendu (le nom manquant est signalé) ;
  5. côté page, la section ne contient ni la chaîne `libellé tronqué`, ni le caractère `…` — le contrôle ne doit **jamais** asserter l'apparition de `…` sur le rendu, et la page ne doit pas invoquer une troncature inexistante.
  Les constats sont accumulés et joints à **une** assertion nommant la page, la section, le libellé attendu, le nom complet attendu et le fichier source cité.

Ne pas ajouter au tableau un emplacement qui ne serait pas dans `display_slots` (trophées, monture séparée, objet MJ…) : le tableau décrit ce que l'outil affiche, et une ligne inventée est signalée. Ne pas toucher `dofus_stuff/`.</action>
  <acceptance_criteria>
    - La section `## Correspondance des libellés` contient un tableau dont les libellés sont **exactement** les 17 de `display_slots` (aucun inventé, aucun manquant), chacun sur la même ligne que son nom complet — les six `dofus_1` … `dofus_6` figurent **explicitement** (une même ligne peut les porter ensemble, `display_slots` les comptant pour six libellés distincts) et chacun porte le nom complet `dofus` recopié du commentaire de `_GROUP_SLOTS`.
    - Chaque nom complet cité est encore lisible dans la source citée, sur une ligne portant l'aiguille épinglée (commentaires de `dofus_stuff/web/dofusbook_export.py:26-37` pour les seize emplacements exportés, `dofus_stuff/model/solver_spec.py` pour `prysma`).
    - La section ne contient ni `libellé tronqué` ni `…`, et le module n'asserte nulle part l'apparition de `…` sur un rendu.
    - Retirer un libellé de `display_slots` (code), supprimer une ligne du tableau (page) ou renommer un nom complet dans le commentaire d'export (source) rougit le module en nommant le libellé, le nom attendu et le fichier.
    - `./.venv/Scripts/python.exe -m pytest -q` reste vert, module compris ; `.data/dofus.sqlite3` garde taille et `mtime_ns`.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in slots_page nom_source libelle_page code_slot; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in slots_page) sed -i '/amulette/d' "$T/docs/parcours-simplifie.md"; motif='amulet';; nom_source) sed -i 's/# 4 amulette (am)/# 4 emplacement (am)/' "$T/dofus_stuff/web/dofusbook_export.py"; motif='amulette';; libelle_page) sed -i 's/`amulet`/`amuletX`/' "$T/docs/parcours-simplifie.md"; motif='amuletX';; code_slot) sed -i 's/^        "prysma",$/        /' "$T/dofus_stuff/optimize/api.py"; motif='prysma';; esac; out="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 03-02 EN ECHEC"; exit 1; fi; echo "morsures 03-02 (tache 2) : 4/4 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 03-02 EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>Le critère 2 est tenu pour la correspondance des libellés : la page couvre exactement les emplacements que l'outil affiche, chaque nom complet est re-vérifié dans la source qui le porte, aucune troncature n'est invoquée, et quatre dérives (page, code, source) rougissent le module sur une copie verte avant mutation.</done>
</task>

</tasks>

<threat_model>
**Surface** : mêmes fichiers que 03-01 (une page `docs/`, un module de test), aucun serveur, aucun réseau, aucun secret, aucune dépendance ajoutée. Les menaces propres à ce plan sont celles des **valeurs non déterministes** et des **libellés inventés**.

| # | Menace | Mitigation | Résidu |
|---|--------|-----------|--------|
| T-7 | **Test instable** : épingler `Méthode`, un score ou le nombre de pages — la base réelle a donné deux méthodes et six puis sept pages pour la même demande | Toute lecture passe par la **fixture minimale** (déterminisme mesuré : même empreinte SHA-256 de charge utile sur trois exécutions) ; le total de pages est lu dans le rendu, jamais épinglé ; la position des diagnostics est exigée par **ordre relatif** | Un changement de générateur du solveur modifierait l'ordre, jamais le contrôle — c'est voulu |
| T-8 | **Faux négatif par la mauvaise fenêtre** : asserter sur `response.data` (la charge utile y porte tout le résultat, accents échappés) | Assertions limitées aux lignes du corps et à la ligne de statut ; garde de forme du plan 03-01 ; interdiction explicite dans les prohibitions du frontmatter | Aucun |
| T-9 | **Nom complet inventé** : écrire « amulette » de mémoire depuis la connaissance du jeu | Chaque nom complet est recopié depuis la source citée **et** re-vérifié sur la ligne qui le porte (`_GROUP_SLOTS`), avec l'aiguille épinglée | L'équivalence libellé technique ↔ mot affiché dans le jeu reste une lecture humaine, annoncée comme telle dans la page |
| T-10 | **Garantie fausse sur la troncature** : promettre au lecteur une troncature qui n'arrive jamais, ou asserter `…` sur un rendu correct | La page ne contient ni « libellé tronqué » ni `…` ; le module n'asserte jamais `…` sur un rendu ; la raison est écrite dans la page et dans le module (mesure M7) | Aucun : la mesure est rejouable |
| T-11 | **Écriture sous `.data/`** par un rendu de résultat (la fixture charge la base) | Aucun test n'écrit ; `mtime_ns` et taille comparés après la tâche | La base reste lue, pas modifiée |

**Hors périmètre** : publication, déploiement, synchronisation Dofusdude, commandes destructrices — aucun de ces gestes n'est nécessaire ici, et aucun n'est exécuté par les contrôles de ce plan.
</threat_model>

<verification>
**Contrôles automatisés** : `tests/test_docs_parcours.py` (V5, V6, V7), `tests/test_docs_structure.py` et `tests/test_docs_code_anchor.py` inchangés (V14 et la garde du socle), la suite complète, et six morsures sur copie verte avant mutation : carte de pagination renommée, diagnostics retirés du flux simplifié, ligne de tableau supprimée, nom complet renommé dans la source, libellé du tableau altéré, libellé retiré de `display_slots`.

**Non automatisable, nommé comme tel** : le lecteur doit reconnaître un mot du jeu (« coiffe » pour `hat`) — la correspondance est adossée au code mais son adéquation au vocabulaire du joueur reste une lecture humaine, écrite dans la page. Aucun blocage : rien ici ne dépend d'un geste humain ou d'un secret.

**Références de vérification de phase** : `03-VALIDATION.md` § Per-Task Verification Map assigne V5–V7 à ce plan ; `/gsd:verify-work` rejouera la suite et la mesure de non-régression `.data/`.
</verification>

<success_criteria>
- `## Lire le résultat` décrit la carte de pagination dans la ligne de statut, l'absence de carte sur un résultat d'une page, la barre `F7=Page prec`/`F8=Page suiv`, et l'emplacement **en fin de résultat** des trois diagnostics — jamais « dernière page » (critère 2, V5, V6, ÉCR-2).
- `## Correspondance des libellés` couvre exactement les 17 libellés de `display_slots`, chacun avec son nom complet re-vérifié dans la source citée, sans invoquer de troncature (critère 2, V7, ÉCR-1).
- Aucune valeur volatile n'est figée par la page, et aucun test n'asserte sur `data-stuff-payload` ou sur la réponse entière.
- Six dérives épinglées rougissent le module sur une copie verte avant mutation, et la page non mutée est verte.
- La suite complète est verte et `.data/dofus.sqlite3` est intact (taille + `mtime_ns`).
</success_criteria>

## Couverture des sources (ce plan)

| Source | Élément | Consommé par |
|--------|---------|--------------|
| ROADMAP § Phase 3 | Critère 2, première moitié (carte de pagination, emplacement réel de `Méthode`, `Score`, `Indice de recherche`) | tâche 1 (reformulée par ÉCR-2 : position, pas indice de page) |
| ROADMAP § Phase 3 | Critère 2, seconde moitié (`table de correspondance libellé tronqué → nom complet de slot`) | tâche 2 (reformulée par ÉCR-1 : libellé technique → nom complet) |
| REQUIREMENTS | SIMP-02 | `requirements` du plan |
| CONTEXT | D-32 (écrans web lus par le client de test Flask) | tâche 1 (rendu réel du résultat) |
| CONTEXT | D-38 (« Lire le résultat » est une section de cette page, rattachée à l'écran qui l'expose) | tâche 1 (titre épinglé `## Lire le résultat`) |
| CONTEXT | D-42 (aucune constante écrite de mémoire ; message citant page, valeur attendue, fichier) | tâches 1 et 2 |
| CONTEXT | D-35 (aucune sémantique inventée) | tâche 2 (noms complets recopiés de la source) |
| CONTEXT | D-41 (une limite non adossée est retirée ou présentée comme une interprétation) | tâche 2 (la correspondance est présentée comme une lecture, adossée au code) |
| CONTEXT | D-45 (conventions des phases 1-2 : normalisation D-11, helpers partagés D-12, message localisant D-13, API publique D-14, pas de `main()` ni d'écriture `.data/` D-15) | tâches 1 et 2 |
| RESEARCH | M6 (carte dans la ligne de statut, bloc de diagnostics en fin de résultat, barre de touches du résultat, total de pages variable, charge utile échappée) | tâche 1 |
| RESEARCH | M7 (aucune troncature, `display_slots`, 17 libellés, règle des emplacements vides) | tâche 2 (et `(vide)` en tâche 1) |
| RESEARCH | M9 (`_GROUP_SLOTS` et ses commentaires = noms complets lisibles dans le code) | tâche 2 |
| RESEARCH | M11 (fixture déterministe, base réelle non déterministe et coûteuse) | tâche 1 (aucune valeur volatile) |
| RESEARCH | Pitfalls 6 et 7 (non-déterminisme du score, `tojson` de la charge utile) | tâche 1 (interdiction de figer une valeur, interdiction d'asserter sur `response.data`) |
| PATTERNS | Patron `LIBELLES_SOURCE` (libellé + porteur sur la même ligne) | tâche 2 |
| PATTERNS | Gap 3 / ÉCR-1 (rien à analoguer : la troncature n'existe pas) | tâche 2 |
| VALIDATION | V5, V6, V7 | tâches 1 et 2 (V7 en tâche 2) |
| VALIDATION | V14, V15 | tâche 1 (mesure locale) et la suite complète |

**Lignes `unresolved` (assumées)** : SIMP-02 est une exigence documentaire, donc sans prédicat évaluable par la sonde d'arêtes ; la couverture d'arêtes de ce plan est écrite à la main depuis V5, V6 et V7 (voir `must_haves.assumptions`).

## Contributions et sondes

| Élément | État | Emplacement |
|---------|------|-------------|
| Composants, libs, capabilities, fichiers réservés | — | Absents du dépôt : rien à brancher ni à déclarer (même conclusion qu'en 03-01) |
| Tables canon | — | Ce plan n'écrit aucune ligne de table de suivi ; il lit les tables de la page qu'il complète |
| Sonde jetable | `.gsd-tmp/plan-p3/` si l'exécuteur en écrit une | Non suivie, non indexée, jamais supprimée ; les morsures travaillent dans un `mktemp -d` hors dépôt |

## Artifacts this phase produces (ce plan)

- **Créés** : les sections `## Lire le résultat` et `## Correspondance des libellés` de `docs/parcours-simplifie.md` (insérées avant `## Source de vérité`).
- **Créés dans `tests/test_docs_parcours.py`** : `_libelles_display_slots`, la table épinglée `SLOTS_MESURE`, et les tests `test_pagination_et_emplacement_du_calcul`, `test_correspondance_libelles_slots` (le helper `_touches` est posé par le plan 03-01 et seulement réutilisé ici — D-12 : un seul exemplaire).
- **Non modifiés** : `dofus_stuff/**`, `tests/conftest.py`, `tests/test_docs_structure.py`, `tests/test_docs_code_anchor.py`, `docs/sommaire.md`, `docs/cli.md`, `docs/installation.md`.

