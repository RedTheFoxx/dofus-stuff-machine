---
phase: 03-parcours-simplifi-document-depuis-le-rendu-r-el
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/parcours-simplifie.md
  - docs/sommaire.md
  - tests/test_docs_parcours.py
autonomous: true
requirements:
  - SIMP-01
estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Les trois questions et la ligne `AVANCE : personnaliser les réglages` sont exigées dans les **lignes du corps** rendues par le client de test Flask (GET /optimize/quick/classe puis deux POST valides) et citées par la page : `1/3 - Quelle est votre classe ?`, `2/3 - Quels éléments privilégier ?`, `3/3 - Quel est votre niveau ? (1 à 200)`, `AVANCE : personnaliser les réglages`. Retirer la ligne AVANCE (`dofus_stuff/web/routes.py:1003`) ou renommer une question rougit le module en nommant le libellé et le fichier de code (critère 1, SIMP-01, D-32, mesure M2, V1)."
    - "Chaque entrée citée par la page est rejouée sur le rendu et classée par ce que l'outil fait réellement : une entrée acceptée redirige (302) ou atteint l'appel d'optimisation, une entrée refusée re-rend le même écran en 200 avec le message exact au début de la ligne de statut. Sont couvertes les trois étapes, avec au moins un nom accentué, une casse différente, un numéro avec zéro de tête et les trois séparateurs d'éléments (`terre air`, `terre,air`, `terre+air`) (critère 1, SIMP-01, D-32, mesure M3, V2)."
    - "Les trois messages de refus sont exigés au début de la ligne de statut, avec leur texte exact : `Saisissez le nom ou le numéro de votre classe.`, `Exemple : feu, terre air, ou multi.`, `Saisissez un niveau entre 1 et 200.` Un renommage du message dans `dofus_stuff/web/routes.py` (lignes 964, 972 et 976) rougit en nommant le message attendu et le fichier (critère 1, D-13, mesure M3, V3)."
    - "La correspondance numéro ↔ libellé des deux menus est comparée **section par section** : le menu des classes est lu dans la section `## Question 1/3 : la classe` de la page et comparé aux couples réellement rendus par `dofus_stuff/web/routes.py:989` (19 couples, 1 → Cra … 19 → Zobal) ; le menu des éléments est lu dans `## Question 2/3 : les éléments` et comparé aux couples rendus par `dofus_stuff/web/routes.py:993` (1 → Terre, 2 → Feu, 3 → Eau, 4 → Air). Un libellé inversé (6 ↔ 9), un numéro décalé, une ligne supprimée dans la page, ou une permutation de `CLASSES` côté code rougissent le module en nommant la section, le couple attendu et `routes.py:989` (critère 5, D-34, mesures V4 et preuve de morsure du prototype)."
    - "Le sommaire et la page restent cohérents sans exception : `docs/sommaire.md` liste `parcours-simplifie.md`, le H1 unique de la page est égal (après normalisation) à son libellé d'index, et la ligne de retour vers le sommaire se résout. Ces trois propriétés sont prouvées par les tests de la phase 1, inchangés, exécutés par le même interpréteur (D-39, D-06, V14)."
    - "Le bloc « Source de vérité » de la page cite `dofus_stuff/web/routes.py` et `dofus_stuff/optimize/recommend.py`, et **tout** chemin `….(py|toml|js|md|json|sql)` écrit entre accents graves n'importe où dans la page existe sur disque depuis la racine du dépôt : un chemin inventé est nommé par l'échec (D-01, D-03, leçon WR-01)."
    - statement: "Le module d'ancrage n'importe du produit que sa surface publique, n'importe ni `dofus_stuff.database`, ni `sqlite3`, ni `subprocess`, ni `socket`, et n'exécute aucun `main()` : la propriété est vérifiée sur le texte du module par `ast` (démonstration indirecte, qui ne prouve pas l'absence de toute lecture de `.data/` par un autre chemin — c'est la mesure d'empreinte du plan 03-04 qui la prouve directement)."
      verification: backstop
    - statement: "Le parcours en ligne de commande ne pose pas les trois questions : la page l'affirme explicitement (la phrase épinglée `ne pose pas ces trois questions` est exigée dans la page, normalisée), et l'absence de toute invite de classe ou d'élément est vérifiée sur les **littéraux** de `dofus_stuff/optimize/profile_input.py` par `ast` (124 littéraux mesurés à la planification, aucun ne contient une classe ni un élément, même après normalisation accents/casse). Démonstration indirecte : le contrôle lit le texte du module, il n'exécute jamais le CLI (D-15) ; une invite composée ailleurs ou dynamiquement lui échapperait."
      verification: backstop
  artifacts:
    - path: "docs/parcours-simplifie.md"
      provides: "Page du parcours simplifié : H1, introduction, trois sections de questions avec les écrans rendus verbatim, entrées acceptées et refusées, messages d'erreur réels, bloc Source de vérité, ligne de retour"
      contains: "## Question 1/3 : la classe"
      min_lines: 40
    - path: "docs/sommaire.md"
      provides: "Index unique : une ligne de plus vers la nouvelle page, sans cible morte et sans doublon de libellé"
      contains: "[Parcours simplifié](parcours-simplifie.md)"
      min_lines: 21
    - path: "tests/test_docs_parcours.py"
      provides: "Module d'ancrage du parcours simplifié : extraction des lignes du corps et de la ligne de statut, couples numéro ↔ libellé par section, entrées rejouées sur le rendu, garde sans base ni exécution"
      contains: "def _lignes_du_corps("
      min_lines: 150
  key_links:
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/web/routes.py"
      via: "rendu réel des trois écrans par la fixture client (client.post(\"/optimize/quick/<etape>\", data={\"cmd\": …})), ensuite lu dans les lignes du corps et la ligne de statut"
      pattern: "/optimize/quick/"
    - from: "tests/test_docs_parcours.py"
      to: "docs/parcours-simplifie.md"
      via: "lecture UTF-8 de la page, puis comparaison par section (section(texte, TITRE_*, PAGE)) des couples cités et des entrées citées"
      pattern: "parcours-simplifie.md"
    - from: "docs/sommaire.md"
      to: "docs/parcours-simplifie.md"
      via: "ligne d'index dont la cible résolue est la nouvelle page, et dont le libellé normalisé égale le H1 de la page"
      pattern: "[Parcours simplifié](parcours-simplifie.md)"
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/optimize/profile_input.py"
      via: "lecture du module CLI et analyse de ses littéraux de chaîne par ast : le contrôle exige que la page affirme, avec la chaîne épinglée `ne pose pas ces trois questions`, ce que le module ne contient pas (aucune invite de classe ni d'élément)"
      pattern: "profile_input"
    - from: "tests/test_docs_parcours.py"
      to: "tests/conftest.py"
      via: "fixtures client, docs_dir, normalize, section et lignes_exemple consommées telles quelles, sans redéfinition locale (D-12)"
      pattern: "def section("
  prohibitions:
    - statement: "La page ne doit pas présenter comme un fait du produit un phénomène que le parcours simplifié ne produit pas : en particulier, elle ne doit pas laisser croire que le parcours en ligne de commande pose les trois mêmes questions, ni inventer un message, une entrée acceptée ou un libellé d'écran."
    - statement: "La page ne doit pas devenir une seconde source : elle ne recopie pas la surface de commandes de `docs/cli.md` (aucune ligne de commande dans un bloc balisé `console`) et n'ajoute aucun lien externe."
    - statement: "Le harnais ne doit pas pouvoir passer sans lire ce que le lecteur voit : aucune assertion ne porte sur la réponse HTTP entière ni sur la charge utile `data-stuff-payload`, et aucune valeur volatile (score, méthode, total de pages) n'est épinglée dans un test."
  assumptions:
    - "Exigence **documentaire** : la sonde d'arêtes de la chaîne GSD classe des formes de code, pas une exigence de rédaction — SIMP-01 n'a donc pas de prédicat à évaluer, et son classement retombe en « à revoir à la main ». Décision enregistrée : la couverture d'arêtes de ce plan est **écrite à la main** depuis les vérités V1, V2, V3, V4 et V14 de `03-VALIDATION.md`, chacune avec son contrôle falsifiable ; cette ligne est conservée telle quelle, jamais absorbée en silence."
    - "Le libellé du H1 de la page n'est pas falsifiable : il est libre (Claude's Discretion). Seule la correspondance H1 ↔ libellé d'index l'est, et c'est elle qui est testée (limite mesurée écrite en `03-RESEARCH.md` § « Vérités non falsifiables »)."
    - "Les probes jetables de la recherche (`.gsd-tmp/research-p3/`) restent sur disque, non suivis et non indexés : le porteur du projet interdit toute suppression de données, donc l'hypothèse A1 de la recherche (les supprimer avant la fin de la phase) est **écartée** au profit de « ne rien ajouter d'autre sous `.gsd-tmp/` et ne jamais indexer ce dossier »."
---

<objective>
**Cadrage dérivé (non une décision produit nouvelle)** — repris du `**Goal:**` de la phase 3 dans `.planning/ROADMAP.md` (« Un lecteur peut dérouler le flux simplifié classe → éléments → niveau, lire son résultat et comprendre ce que l'outil suppose. »), de ses critères de succès 1 et 5 et des décisions D-32 à D-45 de `03-CONTEXT.md` :
**En tant que** lecteur qui découvre l'interface web, **je veux** dérouler les trois questions du parcours simplifié en sachant exactement ce que je peux taper et ce que l'outil me répondra en cas d'erreur, **afin de** ne pas deviner la saisie attendue et ne pas croire que le parcours en ligne de commande pose les mêmes questions.

Ouvrir la page `docs/parcours-simplifie.md` et son module d'ancrage : la première tranche verticale rend réellement les trois écrans par le client de test Flask, cite leurs libellés tels qu'ils s'affichent, éprouve chaque entrée citée sur le rendu, exige les trois messages de refus au début de la ligne de statut, et compare **section par section** les couples numéro ↔ libellé des deux menus rendus.

Purpose : c'est SIMP-01 et le critère 5. La recherche a mesuré trois pièges que ce plan évite par construction : `assert "Méthode" in response.data` (ou toute assertion sur la réponse entière) est **faux sur une page correcte**, parce que `data-stuff-payload` porte tout le résultat sur **chaque** page et que `tojson` y échappe les accents (§ M6, Pitfall 2) ; un dictionnaire `numéro → libellé` **global** rapporte quatre constats sur une page correcte, parce que les numéros 1–4 existent dans les deux menus (§ M7, Pitfall 1) ; et un refus de saisie n'est **pas** un code d'erreur — la vue re-rend le même écran en 200 avec le message dans la ligne de statut (§ M3, Pitfall 4).

Output : `docs/parcours-simplifie.md` (créée : en-tête du gabarit D-01, trois sections de questions, bloc « Source de vérité », ligne de retour), `docs/sommaire.md` (une ligne d'index ajoutée dans la table), `tests/test_docs_parcours.py` (créé : extraction des lignes du corps et de la ligne de statut, entrées rejouées, couples par section, garde sans base ni exécution).

Ordonnancement déclaré : **vague 1**, sans dépendance, et **tranche verticale en tête de plan** (tâche 1, `type="tracer"`) : les quatre fichiers du parcours simplifié sont touchés de bout en bout avant toute expansion. Ce plan est la tête de la phase ; les plans 03-02, 03-03 et 03-04 **étendent la même page et le même module** et ne peuvent donc pas s'exécuter en parallèle (chevauchement de fichiers ⇒ vagues successives 2, 3 et 4).

**Ce que ce plan ne fait pas** (frontières D-37, D-43, D-44) : il ne documente aucune commande (`docs/cli.md` en reste propriétaire), aucun écran du wizard avancé (phase 4), aucun fonctionnement de la base locale (phase 5), aucun message de dépannage ni terme de glossaire (phase 6). Les renvois vers les pages non encore livrées se font **en prose, sans lien** ; le seul lien sortant du parcours simplifié est le lien relatif vers `cli.md`, qui existe déjà.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-CONTEXT.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-PATTERNS.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-VALIDATION.md
@.planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-REVIEW.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
@docs/installation.md
@docs/sommaire.md
@dofus_stuff/web/routes.py
@dofus_stuff/optimize/recommend.py
@tests/conftest.py
@tests/test_recommend.py
@tests/test_docs_code_anchor.py
</context>
<tasks>

<task type="tracer">
  <name>Tâche 1 (tranchant) : la page s'ouvre et les trois questions sont citées depuis le rendu réel</name>
  <precondition>Le dépôt est à l'état livré de la phase 2 : `.venv/Scripts/python.exe -m pytest -q` est vert (mesuré pendant la planification : 169 passed in 2.57s) et `docs/parcours-simplifie.md` n'existe pas encore (`docs/` contient `installation.md`, `cli.md`, `sommaire.md`).</precondition>
  <files>docs/parcours-simplifie.md, docs/sommaire.md, tests/test_docs_parcours.py</files>
  <read_first>
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-CONTEXT.md
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-PATTERNS.md
    docs/installation.md
    docs/sommaire.md
    dofus_stuff/web/routes.py
    dofus_stuff/web/templates/screen.html
    tests/conftest.py
    tests/test_recommend.py
  </read_first>
  <action>Écrire la tranche verticale complète du parcours simplifié : la page, son entrée d'index et le module d'ancrage qui rend réellement les trois écrans. C'est la tranche la plus mince qui traverse toutes les couches de la phase (page `docs/` → index → module de test → vue Flask → fichier source cité), et elle est écrite pour de bon : aucune ébauche jetable.

**1. `docs/parcours-simplifie.md`** (nouveau fichier, **CRLF** et UTF-8 sans BOM comme tout `docs/`, mesure M13 ; leçon : chaque exécuteur précédent s'y est fait prendre). Gabarit D-01 :
- H1 unique **`# Parcours simplifié`** — le libellé retenu doit être **exactement** celui de la ligne d'index (comparaison normalisée accents/casse par `problemes_h1`) ; `Installation` et `CLI` sont déjà pris, `parcours simplifie` est libre ;
- un premier paragraphe d'introduction (une à deux phrases : cette page suit les trois questions du parcours simplifié de l'interface web) puis un **second** paragraphe qui borne la page : ses affirmations viennent du rendu réel de la vue web (D-36) ; la surface des commandes vit dans `docs/cli.md` (renvoi par un **lien relatif** `[la page CLI](cli.md)` — cible existante, résolue par les tests de la phase 1, D-37) ; le wizard avancé et la base locale sont renvoyés **en prose, sans lien** tant que leurs pages n'existent pas (D-43, D-44) ; et il porte la phrase mesurée qui évite une croyance fausse : le parcours en ligne de commande **ne pose pas ces trois questions** (la classe et les éléments n'existent que dans le flux web — mesure : aucune invite de classe ni d'élément dans `dofus_stuff/optimize/profile_input.py`, 124 littéraux, zéro occurrence du mot entier `classe` ou `element`), l'énoncé épinglé étant la chaîne `ne pose pas ces trois questions` ;
- la page est construite sur un **contrat de titres** épinglé pour toute la phase (chaque titre est écrit au caractère près : le module les épinlera littéralement et le helper `section` ne normalise pas un titre) :
  - écrits par **ce plan** : `## Question 1/3 : la classe`, `## Question 2/3 : les éléments`, `## Question 3/3 : le niveau`, puis `## Source de vérité` (dernière section) ;
  - écrits par **03-02** : `## Lire le résultat` (titre imposé par D-38) et `## Correspondance des libellés` ;
  - écrit par **03-03** : `## Sauvegarder et exporter` (titre imposé par D-38) ;
  - écrits par **03-04** : `## Ce que l'outil suppose` et `## Ce que l'outil ne fait pas` ;
  - **ordre** : les plans suivants insèrent leurs sections **avant** `## Source de vérité` ; la ligne de retour `[Retour au sommaire](sommaire.md)` reste la **toute dernière ligne** de la page (gabarit D-01), et aucun plan ne la déplace ; le module de ce plan exige déjà cette dernière ligne ;
- dans chaque section, une phrase courte en français puis un bloc **```text** contenant les lignes **telles qu'elles sont rendues** (un écran d'interface est une sortie, donc la balise `text` — même règle que `docs/installation.md:111`) : `VOTRE STUFF EN 3 CHOIX`, la question (`1/3 - Quelle est votre classe ?`, `2/3 - Quels éléments privilégier ?`, `3/3 - Quel est votre niveau ? (1 à 200)`), le menu de classes sur ses **sept lignes de trois couples** et le menu d'éléments (`1. Terre    2. Feu    3. Eau    4. Air`), les lignes d'indication de l'étape 2 (`Un ou plusieurs : feu / terre air / 1 3 / multi`, `Le multi valorise aussi votre élément le plus faible.`) et de l'étape 3 (`ENTREE lance la recherche de votre équipement.`, `PA/PM et vitalité sont pris en compte selon le niveau.`, `Points répartis automatiquement, sans exo ni parchemins.`, `Jets moyens ; préférences de classe ajustables après calcul.` — ces quatre lignes de l'étape 3 sont **verbatim** `dofus_stuff/web/routes.py:999-1002` — et la ligne `AVANCE : personnaliser les réglages` (`dofus_stuff/web/routes.py:1003`). Les couples **et leur numéro** sont ceux mesurés (§ M2) : 1 Cra, 2 Ecaflip, 3 Eliotrope, 4 Eniripsa, 5 Enutrof, 6 Feca, 7 Forgelance, 8 Huppermage, 9 Iop, 10 Osamodas, 11 Ouginak, 12 Pandawa, 13 Roublard, 14 Sacrieur, 15 Sadida, 16 Sram, 17 Steamer, 18 Xelor, 19 Zobal. La rédaction se fait depuis le rendu, pas depuis cette liste : exécuter la sonde de rendu, copier les lignes, puis vérifier que le résultat est identique ;
- une sous-section `### Passer aux réglages détaillés` dans la section 3 : `AVANCE` accepte les mêmes orthographes que les autres saisies (`AVANCE`, `avance`, entouré d'espaces), il est **rendu sur les trois étapes** (mesure : la ligne est ajoutée sans condition, `routes.py:1003`) et il ouvre les **réglages détaillés** — la page dit cela **sans décrire les écrans du wizard** (frontière D-43) ;
- `## Source de vérité` (dernière section avant la ligne de retour) : huit puces nommant `dofus_stuff/web/routes.py` (écrans et libellés des trois questions), `dofus_stuff/optimize/recommend.py` (liste des classes, paliers PA/PM, heuristiques de classe), `dofus_stuff/optimize/api.py` (lignes du résultat et emplacements affichés), `dofus_stuff/model/solver_spec.py` (capital de points par niveau), `dofus_stuff/optimize/score.py` (« indice de recherche »), `dofus_stuff/optimize/candidates.py` (sélection du catalogue), `dofus_stuff/web/static/js/terminal.js` (sauvegardes du navigateur), `dofus_stuff/web/dofusbook_export.py` (export Dofusbook). Ces huit chemins existent (vérifié par `git ls-files`) et le contrôle du module exige que **tout** chemin `….(py|toml|js|md|json|sql)` cité entre accents graves **n'importe où dans la page** existe sur disque (leçon WR-01 : un motif limité au bloc Source de vérité laisse échapper les chemins cités ailleurs) ;
- dernière ligne : `[Retour au sommaire](sommaire.md)`.

**2. `docs/sommaire.md`** : ajouter **une** ligne dans la table `## Index`, entre `Installation` et `CLI` (le parcours conseillé, lui, cite déjà « Parcours simplifié » en prose numérotée : ne pas y toucher) : `| [Parcours simplifié](parcours-simplifie.md) | Obtenir un stuff en 3 questions, lire puis sauvegarder le résultat |`. Ne rien changer d'autre (H1, introduction, liste numérotée, CRLF).

**3. `tests/test_docs_parcours.py`** (nouveau module, CRLF et UTF-8 sans BOM, docstring en français, identifiants en ASCII, messages d'échec en français — suivre la forme de `tests/test_docs_cli.py`). Docstring de tête disant ce qui est exclu : aucun serveur, aucun socket, aucun `main()`, aucune écriture sous `.data/`, aucune introspection privée (D-14, D-15) ; et que l'ancrage dépend de la **fixture `client`** (D-32). Contenu de cette tâche :
- constantes : `RACINE_DEPOT = Path(__file__).resolve().parents[1]`, `PAGE = "parcours-simplifie.md"`, `SOMMAIRE = "sommaire.md"`, `SOURCE_ROUTES = "dofus_stuff/web/routes.py"`, `SOURCE_RECOMMEND = "dofus_stuff/optimize/recommend.py"`, `SOURCE_API = "dofus_stuff/optimize/api.py"`, `SOURCE_JS = "dofus_stuff/web/static/js/terminal.js"`, `SOURCE_DOFUSBOOK = "dofus_stuff/web/dofusbook_export.py"`, `SOURCE_SPEC = "dofus_stuff/model/solver_spec.py"`, `SOURCE_SCORE = "dofus_stuff/optimize/score.py"`, `SOURCE_CANDIDATES = "dofus_stuff/optimize/candidates.py"`, les quatre titres de section épinglés (`TITRE_SOURCE = "## Source de vérité"`, `TITRE_CLASSE = "## Question 1/3 : la classe"`, `TITRE_ELEMENTS = "## Question 2/3 : les éléments"`, `TITRE_NIVEAU = "## Question 3/3 : le niveau"`), `TITRE_RESULTAT`, `TITRE_SAUVEGARDE`, `TITRE_SUPPOSE`, `TITRE_LIMITES` (les quatre derniers sont posés dès maintenant, ils seront consommés par les plans 03-02 à 03-04), `CHEMIN_CITE` (le motif de `tests/test_docs_code_anchor.py:25`, réutilisé tel quel) et `MOTIF_COUPLE_RENDU` (mesuré et éprouvé : `(?P<numero>\d{1,2})\.\s+(?P<libelle>[A-Za-zÀ-ÿ][A-Za-zÀ-ÿ' -]*)`) ;
- helpers **locaux au module** : `_texte_page(docs_dir)` (lit `docs_dir / PAGE` en `encoding="utf-8"` et, si la page est absente, lève une `AssertionError` nommant la page, le chemin attendu et `SOURCE_ROUTES` — jamais un `FileNotFoundError` brut, leçon IN-03 ; le module **ne promeut rien** dans `tests/conftest.py` (D-12, mesure M15 : les helpers utiles y sont déjà, un helper neuf dupliquerait `_texte_page` de `tests/test_docs_cli.py`)) ; `_lignes_du_corps(reponse)` (les lignes visibles du corps : couper sur `id="body">` puis sur `<div class="row status`, puis extraire les `<div class="row">…</div>` — les autres lignes du gabarit portent une classe et ne matchent pas, vérifié dans `dofus_stuff/web/templates/screen.html:31-35,49`) ; `_statut(reponse)` (le texte de `<div class="row status …">`, qui est la **seule** source des messages d'erreur et de la carte de pagination) ; `_couples_de_section(titre)` puis `_couples_du_rendu(lignes)` ; `_client_etape(app, etape)` qui rend un client **neuf** (`app.test_client()`) dont la session porte l'état minimal de l'étape (`{}` pour `classe`, `{"classe": "Cra"}` pour `elements`, `{"classe": "Cra", "elements": ["terre"]}` pour `niveau`, écrits par `client.session_transaction()` — l'API de test publique déjà employée en `tests/test_web.py:553`) ;
- `test_trois_questions_et_avance_rendus(docs_dir, app, normalize)`, **un seul point d'assertion** avec constats accumulés (forme mesurée en phase 2 : une assertion par constat rendait des motifs inatteignables) : rendre l'écran 1 par `GET /optimize/quick/classe`, puis l'écran 2 et l'écran 3 en rejouant le flux (`POST classe=Cra` → `GET /optimize/quick/elements` ; `POST elements=terre` → `GET /optimize/quick/niveau`), exiger dans les **lignes du corps** de chaque écran le libellé de sa question, exiger `AVANCE : personnaliser les réglages` dans les **trois** corps (mesure : la ligne est inconditionnelle), exiger le champ de saisie `CHOIX` et la barre `ESC=Retour`, et exiger les mêmes libellés cités par la section correspondante de la page. Les constats sont joints à une seule assertion nommant la page, le libellé attendu et `SOURCE_ROUTES` (D-13) ;
- `test_lignes_du_corps_ne_sont_pas_la_reponse_entiere(app)` : rendre un écran de question, exiger que `_lignes_du_corps` rende entre 1 et 18 lignes (les écrans de questions tiennent sur une page, mesure `data-body-total="1"`) et que la réponse HTTP brute soit plus longue que la concaténation des lignes du corps — c'est la garde qui rend impossible l'assertion paresseuse sur `response.data` (Pitfall 2) ;
- `test_source_de_verite_et_chemins_cites(docs_dir, section)` : la section `## Source de vérité` existe et cite au moins un chemin ; chaque chemin entre accents graves terminant par `.py`, `.toml`, `.js`, `.md`, `.json` ou `.sql`, **dans la page entière**, existe sur disque depuis `RACINE_DEPOT`, les manquants étant nommés ;
- `test_sans_base_locale_ni_execution()` : la garde statique de la propriété — analyser **le texte de ce module** par `ast` et exiger qu'aucun import ne vienne de `dofus_stuff` hors `dofus_stuff.web.dofusbook_export` (surface publique, pure, introduite par le plan 03-03), qu'aucun import ne soit `sqlite3`, `subprocess` ou `socket`, et qu'aucun appel à `main` n'existe. La propriété est vérifiée par `ast`, jamais par recherche de chaînes : le module cite lui-même ces noms dans son message d'échec (patron `tests/test_docs_cli.py:993-1057`), et l'allow-list est **propre à ce module** (elle n'élargit pas celle de `test_docs_cli.py`). Écrire dans le module que cette garde est une démonstration **indirecte** : la preuve directe qu'aucune écriture n'a lieu sous `.data/` est la mesure d'empreinte du plan 03-04.

Ne pas écrire de contenu que ce plan ne peut pas prouver : ni commande, ni écran du wizard, ni valeur de résultat. Ne pas ajouter de lien externe (`dofusbook.net` sera cité comme chaîne, jamais comme lien : plan 03-03). Ne pas modifier `tests/conftest.py`, `tests/test_docs_structure.py`, `tests/test_docs_code_anchor.py`, `tests/test_docs_cli.py` ni quoi que ce soit sous `dofus_stuff/`.</action>
  <acceptance_criteria>
    - `docs/parcours-simplifie.md` existe, commence par `# Parcours simplifié` (H1 unique) et se termine par `[Retour au sommaire](sommaire.md)` ; ses trois sections `## Question 1/3 : la classe`, `## Question 2/3 : les éléments`, `## Question 3/3 : le niveau` et sa section `## Source de vérité` portent **exactement** ces titres.
    - Les lignes du corps rendues par la vue sont **identiques** à celles citées par la page pour les trois écrans : le test les compare libellé par libellé (normalisation D-11) et nomme `routes.py` en cas d'écart ; `AVANCE : personnaliser les réglages` est exigé dans les trois corps.
    - `docs/sommaire.md` liste la page, le H1 de la page est égal (normalisé) à son libellé d'index et la ligne de retour résout : `./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q` reste vert **sans exception ni liste blanche** (D-06, D-39).
    - Les trois menus de classes (19 couples) et d'éléments (4 couples) de la page sont les couples **rendus** : les numéros 1 à 19 et 1 à 4 sont présents une fois chacun, et la comparaison par section du module le vérifie (le test dédié arrive en tâche 3 ; cette tâche livre la page qui les porte).
    - Chaque chemin entre accents graves de la page existe sur disque, et `tests/test_docs_parcours.py` ne définit aucun helper déjà exposé par `tests/conftest.py` (grep : une seule définition de `_lignes_de_code`, `_section` et `_normalize` dans `tests/`).
    - `tests/test_docs_parcours.py` contient la garde statique `ast` et elle passe ; aucun import de `dofus_stuff.database`.
    - Les deux fichiers écrits sont en CRLF, UTF-8 sans BOM (vérification par comptage d'octets, comme la mesure M13), et aucun fichier de `.data/` n'est touché : `mtime_ns` et taille de `.data/dofus.sqlite3` identiques après la tâche (mesurés à la planification : 24 989 696 octets, `mtime_ns` 1788730056843137500).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -20; exit 1; fi; (cd "$T" && sed -i 's/AVANCE : personnaliser les réglages/RECUL : personnaliser les réglages/' dofus_stuff/web/routes.py); out="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF 'AVANCE : personnaliser les réglages'; then echo "mutation detectee : libelle AVANCE renomme dans le code (copie verte avant mutation)"; else echo "MUTATION NON DETECTEE : le libelle renomme dans le code ne fait plus rougir le module"; printf '%s\n' "$out" | tail -30; exit 1; fi</automated>
    <fails_when>exit non-zero, ou la ligne "MUTATION NON DETECTEE : le libelle renomme dans le code ne fait plus rougir le module", ou la ligne "COPIE ROUGE AVANT MUTATION"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>La tranche verticale est réelle et prouvée : la page existe, l'index la liste, les trois écrans sont rendus par le client de test Flask, les libellés cités sont ceux du rendu (muté côté code, le module rougit en nommant `AVANCE`), les chemins cités existent, la garde sans base ni exécution passe, et la suite complète reste verte.</done>
</task>

<task type="auto">
  <name>Tâche 2 : les entrées acceptées et les refus réels, rejoués sur le rendu</name>
  <files>docs/parcours-simplifie.md, tests/test_docs_parcours.py</files>
  <read_first>
    docs/parcours-simplifie.md
    tests/test_docs_parcours.py
    dofus_stuff/web/routes.py
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-PATTERNS.md
  </read_first>
  <action>Étendre la page et le module avec la matrice d'entrées **mesurée** (critère 1). La règle de fond : la page est la source des valeurs rejouées, et chaque valeur citée est classée par ce que l'outil fait vraiment — le refus **n'est pas** un code d'erreur (mesure M3, Pitfall 4).

**Page** : dans chaque section de question, ajouter deux sous-sections courtes, `### Entrées acceptées` et `### Erreurs et refus`, construites sur des **lignes de tableau** (le contrôle lit les lignes de tableau, jamais la prose : leçon WR-01) :
- `## Question 1/3 : la classe` : acceptées `Cra`, `crâ`, `1`, `19`, `019`, ` 3 ` avec, pour chacune, l'effet observé (passe à l'étape 2) ; et l'explication mesurée : la comparaison ignore **accents et casse** (égalité après normalisation), un **numéro** de 1 à 19 est accepté (zéro de tête admis), les espaces autour sont retirés, et **le début du nom ne suffit pas** (`eliotrop` est refusé) — c'est une égalité, pas un préfixe (Pitfall 8). Refus cités : `eliotrop`, `0`, `20`, chacun avec le message exact.
- `## Question 2/3 : les éléments` : acceptées `terre`, `terre air`, `terre,air`, `terre+air`, `1 3`, `multi` ; et l'explication mesurée : plusieurs éléments se séparent par **espace, virgule ou plus** (mélangeables), les chiffres `1 2 3 4` désignent Terre, Feu, Eau, Air, `multi` **seul** vaut les quatre éléments, les doublons sont fusionnés. Refus cités : `multi terre`, `arbre`, `5`.
- `## Question 3/3 : le niveau` : acceptées `150`, `050`, `200` (un entier de 1 à 200, zéro de tête admis) et `AVANCE` ; refus cités : `201`, `0`, `50.0`.
- Pour les six refus cités, la page cite le message **au mot près** dans la sous-section `### Erreurs et refus` de la section concernée : `Saisissez le nom ou le numéro de votre classe.`, `Exemple : feu, terre air, ou multi.`, `Saisissez un niveau entre 1 et 200.` ; et elle dit **où** le lecteur le lit : dans la **ligne de statut**, en bas de l'écran, qui reste l'écran courant (accompagnée de `ENTREE=SUIVANT` aux étapes 1 et 2, `ENTREE=CALCULER` à l'étape 3).

**Module** : ajouter la table épinglée `ENTREES_MESUREES`, liste de couples `(etape, valeur, attendu)` où `attendu` vaut soit `("redirige", "<cible>")` soit `("refuse", "<début du message>")` soit `("optimisation", niveau_attendu)` pour les niveaux acceptés, et le test `test_entrees_citees_acceptees_et_refusees(docs_dir, app, section)` :
- pour chaque couple, obtenir un client **neuf** par `_client_etape(app, etape)` (le client neuf est indispensable : un `POST` accepté modifie l'état de session et fausserait le verdict suivant) ;
- rejouer `POST /optimize/quick/<etape>` avec `data={"cmd": valeur}` : accepter un `302` dont le `Location` mène à l'étape attendue (`/optimize/quick/elements`, `/optimize/quick/niveau`, `/optimize/result`) ou à `/optimize/wizard/recap` pour `AVANCE` ; accepter un refus sous la forme `200` **et** `_statut(reponse).startswith(message attendu)` ; pour les niveaux acceptés `150`, `050` et `200`, court-circuiter le solveur par `patch("dofus_stuff.web.routes._run_optimize_and_redirect", return_value="computed")` (patron mesuré `tests/test_recommend.py:85-92`), exiger la sentinelle puis relire `session["recommendation_input"]["niveau"]` pour prouver l'entier retenu (mesure : `050` vaut 50) ; l'assertion finale est unique et joint les constats (page, valeur, attendu, fichier de code) ;
- direction inverse, limitée aux **lignes de tableau** de chaque section : chaque valeur citée par la table d'entrées acceptées de la section appartient à la liste épinglée des valeurs acceptées de cette étape, et chaque valeur citée par la table des refus appartient à la liste épinglée des refus — une valeur inventée par la page est nommée. Les listes épinglées ne sont **pas** vides (garde-fou : une règle sans objet est infalsifiable) ;
- les couples épinglés, eux, sont rejoués sur le rendu : c'est le rendu qui décide du verdict, jamais la page.

Écrire dans le module la limite mesurée de ce contrôle : le `maxlength="40"` du champ est une contrainte **du navigateur** ; `client.post` l'ignore, et le serveur n'a pas de borne de longueur propre — la page ne doit donc pas présenter 40 caractères comme une protection du serveur (V5 de la section sécurité de la recherche). Ne pas inventer d'entrée que la mesure n'a pas produite, ne pas citer de valeur de résultat, ne pas exécuter `main()` ni le solveur (sauf le `patch` ci-dessus).</action>
  <acceptance_criteria>
    - Les entrées acceptées épinglées (six pour la classe : `Cra`, `crâ`, `1`, `19`, `019`, ` 3 ` ; six pour les éléments : `terre`, `terre air`, `terre,air`, `terre+air`, `1 3`, `multi` ; trois niveaux : `150`, `050`, `200`) plus `AVANCE` sont rejouées sur le rendu et **acceptées** : le verdict est lu sur la réponse HTTP (`302` vers l'étape attendue, ou sentinelle d'optimisation atteinte pour les niveaux), jamais sur le texte de la page.
    - Les neuf refus épinglés (`eliotrop`, `0`, `20`, `multi terre`, `arbre`, `5`, `201`, `0`, `50.0`) re-rendent le même écran en `200` et la ligne de statut commence par le message exact attendu ; un message reformulé dans `dofus_stuff/web/routes.py` (lignes 964, 972, 976) rougit en nommant le message attendu.
    - Chaque valeur citée par une table de la page appartient à la liste épinglée de son étape (aucune valeur inventée ne passe), et les tables des sections citent toutes les valeurs épinglées de leur étape.
    - Le test n'assert que sur les lignes du corps et la ligne de statut : aucune assertion ne porte sur `response.data` (garde de forme vérifiée en tâche 1).
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q` est vert et le module reste sous ~1 s (budget mesuré de la phase : les rendus de questions ne touchent pas le catalogue, seul le `patch` évite le solveur).
    - Aucun accès à `.data/`, aucun socket, aucun sous-processus : `mtime_ns` et taille de `.data/dofus.sqlite3` inchangés après la tâche.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -20; exit 1; fi; (cd "$T" && sed -i 's/Saisissez le nom ou le numéro de votre classe./Choisissez une classe./' dofus_stuff/web/routes.py); out="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF 'Saisissez le nom ou le numéro de votre classe.'; then echo "mutation detectee : message de refus classe reformule dans le code (copie verte avant mutation)"; else echo "MUTATION NON DETECTEE : le message reformule dans le code ne fait plus rougir le module"; printf '%s\n' "$out" | tail -30; exit 1; fi</automated>
    <fails_when>exit non-zero, ou la ligne "MUTATION NON DETECTEE : le message reformule dans le code ne fait plus rougir le module", ou "COPIE ROUGE AVANT MUTATION"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>SIMP-01 est tenu pour les entrées : chaque valeur citée par la page est acceptée ou refusée par le rendu, les trois messages de refus sont exigés à la ligne de statut, une valeur inventée par la page est nommée, et un renommage du message dans le code rougit le module (morsure mesurée sur une copie verte avant mutation).</done>
</task>
<task type="auto">
  <name>Tâche 3 : les couples numéro ↔ libellé comparés section par section, avec messages localisants et morsures éprouvées</name>
  <files>docs/parcours-simplifie.md, tests/test_docs_parcours.py</files>
  <read_first>
    docs/parcours-simplifie.md
    tests/test_docs_parcours.py
    dofus_stuff/web/routes.py
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-PATTERNS.md
  </read_first>
  <action>Poser le contrôle du critère 5 (D-34, mesure M2 et preuve de morsure de la recherche) : le couple **numéro ↔ libellé** des deux menus est extrait du **rendu** et comparé à ce que la page cite, **section par section**.

**Page** : les deux menus apparaissent **une seule fois chacun**, dans le bloc `text` de leur section (`## Question 1/3 : la classe` : les sept lignes de trois couples telles qu'elles sortent du rendu, `{j+1:2}. {CLASSES[j]:12}` joint par quatre espaces ; `## Question 2/3 : les éléments` : `1. Terre    2. Feu    3. Eau    4. Air`). Aucun second exemplaire des couples ailleurs dans la page (une table récapitulative des 19 classes ferait deux vérités à tenir) ; les espaces de remplissage font partie de la ligne rendue et sont recopiés, la comparaison les ignore (`strip()`).

**Module** : ajouter
- `_couples_de_section(texte, titre, section)` — les couples cités par **une** section (`section(texte, titre, PAGE)` puis `finditer` de `MOTIF_COUPLE_RENDU` sur les seules lignes de la section) ; rend un dictionnaire `numero → liste des libellés trouvés` (une liste, jamais une valeur unique : un numéro cité deux fois avec deux libellés différents doit être visible) ;
- `_couples_du_rendu(lignes)` — les couples **rendus**, même motif, appliqué aux lignes du corps ;
- `test_couples_numeros_libelles_par_section(docs_dir, app, section, normalize)` — la vérité vient du rendu : `GET /optimize/quick/classe` pour le menu des classes (`dofus_stuff/web/routes.py:989`) et, pour le menu des éléments, l'écran obtenu après un `POST classe=Cra` (`dofus_stuff/web/routes.py:993`). Les deux ensembles de couples rendus sont **non vides et de taille attendue** (19 pour les classes, 4 pour les éléments — littéraux mesurés, et si le produit grandit c'est la page qui doit être mise à jour dans le même changement : la garde `len(...) == 19` existe pour qu'une vérité vide ne fasse pas passer le contrôle en silence). Puis, pour chaque menu, comparer la vérité rendue aux couples de la section correspondante et accumuler **tous** les constats avant une **assertion unique** :
  - numéro rendu absent de la page → `aucun libellé` ;
  - libellé différent → constat nommant le libellé de la page et le couple attendu ;
  - numéro cité par la page qui n'existe dans **aucun** menu rendu → constat nommant les deux lignes de code ;
  - numéro cité deux fois dans la même section avec deux libellés différents → constat listant les deux.
- forme **exacte** des messages (D-13, D-42, reprise de la preuve de morsure de la recherche) : `docs/parcours-simplifie.md : dans « <titre de section> », le numero <n> est associe au libelle « <libelle de la page> » ; attendu « <libelle rendu> » (couple « <n>. <libelle rendu> » tel que rendu par dofus_stuff/web/routes.py:<989|993>)` et, pour un numéro absent, `docs/parcours-simplifie.md : dans « <titre de section> », le numero <n> n'est associe a aucun libelle ; attendu le couple « <n>. <libelle rendu> » tel que rendu par dofus_stuff/web/routes.py:<989|993>`. Identifiants de code en ASCII, message en français, numéro de ligne réel du fichier cité.

- `test_parcours_cli_ne_pose_pas_les_trois_questions(docs_dir, section, normalize)` — le contrôle du fait mesuré qui évite une croyance fausse (D-33, D-36) : (a) la page contient la chaîne épinglée `ne pose pas ces trois questions` (comparaison normalisée) ; (b) les **littéraux de chaîne** de `dofus_stuff/optimize/profile_input.py`, collectés par `ast` (`ast.Constant` de type `str`), ne contiennent ni le mot entier `classe` ni le mot entier `element` après normalisation accents/casse (mesure de planification : 124 littéraux, zéro occurrence). Le message d'échec nomme le module, le littéral trouvé et la page à réviser. Garde anti-objet-vide : le nombre de littéraux lus est exigé non nul (un module renommé ou vide ferait passer le contrôle sans rien prouver). Le module doit écrire sa limite : la sonde lit le **texte** du module, elle n'exécute jamais le CLI (D-15).

**Ce que le contrôle ne fait pas** : il ne compare pas « la page entière » à un dictionnaire global (mesuré faux : les numéros 1–4 existent dans les deux menus, Pitfall 1) ; il n'assert rien sur les `…` (la troncature n'existe pas dans ce flux, ÉCR-1) ; il ne lit aucune valeur de mémoire (chaque libellé attendu vient du rendu de la même exécution). Ne pas modifier `dofus_stuff/`, ne pas toucher `.data/`, ne pas ajouter de test qui exécute le solveur.</action>
  <acceptance_criteria>
    - L'extraction des couples se fait **par section** : les numéros 1 à 4 du menu des éléments ne sont jamais comparés aux libellés du menu des classes, et un dictionnaire global `numéro → libellé` sur toute la page n'existe pas dans le module (grep : aucune construction d'un tel dictionnaire hors helper par section).
    - Le contrôle est vert sur la page livrée **sans aucune mutation** — condition indispensable du critère 5 — et rouge sur chacune des cinq dérives épinglées : libellé inversé dans la page (6 ↔ 9), numéro décalé dans la page (6 → 3), élément inversé dans la page (1 ↔ 3), ligne du menu supprimée dans la page, permutation de `CLASSES` côté code. Chaque rouge nomme la page, la section, le couple attendu et `dofus_stuff/web/routes.py:<ligne>`.
    - Les vérités rendues des deux menus sont non vides et comptées (19 classes, 4 éléments) : une vérité vide ne peut pas rendre le contrôle silencieusement vert.
    - Le contrôle du parcours CLI est vert et non vide : la page contient la chaîne épinglée `ne pose pas ces trois questions`, les littéraux de `dofus_stuff/optimize/profile_input.py` sont lus par `ast` (nombre non nul, mesuré 124) et aucun ne mentionne une classe ni un élément ; ajouter une invite de classe à ce module ferait rougir le contrôle.
    - Les couples des deux menus n'apparaissent qu'une fois dans la page (aucune table récapitulative concurrente).
    - `./.venv/Scripts/python.exe -m pytest -q` reste vert, module ajouté compris, et `.data/dofus.sqlite3` garde taille et `mtime_ns` (24 989 696 octets, `mtime_ns` 1788730056843137500 mesurés à la planification).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in page_swap page_numero page_element page_ligne code_derive; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in page_swap) sed -i 's/6\. Feca/6. Iop/; s/9\. Iop/9. Feca/' "$T/docs/parcours-simplifie.md"; motif='6. Feca';; page_numero) sed -i 's/6\. Feca/3. Feca/' "$T/docs/parcours-simplifie.md"; motif='Eliotrope';; page_element) sed -i 's/1\. Terre/1. Eau/; s/3\. Eau/3. Terre/' "$T/docs/parcours-simplifie.md"; motif='1. Terre';; page_ligne) sed -i '/9\. Iop/d' "$T/docs/parcours-simplifie.md"; motif='9. Iop';; code_derive) sed -i 's/"Cra", "Ecaflip"/"Ecaflip", "Cra"/' "$T/dofus_stuff/optimize/recommend.py"; motif='1. Ecaflip';; esac; out="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "BATTERIE DE MORSURES EN ECHEC"; exit 1; fi; echo "batterie de morsures complete : 5/5 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "BATTERIE DE MORSURES EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>Le critère 5 est tenu dans les deux sens : le contrôle est vert sur la page livrée, et cinq dérives (quatre côté page, une côté code) le font rougir en nommant la page, la section, le couple attendu et le fichier:ligne réel.</done>
</task>

</tasks>

<threat_model>
**Surface de cette phase** : des fichiers Markdown sous `docs/`, un module de test sous `tests/`, aucun serveur lancé, aucune dépendance ajoutée, aucun réseau, aucun secret. Les menaces réelles ne sont donc pas des menaces réseau mais des menaces de **fausseté** : un document qui affirme, et un contrôle qui approuve sans rien mesurer.

| # | Menace | Mitigation dans ce plan | Résidu |
|---|--------|-------------------------|--------|
| T-1 | **Confiance fausse** : un contrôle qui passe en lisant la réponse HTTP entière (où `data-stuff-payload` porte tout le résultat échappé) au lieu de ce que le lecteur voit | `_lignes_du_corps` / `_statut` sont les seules sources d'assertion ; un test dédié exige que les lignes du corps soient un **sous-ensemble strict** de la réponse et tiennent sur une page (≤ 18 lignes) | Un libellé rendu uniquement dans la charge utile ne serait pas couvert — assumé : il n'est pas « affiché » |
| T-2 | **Affirmation non mesurée** : la page cite une entrée, un libellé ou un message que l'outil ne produit pas | Toute valeur acceptée/refusée citée est rejouée sur le rendu ; toute valeur citée par une table doit appartenir à la liste épinglée, elle-même dérivée de la mesure ; les couples sont comparés au rendu, jamais à une liste écrite de mémoire | Les affirmations en **prose** (hors table) ne sont couvertes que par les contrôles de cette phase, pas par une validation de chaque phrase |
| T-3 | **Écriture implicite sous `.data/`** par le harnais (la fixture `app` charge la base locale) | Aucun test n'écrit ; `mtime_ns` + taille du fichier sont comparés avant/après (V15) ; garde `ast` : aucun import de `dofus_stuff.database`, aucun `sqlite3`, aucun `subprocess` (le module de test ne lance jamais `main()`) | La base **est** lue (fixture) : c'est le comportement mesuré du produit, pas une écriture |
| T-4 | **Fuite de contexte local** : la page citerait un chemin absolu de la machine (`C:\Users\…`) ou un fichier non versionné | Le contrôle des chemins exige que **tout** chemin entre accents graves existe depuis la racine du dépôt ; un chemin absolu local ne résout pas | Les chemins `.gsd-tmp/` non suivis ne doivent pas être cités (interdiction explicite dans les tâches) |
| T-5 | **Injection par contenu lu** : un contenu du dépôt (guide, base, sortie d'outil) influencerait l'exécution | Aucun contenu externe n'est exécuté ; les seules API appelées sont publiques (D-14) ; le `patch` se limite à court-circuiter le solveur | Aucun |
| T-6 | **Dépendance ajoutée** pour un besoin déjà couvert | Aucune installation : `flask`, `pytest`, `ortools`, `msgpack` sont déjà présents et le plan n'ajoute rien à `pyproject.toml` | Aucun |

**Hors périmètre (et pourquoi)** : publication, déploiement, suppression de données, resynchronisation Dofusdude — le mode hors-ligne de la base locale reste la règle (`.data/dofus.sqlite3`), aucune commande destructive n'est employée par ce plan ni par ses tests.
</threat_model>

<verification>
**Contrôles automatisés de ce plan** (tous exécutés par `.venv/Scripts/python.exe -m pytest`) : le module `tests/test_docs_parcours.py` (V1–V4) ; `tests/test_docs_structure.py` inchangé, qui porte V14 (index ↔ H1 ↔ lien de retour ↔ liens relatifs) ; `tests/test_docs_code_anchor.py` inchangé, qui porte la garde de la phase 1 ; la suite complète ; et la batterie de morsures (copie verte vérifiée avant chaque mutation, cinq dérives).

**Ce qui n'est pas automatisable ici et n'est pas maquillé** : la formulation du H1 (libre, Claude's Discretion) — seul le couple H1 ↔ libellé d'index est testé ; le comportement JavaScript (aucun moteur disponible) — il ne concerne pas ce plan, mais il sera traité au plan 03-03 comme contrôle de **littéraux** et nommé comme tel. Aucune validation humaine n'est exigée par ce plan, donc aucun blocage : rien n'y dépend d'un geste humain.

**Décision enregistrée pour le critère 5 de la phase** : la « suite reste verte » est prouvée par la suite complète **et** par la condition « page non mutée verte » de la batterie de morsures — une morsure mesurée sans cette condition ne prouverait rien.

**Vérification transverse de phase** : `/gsd:verify-work` rejouera la suite, la mesure de non-régression `.data/` (V15) et les contrôles de cette phase ; `03-VALIDATION.md` doit être mis à jour (`status: validated`) par `/gsd:validate-phase`, pas par l'exécuteur de ce plan.
</verification>

<success_criteria>
- `docs/parcours-simplifie.md` existe, est atteignable depuis `docs/sommaire.md`, et son H1 correspond à son libellé d'index (D-06, D-39, V14).
- Les trois questions et `AVANCE : personnaliser les réglages` sont exigés dans les lignes du corps réellement rendues, et cités par la page (critère 1, V1).
- Chaque entrée acceptée épinglée (classe : `Cra`, `crâ`, `1`, `19`, `019`, ` 3 ` ; éléments : `terre`, `terre air`, `terre,air`, `terre+air`, `1 3`, `multi` ; niveau : `150`, `050`, `200`, plus `AVANCE`) est acceptée par le rendu, et chaque refus épinglé (`eliotrop`, `0`, `20`, `multi terre`, `arbre`, `5`, `201`, `0`, `50.0`) re-rend l'écran en 200 avec le message exact au début de la ligne de statut (critère 1, V2, V3).
- Les couples numéro ↔ libellé des deux menus sont comparés au rendu **section par section**, la page non mutée est verte et cinq dérives la font rougir en nommant la page, la section, le couple attendu et `dofus_stuff/web/routes.py:<ligne>` (critère 5, V4).
- Le bloc « Source de vérité » cite `dofus_stuff/web/routes.py` et `dofus_stuff/optimize/recommend.py`, et tout chemin entre accents graves de la page existe sur disque.
- La garde `ast` passe : le module d'ancrage n'importe aucun module produit hors la surface publique admise, aucun `sqlite3`, `subprocess` ou `socket` (V15b).
- `./.venv/Scripts/python.exe -m pytest -q` est vert (169 tests existants + les nouveaux), la COPIE AVANT MUTATION de chaque morsure est verte, et `.data/dofus.sqlite3` est intact (taille + `mtime_ns`).
</success_criteria>

## Couverture des sources

Audit exigé : chaque élément des quatre sources apparaît ci-dessous, et chaque ligne dit **où** l'élément est consommé. Une ligne sans destination serait un écart, pas une omission silencieuse.

| Source | Élément | Consommé par |
|--------|---------|--------------|
| ROADMAP § Phase 3 | **Goal** (« …dérouler le flux simplifié classe → éléments → niveau, lire son résultat et comprendre ce que l'outil suppose ») | 03-01 (classe → éléments → niveau), 03-02 (lire son résultat), 03-04 (ce que l'outil suppose) |
| ROADMAP | Critère 1 (trois questions, `AVANCE`, entrées acceptées, messages réels) | **03-01** (tâches 1 et 2) |
| ROADMAP | Critère 2 (pagination, emplacement de `Méthode`/`Score`/`Indice`, table libellé → slot) | 03-02 (reformulé par ÉCR-1 et ÉCR-2) |
| ROADMAP | Critère 3 (sauvegarde navigateur 20 max, export Dofusbook, rattachés à l'écran) | 03-03 (reformulé par ÉCR-5) |
| ROADMAP | Critère 4 (points par niveau, paliers 40/100/150, heuristiques, ni exo ni parchemins, limites) | 03-04 (ÉCR-3, ÉCR-4) |
| ROADMAP | Critère 5 (numéro associé au mauvais libellé détecté, suite verte) | **03-01** tâche 3 (batterie de morsures) + 03-04 (contrôle de clôture) |
| REQUIREMENTS | SIMP-01 | **03-01** `requirements` |
| REQUIREMENTS | SIMP-02 | 03-02 et 03-03 `requirements` |
| REQUIREMENTS | SIMP-03 | 03-03 `requirements` |
| REQUIREMENTS | SIMP-04 | 03-04 `requirements` |
| CONTEXT | D-32 (les écrans web sont lus au client de test Flask, en processus : libellés, ordre des écrans et carte de pagination dérivés de cette sortie) | 03-01 (fixture `app`, aucun rendu sur la base réelle) ; 03-02 |
| CONTEXT | D-33 (les libellés des questions guidées CLI ne sont pas obtenus en exécutant `main()` : ils sont lus dans leur module source et ancrés par test) | **03-01** tâche 3 : le contrôle négatif sur `dofus_stuff/optimize/profile_input.py` et la phrase de la page qui dit que le CLI ne pose pas ces trois questions |
| CONTEXT | D-34 (l'assertion négative du critère 5 est construite sur la correspondance réellement rendue) | **03-01** tâche 3 |
| CONTEXT | D-35 (aucune sémantique inventée : tout libellé cité est réellement lu) | tous les plans (03-01 en premier) |
| CONTEXT | D-36 (la page décrit le parcours guidé tel que le lecteur le vit ; les écrans web sont la référence de rendu, le parcours CLI est mentionné sans être redupliqué) | **03-01** (introduction + la phrase mesurée : le CLI ne pose pas ces trois questions) |
| CONTEXT | D-37 (la surface de commandes reste propriété de `docs/cli.md` : renvoi en prose, pas de recopie) | 03-01 (lien relatif vers `cli.md`, aucune ligne de commande) ; 03-03 (export décrit sans commande) |
| CONTEXT | D-38 (« Lire le résultat » et « Sauvegarder et exporter » sont des **sections de cette page**, rattachées à l'écran qui les expose) | 03-02 (titre `## Lire le résultat` épinglé) et 03-03 (titre `## Sauvegarder et exporter` épinglé) |
| CONTEXT | D-39 (`docs/sommaire.md` gagne l'entrée maintenant, exhaustivité bidirectionnelle verte sans exception, `README.md` garde son lien unique) | **03-01** (ligne d'index ajoutée ; `## Parcours conseillé` cite déjà « Parcours simplifié » en prose ; suite de la phase 1 non modifiée) |
| CONTEXT | D-40 (chaque hypothèse est présentée avec la valeur réellement présente dans le code, et un contrôle vérifie qu'elle y figure encore) | 03-04 (capital, paliers, heuristiques, exo/parchemins) |
| CONTEXT | D-41 (une limite non adossée à quelque chose de lisible est retirée ou présentée comme une interprétation, jamais comme une vérité de code) | 03-04 (`## Ce que l'outil ne fait pas`) |
| CONTEXT | D-42 (aucune valeur écrite de mémoire) | 03-01 (couples et entrées dérivés du rendu) ; 03-02 (table dérivée de `display_slots`) |
| CONTEXT | D-43 (le wizard renvoyé en prose, sans lien) | 03-01 (introduction), 03-04 |
| CONTEXT | D-44 (base locale et dépannage en prose, sans lien) | 03-01 (introduction), 03-04 |
| CONTEXT | D-45 (pas de section de commandes ; renvoi à `docs/cli.md`) | 03-01 (lien relatif vers `cli.md`) ; 03-03 (export décrit sans ligne de commande) |
| RESEARCH | M1 (écrans et libellés d'étapes) | 03-01 tâche 1 |
| RESEARCH | M2 (menus, 19 + 4 couples) | **03-01** tâches 1 et 3 |
| RESEARCH | M3 (entrées acceptées et refusées, messages) | **03-01** tâche 2 |
| RESEARCH | M4 (statuts, frontière de la saisie) | 03-01 (ligne de statut) ; 03-02 (statuts du résultat) |
| RESEARCH | M5 (pagination) | 03-02 |
| RESEARCH | M6 (`Méthode`/`Score`/`Indice` en fin de résultat, charge utile échappée) | 03-02 (ÉCR-2) ; 03-01 (garde anti-`response.data`) |
| RESEARCH | M7 (`display_slots`, pas de `…` dans ce flux) | 03-02 (ÉCR-1) |
| RESEARCH | M8 (sauvegarde/export, `MAX_SAVES`, clé de stockage) | 03-03 (ÉCR-5) |
| RESEARCH | M9 (export Dofusbook, 10 groupes, `prysma` exclu) | 03-03 |
| RESEARCH | M10 (heuristiques de classe) | 03-04 (ÉCR-3) |
| RESEARCH | M11 (capital et paliers PA/PM) | 03-04 |
| RESEARCH | M12 (« ni exo ni parchemins », sélection du catalogue) | 03-04 (ÉCR-4) |
| RESEARCH | M13 (conventions : CRLF, UTF-8 sans BOM, balise `text`) | 03-01, 03-02, 03-03, 03-04 |
| RESEARCH | M14 (sommaire, gabarit des pages) | 03-01 |
| RESEARCH | M15 (`conftest.py` partagé, pas de duplication de helpers) | 03-01 (aucun helper promu) |
| RESEARCH | M16 (empreinte `.data/`) | 03-01 (`mtime_ns`/taille) ; 03-04 (SHA-256 avant/après, clôture) |
| RESEARCH | Finding porté au plan : le parcours guidé CLI **ne pose pas** les trois questions (aucune invite de classe ni d'élément dans `dofus_stuff/optimize/profile_input.py`) | **03-01** (phrase de la page + contrôle négatif des littéraux, tâche 3) |
| RESEARCH | Pitfall 1 (dictionnaire global faux) | **03-01** tâche 3 |
| RESEARCH | Pitfall 2 (`response.data` porte tout le résultat) | **03-01** tâche 1 et 2 |
| RESEARCH | Pitfalls 3, 5, 8 (fixture locale, apostrophes, égalité de classe sans préfixe) | 03-01 tâche 2 |
| RESEARCH | Pitfall 4 (un refus est un 200 + message) | **03-01** tâche 2 |
| RESEARCH | Pitfalls 6, 7 (non-déterminisme du score, `tojson`) | 03-02 (aucune valeur volatile épinglée) |
| PATTERNS | S1–S5 (analogs de pages, harnais, helpers, `patch` admis) | 03-01 (forme du module), puis 03-02, 03-03, 03-04 |
| PATTERNS | Gap 3 / ÉCR-1 | 03-02 |
| VALIDATION | V1, V2, V3, V4 | **03-01** (V4 par la tâche 3) |
| VALIDATION | V5, V6, V7 | 03-02 |
| VALIDATION | V8, V9, V10 | 03-03 |
| VALIDATION | V11, V12, V13 | 03-04 |
| VALIDATION | V14 | 03-01 (phase 1 non modifiée) + 03-04 (clôture) |
| VALIDATION | V15 | 03-01 (mesure locale) + 03-04 (mesure de clôture, SHA-256) |
| VALIDATION | 4 lignes `unresolved` de la sonde d'arêtes | 03-01, 03-02, 03-03, 03-04 : chacune est reprise en `must_haves.assumptions` du plan concerné, avec son contrôle écrit à la main (voir le plan correspondant) |

**Décisions d'écart (ÉCR) et leur destination** : ÉCR-1 (la troncature n'existe pas → table « libellé technique → nom complet ») → 03-02 ; ÉCR-2 (« dernière page » est faux sur la fixture → assertion **positionnelle**) → 03-02 et 03-03 ; ÉCR-3 (heuristiques confirmées → préférences de style, jamais une simulation) → 03-04 ; ÉCR-4 (le mot « parcho » à l'écran contre « zéro parchemin » → une phrase qui lève l'ambiguïté) → 03-04 ; ÉCR-5 (éviction silencieuse → « les plus anciennes sont remplacées ») → 03-03.

## Contributions et sondes

| Élément | État | Emplacement |
|---------|------|-------------|
| Aucun composant, lib, capability ou fichier réservé | — | Vérifié : `lib/`, `capabilities.json` et les espaces réservés sont **absents** du dépôt (`ls`), donc rien à brancher et rien à déclarer ; le placer serait un écart de nommage sans utilisateur |
| Tables canon | — | Ce plan n'écrit aucune ligne de table ; il **lit** `docs/sommaire.md` pour la table de documentation (M14) |
| Sonde de recherche | Conservée, non indexée | `.gsd-tmp/research-p3/` (probes jetables de la recherche) : **jamais** indexée, jamais citée par la page, **jamais supprimée** (interdiction de destruction de données) |
| Sonde de ce plan | `.gsd-tmp/plan-p3/` (si l'exécuteur en écrit une) | Non suivie, non indexée ; aucune donnée du projet n'y est copiée. La batterie de morsures travaille dans un `mktemp -d` hors du dépôt |
| Mise à jour, après exécution | `.planning/ROADMAP.md` (lignes du plan de phase 3) et `.planning/STATE.md` | Comme en phase 2 : la mise à jour du ROADMAP accompagne la création des plans, pas l'exécution |

## Artifacts this phase produces

Liste **par plan** (chaque plan porte la sienne ; l'union de la phase est : une page `docs/parcours-simplifie.md`, une ligne d'index dans `docs/sommaire.md`, un module `tests/test_docs_parcours.py`).

- **Créés par ce plan** : `docs/parcours-simplifie.md` (H1 `# Parcours simplifié`, les trois sections de questions, `## Source de vérité`, ligne de retour) ; la ligne d'index `| [Parcours simplifié](parcours-simplifie.md) | … |` dans `docs/sommaire.md` ; dans `tests/test_docs_parcours.py` : `RACINE_DEPOT`, `PAGE`, `SOMMAIRE`, `SOURCE_ROUTES`, `SOURCE_RECOMMEND`, `SOURCE_API`, `SOURCE_JS`, `SOURCE_DOFUSBOOK`, `SOURCE_SPEC`, `SOURCE_SCORE`, `SOURCE_CANDIDATES`, `TITRE_SOURCE`, `TITRE_CLASSE`, `TITRE_ELEMENTS`, `TITRE_NIVEAU`, `TITRE_RESULTAT`, `TITRE_SAUVEGARDE`, `TITRE_SUPPOSE`, `TITRE_LIMITES`, `CHEMIN_CITE`, `MOTIF_COUPLE_RENDU`, `ENTREES_MESUREES`, les helpers `_texte_page`, `_lignes_du_corps`, `_statut`, `_client_etape`, `_couples_de_section`, `_couples_du_rendu`, et les tests `test_trois_questions_et_avance_rendus`, `test_lignes_du_corps_ne_sont_pas_la_reponse_entiere`, `test_entrees_citees_acceptees_et_refusees`, `test_couples_numeros_libelles_par_section`, `test_parcours_cli_ne_pose_pas_les_trois_questions`, `test_source_de_verite_et_chemins_cites`, `test_sans_base_locale_ni_execution`.
- **Créés par 03-02** : les sections `## Lire le résultat` (D-38) et `## Correspondance des libellés` de la page, insérées avant `## Source de vérité` ; dans le module : `SLOTS_MESURE` (les couples libellé technique → nom complet dérivés de `api.py:362-380`) et les tests de lecture du résultat.
- **Créés par 03-03** : la section `## Sauvegarder et exporter` (D-38), insérée avant `## Source de vérité` ; dans le module : les constantes de sauvegarde/export (`MAX_SAUVEGARDES_ATTENDU`, la clé de stockage, les groupes Dofusbook) et les tests correspondants.
- **Créés par 03-04** : les sections `## Ce que l'outil suppose` et `## Ce que l'outil ne fait pas`, insérées avant `## Source de vérité` ; dans le module : `HYPOTHESES_MESUREES` et le test de clôture de la page (complétude, absence de bloc `console`, absence de lien externe, CRLF et UTF-8 sans BOM, empreinte `.data/`).


