---
phase: 03-parcours-simplifi-document-depuis-le-rendu-r-el
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - docs/parcours-simplifie.md
  - tests/test_docs_parcours.py
autonomous: true
requirements:
  - SIMP-04
estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Le capital de points est prouvé **par balayage** sur la surface publique pure : pour les niveaux 1, 39, 40, 99, 100, 149, 150 et 200, `capital_spent(recommendation_spec(\"Cra\", [\"terre\"], niveau).goals) == total_capital_for_level(niveau) == 5 * (niveau - 1)` (`dofus_stuff/model/solver_spec.py:264-269`, `dofus_stuff/optimize/recommend.py:24`), et la page cite la formule `5 * (niveau - 1)` — mesure de planification : ces huit niveaux étaient déjà balayés par la recherche (V11, D-40)."
    - "Les paliers PA/PM sont prouvés sur le **comportement** de `recommendation_spec`, pas sur une recopie : les cibles changent exactement aux niveaux 40, 100 et 150 (PA : 6 < 40, 8 de 40 à 99, 10 de 100 à 149, 11 dès 150 ; PM : 3, 4, 5, 6) et la base PA passe de 6 à 7 au niveau 100, et la page cite `40`, `100`, `150` et ces valeurs dans sa section `## Ce que l'outil suppose` (V11, `dofus_stuff/optimize/recommend.py:35-39`)."
    - "Les heuristiques de classe sont présentées comme des **préférences de style de jeu**, jamais comme une simulation de classe : les quatre ensembles du code sont extraits par `ast` (`dofus_stuff/optimize/recommend.py:45,47,51,53`), le nombre de classes sans objectif propre est **calculé** (9 = 19 moins l'union des ensembles) et la page cite les classes, les objectifs, ce nombre, la formule « préférences de style de jeu » et le libellé rendu `Jets moyens ; préférences de classe ajustables après calcul.` (`dofus_stuff/web/routes.py:1002`) — ÉCR-3, V12."
    - "L'absence d'exo et de parchemins est ancrée sur les deux lignes littérales du rendu : `Points inclus ; sans exo/parchemins. Jets moyens sauf réglage avancé.` (`dofus_stuff/optimize/api.py:316`) et `Recherche sur une sélection du catalogue ; optimalité globale non garantie.` (`dofus_stuff/optimize/api.py:434`) sont exigées **normalisées dans la page**, et la ligne `base+parcho` (`dofus_stuff/optimize/api.py:429`) est levée d'ambiguïté par une phrase qui dit que les parchemins saisis sont nuls ici — sans contredire l'hypothèse, sans la taire (ÉCR-4, V13)."
    - "L'« indice de recherche » est présenté comme un rapport **interne au solveur**, pas comme une qualité en combat : les trois modes rendus entre crochets (`optimal_prouve`, `borne_solver`, `borne_heuristique`, `dofus_stuff/optimize/score.py:8`) sont exigés dans la page, et la section dit explicitement que ce n'est pas une mesure de combat (critère 4)."
    - "La recherche sur une **sélection** du catalogue est décrite comme un mécanisme (filtre de plausibilité, plafond de niveau, profondeur par emplacement `top_k=40` lue dans `dofus_stuff/optimize/recommend.py:58`), sans **aucun** chiffre de catalogue : les nombres mesurés (milliers de candidats, centaines de panoplies) sont interdits dans la phrase (« valeurs volatiles », PROJECT.md « Out of Scope ») et le contrôle le vérifie par l'absence de tout nombre à quatre chiffres et plus dans la section (critère 4)."
    - "La page est **complète et conforme** à la fin de la phase : ses sections de niveau 2 sont exactement les neuf titres épinglés, dans l'ordre ; elle n'a qu'un H1 ; elle ne contient aucun bloc de code balisé `console` ni aucun lien externe ; elle est en CRLF sans BOM ; et la suite des tests de la phase 1 (index, H1, liens relatifs) reste verte sans exception (critère 5, V14, D-39)."
    - "L'empreinte de la base locale est mesurée **avant et après** un rendu réel : taille, `mtime_ns` et SHA-256 de `.data/dofus.sqlite3` identiques (mesure directe ; la garde `ast` du plan 03-01 n'en était qu'une démonstration indirecte), et la mesure **discrimine** : appliquée à une copie de la base modifiée dans un dossier temporaire, l'empreinte change (V15)."
  artifacts:
    - path: "docs/parcours-simplifie.md"
      provides: "Les sections « Ce que l'outil suppose » (capital, paliers PA/PM, heuristiques de classe, ni exo ni parchemins) et « Ce que l'outil ne fait pas » (indice de recherche, sélection du catalogue, wizard et base locale renvoyés en prose)"
      contains: "## Ce que l'outil suppose"
      min_lines: 100
    - path: "tests/test_docs_parcours.py"
      provides: "Contrôles de clôture : hypothèses prouvées par balayage, limites ancrées, complétude et conformité de la page, empreinte `.data/` mesurée avant/après"
      contains: "capital_spent"
      min_lines: 330
  key_links:
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/optimize/recommend.py"
      via: "appel public de recommendation_spec sur huit niveaux pour prouver le capital et les paliers PA/PM, et extraction par ast des quatre ensembles de classes"
      pattern: "recommendation_spec"
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/model/solver_spec.py"
      via: "surface publique capital_spent et total_capital_for_level, utilisée comme vérité du capital de points"
      pattern: "capital_spent"
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/optimize/api.py"
      via: "exigence des lignes littérales de l'hypothèse (sans exo/parchemins, sélection du catalogue) et de la ligne base+parcho levée d'ambiguïté"
      pattern: "base+parcho"
    - from: "tests/test_docs_parcours.py"
      to: "dofus_stuff/optimize/score.py"
      via: "lecture des trois modes de compatibilité exigés dans la page pour borner ce que « indice de recherche » veut dire"
      pattern: "optimal_prouve"
    - from: "tests/test_docs_parcours.py"
      to: ".data/dofus.sqlite3"
      via: "empreinte (taille, mtime_ns, SHA-256) mesurée avant et après un rendu réel du résultat : aucune écriture tolérée"
      pattern: "dofus.sqlite3"
  prohibitions:
    - statement: "La page ne doit pas présenter les heuristiques de classe comme une simulation des sorts ou du gameplay de la classe, ni promettre un résultat de combat : le code les nomme lui-même des préférences de style de jeu."
    - statement: "La page ne doit contenir aucun chiffre de catalogue (nombre de candidats, d'objets ou de panoplies) : ces valeurs sont volatiles et interdites en prose ; le mécanisme est décrit sans elles."
    - statement: "La page ne doit pas lier `docs/wizard-avance.md` ni `docs/base-locale.md` (ces pages n'existent pas encore) : le renvoi reste en prose, et l'ajout du lien appartient à la phase qui crée la page cible."
    - statement: "Le nouveau bloc de tests ne doit pas exécuter le solveur plus d'une fois : les hypothèses sont prouvées par `recommendation_spec` (pur), et le seul calcul réel est celui de l'empreinte `.data/`, patché comme en 03-02 si nécessaire."
  assumptions:
    - "Exigence **documentaire** : la sonde d'arêtes n'a pas de prédicat pour SIMP-04, donc ce plan est traité comme non classé. Décision enregistrée : la couverture d'arêtes est écrite à la main depuis V11, V12, V13, V14 et V15, chacune avec son contrôle falsifiable."
    - "Les limites « pas une qualité en combat » et « recherche sur une sélection » sont **adossées au code** (score interne borné, préfiltrage et profondeur par emplacement) ; ce que la page ajoute de non mesurable — l'intérêt pratique de l'indice pour un joueur — est présenté comme une interprétation de lecture, jamais comme une vérité de code (D-41)."
    - "Le balayage des paliers et du capital couvre les niveaux de bordure (39/40, 99/100, 149/150) et les bornes 1 et 200, pas les 200 niveaux : la recherche a montré qu'un balayage complet n'apporterait rien (la cible ne dépend que du palier), et cette limite est écrite dans le module."
---

<objective>
**Cadrage dérivé (non une décision produit nouvelle)** — repris du `**Goal:**` de la phase 3 (« … comprendre ce que l'outil suppose. »), de son critère de succès 4 et des décisions D-40, D-41, D-43 et D-44 de `03-CONTEXT.md` :
**En tant que** lecteur qui compare le stuff proposé à ce qu'il attend, **je veux** savoir sur quelles hypothèses l'outil s'appuie (points par niveau, paliers PA/PM, préférences de classe, ni exo ni parchemins) et ce qu'il ne fait pas (ce que veut dire l'indice de recherche, et que la recherche ne balaie qu'une sélection du catalogue), **afin de** juger le résultat sans lui prêter une garantie qu'il n'offre pas.

Purpose : c'est SIMP-04 et le critère 4 de la phase, plus le contrôle de **clôture** de la page (critère 5 : « la suite reste verte »). Trois écarts mesurés sont ici traités explicitement : ÉCR-3 (les heuristiques de classe **existent** : le code les nomme des préférences de style de jeu, pas une simulation), ÉCR-4 (l'écran affiche le mot `parcho` alors que le parcours suppose zéro parchemin — une phrase lève l'ambiguïté, ni contradiction affirmée ni silence), et la règle D-41 (une limite non adossée au code est retirée ou présentée comme une interprétation).

Output : les sections `## Ce que l'outil suppose` et `## Ce que l'outil ne fait pas` de `docs/parcours-simplifie.md` (insérées avant `## Source de vérité`), et, dans `tests/test_docs_parcours.py`, les contrôles d'hypothèses (balayage par API publique), de limites (ancrage source) et de clôture (complétude, conformité CRLF/BOM, absence de bloc de commandes et de lien externe, empreinte `.data/` avant/après).

Ordonnancement déclaré : **vague 4**, `depends_on: [03-03]` — dernière vague de la phase : elle ferme la page et la vérifie en entier.

**Ce que ce plan ne fait pas** : il ne documente ni le wizard avancé (phase 4), ni le fonctionnement de la base locale (phase 5), ni le dépannage et le glossaire (phase 6) — renvois en prose, **sans lien** tant que les pages cibles n'existent pas (D-43, D-44) ; il ne cite aucun nombre de catalogue ; il ne touche pas à `.data/` (il en **mesure** l'empreinte) ; il n'ajoute aucune commande.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-CONTEXT.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-PATTERNS.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-VALIDATION.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-01-PLAN.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-02-PLAN.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-03-PLAN.md
@docs/parcours-simplifie.md
@tests/test_docs_parcours.py
@dofus_stuff/optimize/recommend.py
@dofus_stuff/model/solver_spec.py
@dofus_stuff/optimize/api.py
@dofus_stuff/optimize/score.py
@dofus_stuff/optimize/candidates.py
@dofus_stuff/web/routes.py
@dofus_stuff/web/screens.py
@tests/test_docs_structure.py
</context>
<tasks>

<task type="auto">
  <name>Tâche 1 : la section « Ce que l'outil suppose » et les hypothèses prouvées par balayage</name>
  <files>docs/parcours-simplifie.md, tests/test_docs_parcours.py</files>
  <read_first>
    docs/parcours-simplifie.md
    tests/test_docs_parcours.py
    dofus_stuff/optimize/recommend.py
    dofus_stuff/model/solver_spec.py
    dofus_stuff/optimize/api.py
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
  </read_first>
  <action>Écrire les hypothèses du critère 4 **depuis les valeurs du code**, et les prouver par balayage sur la surface publique pure (aucun solveur lancé).

**Page** — insérer `## Ce que l'outil suppose` après `## Sauvegarder et exporter` et avant `## Source de vérité` :
- **points** : le capital disponible vaut `5 * (niveau - 1)` (`dofus_stuff/model/solver_spec.py:264-269`, appliqué en `dofus_stuff/optimize/recommend.py:24`) et la répartition automatique **utilise tout le capital** (mesure : le capital consommé égale le capital disponible pour les niveaux balayés) ;
- **paliers PA/PM** : un petit tableau (niveau → PA visés → PM visés) avec les quatre paliers réels — `1` à `39` : 6 PA / 3 PM ; `40` à `99` : 8 PA / 4 PM ; `100` à `149` : 10 PA / 5 PM ; `150` à `200` : 11 PA / 6 PM — et la mention que la **base** PA passe de 6 à 7 au niveau 100 (`dofus_stuff/optimize/recommend.py:35-39`). Les valeurs sont recopiées du fichier, jamais de mémoire ;
- **heuristiques de classe** (ÉCR-3) : la page dit que l'outil ajoute des objectifs selon la classe — `% Dommages distance` pour Cra, Enutrof, Sadida, Eniripsa, Steamer, Osamodas ; `% Dommages mêlée` pour Iop, Sacrieur, Ouginak, Zobal ; `Portée` (cible 2 avant le niveau 100, 4 ensuite) pour Cra, Enutrof, Sadida ; `Invocation` (base 1, cible 3) pour Osamodas, Sadida — qu'aucun objectif propre n'est ajouté pour les **neuf** autres classes (Ecaflip, Eliotrope, Feca, Forgelance, Huppermage, Pandawa, Roublard, Sram, Xelor), et surtout que ce sont des **préférences de style de jeu**, pas une simulation des sorts de la classe (le code le dit en `dofus_stuff/optimize/recommend.py:44`) et qu'elles sont **ajustables après le calcul** — la page cite le libellé rendu `Jets moyens ; préférences de classe ajustables après calcul.` (`dofus_stuff/web/routes.py:1002`) ;
- **ni exo ni parchemins** (ÉCR-4) : la page cite la ligne rendue `Points inclus ; sans exo/parchemins. Jets moyens sauf réglage avancé.` (`dofus_stuff/optimize/api.py:316`) **et** lève d'**une** phrase l'ambiguïté de la ligne `base+parcho` (`dofus_stuff/optimize/api.py:429`) : ce total additionne la base et les parchemins **saisis**, qui sont nuls dans ce parcours — ni contradiction affirmée, ni silence.

**Module** — ajouter :
- `_ensembles_de_classes()` : parcours `ast` de `dofus_stuff/optimize/recommend.py` collectant les `ast.Set` de littéraux de chaîne (les quatre ensembles d'heuristiques) ;
- `NIVEAUX_BALAYES = (1, 39, 40, 99, 100, 149, 150, 200)` et `test_hypotheses_prouvees_par_balayage(docs_dir, section, normalize)` :
  1. **capital** : pour chaque niveau balayé, `capital_spent(recommendation_spec("Cra", ["terre"], niveau).goals) == total_capital_for_level(niveau) == 5 * (niveau - 1)` — appels **publics et purs**, aucun solveur (surface : `dofus_stuff/model/solver_spec.py:264-269`, `dofus_stuff/optimize/recommend.py:13`) ;
  2. **paliers** : sur les niveaux de bordure, les cibles de `spec.goals["PA"]` et `spec.goals["PM"]` valent exactement 6/8/8/10/10/11/11 (PA, niveaux 39/40/99/100/149/150/200) et 3/4/4/5/5/6/6 (PM), et la base PA vaut 6 jusqu'à 99 puis 7 à partir de 100 : le contrôle **mesure** les paliers au lieu de recopier la ligne conditionnelle ;
  3. **heuristiques** : les quatre ensembles épinglés (`distance`, `mêlée`, `Portée`, `Invocation`) sont **parmi** les ensembles extraits par `ast` ; le nombre de classes sans objectif propre est **calculé** (`len(CLASSES) - len(union)` == 9) ; le fichier contient le commentaire `# Broad playstyle preferences, not a simulation of class spells.` ;
  4. **page** : la section contient `5 * (niveau - 1)`, les trois bornes `40`, `100`, `150`, les valeurs visées (`6`, `8`, `10`, `11`, `3`, `4`, `5`, `6`), les treize noms de classes des quatre groupes, les quatre noms d'objectifs, le nombre calculé de classes sans objectif, la tournure `préférences de style de jeu`, le libellé `Jets moyens ; préférences de classe ajustables après calcul.`, la ligne `Points inclus ; sans exo/parchemins. Jets moyens sauf réglage avancé.` (normalisée) et le fragment `base+parcho` accompagné de la tournure `aucun parchemin` ;
  5. **bidirectionnel** : les deux lignes littérales d'hypothèse sont exigées **dans la page et dans `dofus_stuff/optimize/api.py`** (retirer l'une ou l'autre rougit).
  Les constats sont accumulés et joints à **une** assertion nommant la page, la valeur attendue et le fichier. Le module écrit la limite du balayage : les niveaux de bordure et les bornes, pas les 200 niveaux (la cible ne dépend que du palier).

Ne pas présenter les heuristiques comme une simulation de classe, ne citer aucun poids numérique interne (`weight=…`), ne pas recopier d'option de commande (`--top-k` reste décrit dans la page CLI, D-37).</action>
  <acceptance_criteria>
    - Le balayage passe : capital consommé == capital disponible == `5 * (niveau - 1)` sur les huit niveaux, cibles PA 6/8/10/11 aux bornes 39/40/99/100/149/150 et base PA 6 puis 7 au niveau 100, cibles PM 3/4/5/6.
    - Les quatre ensembles de classes sont retrouvés par `ast` dans `dofus_stuff/optimize/recommend.py`, le nombre de classes sans objectif propre est calculé (9) et le commentaire du code sur les préférences de style de jeu est exigé.
    - La section `## Ce que l'outil suppose` cite la formule `5 * (niveau - 1)`, les paliers et leurs valeurs, les classes et objectifs, le nombre de classes sans objectif, la tournure `préférences de style de jeu`, le libellé ajustable après calcul, la ligne « sans exo/parchemins » et la levée d'ambiguïté de `base+parcho`.
    - Les deux lignes littérales d'hypothèse sont exigées des deux côtés (page et `dofus_stuff/optimize/api.py`).
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q` est vert et aucun solveur n'est lancé par ce test (appels purs uniquement).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in palier_pa capital classe_retiree ligne_hypothese; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in palier_pa) sed -i 's/else 10 if level < 150 else 11/else 10 if level < 150 else 12/' "$T/dofus_stuff/optimize/recommend.py"; motif='PA';; capital) sed -i 's/capital = 5 \* (level - 1)/capital = 4 * (level - 1)/' "$T/dofus_stuff/optimize/recommend.py"; motif='capital';; classe_retiree) sed -i 's/"Cra", "Enutrof", "Sadida", "Eniripsa", "Steamer", "Osamodas"/"Cra", "Enutrof", "Sadida", "Eniripsa", "Steamer"/' "$T/dofus_stuff/optimize/recommend.py"; motif='Osamodas';; ligne_hypothese) sed -i 's/Points inclus ; sans exo\/parchemins. Jets moyens sauf réglage avancé./Points inclus ; sans exo ni parchemin./' "$T/dofus_stuff/optimize/api.py"; motif='sans exo\/parchemins';; esac; out="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 03-04 EN ECHEC"; exit 1; fi; echo "morsures 03-04 (tache 1) : 4/4 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 03-04 EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>Le critère 4 est tenu pour les hypothèses : capital et paliers sont **mesurés** sur la surface publique, les heuristiques sont nommées et dénombrées depuis le code, les deux lignes littérales sont exigées des deux côtés, l'ambiguïté de `base+parcho` est levée, et quatre dérives du code rougissent le module sur une copie verte avant mutation.</done>
</task>

<task type="auto">
  <name>Tâche 2 : la section « Ce que l'outil ne fait pas », ancrée sur le code et sans chiffre de catalogue</name>
  <files>docs/parcours-simplifie.md, tests/test_docs_parcours.py</files>
  <read_first>
    docs/parcours-simplifie.md
    tests/test_docs_parcours.py
    dofus_stuff/optimize/score.py
    dofus_stuff/optimize/candidates.py
    dofus_stuff/optimize/recommend.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/screens.py
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-RESEARCH.md
  </read_first>
  <action>Écrire les limites du critère 4 — celles qui sont **adossées au code** (D-41) — et les contrôler.

**Page** — insérer `## Ce que l'outil ne fait pas` après `## Ce que l'outil suppose` et avant `## Source de vérité` :
- **l'indice de recherche n'est pas une qualité en combat** : la page dit ce qu'il mesure réellement (une comparaison du score retenu à une borne produite par la recherche elle-même, bornée à 100 %) et cite les trois modes rendus entre crochets — `optimal_prouve` (optimalité prouvée), `borne_solver`, `borne_heuristique` (`dofus_stuff/optimize/score.py:8`) — et la phrase épinglée `n'est pas une qualité en combat` ;
- **la recherche ne balaie pas tout le catalogue** : la page cite la phrase rendue `Recherche sur une sélection du catalogue ; optimalité globale non garantie.` (`dofus_stuff/optimize/api.py:434`) et décrit le mécanisme — un filtre de **plausibilité**, le plafond de niveau du personnage, une profondeur par emplacement (`dofus_stuff/optimize/recommend.py:58` la fixe pour ce parcours ; la page CLI documente ce réglage, cette page n'en recopie pas le nom d'option, D-37) — **sans aucun chiffre de catalogue** : la page ne cite ni nombre de candidats, ni nombre d'objets, ni nombre de panoplies (valeurs volatiles interdites en prose) ;
- **si le navigateur refuse d'ouvrir Dofusbook**, l'outil affiche l'URL dans la ligne de statut, coupée à la largeur de l'écran (`dofus_stuff/web/routes.py:1337` : la coupe est réelle et **sans** points de suspension, ce qui n'a rien à voir avec les libellés d'emplacement de la section de correspondance) ;
- **ce que cette page ne décrit pas** : les réglages avancés (wizard) et la base locale sont renvoyés **en prose, sans lien** (leurs pages n'existent pas encore ; le lien sera ajouté par la phase qui les crée, D-43, D-44) — la section nomme `réglages avancés` et `base locale` sans créer de lien.

**Module** — ajouter `test_limites_ancrees_sur_le_code(docs_dir, section, normalize)` :
1. les trois modes sont exigés dans `dofus_stuff/optimize/score.py` **et** dans la section, ainsi que la phrase épinglée `n'est pas une qualité en combat` et le libellé `Indice de recherche` ;
2. la phrase du catalogue est exigée **dans la page** (normalisée) et dans `dofus_stuff/optimize/api.py` ; le filtre de plausibilité est exigé sous son nom de code (`_is_plausible_equipment`) dans `dofus_stuff/optimize/candidates.py` et la profondeur par emplacement sous le fragment `top_k=40` dans `dofus_stuff/optimize/recommend.py` ; la page cite le mot `plausibilité` ;
3. **interdiction des valeurs volatiles** : la section ne contient **aucun** nombre de trois chiffres ou plus (`\d{3,}`) — les chiffres de catalogue mesurés sont de cet ordre et ne doivent pas entrer dans la prose ; le message d'échec cite la valeur trouvée et rappelle la règle ;
4. la coupe de l'URL : le fragment `url[:COLS]` est exigé dans `dofus_stuff/web/routes.py`, la section dit que l'URL est affichée quand l'ouverture échoue, et la page cite la valeur de `COLS` **extraite** de `dofus_stuff/web/screens.py` (`COLS = …`) ;
5. le renvoi sans lien : la section nomme `réglages avancés` et `base locale`, et la page ne contient **aucun** lien Markdown vers `wizard-avance.md` ni vers `base-locale.md` (le renvoi reste en prose ; l'ajout du lien appartient à la phase qui crée la cible).
Les constats sont accumulés et joints à **une** assertion nommant la page, la valeur attendue et le fichier. Le module écrit que ce qui n'était pas adossé au code a été **soit retiré, soit présenté comme une interprétation** (D-41).</action>
  <acceptance_criteria>
    - La section `## Ce que l'outil ne fait pas` existe, avant `## Source de vérité`, cite les trois modes (`optimal_prouve`, `borne_solver`, `borne_heuristique`), la phrase `n'est pas une qualité en combat`, la phrase du catalogue, le mot `plausibilité`, `réglages avancés` et `base locale`.
    - La section ne contient aucun nombre de trois chiffres ou plus (aucune valeur de catalogue), et la page ne lie ni `wizard-avance.md` ni `base-locale.md`.
    - Les ancrages source sont exigés et vérifiés : `_is_plausible_equipment` dans `dofus_stuff/optimize/candidates.py`, `top_k=40` dans `dofus_stuff/optimize/recommend.py`, `url[:COLS]` dans `dofus_stuff/web/routes.py`, la phrase du catalogue dans `dofus_stuff/optimize/api.py`, la valeur de `COLS` extraite de `dofus_stuff/web/screens.py` et citée par la page.
    - Retirer l'un de ces fragments du code, ou insérer un chiffre de catalogue dans la section, rougit en nommant la valeur et le fichier.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q` est vert ; `.data/dofus.sqlite3` garde taille et `mtime_ns`.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in mode_retire plausibilite chiffre_volatile lien_interdit; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in mode_retire) sed -i 's/borne_heuristique/borne_heur/' "$T/dofus_stuff/optimize/score.py"; motif='borne_heuristique';; plausibilite) sed -i 's/_is_plausible_equipment/_is_plausible/' "$T/dofus_stuff/optimize/candidates.py"; motif='_is_plausible_equipment';; chiffre_volatile) sed -i 's/^## Ce que l.outil ne fait pas$/## Ce que l.outil ne fait pas\n\nLa recherche garde 1219 objets candidats./' "$T/docs/parcours-simplifie.md"; motif='1219';; lien_interdit) sed -i 's/^## Source de vérité$/Voir [le wizard](wizard-avance.md).\n\n## Source de vérité/' "$T/docs/parcours-simplifie.md"; motif='wizard-avance.md';; esac; out="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 03-04 EN ECHEC"; exit 1; fi; echo "morsures 03-04 (tache 2) : 4/4 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 03-04 EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>Le critère 4 est tenu pour les limites : l'indice est borné à ce qu'il mesure, la recherche sur une sélection est décrite sans aucun chiffre de catalogue, la coupe de l'URL en cas d'échec est dite, les renvois wizard/base restent sans lien, et quatre dérives rougissent le module sur une copie verte avant mutation.</done>
</task>

<task type="auto">
  <name>Tâche 3 : le contrôle de clôture de la page et l'empreinte de la base locale</name>
  <files>docs/parcours-simplifie.md, tests/test_docs_parcours.py</files>
  <read_first>
    docs/parcours-simplifie.md
    tests/test_docs_parcours.py
    tests/test_docs_structure.py
    tests/conftest.py
    dofus_stuff/web/routes.py
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-VALIDATION.md
  </read_first>
  <action>Fermer la phase par deux contrôles qui ne dépendent d'aucune valeur écrite de mémoire : la **complétude et la conformité de la page**, et l'**empreinte de la base locale** avant/après un rendu réel.

**Module** — ajouter `_empreinte(chemin)` (rend `(taille, mtime_ns, sha256)`), puis :
1. `test_page_complete_et_sans_derive(docs_dir, normalize)` :
   - les sections de niveau 2 de la page sont **exactement** les neuf titres épinglés, **dans l'ordre** : `## Question 1/3 : la classe`, `## Question 2/3 : les éléments`, `## Question 3/3 : le niveau`, `## Lire le résultat`, `## Correspondance des libellés`, `## Sauvegarder et exporter`, `## Ce que l'outil suppose`, `## Ce que l'outil ne fait pas`, `## Source de vérité` (comparaison normalisée ; toute section manquante, renommée ou déplacée est nommée) ;
   - la page a **un seul** H1 et sa dernière ligne non vide est `[Retour au sommaire](sommaire.md)` ;
   - la page ne contient **aucun** bloc de code balisé `console` ni aucun lien externe (`](http`) : décision de périmètre D-37 et D-01, contrôlée ici pour la page entière ;
   - la page ne lie ni `wizard-avance.md` ni `base-locale.md` (D-44) ;
   - les octets sont en **CRLF sans BOM** : lire le fichier en binaire, exiger l'absence de BOM et l'égalité entre le nombre de `\n` et le nombre de `\r\n` (une conversion LF accidentelle est nommée — la convention a déjà été prise en défaut dans les phases précédentes) ;
2. `test_empreinte_data_inchangee(app)` — mesure **directe** de V15 : chemin `RACINE_DEPOT / ".data" / "dofus.sqlite3"` ; si le fichier est absent, `pytest.skip` avec le motif écrit (« base locale absente : la mesure d'empreinte n'a pas d'objet ») ; empreinte **avant**, puis un **rendu réel du résultat** sur la fixture (le flux complet : injection de session, `POST` de l'étape 3 ; le solveur lit la base), puis empreinte **après** : taille, `mtime_ns` et SHA-256 doivent être **identiques**. Aucune écriture sous `.data/` n'est tolérée : c'est le seul test du module qui touche la base.
Les messages nomment le chemin, la composante qui a changé et les deux valeurs. Ne **jamais** provoquer une modification de `.data/` pour « prouver » que le contrôle mord : la morsure de la **mesure** se démontre sur une **copie** du fichier dans un dossier temporaire (voir la vérification).</action>
  <acceptance_criteria>
    - Les neuf titres de section sont exigés, dans l'ordre, et toute dérive (section manquante, renommée, déplacée) est nommée ; la page a un seul H1 et se termine par la ligne de retour.
    - La page ne contient aucun bloc `console`, aucun lien externe, aucun lien vers `wizard-avance.md` ou `base-locale.md`, et ses octets sont en CRLF sans BOM (une conversion LF est détectée).
    - L'empreinte de `.data/dofus.sqlite3` (taille, `mtime_ns`, SHA-256) est identique avant et après un rendu réel du résultat ; la base absente produit un `skip` explicite, jamais un faux vert.
    - La mesure **discrimine** : appliquée à une copie du fichier modifiée dans un dossier temporaire, l'empreinte change (démontré par la vérification, sans jamais toucher `.data/`).
    - `./.venv/Scripts/python.exe -m pytest -q` est vert (phase 1 comprise, sans exception), et la batterie de morsures de la phase reste verte sur la page finale.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_parcours.py -q && ./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py tests/test_docs_code_anchor.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans l'un des deux résumés pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; cp .data/dofus.sqlite3 "$T/copie.sqlite3" 2>/dev/null || : ; "$PY" - "$T/copie.sqlite3" <<'PYEOF'
import importlib.util, pathlib, sys
cible = pathlib.Path(sys.argv[1])
if not cible.exists():
    cible.write_bytes(b"empreinte de demonstration\n")
spec = importlib.util.spec_from_file_location("tdp", "tests/test_docs_parcours.py")
module = importlib.util.module_from_spec(spec)
spec.loader.exec_module(module)
avant = module._empreinte(cible)
with cible.open("ab") as fh:
    fh.write(b"octet ajoute")
apres = module._empreinte(cible)
if avant != apres:
    print("mesure discriminante : la copie modifiee dans le dossier temporaire change d'empreinte")
else:
    print("MESURE NON DISCRIMINANTE : l'empreinte ne change pas apres modification de la copie")
    sys.exit(1)
print("fichier de reference inchange :", module._empreinte(pathlib.Path(".data/dofus.sqlite3"))[:2] if pathlib.Path(".data/dofus.sqlite3").exists() else "absent")
PYEOF</automated>
    <fails_when>exit non-zero, ou la ligne "MESURE NON DISCRIMINANTE : l'empreinte ne change pas apres modification de la copie"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in section_renommee lien_externe lf_converti; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in section_renommee) sed -i 's/^## Ce que l.outil ne fait pas$/## Limites/' "$T/docs/parcours-simplifie.md"; motif='Ce que l';; lien_externe) sed -i 's|^## Source de vérité$|[lien](https://exemple.invalid)\n\n## Source de vérité|' "$T/docs/parcours-simplifie.md"; motif='lien externe';; lf_converti) "$PY" -c "p='$T/docs/parcours-simplifie.md'; d=open(p,'rb').read().replace(b'\r\n', b'\n'); open(p,'wb').write(d)"; motif='CRLF';; esac; out="$(cd "$T" && "$PY" -m pytest tests/test_docs_parcours.py -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 03-04 EN ECHEC"; exit 1; fi; echo "morsures 03-04 (tache 3) : 3/3 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 03-04 EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>La phase est close : la page est complète et conforme (neuf sections dans l'ordre, un H1, CRLF sans BOM, aucun bloc de commandes, aucun lien externe ni vers les pages inexistantes), la base locale est intacte après un rendu réel (empreinte identique) et la mesure d'empreinte discrimine, démontré sur une copie temporaire — sans jamais toucher `.data/`.</done>
</task>

</tasks>

<threat_model>
**Surface** : une page `docs/`, un module de test, des lectures de sources produit et une lecture de la base locale. Aucun réseau, aucun secret, aucune dépendance ajoutée. La menace principale de ce plan est **la preuve par la donnée du projet** : il est tentant de modifier `.data/` pour montrer qu'un contrôle mord.

| # | Menace | Mitigation | Résidu |
|---|--------|-----------|--------|
| T-17 | **Destruction ou modification de données** : prouver la morsure de l'empreinte en écrivant dans `.data/dofus.sqlite3` | Interdiction explicite : la base est **lue** et son empreinte comparée ; la morsure de la mesure se démontre sur une **copie** dans `mktemp -d` ; aucune commande destructive (`db clear`, drop) n'est employée nulle part | Aucun : la règle est écrite dans la tâche et dans la vérification |
| T-18 | **Faux vert par interruption** : la mesure d'empreinte passe parce qu'aucun rendu réel n'a eu lieu | Le test exécute le flux complet jusqu'au résultat (le solveur lit la base) avant la seconde mesure ; un `skip` explicite remplace un faux vert si la base est absente | Une base absente laisse la mesure sans objet : c'est dit, jamais masqué |
| T-19 | **Limite inventée** : présenter comme une vérité de code une lecture de joueur | Chaque limite de la section est adossée à un fichier ou une ligne citée (`score.py`, `candidates.py`, `recommend.py:58`, `routes.py:1337`) ; ce qui n'était adossé à rien est retiré ou présenté comme une interprétation (D-41) | L'intérêt pratique de l'indice pour un joueur reste une lecture, annoncée comme telle |
| T-20 | **Valeurs volatiles en prose** : citer des nombres de catalogue mesurés (milliers de candidats, centaines de panoplies) | La section des limites n'accepte **aucun** nombre de trois chiffres ou plus, et le message d'échec nomme la valeur trouvée | Aucun |
| T-21 | **Dérive de format** : page réécrite en LF ou avec BOM par un outil | Contrôle binaire CRLF/BOM dans le module, avec une morsure qui le prouve (conversion LF) | Aucun |

**Hors périmètre** : publication, déploiement, synchronisation Dofusdude (`db clear`, drop, suppression sous `.data/` : jamais), achat, action humaine — rien de tout cela n'est requis par la phase, et aucun de ces gestes n'est exécuté.
</threat_model>

<verification>
**Contrôles automatisés** : `tests/test_docs_parcours.py` (V11, V12, V13, V14 pour la partie structurelle, V15 pour l'empreinte), `tests/test_docs_structure.py` et `tests/test_docs_code_anchor.py` (socle des phases 1-2,inchangés), la suite complète, la démonstration de discrimination de l'empreinte sur une copie temporaire, et onze morsures sur copie verte avant mutation (quatre en tâche 1, quatre en tâche 2, trois en tâche 3).

**Non automatisable, nommé comme tel** : aucune. Ce plan ne dépend d'aucun geste humain, d'aucun secret et d'aucun réseau ; la seule action physique possible (ouvrir Dofusbook) est interdite aux tests et gardée par le plan 03-03.

**Clôture de phase** : après cette vague, la page porte les neuf sections épinglées et la suite complète est verte ; `/gsd:verify-work` rejouera la suite, la mesure `.data/` et les critères 1 à 5, puis `/gsd:validate-phase` mettra `03-VALIDATION.md` à jour (`status: validated`) — pas l'exécuteur de ce plan.
</verification>

<success_criteria>
- `## Ce que l'outil suppose` prouve les hypothèses par balayage (capital, paliers PA/PM, heuristiques dénombrées) et lève l'ambiguïté de `base+parcho` (critère 4, V11, V12, V13, ÉCR-3, ÉCR-4).
- `## Ce que l'outil ne fait pas` borne l'indice de recherche, décrit la sélection du catalogue sans chiffre volatil, dit la coupe de l'URL en cas d'échec et renvoie wizard/base **sans lien** (critère 4, D-41, D-43, D-44).
- La page est complète et conforme : neuf sections dans l'ordre, un H1, ligne de retour, CRLF sans BOM, aucun bloc `console`, aucun lien externe ni vers une page inexistante (critère 5, V14, D-01, D-37).
- `.data/dofus.sqlite3` est intact après un rendu réel (taille, `mtime_ns`, SHA-256 identiques) et la mesure d'empreinte discrimine, démontré sur une copie temporaire (V15).
- La suite complète est verte (phases 1-2 comprises) et onze morsures rougissent le module sur une copie verte avant mutation.
</success_criteria>

## Couverture des sources (ce plan)

| Source | Élément | Consommé par |
|--------|---------|--------------|
| ROADMAP § Phase 3 | Critère 4 (hypothèses : points par niveau, paliers 40/100/150, heuristiques de classe, ni exo ni parchemins ; limites : indice ≠ combat, sélection du catalogue) | tâche 1 (hypothèses) et tâche 2 (limites) |
| ROADMAP § Phase 3 | Critère 5 (le contrôle détecte une dérive et **la suite reste verte**) | tâche 3 (complétude et conformité) et la vérification de chaque tâche |
| REQUIREMENTS | SIMP-04 | `requirements` du plan |
| CONTEXT | D-40 (chaque hypothèse est présentée avec la valeur réellement présente dans le code, et un contrôle vérifie qu'elle y figure encore) | tâche 1 (balayage et ancrage source) |
| CONTEXT | D-41 (une limite non adossée est retirée ou présentée comme une interprétation) | tâche 2 |
| CONTEXT | D-43 (ni les écrans du wizard ni le fonctionnement de la base locale) | tâche 2 (renvois en prose) |
| CONTEXT | D-44 (renvoi en prose, sans lien, tant que la page cible n'existe pas) | tâche 2 et tâche 3 (contrôle négatif de lien) |
| CONTEXT | D-42 (aucune constante écrite de mémoire) | tâches 1, 2 et 3 |
| CONTEXT | D-39 (le test d'exhaustivité reste vert sans exception) | tâche 3 (suite de la phase 1) |
| CONTEXT | D-45 (conventions des phases 1-2, y compris CRLF et UTF-8 sans BOM) | tâche 3 |
| RESEARCH | M10 (capital et paliers mesurés, heuristiques confirmées, ni exo ni parchemins, « parcho » à l'écran, indice borné par le solveur, sélection du catalogue, interdiction des nombres de catalogue) | tâches 1 et 2 |
| RESEARCH | M11 (fixture déterministe ; base réelle coûteuse et non déterministe) | tâche 3 (rendu réel unique et fixture) |
| RESEARCH | M12 (la base locale n'est pas modifiée par le harnais) | tâche 3 (empreinte) |
| RESEARCH | M13 (CRLF, UTF-8 sans BOM) | tâche 3 |
| PATTERNS | Patron `LIBELLES_SOURCE` (libellé + porteur sur la même ligne) | tâche 2 (ancrages source) |
| VALIDATION | V11, V12, V13, V14, V15 | tâches 1, 2 et 3 |
| VALIDATION | « Vérités non falsifiables » (comportement JS, formulation du H1) | tâche 3 (le H1 est libre ; seule sa correspondance avec l'index est testée par la phase 1) |

**Lignes `unresolved` (assumées)** : SIMP-04 est une exigence documentaire, sans prédicat évaluable par la sonde d'arêtes ; la couverture d'arêtes de ce plan est écrite à la main depuis V11, V12, V13, V14 et V15, et le balayage (huit niveaux de bordure, pas les 200) est annoncé comme tel dans le module.

## Contributions et sondes

| Élément | État | Emplacement |
|---------|------|-------------|
| Composants, libs, capabilities, fichiers réservés | — | Absents du dépôt : rien à brancher ni à déclarer |
| Tables canon | — | Ce plan n'écrit aucune ligne de table de suivi |
| Sonde jetable | `.gsd-tmp/plan-p3/` si l'exécuteur en écrit une | Non suivie, non indexée, jamais supprimée ; les morsures et la copie de démonstration travaillent dans un `mktemp -d` hors dépôt |
| Données du projet | — | `.data/` est **lu** et mesuré (empreinte), jamais écrit ; aucune suppression, aucun `db clear`, aucun drop |

## Artifacts this phase produces (ce plan)

- **Créés** : les sections `## Ce que l'outil suppose` et `## Ce que l'outil ne fait pas` de `docs/parcours-simplifie.md` (insérées avant `## Source de vérité`).
- **Créés dans `tests/test_docs_parcours.py`** : `NIVEAUX_BALAYES`, `_ensembles_de_classes`, `_empreinte`, et les tests `test_hypotheses_prouvees_par_balayage`, `test_limites_ancrees_sur_le_code`, `test_page_complete_et_sans_derive`, `test_empreinte_data_inchangee`.
- **État final de la phase** : `docs/parcours-simplifie.md` porte ses neuf sections épinglées ; `docs/sommaire.md` porte sa ligne d'index ; `tests/test_docs_parcours.py` porte l'ensemble des contrôles de la phase.
- **Non modifiés** : `dofus_stuff/**`, `tests/conftest.py`, `tests/test_web.py`, `tests/test_docs_structure.py`, `tests/test_docs_code_anchor.py`, `docs/cli.md`, `docs/installation.md`.

