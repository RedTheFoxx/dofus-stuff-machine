---
phase: 06-depannage-glossaire-completude-et-preuve-finale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/depannage.md
  - docs/sommaire.md
  - tests/test_docs_depannage.py
autonomous: true
requirements:
  - AIDE-01
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Chaque rubrique de `docs/depannage.md` s'adosse a un message **reellement produit** (`D-93`) : les cinq familles du critere 1 sont cinq sections de niveau 2, dans l'ordre du critere (`## Base absente ou vide`, `## Saisie invalide`, `## Calcul long`, `## Clavier inactif`, `## Resultat pagine`), et chaque entree de `MESSAGES` est citee verbatim dans le corps de la rubrique de sa famille — critere de succes 1, AIDE-01."
    - "Chaque message declare est confronte a sa **provenance reelle**, jamais a une memoire (`D-19`) : mode `cli_status` = sortie capturee de `_print_db_status` sur une base construite sous `tmp_path` (base vide et base peuplee de la fixture `app`), mode `web_render` = statut ou corps rendu par le client de test Flask **en processus**, mode `source_literal` = litteral lu dans le fichier producteur nomme ; un message non retrouve par sa provenance est un constat portant un motif nomme (`D-13`, `D-14`)."
    - "La famille « clavier inactif » est traitee comme un **mecanisme**, jamais comme un message (`D-76`, `06-RESEARCH.md` § A.4) : la rubrique cite `autofocus`, `activeElement` et `preventDefault`, le controle exige que chaque jeton soit reellement porte par le fichier nomme, et il exige que les trois ecrans `/version`, `/db/status` et `/quit` ne rendent **aucun** champ de saisie (`id=\"main-input\"` absent, mesure) ; `MESSAGES` ne porte **aucune** entree pour cette famille et cette absence est elle-meme controlee."
    - "Aucune valeur volatile, aucun chemin de poste dans la page (`D-73`, `D-75`) : le controle refuse tout nombre de quatre chiffres ou plus et tout motif de chemin absolu, alors que la sortie reellement capturee porte `Fichier : ` suivi d'un chemin de dossier temporaire — la page cite donc les **libelles**, jamais la ligne entiere."
    - "Aucune duree d'attente n'est promise (`D-19`) : la rubrique « calcul long » cite le **seul** message d'attente du produit et le **budget** de calcul (l'option `--time-limit` et le champ rendu `DUREE`), et le controle refuse les jetons de promesse de duree portes par `JETONS_PROMESSE_DUREE`."
    - "La page respecte le gabarit verrouille (`D-102`) : un seul H1 egal au libelle d'index (`# Dépannage`), une ligne de retour `[Retour au sommaire](sommaire.md)` en derniere ligne non vide, octets CRLF sans BOM, un bloc `## Source de vérité` dont chaque chemin cite existe depuis la racine du depot, aucun lien externe, aucun jeton de brouillon."
    - "`docs/sommaire.md` croit d'**exactement une** ligne d'index, ajoutee en fin de table, et la liste « Parcours conseillé » n'est **pas** convertie en liens : mesure de cette session — `problemes_index`, `problemes_h1` et `test_sommaire_index_labels_are_unique` lisent **tout** lien de `sommaire.md` comme une entree d'index, donc un parcours en liens re-emettrait les libelles de l'index et ferait rougir trois gardes vertes ; le parcours reste du texte et sa garde est livree au plan 06-02."
    - statement: "La prose libre de la page (l'invitation a chercher par le message, la clarte des renvois) n'est pas verifiee par un controle : les constats portent sur les rubriques, les messages et les valeurs **cites**, et ce plan ne revendique aucune exhaustivite de redaction (`D-85`). Non confirmable au-dela : backstop explicite."
      verification: backstop
  artifacts:
    - path: "docs/depannage.md"
      provides: "La page unique du sujet : les cinq rubriques du critere 1 (base absente ou vide, saisie invalide, calcul long, clavier inactif, resultat pagine), chacune indexee par le message reellement produit, plus le renvoi vers la page qui definirait deja la meme chose (`D-17`) et le bloc Source de verite"
      contains: "## Source de vérité"
      min_lines: 90
    - path: "docs/sommaire.md"
      provides: "La ligne d'index « Dépannage » en fin de table `## Index`, sans aucune autre modification et sans conversion du parcours conseille en liens"
      contains: "| [Dépannage](depannage.md) |"
      min_lines: 24
    - path: "tests/test_docs_depannage.py"
      provides: "Le module d'ancrage message → rubrique : docstring de contrat et de limites, garde de cloture auto-analysee, table `MESSAGES` de provenance, fonctions pures `problemes_rubriques`, `problemes_messages`, `problemes_page` et leurs tests, plus les deux controles structurels (mecanisme du clavier, deux formes de la pagination)"
      contains: "def test_messages_de_base_absente_ou_vide"
      min_lines: 220
  key_links:
    - from: "tests/test_docs_depannage.py"
      to: "dofus_stuff/cli.py"
      via: "capture reelle de `_print_db_status` sur une base `tmp_path` (base vide **et** base peuplee de la fixture `app`), et lecture des litteraux de refus que seule `main()` imprime"
      pattern: "_print_db_status"
    - from: "tests/test_docs_depannage.py"
      to: "dofus_stuff/web/routes.py"
      via: "client de test Flask en processus : statut et corps rendus des refus (recherche, liste, objet, systeme, base, wizard) et de l'ecran de niveau du flux simplifie conduit pas a pas"
      pattern: "follow_redirects"
    - from: "tests/test_docs_depannage.py"
      to: "dofus_stuff/web/static/js/terminal.js"
      via: "lecture du fichier pour les jetons du mecanisme du clavier (`activeElement`, `preventDefault`) : aucun navigateur, aucun DOM, l'execution JS n'est pas revendiquee (`D-85`)"
      pattern: "activeElement"
    - from: "docs/depannage.md"
      to: "docs/sommaire.md"
      via: "la ligne d'index ajoutee dans la table et la derniere ligne de retour `[Retour au sommaire](sommaire.md)`"
      pattern: "sommaire.md"
    - from: "docs/depannage.md"
      to: "docs/installation.md"
      via: "les renvois (`D-17`) : le mecanisme du clavier et l'erreur d'arguments globale sont deja decrits la, la rubrique y renvoie au lieu de les redefinir"
      pattern: "installation.md"
  prohibitions:
    - statement: "Ne jamais modifier `dofus_stuff/**` pour aligner la documentation : le code est le referentiel, la page s'y conforme (`D-103`)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais ecrire, vider, supprimer, renommer ni ouvrir par SQLite quoi que ce soit sous `.data/` : la base du depot n'est jamais lue, et toute base construite par un controle l'est sous `tmp_path` (`D-104`)."
      status: resolved
      verification: test
    - statement: "Ne jamais executer `main()`, lancer le solveur, ouvrir une socket ou une connexion reseau depuis un controle, ni poster un formulaire de confirmation (`D-103`, `D-104`)."
      status: resolved
      verification: test
    - statement: "Ne jamais epingler une valeur volatile dans la prose de la page — chemin de poste, dossier temporaire, horodatage, compteur d'objets, identifiant d'objet (`D-73`, `D-75`)."
      status: resolved
      verification: test
    - statement: "Ne jamais inventer un message pour une famille qui n'en produit pas, ni promettre une duree d'attente que le produit n'expose pas (`D-19`, `D-76`)."
      status: resolved
      verification: test
    - statement: "Ne jamais publier, deployer a distance, pousser vers un depot distant ni effectuer d'achat : commits locaux seulement, indexation par chemin explicite, jamais `git add .` (`D-105`)."
      status: resolved
      verification: judgment
  assumptions:
    - "**Sonde d'arete spec-less : l'orchestrateur n'a PAS execute la sonde deterministe de la section A** (aucun SPEC de phase fourni : `EDGE_ABSENT` / `PROHIB_ABSENT`). La liste d'aretes de ce plan est donc **derivee par le planificateur** des cinq criteres de succes du ROADMAP § Phase 6, des textes de AIDE-01 et de l'inventaire mesure de `06-RESEARCH.md` § Measured inventory A. Chaque arete classable a recu un critere confirmable ci-dessus ; la prose libre est ecrite en **backstop** (dernier element de `truths`) et non en vert silencieux ; les six prohibitions retenues sont au bloc `prohibitions`, aucune n'est ecartee en silence."
    - "**Corrections de mesure apportees par la planification (elles prevalent sur `06-RESEARCH.md` la ou elles divergent)** : (a) `POST /optimize/quick/niveau` avec un niveau hors bornes ne rend **pas** le refus `Saisissez un niveau entre 1 et 200.` si l'etape n'est pas precedee de la classe et des elements — mesure de cette session : sans session conduite, le statut rendu est `ENTREE=SUIVANT` ; conduit pas a pas (`POST /optimize/quick/classe` puis `POST /optimize/quick/elements`), le meme POST rend `Saisissez un niveau entre 1 et 200. — ENTREE=CALCULER`. Le controle doit donc **conduire le flux** avant de sonder le refus. (b) `GET /db/status` sur une base vide ne porte **aucun** message dans sa ligne de statut (mesure : `&nbsp;`) : les libelles `VERSION JEU : (aucune)`, `DERNIER CHECK : (AUCUN)` et `ENTREES : 0` sont des **lignes de corps**, donc le lecteur local doit extraire le corps, pas le statut. (c) `DERNIER CHECK : (AUCUN)` est a `dofus_stuff/web/routes.py:763` (et non `:762`), et `Version jeu : (aucune)` / `Entrées : 0` sont **composes** par f-string (`cli.py:217`, `:224`) : ils ne sont prouvables que par capture, jamais par litteral de source."
    - statement: "Le rendu Markdown des pages hors GitHub n'est pas observable en processus (aucun moteur de rendu installe, publication distante interdite) : declare comme limite, aucune validation humaine n'est revendiquee (`D-85`)."
      verification: backstop
    - statement: "Le comportement d'un navigateur reel (DOM, focus apres `preventDefault`) n'est pas observable : la rubrique du clavier s'arrete au mecanisme lu dans `terminal.js` et `screen.html` et au rendu des ecrans sans champ (`D-85`)."
      verification: backstop
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 6 (« le lecteur trouve une réponse en cherchant par message d'erreur ou par terme, et la documentation livrée est prouvée complète, non destructive et gardée par des contrôles qui échouent réellement quand une cible dérive. ») et de son critère de succès 1, plus AIDE-01 et D-92/D-93 de `06-CONTEXT.md` :
**En tant que** lecteur qui vient de voir un message à l'écran et ne veut pas lire le code, **je veux** retrouver la rubrique de dépannage correspondant exactement au message affiché, sur la surface où il apparaît, **afin de** savoir quoi faire sans deviner ni chercher dans le dépôt.

Mode MVP : le `**Goal:**` du ROADMAP n'est pas redige en recit utilisateur. Le cadrage ci-dessus en est **derive** (comme aux plans 04-01 et 05-01 de la phase precedente), jamais invente : il ne reformule que le critere 1 et des decisions verrouillees. Aucun retour a `/gsd mvp-phase` n'est demande.

Purpose : c'est **AIDE-01** et le critere 1 — la page de depannage **indexee par le message reellement produit** (`D-93`), et la sortie d'ou un lecteur cherche : les cinq familles du critere (base absente ou vide, saisie invalide, calcul long, clavier inactif, resultat pagine). La page est ecrite **depuis une lecture `fichier:ligne` faite pendant la redaction** (`D-19`) : chaque message est mesure sur la fonction de production (`_print_db_status`), sur le rendu du client de test Flask en processus, ou sur le litteral du fichier qui le porte. Un message plausible mais faux est le defaut que ce jalon a deja paye deux fois : il est donc traite ici comme le risque principal, avec un controle par message et par provenance.

Output : `docs/depannage.md` (les cinq rubriques, le bloc Source de verite et la ligne de retour), la ligne d'index `Dépannage` de `docs/sommaire.md`, et `tests/test_docs_depannage.py` (garde de cloture auto-analysee, table `MESSAGES` de provenance, fonctions pures de constats, controles du mecanisme du clavier et des deux formes de la pagination).

Ordonnancement declare : **vague 1**, `depends_on: []` — c'est le premier plan de la phase, et il ouvre la tranche verticale (page + index + module d'ancrage + messages du produit mesure) que 06-02, 06-03 et 06-04 etendent ensuite.

**Ce que ce plan ne fait pas** : il n'ecrit ni `docs/glossaire.md` ni le parcours conseille final (plan 06-02), ni la liste epinglee des 8 pages (plan 06-03), ni la preuve de mutation du harnais, l'empreinte de `.data/`, l'audit de perimetre `git status` et l'execution finale (plan 06-04) ; il ne modifie **aucun** fichier de `dofus_stuff/**` (`D-103`), ne touche ni `README.md` ni `GUIDE_WIZARD.md`, et n'ouvre aucune resynchronisation.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-VALIDATION.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-01-PLAN.md
@.claude/CLAUDE.md
@docs/sommaire.md
@docs/installation.md
@docs/parcours-simplifie.md
@docs/wizard-avance.md
@docs/cli.md
@docs/base-locale.md
@tests/conftest.py
@tests/test_docs_structure.py
@tests/test_docs_base_locale.py
@tests/test_docs_wizard.py
@dofus_stuff/cli.py
@dofus_stuff/sync.py
@dofus_stuff/web/routes.py
@dofus_stuff/web/optimize_wizard.py
@dofus_stuff/web/templates/screen.html
@dofus_stuff/web/static/js/terminal.js
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : tranche verticale « la page existe, elle est indexee, et la premiere famille de messages est ancree sur le produit »</name>
  <files>docs/depannage.md, docs/sommaire.md, tests/test_docs_depannage.py</files>
  <reversibility rating="reversible">Creer une page `docs/`, une ligne d'index et un module de test est annulable par un commit de retour ; rien n'est publie, rien n'est deplace hors du depot, `.data/` n'est pas approche.</reversibility>
  <read_first>
    docs/sommaire.md
    docs/installation.md
    docs/parcours-simplifie.md
    docs/wizard-avance.md
    docs/cli.md
    docs/base-locale.md
    tests/conftest.py
    tests/test_docs_structure.py
    tests/test_docs_base_locale.py
    tests/test_docs_wizard.py
    dofus_stuff/cli.py
    dofus_stuff/sync.py
    dofus_stuff/web/routes.py
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
    .gsd-tmp/mesure-06-plan.py
    .claude/CLAUDE.md
  </read_first>
  <action>Ecrire la tranche la plus fine qui traverse **toutes** les couches de la phase — page `docs/`, index du sommaire, module d'ancrage neuf, messages du produit mesures — et la rendre verifiable de bout en bout avant toute expansion. `.gsd-tmp/mesure-06-plan.py` est le script de mesure de la planification (hors livrable, non suivi par git) : le rejouer avec l'interpreteur epingle avant d'ecrire, puis appeler la lecture `fichier:ligne` pour chaque message cite (`D-19`).

**Page** (`docs/depannage.md`, nouveau fichier, UTF-8 sans BOM, fins de ligne CRLF comme les six pages livrees — convention mesuree) — premiere version **livrable**, pas une ebauche :
- un `H1` exactement `# Dépannage` : c'est le libelle d'index exige par le gabarit (`D-102`), et il correspond deja a l'entree `6. Dépannage` du parcours conseille (garde livre au plan 06-02) ;
- une phrase d'introduction en francais qui dit **comment se servir de la page** : on cherche par le **message affiche** (`D-93`), l'outil a deux surfaces (ligne de commande et interface web) et les deux n'ecrivent pas toujours la meme chaine, et une famille de symptomes — la saisie qui semble sans effet — n'affiche **aucun** message : c'est un mecanisme, traite comme tel ; renvoi vers [le sommaire](sommaire.md) et vers les pages qui detaillent chaque surface ;
- `## Base absente ou vide` : les messages **mesures** de cette famille, chacun entre accents graves, avec la surface ou il apparait et le geste attendu ;
  * ligne de commande (capture reelle de `_print_db_status` sur une base vide) : `Version jeu : (aucune)`, `Dernier check : (aucun)`, `Entrées : 0` — et l'absence de la ligne `Par catégorie :` sur une base vide, signalee comme conditionnelle ; la page precise que le premier contact **cree** le fichier (renvoi vers la page de la base locale, qui le decrit en detail) ;
  * ligne de commande (litteraux de refus, lus dans le fichier) : `Erreur : aucune version en base` (`dofus_stuff/cli.py`) et le refus de synchronisation hors-ligne (`dofus_stuff/sync.py`) ;
  * interface web (corps et statut rendus) : `VERSION JEU : (aucune)`, `DERNIER CHECK : (AUCUN)`, `ENTREES : 0` en **lignes de corps** de l'ecran d'etat (sa ligne de statut est vide — mesure de cette session), et, sur l'ecran de version, le statut `AUCUNE VERSION EN BASE` avec le corps `ERREUR : AUCUNE VERSION EN BASE.` ;
  * renvois (`D-17`) : la rubrique « base absente » de `docs/installation.md` et le passage hors-ligne de `docs/cli.md` disent deja quoi faire — la page **renvoie** vers eux, elle ne les reecrit pas ;
  * **forme extractible** : chaque message de la rubrique occupe une ligne de tableau dont la **premiere cellule** est le message entre accents graves — colonnes `Message affiché`, `Où il apparaît`, `Ce qu'il faut faire`. La page reste lisible et devient extrayible sans heuristique : le controle apparie exactement, dans les deux sens, les lignes citees et la table `MESSAGES` du module ;
- `## Source de vérité` : un chemin **existant** par ligne, entre accents graves — `dofus_stuff/cli.py`, `dofus_stuff/sync.py`, `dofus_stuff/web/routes.py` (`D-03`, `D-69`) — aucun chemin invente ;
- derniere ligne non vide, exactement `[Retour au sommaire](sommaire.md)` ; aucun lien externe, aucun jeton de brouillon, aucune valeur volatile (ni chemin de poste, ni dossier temporaire, ni compteur) : la sortie capturee porte un chemin de dossier temporaire, il n'est **jamais** recopie.

**Index** (`docs/sommaire.md`) — ajouter **une seule** ligne, en fin de table `## Index`, dans la forme des cinq lignes existantes : `| [Dépannage](depannage.md) | ... |`, deuxieme colonne decrivant le sujet en une proposition. Deux contraintes mesurees : la liste « Parcours conseille » **reste du texte simple** (toute conversion en liens re-emettrait les libelles de l'index et ferait rougir `problemes_index`, `problemes_h1` et `test_sommaire_index_labels_are_unique`, qui lisent tout lien du sommaire comme une entree d'index), et aucune ligne existante n'est deplacee ni reformulee (`D-102`).

**Module** (`tests/test_docs_depannage.py`, nouveau fichier) — squelette **complet et definitif** de l'outillage ; les taches 2 et 3, puis le plan 06-04, n'auront qu'a l'etendre :
- docstring de module sur le patron de `tests/test_docs_base_locale.py` : le contrat va **du produit vers la page** (capture de la fonction de production, rendu du client de test Flask en processus, litteraux lus dans le fichier producteur), aucun serveur n'est lance, aucune socket ouverte, `main()` jamais execute, rien n'est ecrit sous `.data/` ; puis les **limites nommees** (`D-85`) : l'execution JavaScript et le DOM ne sont pas observables (le mecanisme du clavier est lu, pas execute), le rendu Markdown hors GitHub n'est pas observable, la prose libre de la page n'est pas verifiee, l'assertion CRLF depend de `core.autocrlf` du poste, et la comparaison de provenance est une **appartenance de sous-chaine** sur le texte normalise (un jeton cite qui serait contenu dans un autre passerait — limite mesuree, ecrite) ;
- imports : `ast`, `re`, `hashlib`, `pathlib.Path`, `pytest`, la fixture partagee `normalize`, `from dofus_stuff.database import Database`, `from dofus_stuff.cli import _print_db_status`, `from dofus_stuff.catalog import Catalog`, `from dofus_stuff.web import create_app`. **Aucun** import de `sqlite3`, `subprocess`, `socket`, `webbrowser`, `urllib`, `requests` : la garde de cloture ci-dessous les refuse ;
- constantes de chemin : `RACINE_DEPOT = Path(__file__).resolve().parents[1]`, `PAGE = "depannage.md"`, `SOMMAIRE = "sommaire.md"`, plus une constante par source citee (`SOURCE_CLI`, `SOURCE_SYNC`, `SOURCE_ROUTES`, `SOURCE_TERMINAL_JS`, `SOURCE_ECRAN_HTML`, `SOURCE_OPTIMIZE_WIZARD`) ;
- `RUBRIQUES = ((cle, titre, ...))` : les cinq rubriques de niveau 2 dans l'ordre du critere 1, la **cle** etant stable et ASCII (`base`, `saisie`, `calcul`, `clavier`, `pagination`) et le titre francais accentue ; `RUBRIQUES_MECANISME = ("clavier",)` — la famille sans message (`D-76`) ;
- `MESSAGES = ((message, cle_rubrique, mode, source), ...)` : au depart, les dix entrees de la famille `base` ; `mode` vaut `cli_status`, `web_render` ou `source_literal`, et `source` est le chemin relatif a la racine du depot pour le mode `source_literal` (sinon `None`). Chaque ligne est **mesuree** avant d'etre ecrite : la provenance est la fonction de production, pas le nom du fichier a la ligne près ;
- marqueurs du rendu repris **a l'identique** de `tests/test_docs_wizard.py:91-94` (`MARQUEUR_CORPS`, `MARQUEUR_STATUT`, `LIGNE_CORPS`) et lecteurs locaux `_texte_rendu`, `_lignes_du_corps`, `_statut` : le corps est extrait entre `id="body">` et la ligne de statut, **jamais** la reponse entiere ; une page ou une reponse absente leve un `AssertionError` localisant (`D-13`), jamais une exception brute. Les helpers partages de `tests/conftest.py` (`docs_dir`, `normalize`, `section`, `sections`, `lignes_de_code`) sont **utilises**, jamais recopies (`D-12`) ; les trois lecteurs de rendu sont locaux au module, comme la phase 5 l'a fait, et la docstring le dit (un lecteur local n'est pas un helper partage) ;
- `_produire(client, tmp_path) -> dict[str, str]` : la **sonde unique** qui construit l'ensemble des chaines reellement produites, associees a leur provenance lisible. Elle (a) capture `_print_db_status` sur une base **vide** construite sous `tmp_path` (le dossier n'existe pas encore : l'appel cree le fichier) et sur la base **peuplee** de la fixture `app` (`client.application.extensions["web_config"]["data_dir"]`), avec `db.close()` dans un `finally` — sans quoi le nettoyage de `tmp_path` echoue sur Windows ; (b) rend, par le client de test Flask, le statut et le corps de `GET /version`, `GET /db/status` et `GET /self-test` sur une application a catalogue vide construite par `create_app(data_dir=tmp_path / "...", offline=True, catalog=Catalog(version=None, items={}), load_catalog=False)` ; (c) sonde les refus par `client.post(..., follow_redirects=True)` ; (d) lit les litteraux de source dans les fichiers nommes. Chaque sonde **verifie sa propre prise** (route rendue 200 ou redirection suivie, texte non vide, message attendu present) et leve un `AssertionError` localisant si elle ne prend rien : une sonde muette ne doit jamais passer pour un vert ;
- constantes de **motif de morsure**, portees par le module et **jamais** ecrites en clair dans une ligne d'assertion (pytest reproduit la ligne source de l'assert ; regle posee au plan 03-03) — valeurs ASCII, sans apostrophe, chacune incluse dans le constat qui la concerne : `MOTIF_RUBRIQUE_ABSENTE = "rubrique de depannage absente"`, `MOTIF_RUBRIQUE_VIDE = "rubrique de depannage sans message"`, `MOTIF_MESSAGE_ABSENT = "message non cite par sa rubrique"`, `MOTIF_MESSAGE_INVENTE = "message cite non declare"`, `MOTIF_MESSAGE_NON_PRODUIT = "message non produit par le code"`, `MOTIF_MECANISME = "mecanisme du clavier"`, `MOTIF_CHAMP_PRESENT = "champ de saisie inattendu"`, `MOTIF_VOLATILES = "valeur volatile"`, `MOTIF_CHEMIN_ABSOLU = "chemin absolu"`, `MOTIF_PROMESSE_DUREE = "promesse de duree"`, `MOTIF_CRLF = "fins de ligne"`, `MOTIF_SOURCE = "chemin de source absent"`, `MOTIF_GARDE = "garde de cloture du harnais"` ;
- `MOTIF_VOLATILE = re.compile(r"\d{4,}")` et `MOTIF_CHEMIN_POSTE = re.compile(r"[A-Za-z]:[\\/]")` : les deux refus de valeur volatile et de chemin de poste (`D-73`, `D-75`) ;
- `JETONS_PROMESSE_DUREE` : le tuple des formulations qui promettraient une duree d'attente — cinq formulations francaises courantes, **ecrites dans le module uniquement**, jamais recopiees dans la page ni presentees comme des messages produits (`D-19`) ;
- fonctions **pures** de constats (`D-13`, patron `problemes_*` de `tests/test_docs_structure.py`) : `problemes_rubriques(docs_dir) -> list[str]` (chaque rubrique declaree existe, est non vide, dans l'ordre declare ; une rubrique de `RUBRIQUES_MECANISME` ne porte **aucune** entree de `MESSAGES`, et toute rubrique hors mecanisme en porte au moins une), `problemes_messages(docs_dir, produites) -> list[str]` (chaque message declare est cite **dans le corps de sa rubrique** et retrouve par sa provenance : presence dans `produites` pour `cli_status` et `web_render`, presence du litteral dans le fichier nomme pour `source_literal` ; le constat cite le message, la rubrique, la provenance et le fichier producteur), et `problemes_page(docs_dir) -> list[str]` (CRLF et absence de BOM sur les octets — les gardes de structure ne les verifient pas, mesure de cette session —, aucun nombre de quatre chiffres ou plus, aucun motif de chemin absolu, aucun jeton de `JETONS_PROMESSE_DUREE`, chaque chemin entre accents graves du bloc `## Source de vérité` existant depuis la racine du depot) ;
- garde de cloture **reecrite** (jamais recopiee) : `RACINES_INTERDITES` (`sqlite3`, `subprocess`, `socket`, `multiprocessing`, `ctypes`, `webbrowser`, `urllib`, `requests`, `http`, `ftplib`, `smtplib`), `APPELS_SUPPRESSION` (`remove`, `unlink`, `rmdir`, `rmtree`), `APPEL_PRODUIT = "main"`, `APPELS_CALCUL_PRODUIT = ("optimize_stuff", "_run_optimize_and_redirect")` — le solveur n'est jamais lance par un controle — et `CHEMINS_DESTRUCTIFS = ("/db/clear", "/db/sync")` : tout appel de type `post(...)` du controle dont le chemin commence par l'un de ces deux chemins est un constat portant `MOTIF_CHEMIN_DESTRUCTIF = "chemin destructif"` (mesure de cette session : les deux seuls points d'entree de l'interface web qui visent la base locale ou le reseau sont `POST /db/clear` et `POST /db/sync` ; l'ecran de sortie est en `GET` seulement et repond 405 sur `POST`, donc aucun controle ne l'appelle) ; plus le resserrement de la phase 5 : tout appel construisant une base (`Database`, `create_app`) recoit un `data_dir` derive de `tmp_path` (le repertoire de la fixture `app` est admis), et `DEFAULT_DATA_DIR` n'est jamais passe — constat portant `MOTIF_DATA_DIR`. Les helpers `_imports_du_module` et `_appels_du_module` sont repris du patron (`ast.walk` de l'arbre entier, imports imbriques compris) et le test `test_garde_de_cloture_du_harnais` **s'auto-analyse** par `ast.parse(Path(__file__).read_text(encoding="utf-8"))` — jamais une recherche de chaines, le module citant lui-meme les noms interdits dans ses messages ;
- tests de la tranche : `test_garde_de_cloture_du_harnais()`, `test_rubriques_de_depannage(docs_dir)` (appelle `problemes_rubriques`), `test_messages_de_base_absente_ou_vide(docs_dir, client, tmp_path)` (appelle `_produire` puis `problemes_messages`) et `test_page_close_et_sans_valeur_volatile(docs_dir)` (appelle `problemes_page`). Constats accumules dans `constats: list[str]` et joints a **une seule** assertion `assert not constats` ; chaque constat suit le message de `D-13` : `{page} : <ce qui est faux> ; attendu <valeur> <producteur, fichier de code ou ligne>`.

Contraintes de forme : aucun bloc de code balise `console` dans la page, aucun lien externe, aucun jeton de brouillon (les gardes de la phase 1 les refusent des que la page entre dans `docs/`). Le module n'execute jamais `main()`, n'importe jamais `sqlite3` et n'ecrit rien sous `.data/`.
  </action>
  <acceptance_criteria>
    - `docs/depannage.md` existe, commence par `# Dépannage`, porte `## Base absente ou vide` et `## Source de vérité`, et sa derniere ligne non vide est `[Retour au sommaire](sommaire.md)`.
    - La rubrique « base absente ou vide » cite `Version jeu : (aucune)`, `Dernier check : (aucun)`, `Entrées : 0`, `VERSION JEU : (aucune)`, `DERNIER CHECK : (AUCUN)`, `ENTREES : 0`, `AUCUNE VERSION EN BASE`, `ERREUR : AUCUNE VERSION EN BASE.`, `Erreur : aucune version en base` et le refus de synchronisation hors-ligne, chacun adosse a sa provenance.
    - La page ne contient aucun nombre de quatre chiffres ou plus, aucun motif de chemin absolu, et ne recopie pas la ligne `Fichier : ` de la sortie capturee.
    - Chaque message cite par la rubrique « base absente ou vide » occupe une ligne de tableau dont la premiere cellule est le message entre accents graves, et le controle apparie ces lignes et la table `MESSAGES` dans les deux sens (message declare non cite, et message cite non declare, sont deux constats distincts).
    - `docs/sommaire.md` porte la ligne `| [Dépannage](depannage.md) |` en fin de table, la liste « Parcours conseillé » est **restee du texte simple**, et aucune ligne existante n'a bouge.
    - `tests/test_docs_depannage.py` porte `problemes_rubriques`, `problemes_messages`, `problemes_page`, `_produire`, `MESSAGES`, `RUBRIQUES`, `RUBRIQUES_MECANISME` et `test_garde_de_cloture_du_harnais`.
    - Retirer une rubrique de la page, deroger un message de sa rubrique, ou remplacer un message par une variante accentuee non produite rougit le module.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_depannage.py tests/test_docs_structure.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in rubrique_retiree message_supprime message_invente source_renommee; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_depannage.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in rubrique_retiree) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();open(p,"w",encoding="utf-8",newline="").write(s.replace("## Base absente ou vide","## Base absente ou inutilisable",1))' "$T/docs/depannage.md"; motif='rubrique de depannage absente';; message_supprime) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();L=[l for l in s.splitlines(True) if "Version jeu : (aucune)" not in l];open(p,"w",encoding="utf-8",newline="").write("".join(L))' "$T/docs/depannage.md"; motif='message non cite par sa rubrique';; message_invente) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();anc="## Base absente ou vide";i=s.index(anc)+len(anc);j=s.index("\r\n",i)+2;ligne="| `VERSION JEU : ABSENTE` | ligne de commande | rien a faire |\r\n";open(p,"w",encoding="utf-8",newline="").write(s[:j]+ligne+s[j:])' "$T/docs/depannage.md"; motif='message cite non declare';; source_renommee) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();open(p,"w",encoding="utf-8",newline="").write(s.replace("Base locale vide et --offline : impossible de synchroniser","Base locale vide",1))' "$T/dofus_stuff/sync.py"; motif='message non produit par le code';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 06-01 (TACHE 1) EN ECHEC"; exit 1; fi; echo "morsures 06-01 tache 1 : 4/4 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 06-01 (TACHE 1) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>La page existe, elle est indexee, la rubrique « base absente ou vide » est ancree sur la capture reelle des deux surfaces, le module porte ses fonctions pures et sa garde de cloture auto-analysee, trois derives epinglees font rougir la suite sur une copie verte avant mutation, et la suite entiere reste verte.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : les deux familles a messages atteignables — « saisie invalide » et « calcul long »</name>
  <files>docs/depannage.md, tests/test_docs_depannage.py</files>
  <read_first>
    docs/depannage.md
    docs/parcours-simplifie.md
    docs/wizard-avance.md
    docs/cli.md
    tests/test_docs_depannage.py
    tests/test_docs_wizard.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/optimize_wizard.py
    dofus_stuff/optimize/profile_input.py
    dofus_stuff/cli.py
    .gsd-tmp/mesure-06-plan.py
  </read_first>
  <behavior>
    - `SAISIE REQUISE` rendu par `POST /search` avec une recherche vide est cite par la rubrique « saisie invalide » et retrouve dans le statut rendu.
    - `Saisissez un niveau entre 1 et 200.` est rendu par `POST /optimize/quick/niveau` **seulement** quand le flux a ete conduit par la classe puis les elements : le controle conduit le flux et ne poste jamais un niveau valide (aucun calcul n'est lance).
    - `Calcul en cours (CP-SAT)…` est un litteral de `dofus_stuff/cli.py` et `ENTREE=CALCULER` est rendu par l'ecran de niveau ; aucun des deux n'est une duree.
  </behavior>
  <action>Etendre la page et le module aux deux familles dont les messages sont atteignables en processus.

**Page** — deux sections de niveau 2, inserees **juste avant** `## Source de vérité`, dans l'ordre du critere 1 :
- `## Saisie invalide` : les refus **rendus**, chacun dans sa ligne de tableau (forme extractible de la tache 1), avec la surface et le geste attendu ;
  * refus de la recherche : `SAISIE REQUISE` (recherche vide), `LIMITE INVALIDE` (limite illisible), `AUCUN RESULTAT.` (aucun objet trouve) ;
  * refus d'identification : `ID INVALIDE — ENTIER ATTENDU` (identifiant non numerique), et le message d'objet introuvable **sous sa forme produite** — `ÉQUIPEMENT INTROUVABLE` en majuscules, suivi de l'identifiant, parce que le rendu passe le message du catalogue en majuscules (`D-19` : la page ne cite pas la forme minuscule du code) ; l'identifiant lui-meme est une **valeur d'execution** et n'est jamais epingle (`D-75`) ;
  * refus de choix d'ecran : `OPTION INVALIDE — SAISIR 1 A 5` (menu racine), `OPTION INVALIDE — SAISIR 1 A 4` (ecran systeme) et `OPTION INVALIDE — SAISIR 1 A 3` (ecran de la base) — trois bornes pour trois ecrans, mesurees, presentees comme telles et non comme une borne unique ; l'ecran de sortie n'est **pas** de cette famille et la page ne lui attribue aucun message : mesure de cette session, il accepte seulement `GET` et repond 405 sur `POST`, sans ligne de statut — un controle qui lui preterait un message serait muet, ce que `_produire` refuse (`MOTIF_MESSAGE_NON_PRODUIT`) ;
  * refus du flux simplifie : `Saisissez le nom ou le numéro de votre classe.`, `Exemple : feu, terre air, ou multi.` et `Saisissez un niveau entre 1 et 200.` — la page dit que ces refus appartiennent **au flux** (l'etape precedente doit etre renseignee) et renvoie (`D-17`) a `docs/parcours-simplifie.md`, qui porte deja la table des saisies et de leurs refus ;
  * refus du wizard : `SAISIE INVALIDE` (etape des emplacements), avec renvoi a `docs/wizard-avance.md` ;
  * refus de la ligne de commande : `Saisie invalide — voir aide syntaxe` et, en prose, le renvoi vers `docs/cli.md` pour l'erreur d'arguments produite par l'analyseur (`D-17` : la page ne la reecrit pas) ;
- `## Calcul long` : le **seul** message d'attente du produit et le **budget**, jamais une duree ;
  * `Calcul en cours (CP-SAT)…` pour la ligne de commande, `ENTREE=CALCULER` pour l'interface web — la page nomme les deux surfaces (le web ne porte aucun message d'attente, il porte une ligne de statut) ;
  * le budget : l'option `--time-limit` et les champs `DUREE` du recapitulatif du wizard, renvoi vers `docs/wizard-avance.md` qui explique deja les reglages ;
  * la fin du calcul : `CALCUL TERMINE`, presente comme ce que le lecteur voit **a la fin** (le calcul est synchrone) ;
  * la page ne promet **aucune** duree d'attente : ce que le produit expose est un budget et un etat (`D-19`, `D-76`), et le seul jeton qui pourrait promettre une duree est refuse par le controle de la tache 1.

**Module** :
- `MESSAGES` gagne les entrees des deux nouvelles rubriques, chacune avec sa provenance — `web_render` pour les refus rendus, `source_literal` pour ce que seul `main()` ou une execution imprimeraient : le mode `source_literal` prouve que la chaine est un litteral du fichier nomme, **pas** qu'elle est atteinte a l'execution, et la docstring du module ecrit cette limite (le produit n'est jamais execute : `main()` imprimerait l'attente en lancant le calcul, et le calcul est justement ce qu'un controle ne doit pas lancer) ;
- `_produire` gagne les sondes correspondantes, chacune verifiant sa propre prise : les refus par `client.post(..., follow_redirects=True)` (recherche vide, limite illisible, recherche sans resultat, identifiant illisible sur la liste et sur l'objet, identifiant inconnu, choix d'ecran des trois ecrans de menu et d'etat, etape des emplacements du wizard), et le **flux simplifie conduit pas a pas** — `POST /optimize/quick/classe` puis `POST /optimize/quick/elements` avant de sonder l'etape du niveau, sans quoi le refus du niveau n'est pas atteint (correction de mesure de ce plan) ; le controle ne poste **jamais** un niveau valide, car ce POST lance le calcul : seul un niveau hors bornes est poste, ce qui prouve aussi que le refus est rendu **avant** tout calcul ;
- `_produire` leve un `AssertionError` portant `MOTIF_MESSAGE_NON_PRODUIT` et nommant la route sondee des qu'un message declare en mode `cli_status` ou `web_render` n'est pas observe : une sonde muette est un constat, jamais un vert silencieux ; les messages de calcul long atteignables (`ENTREE=CALCULER`, les champs `DUREE` du recapitulatif) sont sondes par des `GET` seulement ;
- `problemes_messages` reste inchange : il confronte page, table declaree et `produites`, dans les deux sens.
  </action>
  <acceptance_criteria>
    - La page porte `## Saisie invalide` et `## Calcul long`, toutes deux avant `## Source de vérité`, chacune avec ses lignes de message a premiere cellule entre accents graves.
    - Les trois bornes de `OPTION INVALIDE — SAISIR 1 A N` sont citees et la page ne presente aucune d'elles comme la borne unique du produit.
    - Le message d'objet introuvable est cite sous sa forme majuscule produite, sans identifiant epingle.
    - Le controle du refus de niveau conduit le flux (classe puis elements) et ne poste aucun niveau valide : `optimize_stuff` n'est jamais appele.
    - La rubrique « calcul long » ne contient aucun jeton de `JETONS_PROMESSE_DUREE` et cite `--time-limit` ainsi qu'un champ `DUREE`.
    - Supprimer une ligne de message de la rubrique « saisie invalide », ou renommer le refus de niveau dans `dofus_stuff/web/routes.py`, rougit le module.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_depannage.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in saisie_hors_rubrique refus_niveau_renomme; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_depannage.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in saisie_hors_rubrique) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();L=[l for l in s.splitlines(True) if "SAISIE REQUISE" not in l];open(p,"w",encoding="utf-8",newline="").write("".join(L))' "$T/docs/depannage.md"; motif='message non cite par sa rubrique';; refus_niveau_renomme) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();open(p,"w",encoding="utf-8",newline="").write(s.replace("Saisissez un niveau entre 1 et 200.","Niveau hors limites.",1))' "$T/dofus_stuff/web/routes.py"; motif='message non produit par le code';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 06-01 (TACHE 2) EN ECHEC"; exit 1; fi; echo "morsures 06-01 tache 2 : 2/2 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 06-01 (TACHE 2) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>Les deux rubriques portent leurs messages mesures, la ligne de commande et l'interface web sont distinguees pour chacune, le refus de niveau est prouve par un flux conduit sans lancer de calcul, aucune duree n'est promise, et deux derives epinglees font rougir la suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 3 : la famille sans message (mecanisme du clavier) et les deux formes de la pagination</name>
  <files>docs/depannage.md, tests/test_docs_depannage.py</files>
  <read_first>
    docs/depannage.md
    docs/installation.md
    docs/parcours-simplifie.md
    tests/test_docs_depannage.py
    tests/test_docs_wizard.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/templates/screen.html
    dofus_stuff/web/static/js/terminal.js
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
  </read_first>
  <behavior>
    - Les trois ecrans `/version`, `/db/status` et `/quit` ne rendent aucun champ de saisie, et la rubrique cite les trois jetons du mecanisme portes par `screen.html` et `terminal.js`.
    - Le statut de `GET /list?page=1&size=1` porte un motif de pagination, celui de `GET /sets?page=1&size=1` (une seule page) n'en porte aucun, alors que le corps des deux le porte.
    - Rendre le motif de pagination inconditionnel dans le statut fait rougir le module (le piege mesure qui a deja coute un constat de registre).
  </behavior>
  <action>Livrer les deux rubriques finales et les deux controles structurels de la page.

**Page** — deux sections de niveau 2, inserees **juste avant** `## Source de vérité`, apres les trois premieres :
- `## Clavier inactif` : cette famille **n'affiche aucun message**, et la rubrique le dit — c'est un mecanisme, pas une chaine (`D-76`, `D-93`). Elle decrit ce que le code porte : le champ de saisie recoit le focus a l'affichage et au clic ailleurs, la touche Entree ne soumet le formulaire que si ce champ est actif, et les ecrans **sans champ de saisie** (etat de la base, version, sortie) laissent le clavier sans effet de saisie — donc si la frappe ne fait rien, le premier geste est de verifier que l'ecran attend bien une saisie ; la rubrique **renvoie** (`D-17`) vers `docs/installation.md`, qui est le seul endroit qui decrit le pilotage clavier (champ, reinvite au clic, barre dynamique), et elle ne redefinit pas ce que cette page dit ;
- `## Résultat paginé` : la pagination a **deux formes** et la page les distingue, parce que la ligne de statut ne porte le motif `PAGE n/total` que lorsque l'ecran est reellement pagine, tandis que le corps le porte toujours ;
  * interface web : le motif `PAGE {page}/{total}` compose par la ligne de statut, les deux touches de navigation de page, et le fait que le motif disparait du statut quand l'ecran n'a qu'une page (mesure de cette session ; c'est exactement le piege d'une assertion positionnelle fausse deja payee par le jalon) ;
  * ligne de commande : le message de fin de page qui annonce qu'il reste des objets a parcourir ;
  * renvoi vers `docs/parcours-simplifie.md`, qui porte deja le mecanisme de pagination.

**Module** :
- `MECANISMES_CLAVIER = ((jeton, chemin_source), ...)` : les trois jetons du mecanisme (`autofocus` dans le gabarit d'ecran et `activeElement` / `preventDefault` dans le script du terminal) avec leur fichier ; `ECRANS_SANS_CHAMP = ("/version", "/db/status", "/quit")` — trois ecrans mesures sans champ de saisie, dont l'ecran de sortie qui n'accepte que `GET` (405 sur `POST`, sans ligne de statut) ; un controle qui lui supposerait un message est donc muet par construction, et c'est exactement ce que cette famille declare au lieu de l'inventer ;
- `problemes_clavier(docs_dir, rendus) -> list[str]` : la rubrique existe, cite chaque jeton declare, et chaque jeton est **reellement porte** par le fichier nomme ; chaque ecran de `ECRANS_SANS_CHAMP` ne rend aucun champ de saisie (`id="main-input"` absent du corps rendu) ; `MESSAGES` ne porte aucune entree pour la cle `clavier` et cette absence est verifiee (un message invente pour cette famille est un constat portant `MOTIF_MESSAGE_INVENTE`) ;
- `MOTIF_PAGINATION = "formes de la pagination"` et le test `test_pagination_a_deux_formes(docs_dir, client, norm?)` : il rend `GET /list?page=1&size=1` et `GET /sets?page=1&size=1`, extrait le statut et le corps par les lecteurs locaux, et exige que le statut de la liste porte un motif `PAGE nombre/nombre` alors que celui de l'ecran a page unique n'en porte aucun, le corps des deux en portant un ; les constats accumules portent `MOTIF_PAGINATION` et citent la route, le statut rendu et le fichier producteur ;
- `MESSAGES` gagne les entrees de la rubrique « resultat pagine » (les deux touches de navigation de page et le message de fin de page de la ligne de commande, ce dernier en mode `source_literal` puisque seule l'execution l'imprimerait) ;
- le test de balayage final `test_les_cinq_familles_sont_couvertes(docs_dir)` : les cinq rubriques existent **dans l'ordre du critere 1**, chacune avant `## Source de vérité`, `RUBRIQUES_MECANISME` ne porte que `clavier`, et les cinq cles de `RUBRIQUES` portent chacune soit au moins une entree de `MESSAGES`, soit la declaration de mecanisme — aucune famille muette, aucune famille inventee.
  </action>
  <acceptance_criteria>
    - La page porte les cinq rubriques, dans l'ordre du critere 1, toutes avant `## Source de vérité`.
    - La rubrique « clavier inactif » est decrite comme un mecanisme, cite `autofocus`, `activeElement` et `preventDefault`, renvoie vers `docs/installation.md`, et `MESSAGES` ne porte aucune entree pour elle.
    - `problemes_clavier` exige l'absence du champ de saisie sur les trois ecrans sans saisie, et la presence de chaque jeton declare dans le fichier nomme.
    - `test_pagination_a_deux_formes` prouve le motif de pagination present dans le statut de la liste et absent de celui d'un ecran a page unique, alors que le corps des deux le porte.
    - Retirer un jeton de mecanisme de la page, ou rendre le motif de pagination inconditionnel dans le statut (`dofus_stuff/web/routes.py`), rougit le module.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_depannage.py tests/test_docs_structure.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in jeton_mecanisme_retire pagination_inconditionnelle; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_depannage.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in jeton_mecanisme_retire) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();open(p,"w",encoding="utf-8",newline="").write(s.replace("activeElement","elementActif",1))' "$T/docs/depannage.md"; motif='mecanisme du clavier';; pagination_inconditionnelle) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();open(p,"w",encoding="utf-8",newline="").write(s.replace("if total \u003e 1:","if True:",1))' "$T/dofus_stuff/web/routes.py"; motif='formes de la pagination';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 06-01 (TACHE 3) EN ECHEC"; exit 1; fi; echo "morsures 06-01 tache 3 : 2/2 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 06-01 (TACHE 3) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>Les cinq familles du critere 1 sont couvertes, la famille sans message est traitee comme un mecanisme adosse au code et au rendu, la pagination est decrite dans ses deux formes et prouvee comme telle, et les deux derives epinglees font rougir la suite.</done>
</task>

</tasks>

<!-- planner-discipline-allow: db clear -->

<threat_model>
**Surface** : une page `docs/` neuve, une ligne d'index dans un fichier existant, un module de test neuf ; les sondes sont la fonction de production `_print_db_status` sur une base `tmp_path`, le client de test Flask **en processus**, et la lecture des sources produit. Aucune dependance ajoutee, aucun fichier de `dofus_stuff/**` modifie (`D-103`), aucune publication. Les menaces reelles de ce plan sont **documentaires** (une page qui affirme un message que le produit ne produit pas) et **de harnais** (un controle qui ouvrirait la base du depot, executerait `main()` ou lancerait le solveur).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-01 | Spoofing (documentation qui ment) | `docs/depannage.md` : les cinq rubriques et leurs messages | high | mitigate | Chaque message declare est confronte a sa provenance (capture de `_print_db_status`, rendu du client de test, litteral du fichier nomme) et le controle apparie dans les deux sens les lignes citees et la table declaree ; quatre morsures de la vague 1 (rubrique retiree, message supprime, message invente, litteral de source renomme) prouvent que la page et le controle rougissent quand la cible derive |
| T-06-01-02 | Information disclosure | Prose de la page : chemin de poste, dossier temporaire, identifiant d'objet | high | mitigate | La sortie reellement capturee porte un chemin de dossier temporaire : la page ne cite que les **libelles** ; `MOTIF_VOLATILE` (quatre chiffres ou plus) et `MOTIF_CHEMIN_POSTE` refusent toute valeur volatile ou absolue, et le message d'objet introuvable est cite sans identifiant |
| T-06-01-03 | Tampering | `.data/dofus.sqlite3` | high | mitigate | Aucune ecriture sous `.data/` : toute base construite par un controle l'est sous `tmp_path` (dossier neuf ou repertoire de la fixture `app`) ; la garde `ast` refuse `sqlite3`, verifie que `data_dir` derive de `tmp_path` et que `DEFAULT_DATA_DIR` n'est jamais passe, et refuse tout `post(...)` vers les chemins destructifs ; l'empreinte de la suite est mesuree au plan 06-04 |
| T-06-01-04 | Elevation of privilege | Harnais : execution du produit, du solveur, d'une socket | high | mitigate | Garde `ast` auto-analysee : racines interdites, `APPEL_PRODUIT = "main"`, `APPELS_CALCUL_PRODUIT` (`optimize_stuff`, `_run_optimize_and_redirect`), appels de suppression et paires de confirmation refusees ; le flux simplifie est conduit en ne postant qu'un niveau **hors bornes**, ce qui prouve que le refus precede tout calcul, et les ecrans de calcul long sont sondes par des `GET` |
| T-06-01-05 | Repudiation (signal perdu) | Sonde qui ne prend rien et passe pour un vert | medium | mitigate | `_produire` verifie sa propre prise (route rendue, texte non vide, message attendu observe) et leve un constat portant `MOTIF_MESSAGE_NON_PRODUIT` et nommant la route ; `RUBRIQUES_MECANISME` documente l'absence de message d'une famille au lieu de la laisser muette |
| T-06-01-SC | Tampering | Installations de paquets | low | accept | Ce plan n'ajoute **aucune** dependance : `pyproject.toml` est inchange, aucun `pip install` n'est execute ; la porte de legitimite des paquets est sans objet (aucun `[ASSUMED]`, aucun `[SUS]`, aucun `[SLOP]`) |

**Hors perimetre** : publication, deploiement distant, synchronisation Dofusdude, suppression de donnees, achat. Aucune commande destructrice n'est executee ; l'occurrence du `README.md` est traitee au plan 06-03 (`D-100`), et `.planning/WINDOWS.md` n'est pas corrige a la main (`D-101`).
</threat_model>

<verification>
**Controles automatises** : `tests/test_docs_depannage.py` (garde de cloture auto-analysee, cinq rubriques, messages appariees dans les deux sens avec leur provenance, mecanisme du clavier, deux formes de la pagination, page sans valeur volatile), `tests/test_docs_structure.py` et les autres modules d'ancrage inchanges dans leurs gardes et verts avec la page indexee, la suite entiere, et **huit morsures** sur copie verte avant mutation (rubrique retiree, message supprime, message invente, litteral de source renomme, ligne de message de la saisie supprimee, refus de niveau renomme dans le produit, jeton de mecanisme retire, motif de pagination rendu inconditionnel).

**Non automatisable, nomme comme tel** : la prose libre de la page, le rendu Markdown hors GitHub et le comportement d'un navigateur reel sont declares en `backstop` dans `must_haves.truths` et ne sont **pas** revendiques comme verifies. Aucun blocage : rien de ce plan ne depend d'un geste humain, d'un secret ou d'un reseau.

**References de verification de phase** : `06-VALIDATION.md` § Per-Task Verification Map assigne la ligne `AIDE-01 / 06-01 / Message → rubrique` a ce plan (vague 1, `tests/test_docs_depannage.py`) ; les selecteurs `-k` provisoires du document de validation seront remplaces par les noms reels des tests (lecon des phases 3 a 5 : un selecteur qui ne selectionne rien est un faux temoin).
</verification>

<success_criteria>
- `docs/depannage.md` existe, est indexee, et permet de retrouver chaque rubrique **par le message reellement produit** : les cinq familles du critere 1 sont couvertes, chacune adossee a la capture, au rendu ou au litteral du fichier producteur (`D-93`, `D-19`, critere 1, AIDE-01).
- La famille « clavier inactif » est traitee comme un mecanisme, adosse au code et au rendu des ecrans sans champ, et jamais comme un message invente (`D-76`).
- La pagination est decrite dans ses deux formes et le controle prouve que le motif du statut depend de l'ecran, alors que celui du corps ne depend pas (`D-19`, pitfall mesure du jalon).
- Aucune valeur volatile, aucun chemin de poste, aucune duree promise dans la page (`D-73`, `D-75`, `D-19`).
- `docs/sommaire.md` croit d'une seule ligne d'index, sans conversion du parcours conseille en liens (mesure : trois gardes vertes lisent tout lien du sommaire comme une entree d'index).
- Huit derives epinglees font rougir la suite sur une copie verte avant mutation ; la suite entiere est verte et `.data/dofus.sqlite3` est intact.
</success_criteria>

<output>
Create `.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-01-SUMMARY.md` when done
</output>

## Artefacts produits par cette phase (inventaire tenu a jour par les quatre plans)

| Type | Symbole | Plan |
|------|---------|------|
| page | `docs/depannage.md` | 06-01 (creation : cinq rubriques, bloc Source de verite, ligne de retour) |
| ligne d'index | la ligne `[Dépannage](depannage.md)` du tableau de `docs/sommaire.md` | 06-01 |
| module de test | `tests/test_docs_depannage.py` | 06-01 |
| constantes de rubrique | `RUBRIQUES`, `RUBRIQUES_MECANISME` | 06-01 |
| constante de contrat | `MESSAGES` (message, rubrique, mode, source) | 06-01 (famille base), 06-01 tache 2 (saisie, calcul), 06-01 tache 3 (pagination) |
| constante de mecanisme | `MECANISMES_CLAVIER`, `ECRANS_SANS_CHAMP` | 06-01 |
| fonction pure | `problemes_rubriques`, `problemes_messages`, `problemes_page`, `problemes_clavier` | 06-01 |
| sonde | `_produire` | 06-01 |
| lecteurs locaux | `_texte_rendu`, `_lignes_du_corps`, `_statut` | 06-01 |
| constantes de garde | `RACINES_INTERDITES`, `APPELS_SUPPRESSION`, `APPEL_PRODUIT`, `APPELS_CALCUL_PRODUIT`, `CHEMINS_DESTRUCTIFS`, `MOTIF_GARDE`, `MOTIF_DATA_DIR` | 06-01 |
| constantes de motif | `MOTIF_RUBRIQUE_ABSENTE`, `MOTIF_RUBRIQUE_VIDE`, `MOTIF_MESSAGE_ABSENT`, `MOTIF_MESSAGE_INVENTE`, `MOTIF_MESSAGE_NON_PRODUIT`, `MOTIF_MECANISME`, `MOTIF_CHAMP_PRESENT`, `MOTIF_PAGINATION`, `MOTIF_VOLATILES`, `MOTIF_CHEMIN_ABSOLU`, `MOTIF_PROMESSE_DUREE`, `MOTIF_CRLF`, `MOTIF_SOURCE` | 06-01 |
| constantes de contenu | `MOTIF_VOLATILE`, `MOTIF_CHEMIN_POSTE`, `JETONS_PROMESSE_DUREE`, `MARQUEUR_CORPS`, `MARQUEUR_STATUT` | 06-01 |
| fonctions de test | `test_garde_de_cloture_du_harnais`, `test_rubriques_de_depannage`, `test_messages_de_base_absente_ou_vide`, `test_page_close_et_sans_valeur_volatile`, `test_messages_de_saisie_invalide`, `test_messages_de_calcul_long`, `test_clavier_inactif_est_un_mecanisme`, `test_pagination_a_deux_formes`, `test_les_cinq_familles_sont_couvertes` | 06-01 |
| page | `docs/glossaire.md` + sa ligne d'index + le parcours conseille final verifiable | 06-02 |
| module de test | `tests/test_docs_glossaire.py` (`TERMES_GLOSSAIRE`, `problemes_glossaire`, `problemes_parcours`) | 06-02 |
| module de test | `tests/test_docs_completude.py` (`PAGES_EPINGLEES`, `problemes_completude`, `problemes_readme`, motif de non-duplication) | 06-03 |
| garde du lecteur | le controle de non-destructivite du `README.md` (`D-100`) et son renvoi vers les deux pages | 06-03 |
| module de test | `tests/test_docs_mutation.py` (`_copie_verte`, les quatre familles de derive, l'empreinte de `.data/`) | 06-04 |
| preuve de verification | la batterie rejouable de la suite entiere dans une copie mutee, et l'audit de perimetre `git status` | 06-04 |
| greffon de mesure (hors livrable, non suivi par git) | `.gsd-tmp/no_net.py` — greffon pytest de refus du reseau et de piege `main()`, arme avant la suite finale ; c'est lui qui porte le membre « aucune connexion reseau, `main()` jamais execute » du critere 5, sous `.gsd-tmp/` (ni livrable ni fichier du depot) | 06-04 (tache 3) |

## Couverture des sources (ce plan)

| Source | Element | Consomme par |
|--------|---------|--------------|
| ROADMAP § Phase 6 | Critere 1 (rubriques retrouvees par le message reellement produit, cinq familles) | taches 1, 2 et 3 |
| ROADMAP § Phase 6 | Decomposition imposee : « 06-01 depannage + index + controle message → rubrique » | les trois taches |
| REQUIREMENTS | AIDE-01 | `requirements` du plan |
| RESEARCH | § Measured inventory A (les messages verbatim des cinq familles, leur provenance et la route de mesure sans `.data/` ni `main()`) | taches 1, 2 et 3 |
| RESEARCH | § Measured inventory A.4 (le clavier inactif n'a **aucun** message : mecanisme `autofocus` / refocalisation / garde sur l'element actif, et ecrans sans champ) | tache 3 |
| RESEARCH | § Measured inventory A.5 (les deux formes de la pagination, et le motif absent du statut quand l'ecran n'a qu'une page) | tache 3 |
| RESEARCH | § D-17 duplication map (les messages deja decrits par les pages livrees : renvoi, jamais redefinition) | taches 1, 2 et 3 |
| RESEARCH | Pitfalls 1, 2, 3, 4 et 11 (famille sans message, message a peu pres juste, pagination a deux formes, deux surfaces deux jeux de chaines, assertion CRLF non portable) | tache 2 (pitfalls 2 et 4), tache 3 (pitfalls 1 et 3), tache 1 (pitfall 11, limite ecrite) |
| CONTEXT | D-92 (quatre plans, vagues sequentielles, aucun parallelisme revendique) | frontmatter du plan |
| CONTEXT | D-93 (rubriques indexees par le message reel, aucune rubrique par symptome imagine) | taches 1, 2 et 3 |
| CONTEXT | D-102 (gabarit de page, normalisation, helpers partages, constats, constantes publiques, une seule source par enonce, limites honnetes) | toutes les taches |
| CONTEXT | D-103 et D-104 (code en lecture seule, aucune action destructive sur les donnees) | garde de cloture et sondes |
| CONTEXT | D-105 (commits locaux, indexation par chemin explicite, aucune dependance ajoutee) | livraison du plan |
| Planification | Corrections de mesure de cette session (flux simplifie conduit avant de sonder le refus de niveau ; messages de l'etat de la base en lignes de corps ; ou est le libelle de l'ecran d'etat) | taches 2 et 3, et `assumptions` du frontmatter |

**Audit de completude de la phase (les quatre types de sources)** : GOAL — les cinq criteres du ROADMAP sont couverts par les taches de ce plan (critere 1) et par les plans 06-02 (critere 2), 06-03 (critere 3) et 06-04 (critere 4 et 5) ; REQ — `AIDE-01` ici, `AIDE-02` au plan 06-02, `GARD-03` aux plans 06-03 et 06-04, `GARD-04` au plan 06-04 ; RESEARCH — les cinq familles de l'inventaire mesure, la carte de duplication D-17 et les pieges applicables sont repartis sur les trois taches, aucun piege applicable n'est laisse sans plan ; CONTEXT — D-92 a D-105 sont reparties sur les quatre plans. Exclusions assumees, conformes aux « Deferred Ideas » de `06-CONTEXT.md` (aucune) : la liste epinglee des 8 pages et la preuve de mutation (plans 06-03 et 06-04), et toute resynchronisation Dofusdude (hors perimetre).


