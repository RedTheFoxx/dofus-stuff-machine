---
phase: 06-depannage-glossaire-completude-et-preuve-finale
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - tests/test_docs_mutation.py
autonomous: true
requirements:
  - GARD-03
  - GARD-04
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "**Critere 4 du ROADMAP, mecanise** : chaque derive epinglee par cette phase est injectee sur une **copie** et le controle correspondant doit **rougir avec son motif**, en processus, sans sous-processus et sans toucher au depot (`GARD-04`). La preuve se fait par `tmp_path` : la copie est construite dans le dossier temporaire de pytest, la derive est ecrite dans la copie, la copie est **verte avant** la mutation (precondition obligatoire : une morsure sur une copie rouge ne prouve rien) et la fonction de constats du module d'ancrage est appelee sur la copie. Aucun `subprocess`, aucun interpreteur re-lance, aucun `chdir` du depot."
    - "Les **cinq familles de derive** sont couvertes, chacune par au moins une mutation et un motif attendu nomme dans la constante de la famille : (1) **page livree** (page rendue absente de la copie par renommage hors de la decouverte `docs/*.md`, page videe) ; (2) **index et parcours** (ligne d'index retiree, libelle de parcours renomme — et la forme en lien refusee) ; (3) **messages du depannage** (ligne de message supprimee, message non declare ajoute, litteral du produit renomme, message declare pour un ecran muet) ; (4) **glossaire** (terme retire, employeur faux) ; (5) **README** (renvoi non destructif retire, mot d'avertissement retire, occurrence de la commande disparue). Toute mutation dont la morsure n'est pas observee fait **echouer la batterie**, jamais un avertissement discret."
    - "Le **trou museliere** est traite explicitement : l'ecran de sortie du produit (`GET /quit`) ne rend aucun message et n'accepte pas `POST` (mesure : 405, aucune ligne de statut), donc une derivation qui pretend lui attribuer un message doit etre refusee par la sonde du module d'ancrage (`message non produit par le code`) — c'est la mutation `ecran_muet_declare`, et c'est ce qui rend le trou **ferme par un controle** au lieu d'etre une note dans un rapport."
    - "**`GARD-03`, volet donnees** : la base du depot est verifiee par une **empreinte en lecture seule** (taille, `mtime_ns`, SHA-256 lus en octets — jamais ouverte par SQLite, ce que la garde de cloture refuse), et l'empreinte de reference est celle **mesuree** : 24 989 696 octets, `mtime_ns` 1788730056843137500, SHA-256 `e3793d64cb7939ad1a51837b075b6b95e03d64c878fcb9cc07f86c00bb8fef7b`, relevee par la planification et **re-mesuree par l'executeur avant d'ecrire la constante**."
    - "**Critere 5 du ROADMAP** : le rapport de cette phase cite un **compteur reel** (le nombre de tests du resume de la derniere execution) et une **duree reelle** (la duree du resume), tous deux **lus** dans la sortie des **deux** executions finales — la suite entiere, puis la suite rejouee sous le greffon qui refuse le reseau et piege `main()` (`06-RESEARCH.md` § E.2), la seconde etant la seule a satisfaire le membre « aucune connexion reseau, `main()` jamais execute » du critere ; l'audit de perimetre, lui, est rendu contre **deux jeux de chemins nommes** — les huit chemins de livrable declares par les quatre plans, et un jeu tolere d'outillage et de bookkeeping — et la divergence est ecrite dans le rapport (`06-RESEARCH.md` § E.3) ; la derive heritee du registre `.planning/WINDOWS.md` est nommee a l'identique, jamais presentee comme resolue (`D-101`) ; aucun chiffre n'est ecrit a l'avance dans un plan, aucun « devrait passer » n'est revendique, et un `SUMMARY` qui porterait un compteur non mesure est un defaut du plan lui-meme (`D-85`, `D-98`)."
    - statement: "Le fait qu'aucune derive **non** couverte par les cinq familles ne puisse casser la documentation ne peut pas etre demontre exhaustivement : la preuve porte sur les derives **declarees**, et cette limite est ecrite dans le module et dans le rapport (`D-85`). Non confirmable au-dela : backstop explicite."
      verification: backstop
  artifacts:
    - path: "tests/test_docs_mutation.py"
      provides: "La batterie de mutation en processus : construction de la copie, redirection de la racine des modules d'ancrage, precondition de copie verte, les cinq familles de derive avec leur motif attendu, l'empreinte en lecture seule de la base du depot, et le contrat de rapport (compteur et duree lus, jamais ecrits a l'avance)"
      contains: "def test_chaque_derive_declenche_son_motif"
      min_lines: 240
  key_links:
    - from: "tests/test_docs_mutation.py"
      to: "tests/test_docs_depannage.py"
      via: "les fonctions pures `problemes_messages`, `problemes_page` et la sonde `_produire`, appelees sur la copie mutee : c'est la morsure du controle du plan 06-01 qui est prouvee, pas une reimplementation"
      pattern: "problemes_messages"
    - from: "tests/test_docs_mutation.py"
      to: "tests/test_docs_glossaire.py"
      via: "`problemes_glossaire` et `problemes_parcours` sur la copie mutee (familles glossaire et index/parcours)"
      pattern: "problemes_parcours"
    - from: "tests/test_docs_mutation.py"
      to: "tests/test_docs_completude.py"
      via: "`problemes_pages_epinglees`, `problemes_couverture_index` et `problemes_readme` sur la copie mutee (familles page livree et README)"
      pattern: "problemes_pages_epinglees"
    - from: "tests/test_docs_mutation.py"
      to: ".data/dofus.sqlite3"
      via: "empreinte en lecture seule : les octets sont lus, jamais ouverts par SQLite, jamais ecrits, jamais renommes (`GARD-03`)"
      pattern: "EMPREINTE_BASE"
  prohibitions:
    - statement: "Ne jamais modifier `dofus_stuff/**` pour aligner la documentation ni pour faire passer une mutation **dans le depot** : la copie mutee vit dans `tmp_path`, et le module ne modifie aucun fichier du depot (`D-103`)."
      status: resolved
      verification: test
    - statement: "Ne jamais ecrire, vider, supprimer, renommer ni ouvrir par SQLite quoi que ce soit sous `.data/` : la base du depot n'est lue qu'en **octets** pour l'empreinte, et toute base construite par un controle l'est sous `tmp_path` (`D-104`)."
      status: resolved
      verification: test
    - statement: "Ne jamais executer `main()`, lancer le solveur, ouvrir une socket ou une connexion reseau depuis un controle, ni poster vers un chemin destructif de l'interface web (`/db/clear`, `/db/sync`), ni lancer un sous-processus (le rejeu de la suite est une commande d'executeur, pas une assertion) (`D-103`, `D-104`)."
      status: resolved
      verification: test
    - statement: "Ne jamais presenter une mutation non mordante comme un succes : une derive dont la morsure n'est pas observee fait echouer la batterie, et le rapport ecrit cette liste, jamais un compte de morsures embellis (`D-85`)."
      status: resolved
      verification: test
    - statement: "Ne jamais ecrire un compteur de tests, une duree ou un resultat de suite **avant** de l'avoir mesure : le rapport cite la sortie de la commande finale (`D-85`, critere 5)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais publier, deployer a distance, pousser vers un depot distant ni effectuer d'achat : commits locaux seulement, indexation par chemin explicite, jamais `git add .` (`D-105`)."
      status: resolved
      verification: judgment
  assumptions:
    - "**Sonde d'arete spec-less : l'orchestrateur n'a PAS execute la sonde deterministe de la section A** (aucun SPEC de phase : `EDGE_ABSENT` / `PROHIB_ABSENT`). Les aretes de ce plan sont **derivees par le planificateur** du critere de succes 4 du ROADMAP § Phase 6 (« chaque controle est prouve mordant sur une derive injectee »), du texte de `GARD-04` et des mesures de `06-RESEARCH.md` (routes et ecrans, empreinte de la base du depot, ecran de sortie muet). Les aretes classables ont recu un critere confirmable ; l'exhaustivite des derives non declarees est ecrite en **backstop** ; les six prohibitions sont au bloc `prohibitions`."
    - "**Pourquoi la preuve est en processus** : le harnais interdit `subprocess`, et re-lancer pytest depuis un test serait un sous-processus. La morsure est donc obtenue en appelant les **fonctions pures** des modules d'ancrage sur une copie mutee dans `tmp_path`, ce qui oblige a une seule exigence de forme sur les modules des plans 06-01 a 06-03 : leurs fonctions de constats lisent la racine depuis la constante de module (`RACINE_DEPOT`), jamais depuis une valeur capturee a l'import. Si un module capture la racine, c'est un constat a corriger **dans le module d'ancrage** (fichier de test, changement autorise et attendu) ; le plan 06-04 nomme cette adaptation pour que l'executeur ne la cherche pas."
    - statement: "Les cinq familles couvrent les derives **declarees** ; une derive hors de ces familles (un renommage de fichier dans `docs/`, une page ajoutee hors index) est interceptee par les familles 1 et 2 autant que par les gardes livrees, mais cette couverture n'est pas demontree exhaustivement : limite ecrite (`D-85`)."
      verification: backstop
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 6 et de ses criteres 4 et 5, plus `GARD-04`, `D-97` et `D-98` de `06-CONTEXT.md` :
**En tant que** porteur du projet, **je veux** que la documentation livree soit accompagnee d'une preuve **rejouable** que ses controles rougissent quand la documentation derive, et que la preuve cite un compteur et une duree reellement mesures, **afin de** ne pas confondre « la suite est verte » avec « les gardes protegent quelque chose ».

Mode MVP : le `**Goal:**` du ROADMAP n'est pas redige en recit utilisateur. Le cadrage ci-dessus en est **derive** (comme aux plans 04-01, 05-01, 06-01, 06-02 et 06-03), jamais invente. Aucun retour a `/gsd mvp-phase` n'est demande.

Purpose : c'est **GARD-04** et les criteres 4 et 5 — la preuve que les gardes mordent, faite **en processus** sur une copie (le harnais interdit les sous-processus), et le rapport final honnete : compteur lu, duree lue, empreinte de la base du depot en lecture seule.

Output : `tests/test_docs_mutation.py` — construction de la copie, redirection de la racine des modules d'ancrage, precondition de copie verte, cinq familles de derive avec leur motif attendu, empreinte de la base du depot, et le contrat de rapport.

Ordonnancement declare : **vague 4**, `depends_on: [06-01, 06-02, 06-03]` — la preuve de morsure mutile les livrables des trois plans precedents : elle ne peut s'executer qu'apres eux, et elle **ne les modifie pas** (la copie vit dans `tmp_path`). Aucune execution parallele n'est revendiquee (`D-92`).

**Ce que ce plan ne fait pas** : il n'ajoute ni page, ni ligne d'index, ni surveillance de page (`docs/` est gele par les plans 06-01 a 06-03) ; il ne touche ni `README.md` ni `dofus_stuff/**` (`D-103`, `D-104`) ; il ne lance aucun sous-processus et n'ouvre aucune base par SQLite.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-VALIDATION.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
@.claude/CLAUDE.md
@tests/conftest.py
@tests/test_docs_depannage.py
@tests/test_docs_glossaire.py
@tests/test_docs_completude.py
@docs/depannage.md
@docs/glossaire.md
@docs/sommaire.md
@README.md
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : tranche verticale « la copie mutee existe, la copie est verte avant mutation, et une derive de page rougit le controle du plan 06-03 »</name>
  <files>tests/test_docs_mutation.py</files>
  <reversibility rating="reversible">Un module de test neuf est annulable par un commit de retour ; il n'ecrit que dans `tmp_path` et ne modifie aucun livrable.</reversibility>
  <read_first>
    tests/conftest.py
    tests/test_docs_completude.py
    tests/test_docs_depannage.py
    tests/test_docs_glossaire.py
    docs/sommaire.md
    README.md
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md
  </read_first>
  <action>Ouvrir la tranche verticale de la preuve de morsure : la copie, sa verite prealable, et la premiere famille de derive.

**Module** (`tests/test_docs_mutation.py`, nouveau fichier) — docstring de module ecrivant le contrat et les limites : la preuve se fait sur une **copie** dans `tmp_path`, en appelant les fonctions pures des modules d'ancrage (jamais une reimplementation du controle : une reimplementation prouverait la reimplementation), la **copie doit etre verte avant la mutation** (une morsure sur une copie deja rouge ne prouve rien, et c'est un constat de la meme famille), aucun sous-processus n'est lance (l'interdiction est celle du harnais, et le rejeu complet de la suite est une commande d'executeur), la racine des modules d'ancrage est **redirigee** vers la copie par la constante de module (si un module capture la racine a l'import, c'est un constat a corriger dans ce module — fichier de test, changement autorise), et les derives couvertes sont celles **declarees** dans les cinq familles, jamais une garantie d'exhaustivite (`D-85`) ; la morsure est cherchee par **presence du motif** dans les constats (appartenance de sous-chaine), donc elle prouve que le controle rougit **avec ce motif**, jamais que ce motif est la seule cause du rouge — limite ecrite dans le module et dans le rapport ;
- imports : `ast`, `hashlib`, `importlib.util`, `re`, `shutil`, `pathlib.Path`, `pytest`, et les fixtures partagees `normalize` ; **aucun** import de `sqlite3`, `subprocess`, `socket`, `urllib`, `requests` — la garde de cloture les refuse, et le module doit utiliser `shutil` pour copier l'arborescence et `hashlib` pour l'empreinte (les deux seules facons de faire sans ouvrir de base ni lancer de processus) ;
- constantes de chemin : `RACINE_DEPOT = Path(__file__).resolve().parents[1]`, `MODULES_ANCRAGE = ("test_docs_depannage.py", "test_docs_glossaire.py", "test_docs_completude.py")`, `FICHIERS_HORS_DOCS = ("README.md", "GUIDE_WIZARD.md")` ;
- helper local `_charger_module(nom_fichier)` : charge un module de test **par chemin** avec `importlib.util.spec_from_file_location` (nom unique prefixe `ancrage_`), l'ajoute a `sys.modules` avant execution (les dataclasses et les fixtures le supposent) et le retourne ; ce helper est **local** au module (4 lignes, motif de chargement, pas un helper partage : `D-12` ne porte que sur `tests/conftest.py`) ;
- helper local `_copie_du_depot(tmp_path)` : construit `<tmp_path>/depot/` avec `shutil.copytree`/`shutil.copy2` pour `docs/`, `tests/`, `dofus_stuff/` et `README.md`, `GUIDE_WIZARD.md`, `pyproject.toml` — la copie doit contenir **`dofus_stuff/`** parce que les controles de provenance lisent les litteraux du produit par chemin relatif, et **`tests/`** parce que les fixtures partagees et les lecteurs de pages y vivent ; retourne la racine de la copie ;
- helper local `_rediriger(module, copie)` : `monkeypatch.setattr(module, "RACINE_DEPOT", copie)` (ou le nom reel de la constante du module, releve par lecture de ce module) — c'est la piece qui rend la mutation visible par le controle, et le module ecrit pourquoi : sans cette redirection, un controle lirait le depot reel et la mutation porterait sur une copie que personne ne lit ;
- `FAMILLES = ((nom, description, fichier_cible, mutation, motif_attendu), ...)` : la **table unique** des derives declarees, remplie par les taches 2 et 3 ; la tache 1 y met sa famille ;
- `test_la_copie_est_verte_avant_toute_mutation(tmp_path)` : construit la copie, redirige les trois modules, appelle **toutes** les fonctions de constats sur la copie non mutee et exige `not constats` pour chacune — c'est la precondition, et elle est elle-meme un test : sans elle, une morsure pourrait cacher une copie deja rouge ;
- `test_chaque_derive_declenche_son_motif(tmp_path)` : boucle sur `FAMILLES`, et pour chaque entree : nouvelle copie (chaque mutation part d'une copie **fraiche** et verte), application de la mutation sur le fichier cible **de la copie**, appel du controle, puis exigence que **le motif attendu** apparaisse dans les constats ; en l'absence du motif, le test **echoue** en nommant la famille, le fichier et les constats reellement obtenus (jamais un `skip`, jamais un avertissement) ;
- **famille 1 (cette tranche)** : `page_livree` — `docs/cli.md` **renommee hors de la decouverte** (`docs/cli.md` deplacee sous `<copie-hors-docs>/retires/` par `Path.rename`, donc ni `page supprimee` ni appel refuse par la garde du module) : la page est bien absente du disque **livrable**, et le controle rougit avec `page epinglee absente ou non fichier` ; `docs/glossaire.md` reduite a son titre (motif `page videe ou tronquee`), controles appeles depuis `test_docs_completude.py` ;
- garde de cloture reprise du patron (jamais recopiee) : `RACINES_INTERDITES`, `APPELS_SUPPRESSION`, `APPEL_PRODUIT`, `APPELS_CALCUL_PRODUIT`, `CHEMINS_DESTRUCTIFS = ("/db/clear", "/db/sync")` et `test_garde_de_cloture_du_harnais` auto-analyse par `ast.parse(Path(__file__).read_text(encoding="utf-8"))`. **Consequence de forme, ecrite dans le module** : `APPELS_SUPPRESSION` refuse tout **appel** nomme `remove`, `unlink`, `rmdir` ou `rmtree`, y compris dans le module lui-meme (le patron s'auto-analyse) ; la mise en absence d'une page se fait donc par `Path.rename` (tache 2), jamais par un appel de suppression — sans quoi la garde du module rougirait sur une livraison conforme, ce qui serait un defaut du plan et non du module.

Contraintes de forme : aucun bloc de code balise `console`, aucun lien externe, aucun jeton de brouillon ; **aucune ecriture hors de `tmp_path`**, aucune lecture de `.data/` dans cette tache, aucun `monkeypatch.chdir`.
  </action>
  <acceptance_criteria>
    - `tests/test_docs_mutation.py` porte `FAMILLES`, `_charger_module`, `_copie_du_depot`, `_rediriger`, `test_la_copie_est_verte_avant_toute_mutation` et `test_chaque_derive_declenche_son_motif`.
    - `test_la_copie_est_verte_avant_toute_mutation` appelle les fonctions des trois modules d'ancrage sur la copie non mutee et exige l'absence de constats (la copie contient `docs/`, `tests/`, `dofus_stuff/`, `README.md`, `GUIDE_WIZARD.md`).
    - La famille `page_livree` est declaree dans `FAMILLES` et ses deux mutations rougissent avec leur motif.
    - Une mutation qui ne mordrait pas fait echouer le test en nommant la famille et les constats obtenus : le module ne contient ni `skip`, ni `xfail`, ni `warnings` pour les derives.
    - Le module n'ecrit rien hors de `tmp_path`, n'importe ni `sqlite3` ni `subprocess`, et sa garde de cloture est auto-analysee.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_mutation.py -q -k "copie_est_verte or chaque_derive"</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest, ou "no tests ran" (selecteur vide)</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; "$PY" -c "import ast,pathlib;src=pathlib.Path('tests/test_docs_mutation.py').read_text(encoding='utf-8');arb=ast.parse(src);noms=[n.name for n in ast.walk(arb) if isinstance(n,(ast.FunctionDef,))];attendu=['test_la_copie_est_verte_avant_toute_mutation','test_chaque_derive_declenche_son_motif'];manquants=[a for a in attendu if a not in noms];branches=[n.value for n in ast.walk(arb) if isinstance(n,ast.Attribute) and n.attr in ('skip','xfail')];print('fonctions attendues manquantes :',manquants);print('skip/xfail rencontres :',len(branches));raise SystemExit(1 if manquants or branches else 0)"</automated>
    <fails_when>exit non-zero (une fonction de preuve attendue est absente, ou le module esquive une derive par un saut ou une marque d'echec attendu)</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>La copie mutee existe dans `tmp_path`, elle est prouvee verte avant mutation, la redirection de racine est en place, la famille « page livree » rougit le controle du plan 06-03 avec ses motifs, et aucune derive ne peut etre esquivee par un saut.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : les quatre familles restantes — index et parcours, messages, glossaire, README</name>
  <files>tests/test_docs_mutation.py</files>
  <read_first>
    tests/test_docs_mutation.py
    tests/test_docs_depannage.py
    tests/test_docs_glossaire.py
    tests/test_docs_completude.py
    docs/depannage.md
    docs/glossaire.md
    docs/sommaire.md
    README.md
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
  </read_first>
  <behavior>
    - Chaque famille declaree a **au moins une** mutation dont la morsure est observee avec le motif attendu ; une famille vide ou muette fait echouer la batterie.
    - Les mutations portent sur la **copie** : `docs/`, `README.md` et, pour la famille des messages, un litteral du produit dans la copie de `dofus_stuff/` — le depot n'est jamais modifie.
    - Le trou museliere est couvert : declarer comme rendu par l'interface web un message qu'aucune sonde n'observe fait mordre la sonde du module d'ancrage.
  </behavior>
  <action>Etendre `FAMILLES` aux quatre familles restantes, en reutilisant les motifs declares par les modules d'ancrage (jamais des motifs reecrits) :

- famille `index_et_parcours` (controles de `test_docs_glossaire.py` et `test_docs_completude.py`) : (a) **retirer** du sommaire de la copie la ligne d'index dont la cible est `glossaire.md` (motif `page livree non indexee`) ; (b) **renommer** l'entree numerotee du parcours qui nomme `Glossaire` en lui ajoutant un suffixe (motif `parcours conseille`) ; (c) **convertir** une entree numerotee du parcours en lien Markdown (motif `parcours conseille en liens`) ;
- famille `messages_du_depannage` (controles de `test_docs_depannage.py`) : (a) **supprimer** de `docs/depannage.md` la ligne dont la premiere cellule est le message de saisie requise (motif `message non cite par sa rubrique`) ; (b) **inserer** dans la rubrique de la base une ligne de message non declare (motif `message cite non declare`) ; (c) **renommer** dans la copie de `dofus_stuff/sync.py` le litteral du message de base vide (`Base locale vide et --offline : impossible de synchroniser`) — la page ne bouge pas, c'est le **produit** qui derive, et le controle doit rougir (motif `message non produit par le code`) ; (d) **`ecran_muet_declare`** : dans la copie de `tests/test_docs_depannage.py`, passer de `source_literal` a `web_render` **la seule ligne** qui declare le message d'attente de la ligne de commande — la declaration affirme alors un rendu que la ligne de commande ne produit pas en processus, et la sonde doit refuser (motif `message non produit par le code`) ; c'est la mecanisation du trou museliere : l'ecran de sortie ne rend rien et n'accepte pas `POST` (mesure : 405, aucune ligne de statut), donc une declaration qui lui preterait un message serait muette, et une sonde muette est un constat, jamais un vert ;
- famille `glossaire` (controles de `test_docs_glossaire.py`) : (a) **supprimer** une ligne de terme (motif `terme declare absent de la page`) ; (b) **pointer** l'employeur d'un terme vers un fichier qui ne l'emploie pas (motif `terme non employe par le fichier cite`) ;
- famille `readme` (controles de `test_docs_completude.py`) : (a) **retirer** de la copie du `README.md` toutes les lignes qui portent le renvoi `docs/base-locale.md` (motif `invitation a detruire les donnees`) ; (b) **retirer** le mot d'avertissement du bloc de la commande de nettoyage, en le remplacant par un mot neutre (meme motif) ; (c) dans la copie, **retirer** la ligne de la commande de nettoyage elle-meme pour prouver que le controle exige que l'information reste documentee (meme motif).

**Module** :
- chaque entree de `FAMILLES` porte le **nom du module d'ancrage** et le nom de la fonction de constats a appeler, ce qui evite toute reimplementation : la batterie est un orchestrateur de mutations, pas un second jeu de controles ;
- helper local `_appliquer(copie, entree)` : applique la mutation sur la copie (lecture/ecriture **texte** en `utf-8` avec `newline=""` pour preserver les CRLF des pages) ; pour rendre une page **absente**, `Path.rename` la deplace sous `<copie>/../retires/` (dossier cree par `mkdir`), donc hors de `docs/` et hors de la decouverte `docs/*.md` — **jamais** `Path.unlink`, `Path.remove`, `rmdir` ni `shutil.rmtree`, qui sont exactement les quatre noms d'appel que la garde de cloture du module refuse par `APPELS_SUPPRESSION` : une suppression litterale ferait rougir la garde du module sur une livraison conforme, et c'est l'axe de la mutation qui doit bouger, pas la garde du harnais ; les mutations qui portent sur un module d'ancrage ou sur un litteral du produit visent la copie de `tests/` ou de `dofus_stuff/`, jamais le depot ;
- `test_les_familles_declarent_leurs_mutations()` : verifie que les **cinq** familles sont presentes dans `FAMILLES`, que chacune porte au moins une mutation, et que chaque entree nomme un module d'ancrage, un fichier cible, une mutation et un motif attendu — une famille declaree sans mutation est un constat portant `livrable non couvert par une mutation`, jamais un silence ;
- le module ecrit dans sa docstring le **trou museliere** et sa fermeture (ecran muet, sonde muette, constat), et rappelle que les motifs attendus sont **lus** dans les modules d'ancrage (`import ancrage_...` puis reference a la constante) autant que possible : un motif recopie d'un module a l'autre serait une seconde source de verite (`D-14`, `D-17`).
  </action>
  <acceptance_criteria>
    - `FAMILLES` porte les cinq familles, chacune avec au moins une mutation, un fichier cible, un module d'ancrage et un motif attendu.
    - Les mutations portent sur la copie dans `tmp_path` : aucun fichier du depot n'est modifie par la batterie (verifie par une copie fraiche par mutation et par l'absence d'ecriture hors `tmp_path`).
    - Les trois mutations du README portent le motif `invitation a detruire les donnees` et prouvent respectivement le renvoi manquant, l'avertissement manquant et l'occurrence disparue.
    - La mutation `ecran_muet_declare` fait rougir la sonde avec `message non produit par le code` et le module nomme l'ecran muet mesure (`GET /quit`, 405 sur `POST`, aucune ligne de statut).
    - Toutes les mutations mordent : aucune famille muette, aucun `skip`, aucun `xfail`.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_mutation.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; bat="$("$PY" -m pytest tests/test_docs_mutation.py -q 2>&1)"; if echo "$bat" | grep -qE '[0-9]+ (failed|error)|no tests ran'; then echo "$bat" | tail -20; echo "BATTERIE ROUGE OU NON SELECTIONNEE : le controle de non-modification du depot ne prouverait rien"; exit 1; fi; echo "$bat" | tail -5; echo "--- aucun fichier du depot ne doit avoir bouge pendant la batterie ---"; if ! etats="$(git status --porcelain)"; then echo "GIT EN ECHEC : l'audit ne peut pas conclure"; exit 1; fi;
printf '%s\n' "$etats" | grep -vE "^\?\? \.gsd-tmp/" | grep -E "docs/|README.md|dofus_stuff/" && { echo "FICHIER DU DEPOT MODIFIE PAR LA BATTERIE"; exit 1; }; echo "batterie : depot intact (les livrables ne changent pas, la copie vit dans tmp_path)"</automated>
    <fails_when>exit non-zero, ou une ligne "BATTERIE ROUGE OU NON SELECTIONNEE" (la batterie n'a pas tourne ou n'est pas verte, donc le controle de non-modification ne prouve rien), ou une ligne "FICHIER DU DEPOT MODIFIE PAR LA BATTERIE" (une mutation a touche le depot au lieu de la copie), ou "GIT EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>Les cinq familles de derive sont declarees et mordent, chacune par le controle du module d'ancrage correspondant, le trou museliere est ferme par un controle, et la batterie ne touche jamais un fichier du depot.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 3 : la preuve finale — empreinte de la base du depot, couverture des livrables, et rapport au compteur lu</name>
  <files>tests/test_docs_mutation.py</files>
  <read_first>
    tests/test_docs_mutation.py
    tests/conftest.py
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-VALIDATION.md
  </read_first>
  <behavior>
    - La base du depot est intacte : taille, `mtime_ns` et SHA-256 egaux a l'empreinte de reference, lue en octets, jamais ouverte par SQLite.
    - Chaque livrable de la phase est la cible d'au moins une mutation declaree : une page livree sans mutation est un constat.
    - Le rapport cite le compteur et la duree **lus** dans la sortie des **deux** executions finales (la suite entiere, puis la suite sous le greffon de refus reseau et de piege `main()`), et l'audit de perimetre ne montre, au perimetre livrable, que les huit chemins declares par les quatre plans : tout autre chemin hors du jeu tolere nomme (outillage et bookkeeping d'execution) est un ecart rapporte, et `dofus_stuff/**` reste absent.
  </behavior>
  <action>Fermer la phase par ses trois preuves d'ensemble.

**Module** — empreinte et couverture des livrables :
- `CHEMIN_BASE = ".data/dofus.sqlite3"`, `EMPREINTE_BASE = (24989696, 1788730056843137500, "e3793d64cb7939ad1a51837b075b6b95e03d64c878fcb9cc07f86c00bb8fef7b")` — l'empreinte **mesuree** par la planification (octets, `mtime_ns`, SHA-256) ; la docstring ecrit que l'executeur **re-mesure** la triple avant d'ecrire la constante et que ce chiffre vient du disque, jamais d'une estimation ; la lecture se fait par `Path.read_bytes()` et `hashlib.sha256`, **jamais** par SQLite (la garde de cloture refuse l'import, donc le controle et la regle sont coherents) ;
- `MOTIF_EMPREINTE_BASE = "empreinte de la base du depot"` et `MOTIF_COUVERTURE_MUTATION = "livrable non couvert par une mutation"` ;
- `test_la_base_du_depot_est_intacte()` : le fichier existe (son absence est un constat : il etait la au debut de la phase), et les trois valeurs sont egales a `EMPREINTE_BASE` ; le constat nomme les trois valeurs attendues et les valeurs lues, jamais un seul des trois ;
- `test_chaque_livrable_est_couvert_par_une_mutation()` : derive des `FAMILLES` la liste des fichiers cibles et exige que chaque livrable de la phase y figure (`docs/depannage.md`, `docs/glossaire.md`, `docs/sommaire.md`, `README.md`, plus les modules d'ancrage pour la famille des messages) ; c'est la mecanisation de la formule du critere 4 du ROADMAP : chaque page livree est prouvee durable par une mutation reelle.

**Preuves d'executeur (commandes, pas assertions)** — a rejouer dans cet ordre, apres la tache 2 :
1. l'empreinte de la base du depot **avant** les deux executions finales : `./.venv/Scripts/python.exe -c` lisant `read_bytes()` et `hashlib.sha256`, avec `stat().st_size` et `st_mtime_ns` — les trois valeurs doivent egaler `EMPREINTE_BASE` ;
2. la suite **entiere** : `./.venv/Scripts/python.exe -m pytest -q`, dont la derniere ligne porte le compteur et la duree (`N passed in X.XXs`) ; ces deux valeurs sont **lues** ici et recopiees telles quelles dans `06-04-SUMMARY.md` : aucun chiffre n'est ecrit a l'avance dans un plan, et un compteur non mesure serait un defaut du plan lui-meme ;
3. la suite **sous le greffon de refus reseau et de piege `main()`** : l'executeur ecrit `.gsd-tmp/no_net.py` (greffon pytest d'une trentaine de lignes, **hors livrable**, non suivi par git, meme statut que le script de mesure du plan 06-01, et tolere par l'audit de perimetre au titre du jeu tolere nomme), puis rejoue la suite par `PYTHONPATH="$PWD/.gsd-tmp" ./.venv/Scripts/python.exe -m pytest -q -p no_net`. Le greffon fait trois choses, et la troisieme est ce qui le rend **actif** et non declaratif : `installer()` remplace `socket.socket`, `socket.create_connection`, `socket.socketpair`, `urllib.request.urlopen` **et** `dofus_stuff.cli.main` / `dofus_stuff.web.__main__.main` par un piege qui leve ; `pytest_configure(config)` appelle `installer()` (les pieges sont donc armes avant la collecte et avant `tests/conftest.py`) ; `verifie_arme()` tente les **quatre** appels reseau et exige que chacun leve, puis verifie **par identite** — jamais par un appel, puisque appeler un `main()` non piege est precisement ce que la phase refuse — que les deux noms `main` ne sont plus les fonctions du produit. C'est la seule preuve du membre « aucune connexion reseau, `main()` jamais execute » du critere 5 : la lecture `ast` de la tache 1 porte sur le code **ajoute**, jamais sur la suite finale, et la mesure de provenance du greffon est `06-RESEARCH.md` § E.1 et E.2 — mesure d'une session anterieure, dont le compteur et la duree ne sont **pas** recopies : ils sont relus dans la sortie de cette phase, dont la suite est plus nombreuse ;
4. l'empreinte **apres** les deux executions : meme commande, meme comparaison — aucune des deux suites ne doit avoir approche `.data/` ; un ecart se constate et se rapporte, il ne se corrige pas en silence ;
5. l'audit de perimetre, rendu contre **deux jeux de chemins nommes** (`06-RESEARCH.md` § E.3 : la formulation du critere est fausse a la lettre, et c'est la version mesuree qui est appliquee) — les **huit chemins de livrable** declares par les quatre plans (`docs/depannage.md`, `docs/glossaire.md`, `docs/sommaire.md`, `README.md`, `tests/test_docs_depannage.py`, `tests/test_docs_glossaire.py`, `tests/test_docs_completude.py`, `tests/test_docs_mutation.py`) et un **jeu tolere, nomme** : `.gitignore`, l'ensemble `.planning/**` (dont `.planning/config.json` et `.planning/state.json`, deja modifies avant la phase, et les plans, resumes et fichiers de verification de la phase), `.gsd-tmp/` (dont le greffon de preuve), `.gsd/`, `.doc-agent/`, `doc-agent.toml`, `gsd-auto-rules.toml`, `gsd-auto.toml`. Ce jeu est tolere parce que ce sont de l'**outillage** et du **bookkeeping** d'execution, presents ou modifies **avant** la phase par l'automatisation, jamais des livrables : le tolerer n'est pas masquer un ecart, c'est nommer une divergence que la planification a mesuree, et l'audit ecrit cette divergence dans le rapport. L'audit exige en plus, separement, que `git status --porcelain -- dofus_stuff` soit **vide** (lecture seule, `D-103`) ; tout chemin ni livrable declare ni tolere — une page de `docs/` non declaree, un fichier de `dofus_stuff/**`, tout autre fichier — est un ecart de perimetre qui fait **echouer** l'audit, la decision etant prise **hors** de la boucle de lecture (jamais un `exit` execute dans un sous-shell de pipeline, qui ne pourrait pas faire echouer la commande) ;
6. les commits locaux, par **chemin explicite**, un par plan livre, jamais `git add .` (`D-105`).

**Rapport** (`06-04-SUMMARY.md`, et la section de preuve de `06-VALIDATION.md` mise a jour au moment de la verification de phase) : les cinq familles avec, pour chacune, la mutation, le fichier cible, le motif observe ; le nombre de mutations mordantes ; les **deux** executions de la suite avec leur compteur et leur duree **lus** cote a cote — la suite entiere, puis la suite sous le greffon de refus reseau et de piege `main()` (`06-RESEARCH.md` § E.2), la seconde n'etant jamais presentee comme un doublon de la premiere, puisque c'est elle qui satisfait le membre « aucune connexion reseau, `main()` jamais execute » du critere 5, et qu'aucun chiffre n'est pre-rempli ni repris d'une session anterieure ; la triple d'empreinte de la base du depot avant et apres, identique ; **la divergence declaree de l'audit de perimetre**, ecrite et non masquee : les huit chemins de livrable declares par les quatre plans, **plus** le jeu tolere nomme que `git status` fait apparaitre (`.gitignore`, `.planning/**` dont `.planning/config.json`, `.planning/state.json` et les fichiers de la phase, `.gsd-tmp/` dont le greffon de preuve, `.gsd/`, `.doc-agent/`, `doc-agent.toml`, `gsd-auto-rules.toml`, `gsd-auto.toml`) avec la raison de cette tolerance (outillage et bookkeeping d'execution, presents ou modifies **avant** la phase, jamais des livrables — `06-RESEARCH.md` § E.3), et le fait mesure que `git status --porcelain -- dofus_stuff` est **vide** ; **la derive heritee du registre `.planning/WINDOWS.md`, nommee a l'identique** — `windows_ledger_table_drift`, ligne `id=5`, verdict d'outil consigne dans `04-03-SUMMARY.md:220` et `05-03-SUMMARY.md:238`, heritee de la phase 3, **non corrigee** et jamais presentee comme resolue, `windows fixed 5` **jamais tente** (reecrire le registre serait une action hors perimetre : `D-101`) ; la liste des limites declarees (exhaustivite des derives, prose, rendu hors GitHub, ordre de lecture du parcours, ecran muet traite par la sonde et non par un rendu, et la **reconnaissance par sous-chaine** : la morsure exige la presence du motif dans les constats, elle ne prouve pas que ce motif est la seule cause du rouge).
  </action>
  <acceptance_criteria>
    - `test_la_base_du_depot_est_intacte` compare les trois valeurs (octets, `mtime_ns`, SHA-256) et n'ouvre jamais la base par SQLite (aucun import de `sqlite3` dans le module).
    - `test_chaque_livrable_est_couvert_par_une_mutation` exige que chaque livrable de la phase soit la cible d'au moins une mutation declaree.
    - Les **six** commandes d'executeur sont rejouees dans l'ordre, et le `SUMMARY` porte le compteur et la duree **lus** dans la sortie des **deux** executions de la suite (aucune valeur pre-remplie, aucune valeur reprise d'une session anterieure) ; le greffon de refus reseau et de piege `main()` a ete **arme** avant l'execution, et le rapport cite sa sortie a cote de celle de la suite entiere.
    - L'audit de perimetre ne montre, au perimetre livrable, que les **huit** chemins declares par les quatre plans ; le **jeu tolere** (outillage et bookkeeping d'execution : `.gitignore`, `.planning/**`, `.gsd-tmp/`, `.gsd/`, `.doc-agent/`, `doc-agent.toml`, `gsd-auto-rules.toml`, `gsd-auto.toml`) est **nomme** dans le rapport avec la raison de sa tolerance — jamais fondu dans un filtre silencieux — et aucun fichier de `dofus_stuff/**` n'a bouge (`git status --porcelain -- dofus_stuff` vide).
    - L'empreinte de la base du depot est identique avant et apres l'execution finale, et les commits sont locaux, par chemin explicite.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_mutation.py -q -k "base_du_depot or couvert"</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest, ou "no tests ran" (selecteur vide)</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; fp() { "$1" -c "import hashlib,pathlib;p=pathlib.Path('.data/dofus.sqlite3');b=p.read_bytes();print('taille=%d mtime_ns=%d sha256=%s'%(len(b),p.stat().st_mtime_ns,hashlib.sha256(b).hexdigest()))"; }; test -f .gsd-tmp/no_net.py || { echo "GREFFON ABSENT : ecrire .gsd-tmp/no_net.py (greffon de refus reseau et de piege main(), hors livrable) avant de rejouer cette preuve (06-RESEARCH.md E.2)"; exit 1; }; if ! PYTHONPATH="$PWD/.gsd-tmp" "$PY" -c "import no_net;ok=no_net.verifie_arme();print('PIEGE ARME' if ok else 'PIEGE INACTIF');raise SystemExit(0 if ok else 1)"; then echo "GREFFON PRESENT MAIS NON ARME : la preuve de refus reseau et de piege main() ne prouverait rien"; exit 1; fi; AVANT="$(fp "$PY")"; echo "AVANT $AVANT"; suite="$("$PY" -m pytest -q 2>&1)"; if echo "$suite" | grep -qE '[0-9]+ (failed|error)'; then echo "$suite" | tail -20; echo "SUITE FINALE ROUGE : aucun compteur ne peut etre cite"; exit 1; fi; echo "$suite" | tail -2; suite_no_net="$(PYTHONPATH="$PWD/.gsd-tmp" "$PY" -m pytest -q -p no_net 2>&1)"; if echo "$suite_no_net" | grep -qE '[0-9]+ (failed|error)'; then echo "$suite_no_net" | tail -20; echo "SUITE SOUS GREFFON ROUGE : la preuve de refus reseau et de piege main() ne serait pas verte"; exit 1; fi; echo "$suite_no_net" | tail -2; APRES="$(fp "$PY")"; echo "APRES $APRES"; if [ "$AVANT" != "$APRES" ]; then echo "EMPREINTE DE LA BASE DU DEPOT MODIFIEE : $AVANT puis $APRES"; exit 1; fi; echo "base du depot intacte et identique avant et apres les deux executions : compteur et duree des deux suites lus ci-dessus, aucun chiffre pre-rempli"</automated>
    <fails_when>exit non-zero, ou une ligne "GREFFON ABSENT", "GREFFON PRESENT MAIS NON ARME", "SUITE FINALE ROUGE", "SUITE SOUS GREFFON ROUGE" ou "EMPREINTE DE LA BASE DU DEPOT MODIFIEE", ou une des deux lignes de suite qui ne porte pas un compteur de tests reussis et une duree (un resume sans compteur ni duree ne satisfait pas le critere 5)</fails_when>
    <automated>if ! etats="$(git status --porcelain)"; then echo "GIT EN ECHEC : l'audit de perimetre ne peut pas conclure"; exit 1; fi; if [ -z "$etats" ]; then echo "AUCUN CHANGEMENT : la tache 3 et le plan n'ont rien livre"; exit 1; fi; echo "PERIMETRE MESURE :"; echo "$etats";
HORS=""; while IFS= read -r chemin; do [ -n "$chemin" ] || continue; case "$chemin" in docs/depannage.md|docs/glossaire.md|docs/sommaire.md|README.md|tests/test_docs_depannage.py|tests/test_docs_glossaire.py|tests/test_docs_completude.py|tests/test_docs_mutation.py) continue;; esac; case "$chemin" in .planning/*|.gsd-tmp/*|.gsd/*|.doc-agent/*|doc-agent.toml|gsd-auto-rules.toml|gsd-auto.toml|.gitignore) continue;; esac; HORS="$HORS $chemin"; done <<< "$(echo "$etats" | awk '{print $NF}')"; if ! dofus="$(git status --porcelain -- dofus_stuff)"; then echo "GIT EN ECHEC : le controle de lecture seule de dofus_stuff n'a pas conclu"; exit 1; fi; if [ -n "$dofus" ]; then echo "$dofus"; echo "HORS PERIMETRE : dofus_stuff/** a bouge (lecture seule, D-103)"; exit 1; fi; if [ -n "$HORS" ]; then echo "HORS PERIMETRE :$HORS"; exit 1; fi; echo "audit de perimetre : huit chemins de livrable declares, jeu tolere nomme (outillage et bookkeeping d'execution) ecarte, dofus_stuff/** absent"</automated>
    <fails_when>exit non-zero, ou une ligne "HORS PERIMETRE" (un chemin ni livrable declare ni outillage/bookkeeping tolere : une page de `docs/` non declaree, un fichier de `dofus_stuff/**`, tout autre fichier, ou `dofus_stuff/**` lui-meme), ou "GIT EN ECHEC", ou "AUCUN CHANGEMENT"</fails_when>
  </verify>
  <done>La base du depot est prouvee intacte en lecture seule (empreinte identique avant et apres les deux executions), chaque livrable est couvert par une mutation declaree, les cinq familles mordent, le greffon de refus reseau et de piege `main()` est arme et la suite qu'il encadre est verte, l'audit de perimetre ne montre que les huit chemins declares au perimetre livrable (jeu tolere nomme et divergence ecrite), `dofus_stuff/**` est absent de `git status`, et le rapport cite le compteur et la duree lus dans la sortie des deux suites finales.</done>
</task>

</tasks>
<!-- planner-discipline-allow: db clear -->
<!-- planner-allow: PURGE OUI -->

<threat_model>
**Surface** : un module de test neuf qui **copie** le depot dans `tmp_path`, le mute, et appelle les fonctions de constats des modules d'ancrage ; plus une empreinte en **lecture seule** des octets de la base du depot. Aucun sous-processus, aucune socket, aucune base ouverte, aucune ecriture hors `tmp_path`, aucun fichier du depot modifie. Les menaces reelles sont : une mutation qui ne mord pas et qu'on presenterait comme un succes, une mutation qui toucherait le depot au lieu de la copie, et un rapport qui citerait un compteur non mesure.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-01 | Repudiation (preuve qui ne prouve rien) | La batterie de mutation : cinq familles, copie fraiche par mutation | high | mitigate | La copie est **verte avant** chaque mutation (precondition testee), la mutation porte sur la copie fraiche, le motif attendu est exige dans les constats, et une famille muette ou vide est un constat ; aucun `skip`, aucun `xfail` (verifie par lecture de l'arbre du module) |
| T-06-04-02 | Tampering | Le depot (pages, `README.md`, `dofus_stuff/**`) | high | mitigate | Toutes les mutations vivent dans `tmp_path` ; l'audit de perimetre de la tache 3 (`git status --porcelain`) et le controle de la tache 2 (aucun chemin de livrable ne bouge pendant la batterie) refusent toute modification du depot, et aucun fichier de `dofus_stuff/**` ne peut changer (`D-103`) |
| T-06-04-03 | Tampering / Information disclosure | `.data/dofus.sqlite3` | high | mitigate | Empreinte en lecture seule (octets, `mtime_ns`, SHA-256) avant et apres la suite finale, avec l'empreinte mesuree comme reference ; le module n'importe jamais `sqlite3` (la garde de cloture le refuse, donc la regle et le controle sont coherents) ; toute base construite par un controle l'est sous `tmp_path` (`D-104`) |
| T-06-04-04 | Elevation of privilege | Harnais : sous-processus, reseau, chemins destructifs | high | mitigate | Garde `ast` reecrite : racines interdites (dont `subprocess`), appel au point d'entree du produit, appels de calcul et de suppression, `CHEMINS_DESTRUCTIFS = ("/db/clear", "/db/sync")` refuses ; le rejeu de la suite est une **commande d'executeur** documentee, jamais une assertion d'un test |
| T-06-04-05 | Spoofing (rapport qui arrange) | Le `SUMMARY` et la section de preuve | high | mitigate | Le contrat de rapport exige un compteur et une duree **lus** dans la sortie de la suite finale, la triple d'empreinte avant et apres, et la liste des limites ; aucun chiffre n'est ecrit a l'avance dans un plan, et un ecart d'empreinte se rapporte au lieu de se corriger en silence (critere 5, `D-85`) |
| T-06-04-06 | Repudiation (trou museliere) | L'ecran de sortie du produit et les declarations de rendu | high | mitigate | L'ecran muet (mesure : 405 sur `POST`, aucune ligne de statut) ne peut pas etre couvert par une declaration rendue : la mutation `ecran_muet_declare` deplace la declaration du message d'attente vers la famille rendue et la sonde refuse (`message non produit par le code`), donc le trou est **ferme par un controle** et non par une note |
| T-06-04-SC | Tampering | Installations de paquets | low | accept | Aucune dependance ajoutee (`shutil`, `hashlib`, `importlib` sont de la bibliotheque standard) ; aucun paquet installe, aucun `[ASSUMED]`, `[SUS]` ni `[SLOP]` |

**Hors perimetre** : publication, deploiement distant, synchronisation Dofusdude, suppression de donnees, achat. Le module ne modifie **aucun** fichier livre ; les six commandes d'executeur de la tache 3 sont des lectures, deux executions de suite et l'ecriture d'un greffon **hors livrable** sous `.gsd-tmp/`, jamais une commande destructrice (ni `db clear`, ni `db sync`, ni `db status` : aucun fichier de `dofus_stuff/**` n'est execute par ces commandes, la seule invocation du produit est celle d'un piege arme).
</threat_model>

<verification>
**Controles automatises** : `tests/test_docs_mutation.py` (copie verte avant mutation, cinq familles de derive avec motif attendu, traitement du trou museliere, couverture des livrables par les mutations, empreinte en lecture seule de la base du depot, garde de cloture auto-analysee), la suite entiere apres chaque tache, plus l'audit de perimetre et la comparaison d'empreinte de la tache 3.

**Preuves d'executeur qui ne sont pas des assertions** (et qui sont nommees comme telles) : le rejeu des **deux** suites pour **lire** leurs compteurs et leurs durees (critere 5) — la suite entiere, puis la suite sous le greffon de refus reseau et de piege `main()`, dont l'armement est verifie avant l'execution —, la comparaison d'empreinte avant et apres, et l'audit `git status` rendu contre les deux jeux de chemins nommes. Ces preuves sont des commandes rejouees dans l'ordre, pas des tests : un test ne peut pas se prouver lui-meme. **Pointeur de lecture (INFO-3)** : les trois derives litterales du critere 4 — completude, ancrage de H1 ↔ libelle d'index, renvois obsoletes — sont **deja** prouvees mordantes par le controle livre `tests/test_docs_structure.py:533-603` (`test_mutation_detecte_les_trois_derives`, mesure de la phase 1) ; ce plan n'en revendique pas la primeur, il ajoute les cinq familles de derive **de cette phase** et la preuve que les controles **neufs** mordent.

**Non automatisable, nomme comme tel** : l'exhaustivite des derives **non** declarees (la preuve porte sur les cinq familles ecrites), le rendu Markdown hors GitHub, la prose des pages, et l'ordre de lecture du parcours sont declares en `backstop` dans `must_haves.truths` ; le retrait d'une ligne du `README.md` n'est **plus** un `judgment` depuis la revision du plan 06-03 : il est decide par la conservation des jetons de `INVOCATIONS_CONSERVEES` et par la comparaison `git show HEAD:README.md` ↔ copie de travail (seule une reformulation sans perte y echappe, limite declaree). Aucun blocage : rien ici ne depend d'un geste humain, d'un secret ou d'un reseau.

**References de verification de phase** : `06-VALIDATION.md` § Per-Task Verification Map assigne la ligne `GARD-04 / 06-04 / Preuve de morsure` a ce plan (vague 4, `tests/test_docs_mutation.py`) et la ligne `GARD-03 / Empreinte .data` a sa tache 3.
</verification>

<success_criteria>
- Les **cinq familles de derive** declarees mordent : chaque mutation fait rougir le controle du module d'ancrage correspondant avec son motif attendu, sur une copie verte avant mutation, sans sous-processus et sans toucher au depot (`GARD-04`, critere 4).
- Chaque **livrable** de la phase est la cible d'au moins une mutation declaree : chaque page livree est prouvee durable par une mutation reelle (formule du critere 4).
- Le **trou museliere** est ferme par un controle : une declaration de rendu que le produit ne produit pas est un constat (`message non produit par le code`), l'ecran de sortie muet etant mesure (405 sur `POST`, aucune ligne de statut).
- La **base du depot** est prouvee intacte par une empreinte en lecture seule, avant et apres les deux executions finales, et aucun fichier de `dofus_stuff/**` n'a bouge (`GARD-03`, `D-103`, `D-104`).
- Le **rapport** cite le compteur reel et la duree reelle lus dans la sortie des **deux** suites finales (la suite entiere, puis la suite sous le greffon de refus reseau et de piege `main()`, la seule a prouver le membre « aucune connexion reseau, `main()` jamais execute »), l'empreinte avant et apres, la **divergence declaree** du perimetre, la derive heritee de `.planning/WINDOWS.md` nommee a l'identique, et la liste des limites (critere 5, `D-85`, `D-101`) — aucun chiffre pre-rempli.
- L'audit de perimetre : au **perimetre livrable**, seuls les huit chemins declares par les quatre plans ; le jeu tolere (outillage et bookkeeping d'execution) est **nomme** et explique dans le rapport, `dofus_stuff/**` est absent de `git status`, et un chemin hors de ces deux jeux fait **echouer** l'audit (la decision est prise hors de la boucle de lecture, donc son code de sortie est reellement atteignable).
</success_criteria>

<output>
Create `.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-04-SUMMARY.md` when done
</output>

## Couverture des sources (ce plan)

| Source | Element | Consomme par |
|--------|---------|--------------|
| ROADMAP § Phase 6 | Critere 4 (les gardes echouent sur une derive injectee, chaque page livree prouvee durable par une mutation reelle) | taches 1, 2 et 3 |
| ROADMAP § Phase 6 | Critere 5 (le rapport cite un compteur et une duree reels) | tache 3, et le contrat de rapport |
| ROADMAP § Phase 6 | Decomposition imposee : « 06-04 preuve de mutation et duree » | les trois taches |
| REQUIREMENTS | GARD-03 et GARD-04 | `requirements` du plan |
| RESEARCH | Les routes et ecrans mesures (dont l'ecran de sortie en `GET` seulement, 405 sur `POST`, sans ligne de statut) et les deux seuls points d'entree visant la base ou le reseau | taches 1, 2 et 3 |
| RESEARCH | L'empreinte de la base du depot (octets, `mtime_ns`, SHA-256) relevee par la planification | tache 3 |
| RESEARCH | Pitfall 1 (famille sans message), pitfall 8 (controle non mordant) et le trou museliere de l'ecran muet | tache 2, et `must_haves.truths` |
| CONTEXT | D-97 et D-98 (la preuve de morsure et le rapport de preuve) | `objective` et tache 3 |
| CONTEXT | D-92 (quatre plans, vagues sequentielles) | frontmatter du plan |
| CONTEXT | D-101 (derive heritee du registre `.planning/WINDOWS.md`, nommee et non corrigée) | tache 3 et le rapport |
| CONTEXT | D-85 (limites honnetes, aucun resultat revendique) | docstring, rapport et `must_haves.truths` |
| CONTEXT | D-103 et D-104 (code en lecture seule ; aucune action destructive sur les donnees) | garde de cloture, empreinte, prohibitions |
| CONTEXT | D-105 (commits locaux, indexation par chemin explicite) | tache 3 (commandes d'executeur) |
| Planification | La contrainte « pas de sous-processus » transformee en preuve **en processus** : copie `tmp_path`, redirection de la racine des modules d'ancrage, copie verte avant mutation | taches 1 et 2, et `assumptions` |

**Audit de completude de la phase (les quatre types de sources)** : GOAL — ce plan couvre les criteres 4 et 5 ; les criteres 1, 2 et 3 sont couverts par 06-01, 06-02 et 06-03 ; REQ — `GARD-04` ici, `GARD-03` ici (empreinte et couverture) et au plan 06-03, `AIDE-01` au plan 06-01, `AIDE-02` au plan 06-02 ; RESEARCH — les mesures de routes, l'empreinte de la base et les pieges 1 et 8 sont consommees ; CONTEXT — D-92 a D-105 reparties sur les quatre plans, sans exception. Exclusions assumees : aucune resynchronisation Dofusdude, aucune publication, aucun deploiement, aucun achat (hors perimetre), et aucune modification de `dofus_stuff/**` (`D-103`).


