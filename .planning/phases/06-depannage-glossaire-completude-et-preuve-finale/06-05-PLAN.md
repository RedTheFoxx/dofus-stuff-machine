---
phase: 06-depannage-glossaire-completude-et-preuve-finale
plan: 05
type: execute
wave: 5
depends_on:
  - 06-01
  - 06-04
files_modified:
  - docs/depannage.md
  - tests/test_docs_depannage.py
autonomous: true
gap_closure: true
requirements:
  - AIDE-01
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "L'absolu de portee de `docs/depannage.md` — ligne 74 livree : « un ecran qui n'a qu'une seule page n'ajoute aucun motif a son statut » — est remplace par les deux comportements mesures de la ligne de statut, surface par surface. Surface servie par l'application : `dofus_stuff/web/routes.py:144-145` ne compose `PAGE {page}/{total}` que sous la garde `if total > 1:` (1 occurrence mesuree dans le fichier, sur 3 occurrences de `total > 1` — :133, :137, :144), et la ligne de **corps** est composee toujours (`dofus_stuff/web/routes.py:477` et `:543`, motif `f\"PAGE {page}/{total_pages}` : 2 occurrences mesurees). Surface des deux ecrans de sauvegardes : `dofus_stuff/web/static/js/terminal.js:370` et `:400` composent `\"PAGE \" + page + \"/\" + total` **sans aucune garde** (`total > 1` : 0 occurrence mesuree dans le fichier ; `Math.max(1, ...)` : 5 occurrences), la ligne de statut portant donc `PAGE 1/1` des la premiere page, et les deux compositions sont adossees a `setStatus(` (element `.row.status`, `terminal.js:11`) — le **corps** des deux ecrans, lui, ne porte aucune ligne de pagination. C'est l'inverse de la regle enoncee par la page. T3 de `06-VERIFICATION.md`, critere 1 du ROADMAP, AIDE-01."
    - "La rubrique « Resultat pagine » de la page nomme les **deux** producteurs (`dofus_stuff/web/routes.py`, `dofus_stuff/web/static/js/terminal.js`) et porte la condition mesuree de chacun : « plus d'une page » et « porte toujours » pour la surface servie par l'application, « une seule page » et « aucune ligne de pagination » pour les ecrans de sauvegardes. Mesure du planificateur : la page livree par `06-01` rend **5 constats** sous le nouveau controle — les deux surfaces non nommees, le jeton « aucune ligne de pagination » absent pour la surface des sauvegardes, et **deux** marques d'absolu (« aucun motif a son statut », « n'ajoute aucun motif ») — donc le controle est rouge pour deux raisons independantes avant correction."
    - "Le nouveau controle est lie aux deux comportements **mesures dans le code**, pas a une seule source : il lit les deux fichiers producteurs depuis `RACINE_DEPOT`, exige le procede **garde** de l'un (1 occurrence, garde comprise) et la composition **inconditionnelle** de l'autre (2 occurrences, aucune garde `total > 1`, chaque composition precedee de `setStatus(` dans les 80 caracteres), et un procede deplace ou une occurrence perdue devient un constat portant `formes de la pagination` — jamais un vert silencieux. Le controle **presuppose** le controle comportemental deja livre (`test_pagination_a_deux_formes`, `tests/test_docs_depannage.py:1129`), qui rend les deux ecrans de liste et mesure le statut et le corps ; il ne le remplace ni ne le reecrit."
    - "La phrase d'ouverture de la rubrique « Saisie invalide » (`docs/depannage.md:28`) est desambiguee : `AUCUN RESULTAT.` est produit **apres** une recherche reellement executee (`dofus_stuff/web/routes.py:334-335` puis `:351`) et la page le dit desormais, au lieu de le ranger parmi les refus qui tombent avant tout travail. Observation secondaire de `06-VERIFICATION.md`."
    - "Trois derivees sont injectees sur des **copies** construites sous `mktemp -d`, chacune mesuree **verte** avant sa mutation, et chacune doit faire apparaitre son motif nomme dans la sortie de la copie : reintroduire un absolu de portee (`absolu de pagination non borne`, 2 constats mesures), effacer le jeton de condition de la surface des sauvegardes (`surface de pagination non declaree`, 1 constat mesure), ou deplacer le procede du script (`formes de la pagination`, 1 constat mesure). Aucune ne se solde par `MUTATION NON DETECTEE`."
    - "Le gabarit de la page est intact (`D-102`, `D-69`) : un seul H1 egal au libelle d'index, les cinq rubriques du critere 1 dans l'ordre (`RUBRIQUES`), `## Source de verite` en derniere section, `[Retour au sommaire](sommaire.md)` en derniere ligne non vide, CRLF homogenes sans BOM, aucun nombre de quatre chiffres ou plus ; mesure du planificateur : 99 lignes et 99 CR, 13354 octets avant et 14258 apres, et les quatre modules qui ancrent la page restent verts (39 tests mesures sur une copie corrigee). Les 8 pages epinglees, l'index et `dofus_stuff/` ne bougent pas (`D-103`)."
    - statement: "Backstop nomme (`D-85`) : le rendu Markdown hors GitHub, l'execution du JavaScript et le comportement du DOM (H2 de `06-VERIFICATION.md`) ne sont pas observes par ce plan ; le controle lit `terminal.js`, il ne l'execute pas. Toute generalisation de la page formulee autrement que par les jetons et les marques declares reste hors d'atteinte, et une reformulation de la rubrique, du produit ou du script est une nouvelle mesure a faire."
      verification: backstop
  artifacts:
    - path: "docs/depannage.md"
      provides: "Rubrique « Resultat pagine » bornee a ses deux producteurs mesures — les jetons exiges sont « plus d'une page » et « porte toujours » pour la surface servie par l'application, « une seule page » et « aucune ligne de pagination » pour les ecrans de sauvegardes — et rubrique « Saisie invalide » desambiguee (T3 et observation secondaire de 06-VERIFICATION.md)"
      contains: "porte toujours sa ligne de pagination"
      min_lines: 70
    - path: "tests/test_docs_depannage.py"
      provides: "Controle `problemes_pagination` et son test `test_pagination_bornee_a_ses_deux_producteurs`, avec les motifs `formes de la pagination`, `surface de pagination non declaree` et `absolu de pagination non borne` : la page nomme les deux surfaces et leurs deux conditions, la surface servie par l'application garde sa condition, celle des ecrans de sauvegardes reste inconditionnelle, et l'absolu de portee rougit"
      contains: "def problemes_pagination"
      min_lines: 1290
  key_links:
    - from: "tests/test_docs_depannage.py"
      to: "dofus_stuff/web/routes.py"
      via: "`MOTIF_GARDE_STATUT_ROUTES` lit la garde de la ligne de statut (`routes.py:144-145`) et `MOTIF_CORPS_ROUTES` la ligne de corps des deux ecrans servis par l'application (`routes.py:477` et `:543`)"
      pattern: "MOTIF_GARDE_STATUT_ROUTES"
    - from: "tests/test_docs_depannage.py"
      to: "dofus_stuff/web/static/js/terminal.js"
      via: "`MOTIF_STATUT_JS` lit la composition inconditionnelle des deux ecrans de sauvegardes (`terminal.js:370` et `:400`), `MOTIF_ECRITURE_STATUT_JS` son adossement a `setStatus(` et `MOTIF_GARDE_PAGINATION` l'absence de garde dans ce fichier"
      pattern: "MOTIF_STATUT_JS"
    - from: "docs/depannage.md"
      to: "dofus_stuff/web/static/js/terminal.js"
      via: "la rubrique nomme le script producteur de la ligne de statut des deux ecrans de sauvegardes, et borne la regle a cette surface"
      pattern: "terminal.js"
    - from: "docs/depannage.md"
      to: "tests/test_docs_depannage.py"
      via: "la rubrique porte les jetons de condition que `SURFACES_PAGINATION` exige, surface par surface"
      pattern: "plus d'une page"
  prohibitions:
    - statement: "Ne jamais modifier `dofus_stuff/**` : la page se conforme au code, jamais l'inverse (`D-88`, `D-103`) ; les deux fichiers cites sont lus en octets et jamais ecrits."
      status: resolved
      verification: test
    - statement: "Aucune action destructive sur les donnees : jamais de `db clear`, de `db sync`, de `db status`, de drop ni de suppression sous `.data/` ou `.doc-agent/` ; `.data/dofus.sqlite3` est lu comme octets seulement, pour l'empreinte."
      status: resolved
      verification: test
    - statement: "Aucun acces reseau, aucune execution de `main()`, aucune requete de confirmation, aucun serveur lance : les seules lectures sont les deux fichiers producteurs et le client de test Flask deja livre."
      status: resolved
      verification: test
    - statement: "Ne jamais reecrire un livrable par `read_text`/`write_text` (les fins de ligne universelles y ramenent le CRLF a du LF) ni par `sed -i` (le CR est retire sur cette plateforme) : les octets sont ecrits par `read_bytes`/`write_bytes`, et la page reste a 99 CRLF pour 99 lignes."
      status: resolved
      verification: test
    - statement: "Ne pas affaiblir un controle existant pour obtenir le vert : la garde de cloture, `test_pagination_a_deux_formes`, `problemes_page` et les 14 morsures declarees de `tests/test_docs_mutation.py` restent tels quels ; aucun `skip`, aucun `xfail`, aucun `unlink`, aucune racine interdite en moins."
      status: resolved
      verification: judgment
    - statement: "N'ecrire aucune autre page que `docs/depannage.md` : les 8 pages epinglees et l'index ne bougent pas (`D-102`). Commits locaux a chemin explicite seulement (`D-91`, `D-105`), sans `git add -A` ni poussee."
      status: resolved
      verification: judgment
  assumptions:
    - "Mesures du planificateur, a rejouer si l'un des fichiers producteurs change : `routes.py` porte 1 occurrence de la garde de statut, 2 occurrences du motif de corps, 3 occurrences de `total > 1` ; `terminal.js` porte 2 occurrences de la composition de statut (lignes 370 et 400, chacune precedee de `setStatus(`), 0 occurrence de `total > 1` et 5 occurrences de `Math.max(1, ...)`."
    - "Une generalisation de la page exprimee autrement que par les jetons declares (par exemple « sur tous les ecrans », « en regle generale », « quel que soit l'ecran ») reste hors d'atteinte du controle : le module n'enumerera que les marques mesurees, et le declarent comme limite nommee."
    - "Le controle est invisible pour `tests/test_docs_mutation.py`, qui porte ses 14 morsures declarees et n'est pas modifie : la preuve de morsure du nouveau controle est portee par le troisieme item de `verify` de la tache 2, sur des copies sous `mktemp -d`."
    - "La copie de morsure n'embarque pas `.data/` : la suite complete y est donc rouge pour une raison etrangere a la mutation (le test d'empreinte de la base du depot constate la base absente). La copie est donc mesuree **module par module** (`tests/test_docs_depannage.py`), ce qui isole l'attribution du rouge."
---

<objective>
Rendre vraie la phrase de portee de la page de depannage, puis rendre le controle capable de la voir.

`docs/depannage.md:74` (page livree par `06-01`) affirme que « un ecran qui n'a qu'une seule page n'ajoute aucun motif a son statut ». C'est vrai des ecrans que l'application sert elle-meme (`dofus_stuff/web/routes.py:144-145`), ou la composition de `PAGE {page}/{total}` est sous la garde `if total > 1:` ; c'est faux des deux ecrans de sauvegardes, dont la ligne de statut est ecrite par le script (`dofus_stuff/web/static/js/terminal.js:370` et `:400`) et porte le motif **des la premiere page**, sans aucune garde. C'est l'exact inverse de la regle enoncee, et `T3` de `06-VERIFICATION.md` est reste rouge pour cela. La tache 1 borne donc la phrase a chaque surface et nomme le fichier qui la produit ; la tache 2 ajoute le controle qui rend cette portee obligatoire et qui rougit si une derive reinstalle un absolu, efface la condition d'une surface, ou deplace le procede du script.

Cette reprise ne reouvre aucun critere passe et n'ajoute aucun livrable : les deux fichiers touches sont ceux de `06-01` (la page) et de `06-04` (le module d'ancrage). `dofus_stuff/**` reste en lecture seule (`D-88`, `D-103`).
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
- `06-CONTEXT.md` (`D-19` la pagination a deux formes, `D-69`/`D-102` le gabarit des 8 pages, `D-73`/`D-75` les valeurs volatiles et les chemins de poste, `D-88`/`D-103` le produit en lecture seule, `D-85` le backstop explicite, `D-91`/`D-105` les commits a chemin explicite, `D-97`/`D-98` l'ancrage et la morsure, `D-104` l'empreinte de la base) et `06-RESEARCH.md` (le releve des deux formes de la pagination, du clavier et des commandes destructives).
- La page livree : `docs/depannage.md` — le passage de la « Saisie invalide » (ligne 28), celui de la rubrique « Résultat paginé » (ligne 74) et la ligne de tableau du message `PAGE {page}/{total}` (ligne 78). Le gabarit est pose : H1 = libelle de l'index, cinq rubriques, `## Source de verite` en derniere section, ligne de retour en derniere ligne non vide, CRLF.
- Le produit, en lecture seule : `dofus_stuff/web/routes.py` (lignes 143-152, 474-477, 540-543, 334-351) et `dofus_stuff/web/static/js/terminal.js` (lignes 11, 355-402). La ligne de statut de l'ecran est assemblee par une seule fonction, apres quoi `indicators` est joint : c'est cette fonction qui est gardee pour les ecrans servis par Flask.
- Le module d'ancrage `tests/test_docs_depannage.py` : 1253 lignes, 10 tests dont 9 sans `.data/` ; constantes `SOURCE_ROUTES` (ligne 58), `SOURCE_TERMINAL_JS` (ligne 61), `RUBRIQUES` (ligne 66, la famille `pagination` porte le titre « Résultat paginé »), `MOTIF_PAGINATION` (ligne 151, valeur `formes de la pagination`), `RACINE_DEPOT` (ligne 51), helpers `_texte_page` (272), `_corps_par_titre` (287) ; le controle comportemental deja livre `test_pagination_a_deux_formes` (1129) rend les deux ecrans de liste (`/list?page=1&size=1` statut pagine, `/sets?page=1&size=1` statut non pagine) et constate le corps toujours pagine.
- Mesures du planificateur, deja rejouees (hors livrable, fichier de travail `06-05-prototype.py` du repertoire de travail) : la page livree rend 5 constats sous le nouveau controle, la page corrigee 0, et les trois derivees rendent respectivement 2, 1 et 1 constats portant leur motif nomme. La correction de la tache 1, appliquee a une copie, laisse les quatre modules de la page verts (39 tests) et la page homogene (99 lignes, 99 CR, sans BOM).
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : borner la portee de la pagination a ses deux producteurs, et desambiguer la phrase de la saisie invalide</name>
  <files>docs/depannage.md</files>
  <read_first>
    - docs/depannage.md (les trois passages a corriger : la rubrique « Saisie invalide », la rubrique « Résultat paginé », la ligne de tableau du message `PAGE {page}/{total}`)
    - dofus_stuff/web/routes.py (lignes 143-152 : la garde de la ligne de statut et la composition du motif ; lignes 474-477 et 540-543 : les lignes de corps des deux ecrans servis par l'application ; lignes 334-351 : la recherche qui precede l'ecriture de `AUCUN RESULTAT.`)
    - dofus_stuff/web/static/js/terminal.js (lignes 355-402 : les deux compositions `"PAGE " + page + "/" + total` ecrites par `setStatus`, sans garde ; ligne 11 : l'element `.row.status`)
    - .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-VERIFICATION.md (le defaut `T3` — la portee de la pagination — et l'observation secondaire sur la ligne 28 de la page)
    - .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md (`D-19`, `D-69`, `D-102`, `D-73`, `D-75`)
    - tests/test_docs_depannage.py (le controle `problemes_pagination` de la tache 2, et les jetons que `SURFACES_PAGINATION` exigera dans la rubrique)
    - docs/parcours-simplifie.md (le renvoi du module vers `MOTIF_PAGINATION` : la rubrique « Résultat paginé » reste le lieu unique de la regle)
  </read_first>
  <action>
Faire **trois** remplacements sur `docs/depannage.md`, chacun sur une ancre d'**une seule ligne** (donc sans traverser de sequence CRLF), au niveau des octets. Le mecanisme obligatoire : lire par `read_bytes()`, decoder en UTF-8, remplacer, encoder en UTF-8, ecrire par `write_bytes()` ; l'insertion de plusieurs paragraphes se fait en joignant les nouveaux paragraphes par la sequence **CRLF CRLF** explicite. `read_text`/`write_text` (les fins de ligne universelles y ramenent le CRLF a du LF) et `sed -i` (le CR est retire sur cette plateforme) sont interdits : la mesure du planificateur est 99 CR pour 99 lignes, et la page livree en porte 96 pour 96.

**1. La rubrique « Saisie invalide » (`docs/depannage.md:28`).** Remplacer la ligne entiere, dont l'ancre unique est :

`Ces refus tombent tous **avant** tout travail : aucune recherche, aucun calcul, aucune écriture. Ils sont rendus par la ligne de statut de l'écran — c'est là que le produit écrit ses refus — et la ligne de commande les écrit différemment, quand elle les écrit. Les identifiants et les noms que vous saisissez sont des valeurs d'exécution : cette page ne les recopie jamais.`

par cette ligne unique (l'ouverture ne range plus `AUCUN RESULTAT.` parmi les refus, et le pronom reste coherent avec la phrase qui suit) :

`Les refus de cette rubrique tombent tous **avant** tout travail : aucune recherche, aucun calcul, aucune écriture. Une ligne de cette rubrique fait exception et le dit elle-même : `AUCUN RESULTAT.` est écrit **après** la recherche, quand elle n'a rien trouvé, et il signale seulement que rien ne correspond — ce n'est donc ni un refus, ni une panne. Ces refus sont rendus par la ligne de statut de l'écran — c'est là que le produit écrit ses refus — et la ligne de commande les écrit différemment, quand elle les écrit. Les identifiants et les noms que vous saisissez sont des valeurs d'exécution : cette page ne les recopie jamais.`

Ancrages mesures : `AUCUN RESULTAT.` est ecrit par `dofus_stuff/web/routes.py:351`, apres la recherche de `:334-335` ; la mention « avant tout travail » reste vraie des refus de la rubrique, sauf de cette ligne.

**2. La rubrique « Résultat paginé » (`docs/depannage.md:74`).** Remplacer la ligne entiere, dont l'ancre unique est :

`La pagination a **deux formes**, et le produit ne les écrit pas au même endroit. La ligne de **statut** ne porte le motif de page que lorsque l'écran est réellement paginé — un écran qui n'a qu'une seule page n'ajoute aucun motif à son statut ; le **corps** de l'écran, lui, porte toujours sa ligne de pagination. Un diagnostic qui exigerait le motif dans le statut d'un écran à page unique serait donc faux sur un produit correct.`

par **quatre** paragraphes joints par la sequence CRLF CRLF (la page reste donc en CRLF), chacun borne a la surface mesuree :

`La pagination a **deux formes**, et le produit ne les écrit pas au même endroit. La règle dépend de la surface qui écrit la ligne de statut, et cette page la donne pour chacune : elle nomme à chaque fois le fichier qui la produit.`

`Sur les écrans que l'application sert elle-même (`dofus_stuff/web/routes.py`), la ligne de **statut** ne porte le motif de page que lorsque l'écran compte plus d'une page, et le **corps** de l'écran porte toujours sa ligne de pagination.`

`Sur les deux écrans de sauvegardes, la ligne de **statut** est écrite par le script (`dofus_stuff/web/static/js/terminal.js`) : elle porte le motif **même sur une seule page**, et la liste comme le détail affichent donc `PAGE 1/1`. Leur **corps**, lui, ne porte aucune ligne de pagination.`

`Un diagnostic qui exigerait le motif sur un écran d'une seule page, ou qui exigerait son absence, serait donc faux d'un côté comme de l'autre.`

Ancrages mesures, a ne pas paraphraser : `dofus_stuff/web/routes.py:144-145` compose `PAGE {page}/{total}` sous la garde `if total > 1:` (1 occurrence de la garde dans le fichier) et les lignes de corps de `:477` et `:543` la composent toujours (2 occurrences du motif `f"PAGE {page}/{total_pages}`) ; `dofus_stuff/web/static/js/terminal.js:370` et `:400` composent `"PAGE " + page + "/" + total` par `setStatus(` (2 occurrences de la composition, aucune occurrence de `total > 1` dans le fichier, `total` etant borne par `Math.max(1, ...)`), et ces deux compositions sont les seules du fichier — le corps des deux ecrans, ecrit par `renderBodyRows`, ne porte donc aucune ligne de pagination. La phrase doit rester exactement ce que le code fait : aucun « seulement », aucun « jamais », aucun « quel que soit l'ecran » que le produit ne soutient pas.

**3. La ligne de tableau du message `PAGE {page}/{total}` (`docs/depannage.md:78`).** Remplacer la ligne entiere, dont l'ancre unique est :

`| `PAGE {page}/{total}` | interface web, ligne de statut d'un écran paginé, sous la forme du numéro de page sur le nombre de pages | le motif n'apparaît que si l'écran compte plus d'une page ; utilisez les deux touches de navigation pour changer de page |`

par cette ligne unique (premiere cellule inchangee : le message reste celui de la table `MESSAGES`, et la troisieme cellule porte les deux conditions) :

`| `PAGE {page}/{total}` | interface web, ligne de statut d'un écran paginé ou d'un écran de sauvegardes, sous la forme du numéro de page sur le nombre de pages | sur un écran que l'application sert, le motif n'apparaît que si l'écran compte plus d'une page ; sur les écrans de sauvegardes, écrits par le script, il est là même sur une seule page ; utilisez les deux touches de navigation pour changer de page |`

Respecter le gabarit pendant l'edition (`D-102`, `D-69`) : un seul H1, les cinq rubriques dans l'ordre de `RUBRIQUES`, `## Source de verite` en derniere section, `[Retour au sommaire](sommaire.md)` en derniere ligne non vide, aucun nombre de quatre chiffres ou plus, aucun chemin de poste, aucun lien externe, aucune duree promise. N'ecrire aucune autre page et aucun fichier de `dofus_stuff/`.

Le fichier de travail du planificateur, `.gsd-tmp/06-05-t1-essai.py`, porte exactement ces trois remplacements : il peut servir de reference de frappe, jamais d'ecriture du livrable (les livrables sont ecrits par la session d'execution, et ce fichier de travail n'est pas un livrable).
  </action>
  <acceptance_criteria>
    - `docs/depannage.md` porte, dans la rubrique « Résultat paginé » : `dofus_stuff/web/routes.py`, `dofus_stuff/web/static/js/terminal.js`, `plus d'une page`, `porte toujours`, `une seule page`, `aucune ligne de pagination`, `PAGE 1/1`.
    - La page ne porte **plus** la condition qui rangeait la ligne de statut des ecrans de sauvegardes dans la meme regle que celle des ecrans servis par l'application : la ligne 74 livree n'est plus presente, et la phrase d'ouverture de la rubrique annonce desormais une regle **par surface**, nommant le fichier qui la produit.
    - La rubrique « Saisie invalide » porte `Une ligne de cette rubrique fait exception` et continue de citer `AUCUN RESULTAT.` : le message n'est plus range parmi les refus qui tombent avant tout travail.
    - La ligne de tableau du message `PAGE {page}/{total}` garde sa premiere cellule et porte les deux conditions dans sa troisieme cellule.
    - Le gabarit tient : un seul H1 egal au libelle d'index, cinq rubriques dans l'ordre, `## Source de verite` en derniere section, ligne de retour en derniere ligne non vide, CRLF homogenes sans BOM, aucun nombre de quatre chiffres ou plus, aucun chemin de poste, aucun lien externe.
    - Les quatre modules qui ancrent la page sont verts, et `dofus_stuff/**` n'a pas bouge.
  </acceptance_criteria>
  <verify>
1. Fragments et octets de la page (extraction de la rubrique par son titre, puis controle de ses seuls jetons) :
   `PY="$PWD/.venv/Scripts/python.exe"; "$PY" -c "import pathlib,re;t=pathlib.Path('docs/depannage.md').read_bytes().decode('utf-8');f=re.search('^## R\\u00e9sultat pagin\\u00e9\\r?\\n(.*?)^## ',t,re.M|re.S);assert f is not None,'section de la pagination introuvable';c=f.group(1);attendus=('dofus_stuff/web/routes.py','dofus_stuff/web/static/js/terminal.js',\"plus d'une page\",'porte toujours','une seule page','aucune ligne de pagination','PAGE 1/1');manquants=[a for a in attendus if a not in c];assert not manquants,'fragments absents de la section de la pagination : %r'%manquants;assert 'Une ligne de cette rubrique fait exception' in t,'la rubrique de la saisie invalide n est pas desambiguee';print('page : les deux producteurs sont nommes, leurs deux conditions sont portees, et la rubrique de la saisie invalide est desambiguee')"`
   attendu : la phrase imprimee, exit 0 (mesure du planificateur : cet item passe sur une copie corrigee et rougit sur la page livree).
2. Octets homogenes de la page :
   `PY="$PWD/.venv/Scripts/python.exe"; "$PY" -c "import pathlib;b=pathlib.Path('docs/depannage.md').read_bytes();assert b.count(b'\r\n')==len(b.splitlines()),'fins de ligne non homogenes : %d CR pour %d lignes'%(b.count(b'\r\n'),len(b.splitlines()));assert not b.startswith(b'\xef\xbb\xbf'),'BOM inattendu';print('page : %d lignes, %d CR, aucun BOM'%(len(b.splitlines()),b.count(b'\r\n')))"`
   attendu : 99 lignes pour 99 CR, aucun BOM (mesure du planificateur : 13354 octets avant, 14258 apres).
3. Les quatre modules qui ancrent la page, puis la suite complete :
   `PY="$PWD/.venv/Scripts/python.exe"; "$PY" -m pytest tests/test_docs_depannage.py tests/test_docs_structure.py tests/test_docs_completude.py tests/test_docs_code_anchor.py -q && "$PY" -m pytest -q`
   attendu : 39 tests verts pour les quatre modules (mesure du planificateur sur copie corrigee), zero echec sur la suite complete, les 3 skips nommes restant des skips.
4. Le produit n'a pas bouge (`D-88`, `D-103`) :
   `git status --porcelain -- dofus_stuff | wc -l`
   attendu : `0`.
  </verify>
  <done>La page porte la regle de pagination bornee a ses deux producteurs nommes et leurs deux conditions mesurees, la rubrique de la saisie invalide ne range plus `AUCUN RESULTAT.` parmi les refus qui tombent avant tout travail, le gabarit et les quatre modules d'ancrage tiennent, et `dofus_stuff/**` est intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : le controle qui tient la portee de la pagination, et sa preuve de morsure</name>
  <files>tests/test_docs_depannage.py</files>
  <read_first>
    - tests/test_docs_depannage.py (constantes `SOURCE_ROUTES` ligne 58, `SOURCE_TERMINAL_JS` ligne 61, `RUBRIQUES` ligne 66, `MOTIF_PAGINATION` ligne 151, `RACINE_DEPOT` ligne 51 ; helpers `_texte_page` ligne 272 et `_corps_par_titre` ligne 287 ; controle comportemental `test_pagination_a_deux_formes` ligne 1129 ; garde de cloture du harnais ligne 965, qui lit ce module par `ast` et n'admet ni import de base, de processus, de socket ou de reseau, ni appel de suppression)
    - docs/depannage.md (la rubrique « Résultat paginé » corrigee par la tache 1 : c'est elle que ce controle mesure)
    - dofus_stuff/web/routes.py (lignes 143-152 : la garde de la ligne de statut ; lignes 474-477 et 540-543 : les deux lignes de corps)
    - dofus_stuff/web/static/js/terminal.js (lignes 355-402 : les deux compositions de statut et la ligne 11, l'element `.row.status`)
    - .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md (`D-19`, `D-97`, `D-98`, `D-85`)
  </read_first>
  <behavior>
    - Sur la page **corrigee** de la tache 1, le controle rend **0 constat** : la rubrique nomme les deux fichiers producteurs et porte les quatre jetons de condition. Mesure du planificateur.
    - Sur la page **livree** par `06-01`, il rend **5 constats** : les deux surfaces non nommees, le jeton « aucune ligne de pagination » absent, et **deux** marques d'absolu (« aucun motif a son statut », « n'ajoute aucun motif »). Mesure du planificateur. Le rouge tient donc a ce que la phrase dit du produit, jamais a la presence d'un fichier.
    - Un absolu reintroduit dans la rubrique rougit avec `absolu de pagination non borne` (2 constats), le jeton de condition d'une surface efface rougit avec `surface de pagination non declaree`, et le procede du script deplace rougit avec `formes de la pagination` — dans les trois cas, la copie etait verte avant la mutation.
    - Le controle ne touche ni `dofus_stuff/**` (lecture d'octets seulement), ni `.data/`, ni le reseau, et n'ajoute aucune dependance.
  </behavior>
  <action>
Ajouter a `tests/test_docs_depannage.py` les constantes, la fonction de mesure et le test de la portee de la pagination. Le module garde son style : commentaires qui disent pourquoi, messages qui nomment la source du fait et la decision, docstrings en francais sans accents dans le code (les chaines de la page, elles, portent leurs accents).

**A. Deux motifs, a cote de `MOTIF_PAGINATION` (ligne 151), qui portent les deux nouvelles familles de constats :**

`MOTIF_PAGINATION_SURFACE = "surface de pagination non declaree"`
`MOTIF_PAGINATION_ABSOLU = "absolu de pagination non borne"`

**B. Les procedes mesures, les surfaces et les marques d'absolu, juste apres le bloc `MOTIF_PAGE_COMPOSE` (ligne 212), avec leur fondement mesure en commentaire.** Le motif de la surface servie par l'application est la garde **elle-meme**, pour ne pas se confondre avec les deux autres `total > 1` du meme fichier (`:133`, `:137`) qui bornent la navigation ; la composition du script est adossee a `setStatus(`, ce qui la range sur la ligne de statut (`terminal.js:11`) et jamais dans le corps, ecrit par `renderBodyRows` :

```
# Les deux surfaces qui composent une ligne de statut, et le procede de chacune lu dans son fichier
# producteur, avec le nombre d'occurrences attendu. La surface servie par l'application garde sa
# composition (`routes.py:144-145`) et compose toujours sa ligne de corps (`routes.py:477`, `:543`) ;
# celle des deux ecrans de sauvegardes compose son statut sans aucune garde (`terminal.js:370`, `:400`)
# et n'ecrit aucune ligne de pagination dans son corps. C'est cette asymetrie que la page doit dire,
# surface par surface (D-19).
MOTIF_GARDE_STATUT_ROUTES = re.compile(
    r'if\s+total\s*>\s*1:\s*indicators\.append\(f"PAGE \{page\}/\{total\}"\)'
)
MOTIF_CORPS_ROUTES = re.compile(r'f"PAGE \{page\}/\{total_pages\}')
MOTIF_STATUT_JS = re.compile(r'"PAGE "\s*\+\s*page\s*\+\s*"/"\s*\+\s*total')
MOTIF_ECRITURE_STATUT_JS = "setStatus("
MOTIF_GARDE_PAGINATION = re.compile(r"total\s*>\s*1")

SURFACES_PAGINATION = (
    (
        "application",
        SOURCE_ROUTES,
        (MOTIF_GARDE_STATUT_ROUTES, 1),
        (MOTIF_CORPS_ROUTES, 2),
        True,
        ("plus d'une page", "porte toujours"),
    ),
    (
        "sauvegardes",
        SOURCE_TERMINAL_JS,
        (MOTIF_STATUT_JS, 2),
        None,
        False,
        ("une seule page", "aucune ligne de pagination"),
    ),
)

# Marques d'absolu de portee : elles disent d'un seul tenant ce que le produit fait surface par
# surface. Le controle ne refuse que ces marques, jamais une formulation bornee (D-19).
ABSOLUS_PAGINATION = (
    "aucun motif a son statut",
    "n'ajoute aucun motif",
    "ne porte jamais le motif",
    "le motif n'apparait jamais",
    "sur n'importe quel ecran",
    "quelle que soit la surface",
    "quel que soit l'ecran",
    "sans exception de surface",
)
```

**C. La fonction `problemes_pagination(docs_dir: Path, sections, normalize) -> list[str]`, inseree apres `problemes_page` (qui finit ligne 853) et avant `_imports_du_module` (ligne 856).** Elle lit la seule page du module par `_texte_page`, extrait la rubrique par son titre (`dict(RUBRIQUES)["pagination"]`) avec `_corps_par_titre`, normalise ce corps avec la fixture `normalize` (accents et casse), puis, pour chaque surface de `SURFACES_PAGINATION` dans l'ordre : lit le fichier producteur par `RACINE_DEPOT / fichier` en `read_bytes().decode("utf-8")`, exige le nombre d'occurrences mesure du procede de statut, exige aussi le procede de corps quand la surface en declare un, refuse la presence d'une garde pour la surface declaree inconditionnelle et exige que chaque composition de cette surface soit adossee a `MOTIF_ECRITURE_STATUT_JS` dans les 80 caracteres qui precedent, puis exige le nom du fichier (chemin complet en minuscules ou nom de base) et chaque jeton de la surface dans le corps normalise. Enfin, si et seulement si la surface inconditionnelle a ete mesuree, chaque marque de `ABSOLUS_PAGINATION` presente dans le corps normalise est un constat. Chaque constat nomme le fichier producteur et la decision, et la fonction porte une `Limite nommee` dans sa docstring : elle est statique, elle lit les deux fichiers producteurs, elle n'execute pas le script et ne rend pas les deux ecrans de sauvegardes — c'est `test_pagination_a_deux_formes` (ligne 1129) qui mesure le rendu des ecrans servis par l'application. Le forme des constats, a garder telle quelle (le vocabulaire des messages tient aux motifs nommes et a leurs occurrences mesurees) :

```
    texte = _texte_page(docs_dir)
    titre = dict(RUBRIQUES)["pagination"]
    corps = _corps_par_titre(texte, sections).get(titre)
    if corps is None:
        return [
            f"{MOTIF_PAGINATION} : la rubrique « {titre} » est absente de {PAGE} ; attendu la rubrique "
            f"des deux formes de la pagination, adossee a ses deux producteurs ({SOURCE_ROUTES}, "
            f"{SOURCE_TERMINAL_JS}, D-19)"
        ]
    normalise = normalize(corps)
    constats: list[str] = []
    inconditionnel_mesure = False

    for cle, fichier, statut, corps_mesure, conditionnel, jetons in SURFACES_PAGINATION:
        source = RACINE_DEPOT / fichier
        contenu = source.read_bytes().decode("utf-8") if source.is_file() else ""
        motif, attendues = statut
        occurrences = len(motif.findall(contenu))
        if occurrences != attendues:
            constats.append(
                f"{MOTIF_PAGINATION} : le procede de la surface « {cle} » n'est plus mesure dans "
                f"{fichier} ({occurrences} occurrence(s), attendu {attendues}) ; attendu le procede lu "
                f"dans le fichier producteur, la page decrivant ce que ce fichier fait ({fichier}, D-19)"
            )
            continue
        if corps_mesure is not None:
            motif_corps, attendues_corps = corps_mesure
            occurrences_corps = len(motif_corps.findall(contenu))
            if occurrences_corps != attendues_corps:
                constats.append(
                    f"{MOTIF_PAGINATION} : la ligne de corps de la surface « {cle} » n'est plus composee "
                    f"dans {fichier} ({occurrences_corps} occurrence(s), attendu {attendues_corps}) ; "
                    f"attendu le corps des deux ecrans servis par l'application, toujours pagine "
                    f"({fichier}, D-19)"
                )
                continue
        else:
            garde = MOTIF_GARDE_PAGINATION.search(contenu)
            if garde:
                constats.append(
                    f"{MOTIF_PAGINATION} : la surface « {cle} » porte « {garde.group(0)} » dans {fichier} ; "
                    f"attendu une composition du motif sans garde sur le nombre de pages, la page "
                    f"decrivant ce comportement ({fichier}, D-19)"
                )
                continue
            deplacees = [
                occurrence
                for occurrence in motif.finditer(contenu)
                if MOTIF_ECRITURE_STATUT_JS not in contenu[max(0, occurrence.start() - 80):occurrence.start()]
            ]
            if deplacees:
                constats.append(
                    f"{MOTIF_PAGINATION} : la composition du motif de la surface « {cle} » n'est plus "
                    f"adossee a « {MOTIF_ECRITURE_STATUT_JS} » dans {fichier} ({len(deplacees)} "
                    f"occurrence(s)) ; attendu la composition ecrite sur la ligne de statut, ce fichier "
                    f"portant les seules compositions lues par la page ({fichier}, D-19)"
                )
                continue
            inconditionnel_mesure = True

        noms = (fichier.lower(), Path(fichier).name.lower())
        if not any(nom in normalise for nom in noms):
            constats.append(
                f"{MOTIF_PAGINATION_SURFACE} : la rubrique « {titre} » de {PAGE} ne nomme pas la surface "
                f"« {cle} » ({fichier}) ; attendu le fichier qui compose la ligne de statut, la regle "
                f"dependant de la surface qui l'ecrit (D-19)"
            )
        for jeton in jetons:
            if jeton not in normalise:
                constats.append(
                    f"{MOTIF_PAGINATION_SURFACE} : la rubrique « {titre} » de {PAGE} ne porte pas "
                    f"« {jeton} » pour la surface « {cle} » ; attendu la condition mesuree de cette "
                    f"surface ({fichier}, D-19)"
                )

    if inconditionnel_mesure:
        for marque in ABSOLUS_PAGINATION:
            if marque in normalise:
                constats.append(
                    f"{MOTIF_PAGINATION_ABSOLU} : la rubrique « {titre} » de {PAGE} porte « {marque} » ; "
                    f"attendu une formulation bornee a la surface mesuree, la surface sans garde "
                    f"ecrivant le motif sur une seule page ({SOURCE_TERMINAL_JS}, D-19)"
                )
    return constats
```

**D. Le test `test_pagination_bornee_a_ses_deux_producteurs(docs_dir, sections, normalize)`, insere apres `test_pagination_a_deux_formes` (qui finit ligne 1165).** Docstring : la page livree affirmait la regle de la ligne de statut pour tout ecran, ce controle tient les deux surfaces qui l'ecrivent — celle que l'application sert, ou la composition est gardee, et celle des deux ecrans de sauvegardes, ou le script la compose sans garde — et il refuse une marque d'absolu de portee ; il est lie aux fichiers producteurs (`routes.py:144-145`, `terminal.js:370` et `:400`) et laisse a `test_pagination_a_deux_formes` la mesure du rendu. Corps : `constats = problemes_pagination(docs_dir, sections, normalize)` puis `assert not constats, (f"{PAGE} : constats sur la portee de la pagination : " + " ; ".join(constats) + f" ; attendu la rubrique nommant les deux producteurs de la ligne de statut et portant la condition de chacun, sans aucune marque d'absolu ({SOURCE_ROUTES}, {SOURCE_TERMINAL_JS}, D-19)")`.

Contraintes de cloture a ne pas franchir : aucun import nouveau (seuls `re`, `Path`, `pytest`, le produit et le client de test sont deja importes), aucun appel de suppression, aucune racine interdite, aucun `skip`/`xfail`, aucune lecture ni ecriture sous `.data/`, aucune modification de `tests/test_docs_mutation.py` (ses 14 morsures declarees restent 14) et aucune modification de `dofus_stuff/**`. Ne pas ajouter le nouveau test a une enumeration existante de tests : il est collecte par son nom.
  </action>
  <acceptance_criteria>
    - `tests/test_docs_depannage.py` porte la fonction `problemes_pagination` et le test `test_pagination_bornee_a_ses_deux_producteurs`, les constantes `MOTIF_GARDE_STATUT_ROUTES`, `MOTIF_CORPS_ROUTES`, `MOTIF_STATUT_JS`, `MOTIF_ECRITURE_STATUT_JS`, `MOTIF_GARDE_PAGINATION`, `SURFACES_PAGINATION`, `ABSOLUS_PAGINATION`, `MOTIF_PAGINATION_SURFACE`, `MOTIF_PAGINATION_ABSOLU`.
    - Le controle est lie aux **deux** comportements mesures : le procede garde de `dofus_stuff/web/routes.py:144-145` (et la ligne de corps de `:477` et `:543`) pour la surface servie par l'application, la composition inconditionnelle de `dofus_stuff/web/static/js/terminal.js:370` et `:400` pour les ecrans de sauvegardes, avec l'absence de garde `total > 1` verifiee dans le script.
    - Le module reste vert sur la page corrigee (10 tests dans `tests/test_docs_depannage.py`, zero echec) et le rouge du controle porte **soit** sur une surface non nommee, **soit** sur un jeton de condition absent, **soit** sur une marque d'absolu : jamais sur un fichier manquant ni sur une exception.
    - Les trois derivees mesurees sur des copies rougissent avec leur motif nomme, et aucune ne se solde par `MUTATION NON DETECTEE`.
    - La garde de cloture du harnais reste verte : aucun import, aucun appel, aucune racine nouvelle qui rougirait le controle `ast`.
    - `dofus_stuff/**` et `.data/` n'ont pas bouge : l'empreinte `sha256` de `.data/dofus.sqlite3` reste `e3793d64cb7939ad1a51837b075b6b95e03d64c878fcb9cc07f86c00bb8fef7b` pour `24989696` octets.
  </acceptance_criteria>
  <verify>
1. Le module d'ancrage, seul (aucune dependance a `.data/`, donc rejouable sur une copie) :
   `PY="$PWD/.venv/Scripts/python.exe"; "$PY" -m pytest tests/test_docs_depannage.py -q`
   attendu : 10 tests verts (9 avant ce plan, mesures en 0.4 s).
2. La garde de cloture du harnais et les quatre modules de la page, dans le depot :
   `PY="$PWD/.venv/Scripts/python.exe"; "$PY" -m pytest tests/test_docs_depannage.py::test_garde_de_cloture_du_harnais tests/test_docs_structure.py tests/test_docs_completude.py tests/test_docs_code_anchor.py -q`
   attendu : zero echec.
3. **Preuve de morsure, sur des copies sous `mktemp -d`** (jamais dans le depot). Chaque copie est mesuree **verte** avant sa mutation (`COPIE ROUGE AVANT MUTATION` sinon), puis la mutation est appliquee par ecriture d'octets (aucun `sed -i`, aucun `read_text`), puis la copie est rejouee et doit rougir **en portant son motif nomme** :

   ```
   PY="$PWD/.venv/Scripts/python.exe"; ok=1
   for m in absolu_sans_surface condition_effacee procede_js_efface; do
     T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/
     avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_depannage.py -q 2>&1)"
     if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then
       echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue
     fi
     case "$m" in
       absolu_sans_surface)
         (cd "$T" && "$PY" -c 'import pathlib;q=chr(39);c=pathlib.Path("docs/depannage.md");s=c.read_bytes().decode("utf-8");a="La pagination a **deux formes**";assert a in s,"ancre de la rubrique de pagination absente";c.write_bytes(s.replace(a,"Un ecran qui n"+q+"a qu"+q+"une seule page n"+q+"ajoute aucun motif a son statut. "+a,1).encode("utf-8"))') ;;
       condition_effacee)
         (cd "$T" && "$PY" -c 'import pathlib;q=chr(39);c=pathlib.Path("docs/depannage.md");s=c.read_bytes().decode("utf-8");a="une seule page";assert s.count(a)>=1,"jeton de condition de la surface des sauvegardes absent";c.write_bytes(s.replace(a,"plus d"+q+"une page").encode("utf-8"))') ;;
       procede_js_efface)
         (cd "$T" && "$PY" -c 'import pathlib;q=chr(34);c=pathlib.Path("dofus_stuff/web/static/js/terminal.js");s=c.read_bytes().decode("utf-8");a=q+"PAGE "+q+" + page + "+q+"/"+q+" + total";assert s.count(a)==2,"composition du motif de statut absente ou non mesuree : %d"%s.count(a);c.write_bytes(s.replace(a,q+"PAGE "+q+" + page").encode("utf-8"))') ;;
     esac
     motif=''
     case "$m" in
       absolu_sans_surface) motif='absolu de pagination non borne' ;;
       condition_effacee) motif='surface de pagination non declaree' ;;
       procede_js_efface) motif='formes de la pagination' ;;
     esac
     out="$(cd "$T" && "$PY" -m pytest tests/test_docs_depannage.py -q 2>&1)"
     if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then
       echo "mutation detectee ($m), motif « $motif »"
     else
       echo "MUTATION NON DETECTEE ($m) : motif « $motif » absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0
     fi
   done
   if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 06-05 (TACHE 2) EN ECHEC"; exit 1; fi
   echo "morsures 06-05 tache 2 : 3/3 detectees (copie verte avant chaque mutation)"
   ```

   attendu : les trois lignes `mutation detectee (...)`, chacune avec le motif nomme (mesure du planificateur : `absolu de pagination non borne` 2 constats, `surface de pagination non declaree` 1 constat, `formes de la pagination` 1 constat), puis la ligne finale. La mutation `absolu_sans_surface` reintroduit exactement le defaut de `T3` sur une copie corrigee : c'est la mesure qui remplace la relecture humaine du rouge. La copie n'embarque pas `.data/`, donc la mesure porte **module par module** (la suite complete y serait rouge pour une raison etrangere a la mutation) : `tests/test_docs_depannage.py` ne depend pas de `.data/`.
4. La suite complete dans le depot, avec l'empreinte de la base lue autour d'elle (`GARD-03`, `D-104`), et le produit intact :
   `PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; F='import hashlib,pathlib;b=pathlib.Path(".data/dofus.sqlite3").read_bytes();print("%d:%d:%s"%(len(b),b.count(b"\x00"),hashlib.sha256(b).hexdigest()))'; "$PY" -c "$F" > "$T/avant.txt"; "$PY" -m pytest -q; statut=$?; "$PY" -c "$F" > "$T/apres.txt"; diff -q "$T/avant.txt" "$T/apres.txt" || { echo "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE"; exit 1; }; [ "$statut" -eq 0 ] && echo "suite complete verte, empreinte .data/ identique : $(cat "$T/apres.txt")"; git diff --name-only HEAD -- dofus_stuff | wc -l; git status --porcelain -- dofus_stuff | wc -l`
   attendu : la suite complete verte (250 tests, les 3 skips nommes restant des skips), l'empreinte `24989696:...:e3793d64cb7939ad1a51837b075b6b95e03d64c878fcb9cc07f86c00bb8fef7b`, puis `0` et `0`.
  </verify>
  <done>Le module d'ancrage porte le controle de portee de la pagination, lie aux deux comportements mesures du produit ; il est vert sur la page corrigee, rouge sur la page livree par `06-01` et sur chacune des trois derivees mesurees, avec son motif nomme ; `dofus_stuff/**` et `.data/` sont intacts.</done>
</task>

<done>Le module d'ancrage porte le controle de portee de la pagination, lie aux deux comportements mesures du produit ; il est vert sur la page corrigee, rouge sur la page livree par `06-01` et sur chacune des trois derivees mesurees, avec son motif nomme ; `dofus_stuff/**` et `.data/` sont intacts.</done>
</task>

</tasks>

<threat_model>
Menaces et reponses retenues, du plus probable au plus rare :

- **La page reste fausse d'un autre cote.** La phrase corrigee dit deux choses du code : la ligne de statut de la surface servie par l'application est gardee, celle des ecrans de sauvegardes ne l'est pas. Les deux sont **mesurees** (`routes.py:144-145`, 1 occurrence de la garde ; `terminal.js:370` et `:400`, 2 compositions, 0 occurrence de `total > 1`), et le controle exige le nombre d'occurrences, pas la seule presence. Reponse : le troisieme item de `verify` de la tache 2 rejoue la mesure sur des copies.
- **La reponse du refus.** Le controle ne refuse rien d'autre que ce qui est faux : une surface non nommee, un jeton de condition absent, une marque d'absolu. Il ne refuse pas une reformulation bornee, et la liste des marques est finie et declaree (limite nommee dans les assertions du plan).
- **Un absolu formule autrement.** « sur tous les ecrans », « en regle generale », « quel que soit l'ecran » ou toute generalisation hors des marques declarees echappe au controle. La marque `quel que soit l'ecran` et deux autres formes voisines sont couvertes ; le reste est une limite nommee, pas une garantie.
- **La mesure ne prouve pas la page.** Le controle lit `terminal.js`, il n'execute pas le JavaScript : la phrase « Leur corps ne porte aucune ligne de pagination » est bornee par les deux seules compositions du fichier, adossees a `setStatus(` (`terminal.js:11`), et non par un rendu. Reponse : le controle exige que toutes les compositions du script soient adossees a `setStatus(`, donc une three composition ailleurs (corps ou en-tete) rougit.
- **La derive destructrice.** Aucune commande du plan ne touche `.data/` : lecture d'octets et `sha256` seulement, jamais de `db clear`, de `db sync`, de drop ni de suppression. L'empreinte `e3793d64cb7939ad1a51837b075b6b95e03d64c878fcb9cc07f86c00bb8fef7b` (24989696 octets) est mesuree avant et apres la suite complete, et le module d'ancrage la porte deja pour `D-104`/`GARD-03`.
- **La mutation qui n'ecrit rien.** Les mutations de la copie sont des remplacements d'octets sur des copies sous `mktemp -d` : un `cp -r` incomplet ferait echouer la mutation par son assertion (`ancre ... absente`), et les `sed -i` (qui retireraient le CR de la copie) sont exclus par construction. Si la copie est rouge avant mutation, le script le dit (`COPIE ROUGE AVANT MUTATION`) au lieu de compter une fausse morsure.
- **Le perimetre qui deborde.** Le plan n'ecrit que deux fichiers, ne modifie aucune page epinglee, ne modifie pas `tests/test_docs_mutation.py`, et laisse `dofus_stuff/**` intact : `git diff --name-only HEAD -- dofus_stuff` doit rendre `0` (mesure du planificateur).
</threat_model>

<verification>
A rejouer apres les deux taches, en plus des items de `verify` de chacune :

1. Suite complete et empreinte de la base (item 4 de `verify` de la tache 2) : suite verte, les 3 skips nommes restant des skips, et `sha256` inchange de `.data/dofus.sqlite3`.
2. Suite complete **sans reseau** (critere 4 de la phase) : `PY="$PWD/.venv/Scripts/python.exe"; PYTHONPATH="$PWD/.gsd-tmp" "$PY" -m pytest -q -p no_net` — attendu la banniere `greffon de preuve 06-04 : 6 pieges armes avant la collecte -> socket.socket, socket.create_connection, socket.socketpair, urllib.request.urlopen, dofus_stuff.cli.main, dofus_stuff.web.__main__.main` (6 pieges armes : aucun acces reseau, aucun `main()` execute, aucun serveur) et la suite verte (250 tests mesures apres ce plan, 249 avant).
3. Morsure du plan (item 3 de `verify` de la tache 2) : `3/3 detectees`, chaque derivee avec son motif nomme.
4. La page corrigee reste conforme au gabarit : item 1 et item 2 de `verify` de la tache 1, et les quatre modules de la page verts.
5. Perimetre : `git diff --name-only HEAD -- dofus_stuff | wc -l` -> `0` ; `git status --porcelain -- dofus_stuff | wc -l` -> `0` ; aucune autre page de `docs/` modifiee (`git status --porcelain -- docs` ne liste que `docs/depannage.md`, et le plan 06-05 n'en touche aucune autre).

Limites nommees de cette verification, a recopier dans le resume : le rendu Markdown hors GitHub, l'execution du JavaScript et le comportement du DOM ne sont pas observes ; la copie de morsure n'embarque pas `.data/`, donc la morsure est mesuree module par module ; et une reecriture du produit ou du script invaliderait la mesure, le controle lisant ces deux fichiers.
</verification>

<success_criteria>
- `docs/depannage.md` porte la regle de pagination **bornee a chaque surface**, avec le fichier producteur nomme pour chacune, et l'absolu de portee de la ligne 74 livree a disparu : les deux comportements mesures du produit y sont decrits, l'un garde, l'autre sans garde.
- La rubrique « Saisie invalide » ne range plus `AUCUN RESULTAT.` parmi les refus qui tombent avant tout travail, et le message reste cite.
- `tests/test_docs_depannage.py` porte `problemes_pagination` et son test, lies aux **deux** comportements mesures du produit (`routes.py:144-145` avec sa ligne de corps, `terminal.js:370` et `:400` sans garde), et le module reste a zero echec sur la page corrigee.
- Les trois derivees mesurees rougissent avec leur motif nomme ; aucune ne se solde par `MUTATION NON DETECTEE` ; la mutation `absolu_sans_surface` reintroduit exactement le defaut `T3` sur une copie corrigee, ce qui remplace la relecture humaine du rouge.
- Le gabarit de la page, les 8 pages epinglees, l'index, `tests/test_docs_mutation.py` (14 morsures declarees) et `dofus_stuff/**` sont intacts ; `D-88`, `D-102`, `D-103`, `D-104`, `D-85` tenus.
</success_criteria>

<output>
Apres execution : creer `.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-05-SUMMARY.md`, avec les remplacements appliques a la page (les phrases exactes et les lignes du produit auxquelles chacune est ancree), le mecanisme du nouveau controle et pourquoi il peut rougir, les trois derivations mesurees avec leur motif, les sorties de la suite complete et de l'empreinte de la base, et les limites nommees recopiees du bloc de verification. Un commit local, a chemin explicite (`docs/depannage.md`, `tests/test_docs_depannage.py`, `06-05-PLAN.md`, `06-05-SUMMARY.md`) — jamais `git add -A`, jamais de poussee (`D-91`, `D-105`).
</output>

## Artefacts produits par cette phase (inventaire du plan 06-05)

- `docs/depannage.md` — la rubrique « Résultat paginé » bornee a ses deux producteurs mesures, la rubrique « Saisie invalide » desambiguee, et la ligne de tableau du message `PAGE {page}/{total}` portant les deux conditions ; gabarit et octets intacts (`D-69`, `D-102`).
- `tests/test_docs_depannage.py` — `problemes_pagination` et son test : la page nomme les deux surfaces de la ligne de statut et porte la condition de chacune, la surface servie par l'application reste gardee, celle des ecrans de sauvegardes reste inconditionnelle, et une marque d'absolu de portee rougit (`D-19`, `D-97`, `D-98`).

## Couverture des sources (ce plan)

- `06-VERIFICATION.md` — defaut `T3` (la portee de la pagination, faux des deux ecrans de sauvegardes) et observation secondaire sur la ligne 28 ; les deux sont traites, le premier par la tache 1 et son controle par la tache 2, la seconde par la tache 1.
- `06-RESEARCH.md` — les deux formes de la pagination, relevees ; le plan les mesure a nouveau dans les deux fichiers producteurs avant de les inscrire dans la page.
- `06-CONTEXT.md` — `D-19` (deux formes, statut contre corps), `D-69`/`D-102` (gabarit et octets des 8 pages), `D-73`/`D-75` (valeurs volatiles, chemins de poste), `D-88`/`D-103` (produit en lecture seule), `D-85` (backstop), `D-91`/`D-105` (commits a chemin explicite), `D-97`/`D-98` (ancrage et morsure), `D-104` (empreinte de la base).
- Produit, en lecture seule : `dofus_stuff/web/routes.py` (143-152, 474-477, 540-543, 334-351) et `dofus_stuff/web/static/js/terminal.js` (11, 355-402) — sources de chaque fait affirme par la page et de chaque procede exige par le controle.
- Harnais deja livre : `tests/test_docs_depannage.py` (`problemes_page`, `test_pagination_a_deux_formes`, garde de cloture) et `tests/test_docs_mutation.py` (14 morsures declarees) — non modifies par ce plan, hormis l'ajout du controle et de son test.
