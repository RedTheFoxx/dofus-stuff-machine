---
phase: 06-depannage-glossaire-completude-et-preuve-finale
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - tests/test_docs_completude.py
  - README.md
autonomous: true
requirements:
  - GARD-03
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "La documentation livree est **epinglee et complete** (`D-96`, `GARD-03`) : la constante `PAGES_EPINGLEES` nomme les **huit** pages de `docs/` (les sept pages de contenu et le sommaire) et `PAGES_RACINE` nomme `README.md` et `GUIDE_WIZARD.md` ; le controle exige que chaque chemin epingle existe, soit un fichier, ne porte aucun caractere vide, et que la **decouverte du disque** (`docs/*.md` et les deux fichiers de racine) soit **egale** a la liste declaree dans les deux sens — une page deplacee, renommee, ajoutee ou supprimee est un constat, jamais un silence."
    - "Aucune page livree n'est **videe** (`GARD-03`) : chaque page epinglee porte au moins un titre de niveau 1 et un nombre de lignes non vides superieur a un seuil declare par page, avec un motif par famille (`page vide`). La suppression silencieuse d'un contenu livre est donc impossible sans rouge."
    - "Les pages livrees sont **atteignables** : pour chacune des sept pages de contenu de `docs/`, le tableau `## Index` de `docs/sommaire.md` porte une ligne dont la cible est ce fichier, et le nombre de lignes du tableau egale le nombre de pages de contenu epinglees — l'index et le disque restent en egalite dans les deux sens (`D-95`, `D-96`), sans dupliquer les gardes livrees (`D-12`) : le controle lit l'index pour la **couverture** et laisse `problemes_index` / `problemes_h1` tenir l'egalite libelle ↔ disque et le H1 de la phase 1."
    - "Le `README.md` **n'invite pas a detruire les donnees locales** (`D-100`) : l'occurrence de la commande de nettoyage relevee par la vague 1 (`README.md:80`, ligne citee par le releve de la mesure, jamais recopiee dans un plan) se trouve dans un bloc de lignes contiguës qui doit porter **a la fois** un mot d'avertissement declare (`MOTS_AVERTISSEMENT`, forme de la destruction ou de son caractere irreversible) **et** le renvoi `docs/base-locale.md` — la ligne reste donc documentee, mais entouree de ce qui manquait : l'avertissement et le chemin non destructif ; le controle traite **chaque** occurrence, pas seulement la premiere, la **conservation** des deux autres commandes de base et de la mention des sous-commandes acceptees en alias est controlee par les jetons de `INVOCATIONS_CONSERVEES` (retirer une ligne est un constat, jamais une supposition), et l'absence de retrait est decidee par la comparaison `git show HEAD:README.md` ↔ copie de travail (aucun jugement humain n'est requis pour cette clause, seule une reformulation sans perte lui echappant) ; la fiche de decision de `D-100` est ecrite dans la docstring du module (la ligne de commande reste, elle perd sa forme d'invitation)."
    - "La docstring du module ecrit les **limites** (`D-85`) : la decouverte du disque couvre `docs/*.md` et les deux fichiers de racine, donc une page rangee dans un sous-dossier de `docs/` ne serait pas vue (mesure : `docs/` ne porte aucun sous-dossier) ; un fichier suivi par `git` mais ignore par `.gitignore` est vu ; le controle lit `README.md` **sans le corriger** ; et la qualite redactionnelle des avertissements n'est pas verifiee — c'est un controle d'**occurence et de renvoi**, pas de style."
    - statement: "L'effet reellement ressenti par un lecteur qui suit le `README.md` (comprend-il que la commande detruit sa base ?) n'est pas observable en processus : declare comme limite, aucune validation humaine n'est revendiquee ni simulee (`D-85`)."
      verification: backstop
  artifacts:
    - path: "tests/test_docs_completude.py"
      provides: "Le module de completude : docstring de contrat, de decision D-100 et de limites, garde de cloture auto-analysee, `PAGES_EPINGLEES`, `PAGES_RACINE`, `MOTS_AVERTISSEMENT`, `INVOCATIONS_CONSERVEES`, fonctions pures `problemes_pages_epinglees`, `problemes_couverture_index` et `problemes_readme`, et leurs tests"
      contains: "def test_readme_n_invite_pas_a_detruire"
      min_lines: 200
    - path: "README.md"
      provides: "La ligne de nettoyage de la base encadree par son avertissement et le renvoi vers la page qui documente la base locale et les gestes non destructifs"
      min_lines: 40
  key_links:
    - from: "tests/test_docs_completude.py"
      to: "docs/sommaire.md"
      via: "la lecture du tableau d'index : chaque cible d'entree confrontee a la liste des pages epinglees (couverture dans les deux sens)"
      pattern: "## Index"
    - from: "tests/test_docs_completude.py"
      to: "docs/base-locale.md"
      via: "le renvoi exige dans le bloc de la commande de nettoyage du README : c'est la page qui documente la base locale et son mode hors-ligne"
      pattern: "base-locale.md"
    - from: "README.md"
      to: "docs/base-locale.md"
      via: "le renvoi non destructif ajoute dans le bloc des commandes de base"
      pattern: "base-locale.md"
    - from: "tests/test_docs_completude.py"
      to: "tests/conftest.py"
      via: "les fixtures partagees de lecture de pages (`docs_dir`, `normalize`, `section`) : les helpers ne sont jamais recopies (D-12)"
      pattern: "normalize"
  prohibitions:
    - statement: "Ne jamais modifier `dofus_stuff/**` pour aligner la documentation : le code est le referentiel (`D-103`)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais supprimer, renommer, deplacer ni vider une page livree, et ne jamais retirer une ligne du `README.md` : `D-100` tranche en faveur de l'**encadrement** de la ligne de nettoyage, jamais de sa suppression (et jamais de son remplacement par une commande non documentee)."
      status: resolved
      verification: test
    - statement: "Ne jamais ecrire, vider, supprimer, renommer ni ouvrir par SQLite quoi que ce soit sous `.data/` : la base du depot n'est jamais lue, et ce module n'ouvre aucune base du tout (`D-104`)."
      status: resolved
      verification: test
    - statement: "Ne jamais executer `main()`, lancer le solveur, ouvrir une socket ou une connexion reseau depuis un controle, ni poster un formulaire de confirmation (`D-103`, `D-104`)."
      status: resolved
      verification: test
    - statement: "Ne jamais inventer un fichier epingle, ni une limite de lignes, ni un avertissement : la liste, les seuils et les mots d'avertissement sont lus sur le disque au moment de l'ecriture et declares dans la constante du module (`D-14`, `D-19`)."
      status: resolved
      verification: test
    - statement: "Ne jamais publier, deployer a distance, pousser vers un depot distant ni effectuer d'achat : commits locaux seulement, indexation par chemin explicite, jamais `git add .` (`D-105`)."
      status: resolved
      verification: judgment
  assumptions:
    - "**Sonde d'arete spec-less : l'orchestrateur n'a PAS execute la sonde deterministe de la section A** (aucun SPEC de phase : `EDGE_ABSENT` / `PROHIB_ABSENT`). Les aretes de ce plan sont **derivees par le planificateur** du critere de succes 3 du ROADMAP § Phase 6, du texte de `GARD-03` et des mesures de `06-RESEARCH.md` (les huit pages de `docs/`, la ligne de nettoyage du `README.md`, le sommaire et ses deux pages annoncees). Les aretes classables ont recu un critere confirmable ; l'effet ressenti par un lecteur est ecrit en **backstop** ; les six prohibitions sont au bloc `prohibitions`."
    - "**Adjudication `D-100`** : la ligne de nettoyage de la base locale **reste** dans le `README.md`, encadree par un avertissement et le renvoi vers `docs/base-locale.md`, plutot que d'etre supprimee. Justification : `GARD-03` demande que la documentation n'invite pas a detruire les donnees et que les chemins de reconstruction soient documentes — la documentation des commandes existantes est un contenu livre (la retirer serait une perte d'information et une forme de suppression, que les regles de cette phase interdisent), l'invitation tenait a l'absence d'avertissement et de renvoi, et l'encadrement est **verifiable automatiquement** (deux constats : avertissement manquant, renvoi manquant) alors qu'une suppression ne prouverait rien du besoin. Aucun geste manuel n'est requis : les deux criteres sont des assertions sur le texte du `README.md`."
    - statement: "Le seuil de lignes non vides par page est un **garde-fou anti-vidage**, pas une mesure de qualite : il est declare une fois par page dans la constante, lu sur le disque au moment de l'ecriture, sans pretention d'exhaustivite (`D-85`)."
      verification: backstop
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 6 et de son critere de succes 3, plus `GARD-03` et `D-96`/`D-100` de `06-CONTEXT.md` :
**En tant que** porteur du projet, **je veux** qu'une page livree ne puisse plus disparaitre, etre videe ou cesser d'etre atteignable sans qu'un controle rougisse, et que le `README.md` n'envoie plus personne detruire sa base locale sans avertissement, **afin de** pouvoir livrer la documentation en sachant qu'elle est complete et non dangereuse.

Mode MVP : le `**Goal:**` du ROADMAP n'est pas redige en recit utilisateur. Le cadrage ci-dessus en est **derive** (comme aux plans 04-01, 05-01, 06-01 et 06-02), jamais invente. Aucun retour a `/gsd mvp-phase` n'est demande.

Purpose : c'est **GARD-03** et le critere 3 — la documentation livree est **prouvee complete** (liste epinglee, non vidage, couverture par l'index) et le `README.md` est mis hors d'etat de nuire (`D-100`). Ce plan ferme aussi la boucle documentaire de la phase : les huit pages de `docs/` deviennent un contrat tenu par un module, et le seul fichier de documentation hors `docs/` qui portait une invite dangereuse gagne son garde-fou.

Output : `tests/test_docs_completude.py` (garde de cloture auto-analysee, `PAGES_EPINGLEES`, `PAGES_RACINE`, `MOTS_AVERTISSEMENT`, `INVOCATIONS_CONSERVEES`, fonctions pures `problemes_pages_epinglees`, `problemes_couverture_index`, `problemes_readme`), et le `README.md` dont la ligne de nettoyage est encadree par son avertissement et le renvoi vers `docs/base-locale.md`.

Ordonnancement declare : **vague 3**, `depends_on: [06-01, 06-02]` — la liste epinglee ne peut etre ecrite qu'apres la creation des deux pages de la phase, et la couverture d'index suppose que les deux lignes d'index existent (celle de `Dépannage` livree par 06-01, celle de `Glossaire` par 06-02). Aucune execution parallele n'est revendiquee (`D-92`).

**Ce que ce plan ne fait pas** : il n'ecrit pas la preuve de mutation du harnais, l'empreinte de `.data/`, l'audit de perimetre `git status` ni l'execution finale de la suite (plan 06-04) ; il ne touche ni `GUIDE_WIZARD.md` ni `dofus_stuff/**` (`D-103`), et il ne modifie pas le contenu des pages de `docs/`.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-VALIDATION.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
@.claude/CLAUDE.md
@README.md
@docs/sommaire.md
@docs/base-locale.md
@docs/depannage.md
@docs/glossaire.md
@tests/conftest.py
@tests/test_docs_structure.py
@tests/test_docs_base_locale.py
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : tranche verticale « la liste epinglee existe, elle est confrontee au disque, et une page ne peut plus disparaitre ni se vider »</name>
  <files>tests/test_docs_completude.py</files>
  <reversibility rating="reversible">Un module de test neuf est annulable par un commit de retour ; il ne modifie aucun fichier livre et ne touche ni `.data/` ni le reseau.</reversibility>
  <read_first>
    docs/sommaire.md
    docs/base-locale.md
    docs/depannage.md
    docs/glossaire.md
    tests/conftest.py
    tests/test_docs_structure.py
    tests/test_docs_base_locale.py
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md
  </read_first>
  <action>Ouvrir la tranche verticale de la completude : la liste epinglee, sa confrontation au disque, et le refus du vidage.

**Module** (`tests/test_docs_completude.py`, nouveau fichier) — docstring de module sur le patron de `tests/test_docs_base_locale.py` : le contrat (la liste epinglee est egale au disque dans les deux sens ; aucune page ne peut disparaitre, etre videe ou cesser d'etre atteignable), la **decision `D-100`** ecrite en clair avec sa justification (encadrer, jamais supprimer), et les **limites nommees** (`D-85`) : perimetre de decouverte (`docs/*.md` plus les deux fichiers de racine ; un sous-dossier de `docs/` ne serait pas vu, mesure : il n'en existe aucun), comparaison d'egalite d'ensembles sur des chemins **relatifs a la racine du depot** (donc sensible au separateur de plateforme — les chemins sont construits par `Path` et normalises en chaines a barres obliques avant comparaison, limite ecrite), seuil de lignes non vides par page comme garde-fou anti-vidage et non comme mesure de qualite, et lecture de `README.md` **sans correction** ;
- imports : `ast`, `re`, `pathlib.Path`, `pytest`, et les fixtures partagees `docs_dir`, `normalize`, `section` ; **aucun** import de `sqlite3`, `subprocess`, `socket`, `urllib`, `requests` ; ce module n'ouvre aucune base et n'instancie aucune application (il lit des fichiers texte) : la garde de cloture ne porte donc pas le resserrement `data_dir` de la phase 5, et le module l'ecrit ;
- constantes de chemin : `RACINE_DEPOT = Path(__file__).resolve().parents[1]`, `DOSSIER_DOCS = "docs"`, `SOMMAIRE = "sommaire.md"`, `TITRE_INDEX = "## Index"` ;
- `PAGES_EPINGLEES = ((chemin_relatif, seuil_de_lignes_non_vides), ...)` : les **huit** pages de `docs/` — les sept pages de contenu (`base-locale.md`, `cli.md`, `depannage.md`, `glossaire.md`, `installation.md`, `parcours-simplifie.md`, `wizard-avance.md`) et le sommaire (`sommaire.md`) — chacune avec son seuil, **lu sur le disque** au moment de l'ecriture puis declare (`D-14`, `D-19`) : le seuil n'est pas une exigence de qualite, c'est le refus du vidage ;
- `PAGES_RACINE = ("README.md", "GUIDE_WIZARD.md")` : les deux fichiers de documentation hors `docs/`, verifies existants, fichiers, et non vides (sans seuil de lignes : ce plan ne pretend pas les mesurer) ;
- constantes de **motif de morsure** (valeurs ASCII, sans apostrophe, jamais ecrites en clair dans une ligne d'assertion — regle de la phase 3) : `MOTIF_PAGES = "pages epinglees"`, `MOTIF_PAGE_ABSENTE = "page epinglee absente ou non fichier"`, `MOTIF_PAGE_NON_DECLAREE = "page du disque non epinglee"`, `MOTIF_PAGE_VIDE = "page videe ou tronquee"`, `MOTIF_H1 = "titre de niveau 1 absent"`, `MOTIF_COUVERTURE_INDEX = "page livree non indexee"`, `MOTIF_INDEX_EN_TROP = "entree d'index sans page epinglee"`, `MOTIF_NOMBRE_INDEX = "nombre de lignes d'index"`, `MOTIF_README = "invitation a detruire les donnees"`, `MOTIF_GARDE = "garde de cloture du harnais"` ;
- `MOTIF_TITRE = re.compile(r"^#\s+(?P<titre>\S.*)$", re.MULTILINE)` et `MOTIF_LIGNE_INDEX = re.compile(r"^\|\s*\[(?P<libelle>[^\]]+)\]\((?P<cible>[^)\s]+)\)\s*\|")` : lecteurs locaux (motifs de lecture, pas des helpers partages — `D-12` ne porte que sur `tests/conftest.py`) ;
- fonctions **pures** de constats (`D-13`) : `problemes_pages_epinglees(racine: Path) -> list[str]` — pour chaque page declaree : chemin existant, fichier, un titre de niveau 1, un nombre de lignes non vides au moins egal au seuil declare (`MOTIF_PAGE_ABSENTE`, `MOTIF_H1`, `MOTIF_PAGE_VIDE`) ; puis la **confrontation au disque** dans les deux sens : toute page `docs/*.md` du disque non declaree (`MOTIF_PAGE_NON_DECLAREE`) et toute page declaree absente du disque (`MOTIF_PAGE_ABSENTE`) ; chaque constat suit le message de `D-13` : `{page} : <ce qui est faux> ; attendu <seuil ou chemin>`. La fonction recoit la racine en argument pour rester testable sur une copie ;
- fonctions de garde reprises du patron (jamais recopiees) : `RACINES_INTERDITES`, `APPELS_SUPPRESSION`, `APPEL_PRODUIT = "main"`, `APPELS_CALCUL_PRODUIT`, helpers `_imports_du_module` / `_appels_du_module` (`ast.walk` de l'arbre entier) et `test_garde_de_cloture_du_harnais` qui **s'auto-analyse** par `ast.parse(Path(__file__).read_text(encoding="utf-8"))` — jamais une recherche de chaines ;
- tests de la tranche : `test_garde_de_cloture_du_harnais()`, `test_pages_epinglees_completes(docs_dir)`, `test_aucune_page_ne_peut_etre_videe(tmp_path)` — le troisieme construit dans `tmp_path` une copie minimale (un dossier `docs` avec une page tronquee et une page absente) et exige que `problemes_pages_epinglees` produise **au moins** les deux constats attendus : le controle est ainsi prouve capable de mordre **sans toucher au depot**, ce qui est la forme la moins invasive de la preuve de morsure.

Contraintes de forme : aucun bloc de code balise `console`, aucun lien externe, aucun jeton de brouillon ; le module ne lit jamais `.data/`, n'ecrit jamais dans le depot, et n'execute jamais `main()`.
  </action>
  <acceptance_criteria>
    - `tests/test_docs_completude.py` porte `PAGES_EPINGLEES` (huit pages de `docs/`), `PAGES_RACINE`, `MOTS_AVERTISSEMENT`, `problemes_pages_epinglees` et `test_garde_de_cloture_du_harnais`.
    - `problemes_pages_epinglees` exige l'existence, le type fichier, un titre de niveau 1, le seuil de lignes non vides, et l'egalite d'ensembles dans les deux sens avec les pages de `docs/` trouvees sur le disque.
    - `test_aucune_page_ne_peut_etre_videe` prouve la morsure sur une copie construite dans `tmp_path` (page tronquee et page absente), avec les deux motifs attendus, sans toucher au depot.
    - La garde de cloture est auto-analysee et refuse les racines interdites, l'appel au point d'entree du produit, les appels de calcul et de suppression.
    - Supprimer une page de `docs/` dans une copie, ou la vider, rougit le module avec le motif declare.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_completude.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in page_supprimee page_videe; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_completude.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in page_supprimee) rm "$T/docs/cli.md"; motif='page epinglee absente ou non fichier';; page_videe) "$PY" -c 'import sys;p=sys.argv[1];open(p,"w",encoding="utf-8",newline="").write("# Glossaire\r\n")' "$T/docs/glossaire.md"; motif='page videe ou tronquee';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 06-03 (TACHE 1) EN ECHEC"; exit 1; fi; echo "morsures 06-03 tache 1 : 2/2 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 06-03 (TACHE 1) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>La liste epinglee est declaree, confrontee au disque dans les deux sens, et deux derives epinglees (page supprimee, page videe) font rougir la suite sur une copie verte avant mutation ; aucune page ne peut plus disparaitre ni se vider en silence.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : le README n'invite plus a detruire les donnees — encadrer, jamais supprimer (D-100)</name>
  <files>README.md, tests/test_docs_completude.py</files>
  <read_first>
    README.md
    docs/base-locale.md
    tests/test_docs_completude.py
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
  </read_first>
  <behavior>
    - Le bloc de lignes contiguës du `README.md` qui porte la commande de nettoyage relevee par la vague 1 porte aussi, dans le meme bloc, un mot d'avertissement declare et le renvoi `docs/base-locale.md`.
    - La ligne de commande **reste** dans le `README.md` : retirer l'occurrence est un constat, au meme titre que retirer l'avertissement, le renvoi, ou l'une des deux autres commandes de base dont la conservation est controlee et non supposee.
    - Aucune ligne existante du `README.md` n'est retiree : l'encadrement se fait par des lignes **ajoutees**, et la conservation est decidee par un controle (comparaison `git show HEAD:README.md` ↔ copie de travail), jamais par une relecture.
    - Le controle traite **chaque** occurrence et non la premiere, et il ne touche jamais au dossier `.data/` pour verifier quoi que ce soit.
  </behavior>
  <action>Ecrire le garde-fou du `README.md` et la decision qui le fonde.

**Decision `D-100`, ecrite dans la docstring du module avant tout code** : la ligne de nettoyage de la base locale **reste documentee** dans le `README.md` ; ce qui manquait, c'est l'avertissement et le renvoi vers le geste non destructif. La justification est ecrite en clair : (a) la ligne est un contenu livre, et `GARD-03` demande que la documentation n'invite pas a detruire les donnees tout en documentant les chemins de reconstruction — retirer la ligne perdrait l'information que l'option existe ; (b) une suppression n'est pas verifiable au sens du besoin (aucune assertion ne pourrait distinguer « information absente » de « information jamais livree ») ; (c) l'encadrement est verifiable automatiquement par deux constats distincts ; (d) les regles de cette phase interdisent de supprimer des donnees ou de la documentation livreé, et l'esprit vaut ici comme pour `.data/` : on n'efface pas, on protege.

**`README.md`** — dans le bloc de commandes de la base locale, encadrer l'occurrence relevee par la vague 1 (`README.md:80`, la commande de nettoyage de la base, citee par le releve de mesure et **jamais recopiee dans un plan**) :
- ajouter, dans le **meme bloc de lignes contiguës**, un commentaire qui dit ce que fait la commande — elle `détruit` la base locale et l'operation est `irréversible` — et le renvoi au chemin `docs/base-locale.md`, la page qui documente la base locale et son mode hors-ligne ;
- ne **rien** supprimer d'autre : ni la ligne de commande, ni la ligne de resynchronisation, ni la ligne d'etat, ni la mention des sous-commandes acceptees en alias ; aucune ligne existante n'est retiree ;
- la forme retenue est un **commentaire dans le bloc de code** (les mots d'avertissement et le chemin y sont lisibles tels quels) : le controle exige le mot et le chemin dans le bloc, jamais une mise en forme particuliere ;
- le fichier garde ses fins de ligne CRLF mesurees (`git ls-files --eol README.md` : travail en CRLF, indice en LF) et son encodage UTF-8 sans BOM.

**Module** — ajouter le controle du `README.md` :
- `MOTS_AVERTISSEMENT = ("détruit", "irréversible")` : les formes declarees de l'avertissement, lues dans le `README.md` au moment de l'ecriture (`D-19`) ; la constante est un **contrat de presence**, pas une exigence de style, et le module l'ecrit (`D-85`) ;
- `INVOCATION_DESTRUCTIVE = "<jeton releve>"` : le jeton de la commande de nettoyage, **copie du `README.md` au moment de l'ecriture** (l'executeur le lit sur le disque, il ne le compose pas de memoire) — cette constante sert a exiger que l'information soit **encore la** : une ligne supprimee est un constat ;
- `INVOCATIONS_CONSERVEES = ("<jeton etat>", "<jeton synchro>", "<mention des alias>")` : les jetons des **deux autres commandes de base** du meme bloc et la mention des sous-commandes acceptees en alias, **lus dans le `README.md` au moment de l'ecriture** (`D-19`) — c'est la mecanisation de la clause « aucune ligne existante n'est retiree » : la conservation de chaque jeton est controlee, jamais supposee ;
- `RENVOI_BASE_LOCALE = "docs/base-locale.md"` ;
- `problemes_readme(texte: str, invocation: str, renvoi: str = RENVOI_BASE_LOCALE) -> list[str]` : fonction **pure** qui recoit le texte (jamais le disque) et procede par **bloc de lignes contiguës** — elle decoupe le texte en blocs separeas par une ligne vide, repere **chaque** bloc contenant le jeton d'invocation, et exige pour chacun (a) au moins un mot de `MOTS_AVERTISSEMENT` (`MOTIF_README` : avertissement manquant sur la commande qui detruit la base), (b) le renvoi `renvoi` (`MOTIF_README` : renvoi non destructif manquant) ; puis (c) **chaque** jeton de `INVOCATIONS_CONSERVEES` — le jeton destructif compris — doit apparaitre **au moins une fois** dans le texte (`MOTIF_README` : une ligne de base a disparu de la page alors que l'information doit rester documentee ; le constat nomme le jeton disparu) ; chaque constat nomme le numero de ligne du bloc et suit le message de `D-13` ;
- `test_readme_n_invite_pas_a_detruire(docs_dir)` : lit `README.md` et appelle `problemes_readme` avec le texte **reel** et `INVOCATION_DESTRUCTIVE` ; une seule assertion `assert not constats` ;
- `test_le_controle_du_readme_mord_sur_un_texte_synthetique()` : appel de `problemes_readme` sur trois textes **synthetiques** (un texte sans avertissement, un texte sans renvoi, un texte prive de **tous** les jetons de `INVOCATIONS_CONSERVEES`) et exigence des trois familles de constats — la preuve de morsure est **hors du depot** et immediate, et elle prouve du meme coup que le controle n'est pas un faux temoin quand la page ne porte aucune occurrence (le cas contraire : le garde-fou doit exiger que le jeton reel soit present, donc `INVOCATION_DESTRUCTIVE` non vide) ;
- limiter le perimetre : ce controle ne lit **jamais** `.data/`, ne teste jamais la commande, ne l'execute pas, et ne verifie pas que la base existe — il verifie le **texte du `README.md`** et rien d'autre.
  </action>
  <acceptance_criteria>
    - Le bloc du `README.md` qui porte la commande de nettoyage porte aussi un mot de `MOTS_AVERTISSEMENT` et le chemin `docs/base-locale.md`, et la ligne de commande est toujours presente (le controle exige l'occurrence : une suppression est un constat).
    - Aucune ligne existante du `README.md` n'a ete retiree, et cette clause n'est plus laissee au jugement : (i) `problemes_readme` exige la presence de **chaque** jeton de `INVOCATIONS_CONSERVEES` (les deux autres commandes de base et la mention des sous-commandes acceptees en alias, lus sur le disque a l'ecriture), et (ii) la comparaison `git show HEAD:README.md` ↔ copie de travail, prise **avant** le commit du plan, ne montre aucune ligne non vide disparue. La reference n'est **pas** `git log -1 --numstat -- README.md` : mesure de cette session, le dernier commit ayant touche `README.md` porte `0 2 README.md` (deux suppressions d'une phase anterieure), donc cette lecture conclurait a tort a un retrait. Seule une **reformulation sans perte** (une ligne existante reecrite) reste hors d'atteinte des deux controles : declaree comme limite, sans validation humaine revendiquee.
    - `problemes_readme` prend le texte en argument, traite chaque occurrence et accumule des constats portant `MOTIF_README`.
    - `test_le_controle_du_readme_mord_sur_un_texte_synthetique` exige les trois constats sur trois textes synthetiques, sans toucher au depot.
    - Retirer le renvoi `docs/base-locale.md` du `README.md`, ou le mot d'avertissement, ou l'un des jetons de `INVOCATIONS_CONSERVEES`, rougit le module avec le motif declare ; un retrait de ligne est en plus detecte par la comparaison `git show HEAD:README.md` ↔ copie de travail de la tache.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_completude.py -q -k "readme"</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest, ou "no tests ran" (selecteur vide)</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in renvoi_retire avertissement_retire; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_completude.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif='invitation a detruire les donnees'; case "$m" in renvoi_retire) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();L=[l for l in s.splitlines(True) if "base-locale.md" not in l];open(p,"w",encoding="utf-8",newline="").write("".join(L))' "$T/README.md";; avertissement_retire) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();s=s.replace("détruit","vide").replace("irréversible","simple");open(p,"w",encoding="utf-8",newline="").write(s)' "$T/README.md";; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 06-03 (TACHE 2) EN ECHEC"; exit 1; fi; echo "morsures 06-03 tache 2 : 2/2 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 06-03 (TACHE 2) EN ECHEC"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; "$PY" -c "import subprocess,pathlib;r=subprocess.run(['git','show','HEAD:README.md'],capture_output=True,text=True,encoding='utf-8');avant=r.stdout.splitlines();apres=pathlib.Path('README.md').read_text(encoding='utf-8').splitlines();m=[l for l in avant if l.strip() and l not in apres];print('code retour git show :',r.returncode,'lignes avant :',len(avant),'lignes apres :',len(apres),'lignes disparues :',len(m));print('DISPARUE : '+', '.join(m[:10]));raise SystemExit(1 if (m or r.returncode) else 0)"</automated>
    <fails_when>exit non-zero, ou une ligne "DISPARUE :" suivie d'au moins une ligne du `README.md` (une ligne existante a ete retiree au lieu d'etre encadree, `D-100`), ou un code retour de git non nul, ou un compte de lignes disparues non nul</fails_when>
    <automated>./.venv/Scripts/python.exe -c "import pathlib,re;t=pathlib.Path('README.md').read_text(encoding='utf-8');print('README.md relu : lignes non vides =', len([l for l in t.splitlines() if l.strip()]))"</automated>
    <fails_when>exit non-zero (le `README.md` n'est pas lisible en UTF-8 : un fichier illisible est un constat, jamais un vert)</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>Le `README.md` documente toujours la commande de nettoyage, mais dans un bloc qui dit qu'elle detruit la base et renvoie vers la page de la base locale ; les trois constats (avertissement, renvoi, occurrence disparue) sont prouves sur texte synthetique, et deux derives epinglees font rougir la suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 3 : la cloture de completude — chaque page livree est indexee, et l'index ne promet aucune page</name>
  <files>tests/test_docs_completude.py</files>
  <read_first>
    docs/sommaire.md
    docs/depannage.md
    docs/glossaire.md
    tests/test_docs_completude.py
    tests/test_docs_structure.py
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
  </read_first>
  <behavior>
    - Chaque page de contenu epinglee apparait comme cible d'**exactement une** ligne du tableau `## Index` du sommaire, et aucune entree d'index ne pointe vers une page non epinglee.
    - Le nombre de lignes du tableau d'index egale le nombre de pages de contenu epinglees.
    - Les deux pages livrees par cette phase sont epinglees **et** indexees, et le controle le derive des constantes plutot que d'une enumeration recopiee.
  </behavior>
  <action>Refermer la boucle documentaire de la phase : la couverture de l'index, dans les deux sens, et le compte.

**Module** — ajouter la couverture d'index :
- `problemes_couverture_index(docs_dir: Path) -> list[str]` : fonction **pure** de constats qui lit le tableau du titre `## Index` du sommaire (par la fixture `section` au niveau du test, par le texte au niveau de la fonction) et ne porte que les **deux axes neufs** — la **constante epinglee confrontee a l'index**, et le **compte** : (a) egalite d'ensembles **dans les deux sens** entre les cibles du tableau et les pages de contenu de `PAGES_EPINGLEES` (toutes sauf le sommaire lui-meme) — une page epinglee sans ligne est un constat de `MOTIF_COUVERTURE_INDEX`, une cible hors des pages epinglees est un constat de `MOTIF_INDEX_EN_TROP` — et **au plus une** ligne par cible (un doublon est un constat de `MOTIF_COUVERTURE_INDEX` : `problemes_index` raisonne en ensembles, donc aucune garde livree ne voit une ligne dupliquee, et c'est un axe neuf) ; (b) le nombre de lignes du tableau egale le nombre de pages de contenu epinglees (`MOTIF_NOMBRE_INDEX`, constat nommant les deux nombres) ;
- **non-duplication ecrite** : le module lit l'index pour la **couverture de la constante epinglee** (page epinglee ↔ ligne) et le **compte**, et laisse `tests/test_docs_structure.py` tenir l'egalite index ↔ **disque** (dans les deux sens), le H1 et l'unicite des libelles ; la docstring le dit et **declare la transitivite** : la couverture epinglee est une consequence de l'egalite index ↔ disque et de l'egalite pages epinglees ↔ disque de la tache 1, et elle est gardee **comme lecture de la constante epinglee** (une seule source de verite, `D-14`) plutot que comme seconde derivation du disque ; elle ajoute donc les deux choses qu'aucune garde livree ne porte — l'unicite de la ligne par cible, et le compte — et jamais un controle de libelle ni de H1 sur le sommaire (`D-12`) ;
- `test_couverture_de_l_index(docs_dir, section)` : une seule assertion `assert not constats` ;
- `test_les_deux_pages_de_la_phase_sont_epinglees_et_indexees(docs_dir, section)` : derive des constantes — les deux chemins (`depannage.md`, `glossaire.md`) sont dans `PAGES_EPINGLEES` **et** apparaissent comme cibles du tableau d'index ; aucune enumeration recopiee, donc aucune seconde source de verite ;
- `test_le_nombre_d_entrees_d_index_est_le_nombre_de_pages(docs_dir, section)` : exerce le point (b) seul, pour que le constat de compte soit diagnosticable sans lire les autres ;
- docstring etendue : les trois constats de cette tache et les deux de la tache 1 forment le contrat `GARD-03` de la phase ; la liste epinglee reste la **seule** source de verite de ce qui est livre (`D-14`), et le module ne verifie ni le contenu redactionnel des pages ni leur rendu (`D-85`).

Livraison du plan : relire `docs/sommaire.md` pour confirmer que les deux lignes d'index existent (celle de `Dépannage` livree par 06-01, celle de `Glossaire` par 06-02) et que le tableau porte **sept** lignes ; si le compte differe, le constat de la tache 3 nomme les deux nombres et la correction est dans la ligne d'index manquante, jamais dans le seuil.
  </action>
  <acceptance_criteria>
    - `problemes_couverture_index` exige, avec les trois motifs declares : la **constante epinglee** confrontee au tableau d'index dans les deux sens, **au plus une** ligne par cible, et le **compte** de lignes ; il ne re-derive ni l'egalite index ↔ disque (elle est tenue par `problemes_index`, et le module declare la transitivite au lieu de la reprendre), ni le H1, ni l'unicite des libelles.
    - Les trois tests de la tache existent et portent chacun sur une seule assertion de constats.
    - Le module ne reimplemente ni l'egalite index ↔ disque, ni le H1, ni l'unicite des libelles : ces trois controles restent ceux de `tests/test_docs_structure.py`, la docstring dit ce partage et declare que la couverture epinglee en est une consequence volontairement gardee comme lecture de la constante (`D-12`, `D-14`).
    - Retirer une ligne d'index du sommaire dans une copie rougit le module avec `page livree non indexee`, et le constat de compte nomme les deux nombres.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_completude.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in index_tronque; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_completude.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif='page livree non indexee'; "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();L=[l for l in s.splitlines(True) if "glossaire.md" not in l];open(p,"w",encoding="utf-8",newline="").write("".join(L))' "$T/docs/sommaire.md"; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 06-03 (TACHE 3) EN ECHEC"; exit 1; fi; echo "morsures 06-03 tache 3 : 1/1 detectee (copie verte avant mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 06-03 (TACHE 3) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>La documentation livree est prouvee complete : les huit pages de `docs/` sont epinglees, aucune ne peut etre videe, chacune est atteignable par l'index, l'index ne promet aucune page, les deux pages de la phase sont epinglees et indexees, et la suppression d'une ligne d'index fait rougir la suite.</done>
</task>

</tasks>
<!-- planner-discipline-allow: db clear -->
<!-- planner-discipline-allow: PURGE OUI -->

<threat_model>
**Surface** : un module de test neuf, une seule ligne de documentation produit modifiee (`README.md`) par ajout de commentaire. Aucune sonde, aucune base, aucune application : le module lit des fichiers texte du depot et rien d'autre. Les menaces sont documentaires (une page qui disparait ou se vide sans rouge, un `README.md` qui envoie detruire la base) et de harnais (une lecture disque hors du depot, une correction automatique du `README.md`).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-01 | Tampering (perte silencieuse de contenu livre) | Les huit pages de `docs/` | high | mitigate | `PAGES_EPINGLEES` confrontee au disque dans les deux sens, titre de niveau 1 et seuil de lignes non vides par page, couverture d'index dans les deux sens ; quatre morsures de la vague 3 (page supprimee, page videe, ligne d'index retiree, et les trois constats du `README.md` sur texte synthetique) prouvent la morsure |
| T-06-03-02 | Spoofing (documentation qui invite a detruire) | `README.md` : la commande de nettoyage de la base | high | mitigate | Encadrement exige dans le bloc de l'occurrence (mot d'avertissement declare plus renvoi `docs/base-locale.md`), traitement de **chaque** occurrence, et exigence que l'occurrence soit toujours presente — la fiche `D-100` justifie le choix d'encadrer plutot que de supprimer, et deux derives epinglees (renvoi retire, avertissement retire) font rougir la suite |
| T-06-03-03 | Information disclosure | Module et page : chemin de poste, valeur volatile | medium | mitigate | Le module n'imprime que des chemins **relatifs** a la racine du depot ; aucune valeur volatile n'est citee dans un constat ; `README.md` ne gagne ni chemin de poste ni horodatage (la modification est un commentaire d'avertissement et un renvoi relatif) |
| T-06-03-04 | Tampering | `.data/dofus.sqlite3` | high | mitigate | Aucun import de `sqlite3`, aucune base construite, aucune ecriture : le module ne lit que du texte, et la garde de cloture auto-analysee le prouve ; l'empreinte de la suite est mesuree au plan 06-04, avant et apres l'execution finale |
| T-06-03-05 | Elevation of privilege | Harnais : execution du produit, du solveur, d'une socket ; correction automatique d'un fichier livre | high | mitigate | Garde `ast` reecrite : racines interdites, appel au point d'entree du produit, appels de calcul et de suppression, paires de confirmation ; aucun sous-processus, donc aucun `git` ni interpreteurs lances depuis un test ; le module **lit** le `README.md` et ne l'ecrit jamais (la modification du `README.md` est un acte de mise en oeuvre, pas une ecriture de controle) |
| T-06-03-06 | Repudiation (controle qui ne selectionne rien) | Selecteurs et fixture de copie | medium | mitigate | Les deux selecteurs `-k` portent `no tests ran` dans leurs signaux d'echec ; la morsure de la tache 1 est prouvee sur une copie construite dans `tmp_path` (donc sans toucher au depot) et par la fonction pure ; les tests de la tache 3 sont exerces par la suppression d'une ligne d'index dans la copie |
| T-06-03-SC | Tampering | Installations de paquets | low | accept | Aucune dependance ajoutee ; aucun paquet installe, aucun `[ASSUMED]`, `[SUS]` ni `[SLOP]` |

**Hors perimetre** : publication, deploiement distant, synchronisation Dofusdude, suppression de donnees, achat. Aucun fichier de `dofus_stuff/**` modifie (`D-103`), `.data/` jamais approche (`D-104`), `GUIDE_WIZARD.md` inchange.
</threat_model>

<verification>
**Controles automatises** : `tests/test_docs_completude.py` (garde de cloture auto-analysee, liste epinglee confrontee au disque dans les deux sens, refus du vidage et du titre manquant, couverture d'index dans les deux sens avec le compte, `README.md` encadre et occurrence conservee, morsure prouvee sur copie `tmp_path` et sur texte synthetique), `tests/test_docs_structure.py`, `tests/test_docs_depannage.py`, `tests/test_docs_glossaire.py` et la suite entiere, plus **cinq morsures** sur copie verte avant mutation (page supprimee, page videe, renvoi `docs/base-locale.md` retire, avertissement retire, ligne d'index retiree).

**Non automatisable, nomme comme tel** : l'effet ressenti par un lecteur du `README.md`, la qualite redactionnelle des avertissements, une **reformulation sans perte** d'une ligne du `README.md` (le controle des jetons conserves et la comparaison `git show HEAD:README.md` ↔ copie de travail attrapent la disparition, pas la reecriture) et le fait qu'aucune page ne soit rangee dans un sous-dossier de `docs/` (mesure : il n'en existe aucun) sont declares en `backstop` ou en `judgment`. Aucun blocage : rien ici ne depend d'un geste humain, d'un secret ou d'un reseau.

**References de verification de phase** : `06-VALIDATION.md` § Per-Task Verification Map assigne la ligne `GARD-03 / 06-03 / Page livree intacte` a ce plan (vague 3, `tests/test_docs_completude.py`), et le plan 06-04 consomme ce module pour sa preuve de mutation.
</verification>

<success_criteria>
- La documentation livree est **prouvee complete** : les huit pages de `docs/` sont epinglees et confrontees au disque dans les deux sens, aucune ne peut etre supprimee ni videe en silence, chacune est atteignable depuis l'index, et l'index ne promet aucune page inexistante (`GARD-03`, `D-96`, critere 3).
- Les deux pages de cette phase (`Dépannage`, `Glossaire`) sont epinglees et indexees, et leurs controles derivent des constantes plutot que d'une enumeration recopiee.
- Le `README.md` n'invite plus a detruire les donnees locales : la commande reste documentee, encadree par son avertissement et le renvoi vers `docs/base-locale.md` (`D-100`).
- Le module ne duplique pas les controles de la phase 1 (index ↔ disque, H1, unicite des libelles) et le dit.
- Cinq derives epinglees font rougir la suite sur une copie verte avant mutation ; la suite entiere est verte et `.data/dofus.sqlite3` est intact.
</success_criteria>

<output>
Create `.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-03-SUMMARY.md` when done
</output>

## Couverture des sources (ce plan)

| Source | Element | Consomme par |
|--------|---------|--------------|
| ROADMAP § Phase 6 | Critere 3 (la documentation livree ne peut plus disparaitre, etre videe ou cesser d'etre atteignable sans qu'un controle echoue) | taches 1 et 3 |
| ROADMAP § Phase 6 | Decomposition imposee : « 06-03 completude + garde du lecteur » | les trois taches |
| REQUIREMENTS | GARD-03 | `requirements` du plan |
| RESEARCH | Les huit pages de `docs/` livrees apres la phase, l'index a sept lignes et les deux pages annoncees au parcours | taches 1 et 3 |
| RESEARCH | Le `README.md` et son occurrence de la commande de nettoyage relevee par la vague 1, et l'arbitrage demande sur cette ligne | tache 2 |
| RESEARCH | § D-12 : les gardes de la phase 1 tiennent deja l'egalite index ↔ disque, le H1 et l'unicite des libelles — ne pas les dupliquer | tache 3 (non-duplication ecrite) |
| RESEARCH | Pitfall 7 (inventaire implicite : une page peut disparaitre en silence) | tache 1 |
| CONTEXT | D-96 (la liste epinglee des huit pages et la preuve de completude) | taches 1 et 3 |
| CONTEXT | D-100 (la ligne de nettoyage du `README.md` : encadrer, jamais supprimer) | tache 2, et l'adjudication en `assumptions` |
| CONTEXT | D-92 (quatre plans, vagues sequentielles) | frontmatter du plan |
| CONTEXT | D-14 et D-19 (la constante est la source unique du contrat ; aucun seuil, aucun mot d'avertissement invente) | taches 1, 2 et 3 |
| CONTEXT | D-102 (normalisation, helpers partages, constats, constantes publiques, limites honnetes) | les trois taches |
| CONTEXT | D-103, D-104 et D-105 (code en lecture seule, aucune action destructive sur les donnees, commits locaux par chemin explicite) | garde de cloture et prohibitions |
| Planification | La decouverte du disque restreinte a `docs/*.md` et aux deux fichiers de racine, et la morsure prouvee sur copie `tmp_path` plutot que sur le depot | taches 1 et 3 |

**Audit de completude de la phase (les quatre types de sources)** : GOAL — ce plan couvre le critere 3 ; les criteres 1, 2, 4 et 5 sont couverts par 06-01, 06-02 et 06-04 ; REQ — `GARD-03` ici et au plan 06-04 (preuve de morsure du harnais), `AIDE-01` au plan 06-01, `AIDE-02` au plan 06-02, `GARD-04` au plan 06-04 ; RESEARCH — les mesures du `README.md`, des pages et de l'index, la regle de non-duplication `D-12` et le piege d'inventaire implicite sont consommees ; CONTEXT — D-92 a D-105 reparties sur les quatre plans, sans exception. Exclusions assumees : la preuve de mutation du harnais, l'empreinte de `.data/` et l'audit de perimetre (plan 06-04), et toute resynchronisation Dofusdude (hors perimetre).


