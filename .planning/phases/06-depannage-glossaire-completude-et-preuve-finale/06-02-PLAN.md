---
phase: 06-depannage-glossaire-completude-et-preuve-finale
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - docs/glossaire.md
  - docs/sommaire.md
  - tests/test_docs_glossaire.py
autonomous: true
requirements:
  - AIDE-02
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Le glossaire est un **contrat extractible**, pas une page libre (`D-94`) : `docs/glossaire.md` porte un seul titre de niveau 2 `## Les termes`, chaque entree est une ligne de tableau dont la **premiere cellule** est le terme entre accents graves, et le controle exige l'**egalite d'ensembles dans les deux sens** entre les termes lus dans la page et la constante `TERMES_GLOSSAIRE` du module — un terme de la page non declare, comme un terme declare absent de la page, est un constat portant un motif distinct."
    - "Les entrees sont **triees** et **uniques** (`D-94`) : la suite des libelles normalises (`D-11` : accents, casse, entites, espaces) est strictement croissante, et deux libelles egaux apres normalisation sont un constat ; le controle porte sur l'existence et l'ordre, jamais sur la prose des definitions — limite declaree (`D-85`)."
    - "Chaque terme cite est **reellement employe** (`D-19`) : l'entree porte un chemin `Employe par`, ce chemin existe depuis la racine du depot, et le terme y est present apres normalisation ; un terme que ni `dofus_stuff/**` ni `docs/**` n'emploient est un constat, jamais une definition inventee."
    - "Le glossaire **ne redefinit pas** ce qu'une page livree definit deja (`D-17`) : pour chaque terme deja defini ailleurs, la constante declare la page qui le definit et le controle exige que la ligne porte un lien vers **cette** page (et que la cible resolve), si bien que `base locale`, `synchronisation`, `mode hors-ligne`, `wizard`, `categorie`, `slot`, `jet`, `exo`, `palier`, `heuristique`, `solveur`, `poids`, `cible`, `ID Ankama`, `panoplie`, `trophee`, `familier`, `prysmaradite`, `bouclier`, `score`, `sauvegarde locale` et `ligne de statut` renvoient au lieu d'etre redefinis, tandis que `stuff`, `sommaire`, `parcours conseillé`, `index` et `source de vérité` sont definis par le glossaire (aucune page livree ne les definit)."
    - "Le vocabulaire de **harnais** est refuse (`D-19`) : les termes portes par `TERMES_INTERDITS` (vocabulaire de planification, jamais rencontre par un lecteur a l'ecran) ne peuvent pas etre des libelles d'entree, et le constat nomme le terme refuse."
    - "Le **parcours conseillé final** du sommaire devient verifiable (`D-95`) : `problemes_parcours` exige que la liste numerotee de `## Parcours conseillé` soit une **permutation** des libelles d'index (egalite d'ensembles dans les deux sens apres normalisation, ordre de lecture libre et declare), ne nomme aucune page inexistante, nomme les deux pages livrees par cette phase, et **reste du texte simple** — mesure de cette session : convertir le parcours en liens re-emettrait les libelles de l'index et ferait rougir `problemes_index`, `problemes_h1` et `test_sommaire_index_labels_are_unique`, qui lisent tout lien de `sommaire.md` comme une entree d'index."
    - "`docs/sommaire.md` croit d'**exactement une** ligne d'index (`| [Glossaire](glossaire.md) |`), ajoutee en fin de table ; l'index porte alors les **sept** pages de contenu, et la liste epinglee des huit fichiers est l'objet du plan 06-03 (`D-95`, `D-96`) — ce plan ne duplique ni `problemes_index` ni `problemes_h1`, qui tiennent deja l'egalite index ↔ disque et le H1 (`D-12`)."
    - statement: "La qualite redactionnelle des definitions (clarte, exactitude de la prose) n'est pas verifiee par un controle : les constats portent sur les libelles, leur ordre, leur employeur et le renvoi vers la page qui definit deja ; ce plan ne revendique aucune exhaustivite de redaction (`D-85`). Non confirmable au-dela : backstop explicite."
      verification: backstop
  artifacts:
    - path: "docs/glossaire.md"
      provides: "La page unique du vocabulaire : les termes du produit et de la documentation, tries, chacun adosse a un employeur reel et renvoye vers la page qui le definit deja quand elle existe, plus le bloc Source de verite et la ligne de retour"
      contains: "## Les termes"
      min_lines: 55
    - path: "docs/sommaire.md"
      provides: "La ligne d'index « Glossaire » en fin de table, et la liste « Parcours conseillé » rendue verifiable par le controle du module"
      contains: "| [Glossaire](glossaire.md) |"
      min_lines: 25
    - path: "tests/test_docs_glossaire.py"
      provides: "Le module d'ancrage du vocabulaire : docstring de contrat et de limites, garde de cloture auto-analysee, constante `TERMES_GLOSSAIRE` (terme, employeur, page qui definit), `TERMES_INTERDITS`, fonctions pures `problemes_glossaire`, `problemes_page_glossaire` et `problemes_parcours`, et leurs tests"
      contains: "def test_parcours_conseille_final"
      min_lines: 210
  key_links:
    - from: "docs/glossaire.md"
      to: "docs/base-locale.md"
      via: "les renvois de definition (`D-17`) : la base locale, la synchronisation, le mode hors-ligne, les categories et l'etat de la base sont deja definis la"
      pattern: "base-locale.md"
    - from: "docs/glossaire.md"
      to: "dofus_stuff/cli.py"
      via: "l'employeur du vocabulaire du produit (aide du parseur, options, refus) : chaque ligne `Employe par` est confrontee au fichier, terme cherche dans le texte"
      pattern: "Employé par"
    - from: "tests/test_docs_glossaire.py"
      to: "docs/sommaire.md"
      via: "les fonctions pures qui lisent les libelles d'index et la liste numerotee du parcours conseille dans le meme fichier"
      pattern: "## Parcours conseillé"
    - from: "tests/test_docs_glossaire.py"
      to: "tests/conftest.py"
      via: "la fixture partagee `normalize` (D-11) et les fixtures de page (`docs_dir`, `section`, `sections`) : les helpers ne sont jamais recopies (D-12)"
      pattern: "normalize"
  prohibitions:
    - statement: "Ne jamais modifier `dofus_stuff/**` pour aligner la documentation : le code est le referentiel, la page s'y conforme (`D-103`)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais ecrire, vider, supprimer, renommer ni ouvrir par SQLite quoi que ce soit sous `.data/` : la base du depot n'est jamais lue, et toute base construite par un controle l'est sous `tmp_path` (`D-104`)."
      status: resolved
      verification: test
    - statement: "Ne jamais executer `main()`, lancer le solveur, ouvrir une socket ou une connexion reseau depuis un controle, ni poster un formulaire de confirmation (`D-103`, `D-104`)."
      status: resolved
      verification: test
    - statement: "Ne jamais definir un terme que le depot n'emploie pas, ni un terme du vocabulaire de harnais, ni redefinir un terme qu'une page livree definit deja (`D-17`, `D-19`)."
      status: resolved
      verification: test
    - statement: "Ne jamais convertir le parcours conseille du sommaire en liens : mesure de cette session, tout lien de `sommaire.md` est lu comme une entree d'index par trois gardes vertes, et un parcours en liens re-emettrait les libelles de l'index (`D-05`, `D-95`)."
      status: resolved
      verification: test
    - statement: "Ne jamais epingler une valeur volatile dans la prose de la page — chemin de poste, horodatage, compteur d'objets, identifiant d'objet (`D-73`, `D-75`)."
      status: resolved
      verification: test
    - statement: "Ne jamais publier, deployer a distance, pousser vers un depot distant ni effectuer d'achat : commits locaux seulement, indexation par chemin explicite, jamais `git add .` (`D-105`)."
      status: resolved
      verification: judgment
  assumptions:
    - "**Sonde d'arete spec-less : l'orchestrateur n'a PAS execute la sonde deterministe de la section A** (aucun SPEC de phase fourni : `EDGE_ABSENT` / `PROHIB_ABSENT`). La liste d'aretes de ce plan est donc **derivee par le planificateur** du critere de succes 2 du ROADMAP § Phase 6, du texte d'AIDE-02 et de l'inventaire mesure `06-RESEARCH.md` § Measured inventory B (l'employeur de chaque terme, et la colonne « deja defini ailleurs »). Chaque arete classable a recu un critere confirmable ci-dessus ; la qualite redactionnelle est ecrite en **backstop** (dernier element de `truths`) et non en vert silencieux ; les sept prohibitions retenues sont au bloc `prohibitions`, aucune n'est ecartee en silence."
    - "**Perimetre du vocabulaire** : la liste des termes candidats vient de `06-RESEARCH.md` § B (chaque ligne y porte sa provenance mesuree) et de `.claude/CLAUDE.md:83` (garde-fou de non-oubli, jamais un contrat). Le plan fixe la **regle** (definir seulement ce qu'aucune page livree ne definit, renvoyer pour le reste) et le **minimum** a couvrir ; l'executeur confirme chaque employeur par une lecture `fichier:ligne` au moment de la redaction (`D-19`) et peut ajouter un terme reellement employe, a condition que la constante et la page restent en egalite d'ensembles."
    - statement: "Le rendu Markdown du glossaire hors GitHub n'est pas observable en processus (aucun moteur de rendu installe, publication distante interdite) : declare comme limite, aucune validation humaine n'est revendiquee (`D-85`)."
      verification: backstop
    - statement: "L'ordre de lecture du parcours conseille (le wizard avant la reference CLI) reste celui livre aujourd'hui : le controle exige une **permutation** des libelles d'index, jamais le meme ordre, et cette liberte est ecrite dans la docstring du module (`D-85`)."
      verification: backstop
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 6 et de son critere de succes 2, plus AIDE-02 et D-94/D-95 de `06-CONTEXT.md` :
**En tant que** lecteur qui rencontre un mot de l'outil ou de la documentation sans savoir ce qu'il recouvre, **je veux** trouver sa definition en un seul endroit, avec la page qui le detaille deja, **afin de** comprendre la documentation sans revenir au code ni relire six pages.

Mode MVP : le `**Goal:**` du ROADMAP n'est pas redige en recit utilisateur. Le cadrage ci-dessus en est **derive** (comme aux plans 04-01, 05-01 et 06-01), jamais invente. Aucun retour a `/gsd mvp-phase` n'est demande.

Purpose : c'est **AIDE-02** et le critere 2 — un glossaire du vocabulaire **du produit et de la documentation** (`D-94`), dont chaque entree est reellement employee (employeur confronte au fichier), triee, unique, et **renvoyee** vers la page qui definit deja le terme quand elle existe (`D-17`). C'est aussi le plan qui rend le **parcours conseille final** du sommaire verifiable (`D-95`) : mesure de cette session, la section annonce deja les deux pages de cette phase mais **aucune garde ne la lit**, et la convertir en liens casserait trois gardes vertes — elle reste donc du texte, et gagne un controle.

Output : `docs/glossaire.md` (les termes, le bloc Source de verite, la ligne de retour), la ligne d'index `Glossaire` de `docs/sommaire.md`, et `tests/test_docs_glossaire.py` (garde de cloture auto-analysee, constante `TERMES_GLOSSAIRE`, fonctions pures `problemes_glossaire`, `problemes_page_glossaire` et `problemes_parcours`).

Ordonnancement declare : **vague 2**, `depends_on: [06-01]` — le parcours conseille final ne devient vrai sur disque que lorsque les **deux** pages de la phase existent (`Dépannage` livree par 06-01, `Glossaire` par ce plan), et les deux plans ecrivent dans le meme fichier `docs/sommaire.md` : la dependance est de fichier autant que de contenu, et aucune execution parallele n'est revendiquee (`D-92`).

**Ce que ce plan ne fait pas** : il n'ecrit pas la liste epinglee des huit fichiers ni le controle de non-destructivite du `README.md` (plan 06-03), ni la preuve de mutation du harnais, l'empreinte de `.data/`, l'audit de perimetre et l'execution finale (plan 06-04) ; il ne modifie **aucun** fichier de `dofus_stuff/**` (`D-103`) et ne touche ni `README.md` ni `GUIDE_WIZARD.md`.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
@.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-VALIDATION.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
@.claude/CLAUDE.md
@docs/sommaire.md
@docs/base-locale.md
@docs/parcours-simplifie.md
@docs/wizard-avance.md
@docs/cli.md
@docs/depannage.md
@tests/conftest.py
@tests/test_docs_structure.py
@tests/test_docs_depannage.py
@dofus_stuff/cli.py
@dofus_stuff/database.py
@dofus_stuff/model/solver_spec.py
@dofus_stuff/web/optimize_wizard.py
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : tranche verticale « la page existe, son index est coherent, et les premiers termes sont employes et tries »</name>
  <files>docs/glossaire.md, docs/sommaire.md, tests/test_docs_glossaire.py</files>
  <reversibility rating="reversible">Creer une page `docs/`, une ligne d'index et un module de test est annulable par un commit de retour ; rien n'est publie, rien n'est deplace hors du depot, `.data/` n'est pas approche.</reversibility>
  <read_first>
    docs/sommaire.md
    docs/base-locale.md
    docs/parcours-simplifie.md
    docs/wizard-avance.md
    docs/cli.md
    docs/depannage.md
    tests/conftest.py
    tests/test_docs_structure.py
    tests/test_docs_depannage.py
    dofus_stuff/cli.py
    dofus_stuff/model/solver_spec.py
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-CONTEXT.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
  </read_first>
  <action>Ouvrir la tranche verticale du glossaire : la page, sa ligne d'index et le module qui la garde, avec le premier groupe de termes — ceux que le glossaire est le **premier** a definir, parce qu'aucune page livree ne le fait.

**Page** (`docs/glossaire.md`, nouveau fichier, UTF-8 sans BOM, fins de ligne CRLF comme les sept autres pages de `docs/`) :
- un `H1` exactement `# Glossaire` : c'est le libelle d'index exige par le gabarit (`D-102`), et il correspond deja a l'entree `7. Glossaire` du parcours conseille ;
- une phrase d'introduction en francais : ce que la page recense, et la regle du renvoi — quand une page de la documentation definit deja un terme, l'entree **renvoie** vers elle au lieu de la redefinir (`D-17`) ;
- `## Les termes` : **un seul** titre de niveau 2, puis un tableau a quatre colonnes — `Terme`, `Définition`, `Employé par`, `Déjà défini dans`. Chaque ligne porte, en **premiere cellule**, le terme entre accents graves (forme extractible : le controle lit la premiere cellule de chaque ligne du tableau) ;
  * `stuff` : defini ici (une phrase : l'ensemble d'equipement optimise pour un niveau, tel que l'outil le compose) ;
  * `sommaire` : defini ici (point d'entree unique de la documentation) ;
  * `parcours conseillé` : defini ici (l'ordre de lecture propose par le sommaire) ;
  * `index` : defini ici (la table page → sujet du sommaire) ;
  * `source de vérité` : defini ici (le bloc par lequel une page nome le code dont elle procede), en precisant que c'est un **titre de section du gabarit** et non un terme du produit ;
  * la colonne `Employé par` porte le chemin du fichier **reellement employeur**, confronte au fichier au moment de la redaction (`D-19`) — pour ces cinq termes, le produit ou la documentation les emploie (`dofus_stuff/cli.py` pour `stuff`, `docs/sommaire.md` pour `sommaire`, `parcours conseillé` et `index`, les pages livrees pour `source de vérité`) ;
  * la colonne `Déjà défini dans` vaut un tiret pour ces cinq termes : le glossaire est bien le premier a les definir ; pour tout autre terme, elle porte un **lien** vers la page qui le definit (`D-17`) ;
- `## Source de vérité` : un chemin **existant** par ligne, entre accents graves — `docs/sommaire.md`, `dofus_stuff/cli.py`, `dofus_stuff/model/solver_spec.py`, `dofus_stuff/web/optimize_wizard.py` (`D-03`, `D-69`) ; aucun chemin invente ;
- derniere ligne non vide, exactement `[Retour au sommaire](sommaire.md)` ; aucun lien externe, aucun jeton de brouillon, aucune valeur volatile.

**Index** (`docs/sommaire.md`) — ajouter **une seule** ligne, en fin de table `## Index`, dans la forme des six lignes existantes : `| [Glossaire](glossaire.md) | ... |`. La liste « Parcours conseille » **reste du texte simple**, et le planificateur a verifie le motif par mesure : `problemes_index`, `problemes_h1` et `test_sommaire_index_labels_are_unique` lisent **tout** lien de `sommaire.md` comme une entree d'index, donc un parcours converti en liens re-emettrait les libelles de l'index et ferait rougir trois gardes vertes (chaque entree d'index devrait alors rester un libelle unique dans tout le fichier).

**Module** (`tests/test_docs_glossaire.py`, nouveau fichier) — squelette **complet et definitif** de l'outillage, etendu par les taches 2 et 3 :
- docstring de module sur le patron de `tests/test_docs_base_locale.py` : le contrat va **de la page vers le depot** (les termes sont confrontes a leur employeur, les renvois a leur cible), aucun serveur, aucune socket, `main()` jamais execute, rien n'est ecrit sous `.data/` ; puis les **limites nommees** (`D-85`) : la prose des definitions n'est pas verifiee, la comparaison d'emploi est une **appartenance de sous-chaine** sur le texte normalise (un terme qui serait contenu dans un autre passerait — limite mesuree, ecrite), le tri est celui de la normalisation `D-11` et non une collation linguistique, l'assertion CRLF depend de `core.autocrlf` du poste, et l'ordre de lecture du parcours est libre par decision de plan ;
- imports : `ast`, `re`, `pathlib.Path`, `pytest`, et les fixtures partagees `docs_dir`, `normalize`, `section`, `sections` ; **aucun** import de `sqlite3`, `subprocess`, `socket`, `urllib`, `requests` (la garde de cloture les refuse). Ce module n'ouvre **aucune** base et n'instancie **aucune** application : la garde de cloture ne porte donc pas le resserrement `data_dir` de la phase 5, et le module l'ecrit (une garde qui controlerait un risque inexistant serait du bruit) ;
- constantes de chemin : `RACINE_DEPOT = Path(__file__).resolve().parents[1]`, `PAGE = "glossaire.md"`, `SOMMAIRE = "sommaire.md"`, `TITRE_TERMES = "## Les termes"`, `TITRE_PARCOURS = "## Parcours conseillé"`, `LIEN_RETOUR = "[Retour au sommaire](sommaire.md)"` ;
- `TERMES_GLOSSAIRE = ((terme, employeur, page_qui_definit_ou_None), ...)` : au depart les cinq termes de cette tache ; `employeur` est le chemin relatif a la racine du depot, `page_qui_definit` vaut `None` quand le glossaire est le premier a definir le terme ;
- `TERMES_INTERDITS = ("page épinglée", "renvoi", "ancrage")` : le vocabulaire de **harnais et de planification**, mesure (`06-RESEARCH.md` § B) comme absent des deux `docs/` et de `dofus_stuff/**` ; le definir exposerait au lecteur des mots qu'il ne rencontrera jamais a l'ecran (`D-19`) ;
- `MOTIF_LIGNE_TERME = re.compile(r"^\|\s*`(?P<terme>[^`]+)`\s*\|(?P<reste>.*)\|\s*$")` : le lecteur des lignes du tableau, applique dans la section `## Les termes` ; `MOTIF_LIEN_MARKDOWN = re.compile(r"\[[^\]]*\]\((?P<cible>[^)\s]+)\)")` : le motif de renvoi, **local** au module (motif de lecture, jamais un helper partage : `D-12` ne porte que sur les helpers de `tests/conftest.py`) ;
- constantes de **motif de morsure**, portees par le module et **jamais** ecrites en clair dans une ligne d'assertion (pytest reproduit la ligne source de l'assert ; regle posee au plan 03-03) — valeurs ASCII, sans apostrophe : `MOTIF_TERMES = "termes du glossaire"`, `MOTIF_TERME_NON_DECLARE = "terme cite non declare"`, `MOTIF_TERME_ABSENT = "terme declare absent de la page"`, `MOTIF_TRI = "ordre des termes"`, `MOTIF_DOUBLON = "terme duplique"`, `MOTIF_EMPLOYEUR = "terme non employe par le fichier cite"`, `MOTIF_RENVOI_MANQUANT = "renvoi de definition manquant"`, `MOTIF_TERME_INTERDIT = "terme hors du vocabulaire du produit"`, `MOTIF_PARCOURS = "parcours conseille"`, `MOTIF_PARCOURS_LIEN = "parcours conseille en liens"`, `MOTIF_VOLATILES = "valeur volatile"`, `MOTIF_CRLF = "fins de ligne"`, `MOTIF_SOURCE = "chemin de source absent"`, `MOTIF_GARDE = "garde de cloture du harnais"` ;
- `MOTIF_VOLATILE = re.compile(r"\d{4,}")` et `MOTIF_CHEMIN_POSTE = re.compile(r"[A-Za-z]:[\\/]")` : les deux refus de `D-73`/`D-75` ;
- fonctions **pures** de constats (`D-13`) : `problemes_glossaire(docs_dir) -> list[str]` — lit la page, extrait les libelles du tableau de `## Les termes`, et exige (a) l'egalite d'ensembles **dans les deux sens** avec `TERMES_GLOSSAIRE` (un libelle non declare porte `MOTIF_TERME_NON_DECLARE`, un terme declare absent porte `MOTIF_TERME_ABSENT`), (b) la suite des libelles normalises strictement croissante (`MOTIF_TRI`) et sans doublon (`MOTIF_DOUBLON`), (c) pour chaque terme, l'existence du fichier employeur et la presence du terme dans son texte normalise (`MOTIF_EMPLOYEUR`), (d) quand une page de definition est declaree, la presence d'un lien vers **cette** page dans la ligne et la resolution de la cible depuis `docs/` (`MOTIF_RENVOI_MANQUANT`), (e) aucun libelle dans `TERMES_INTERDITS` (`MOTIF_TERME_INTERDIT`) ; et `problemes_page_glossaire(docs_dir) -> list[str]` — CRLF et absence de BOM, aucun nombre de quatre chiffres ou plus, aucun motif de chemin absolu, chaque chemin entre accents graves du bloc `## Source de vérité` existant depuis la racine du depot, la derniere ligne non vide egale a `LIEN_RETOUR` ;
- garde de cloture **reecrite** (jamais recopiee) : `RACINES_INTERDITES` (`sqlite3`, `subprocess`, `socket`, `multiprocessing`, `ctypes`, `webbrowser`, `urllib`, `requests`, `http`, `ftplib`, `smtplib`), `APPELS_SUPPRESSION`, `APPEL_PRODUIT = "main"`, `APPELS_CALCUL_PRODUIT = ("optimize_stuff", "_run_optimize_and_redirect")` ; helpers `_imports_du_module` et `_appels_du_module` repris du patron (`ast.walk` de l'arbre entier, imports imbriques compris) et test `test_garde_de_cloture_du_harnais` qui **s'auto-analyse** par `ast.parse(Path(__file__).read_text(encoding="utf-8"))` — jamais une recherche de chaines ;
- tests de la tranche : `test_garde_de_cloture_du_harnais()`, `test_entrees_du_glossaire(docs_dir, normalize)` et `test_page_glossaire_close_et_sans_valeur_volatile(docs_dir)`. Constats accumules et joints a **une seule** assertion `assert not constats`, chacun suivant le message de `D-13` : `{page} : <ce qui est faux> ; attendu <valeur> <page de definition ou fichier employeur>`.

Contraintes de forme : aucun bloc de code balise `console`, aucun lien externe, aucun jeton de brouillon. Le module n'execute jamais `main()`, n'importe jamais `sqlite3` et ne lit jamais `.data/`.
  </action>
  <acceptance_criteria>
    - `docs/glossaire.md` existe, commence par `# Glossaire`, porte un unique `## Les termes` et `## Source de vérité`, et sa derniere ligne non vide est `[Retour au sommaire](sommaire.md)`.
    - `## Les termes` porte un tableau dont la premiere cellule de chaque ligne est un terme entre accents graves, avec les cinq premiers termes declares et leur employeur.
    - `docs/sommaire.md` porte la ligne `| [Glossaire](glossaire.md) |` en fin de table, la liste « Parcours conseillé » est **restee du texte simple**, et aucune ligne existante n'a bouge.
    - `tests/test_docs_glossaire.py` porte `TERMES_GLOSSAIRE`, `TERMES_INTERDITS`, `problemes_glossaire`, `problemes_page_glossaire` et `test_garde_de_cloture_du_harnais`.
    - Retirer un terme de la page, inverser deux termes, ou pointer un employeur qui n'emploie pas le terme, rougit le module.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_glossaire.py tests/test_docs_structure.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in terme_retire ordre_inverse employeur_faux; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_glossaire.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in terme_retire) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();L=[l for l in s.splitlines(True) if "`stuff`" not in l];open(p,"w",encoding="utf-8",newline="").write("".join(L))' "$T/docs/glossaire.md"; motif='terme declare absent de la page';; ordre_inverse) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();lignes=[l for l in s.splitlines(True) if l.startswith("| `")];a=lignes[0];b=lignes[1];open(p,"w",encoding="utf-8",newline="").write(s.replace(a+b,b+a,1))' "$T/docs/glossaire.md"; motif='ordre des termes';; employeur_faux) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();open(p,"w",encoding="utf-8",newline="").write(s.replace("`dofus_stuff/cli.py`","`dofus_stuff/database.py`",1))' "$T/docs/glossaire.md"; motif='terme non employe par le fichier cite';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 06-02 (TACHE 1) EN ECHEC"; exit 1; fi; echo "morsures 06-02 tache 1 : 3/3 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 06-02 (TACHE 1) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>La page existe, elle est indexee, le premier groupe de termes est declare et employe, le tri et l'egalite d'ensembles sont controles dans les deux sens, trois derives epinglees font rougir la suite sur une copie verte avant mutation, et la suite entiere reste verte.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 2 : l'inventaire complet des termes — ce que le glossaire definit, et ce qu'il renvoie</name>
  <files>docs/glossaire.md, tests/test_docs_glossaire.py</files>
  <read_first>
    docs/glossaire.md
    docs/base-locale.md
    docs/parcours-simplifie.md
    docs/wizard-avance.md
    docs/cli.md
    docs/depannage.md
    tests/test_docs_glossaire.py
    dofus_stuff/cli.py
    dofus_stuff/database.py
    dofus_stuff/model/solver_spec.py
    dofus_stuff/web/optimize_wizard.py
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
  </read_first>
  <behavior>
    - Chaque terme du vocabulaire du produit et de la documentation figure une seule fois, et la page et `TERMES_GLOSSAIRE` restent en egalite d'ensembles dans les deux sens.
    - Pour chaque terme deja defini par une page livree, la ligne porte un lien vers **cette** page et la cible resolve depuis `docs/` ; pour les termes que le glossaire est le premier a definir, la colonne porte un tiret.
    - Chaque terme est employe par le fichier qu'il declare, et aucun libelle n'appartient au vocabulaire de harnais.
  </behavior>
  <action>Completer le glossaire avec le reste du vocabulaire, et refermer les deux controles qui distinguent « defini ici » de « renvoye ailleurs ».

**Page** — etendre le tableau de `## Les termes` aux termes que les pages livrees definissent deja : `base locale`, `synchronisation`, `mode hors-ligne`, `sauvegarde locale`, `catégorie`, `slot`, `jet`, `exo`, `palier`, `panoplie`, `trophée`, `familier`, `prysmaradite`, `bouclier`, `score`, `cible`, `poids`, `heuristique`, `solveur`, `wizard`, `ID Ankama`, `ligne de statut` — cette liste vient de `06-RESEARCH.md` § B, ou chaque ligne porte sa provenance mesuree et la colonne « deja defini ailleurs » ; l'executeur **confirme chaque employeur et chaque page de definition par une lecture** (`fichier:ligne`) avant d'ecrire la ligne (`D-19`), et peut ajouter un terme reellement employe, jamais en inventer un ;
- pour ces termes, la colonne `Déjà défini dans` porte un **lien** vers la page qui le definit deja (par exemple les termes de la base locale, de la synchronisation et du mode hors-ligne renvoient a `docs/base-locale.md`, ceux du wizard a `docs/wizard-avance.md`, ceux de la ligne de commande a `docs/cli.md`, et les messages rendus a `docs/depannage.md`), et la colonne `Définition` reste **courte** : une phrase de renvoi, jamais la definition complete de la page cible — c'est exactement `D-17` (une seule source par enonce) ;
- les termes que le glossaire est seul a definir restent sans page cible (tiret en derniere colonne) ; la page ne definit donc que ce qu'aucune page livree ne definit ;
- la **definition** d'une entree ne contient jamais un libelle du vocabulaire de harnais, et le contre-exemple est ecrit dans la docstring du module : c'est la garde du lecteur, mesuree (`06-RESEARCH.md` § B : le vocabulaire de planification n'apparait ni dans les deux `docs/` ni dans `dofus_stuff/**`, et un lecteur ne le rencontrera jamais a l'ecran) ;
- l'ensemble reste **trié** apres normalisation (`D-11`) : l'executeur ajoute les nouveaux termes a leur place et recoit `MOTIF_TRI` tant que l'ordre n'est pas celui de la normalisation — l'ordre de la normalisation et les accents de l'affichage peuvent differer, et c'est l'ordre normalise qui fait foi (limite declaree en docstring).

**Module** :
- `TERMES_GLOSSAIRE` recoit chaque terme avec son employeur et sa page de definition (`None` pour les cinq du plan initial) ; la constante reste la **source unique** du contrat (`D-14`) ;
- `problemes_glossaire` reste la seule fonction de constats pour la page ; les points (c) employeur, (d) renvoi et (e) vocabulaire interdit sont ceux deja ecrits a la tache 1 : cette tache n'ajoute **aucun** controle nouveau, elle remplit le contrat et le prouve ;
- `test_renvois_de_definition(docs_dir, normalize)` : nouveau test qui exerce le point (d) avec son propre jeu — il verifie qu'au moins un terme declare une page de definition (le contrat n'est pas vide, sinon le controle serait un faux temoin) et que chaque cible declaree est bien un nom de page de `docs/`, puis laisse `problemes_glossaire` accumuler le reste ;
- `test_le_vocabulaire_du_harnais_est_absent(docs_dir)` : nouveau test qui exerce le point (e) et refuse tout libelle de `TERMES_INTERDITS` en premiere cellule, **et** qui verifie que la constante `TERMES_INTERDITS` n'est pas vide (un refus sans liste est un vert trompeur) ;
- docstring etendue : la limite de l'appartenance de sous-chaine, le fait que le glossaire ne recoit **aucune** provenance d'execution (aucune sonde, aucune base, aucun client) — c'est une page de renvois et de lecture de fichiers, donc les modes de `06-01` n'ont pas d'equivalent ici et l'absence est ecrite, jamais silencieuse.
  </action>
  <acceptance_criteria>
    - Le tableau porte les termes du produit et de la documentation, chacun une seule fois, tries apres normalisation, avec un employeur reellement employeur.
    - Chaque terme deja defini par une page livree porte un lien vers cette page, et la cible resolve ; les termes propres au glossaire portent un tiret.
    - La colonne `Définition` des termes renvoyes ne recopie pas la definition de la page cible (renvoi court, une phrase).
    - Aucun libelle ni aucune definition ne porte un terme de `TERMES_INTERDITS`, et la constante n'est pas vide.
    - `test_renvois_de_definition` et `test_le_vocabulaire_du_harnais_est_absent` existent et echoueraient sur un contrat vide.
    - Retirer un lien de renvoi, ou ajouter une entree de vocabulaire de harnais, rougit le module.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_glossaire.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in renvoi_retire terme_interdit_ajoute; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_glossaire.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in renvoi_retire) "$PY" -c 'import re,sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();i=s.index("## Les termes");j=s.index("## Source");bloc=s[i:j];lignes=[l for l in bloc.splitlines(True) if l.startswith("| `")];cible=[l for l in lignes if "base-locale.md" in l][0];open(p,"w",encoding="utf-8",newline="").write(s.replace(cible,re.sub(r"\[[^\]]*\]\([^)]*\)","-",cible,count=1),1))' "$T/docs/glossaire.md"; motif='renvoi de definition manquant';; terme_interdit_ajoute) "$PY" -c 'import sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();anc="## Les termes";i=s.index(anc)+len(anc);j=s.index("\r\n",i)+2;ligne="| `page épinglée` | notion de planification | `dofus_stuff/cli.py` | - |\r\n";open(p,"w",encoding="utf-8",newline="").write(s[:j]+ligne+s[j:])' "$T/docs/glossaire.md"; motif='terme hors du vocabulaire du produit';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 06-02 (TACHE 2) EN ECHEC"; exit 1; fi; echo "morsures 06-02 tache 2 : 2/2 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 06-02 (TACHE 2) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>Le vocabulaire du produit et de la documentation est couvert, chaque terme est adosse a un employeur reel et renvoye vers la page qui le definit deja quand elle existe, aucun mot de harnais n'entre dans la page, et les deux derives epinglees font rougir la suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Tache 3 : le parcours conseille final devient verifiable — permutation des libelles, et surtout pas des liens</name>
  <files>tests/test_docs_glossaire.py</files>
  <read_first>
    docs/sommaire.md
    tests/test_docs_glossaire.py
    tests/test_docs_structure.py
    .planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-RESEARCH.md
  </read_first>
  <behavior>
    - La liste numerotee de `## Parcours conseillé` est une permutation des libelles du tableau `## Index` : aucun libelle inconnu, aucun libelle oublie.
    - La section ne contient aucun lien Markdown : la mesure de cette session montre que tout lien de `sommaire.md` est lu comme une entree d'index par trois gardes vertes.
    - Les deux pages de cette phase (`Dépannage`, `Glossaire`) sont nommees par le parcours, et le controle le prouve par la permutation plutot que par une enumeration recopiee.
  </behavior>
  <action>Refermer la derniere arete du critere 2 : le **parcours conseille final** de `docs/sommaire.md`, ancre aujourd'hui par aucune garde.

Ordre interne de ce plan, a respecter : la tache 1 a **deja** ajoute la ligne d'index `Glossaire` (et le plan 06-01 celle de `Dépannage`), donc le tableau d'index porte les sept lignes de contenu quand cette tache s'execute ; si la permutation est rouge, le diagnostic est un libelle de parcours qui ne reprend pas un libelle d'index, jamais un index incomplet qu'il faudrait completer ici.

**Module** — ajouter au module les lecteurs et le test du parcours, sans toucher au contrat des termes :
- `MOTIF_ENTREE_NUMEROTEE = re.compile(r"^\s*\d+\.\s+(?P<libelle>.+?)\s*$")` : les lignes de la liste numerotee de la section ; `MOTIF_LIGNE_INDEX` : la lecture des lignes du tableau d'index, dont la **premiere cellule** est le lien d'entree — un motif local de lecture, jamais un helper partage (`D-12`) ;
- `problemes_parcours(sommaire_texte, normalize) -> list[str]` : fonction **pure** de constats (elle prend le texte, pas le disque, pour rester testable sans fixture — `D-13`, `D-14`) qui exige (a) l'egalite d'ensembles **dans les deux sens** entre les libelles de la liste numerotee et les libelles du tableau d'index, apres normalisation (`MOTIF_PARCOURS`, constat nommant le libelle en trop ou manquant) ; (b) qu'aucun libelle numerote ne contienne un lien Markdown (`MOTIF_PARCOURS_LIEN`) ; (c) que chaque libelle numerote apparie un libelle d'index **exact** apres normalisation — la forme `N. Libelle` reprend le libelle d'index tel quel, sans reformulation ;
- `test_parcours_conseille_final(docs_dir, normalize)` : lit `docs/sommaire.md`, extrait les deux sections par les fixtures partagees (`section`), appelle `problemes_parcours`, et exige en plus que la liste numerotee contienne **au moins** autant d'entrees que le tableau d'index (une liste tronquee est un constat, pas une permutation) ; une seule assertion `assert not constats` ;
- `test_les_deux_pages_de_la_phase_sont_dans_le_parcours(docs_dir, normalize)` : verifie que les libelles d'index de `Glossaire` et de `Dépannage` existent **et** apparaissent dans la liste numerotee — le controle reste derive de l'index du sommaire (jamais d'une enumeration recopiee, qui deviendrait une seconde source de verite) ;
- docstring etendue : l'ordre de lecture du parcours est **libre par decision de plan** (le controle exige une permutation, pas un ordre donne), et la mesure de la conversion en liens est citee (`06-RESEARCH.md` § D : `problemes_index`, `problemes_h1` et `test_sommaire_index_labels_are_unique` lisent tout lien de `sommaire.md` comme une entree d'index) — c'est ce qui rend la prohibition `parcours conseille en liens` executable plutot que declarative.
  </action>
  <acceptance_criteria>
    - `problemes_parcours` prend le texte du sommaire et une fonction de normalisation, ne touche pas au disque, et accumule des constats portant les motifs declares.
    - `test_parcours_conseille_final` exige l'egalite d'ensembles dans les deux sens entre la liste numerotee et le tableau d'index.
    - `test_les_deux_pages_de_la_phase_sont_dans_le_parcours` prouve que `Glossaire` et `Dépannage` sont nommes par le parcours, et derive leurs libelles de l'index.
    - La section du parcours ne contient aucun lien Markdown et la liste numerotee n'est pas plus courte que l'index.
    - Renommer une entree numerotee, ou y ecrire un lien Markdown, rougit le module.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_glossaire.py -q -k "parcours"</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest, ou "no tests ran" (selecteur vide)</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in parcours_libelle_faux parcours_en_liens; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_glossaire.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in parcours_libelle_faux) "$PY" -c 'import re,sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();i=s.index("## Parcours conseillé");bloc=s[i:];m=re.search(r"(\d+)\.\s+Glossaire",bloc);fin=i+m.end();open(p,"w",encoding="utf-8",newline="").write(s[:fin]+" des termes"+s[fin:])' "$T/docs/sommaire.md"; motif='parcours conseille';; parcours_en_liens) "$PY" -c 'import re,sys;p=sys.argv[1];s=open(p,encoding="utf-8",newline="").read();i=s.index("## Parcours conseillé");bloc=s[i:];m=re.search(r"(\d+\.\s+)Glossaire",bloc);debut=i+m.start();n=i+m.end();open(p,"w",encoding="utf-8",newline="").write(s[:debut]+m.group(1)+"[Glossaire](glossaire.md)"+s[n:])' "$T/docs/sommaire.md"; motif='parcours conseille en liens';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 06-02 (TACHE 3) EN ECHEC"; exit 1; fi; echo "morsures 06-02 tache 3 : 2/2 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 06-02 (TACHE 3) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>Le parcours conseille final est verifiable : permutation des libelles d'index, aucune entree inconnue ou oubliee, les deux pages de la phase nommees, et la forme en texte simple protegee par un controle qui rougit si on la convertit en liens.</done>
</task>

</tasks>
<!-- planner-discipline-allow: db clear -->

<threat_model>
**Surface** : une page `docs/` neuve, une ligne d'index dans un fichier existant, une section existante (`## Parcours conseillé`) qui gagne un controle, un module de test neuf. Aucune sonde d'execution : ce plan **ne construit aucune base, n'instancie aucune application et n'importe aucun module du produit** — il lit des fichiers texte du depot. Les menaces sont documentaires (un terme que le depot n'emploie pas, un renvoi introuvable, un mot de harnais expose au lecteur) et de harnais (une lecture disque qui sortirait du depot).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Spoofing (definition inventee) | `docs/glossaire.md` : les libelles et leurs employeurs | high | mitigate | Chaque terme declare un employeur reellement employe : le controle exige l'existence du fichier et la presence du terme dans son texte normalise, et l'egalite d'ensembles est verifiee dans les deux sens (terme cite non declare, terme declare absent) ; trois morsures de la vague 2 prouvent la morsure (terme retire, ordre inverse, employeur faux) |
| T-06-02-02 | Information disclosure | Page : chemin de poste, valeur volatile, vocabulaire de harnais | high | mitigate | `MOTIF_VOLATILE` et `MOTIF_CHEMIN_POSTE` refusent toute valeur volatile ou absolue ; `TERMES_INTERDITS` refuse le vocabulaire de planification, mesure comme absent des deux `docs/` et de `dofus_stuff/**`, et le test refuse une liste de refus vide (un refus sans liste serait un vert trompeur) |
| T-06-02-03 | Tampering | `docs/sommaire.md` et l'index des pages | medium | mitigate | Une seule ligne d'index ajoutee, en fin de table ; les gardes livrees (`problemes_index`, `problemes_h1`, `test_sommaire_index_labels_are_unique`) restent les seules sources de verite de l'egalite index ↔ disque et du H1 (`D-12`) ; une morsure de la vague 2 prouve qu'un parcours en liens est refuse |
| T-06-02-04 | Tampering | `.data/dofus.sqlite3` | high | mitigate | Ce module n'importe ni `sqlite3`, ni le produit, et n'ouvre aucune base : la garde de cloture auto-analysee refuse l'import et la construction de base ; l'empreinte de la suite est mesuree au plan 06-04 |
| T-06-02-05 | Elevation of privilege | Harnais : execution du produit, du solveur, d'une socket | high | mitigate | Meme garde `ast` que le plan 06-01, reecrite dans ce module : racines interdites, appel au point d'entree du produit, appels de calcul et de suppression, paires de confirmation ; aucun serveur, aucune socket, aucune requete sortante |
| T-06-02-06 | Repudiation (controle qui ne selectionne rien) | Selecteurs de controle et fixtures | medium | mitigate | Le controle du parcours exige desormais l'egalite d'ensembles et le nombre d'entrees, la ou la section n'etait lue par aucune garde (mesure) ; les deux tests de renvoi et de vocabulaire exigent un contrat non vide, et le `-k "parcours"` de la tache 3 porte `no tests ran` dans ses signaux d'echec |
| T-06-02-SC | Tampering | Installations de paquets | low | accept | Aucune dependance ajoutee, `pyproject.toml` inchange ; aucun paquet installe, aucun `[ASSUMED]`, `[SUS]` ni `[SLOP]` |

**Hors perimetre** : publication, deploiement distant, synchronisation Dofusdude, suppression de donnees, achat. Aucun fichier de `dofus_stuff/**` modifie (`D-103`), `.data/` jamais approche (`D-104`), `README.md` et `GUIDE_WIZARD.md` resserves au plan 06-03.
</threat_model>

<verification>
**Controles automatises** : `tests/test_docs_glossaire.py` (garde de cloture auto-analysee, egalite d'ensembles page ↔ `TERMES_GLOSSAIRE` dans les deux sens, tri et unicite apres normalisation, employeur reellement employeur, renvoi vers la page qui definit deja quand elle existe, vocabulaire de harnais refuse, page close sans valeur volatile, parcours conseille final en permutation et jamais en liens), `tests/test_docs_structure.py`, `tests/test_docs_depannage.py` et la suite entiere, plus **sept morsures** sur copie verte avant mutation (terme retire, ordre inverse, employeur faux, renvoi de definition retire, entree de vocabulaire de harnais ajoutee, libelle de parcours renomme, entree de parcours convertie en lien).

**Non automatisable, nomme comme tel** : la qualite redactionnelle des definitions, le rendu Markdown hors GitHub, la fidelite linguistique du tri (l'ordre est celui de la normalisation `D-11`, pas une collation francaise) et l'ordre de lecture du parcours sont declares en `backstop` dans `must_haves.truths`. Aucun blocage : rien ici ne depend d'un geste humain, d'un secret ou d'un reseau.

**References de verification de phase** : `06-VALIDATION.md` § Per-Task Verification Map assigne la ligne `AIDE-02 / 06-02 / Terme → definition` a ce plan (vague 2, `tests/test_docs_glossaire.py`).
</verification>

<success_criteria>
- `docs/glossaire.md` est livree, indexee, et couvre le vocabulaire **du produit et de la documentation** : chaque terme est employe par un fichier reel, trie, unique, et renvoye vers la page qui le definit deja quand elle existe (`D-17`, `D-94`, `D-19`, critere 2, AIDE-02).
- Le glossaire ne definit **que** ce qu'aucune page livree ne definit, et aucune de ses entrees n'expose un mot de harnais.
- Le parcours conseille final du sommaire est verifiable : permutation des libelles d'index dans les deux sens, les deux pages de la phase nommees, et la forme en texte simple protegee par un controle (`D-95`).
- `docs/sommaire.md` croit d'une seule ligne d'index, sans conversion du parcours en liens, et les gardes livrees restent vertes.
- Sept derives epinglees font rougir la suite sur une copie verte avant mutation ; la suite entiere est verte et `.data/dofus.sqlite3` est intact.
</success_criteria>

<output>
Create `.planning/phases/06-depannage-glossaire-completude-et-preuve-finale/06-02-SUMMARY.md` when done
</output>

## Couverture des sources (ce plan)

| Source | Element | Consomme par |
|--------|---------|--------------|
| ROADMAP § Phase 6 | Critere 2 (glossaire du vocabulaire du produit et de la documentation, employe par le code ou les pages) | taches 1 et 2 |
| ROADMAP § Phase 6 | Decomposition imposee : « 06-02 glossaire + index » | les trois taches |
| REQUIREMENTS | AIDE-02 | `requirements` du plan |
| RESEARCH | § Measured inventory B (l'employeur de chaque terme et la colonne « deja defini ailleurs ») | taches 1 et 2 |
| RESEARCH | § D-17 duplication map (les termes que les pages livrees definissent deja : renvoi, jamais redefinition) | tache 2 |
| RESEARCH | § D et mesures du sommaire (le vocabulaire de harnais absent des deux `docs/` et du produit ; tout lien de `sommaire.md` lu comme une entree d'index par trois gardes vertes ; le parcours annonce deja les deux pages mais n'est lu par aucune garde) | taches 2 et 3 |
| RESEARCH | Pitfall 9 et pitfall non mesurable (terme du glossaire que le code ne produit pas ; la qualite d'un glossaire n'est pas une assertion) | tache 2, et `must_haves.truths` en `backstop` |
| CONTEXT | D-92 (quatre plans, vagues sequentielles) | frontmatter du plan |
| CONTEXT | D-94 (glossaire du vocabulaire du produit et de la documentation, trie, employe, jamais de terme inventé) | taches 1 et 2 |
| CONTEXT | D-95 (le parcours conseille final du sommaire devient verifiable, avec les deux pages nommees) | tache 3 |
| CONTEXT | D-96 (la liste epinglee et la preuve de completude sont au plan 06-03 ; ce plan ne les duplique pas) | `objective` et `must_haves` |
| CONTEXT | D-17 et D-19 (une seule source par enonce ; aucun terme ni message invente) | taches 1 et 2 |
| CONTEXT | D-102 (gabarit, normalisation, helpers partages, constats, constantes publiques, limites honnetes) | les trois taches |
| CONTEXT | D-103 et D-104 (code en lecture seule, aucune action destructive sur les donnees) | garde de cloture et prohibitions |
| Planification | La mesure de cette session sur le sommaire : trois gardes vertes lisent tout lien de `sommaire.md` comme une entree d'index, donc le parcours reste du texte | tache 1 (index), tache 3 (controle de forme) |

**Audit de completude de la phase (les quatre types de sources)** : GOAL — ce plan couvre le critere 2 ; les criteres 1, 3, 4 et 5 sont couverts par 06-01, 06-03 et 06-04 ; REQ — `AIDE-02` ici, `AIDE-01` au plan 06-01, `GARD-03` aux plans 06-03 et 06-04, `GARD-04` au plan 06-04 ; RESEARCH — l'inventaire mesure B, la carte de duplication D-17 et les mesures du sommaire sont consommees, aucun piege applicable laisse sans plan ; CONTEXT — D-92 a D-105 reparties sur les quatre plans, sans exception. Exclusions assumees : la liste epinglee et la preuve de mutation (plans 06-03 et 06-04), et toute resynchronisation Dofusdude (hors perimetre).


