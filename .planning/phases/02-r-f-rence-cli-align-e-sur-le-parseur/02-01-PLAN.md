---
phase: 02-r-f-rence-cli-align-e-sur-le-parseur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/cli.md
  - docs/sommaire.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Un lecteur qui ouvre docs/sommaire.md trouve la ligne d'index « CLI » et atteint docs/cli.md par un lien relatif resolu ; le H1 de la page egale le libelle d'index apres normalisation (D-28, D-06, critere de succes 4 du ROADMAP)."
    - "La page porte une section par sous-commande dans l'ordre reel du parseur (version, self-test, search, item, list, optimize, db, cache), chacune avec un synopsis et au moins un exemple marque (D-16)."
    - "Les quatre options globales (--timeout defaut 15, --data-dir decrit comme le dossier .data/ a la racine du depot, --force-sync defaut False, --offline defaut False) sont documentees avec leur defaut mesure dans le parseur, jamais la valeur absolue propre au poste (D-16, D-18, mesure M2)."
    - "Les 30 options d'optimize sont documentees en tables groupees par theme avec leur defaut mesure, sans semantique inventee pour les 11 options qui n'ont aucun help (D-18, D-19, mesure M2.3)."
    - "L'ordre reel est illustre : un et un seul exemple marque place l'option globale avant sa sous-commande, la ligne « python fetcher.py --offline optimize --demo » (D-27, critere 4a)."
    - "Toute ligne de la page qui cite db clear ou cache clear porte l'avertissement destructeur sur la meme ligne, et aucune ligne d'exemple marque ne cite ces commandes : vider la base n'est jamais presente comme une etape (D-22, D-23, critere 4b)."
    - "db sync et cache fill sont decrits comme reecrivant la base et exigeant le reseau, avec l'incompatibilite mesuree avec --offline (code 1, message « Erreur : --offline incompatible avec db sync ») : la regle « toutes les commandes portent --offline » de docs/installation.md n'est pas reconduite ici (mesure M5, Pitfall 4)."
    - "Le bloc « Source de verite » de la page nomme la fonction build_parser() de dofus_stuff/cli.py et le point d'entree fetcher.py, et chaque chemin entre accents graves de la page existe sur disque (D-30, D-03)."
  artifacts:
    - path: "docs/cli.md"
      provides: "Reference CLI livree : sections par sous-commande dans l'ordre du parseur, options globales, 30 options d'optimize groupees, alias cache, avertissement destructeur, bloc Source de verite, ligne de retour"
      contains: "# CLI"
      min_lines: 90
    - path: "docs/sommaire.md"
      provides: "Ligne d'index « CLI » pointant vers cli.md, ajoutee dans la meme unite de travail que la page"
      contains: "[CLI](cli.md)"
      min_lines: 20
  key_links:
    - from: "docs/sommaire.md"
      to: "docs/cli.md"
      via: "ligne d'index de tableau, cible relative egale au chemin de la page relatif a docs/"
      pattern: "[CLI](cli.md)"
    - from: "docs/cli.md"
      to: "docs/sommaire.md"
      via: "derniere ligne du fichier, lien relatif sans ancre vers (sommaire.md)"
      pattern: "(sommaire.md)"
    - from: "docs/cli.md"
      to: "dofus_stuff/cli.py"
      via: "bloc Source de verite nommant build_parser(), et valeurs par defaut citees relevees par parse_args"
      pattern: "build_parser()"
    - from: "docs/cli.md"
      to: "dofus_stuff/database.py"
      via: "bloc Source de verite pour la suppression effective des tables par db clear"
      pattern: "database.py"
  prohibitions:
    - statement: "Aucune table recapitulative unique de toute la surface CLI en fin de page (D-17)."
      status: resolved
      verification: judgment
      reason: "La page decrit chaque sous-commande dans sa section ; une table globale serait un second referentiel a maintenir."
    - statement: "Aucun lien direct depuis README.md vers docs/cli.md, et README.md n'est pas modifie par ce plan (D-29, D-10)."
      status: resolved
      verification: judgment
      reason: "README.md ne figure pas dans files_modified ; le lien unique vers docs/sommaire.md reste le seul point d'entree."
    - statement: "Aucune semantique inventee : les 11 options d'optimize sans help sont decrites par leur synopsis et leur defaut (D-19)."
      status: resolved
      verification: judgment
      reason: "Mesure M2.3 : --base-vit/--base-str/--base-cha/--base-agi/--base-wis/--scroll-int/--scroll-vit/--scroll-str/--scroll-cha/--scroll-agi/--scroll-wis n'ont aucun help."
    - statement: "Aucun exemple marque ne cite une commande destructrice ou une commande qui reecrit la base, et aucun exemple n'est ecrit avec un commentaire, une continuation ou un metacaractere de shell (D-22, D-24, mesure M4)."
      status: resolved
      verification: test
      reason: "Le controle des exemples marques de 02-03 decoupe chaque ligne par shlex puis parse_args : un commentaire sort en code 2, une continuation leve ValueError."
    - statement: "Aucune ancre, aucun chemin absolu, aucun sous-dossier sous docs/, aucune modification de dofus_stuff/** ni de README.md (D-01, D-29)."
      status: resolved
      verification: test
      reason: "Les invariants de phase 1 (problemes_liens, test_no_anchor_or_absolute_links) restent verts sur la nouvelle page."
  assumptions:
    - "D-26 — limite honnete : ce plan livre la page et son entree d'index ; la completude parser -> page n'est pas revendiquee. Une sous-commande ou une option ajoutee plus tard au parseur et non documentee ne fera pas echouer la suite : le controle (plans 02-02 et 02-03) va de la page vers le parseur, plus une liste epinglee explicite ecrite dans le module de test."
    - "A2 de 02-RESEARCH.md — classement des commandes : db clear et cache clear sont les seules commandes « destructrices » (dofus_stuff/database.py:146-152 supprime items et meta sans confirmation) et portent donc seules le jeton d'avertissement ; db sync et cache fill « reecrivent » la base (replace_kind / pull_all, donnees derivees de l'API, aucune donnee propre a l'utilisateur dans la base) et sont decrites sans ce jeton. Cette classification est un jugement humain derive du code, ecrit ici pour qu'un relecteur ne lise pas D-23 comme violee."
    - "A4 / Pitfall 17 — la valeur absolue du defaut de --data-dir et le contenu de .data/ n'apparaissent jamais dans la page : un lecteur d'une autre machine suivrait une commande fausse."
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 2 dans `.planning/ROADMAP.md` et des decisions D-16 a D-30 de `02-CONTEXT.md` :
**En tant que** lecteur qui recopie une commande lue dans la documentation, **je veux** disposer d'une reference `docs/cli.md` ou chaque sous-commande, chaque option et chaque exemple est reellement analysable par `fetcher.py`, **afin de** ne pas perdre de temps sur une documentation inventee.

Livrer la page de reference CLI et son entree d'index, dans la meme unite de travail : sections par sous-commande dans l'ordre reel du parseur (D-16), options globales et 30 options d'`optimize` avec leurs defauts mesures (D-18, D-19), alias `cache` sans description dupliquee (D-20), avertissement destructeur sur `db clear` (D-22, D-23), ordre global-avant-sous-commande illustre (D-27), gabarit de page de la phase 1 (D-30).

Purpose : c'est l'objet des criteres de succes 1 a 4 de la phase — la page est la surface que les plans 02-02 et 02-03 ancreront sur le parseur, et que les phases 3, 5 et 6 citeront. Livrer la page sans son entree d'index rendrait la suite rouge : mesure de `02-RESEARCH.md` Pitfall 15, une page `cli.md` sans ligne d'index produit deux problemes nommes (`problemes_index` et `problemes_h1`), alors que la paire page + entree `| [CLI](cli.md) | ... |` avec le H1 `# CLI` donne zero probleme sur `problemes_index`, `problemes_h1`, `problemes_retour_sommaire` et `problemes_encodage`.

Output : `docs/cli.md` (nouvelle page) et `docs/sommaire.md` (une ligne d'index ajoutee).

Ordonnancement declare : ce plan est la vague 1 de la phase. Son premier bloc est un tranchant de bout en bout (tracer) : page minimale mais reelle + entree d'index + invariants de phase 1 verts, prouve avant toute largeur. Les deux blocs suivants elargissent la meme page (options globales et tables d'`optimize`, puis alias `cache` et avertissement destructeur). Aucun plan ne peut demarrer avant celui-ci : les plans 02-02 et 02-03 lisent `docs/cli.md`.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-CONTEXT.md
@.planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-RESEARCH.md
@.planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-PATTERNS.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
@docs/sommaire.md
@docs/installation.md
@dofus_stuff/cli.py
@fetcher.py
@tests/test_docs_structure.py
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 (tranchant) : chaine docs/sommaire.md -> docs/cli.md et invariants de phase 1 verts</name>
  <files>docs/cli.md, docs/sommaire.md</files>
  <read_first>
    docs/sommaire.md
    docs/installation.md
    dofus_stuff/cli.py
    fetcher.py
    .planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-CONTEXT.md
    .planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-RESEARCH.md
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
  </read_first>
  <action>Prouver la chaine de bout en bout avant toute largeur : creer `docs/cli.md` avec la forme minimale mais reelle de la page, et ajouter son entree d'index a `docs/sommaire.md` dans le meme travail (D-28) — la page sans son entree rend la suite rouge (Pitfall 15).

Contenu de `docs/cli.md` a ce stade, en francais accentue, UTF-8 strict sans BOM :
1. ligne 1 : le H1 `# CLI` (unique, libelle egal a celui de l'entree d'index apres normalisation — D-06) ;
2. une phrase d'introduction a la troisieme personne disant ce que la page couvre (forme de `docs/installation.md:3`) ;
3. une section `## Options globales` avec un tableau `| Option | Role | Defaut |` des quatre options globales et leur defaut mesure : `--timeout` (defaut 15), `--data-dir` (defaut : le dossier `.data/` a la racine du depot — jamais la valeur absolue du poste), `--force-sync` (defaut faux), `--offline` (defaut faux) ;
4. une section de niveau 2 par sous-commande, dans l'ordre reel du parseur : `## version`, `## self-test`, `## search`, `## item`, `## list`, `## optimize`, `## db`, `## cache`. Chacune porte un synopsis en une phrase (aide du parseur, verbatim : « Afficher la version Dofus de la base locale », « Verifier la base locale et des objets connus », « Rechercher des objets (local) », « Detail d'un equipement par ID Ankama », « Lister une page d'equipements », « Optimiser un stuff pour un niveau et une/des caracteristiques », « Gerer la base locale ») et exactement un exemple ;
5. un bloc `## Source de verite` sur la forme de liste de `docs/installation.md:131-139`, avec `fetcher.py` (point d'entree de la ligne de commande), `dofus_stuff/cli.py` (parseur et commandes reellement disponibles ; la fonction `build_parser()` y declare la surface documentee ici), `dofus_stuff/database.py` (suppression effective des tables par `db clear`, commande destructrice) et `dofus_stuff/optimize/profile_input.py` (choix du mode interactif) ;
6. derniere ligne du fichier : `[Retour au sommaire](sommaire.md)`.

Exemples marques (D-24) : chaque exemple vit dans un bloc de code dont la ligne d'ouverture est exactement trois accents graves suivis de `console`, et chaque ligne d'un tel bloc est une commande complete d'une seule ligne commencant par `python fetcher.py `, sans prompt, sans commentaire en fin de ligne, sans continuation par un antislash, sans metacaractere de shell (`*`, `$`, `~`) — chacune de ces quatre formes a ete mesuree comme cassant `shlex.split` ou `parse_args`, ou comme rendant le controle aveugle (mesure M4). Les huit exemples du tranchant, un par sous-commande : `python fetcher.py --offline version`, `python fetcher.py --offline self-test`, `python fetcher.py --offline search Atcham`, `python fetcher.py --offline item 44`, `python fetcher.py --offline list --page 2`, `python fetcher.py --offline optimize --demo`, `python fetcher.py --offline db status`, `python fetcher.py --offline cache status`. La ligne `python fetcher.py --offline optimize --demo` est l'unique temoin de l'ordre global-avant-sous-commande (D-27) : aucun autre exemple marque de la page ne doit porter `--offline optimize`, pour que cette assertion ait un seul temoin observable.

Entree d'index dans `docs/sommaire.md` : ajouter, a la suite de la ligne existante du tableau `## Index`, la ligne `| [CLI](cli.md) | Commandes, options et exemples de fetcher.py |`. Ne rien changer d'autre dans `docs/sommaire.md` (le parcours conseille en texte numerote reste tel quel : ce n'est pas un lien, il n'est vu par aucun invariant). Ne pas toucher `README.md` (D-29).

Contraintes de redaction qui restent vraies pour toute la page : aucune ancre dans une cible de lien, aucun chemin absolu, aucun sous-dossier sous `docs/`, aucun jeton de brouillon (la chaine « todo », « a completer » ou « lorem » apres normalisation), au moins 300 caracteres une fois les espaces de bord retires. Ne jamais ecrire la commande qui vide la base dans un bloc de code : seule la prose la mentionne, avec son avertissement sur la meme ligne (D-22).

Fin de tache : `.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q` doit etre vert — c'est la preuve de bout en bout du tranchant (exhaustivite sommaire <-> pages livrees, H1 egal au libelle d'index, lien de retour, liens resolus, encodage strict).</action>
  <acceptance_criteria>
    - `docs/cli.md` existe, commence par le H1 `# CLI`, porte la ligne de retour vers le sommaire et fait au moins 300 caracteres hors espaces de bord.
    - `docs/sommaire.md` porte exactement une ligne de plus dans son tableau `## Index`, la ligne `| [CLI](cli.md) | ... |`.
    - Les huit sections de sous-commande existent dans l'ordre `version`, `self-test`, `search`, `item`, `list`, `optimize`, `db`, `cache`.
    - Chaque exemple marque appartient a un bloc d'ouverture `console`, commence par `python fetcher.py ` et ne contient ni commentaire, ni continuation, ni metacaractere de shell.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q` est vert : `problemes_index`, `problemes_h1`, `problemes_retour_sommaire`, `problemes_encodage` et `problemes_liens` ne signalent rien sur la nouvelle page.
    - `README.md` n'est pas modifie.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>grep -q '^# CLI$' docs/cli.md && grep -q '(sommaire.md)' docs/cli.md && grep -q '\[CLI\](cli.md)' docs/sommaire.md && test "$(grep -c '^## ' docs/cli.md)" -ge 9</automated>
    <fails_when>exit non-zero : H1 absent ou different, ligne de retour absente, entree d'index absente, ou moins de 9 titres de niveau 2</fails_when>
  </verify>
  <done>La page minimale et son entree d'index sont livrees ensemble, et les invariants structurels de la phase 1 sont verts sur la nouvelle page.</done>
</task>

<task type="auto">
  <name>Tache 2 : options globales et les 30 options d'optimize avec leurs defauts mesures</name>
  <files>docs/cli.md</files>
  <read_first>
    docs/cli.md
    dofus_stuff/cli.py
    .planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-RESEARCH.md
    .planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-CONTEXT.md
  </read_first>
  <reversibility rating="costly">Le decoupage thematique des tables d'optimize est fixe ici (D-18) : le corriger plus tard impose de reecrire les tables de la page et de revalider les controles d'ancrage des options deja ecrits, ainsi que la section que les phases 3 et 6 citent comme reference.</reversibility>
  <action>Elargir la page livree par la tache 1, sans changer son H1, son entree d'index ni sa ligne de retour (D-16, D-18, D-19).

Dans `## Options globales`, completer le tableau pour que chaque ligne porte le role et le defaut releve par `parse_args` : `--timeout` (defaut 15, « Timeout HTTP (s) »), `--data-dir` (defaut : le dossier `.data/` a la racine du depot — l'aide du parseur imprime un chemin absolu propre au poste, qui ne doit jamais etre recopie : A4, Pitfall 17), `--force-sync` (defaut faux, « Ignorer la fenetre 24h et forcer une verif / sync version »), `--offline` (defaut faux, « Ne pas contacter l'API (echoue si la base locale est vide) »). Puis deux paragraphes adosses a des mesures, chacun citant `dofus_stuff/cli.py` : (i) les options globales sont declarees sur le parseur racine et s'ecrivent donc AVANT la sous-commande — la forme fautive `db status --offline` sort en code 2 avec la ligne `fetcher.py: error: unrecognized arguments: --offline` (mesure M5 ; le bloc d'erreurs equivalent de `docs/installation.md:119-129` reste en place) ; (ii) `--offline` et la synchronisation sont incompatibles : `--offline db sync` sort en code 1 avec `Erreur : --offline incompatible avec db sync` — ne pas reconduire ici la regle « toutes les commandes portent --offline » de la page d'installation, qui serait mecaniquement fausse (Pitfall 4). Ne pas decrire la fenetre de 24 h au-dela de ce que dit l'aide de `--force-sync` : sa mecanique appartient a la page de la phase 5.

Section `## search` : documenter le positionnel `query` (« Terme de recherche (sensible a la casse) ») et `--limit` (« Nombre max de resultats », defaut 10).
Section `## item` : documenter le positionnel `ankama_id` (« ID Ankama de l'objet », entier, requis).
Section `## list` : documenter `--page` (« Numero de page », defaut 1) et `--size` (« Taille de page », defaut 5).
Sections `## version` et `## self-test` : indiquer qu'aucune option propre n'existe (le synopsis suffit).

Section `## optimize` : synopsis (« Optimiser un stuff pour un niveau et une/des caracteristiques »), puis le mode interactif explique une fois, adosse au code : sans `--level`, sans `--max` et sans `--demo`, le CLI entre dans les questions guidees (`dofus_stuff/optimize/profile_input.py`, `needs_interactive_optimize`) — l'exemple `python fetcher.py --offline optimize` est donc accepte par le parseur ; la page le decrit, elle ne l'execute jamais.
Puis les 30 options en quatre tables groupees par theme, chaque ligne portant l'option entre accents graves, son role lu dans l'aide du parseur quand elle en a un, et son defaut mesure (mesure M2.3) :
- profil et objectifs (7) : `--level` (defaut aucun), `--max` (« Caracteristique(s) a maximiser (ex: intelligence) », defaut aucun), `--target` (« Cibles STAT=valeur (ex: intelligence=1200 pa=11) », defaut aucun), `--weight` (« Poids STAT=valeur (ex: intelligence=2 vitalite=0.5) », defaut aucun), `--auto-points` (defaut faux), `--demo` (defaut faux), `--stop-when-satisfied` (defaut faux) ;
- contraintes de selection (6) : `--ban` (« Ankama IDs interdits », defaut aucun), `--force` (« Ankama IDs obligatoires », defaut aucun), `--classic-only` (defaut faux), `--allow-power` (defaut faux), `--allow-damages` (defaut faux), `--allow-crit-damages` (defaut faux) ;
- caracteristiques de base et scrolls (12) : `--base-int`, `--base-vit`, `--base-str`, `--base-cha`, `--base-agi`, `--base-wis`, `--scroll-int`, `--scroll-vit`, `--scroll-str`, `--scroll-cha`, `--scroll-agi`, `--scroll-wis`, tous de type decimal et de defaut `0` — seule `--base-int` a une aide (« Intelligence hors stuff (capital deja converti) ») ; les onze autres sont decrites par leur synopsis et leur defaut, sans semantique inventee (D-19) ;
- RNG et performance (5) : `--jet` (« Mode de jets d'objets (defaut: average) », valeurs `min`, `average`, `max`, defaut `average`), `--top-k` (« Candidats top-K par slot », defaut 30), `--time-limit` (« Timeout CP-SAT en secondes », defaut 5.0), `--no-cpsat` (defaut faux), `--seed` (« Seed RNG », defaut aucun).

Ne pas ecrire de table recapitulative unique en fin de page (D-17) : la seule source de verite reste le parseur, la page le decrit section par section. Ne pas documenter l'option `--all` de `db clear` : elle est acceptee par le parseur mais jamais lue par le code, la documenter serait inventer une semantique (D-19, mesure M7).

Fin de tache : la suite de structure doit rester verte et les 34 jetons d'options (4 globales + 30 d'optimize) doivent etre presents dans la page.</action>
  <acceptance_criteria>
    - Les quatre options globales sont presentes avec leur defaut mesure, et aucun chemin absolu de poste n'apparait dans la page.
    - Les 30 options d'optimize sont presentes, reparties en quatre tables groupees, chacune avec son defaut mesure ; les 11 options sans aide sont decrites sans semantique ajoutee.
    - `--jet` porte ses trois valeurs (`min`, `average`, `max`) et sa valeur par defaut `average`.
    - Les paragraphes d'ordre des options et d'incompatibilite `--offline` avec la synchronisation citent `dofus_stuff/cli.py` et les codes de retour mesures (2 et 1).
    - Aucune table recapitulative unique ; aucune mention de `--all`.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q` reste vert.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>miss="$(for t in --timeout --data-dir --force-sync --offline --level --max --base-int --base-vit --base-str --base-cha --base-agi --base-wis --scroll-int --scroll-vit --scroll-str --scroll-cha --scroll-agi --scroll-wis --jet --top-k --time-limit --no-cpsat --classic-only --demo --target --weight --ban --force --seed --stop-when-satisfied --auto-points --allow-power --allow-damages --allow-crit-damages; do grep -qF -- "$t" docs/cli.md || echo "$t"; done)"; test -z "$miss" || { echo "options absentes de docs/cli.md : $miss"; exit 1; }</automated>
    <fails_when>exit non-zero, ou la ligne "options absentes de docs/cli.md : ..." nommant au moins un jeton d'option</fails_when>
  </verify>
  <done>La page documente la surface d'options reellement mesuree : quatre options globales et trente options d'optimize, avec leurs defauts, leurs themes et les deux pieges d'ordre.</done>
</task>

<task type="auto">
  <name>Tache 3 : alias cache, commandes qui reecrivent ou vident la base, avertissement destructeur</name>
  <files>docs/cli.md</files>
  <read_first>
    docs/cli.md
    dofus_stuff/cli.py
    dofus_stuff/database.py
    .planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-RESEARCH.md
    .planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-CONTEXT.md
  </read_first>
  <action>Completer la page sur quatre points, en gardant l'avertissement destructeur strictement conforme a D-22 et D-23.

1. Section `## db` : tableau des cinq sous-commandes dans l'ordre du parseur, en jetons nus (`status`, `stats`, `sync`, `fill`, `clear`) — jamais la forme « db clear » dans ce tableau, pour que l'avertissement ne se disperse pas. Citer le role des trois sous-commandes qui ont une description dans l'aide, verbatim : `status` « Afficher l'etat de la base », `sync` « Forcer la synchronisation complete », `clear` « Vider la base locale ». Signaler que `stats` et `fill` sont des alias sans description dans l'aide (`argparse.SUPPRESS`), et non des sous-commandes invisibles : mesure M3, l'aide imprime la ligne `    stats               ==SUPPRESS==` — dire « sans description dans l'aide », jamais « invisible ». Preciser que `stats` equivaut a `status` (lecture seule) et `fill` a `sync` (reecriture reseau), d'apres `_normalize_db_command` (`dofus_stuff/cli.py`, table `aliases = {"stats": "status", "fill": "sync"}`).

2. Sous-section de la section `## db` : `### \`db clear\`, commande destructrice`. Le titre lui-meme porte les deux exigences sur la meme ligne (le jeton de commande et le mot d'avertissement, D-22). Le corps decrit, en prose et jamais en bloc de code : la commande vide entierement la base locale (`DELETE FROM items` puis `DELETE FROM meta`, sans confirmation, cf. `dofus_stuff/database.py`), la meme ligne nommant aussi `cache clear` qui vide la base de facon identique ; l'option `--all` est acceptee par le parseur mais jamais lue par le code, elle n'est donc pas documentee ; la commande n'est une etape d'aucun parcours de cette page et ne doit pas etre lancee par inadvertance. Toute ligne de la page citant `db clear` ou `cache clear` porte le mot d'avertissement (« destructrice ») sur la meme ligne, y compris la ligne du bloc « Source de verite » qui cite `dofus_stuff/database.py` : un controle de 02-03 evalue cette co-presence ligne a ligne sur le texte entier, jamais par section (lecon WR-02).

3. Sous-section ou paragraphe de la section `## db` pour `db sync` et `cache fill` : ils reecrivent la base (retelechargement des objets, `replace_kind` / `pull_all`), exigent le reseau, et sont incompatibles avec `--offline` (code 1, `Erreur : --offline incompatible avec db sync`). Ces deux commandes ne portent PAS le jeton d'avertissement destructeur : elles ne detruisent aucune donnee propre a l'utilisateur (les stuffs sauvegardes vivent cote navigateur) — classement A2 de la recherche, ecrit dans les assumptions de ce plan.

4. Section `## cache` : presenter la sous-commande comme l'alias de `db` — forme canonique `cache <sous-commande>` — sans dupliquer la description de `## db`, en renvoyant a cette section. Les trois descriptions du groupe (`Afficher l'etat de la base`, `Forcer la synchronisation complete`, `Vider la base locale`) ne doivent PAS apparaitre dans cette section : c'est la traduction testable de D-20, et c'est pour cette raison que la section `## db` les cite. Ne jamais ecrire `cache clear` dans cette section : le seul endroit de la page qui nomme `cache clear` reste la ligne d'avertissement destructeur.

Exemples marques : aucun bloc `console` de la page ne contient `db clear`, `cache clear`, `db sync` ni `cache fill` — ces commandes qui vident ou reecrivent la base ne sont jamais presentees comme une commande a recopier (D-22, D-23). L'unique temoin de l'ordre global-avant-sous-commande reste `python fetcher.py --offline optimize --demo` (D-27) : ne pas ajouter de second exemple portant `--offline optimize`.

Ne modifier ni le H1, ni l'entree d'index, ni la ligne de retour. Fin de tache : la co-presence de l'avertissement doit etre verifiee sur le texte entier, et la suite de structure rester verte.</action>
  <acceptance_criteria>
    - La section `## db` liste les cinq sous-commandes dans l'ordre du parseur, cite les trois descriptions de l'aide et signale `stats` et `fill` comme alias sans description dans l'aide.
    - Mesure M5 respectee : `db sync` et `cache fill` sont decrits comme reecrivant la base, exigeant le reseau et incompatibles avec `--offline`, sans porter le jeton d'avertissement destructeur.
    - Chaque ligne de `docs/cli.md` qui cite `db clear` ou `cache clear` porte le mot d'avertissement sur la meme ligne, et au moins une ligne les cite (sinon la regle serait vide, donc infalsifiable).
    - Aucun bloc `console` ne cite `db clear`, `cache clear`, `db sync` ou `cache fill`.
    - La section `## cache` declare l'alias et ne reprend aucune des trois descriptions de `db`.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q` reste vert.
  </acceptance_criteria>
  <verify>
    <automated>tot="$(grep -cE '(db|cache) +clear' docs/cli.md || true)"; sans="$(grep -E '(db|cache) +clear' docs/cli.md | grep -cv destruct || true)"; test "$tot" != 0 && test "$sans" = 0 || { echo "avertissement destructeur : $tot ligne(s) citant clear, $sans sans avertissement"; exit 1; }</automated>
    <fails_when>exit non-zero, ou la ligne "avertissement destructeur : ..." indiquant zero ligne citant clear, ou au moins une ligne citant la commande sans son avertissement</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
  </verify>
  <done>L'alias cache est presente sans description dupliquee, les commandes qui vident ou reecrivent la base sont signalees comme telles, et la mention de la commande destructrice porte toujours son avertissement sur la meme ligne.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> lecteur | La page est suivie aveuglement : une commande destructrice ou une option presentee comme normale est recopiee dans un terminal, sur une base de developpement reelle. |
| Page -> base locale `.data/dofus.sqlite3` | La page nomme des commandes qui vident ou reecrivent la base ; elle ne doit jamais les presenter comme une etape, et aucun controle de cette phase ne les execute. |
| Page -> code produit | Les options, defauts et descriptions cites par la page doivent venir de `dofus_stuff/cli.py` (surfaces publiques `parse_args` et aide), jamais d'une relecture approximative. |
| Page -> index (`docs/sommaire.md`) | Une page livree sans entree d'index, ou avec un H1 divergent, casse l'exhaustivite bidirectionnelle qui garde toute la documentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Denial of Service (perte de la base locale) | `docs/cli.md`, mention de `db clear` et `cache clear` | high | mitigate | Avertissement destructeur exige sur la meme ligne que la commande (D-22), interdiction de toute mention dans un bloc `console` (D-24), et controle du plan 02-03 sur le texte entier, jamais par section (lecon WR-02). Aucune execution, a aucun moment. |
| T-02-02 | Denial of Service (quota Dofusdude) | `docs/cli.md`, mention de `db sync` et `cache fill` | medium | mitigate | Les deux commandes sont decrites comme reecrivant la base et exigeant le reseau, avec l'incompatibilite mesuree avec `--offline` (code 1) ; elles n'apparaissent dans aucun exemple marque, donc aucun lecteur ne les recopie depuis un parcours hors-ligne. |
| T-02-03 | Tampering (documentation perimee) | page livree avant les controles des plans suivants | medium | mitigate | Le tranchant de la tache 1 active des maintenant les invariants de phase 1 (exhaustivite, H1, retour, encodage, liens) ; les plans 02-02 et 02-03 ajoutent l'ancrage sur le parseur. La limite D-26 est ecrite dans ce plan et dans le module de test. |
| T-02-04 | Information Disclosure | `--data-dir` et chemins cites par la page | low | mitigate | La page decrit le dossier `.data/` a la racine du depot et ne recopie jamais la valeur absolue imprimee par l'aide (A4) ; chaque chemin cite existe dans le depot. |
| T-02-05 | Spoofing (option inventee) | tables d'options d'`optimize`, options globales | high | mitigate | Aucune option n'est ecrite de memoire : les 34 jetons et leurs defauts sont releves dans les mesures M2 et M2.3, et les controles des plans suivants rejouent chaque sonde sur `build_parser().parse_args`. |
| T-02-SC | Tampering | installs npm/pip/cargo | low | accept | Aucune installation dans cette phase : aucune dependance ajoutee, `pytest` deja present dans `.venv` (mesure : 158 passed au 2026-09-11 avant la phase). Le gate de legitimite des paquets n'a donc pas d'objet. |
</threat_model>

<verification>
- `./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q` apres chaque tache (invariants de structure sur la nouvelle page et son entree d'index).
- `./.venv/Scripts/python.exe -m pytest -q` avant la fin du plan : suite complete verte, aucun test existant ne regresse (reference mesuree avant cette phase : 158 passed).
- `grep -nE '(db|cache) +clear' docs/cli.md` montre chaque mention accompagnee du mot d'avertissement sur la meme ligne ; `grep -nE '^python fetcher.py' docs/cli.md` montre les exemples marques, dont l'unique temoin `--offline optimize`.
- `git status --porcelain -- docs tests README.md` : seuls `docs/cli.md` (nouveau) et `docs/sommaire.md` (une ligne) apparaissent ; `README.md` et `dofus_stuff/**` ne sont pas modifies.
- Horodatage de `.data/dofus.sqlite3` inchange apres la suite : ce plan ne lit ni n'ecrit aucune base, aucune commande du produit n'est executee.
</verification>

<success_criteria>
- Les criteres de succes 1 a 4 du ROADMAP sont documentes sur la page : sous-commandes dans l'ordre du parseur, options globales et options d'`optimize` avec leurs defauts mesures, ordre global-avant-sous-commande illustre, mention destructrice toujours avertie.
- CLI-01, CLI-02 et CLI-03 disposent de leur substance ecrite : les sous-commandes, les options et les exemples a ancrer existent dans `docs/cli.md` (les contrats de test arrivent aux plans 02-02 et 02-03).
- La page respecte le gabarit de la phase 1 (H1 unique, phrase d'introduction, sections courtes, bloc « Source de verite » nommant `build_parser()`, ligne de retour) et n'introduit aucune ancre, aucun chemin absolu, aucun sous-dossier, aucune dependance.
- `README.md`, `dofus_stuff/**`, `GUIDE_WIZARD.md` et `.data/` sont intacts.
</success_criteria>

## Dispositions des contributions

| Contribution | Firing condition | Disposition |
|--------------|------------------|-------------|
| security | S'applique des que `security_enforcement` est actif (ASVS niveau 1, blocage sur `high`) | **S'applique.** Le bloc de menaces ci-dessus est ecrit pour un livrable documentaire : la commande destructrice presentee comme une etape (T-02-01), la commande reseau proposee depuis un parcours hors-ligne (T-02-02), la page qui derive faute de controle (T-02-03), le chemin de poste fige (T-02-04), l'option inventee (T-02-05). Aucune menace applicative inventee pour une phase de documentation. |
| ai-integration | Se declenche si la phase integre une API, un SDK ou un service externe (detecteur `api-coverage.cjs` sur l'etendue de la phase) | **Ne s'applique pas.** Le detecteur a renvoye `detected: false` sur la section de phase du ROADMAP (mesure du 2026-09-11) : la phase est documentaire et n'ajoute aucun appel sortant. La page ne fait que *nommer* des commandes existantes (`db sync`, `cache fill`) et l'option `--offline` ; aucune matrice de couverture d'API, aucun `COVERAGE.md` n'est requis. |
| assumption-delta | Se declenche sur une transition singulier -> pluriel, requis -> optionnel ou derive -> choisi (`assumption-delta scan`) | **Ne s'applique pas.** Le detecteur a renvoye `detected: false` (mesure du 2026-09-11). La phase ne change aucun modele d'identite : elle *documente* l'alias `cache` comme un nom existant des memes sous-commandes que `db`, propriete deja presente dans le parseur gele et prouvee par ailleurs (plan 02-02, egalite des espaces de noms modulo la cle `command`). Aucun choix `promote` / `add-alongside` n'est a prendre. |
| schema-gate | Se declenche si l'etendue de la phase cite un fichier de schema d'ORM (`prisma/schema.prisma`, `drizzle/schema.ts`, `supabase/migrations/*.sql`, `src/entities/**`, `src/collections/**`) | **Ne s'applique pas.** L'etendue de la phase (section de ROADMAP, `02-CONTEXT.md`, `02-RESEARCH.md`, `files_modified` ci-dessus) ne contient aucun de ces motifs : les fichiers de cette phase sont du Markdown et des modules pytest. Aucune tache de push de schema n'est injectee, et aucun push ne doit etre lance. |

## Artifacts this phase produces

Plan 02-01 (perimetre de ce plan) :

- `docs/cli.md` — page de reference CLI : `# CLI`, une phrase d'introduction, `## Options globales` (4 options + 2 paragraphes d'ordre et d'incompatibilite), une section par sous-commande dans l'ordre du parseur (`## version`, `## self-test`, `## search`, `## item`, `## list`, `## optimize`, `## db`, `## cache`), les 4 tables thematiques des 30 options d'`optimize`, la sous-section destructrice, le bloc `## Source de verite`, la ligne de retour ; les exemples marques vivent dans des blocs a balise `console`.
- `docs/sommaire.md` — une ligne d'index ajoutee au tableau `## Index` : `| [CLI](cli.md) | ... |`.

Les symboles de test de cette phase (`tests/test_docs_cli.py`, fixtures de `tests/conftest.py`) sont produits par les plans 02-02 et 02-03.

<output>
Create `.planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-01-SUMMARY.md` when done.
</output>

