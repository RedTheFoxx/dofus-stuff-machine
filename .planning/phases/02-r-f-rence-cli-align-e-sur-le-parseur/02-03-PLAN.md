---
phase: 02-r-f-rence-cli-align-e-sur-le-parseur
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - tests/conftest.py
  - tests/test_docs_cli.py
autonomous: true
requirements:
  - CLI-03
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Chaque ligne des blocs de code marques console de docs/cli.md appartient a la page (elle en est extraite), se decoupe par shlex.split sans ValueError et est acceptee par build_parser().parse_args : un exemple casse ne passe pas (CLI-03, D-24, D-25)."
    - "Chaque sous-commande documentee possede au moins un exemple marque : l'ensemble des sous-commandes atteintes par les exemples est exactement les huit noms epingles, et les manquantes sont nommees (CLI-03)."
    - "Au moins un exemple marque place une option globale avant sa sous-commande, la ligne python fetcher.py --offline optimize --demo : l'ordre reel du parseur est enseigne, pas seulement decrit (critere de succes 4a, D-27)."
    - "Toute ligne de la page qui cite db clear ou cache clear porte l'avertissement destructeur sur la meme ligne, au moins une ligne les cite, et aucune ligne d'exemple marque ne les cite : vider la base n'est jamais une commande a recopier (critere de succes 4b, D-22, D-23)."
    - "Aucun SystemExit ni ValueError ne traverse : chaque echec devient une AssertionError citant la page, l'exemple fautif, la cause et dofus_stuff/cli.py (D-13, D-31)."
    - "Le module d'ancrage CLI n'importe que le parseur public de dofus_stuff.cli : il n'appelle jamais main(), n'ouvre aucune base, ne lance aucun sous-processus et n'ouvre aucune socket, et cette propriete est verifiee sur le texte du module lui-meme (critere de succes 5, D-15)."
    - "La limite D-26 est ecrite dans le module : la completude parser -> page n'est pas revendiquee, l'assertion verbatim des exemples est une garantie de construction, et « hors parcours recommande » n'est pas decidable mecaniquement."
    - "Chaque assertion introduite par la phase porte une preuve planifiee qu'elle peut echouer : une mutation etiquetee executee dans une copie jetable (2 en 02-02 tache 1, 8 en 02-02 tache 2, 5 en 02-03 tache 1, 4 en 02-03 tache 2) ou une declaration de non-mutabilite avec sa raison mesuree dans le tableau Mutation coverage ; le resultat de la batterie est consigne mutation par mutation dans 02-03-SUMMARY.md, aucune assertion ne reposant sur la seule affirmation qu'elle existe (propriete de Nyquist de la phase, T-02-14)."
  artifacts:
    - path: "tests/test_docs_cli.py"
      provides: "Module d'ancrage CLI complete : exemples marques analysables, couverture par sous-commande, ordre global avant sous-commande, garde destructrice, propriete statique sans execution"
      contains: "def test_commande_destructrice_avertie_et_jamais_dans_un_exemple("
      min_lines: 240
    - path: "tests/conftest.py"
      provides: "Projection des blocs marques console : constante BALISE_EXEMPLE, helper _lignes_exemple et fixture lignes_exemple"
      contains: "BALISE_EXEMPLE"
      min_lines: 175
  key_links:
    - from: "tests/test_docs_cli.py"
      to: "tests/conftest.py"
      via: "fixture lignes_exemple, projection des blocs dont la balise d'ouverture est console"
      pattern: "lignes_exemple"
    - from: "tests/test_docs_cli.py"
      to: "docs/cli.md"
      via: "extraction des exemples marques, comparaison verbatim, decoupage shlex et sondage build_parser().parse_args"
      pattern: "cli.md"
    - from: "tests/test_docs_cli.py"
      to: "docs/cli.md"
      via: "controle sur le texte entier, ligne a ligne, de la co-presence de la commande destructrice et de son avertissement"
      pattern: "JETON_DESTRUCTEUR"
    - from: "tests/test_docs_cli.py"
      to: "dofus_stuff/cli.py"
      via: "build_parser().parse_args pour chaque exemple marque, sans jamais executer la commande"
      pattern: "build_cli_parser"
  prohibitions:
    - statement: "Aucune commande documentee n'est executee : pas de subprocess, pas de main(), pas d'appel au CLI ni a l'interface web (D-15, D-22)."
      status: resolved
      verification: test
      reason: "Les exemples passent par shlex.split puis parse_args ; l'assertion statique du module verifie l'absence de tout appel a main et de tout import de sous-processus."
    - statement: "Aucune execution de main(), aucune ouverture de base, aucune ecriture sous .data/, aucune connexion reseau (D-15)."
      status: resolved
      verification: test
      reason: "Le module n'importe que build_parser ; aucun chemin de ce plan ne construit de catalogue, de base ou de client de test."
    - statement: "Aucune regle destructive ni regle d'exemple evaluee par section : l'entete de page et les lignes hors titres de niveau 2 doivent etre couvertes (lecon WR-02)."
      status: resolved
      verification: test
      reason: "Les deux regles s'appliquent ligne a ligne sur le texte entier de la page ; seules les tables d'options utilisent un perimetre de section."
    - statement: "Aucun motif trop etroit : le jeton destructeur couvre db clear et cache clear, avec des espaces quelconques entre les deux mots (lecon WR-01, WR-03)."
      status: resolved
      verification: test
      reason: "Le motif du jeton destructeur est une alternance (db|cache) suivie d'espaces puis du mot clear."
    - statement: "Aucune mutation encodee en dur dans le module, aucun nom de page que cette phase ne livre pas cite par une assertion (lecon WR-05)."
      status: resolved
      verification: judgment
      reason: "Le test de mutation formel est planifie en phase 6 (GARD-03) ; les derives de ce plan sont injectees a la main dans une copie jetable."
  assumptions:
    - "D-26 — limite honnete, ecrite dans le module : la completude parser -> page n'est pas revendiquee (une option ajoutee plus tard au parseur et non documentee ne fera pas echouer la suite) ; l'assertion « verbatim » de CLI-03 est tautologique, la ligne etant extraite de la page — c'est une garantie de construction, pas une exigence de validation ; la qualite « hors parcours recommande » n'est pas decidable mecaniquement et n'est approchee que par la co-presence sur la meme ligne et l'absence de tout bloc marque."
    - "A2 de 02-RESEARCH.md — le caractere « destructeur » de db clear et cache clear est un classement humain derive du code (dofus_stuff/database.py, Database.clear supprime items et meta sans confirmation). db sync et cache fill reecrivent la base sans detruire de donnee propre a l'utilisateur et ne portent donc pas le jeton d'avertissement ; ils sont seulement exclus des exemples marques."
    - "Pitfall 9 — un commentaire en fin de ligne (`# ...`) produit des arguments excedentaires et le parseur sort en code 2, une continuation par antislash leve ValueError : ce sont des regles de redaction de la page, ecrites dans les messages d'echec du test, pas des derives du parseur."
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 2 dans `.planning/ROADMAP.md`, de ses criteres de succes 3, 4 et 5 et des decisions D-22 a D-27 et D-31 de `02-CONTEXT.md` :
**En tant que** lecteur qui recopie un exemple de `docs/cli.md` dans son terminal, **je veux** que cet exemple soit une commande reelle, complete et au bon ordre d'options, et qu'aucun exemple ne me fasse vider ma base locale, **afin de** pouvoir suivre la page sans risque ni surprise.

Fermer le module d'ancrage CLI : extraire les exemples marques de la page, les decouper par `shlex` et les faire accepter par `build_parser().parse_args` ; exiger un exemple par sous-commande documentee ; exiger au moins un exemple qui place l'option globale avant sa sous-commande ; exiger que la commande destructrice ne soit jamais un exemple et que son avertissement partage toujours sa ligne ; rendre verifiable, sur le texte du module, qu'aucun test n'execute rien et n'ouvre ni base ni socket.

Purpose : c'est CLI-03 et les criteres de succes 4 et 5 de la phase. La recherche a mesure quatre pieges que ce plan doit eviter par construction : un chemin Windows non guillemete est avale par `shlex` sans que le parseur proteste (le controle serait aveugle) ; un commentaire en fin de ligne devient des arguments et sort en code 2 ; un antislash de continuation leve `ValueError` ; `*` et `$` ne sont pas etendus mais sont acceptes, donc les montrer enseignerait un comportement que le lecteur n'obtiendra pas. Les regles destructrice et d'exemple doivent aussi etre evaluees sur le texte entier, jamais par section (lecon WR-02).

Output : `tests/conftest.py` (projection des blocs marques `console`), `tests/test_docs_cli.py` (cinq derniers tests de la phase, dont la garde destructrice et la propriete statique).

Ordonnancement declare : vague 3, apres 02-02 (`depends_on: 02-02`), parce que le module de test et la fixture de section existent deja et que ce plan les etend au lieu de les recreer.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-CONTEXT.md
@.planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-RESEARCH.md
@.planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-PATTERNS.md
@.planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-VALIDATION.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-REVIEW.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-REVIEW-FIX.md
@docs/cli.md
@dofus_stuff/cli.py
@tests/conftest.py
@tests/test_docs_cli.py
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : exemples marques analysables, couverture par sous-commande, ordre global avant sous-commande</name>
  <files>tests/conftest.py, tests/test_docs_cli.py</files>
  <read_first>
    tests/conftest.py
    tests/test_docs_cli.py
    docs/cli.md
    dofus_stuff/cli.py
    .planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-RESEARCH.md
  </read_first>
  <action>Etendre le scanner partage et ajouter les trois tests d'exemples au module d'ancrage CLI.

Dans `tests/conftest.py` : ajouter la constante `BALISE_EXEMPLE` valant `console`, puis `_lignes_exemple(texte) -> list[str]` — lignes non vides, debarrassees de leurs espaces de bord, des seuls blocs de code dont la balise d'ouverture est `BALISE_EXEMPLE` — et la fixture de portee session `lignes_exemple` qui l'expose. `_blocs_de_code` et `_lignes_de_code` ne changent pas : la projection marquee est une vue de plus sur le meme scanner unique (D-12), pas un second scanner. Comme `section` et `normalize` en 02-02, la fixture **expose le helper** et ne rend pas directement des lignes : chaque test appelle `lignes_exemple(_texte_page(docs_dir))`. Mesure : iterer la fixture elle-meme leve `TypeError: 'function' object is not iterable` — le contrat est celui des helpers partages, et il est ecrit ici pour qu'il ne soit pas redecouvert a l'execution.

Dans `tests/test_docs_cli.py` :

1. `test_exemples_marques_sont_analysables(docs_dir, lignes_exemple)` : pour chaque ligne de `lignes_exemple(texte)`, dans l'ordre ; (a) garde-fou non vide — si la page ne contient aucun bloc marque, l'echec nomme la page, le marqueur attendu et `SOURCE_CLI`, car une regle sans objet est infalsifiable (le message contient « aucun bloc marque ») ; (b) la ligne existe bien dans le texte de la page (`ligne in texte`, sans normalisation : D-11 ne s'applique pas a une commande, les accents et les espaces se comparent tels quels) ; (c) `shlex.split(ligne)` reussit, un `ValueError` etant converti en `AssertionError` citant la page, la ligne fautive et la cause habituelle — le message contient « guillemet » (guillemet non ferme ou continuation par antislash, deux formes qui n'ont pas leur place dans un exemple) ; (d) les deux premiers jetons sont exactement `python` et `fetcher.py` (test de prefixe, plus robuste que `argv.index("fetcher.py")` qui casserait si une valeur valait `fetcher.py`) ; (e) `build_parser().parse_args(argv[2:])` est accepte, un `SystemExit` etant converti en `AssertionError` citant la page, la ligne fautive et `SOURCE_CLI::build_parser().parse_args()`. Les messages rappellent les quatre regles de redaction mesurees : pas de commentaire en fin de ligne, pas de continuation, pas de metacaractere de shell, chemin portable ou guillemete pour `--data-dir`.

2. `test_chaque_sous_commande_a_un_exemple(docs_dir, lignes_exemple)` : calculer l'ensemble des sous-commandes atteintes par `lignes_exemple(texte)` — pour chaque exemple, prendre ses jetons a partir du troisieme, retenir le premier jeton appartenant a l'ensemble des huit noms epingles de `SONDES_SOUS_COMMANDES`, et ignorer un exemple qui n'en contient aucun ; exiger que cet ensemble soit exactement les huit noms, en nommant dans le message les sous-commandes sans exemple — le message contient « sans exemple » — et celles qui seraient atteintes sans etre documentees. C'est la partie falsifiable de la presence : supprimer le seul exemple d'une sous-commande fait echouer ce test.

3. `test_exemple_hors_ligne_avec_option_globale_avant_sous_commande(docs_dir, lignes_exemple)` : exiger qu'au moins un exemple de `lignes_exemple(texte)` porte les deux jetons `--offline` puis `optimize` a la suite, apres `python fetcher.py` — c'est la forme mesuree comme correcte (`db status --offline` sort en code 2, `fetcher.py: error: unrecognized arguments: --offline`). Le message nomme la page, la forme attendue « --offline optimize » et le fait que l'option globale doit preceder la sous-commande (critere de succes 4a, D-27) ; une page qui deplacerait l'option apres la sous-commande fait donc echouer deux assertions (celle-ci et l'analyse par le parseur).

Les messages de ces trois tests sont ce que la batterie de mutations du `<verify>` cherche : chacun porte la page (`cli.md`), la ligne fautive et le fichier de code, et les fragments « aucun bloc marque », « guillemet », « sans exemple », « --offline optimize » sont les motifs de la batterie (meme mecanisme que la direction page -> parseur du plan 02-02).

Ecrire dans le module, en commentaire, la raison de la forme retenue : la ligne est extraite de la page, donc l'assertion « verbatim » de CLI-03 est une garantie de construction et non une exigence de validation (limite D-26) ; et les commandes qui vident ou reecrivent la base n'apparaissent dans aucun bloc marque par decision de redaction de la page, ce que la tache 2 verifie.</action>
  <acceptance_criteria>
    - `tests/conftest.py` expose `BALISE_EXEMPLE`, `_lignes_exemple` et la fixture `lignes_exemple`, sans second scanner de blocs.
    - Chaque ligne d'un bloc marque `console` de la page est decoupee par `shlex.split` et acceptee par `build_parser().parse_args` ; un exemple fautif produit une assertion citant la page, la ligne et `dofus_stuff/cli.py`.
    - L'ensemble des sous-commandes atteintes par les exemples est exactement les huit noms epingles, et un manque est nomme (« sans exemple »).
    - Au moins un exemple marque porte `--offline` avant `optimize`, la forme attendue etant portee par la constante de module `FORME_ATTENDUE` (le message l'interpole : le litteral « --offline optimize » n'apparait pas dans le corps du test, pour que la batterie de mutations ne le trouve pas dans une ligne source citee par pytest).
    - Les cinq mutations de la batterie du `<verify>` sont detectees : marqueur de bloc d'exemple retire, jeton d'option renomme dans un exemple, guillemet non ferme, seul exemple d'une sous-commande supprime, ordre global/sous-commande inverse.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_cli.py -q` est vert.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_cli.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; MUT() { lab="$1"; motif="$2"; mut="$3"; T="$(mktemp -d)"; cp -r docs tests "$T"/; ( cd "$T" && eval "$mut" ); out="$( cd "$T" && "$PY" -m pytest tests/test_docs_cli.py -q 2>&1 )"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qE 'AssertionError: [^:]*cli[.]md' && printf '%s' "$out" | grep -qF -- "$motif"; then echo "mutation detectee : $lab"; else echo "MUTATION NON DETECTEE : $lab"; printf '%s\n' "$out" | tail -30; exit 1; fi; }; MUT "blocs d'exemple renommes (marqueur console retire)" "aucun bloc marque" "sed -i 's/^...console/\x60\x60\x60bash/' docs/cli.md"; MUT "jeton d'option renomme dans un exemple (--page -> --pages)" "--pages" "sed -i 's|--offline list --page 2|--offline list --pages 2|' docs/cli.md"; MUT "guillemet non ferme dans un exemple" "guillemet" "sed -i 's|--offline search Atcham|--offline search \"Atcham|' docs/cli.md"; MUT "seul exemple d'une sous-commande supprime (list)" "sans exemple" "sed -i '/--offline list/d' docs/cli.md"; MUT "ordre global/sous-commande inverse dans le seul temoin" "--offline optimize" "sed -i 's|--offline optimize --demo|optimize --offline --demo|' docs/cli.md"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE : ..." nommant la mutation dont la derive n'a pas ete vue</fails_when>
  </verify>
  <done>Les exemples marques de la page sont prouves analysables, chaque sous-commande documentee a son exemple, et l'ordre reel des options est enseigne et verifie.</done>
</task>

<task type="auto">
  <name>Tache 2 : garde de la commande destructrice et propriete statique sans execution</name>
  <files>tests/test_docs_cli.py</files>
  <read_first>
    tests/test_docs_cli.py
    tests/conftest.py
    docs/cli.md
    dofus_stuff/cli.py
    dofus_stuff/database.py
    .planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-RESEARCH.md
    .planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-CONTEXT.md
  </read_first>
  <action>Ajouter les deux derniers tests de la phase au module d'ancrage CLI.

1. `test_commande_destructrice_avertie_et_jamais_dans_un_exemple(docs_dir, lignes_exemple, normalize)`. Constantes de module : `JETON_DESTRUCTEUR` — alternance `db` ou `cache`, suivie d'espaces quelconques, puis du mot `clear`, delimitee sur les mots (le motif doit couvrir `cache clear`, sinon la regle laisserait passer exactement la meme destruction sous l'autre nom : lecon WR-01) — et `JETON_AVERTISSEMENT` valant `destruct`, dont la forme normalisee couvre « destructif », « destructrice » et « destruction ». Les trois constats portent sur le **texte entier** de la page, ligne a ligne, jamais par section (l'en-tete de page echapperait a une regle evaluee par section : lecon WR-02), et ils sont **accumules dans une liste** puis joints a **un seul `assert`** — une assertion par constat rendrait inatteignable un motif attribue par `## Mutation coverage` :
   (a) presence : au moins une ligne cite le jeton destructeur, sinon le constat explique que la regle suivante serait vide, donc infalsifiable, et nomme la page — le message contient « infalsifiable » ;
   (b) co-presence : toute ligne qui cite le jeton destructeur contient aussi le jeton d'avertissement, la comparaison se faisant sur la ligne normalisee (D-11) ; le constat nomme le numero de ligne, la ligne fautive et l'avertissement attendu ;
   (c) jamais un exemple : aucune ligne rendue par la fixture `lignes_exemple` (fixture qui expose le helper : l'appel est `lignes_exemple(texte)`) ne cite le jeton destructeur ; le constat nomme la ligne fautive — donc contient `db clear` ou `cache clear` — et rappelle que vider la base ne doit jamais etre presentee comme une commande a recopier (D-22) — le message contient « recopier ».

   **Pourquoi l'assertion unique est necessaire, et non cosmetique (mesure).** La mutation « mention destructrice glissee dans un bloc d'exemple » insere `python fetcher.py --offline cache clear` dans un bloc marque : cette ligne declenche **a la fois** (b) (elle cite la commande sans avertissement) et (c) (elle est un exemple a recopier). Avec une assertion par constat, (b) leve en premier et le test s'arrete la : le motif `recopier` n'apparait jamais dans la sortie et la mutation etiquetee est rapportee `MUTATION NON DETECTEE`, le `<verify>` sortant en code 1 sur une implementation **correcte**. Avec les constats accumules, le message unique porte l'avertissement attendu **et** « recopier » : la sortie contient les deux, et la mutation est vue. Mesure sur un harnais fidele de l'ordre « (a) puis (b) puis (c) » : assertion par constat = `MUTATION NON DETECTEE` pour cette mutation ; constats accumules = mutation detectee, et la page correcte reste verte.

   Ecrire dans le test, en commentaire, les deux limites mesurees : « hors parcours recommande » n'est pas decidable en general, les assertions (b) et (c) l'approchent seulement — une phrase qui encadrerait la commande d'un numero d'etape et de son avertissement passerait ; et le caractere destructeur est un classement humain derive du code (`dofus_stuff/database.py`, `Database.clear` supprime `items` et `meta` sans confirmation), pas une propriete declaree par le parseur. Ecrire aussi que la commande est **parsee** pour prouver que la page cite une commande reelle, et **jamais executee** : cette distinction ne doit pas etre perdue par un lecteur ulterieur du harnais (D-22, garde-fous de 4.5 de `.claude/CLAUDE.md`).

2. `test_sans_execution_ni_base_locale()` : verifier, sur le texte du module de test lui-meme (`Path(__file__).read_text(encoding="utf-8")`), la propriete que sa docstring declare. Utiliser le module standard `ast` (aucune dependance nouvelle) pour ne pas se heurter a ses propres chaines litterales : analyser l'arbre du module et exiger (a) que les modules importes ne contiennent aucun nom commencant par `dofus_stuff` autre que `dofus_stuff.cli` ; (b) qu'aucun import de `subprocess`, `socket` ni `sqlite3` n'existe ; (c) qu'aucun appel de fonction nomme `main` n'existe ; (d) que l'import `build_parser` depuis `dofus_stuff.cli` est bien present. Un echec nomme la page concernee (`cli.md`), la propriete attendue (ancrage par le parseur seul, aucune execution) et `SOURCE_CLI`, **plus le nom du module fautif** (`dofus_stuff.database`, `subprocess`, `socket`, `sqlite3`) : c'est ce nom que la mutation d'injection d'import du `<verify>` cherche dans la sortie. Ecrire en commentaire pourquoi la propriete est verifiee par `ast` et non par une recherche de chaines : le test cite lui-meme les noms interdits, une recherche textuelle se detecterait elle-meme.

Fin de tache : les deux tests doivent etre verts, la suite complete doit rester verte, et la morsure des quatre assertions de la tache doit etre prouvee par la **batterie de mutations** du `<verify>` (mutation du mot d'avertissement, mention destructrice glissee dans un bloc d'exemple, mention destructrice supprimee, import de `dofus_stuff.database` injecte dans la copie du module), chacune etiquetee et exécutée dans une copie jetable. Le tableau `## Mutation coverage` de ce plan rattache chaque assertion de la phase a sa mutation ou a sa raison de non-mutabilite, et le resultat de la batterie est consigne dans `02-03-SUMMARY.md`.</action>
  <acceptance_criteria>
    - `test_commande_destructrice_avertie_et_jamais_dans_un_exemple` porte ses trois constats : presence du jeton destructeur dans la page (message contenant « infalsifiable »), co-presence de l'avertissement sur chacune des lignes concernees, absence du jeton dans tout exemple marque (message contenant « recopier ») ; le motif couvre `db clear` et `cache clear` avec des espaces quelconques. Les trois constats sont **accumules et joints a une seule assertion** (les regles (b) et (c) portent souvent sur la meme ligne), de sorte que le motif `recopier` reste atteignable meme quand la co-presence manque aussi.
    - Les limites mesurees (hors parcours recommande non decidable, classement destructeur derive du code, commande parsee jamais executee) sont ecrites dans le module.
    - `test_sans_execution_ni_base_locale` verifie par `ast` que le module n'importe du produit que `dofus_stuff.cli`, n'importe ni `subprocess` ni `socket` ni `sqlite3` et n'appelle pas `main`, en nommant le module fautif dans son message.
    - Les quatre mutations de la batterie du `<verify>` sont detectees : avertissement retire, mention destructrice glissee dans un bloc d'exemple, mention destructrice supprimee de la page, import de `dofus_stuff.database` injecte dans la copie du module de test.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, `docs/` du depot reste intact (les mutations vivent dans des copies jetables), et `git status --porcelain -- docs tests` ne montre que les fichiers de cette phase.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_cli.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; MUT() { lab="$1"; motif="$2"; mut="$3"; T="$(mktemp -d)"; cp -r docs tests "$T"/; ( cd "$T" && eval "$mut" ); out="$( cd "$T" && "$PY" -m pytest tests/test_docs_cli.py -q 2>&1 )"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qE 'AssertionError: [^:]*cli[.]md' && printf '%s' "$out" | grep -qF -- "$motif"; then echo "mutation detectee : $lab"; else echo "MUTATION NON DETECTEE : $lab"; printf '%s\n' "$out" | tail -30; exit 1; fi; }; MUT "avertissement destructeur retire de la ligne qui cite la commande" "destruct" "sed -i -E '/(db|cache) +clear/ s/destruct[a-z]*//g' docs/cli.md"; MUT "mention destructrice glissee dans un bloc d'exemple" "recopier" "sed -i '/^...console/a python fetcher.py --offline cache clear' docs/cli.md"; MUT "mention destructrice supprimee de la page entiere" "infalsifiable" "sed -i -E '/(db|cache) +clear/d' docs/cli.md"; MUT "import de dofus_stuff.database injecte dans la copie du module de test" "dofus_stuff.database" "printf '\nfrom dofus_stuff.database import Database\n' >> tests/test_docs_cli.py"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE : ..." nommant la mutation dont la derive n'a pas ete vue</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>La commande destructrice est citee avec son avertissement sur la meme ligne et jamais comme exemple, et la propriete « aucun test n'execute rien » est verifiee sur le module lui-meme.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> lecteur | Un exemple marque est une commande que le lecteur recopie : s'il est faux, incomplet ou destructeur, le degat est immediat sur sa machine. |
| Page -> base locale `.data/dofus.sqlite3` | La page nomme la commande qui vide la base ; cette commande ne doit jamais apparaitre comme une commande a recopier, et aucun test ne doit l'executer. |
| Test -> produit | L'ancrage n'utilise que le parseur public ; toute execution ou toute introspection privee franchirait la frontiere interdite (D-14, D-15). |
| Harnais -> arbre de documentation livre | Les derives de preuve de morsure vivent dans une copie jetable ; l'arbre livre doit rester intact pour que la suite garde sa valeur de signal (lecon WR-05). |
| Texte de page -> controle | Une regle evaluee par section laisserait echapper l'entete de page : la garde destructrice et la regle d'exemple portent sur le texte entier, ligne a ligne (lecon WR-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering / Denial of Service (perte de la base locale) | `test_commande_destructrice_avertie_et_jamais_dans_un_exemple` | high | mitigate | Trois assertions : la page doit citer la commande (sinon la regle est vide), chaque ligne qui la cite doit porter l'avertissement sur la meme ligne, et aucun bloc marque ne peut la contenir. Le motif couvre `db clear` et `cache clear`. |
| T-02-12 | Tampering (exemple faux, controle aveugle) | `test_exemples_marques_sont_analysables` | high | mitigate | Chaque exemple est decoupe par `shlex.split` puis accepte par `build_parser().parse_args` ; les quatre regles de redaction mesurees (pas de commentaire, pas de continuation, pas de metacaractere, chemin portable ou guillemete) sont rappelees dans les messages d'echec, car `shlex` avale silencieusement un chemin Windows non guillemete et le parseur l'accepte. |
| T-02-13 | Tampering (execution non voulue par le harnais) | module `tests/test_docs_cli.py` | high | mitigate | `test_sans_execution_ni_base_locale` verifie par `ast` que le module n'importe du produit que `dofus_stuff.cli`, n'importe ni `subprocess` ni `socket` ni `sqlite3` et n'appelle pas `main` ; `shlex` decoupe sans executer and `parse_args` ne construit rien. |
| T-02-14 | Repudiation (regle infalsifiable) | les trois tests d'exemples et la garde destructrice | medium | mitigate | Chaque test porte une garde « non vide » (au moins un bloc marque, au moins un exemple par sous-commande, au moins une ligne citant la commande destructrice, au moins un exemple portant l'ordre attendu) et chaque assertion introduite par la phase est prouvee mordante par une mutation etiquetee en copie jetable : 2 en 02-02 tache 1, 8 en 02-02 tache 2, 5 en 02-03 tache 1 et 4 en 02-03 tache 2. Le tableau `## Mutation coverage` de ce plan est le registre complet de la phase : chaque assertion y est rattachee a sa mutation, ou declaree non isolable avec sa raison mesuree. Aucune assertion n'est couverte par la seule affirmation qu'elle existe. |
| T-02-15 | Denial of Service (quota Dofusdude) | exemples de la page | medium | mitigate | Aucune ligne `db sync` ni `cache fill` dans un bloc marque : les commandes qui reecrivent la base et exigent le reseau ne sont jamais des exemples a recopier, et la page documente l'incompatibilite avec `--offline`. |
| T-02-SC | Tampering | installs npm/pip/cargo | low | accept | Aucune installation dans cette phase : le module n'ajoute que `ast`, `shlex` et `re` de la bibliotheque standard, plus `pytest` deja present dans `.venv`. |
</threat_model>

<verification>
- `./.venv/Scripts/python.exe -m pytest tests/test_docs_cli.py -q` apres chaque tache, puis `./.venv/Scripts/python.exe -m pytest -q` pour la suite complete, avec le compteur re-mesure au moment ou il est cite (Pitfall 17 : ne jamais recopier le 158 passed d'avant la phase).
- Preuves de morsure, dans des copies jetables uniquement (jamais sous `docs/` du depot) : les cinq mutations etiquetees de la tache 1 (marqueur `console` retire, jeton d'option renomme dans un exemple, guillemet non ferme, seul exemple de `list` supprime, ordre global/sous-commande inverse) et les quatre de la tache 2 (avertissement retire, mention destructrice glissee dans un bloc d'exemple, mention destructrice supprimee, import de `dofus_stuff.database` injecte dans la copie du module). Chaque assertion de la phase est rattachee a une mutation ou declaree non isolable, avec sa raison mesuree, dans le tableau `## Mutation coverage` de ce plan ; les batteries de 02-02 couvrent le reste de la phase.
- `git status --porcelain -- docs tests` : seuls `docs/cli.md`, `docs/sommaire.md`, `tests/conftest.py`, `tests/test_docs_code_anchor.py` et `tests/test_docs_cli.py` apparaissent pour l'ensemble de la phase ; `dofus_stuff/**`, `README.md`, `GUIDE_WIZARD.md` et `.data/` sont intacts.
- Horodatage de `.data/dofus.sqlite3` inchange avant et apres la suite complete : aucun test de la phase n'ouvre la base locale, n'ecrit sous `.data/`, ni n'ouvre de connexion reseau (D-15).
</verification>

<success_criteria>
- CLI-03 est vrai : chaque exemple marque de `docs/cli.md` est decoupe par `shlex` et accepte par le parseur reel, chaque sous-commande documentee a son exemple, et au moins un exemple enseigne l'ordre global-avant-sous-commande.
- Le critere de succes 4b est vrai : la commande destructrice n'apparait que sur des lignes qui portent son avertissement, jamais dans un exemple marque (limite « hors parcours recommande » ecrite dans le module).
- Le critere de succes 5 est vrai et verifiable : la suite est verte avec l'interpreteur epingle, aucun test n'appelle `main()`, aucun n'ecrit sous `.data/`, aucun n'ouvre de socket.
- La limite honnete de D-26 est visible dans le module de test, pas seulement dans un plan.
- Chaque assertion introduite par la phase porte une preuve planifiee qu'elle peut echouer — une mutation etiquetee des batteries des `<verify>` (2 en 02-02 tache 1, 8 en 02-02 tache 2, 5 ici en tache 1, 4 en tache 2) — ou est declaree non isolable avec sa raison mesuree dans le tableau `## Mutation coverage` ci-dessous, qui couvre la phase entiere. C'est la propriete de Nyquist de la phase (T-02-14) : aucune assertion ne reste sans preuve ni sans declaration.
- Le resultat de la batterie (mutation par mutation, avec le motif trouve dans la sortie) est consigne dans `02-03-SUMMARY.md`, et `02-VALIDATION.md` renvoie a ces tableaux : la porte de phase et `/gsd:verify-work` n'ont pas a reconstituer la couverture de leur cote.

</success_criteria>

## Mutation coverage

Registre de la phase : pour chaque assertion introduite par 02-01, 02-02 et 02-03, la mutation programmee qui doit la faire rougir, ou la raison mesuree pour laquelle elle n'est pas isolement mutable. Les tableaux detailles par tache sont dans `02-02-PLAN.md` ; celui-ci couvre le reste et fait foi pour 02-03.

Refus de reference, re-mesures pendant la planification avec l'interpreteur epingle : `['list','--pages','2']` et `['optimize','--offline','--demo']` sortent en code 2, tandis que `['--offline','list','--page','2']` et `['--offline','optimize','--demo']` sont acceptes. Les mutations de jetons (`--page` -> `--pages`) et d'ordre (`--offline optimize` -> `optimize --offline`) visent donc des etats qui ne sont pas deja verts, et l'option globale reste acceptee avant sa sous-commande — la page enseigne l'ordre reel, pas une forme invente.

Les copies jetables de cette batterie ne contiennent que `docs/` et `tests/` : le module execute contient aussi les tests de 02-02, dont le controle d'existence des chemins y est rouge par construction (l'arbre produit n'est pas copie). Aucun des motifs attribues ci-dessous n'est produit par cette rougeur parasite — elle ne nomme que `fetcher.py` et `dofus_stuff/cli.py` — de sorte que les neuf verdicts restent des verdicts sur leur mutation.

| Assertion (plan, tache) | Mutation programmee | Motif cherche |
|-------------------------|---------------------|---------------|
| Exemple analyse par `shlex` puis `build_parser().parse_args` (02-03 t1, test 1) | « jeton d'option renomme dans un exemple (`--page` -> `--pages`) » | `--pages` (ligne fautive nommee) |
| Conversion du `ValueError` de `shlex` en `AssertionError` (02-03 t1, test 1) | « guillemet non ferme dans un exemple » | `guillemet` |
| Garde « au moins un bloc marque » (02-03 t1, test 1) | « blocs d'exemple renommes (marqueur `console` retire) » | `aucun bloc marque` |
| Couverture : exactement les huit sous-commandes atteintes par un exemple (02-03 t1, test 2) | « seul exemple d'une sous-commande supprime (`list`) » | `sans exemple` |
| Ordre global avant sous-commande illustre (02-03 t1, test 3) | « ordre global/sous-commande inverse dans le seul temoin » | `--offline optimize` (forme attendue nommee) |
| Presence verbatim de la ligne dans la page (02-03 t1, test 1b) | **Declaree non falsifiable pour cette phase (D-26)** : la ligne est extraite de la page, l'assertion est tautologique — c'est une garantie de construction, ecrite telle quelle dans le module. | — |
| Commande destructrice citee par la page (02-03 t2, test (a)) | « mention destructrice supprimee de la page entiere » | `infalsifiable` |
| Avertissement sur la meme ligne (02-03 t2, test (b)) | « avertissement destructeur retire de la ligne qui cite la commande » | `destruct` |
| Commande destructrice jamais dans un exemple (02-03 t2, test (c)) | « mention destructrice glissee dans un bloc d'exemple » | `recopier` (motif atteignable parce que les constats (b) et (c) sont joints a **une seule** assertion : la ligne inseree declenche les deux, et une assertion par constat laisserait (b) lever en premier — mesure sur harnais fidele, ou cette mutation etait rapportee `MUTATION NON DETECTEE` avec une assertion par constat) |
| Le module de test n'execute rien et n'ouvre ni base ni socket (02-03 t2, test `ast`) | « import de `dofus_stuff.database` injecte dans la copie du module de test » | `dofus_stuff.database` |
| « La suite est verte » (critere de succes 5) | **Declaree non falsifiable par une derive documentaire** : c'est une condition d'execution, falsifiable seulement par une regression du produit. La partie falsifiable est l'assertion statique ci-dessus plus l'horodatage inchange de `.data/dofus.sqlite3`, consigne dans `02-03-SUMMARY.md`. | — |
| « Hors parcours recommande » (critere 4b, seconde moitie) | **Declaree non decidable mecaniquement** : (b) et (c) l'approchent ligne a ligne ; la limite est ecrite dans le module (D-26, 02-RESEARCH.md question 2). | — |
| Le caractere « destructeur » de `db clear` / `cache clear` (classement A2) | **Declaree non falsifiable** : aucun attribut du parseur ne declare cette propriete, le classement est un jugement derive du code (`dofus_stuff/database.py`), ecrit dans les `assumptions` de 02-01 et de 02-03. La mutation (b) prouve que le classement *retenu* est applique, pas qu'il est complet. | — |
| Controles de structure de la page (exhaustivite du sommaire, H1, ligne de retour, liens resolus, encodage) | **Reutilises, non introduits par la phase** : `tests/test_docs_structure.py` et sa preuve de morsure existent depuis la phase 1 ; 02-01 ne fait que livrer une page et une entree d'index conformes. Aucune assertion nouvelle a prouver ici. | — |
| Greps de redaction de 02-01 (presence des 34 jetons d'options, H1, lien vers le sommaire, au moins 9 titres de niveau 2, avertissement destructeur co-present) | **Rejoues par les mutations ci-dessus, aucun invariant propre** : chaque invariant que ces greps verifient est repris par une assertion testee — jetons epingles (mutation « jetons epingles renommes dans la page » -> `--candidats-top`), ordre et existence des sections (mutation « sous-commande renommee » -> `introuvable`), exemples (mutation « seul exemple d'une sous-commande supprime » -> `sans exemple`), avertissement destructeur (mutations de la tache 2 -> `destruct`). Ce sont des controles de contenu a la redaction, pas des assertions independantes. | — |

## Dispositions des contributions

| Contribution | Firing condition | Disposition |
|--------------|------------------|-------------|
| security | S'applique des que `security_enforcement` est actif (ASVS niveau 1, blocage sur `high`) | **S'applique.** Le bloc de menaces ci-dessus traite les menaces reelles de la phase : commande destructrice proposee comme exemple (T-02-11), exemple faux et controle aveugle par `shlex` (T-02-12), execution non voulue par le harnais (T-02-13), regle infalsifiable (T-02-14), commande reseau recopiee depuis un parcours hors-ligne (T-02-15). |
| ai-integration | Se declenche si la phase integre une API, un SDK ou un service externe (detecteur `api-coverage.cjs`) | **Ne s'applique pas.** Le detecteur a renvoye `detected: false` sur la section de phase du ROADMAP (mesure du 2026-09-11). Ce plan *parse* des lignes de commande et lit un fichier Markdown : aucun client, aucun appel sortant, aucune matrice de couverture d'API, aucun `COVERAGE.md`. |
| assumption-delta | Se declenche sur une transition singulier -> pluriel, requis -> optionnel ou derive -> choisi (`assumption-delta scan`) | **Ne s'applique pas.** Le detecteur a renvoye `detected: false` (mesure du 2026-09-11). Aucun modele d'identite n'est introduit ni generalise : la garde porte sur un classement deja exprime au plan 02-01 (assumption A2) et sur des exemples deja ecrits. |
| schema-gate | Se declenche si l'etendue de la phase cite un fichier de schema d'ORM | **Ne s'applique pas.** Aucun motif de schema d'ORM dans l'etendue de la phase (Markdown et modules pytest uniquement) ; aucune tache de push de schema, aucun push. |

## Artifacts this phase produces

Plan 02-03 (perimetre de ce plan) :

- `tests/conftest.py` — ajouts : constante `BALISE_EXEMPLE` valant `console`, helper `_lignes_exemple(texte)`, fixture de portee session `lignes_exemple` (meme `_blocs_de_code`, aucune duplication de scanner).
- `tests/test_docs_cli.py` — ajouts : constantes `JETON_DESTRUCTEUR` et `JETON_AVERTISSEMENT` ; tests `test_exemples_marques_sont_analysables(docs_dir, lignes_exemple)`, `test_chaque_sous_commande_a_un_exemple(docs_dir, lignes_exemple)`, `test_exemple_hors_ligne_avec_option_globale_avant_sous_commande(docs_dir, lignes_exemple)`, `test_commande_destructrice_avertie_et_jamais_dans_un_exemple(docs_dir, lignes_exemple, normalize)`, `test_sans_execution_ni_base_locale()` ; commentaires de limite (D-26) et de separation « parse, jamais execute ».

Etat final de la phase apres ce plan : `docs/cli.md` et son entree d'index (02-01) ; `tests/conftest.py` avec le scanner unique, les helpers de section et les deux projections de blocs (02-02, 02-03) ; `tests/test_docs_code_anchor.py` consommant les fixtures sans definition locale dupliquee (02-02) ; `tests/test_docs_cli.py` avec les dix contrats de CLI-01, CLI-02 et CLI-03.

**Planchers mesures** (`query verify.artifacts` lit ces planchers sur l'arbre : ils ne peuvent passer qu'apres execution, une fois les fichiers ecrits) : `tests/conftest.py` atteint environ 225 lignes apres 02-02 (mesure de depart : 136 lignes — `wc -l` sur l'arbre actuel, plus la derniere ligne sans saut — plus 41 lignes deplacees depuis `tests/test_docs_code_anchor.py` et environ 50 lignes de scanner et de fixtures) ; `BALISE_EXEMPLE`, `_lignes_exemple` et la fixture `lignes_exemple` en ajoutent une trentaine, soit environ 255 apres ce plan, au-dessus du plancher de 175. `tests/test_docs_cli.py` passe d'environ 210 lignes (projection du plan 02-02 : trois listes epinglees de 42 sondes, cinq helpers, cinq tests et les commentaires de limite) a environ 400 avec les cinq tests d'exemples, la garde destructrice, les assertions statiques et les commentaires de limite, au-dessus du plancher de **240**. Ce plancher est volontairement **au-dessus** de la projection de 02-02 : un plancher de 200 etait deja satisfait par le module sorti de 02-02, il ne pouvait donc pas rougir sur un artefact tronque (module de 02-02 conserve et tests de 02-03 perdus), alors que 240 le peut. Il reste bien en dessous de la projection finale d'environ 400 lignes.

<output>
Create `.planning/phases/02-r-f-rence-cli-align-e-sur-le-parseur/02-03-SUMMARY.md` when done.
</output>

