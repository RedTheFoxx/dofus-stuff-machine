---
phase: 05-base-locale-hors-ligne-et-resynchronisation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - docs/base-locale.md
  - tests/test_docs_base_locale.py
autonomous: true
requirements:
  - BASE-03
estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Le premier contact cree la base s'il n'existe pas, et le fait est prouve sur les deux surfaces **hors de `.data/`** : `_print_db_status(db)` sur un `Database(data_dir=tmp_path / ...)` cree le dossier puis le fichier (mesure de la recherche : dossier `False` puis `True`, fichier `False` puis `True`), et `GET /db/status` sur une application dont `data_dir` pointe un dossier absent rend 200 et cree le fichier (`dofus_stuff/database.py:41-42`, `dofus_stuff/cli.py:335-341`, `dofus_stuff/web/routes.py:746-748`) — critere de succes 3, BASE-03."
    - "Le refus de la synchronisation hors-ligne est cite avec son message reel, et le refus precede l'appel reseau : le module lit par `ast` le litteral `Erreur : --offline incompatible avec db sync` (`dofus_stuff/cli.py:346`), exige que le bloc `if args.offline:` du `main` porte un `return 1` et qu'aucun appel a `ensure_up_to_date` ne soit descendant de ce bloc, et exige que le parseur public accepte la forme `--offline db sync` (`parse_args` rendant `offline is True` et `db_command == \"sync\"`) — `main()` n'etant jamais execute (D-15, D-78)."
    - "L'ecran web de synchronisation contacte l'API meme hors-ligne, et le fait est adosse au code sans jamais poster : le module lit par `ast` l'appel `ensure_up_to_date` de `dofus_stuff/web/routes.py` et exige que son argument nomme `offline` soit le litteral `False`, lit le litteral du corps de confirmation et le retrouve dans le corps rendu par `GET /db/sync` (apres normalisation, l'apostrophe sortant en `&#39;`), et n'emet **aucun** POST vers `/db/sync` ni `/db/clear` (D-79, D-81) — critere de succes 3, BASE-03."
    - "Chaque commande destructrice est signalee sur la meme ligne que la commande, et n'apparait dans aucun bloc d'exemple : toute ligne source portant `db clear` ou `PURGE OUI` porte aussi une marque de caractere destructeur, la page ne contient aucun bloc de commandes, et la ligne qui porte `db clear` nomme la base locale tandis que celle qui porte `PURGE OUI` nomme les sauvegardes du navigateur (D-80, Pitfall 7) — critere de succes 4, BASE-03."
    - "L'effet de chaque commande destructrice est lu dans le code sans etre execute : `ast` retrouve `DELETE FROM items` et `DELETE FROM meta` dans `Database.clear` et aucun appel de suppression n'y figure (le fichier est conserve, `dofus_stuff/database.py:146-152`), et la lecture du fichier `dofus_stuff/web/static/js/terminal.js` retrouve le traitement du libelle `PURGE OUI` qui retire la cle des sauvegardes du navigateur (D-81, A2 de la recherche)."
    - "Le perimetre du critere 4 est ecrit noir sur blanc dans le module : le controle porte sur `docs/base-locale.md` et sur le module de cette phase ; l'occurrence de `README.md:80`, seule du depot ou une commande destructrice figure dans un bloc de commandes sans avertissement, est declaree hors du mandat de reecriture de cette phase (D-87) et consignee pour la phase 6 — aucun controle ne rougit a cause d'elle (Pitfall 8, A3 de la recherche)."
    - "Chaque chemin d'ecran cite par la page est declare dans `dofus_stuff/web/routes.py` : le module extrait par `ast` les chemins des decorateurs `get`/`post` et exige que tout jeton entre accents graves commencant par une barre oblique soit un chemin declare (D-76) — critere de succes 5, BASE-03."
    - statement: "La page ne presente jamais l'une des deux commandes destructrices comme l'etape d'un parcours ni ne l'invite a etre suivie : le controle automatique prouve l'absence de bloc d'exemple, l'avertissement sur la meme ligne et la cible reellement detruite ; l'appreciation « aucune invitation » reste une backstop explicite."
      verification: backstop
    - statement: "L'execution du JavaScript de `PURGE OUI` dans un navigateur reste non observable en processus : le controle s'arrete au libelle rendu par l'ecran des sauvegardes et a la lecture de `dofus_stuff/web/static/js/terminal.js`. Backstop explicite (A2, D-85)."
      verification: backstop
  artifacts:
    - path: "docs/base-locale.md"
      provides: "Les trois sections des cas non evidents (creation de la base par le premier contact, refus de la synchronisation hors-ligne, ecran de synchronisation du web qui contacte l'API) et la section des commandes destructrices, inserees avant le bloc Source de verite"
      contains: "## Source de vérité"
      min_lines: 90
    - path: "tests/test_docs_base_locale.py"
      provides: "Le controle de la creation du fichier sur les deux surfaces, le controle du refus hors-ligne, le controle de la synchronisation web, le controle des commandes destructrices, le lecteur `_routes_declarees` et les motifs de morsures associes"
      contains: "def test_commandes_destructrices"
      min_lines: 420
  key_links:
    - from: "tests/test_docs_base_locale.py"
      to: "dofus_stuff/cli.py"
      via: "lecture par `ast` du litteral de refus et de la structure du `main` (bloc `if args.offline:` portant `return 1` avant tout appel a `ensure_up_to_date`), plus la sonde publique `build_parser().parse_args` — `main()` jamais execute"
      pattern: "incompatible avec db sync"
    - from: "tests/test_docs_base_locale.py"
      to: "dofus_stuff/web/routes.py"
      via: "lecture par `ast` des chemins de routes et de l'appel `ensure_up_to_date` dont l'argument nomme `offline` vaut `False`, et rendu de `GET /db/sync` et `GET /saves` par le client de test"
      pattern: "ensure_up_to_date"
    - from: "tests/test_docs_base_locale.py"
      to: "dofus_stuff/database.py"
      via: "lecture par `ast` du corps de `Database.clear` (`DELETE FROM items`, `DELETE FROM meta`, aucun appel de suppression) et construction d'une base temporaire par `Database(data_dir=tmp_path / ...)`"
      pattern: "DELETE FROM items"
    - from: "tests/test_docs_base_locale.py"
      to: "dofus_stuff/web/static/js/terminal.js"
      via: "lecture du fichier pour retrouver le traitement du libelle `PURGE OUI` et le retrait de la cle des sauvegardes du navigateur"
      pattern: "PURGE OUI"
  prohibitions:
    - statement: "Ne jamais executer une commande destructrice, ni le POST de confirmation de l'ecran de vidage, ni une synchronisation : elles sont documentees pour etre reconnues, jamais lancees (D-80, D-81)."
      status: resolved
      verification: test
    - statement: "Ne jamais faire rougir un controle a cause de `README.md`, que cette phase n'a pas mandat de reecrire : le perimetre du critere 4 est ecrit dans le module (D-87, Pitfall 8)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais modifier `dofus_stuff/**` pour faire passer la page : le refus, l'appel reseau et l'effet destructeur sont lus, pas changes (D-88)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais epingler une valeur volatile dans la prose de la page, ni recopier une aide de parseur avec ses coupures de ligne (D-73, D-75, Pitfall 5)."
      status: resolved
      verification: test
    - statement: "Ne jamais priver une commande destructrice de son avertissement en la deplacant : si l'avertissement est sur une ligne et la commande sur une autre, le controle echoue, et la correction consiste a replacer l'avertissement sur la ligne de la commande, jamais a retirer le controle."
      status: resolved
      verification: test
    - statement: "Ne jamais publier, deployer a distance, pousser vers un depot distant ni effectuer d'achat : commits locaux seulement, indexation par chemin explicite (D-91)."
      status: resolved
      verification: judgment
  assumptions:
    - "**Sonde d'arete spec-less : l'orchestrateur n'a PAS execute la sonde deterministe de la section A** (aucun SPEC de phase). Les aretes de ce plan sont derivees par le planificateur du critere de succes 3 (`db status` cree le fichier, `db sync` refuse `--offline`, l'ecran web de synchronisation contacte l'API) et du critere 4 (commandes destructrices signalees sur la meme ligne, dans aucun parcours), enrichis de l'inventaire mesure de `05-RESEARCH.md` § 9, § 10 et de l'inventaire des occurrences destructrices de la documentation livree. Deux verifications sont ecrites en `backstop` ; les six prohibitions retenues sont au bloc `prohibitions`, aucune n'est ecartee en silence."
    - "Le message de refus de `db sync` en mode hors-ligne est deja cite deux fois par `docs/cli.md` (`:26-29` et `:168-171`) : la nouvelle page le cite une troisieme fois parce que **D-78 l'exige** (« son message reel est cite depuis le code, jamais paraphrase »). Cette duplication est **prescrite par une decision verrouillee** et enregistree comme telle ; le detail de la sous-commande reste chez `docs/cli.md`, qui en est le proprietaire (D-17, item 5 de la table des doublons de la recherche)."
    - "Le controle de la creation du fichier ouvre un `Database` sous `tmp_path`, ferme dans un `finally` : mesure de la recherche, `_print_db_status` laisse la connexion ouverte et le nettoyage du dossier temporaire echoue sur Windows (`PermissionError [WinError 32]`) sans ce `close()`."
    - "La prose libre de la page n'est pas verifiee : les controles portent sur les libelles, les messages et les structures **cites**, et ce plan ne revendique aucune exhaustivite de la redaction (D-85)."
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 5 et de ses criteres de succes 3 et 4, ainsi que des decisions D-76 a D-81 de `05-CONTEXT.md` :
**En tant que** lecteur de la page « Base locale », **je veux** connaitre les comportements qui ne se devinent pas — le premier contact qui cree le fichier, la synchronisation qui refuse le mode hors-ligne, l'ecran web qui contacte l'API malgre le mode hors-ligne — et reconnaitre immediatement les deux commandes qui detruisent des donnees, **afin de** ne pas conclure a tort que ma base est peuplee, de ne pas croire qu'une synchronisation est possible sans reseau, et de ne jamais lancer une commande destructrice par inadvertance.

Mode MVP : le `**Goal:**` du ROADMAP n'est pas redige en recit utilisateur ; le cadrage est **derive**, comme au plan 04-01 de la phase precedente, et ne reformule que des criteres et des decisions verrouillees.

Purpose : c'est **BASE-03** — « les commandes destructrices de la base sont signalees comme telles et jamais presentees comme une etape normale » — et le critere 3, qui porte sur les trois cas non evidents mesures par la recherche. C'est le plan ou la page devient utile en pratique : les comportements decrits ici sont exactement ceux qu'un lecteur deduirait faux d'une phrase generale.

Output : les quatre sections ajoutees a `docs/base-locale.md` (creation de la base, refus hors-ligne, synchronisation web, commandes destructrices) et les quatre controles correspondants dans `tests/test_docs_base_locale.py`, avec leurs morsures.

Ordonnancement declare : **vague 2**, `depends_on: [05-01]` — la page, l'index, la reserve de dette et le module doivent exister et etre verts avant d'etendre la page ; les deux plans touchent les memes deux fichiers, donc aucun parallelisme n'est possible (recouvrement de `files_modified`).

**Ce que ce plan ne fait pas** : il n'ecrit ni le controle des renvois du `README.md` (D-87), ni la mesure d'empreinte de `.data/` autour des rendus, ni le controle de cloture des octets et des valeurs volatiles de la page — c'est le plan 05-03 (critere 5). Il ne modifie aucun fichier de `dofus_stuff/**` (D-88), ne poste jamais vers `/db/sync` ni `/db/clear`, ne lance aucune synchronisation (D-89) et ne reecrit pas `README.md` (D-87).
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-CONTEXT.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-VALIDATION.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-01-PLAN.md
@.claude/CLAUDE.md
@docs/base-locale.md
@docs/cli.md
@tests/test_docs_base_locale.py
@tests/conftest.py
@dofus_stuff/database.py
@dofus_stuff/cli.py
@dofus_stuff/sync.py
@dofus_stuff/web/routes.py
@dofus_stuff/web/static/js/terminal.js
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : tranche verticale « le premier contact cree la base, sur les deux surfaces, hors de `.data/` »</name>
  <files>docs/base-locale.md, tests/test_docs_base_locale.py</files>
  <reversibility rating="reversible">Ajouter une section a une page et un controle a un module de test est annulable par un commit de retour ; aucune commande n'est executee et rien n'est ecrit hors d'un dossier temporaire.</reversibility>
  <read_first>
    docs/base-locale.md
    tests/test_docs_base_locale.py
    tests/conftest.py
    dofus_stuff/database.py
    dofus_stuff/cli.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/templates/screen.html
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
  </read_first>
  <action>Ecrire la section du premier cas non evident et son controle de bout en bout, en traversant les deux surfaces et la seule couche qui les rend observables : un dossier temporaire.

**Page** — une section de niveau 2, inseree **juste avant** `## Source de vérité` (la ligne de retour reste la derniere ligne non vide) : `## Le premier contact crée la base`. La section dit que **consulter l'etat de la base cree la base si elle n'existe pas** — le dossier puis le fichier —, que c'est vrai des deux surfaces (la commande hors-ligne `python fetcher.py --offline db status` et l'ecran d'etat de l'interface web, au chemin `/db/status`), et que la consequence pratique est qu'un etat affiche ne prouve pas que la base est peuplee : une base fraichement creee affiche `Entrées : 0` cote ligne de commande et `ENTREES : 0` cote web, sans aucune categorie ; la section renvoie par lien a [la page CLI](cli.md) pour la surface des commandes. Aucun chemin absolu de poste, aucun nombre d'objets, aucune valeur volatile.

**Module** :
- ajouter le lecteur `_routes_declarees()` : il lit `dofus_stuff/web/routes.py` par `ast` et rend l'ensemble des chemins declares par les decorateurs — tout appel dont la fonction est un attribut nomme `get` ou `post` et dont le premier argument est un litteral de chaine. Aucune introspection privee, aucun balayage par expression reguliere du source ;
- ajouter les constantes de motif `MOTIF_CREATION = "creation de la base par le premier contact"` et `MOTIF_ROUTE = "chemin d ecran cite par la page"` (valeurs ASCII, sans apostrophe, chacune incluse dans le constat qui la concerne) ;
- test `test_le_premier_contact_cree_la_base(docs_dir, section, app, client, catalog, normalize, tmp_path)`, la tranche de bout en bout :
  - **regle de robustesse de l'oracle** : les deux appels d'oracle sont **proteges** — un echec de l'appel doit devenir **un constat**, jamais une exception qui remonte. Sans cette protection, une derive du code produit une erreur au lieu d'un constat, et la morsure `creation_retiree` rapporterait `MUTATION NON DETECTEE` sur un module correct ;
  - surface **ligne de commande** : `dossier = tmp_path / "absent_cli"` ; exiger que le dossier n'existe pas avant l'appel ; `db = Database(data_dir=dossier)` ; appeler `_print_db_status(db)` **dans un `try`**, sortie capturee sous `redirect_stdout(io.StringIO())` — mesure de la recherche : quand le dossier n'est plus cree, `sqlite3.connect` echoue et l'appel **leve** (`OperationalError: unable to open database file`) sans rien creer ; toute exception est donc convertie en **constat portant `MOTIF_CREATION`** qui nomme la surface ligne de commande, l'exception et `SOURCE_DATABASE`, et qui rejoint la liste des constats ; exiger ensuite que le dossier **et** le fichier `dossier / DB_NAME` existent apres l'appel, et que la sortie porte `Version jeu : (aucune)`, `Dernier check : (aucun)` et `Entrées : 0` ; fermer la base dans un `finally` (`db.close()`), sans quoi le nettoyage du dossier temporaire echoue sur Windows (`PermissionError [WinError 32]`, mesure de la recherche) ;
  - surface **web** : `dossier_web = tmp_path / "absent_web"` ; exiger que le dossier n'existe pas avant ; construire `application = create_app(data_dir=dossier_web, offline=True, catalog=catalog, load_catalog=False)` avec `application.config["TESTING"] = True` ; appeler `application.test_client().get("/db/status")` **dans un `try`** — la route ouvre la base par `Database(data_dir=...)` puis `db.open()` (`dofus_stuff/web/routes.py:746-748`) : quand la creation du dossier est retiree, l'erreur **remonte** sous `TESTING = True` au lieu de devenir une reponse ; toute exception est donc convertie en **constat portant `MOTIF_CREATION`** nommant la surface web, l'exception et `SOURCE_ROUTES` ; hors exception, exiger que la reponse rende **200** et que `dossier_web / DB_NAME` existe apres l'appel ; exiger que le corps rendu, extrait par `_lignes_du_corps`, porte le libelle `FICHIER :` — l'ecran d'etat a donc bien ete rendu ;
  - **page** : la section doit nommer les deux surfaces (`db status` et le chemin `/db/status`), porter une marque de creation (au moins une, normalisee, de `cree`, `creation`, `cree le fichier`, `cree la base`) et porter `--offline` ;
  - **chemins d'ecran** : extraire de la page tout jeton entre accents graves commencant par une barre oblique et exiger que chacun soit un chemin declare de `routes.py` (`_routes_declarees()`), constat portant `MOTIF_ROUTE` ; exiger que `/db/status` soit cite ;
  - chaque constat porte le motif de son controle et nomme la page ou la surface, la valeur fautive, la valeur attendue et le fichier producteur (`SOURCE_CLI`, `SOURCE_ROUTES`, `SOURCE_DATABASE`) ; les constats sont accumules et joints a **une seule** assertion ;
- `TITRES_SECTION_ATTENDUS` gagne `## Le premier contact crée la base` **avant** `## Source de vérité`.

Le module n'ecrit rien sous `.data/` : les deux dossiers testes viennent de `tmp_path`. `DEFAULT_DATA_DIR` n'est jamais passe a `Database` ni a `create_app`.
  </action>
  <acceptance_criteria>
    - La page porte `## Le premier contact crée la base` avant `## Source de vérité`, cite `db status`, le chemin `/db/status` et l'effet de creation, et ne contient aucun nombre de quatre chiffres ou plus.
    - `test_le_premier_contact_cree_la_base` prouve la creation du dossier puis du fichier sur la surface ligne de commande, ferme la base qu'il ouvre, et prouve la creation par un `GET /db/status` rendant 200 sur un dossier absent.
    - Quand la creation du dossier est retiree du code, l'echec des deux oracles (`OperationalError` cote ligne de commande, erreur remontee par la route cote web) est converti en **constat portant `MOTIF_CREATION`** : le test ne meurt pas sur une exception et la morsure `creation_retiree` est detectee.
    - Le controle des chemins d'ecran exige que chaque jeton de la page commencant par une barre oblique soit un chemin declare dans `dofus_stuff/web/routes.py`.
    - Retirer la creation du dossier dans `Database.open`, ou citer dans la page un chemin d'ecran que `routes.py` ne declare pas, rougit le module sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q` est vert, la suite entiere reste verte, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256 autour de la suite complete.
    - La batterie mesure la copie verte sur le module seul (`tests/test_docs_base_locale.py`) avant mutation, puis la suite complete apres : la commande automatisee des morsures dure de l'ordre de 10 a 20 secondes (cout assume par D-84).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; mesure() { "$PY" -c "import hashlib,pathlib;p=pathlib.Path('.data/dofus.sqlite3');print(f'{p.stat().st_size}:{p.stat().st_mtime_ns}:{hashlib.sha256(p.read_bytes()).hexdigest()}' if p.exists() else 'base absente')"; }; mesure > "$T/avant.txt"; "$PY" -m pytest -q; statut=$?; mesure > "$T/apres.txt"; if [ "$statut" -ne 0 ]; then echo "SUITE COMPLETE ROUGE (exit $statut)"; exit 1; fi; if diff -q "$T/avant.txt" "$T/apres.txt" >/dev/null; then echo "empreinte .data/ identique autour de la suite complete : $(cat "$T/apres.txt")"; else echo "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE : avant=$(cat "$T/avant.txt") apres=$(cat "$T/apres.txt")"; exit 1; fi</automated>
    <fails_when>exit non-zero, ou la ligne "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE", ou la ligne "SUITE COMPLETE ROUGE", ou une ligne contenant "failed" ou "error"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in creation_retiree route_inventee; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in creation_retiree) sed -i 's/        self.data_dir.mkdir(parents=True, exist_ok=True)/        pass/' "$T/dofus_stuff/database.py"; motif='creation de la base par le premier contact';; route_inventee) printf '\nUn ecran absent : `/db/inexistant`.\n' >> "$T/docs/base-locale.md"; motif='chemin d ecran cite par la page';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 05-02 (TACHE 1) EN ECHEC"; exit 1; fi; echo "morsures 05-02 tache 1 : 2/2 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 05-02 (TACHE 1) EN ECHEC"</fails_when>
  </verify>
  <done>La page explique que le premier contact cree la base, nomme les deux surfaces et le chemin d'ecran, et le module le prouve sur un dossier temporaire pour la ligne de commande comme pour le web ; deux derives epinglees (creation retiree du code, chemin d'ecran invente dans la page) font rougir la suite sur une copie verte avant mutation, et l'empreinte de `.data/` est inchangee.</done>
</task>

<task type="auto">
  <name>Tache 2 : le refus de la synchronisation hors-ligne et l'ecran web qui contacte l'API malgre le mode hors-ligne</name>
  <files>docs/base-locale.md, tests/test_docs_base_locale.py</files>
  <read_first>
    docs/base-locale.md
    docs/cli.md
    tests/test_docs_base_locale.py
    dofus_stuff/cli.py
    dofus_stuff/sync.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/__main__.py
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
  </read_first>
  <action>Ecrire les deux cas ou le reseau est en jeu — un refus et une exception — et les ancrer sans jamais executer `main()` ni poster un formulaire.

**Page** — deux sections de niveau 2, inserees **juste avant** `## Source de vérité` :
- `## La synchronisation refuse le mode hors-ligne` : la synchronisation a besoin du reseau, donc l'option globale `--offline` lui est **incompatible** ; la section cite le **message reel** tel que le code le porte (entre accents graves ou dans un bloc de citation), dit que la commande sort avec un **code de retour non nul**, rappelle que l'option globale s'ecrit **avant** la sous-commande (la forme fautive place l'option apres), et renvoie a [la page CLI](cli.md), proprietaire de la surface des commandes et du detail des sous-commandes (D-17, D-78) ;
- `## L'écran de synchronisation du web contacte l'API` : c'est le **seul endroit ou le mode hors-ligne ne s'applique pas** ; l'ecran de confirmation annonce que l'operation **contacte l'API Dofusdude** et peut prendre plusieurs minutes, et confirmer lance reellement la synchronisation **meme si l'interface a demarre hors-ligne**, parce que le mode hors-ligne regle le **demarrage** (« ne pas contacter l'API au demarrage ») et non ce bouton ; la section cite la phrase du corps de confirmation telle que l'ecran la rend et dit explicitement le cas « meme hors-ligne » (D-79) ; le chemin de l'ecran est cite entre accents graves.

**Module** :
- constantes de motif `MOTIF_REFUS = "refus de la synchronisation hors-ligne"` et `MOTIF_SYNCHRO_WEB = "synchronisation web hors-ligne"` (ASCII, sans apostrophe, incluses dans leurs constats) ;
- test `test_le_refus_de_la_synchronisation(docs_dir, section, normalize)` :
  - **le message est produit par le code** : lire `dofus_stuff/cli.py` par `ast`, collecter les litteraux de chaine du module et exiger que le message de refus y figure ; le constat porte le litteral lu et `SOURCE_CLI` (mecanisme `ast`, plus faible qu'un rendu mais seule route possible : `main()` n'est jamais execute, D-15) ;
  - **le refus precede l'appel reseau** : localiser la fonction `main` dans l'arbre, y chercher un `if` dont le test est l'attribut `offline` de `args` et dont le corps contient un `return` de valeur `1` ; exiger la presence d'un tel bloc **et** qu'aucun appel a `ensure_up_to_date` ne soit descendant de ce corps — la synchronisation est inatteignable quand `--offline` est donne ; le constat nomme ce qui manque (bloc de refus absent, ou appel reseau present dans le bloc de refus) ;
  - **la forme est acceptee par le parseur public** : `parseur_cli().parse_args(["--offline", "db", "sync"])` porte `offline is True` et `db_command == "sync"` — la commande est analysee, jamais executee ;
  - **la page** cite le message de refus **verbatim** et porte une marque de code de retour non nul (au moins une, normalisee, de `code de retour 1`, `code de retour`, `code 1`) ; constat portant `MOTIF_REFUS` ;
- test `test_la_synchronisation_web_contacte_l_api(docs_dir, section, client, normalize)` :
  - lire `dofus_stuff/web/routes.py` par `ast`, localiser l'appel dont la fonction se termine par `ensure_up_to_date` et exiger qu'il porte un argument **nomme** `offline` dont la valeur est le litteral `False` ; constat portant `MOTIF_SYNCHRO_WEB` et `SOURCE_ROUTES` — c'est le fait qui prouve que le mode hors-ligne ne s'applique pas a cet ecran ;
  - lire le litteral de la phrase du corps de confirmation dans le meme fichier par `ast` et le retrouver dans le **corps rendu** de `GET /db/sync`, extrait par `_lignes_du_corps` et compare apres la fixture `normalize` (l'apostrophe sort en `&#39;` dans le HTML rendu) ; exiger aussi l'invite de confirmation rendue ;
  - exiger que la page cite la phrase du corps de confirmation (normalisee) et porte une marque du cas hors-ligne (au moins une, normalisee, de `meme hors-ligne`, `y compris hors-ligne`, `quel que soit le mode hors-ligne`) ;
  - **aucun POST n'est emis** : le module ne poste jamais vers `/db/sync` ni `/db/clear` — l'ecran se lit en `GET` seulement (D-81, Pitfall 6) ; la garde de cloture du plan 05-01 interdit deja la paire de confirmation, et ce test ne l'approche pas ;
- `TITRES_SECTION_ATTENDUS` gagne les deux nouveaux titres **avant** `## Source de vérité`.
  </action>
  <acceptance_criteria>
    - La page porte `## La synchronisation refuse le mode hors-ligne` et `## L'écran de synchronisation du web contacte l'API`, chacune avant `## Source de vérité`.
    - La section du refus cite le message reel du code et une marque de code de retour non nul ; la section de l'ecran web cite la phrase du corps de confirmation et dit le cas hors-ligne.
    - `test_le_refus_de_la_synchronisation` lit le litteral de refus par `ast`, exige le bloc `if args.offline:` portant `return 1` sans appel reseau descendant, et sonde `parse_args` sur la forme `--offline db sync`.
    - `test_la_synchronisation_web_contacte_l_api` exige que l'argument `offline` de l'appel de synchronisation soit le litteral `False` et retrouve la phrase du corps dans le rendu de `GET /db/sync` ; aucun POST n'est emis.
    - Renommer le litteral de refus, neutraliser le bloc de refus, remplacer `offline=False` par `offline=True` dans l'appel, ou remplacer la phrase du corps de confirmation, rougit le module sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
    - La batterie mesure la copie verte sur le module seul (`tests/test_docs_base_locale.py`) avant mutation, puis la suite complete apres : la commande automatisee des morsures dure de l'ordre de 20 a 30 secondes (cout assume par D-84).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in refus_message_renomme refus_desactive synchro_web_hors_ligne_retire corps_confirmation_renomme; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in refus_message_renomme) sed -i 's/Erreur : --offline incompatible avec db sync/Erreur : --offline refuse avec db sync/' "$T/dofus_stuff/cli.py"; motif='refus de la synchronisation hors-ligne';; refus_desactive) sed -i 's/                    if args.offline:/                    if False:/' "$T/dofus_stuff/cli.py"; motif='refus de la synchronisation hors-ligne';; synchro_web_hors_ligne_retire) sed -i 's/            offline=False,/            offline=True,/' "$T/dofus_stuff/web/routes.py"; motif='synchronisation web hors-ligne';; corps_confirmation_renomme) sed -i "s/CETTE OPERATION CONTACTE L'API DOFUSDUDE/CETTE OPERATION UTILISE LE RESEAU/" "$T/dofus_stuff/web/routes.py"; motif='synchronisation web hors-ligne';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 05-02 (TACHE 2) EN ECHEC"; exit 1; fi; echo "morsures 05-02 tache 2 : 4/4 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 05-02 (TACHE 2) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>Le refus de la synchronisation hors-ligne et l'appel reseau de l'ecran web sont decrits et adosses au code, l'un par le litteral et la structure du `main` (jamais execute), l'autre par l'argument `offline=False` et le corps rendu de `GET /db/sync` ; quatre derives epinglees font rougir la suite sur une copie verte avant mutation, et la suite entiere reste verte.</done>
</task>

<task type="auto">
  <name>Tache 3 : les deux commandes destructrices, signalees sur la meme ligne et hors de tout parcours</name>
  <files>docs/base-locale.md, tests/test_docs_base_locale.py</files>
  <read_first>
    docs/base-locale.md
    docs/cli.md
    tests/test_docs_base_locale.py
    dofus_stuff/database.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/static/js/terminal.js
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
  </read_first>
  <action>Ecrire la section des commandes destructrices et le controle qui les rend reconnaissables sans jamais les executer. C'est le critere 4, et c'est aussi le point ou deux commandes voisines mais differentes risquent d'etre confondues (Pitfall 7).

**Page** — une section de niveau 2, inseree **juste avant** `## Source de vérité` : `## Les commandes destructrices`. Son **titre ne nomme aucune des deux commandes** (aucun jeton destructeur dans une ligne de titre), pour que chaque occurrence d'une commande soit une ligne de prose porteuse d'avertissement :
- **une ligne** qui porte la premiere commande (et son second nom, l'alias de cache) et la qualifie de **destructrice** dans la meme ligne : elle dit qu'elle **vide entierement la base locale** et cite l'effet reel tel que le code l'ecrit — les deux instructions de suppression des tables `items` puis `meta` — en precisant que **le fichier de la base n'est pas supprime du disque** ; elle renvoie par lien a [la page CLI](cli.md), qui reste proprietaire du detail de cette sous-commande ;
- **une ligne distincte** qui porte la seconde commande et la qualifie de **destructrice** dans la meme ligne. La phrase est ecrite **exactement** ainsi, sur une seule ligne : `PURGE OUI est destructrice : elle supprime les sauvegardes du navigateur pour cette interface.` — c'est cette chaine litterale que la morsure `purge_cible_confondue` substitue (`sauvegardes du navigateur` remplace par `donnees de la base`) : la formulation ecrite dans la page et le litteral de la morsure sont **une seule et meme chaine**, sans quoi la substitution ne mute rien et la morsure resterait sterile ; elle dit ce qu'elle detruit reellement et **pas** la base locale — elle ne nomme donc la base ni comme sa cible ni comme son effet ; le mot `localStorage` n'est **pas** ecrit sur cette ligne (le module lit le fichier JavaScript pour prouver l'effet, la page dit la cible en francais) ;
- une phrase qui dit que **ces commandes ne sont l'etape d'aucun parcours de cette page** et qu'elles existent pour etre **reconnues**, pas pour etre suivies ;
- aucun bloc de commandes balise console sur la page (regle deja tenue par les pages des phases 3 et 4), aucun nombre de quatre chiffres ou plus, aucun chemin absolu de poste (D-80, D-69).

**Module** :
- constantes : `COMMANDE_DESTRUCTRICE = re.compile(r"\bdb\s+clear\b")` (le motif deja employe par `tests/test_docs_structure.py:20` et `tests/test_docs_wizard.py:186`), `JETON_PURGE = "PURGE OUI"`, `BALISE_COMMANDE` (la balise de bloc de commandes de la console, celle que `tests/test_docs_parcours.py` nomme ainsi), `MARQUES_DESTRUCTRICES = ("destruct", "detruit", "irreversible", "danger", "attention")`, `MARQUES_BASE = ("base locale", "dofus.sqlite3", "sqlite")`, `MARQUES_SAUVEGARDES = ("sauvegardes", "navigateur")`, `MARQUES_HORS_PARCOURS = ("aucun parcours", "aucune etape")`, `LIMITE_PERIMETRE` (la constante de texte qui declare le perimetre : le controle porte sur `docs/base-locale.md` et sur ce module ; l'occurrence de `README.md` ligne 80, seule du depot ou une commande destructrice figure dans un bloc de commandes sans avertissement, est hors du mandat de reecriture de cette phase (D-87 limite le travail du README a la resolution de ses renvois) et reste consignee pour la phase 6, proprietaire de la completude) ; motifs `MOTIF_DESTRUCTRICES = "commandes destructrices"`, `MOTIF_EFFET = "effet de la commande destructrice"`, `MOTIF_EXEMPLE = "bloc de commandes"` ;
- test `test_commandes_destructrices(docs_dir, section, client, normalize)` :
  - **reconnaissance ligne a ligne (D-80)** : construire la liste des lignes de la page ; pour chacun des deux jetons, collecter les lignes porteuses ; exiger qu'il y en ait **au moins une** par jeton, et que **chacune** de ces lignes porte au moins une marque de `MARQUES_DESTRUCTRICES` (normalisee) — c'est la lecture littérale de « signalee comme telle sur la meme ligne » ; un jeton present sur une ligne sans avertissement est un constat qui nomme la ligne fautive, meme si une autre ligne de la page porte l'avertissement (constat portant `MOTIF_DESTRUCTRICES`) ;
  - **cibles distinctes (Pitfall 7)** : la ou les lignes portant la premiere commande doivent porter au moins une marque de `MARQUES_BASE` ; la ou les lignes portant la seconde doivent porter au moins une marque de `MARQUES_SAUVEGARDES` (constat portant `MOTIF_DESTRUCTRICES`, le meme motif que la reconnaissance ligne a ligne : c'est ce motif que la morsure `purge_cible_confondue` cherche dans la sortie) — le controle encode la distinction mesuree par la recherche : la seconde commande detruit les sauvegardes du navigateur, pas la base SQLite ;
  - **hors de tout parcours** : exiger que la section porte au moins une marque de `MARQUES_HORS_PARCOURS` (normalisee) ; exiger que la page ne contienne **aucun** bloc de commandes (`BALISE_COMMANDE` absent du texte) et qu'aucune ligne d'exemple (`lignes_exemple(texte)`) ne porte l'un des deux jetons — les deux regles sont complementaires : la premiere dit qu'aucun exemple n'existe, la seconde resterait vraie si une page voisine en ajoutait un (constat portant `MOTIF_EXEMPLE`) ;
  - **effet de la premiere commande, lu sans etre execute** : lire `dofus_stuff/database.py` par `ast`, localiser la fonction `clear`, y exiger les deux litteraux de suppression (`DELETE FROM items`, `DELETE FROM meta`) et **aucun** appel dont le nom figure dans `APPELS_SUPPRESSION` — le fichier est vide, jamais supprime ; exiger que la page cite les deux litteraux et porte une marque disant que le fichier n'est pas supprime (au moins une, normalisee, de `le fichier n'est pas supprimé`, `fichier conservé`, `ne supprime pas le fichier`) ; constat portant `MOTIF_EFFET` ;
  - **effet de la seconde commande, lu sans etre execute** : rendre `GET /saves` et exiger que la ligne de statut (`_statut`, comparee apres normalisation) porte le libelle de la commande ; lire le texte de `dofus_stuff/web/static/js/terminal.js` et exiger que le traitement du libelle y soit encore — la comparaison du libelle dans le fichier JavaScript **et** l'appel qui retire la cle des sauvegardes du navigateur ; constat portant `MOTIF_EFFET`. Aucun navigateur n'est lance, aucun JavaScript n'est compile : la limite est declaree dans la docstring du test (D-85, A2) ;
  - **perimetre declare** : la docstring du test cite `LIMITE_PERIMETRE` et dit que `README.md` n'est pas concerne par ce controle ; aucun constat ne mentionne le README, et la suite ne peut pas rougir a cause de lui (Pitfall 8, A3) ;
  - les constats sont accumules et joints a **une seule** assertion, chacun nommant la page, la ligne ou la valeur fautive, la valeur attendue et le fichier producteur (`SOURCE_DATABASE`, `SOURCE_ROUTES`, `SOURCE_TERMINAL_JS`) ;
- `TITRES_SECTION_ATTENDUS` gagne `## Les commandes destructrices` **avant** `## Source de vérité`.
  </action>
  <acceptance_criteria>
    - La page porte `## Les commandes destructrices` avant `## Source de vérité`, et son titre ne contient aucun des deux jetons de commande.
    - Chaque ligne portant l'une des deux commandes porte une marque de caractere destructeur ; la ligne de la premiere commande nomme la base locale, celle de la seconde nomme les sauvegardes du navigateur.
    - La section dit que ces commandes ne sont l'etape d'aucun parcours, la page ne contient aucun bloc de commandes, et aucune ligne d'exemple ne porte un jeton destructeur.
    - `test_commandes_destructrices` lit par `ast` les deux litteraux de suppression de `Database.clear` sans y trouver d'appel de suppression, rend `GET /saves` pour y lire le libelle de la seconde commande, et lit `dofus_stuff/web/static/js/terminal.js` pour y retrouver son traitement.
    - `LIMITE_PERIMETRE` et la docstring du test declarent le perimetre et l'exclusion de `README.md` ; aucun constat ne mentionne le README.
    - Ajouter un bloc de commandes portant la premiere commande, ajouter une ligne portant un jeton sans avertissement, confondre la cible de la seconde commande, ou retirer un litteral de suppression du code, rougit le module sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
    - La batterie mesure la copie verte sur le module seul (`tests/test_docs_base_locale.py`) avant mutation, puis la suite complete apres : la commande automatisee des morsures dure de l'ordre de 20 a 30 secondes (cout assume par D-84).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in bloc_console_ajoute ligne_sans_avertissement_added purge_cible_confondue effet_clear_retire; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in bloc_console_ajoute) printf '\n```console\npython fetcher.py db clear\n```\n' >> "$T/docs/base-locale.md"; motif='bloc de commandes';; ligne_sans_avertissement_added) printf '\nVoir aussi db clear pour repartir de zero.\n' >> "$T/docs/base-locale.md"; motif='commandes destructrices';; purge_cible_confondue) sed -i 's/sauvegardes du navigateur/donnees de la base/' "$T/docs/base-locale.md"; motif='commandes destructrices';; effet_clear_retire) sed -i 's/        cur = conn.execute("DELETE FROM items")/        cur = conn.execute("SELECT 1")/' "$T/dofus_stuff/database.py"; motif='effet de la commande destructrice';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 05-02 (TACHE 3) EN ECHEC"; exit 1; fi; echo "morsures 05-02 tache 3 : 4/4 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 05-02 (TACHE 3) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>Les deux commandes destructrices sont presentees comme telles, chacune sur sa ligne, avec la cible qu'elle detruit reellement, hors de tout parcours et hors de tout bloc d'exemple ; l'effet de chacune est lu dans le code sans etre execute, le perimetre du controle exclut explicitement `README.md`, et quatre derives epinglees font rougir la suite sur une copie verte avant mutation.</done>
</task>

</tasks>

<!-- planner-discipline-allow: db clear -->
<!-- planner-discipline-allow: PURGE OUI -->

<threat_model>
**Surface** : deux sections et deux controles de plus dans les deux memes fichiers, plus des lectures de code par `ast` et un rendu de plus (`GET /db/sync`, `GET /saves`). Aucun reseau, aucun secret, aucune dependance, aucun fichier de `dofus_stuff/**` modifie (D-88). La menace reelle est celle d'un controle qui **toucherait** ce que la page decrit : l'ecran de synchronisation qui appelle `ensure_up_to_date(offline=False)`, l'ecran de vidage et sa confirmation, et la base du depot.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Elevation of privilege | Le harnais : synchronisation reseau ou execution du produit par un `POST` | high | mitigate | L'ecran de synchronisation est lu en `GET` seulement ; le module ne poste jamais vers `/db/sync` ni `/db/clear` ; la garde de cloture du plan 05-01 refuse la paire de confirmation (`confirm` dans `O`/`Y`/`OUI`/`YES`, ce que `dofus_stuff/web/routes.py:810` accepte) ; `main()` n'est jamais execute et le refus hors-ligne est prouve par `ast` et par la sonde publique du parseur |
| T-05-08 | Tampering | `.data/dofus.sqlite3` : une commande destructrice executee par un controle | high | mitigate | Aucune commande destructrice n'est executee : les effets sont lus par `ast` dans `Database.clear` et par la lecture du fichier JavaScript ; les bases temporaires viennent de `tmp_path` et sont fermees dans un `finally` ; l'empreinte `taille:mtime_ns:sha256` est mesuree avant et apres la suite complete (tache 1) |
| T-05-09 | Spoofing (documentation qui ment) | Sections des cas non evidents et des commandes destructrices | high | mitigate | Chaque valeur citee est lue dans le code (litteraux par `ast`), dans le rendu (corps et ligne de statut) ou dans la sortie capturée ; les morsures des taches 1, 2 et 3 (creation retiree, chemin invente, refus renomme, refus desactive, `offline=False` retire, corps de confirmation renomme, ligne sans avertissement, cible confondue, effet retire) prouvent que la page et le module rougissent quand le code change |
| T-05-10 | Repudiation (faux rouge sur un fichier hors mandat) | Le critere 4 applique hors du perimetre de la phase | medium | mitigate | Le module porte `LIMITE_PERIMETRE` et la docstring du test declare que le controle porte sur `docs/base-locale.md` ; l'occurrence de `README.md` ligne 80 est consignee pour la phase 6 et aucun constat ne la mentionne (Pitfall 8, A3) |
| T-05-11 | Tampering | Un avertissement deplace hors de la ligne de sa commande | medium | mitigate | Le controle exige l'avertissement sur **chaque** ligne portant un jeton, et la morsure `ligne_sans_avertissement_added` prouve qu'une ligne porteuse non avertie est signalee ; la correction consiste a replacer l'avertissement, jamais a retirer le controle |
| T-05-12 | Information disclosure | Valeurs volatiles et chemins absolus de poste | low | mitigate | Motif `\\d{4,}` deja exige par le plan 05-01 ; la section ne cite aucun chemin absolu ; `DOFUS_SECRET_KEY` reste absent de toutes les pages (mesure : 0 occurrence) |
| T-05-SC | Tampering | Installations de paquets | low | accept | Cette phase n'ajoute aucune dependance : `pyproject.toml` est inchange, aucun `pip install` n'est execute ; la porte de legitimite des paquets n'a aucun sujet ici |

**Hors perimetre** : publication, deploiement distant, synchronisation Dofusdude, suppression de donnees, achat. Les deux commandes destructrices sont **documentees**, jamais lancees.
</threat_model>

<verification>
**Controles automatises** : `tests/test_docs_base_locale.py` (creation de la base sur les deux surfaces et chemins d'ecran declares, refus de la synchronisation hors-ligne, synchronisation web hors-ligne, commandes destructrices), les gardes existantes inchangees, la suite entiere, l'empreinte de `.data/dofus.sqlite3` autour de la suite, et **dix morsures** sur copie verte avant mutation : creation retiree du code, chemin d'ecran invente dans la page, message de refus renomme, bloc de refus desactive, `offline=False` retire de l'appel de synchronisation, corps de confirmation renomme, bloc de commandes ajoute, ligne porteuse sans avertissement ajoutee, cible de la seconde commande confondue, litteral de suppression retire du code.

**Non automatisable, nomme comme tel** : l'execution du JavaScript de la seconde commande dans un navigateur, et l'appreciation de redaction « aucune invitation ». Ces deux points sont declares en `backstop` dans `must_haves.truths` et ne sont **pas** revendiques comme verifies. Aucun blocage : rien de cette phase ne depend d'un geste humain, d'un secret ou d'un reseau.

**References de verification de phase** : `05-VALIDATION.md` § Per-Task Verification Map assigne les lignes `db_status`, `db_sync_offline`, `synchro_web` et `destructrices` a ce plan (vague 2) ; `/gsd:verify-work` rejouera la suite entiere et la mesure de non-regression de `.data/`.
</verification>

<success_criteria>
- La page couvre les trois cas non evidents du critere 3 — le premier contact qui cree la base, le refus de la synchronisation en mode hors-ligne, l'ecran web de synchronisation qui contacte l'API — et les deux commandes destructrices du critere 4, chacune signalee sur sa ligne et hors de tout parcours (BASE-03 ; D-77 a D-81).
- Chaque fait est adosse soit a un litteral lu par `ast`, soit a un corps rendu, soit a la sortie capturée d'une fonction, et `main()` n'est jamais execute, aucun `POST` n'est emis, aucune commande destructrice n'est lancee.
- Le perimetre du critere 4 est ecrit dans le module, et l'occurrence de `README.md` est consignee pour la phase 6 sans faire rougir la suite.
- Dix derives epinglees font rougir la suite sur une copie verte avant mutation ; la suite entiere est verte et `.data/dofus.sqlite3` est intact.
</success_criteria>

<output>
Create `.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-02-SUMMARY.md` when done
</output>

## Artefacts produits par cette phase (inventaire tenu a jour par les trois plans)

| Type | Symbole | Plan |
|------|---------|------|
| page | `docs/base-locale.md` | 05-01 (creation), 05-02 (quatre sections) |
| ligne d'index | `\| [Base locale](base-locale.md) \|` dans `docs/sommaire.md` | 05-01 |
| constante de dette | `PAGES_INEXISTANTES = ()` et ses commentaires remis en verite (`tests/test_docs_parcours.py`) | 05-01 |
| renvois | les deux renvois vers la base locale dans `docs/parcours-simplifie.md` (liens) | 05-01 |
| module de test | `tests/test_docs_base_locale.py` | 05-01 |
| constante de module | `TITRES_SECTION_ATTENDUS` | 05-01, completee en 05-01 (taches 2 et 3) et en 05-02 |
| helpers locaux | `_texte_page`, `_lignes_du_corps`, `_statut`, `_routes_declarees` | 05-01, `_routes_declarees` en 05-02 |
| helpers locaux | `_empreinte`, `renvois_morts` | 05-03 |
| constantes de garde | `RACINES_INTERDITES`, `APPELS_SUPPRESSION`, `APPEL_PRODUIT`, `APPELS_SYNCHRO_PRODUIT`, `CONFIRMATIONS_INTERDITES`, `MOTIF_GARDE` | 05-01 |
| constantes de motif | `MOTIF_FICHIER`, `MOTIF_CATEGORIES`, `MOTIF_FENETRE`, `MOTIF_INDEX`, `MOTIF_H1`, `MOTIF_DEFAUTS`, `MOTIF_CHAMPS_CLI`, `MOTIF_CHAMPS_WEB`, `MOTIF_CONDITION`, `MOTIF_VOLATILES`, `MOTIF_CRLF`, `MOTIF_DATA_DIR`, `MOTIF_CONFIRMATION` | 05-01 |
| constantes de motif | `MOTIF_CREATION`, `MOTIF_ROUTE`, `MOTIF_REFUS`, `MOTIF_SYNCHRO_WEB`, `MOTIF_DESTRUCTRICES`, `MOTIF_EFFET`, `MOTIF_EXEMPLE` | 05-02 |
| constantes de motif | `MOTIF_README`, `MOTIF_SET` | 05-03 |
| constantes de contenu | `MOTIF_VOLATILE`, `MARQUES_DESTRUCTRICES`, `MARQUES_BASE`, `MARQUES_SAUVEGARDES`, `MARQUES_HORS_PARCOURS`, `COMMANDE_DESTRUCTRICE`, `JETON_PURGE`, `BALISE_COMMANDE`, `LIMITE_PERIMETRE` | 05-01 (`MOTIF_VOLATILE`), 05-02 (le reste) |
| fonctions de test | `test_garde_de_cloture_du_harnais`, `test_page_et_index_de_la_base_locale`, `test_defauts_hors_ligne`, `test_champs_de_la_ligne_de_commande`, `test_champs_de_la_surface_web` | 05-01 |
| fonctions de test | `test_le_premier_contact_cree_la_base`, `test_le_refus_de_la_synchronisation`, `test_la_synchronisation_web_contacte_l_api`, `test_commandes_destructrices` | 05-02 |
| fonctions de test | `test_renvois_du_readme_resolus`, `test_renvoi_base_locale_legitime`, `test_page_close_et_sans_valeur_volatile`, `test_data_locale_non_modifiee_autour_des_rendus` | 05-03 |

## Couverture des sources (ce plan)

| Source | Element | Consomme par |
|--------|---------|--------------|
| ROADMAP § Phase 5 | Critere 3 (`db status` cree le fichier, `db sync` refuse `--offline`, l'ecran web de synchronisation contacte l'API) | tache 1 (creation) et tache 2 (refus et appel API) |
| ROADMAP § Phase 5 | Critere 4 (commandes destructrices signalees sur la meme ligne, dans aucun parcours ; les controles n'ecrivent pas sous `.data/`, n'executent aucune synchronisation et n'ouvrent aucune connexion reseau) | tache 3 (reconnaissance et cibles), et l'absence de POST dans la tache 2 |
| ROADMAP § Phase 5 | Brief du plan 05-02 (couvrir les cas non evidents et les avertissements destructifs) | taches 1, 2 et 3 |
| REQUIREMENTS | BASE-03 | `requirements` du plan |
| RESEARCH | § 9 (a) creation du fichier par le premier contact, mesuree sur les deux surfaces | tache 1 |
| RESEARCH | § 9 (b) refus de la synchronisation hors-ligne, message verbatim et code de retour | tache 2 |
| RESEARCH | § 9 (c) l'ecran web qui passe `offline=False`, le corps de confirmation et le fait que `GET` est sur et `POST` non | tache 2 |
| RESEARCH | § 10 (les deux commandes destructrices, leurs aides, `DELETE FROM items` et `DELETE FROM meta`, le fichier conserve, la ligne de statut de l'ecran des sauvegardes, le traitement JavaScript) | tache 3 |
| RESEARCH | Inventaire des occurrences destructrices de la documentation livree, item 1 (`README.md` ligne 80) et item 8 (`docs/parcours-simplifie.md` reste proprietaire de l'ecran des sauvegardes) | tache 3 (perimetre declare) |
| RESEARCH | Pitfall 6 (`GET` sur, `POST` non), Pitfall 7 (les deux commandes ne detruisent pas la meme chose), Pitfall 8 (le seul cas destructeur presente comme une etape est dans le README) | taches 2 et 3 |
| RESEARCH | § Feasibility verdict : mecanismes 4 (rendu), 5 (sortie capturée) et 6 (litteraux par `ast`), plus la liste de ce qu'un controle ne doit jamais faire | taches 1, 2 et 3 |
| CONTEXT | D-77 (`db status` cree le fichier, prouve sans toucher `.data/`) | tache 1 |
| CONTEXT | D-78 (refus de `db sync --offline`, message reel du code) | tache 2 |
| CONTEXT | D-79 (l'ecran web contacte l'API meme hors-ligne) | tache 2 |
| CONTEXT | D-80, D-81 (avertissement sur la meme ligne, aucune execution destructrice, integrite mesuree) | tache 3 |
| CONTEXT | D-76 (aucun nom cite qui ne soit produit par le code) | tache 1 (chemins d'ecran declares) et tache 2 (litteraux et rendu) |
| CONTEXT | D-83, D-84, D-85 (constats localisants, morsures sur copie verte, limites declarees) | les trois taches |
| CONTEXT | D-86, D-87, D-88, D-89, D-91 (renvois par lien, perimetre du README, code non modifie, aucune resynchronisation, commits locaux) | taches 2 et 3 |

**Limites de la phase, delimitees de nouveau ici (Pitfall 8, A3)** : le critere 4 est applique **au perimetre de cette phase** — la page `docs/base-locale.md` et son module. L'occurrence de `README.md` ligne 80, seule du depot ou une commande destructrice figure dans un bloc de commandes sans avertissement, est **hors du mandat de reecriture** de cette phase (D-87) : elle est consignee ici et dans `LIMITE_PERIMETRE`, pour la phase 6, proprietaire de la completude. Aucun controle de ce plan ne rougit a cause d'elle.

**Audit de complétude de la phase (les quatre types de sources)** : GOAL — les criteres 3 et 4 sont couverts par ce plan, les criteres 1, 2 et 5 par les plans 05-01 et 05-03 ; REQ — `BASE-03` ici et en 05-03, `BASE-01` et `BASE-02` en 05-01 ; RESEARCH — les sections 9, 10, l'inventaire destructeur, les pieges 6, 7 et 8 et le verdict de faisabilite sont consommes ici, les pieges 1 a 5, 9, 10 et 11 par les plans 05-01 et 05-03 ; CONTEXT — D-76 a D-81 ici, D-68 a D-76 et D-82 a D-86 en 05-01, D-87 a D-91 reparties. Exclusions assumees, conformes aux « Deferred Ideas » de `05-CONTEXT.md` : liste epinglee des pages et preuve de completude (phase 6), page de depannage et glossaire (phase 6), toute resynchronisation Dofusdude (hors perimetre).

