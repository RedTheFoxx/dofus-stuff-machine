---
phase: 05-base-locale-hors-ligne-et-resynchronisation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/base-locale.md
  - docs/sommaire.md
  - docs/parcours-simplifie.md
  - tests/test_docs_parcours.py
  - tests/test_docs_base_locale.py
autonomous: true
requirements:
  - BASE-01
  - BASE-02
estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Le nom du fichier de la base et son dossier sont lus dans le code, pas recopies : la page cite `dofus.sqlite3` et le dossier `.data/` a la racine du depot, et le controle exige l'egalite avec `DB_NAME` et avec `DEFAULT_DATA_DIR.name` (`dofus_stuff/database.py:12-13`, D-74) — critere de succes 1, BASE-01."
    - "Les sept categories reellement stockees sont nommees et aucune autre : l'ensemble des `kind` cites par la section des categories est exactement `ITEM_KINDS` (`dofus_stuff/database.py:18-26`), qui egale de plus `tuple(kind for _, _, kind in SYNC_SOURCES)` (`dofus_stuff/api.py:19-28`, D-14, D-74) ; retirer une categorie du code fait rougir le module — critere de succes 1, BASE-01."
    - "La fenetre de re-check est citee par sa constante nommee `CHECK_INTERVAL_SECONDS` (`dofus_stuff/sync.py:11`) et par son expression `24 * 60 * 60`, sans aucune duree calculee, aucun horodatage, aucune date (D-73) — critere de succes 1, BASE-01."
    - "Les deux defauts hors-ligne sont enonces separement, chacun dans la section de sa surface, et chacun confronte au defaut reellement mesure de son parseur public : web `parse_args([]).offline is True` (`dofus_stuff/web/__main__.py:21-31`), CLI `parse_args([\"db\", \"status\"]).offline is False` (`dofus_stuff/cli.py:46-50`) ; la phrase d'aide citee par chaque section est celle que le parseur rend, comparee apres normalisation des espaces (D-71, D-72, Pitfall 5) — critere de succes 2, BASE-02."
    - "Les champs de l'etat de la base sont decrits par leurs noms et par surface, dans la forme exacte de chaque surface : la section ligne de commande cite les libelles produits par `_print_db_status` (`Fichier :`, `Version jeu :`, `Dernier check :`, `Entrees :` accentue, `Par categorie :`, `(aucune)`, `(aucun)`) et la section web cite ceux rendus par `GET /db/status` (`FICHIER :`, `VERSION JEU :`, `DERNIER CHECK :`, `ENTREES :` sans accent, `PAR CATEGORIE :`, `(aucune)`, `(AUCUN)`) ; la comparaison est exacte, jamais normalisee, pour que l'ecart d'accent reste un fait observable (Pitfall 4) — critere de succes 2 et 5, BASE-02."
    - "Le champ du nombre d'objets par categorie est prouve conditionnel des deux cotes : `Par categorie :` et `PAR CATEGORIE :` apparaissent sur une base peuplee et disparaissent sur une base vide (condition `if isinstance(by_kind, dict) and by_kind`, `dofus_stuff/cli.py:226` et `dofus_stuff/web/routes.py:766`) ; la page ne cite donc pas ce champ comme toujours affiche (Pitfall 3)."
    - "La page respecte le gabarit verrouille : un seul `H1` egal au libelle d'index apres normalisation, une ligne de retour `[Retour au sommaire](sommaire.md)` en derniere ligne non vide, un bloc `## Source de verite` dont chaque chemin entre accents graves existe depuis la racine du depot, des octets CRLF sans BOM, aucun lien externe, aucun jeton de brouillon (D-69, D-90)."
    - "`docs/sommaire.md` croit d'exactement une ligne d'index, en fin de table, sans remanier les lignes existantes ni la liste « Parcours conseille » (D-70) ; creer la page sans la ligne rougit, ajouter la ligne sans la page rougit (D-06)."
    - "La dette D-44/D-63 est levee dans le meme commit que sa cible : `PAGES_INEXISTANTES` (`tests/test_docs_parcours.py:2327`) ne declare plus inexistante une page qui existe, le commentaire et la docstring qui annoncaient encore la dette sont reecrits, et les deux renvois en prose de `docs/parcours-simplifie.md` deviennent des liens vers `base-locale.md` (D-63, D-86)."
    - "La garde de cloture du nouveau module est reecrite, jamais recopiee : elle interdit toujours les racines de risque (`sqlite3`, `subprocess`, `socket`, `multiprocessing`, `ctypes`, `webbrowser`, `urllib`, `requests`, `http`, `ftplib`, `smtplib`), l'appel `main`, les appels de suppression, et elle ajoute les deux interdits propres a cette phase — `DEFAULT_DATA_DIR` jamais passe comme `data_dir`, et jamais la paire de confirmation (`confirm` valant `O`, `Y`, `OUI` ou `YES`) que `dofus_stuff/web/routes.py:810` accepte ; elle n'interdit plus l'import de `dofus_stuff.database`, qui est la matiere de la page (Pitfall 2)."
    - statement: "Aucune phrase de la page n'invite a lancer une commande destructrice et aucune ne la presente comme une etape d'un parcours. Le controle automatique prouve l'absence des jetons destructeurs dans les blocs d'exemple et la presence de l'avertissement sur la meme ligne ; le jugement de redaction (« aucune invitation ») reste une backstop explicite, jamais un vert silencieux."
      verification: backstop
    - statement: "L'effet reel de `PURGE OUI` dans un navigateur (purge des sauvegardes du `localStorage`) n'est pas observable en processus : le controle s'arrete au libelle rendu par l'ecran `SAV-01` et a la lecture du fichier `dofus_stuff/web/static/js/terminal.js`. Non confirmable au-dela : backstop explicite (A2 de la recherche, D-85)."
      verification: backstop
  artifacts:
    - path: "docs/base-locale.md"
      provides: "La page unique du sujet : fichier et dossier de la base, tables et cles de meta, sept categories stockees, fenetre de re-check de 24 h, deux defauts hors-ligne par surface, champs de l'etat de la base par surface, bloc Source de verite et ligne de retour"
      contains: "## Source de vérité"
      min_lines: 60
    - path: "docs/sommaire.md"
      provides: "La ligne d'index « Base locale » dans la table `## Index`, sans autre modification"
      contains: "| [Base locale](base-locale.md) |"
      min_lines: 23
    - path: "tests/test_docs_base_locale.py"
      provides: "Le module d'ancrage : docstring de contrat et de limites, imports de constantes publiques, marqueurs et lecteurs du rendu, garde de cloture reecrite, controle de la page et de l'index, controle des deux defauts hors-ligne, controle des champs par surface"
      contains: "def test_garde_de_cloture_du_harnais"
      min_lines: 260
    - path: "tests/test_docs_parcours.py"
      provides: "Reserve de dette videe et commentaires remis en verite apres la livraison de la page"
      contains: "PAGES_INEXISTANTES = ()"
      min_lines: 2990
  key_links:
    - from: "tests/test_docs_base_locale.py"
      to: "dofus_stuff/database.py"
      via: "import des constantes publiques `DB_NAME`, `DEFAULT_DATA_DIR`, `META_GAME_VERSION`, `META_LAST_CHECKED_AT`, `ITEM_KINDS` et de `Database` pour construire une base temporaire ; c'est ce module qui rend la garde de la phase 4 inapplicable telle quelle (Pitfall 2)"
      pattern: "ITEM_KINDS"
    - from: "tests/test_docs_base_locale.py"
      to: "dofus_stuff/sync.py"
      via: "import de `CHECK_INTERVAL_SECONDS`, la constante nommee que la page cite comme fenetre de re-check"
      pattern: "CHECK_INTERVAL_SECONDS"
    - from: "tests/test_docs_base_locale.py"
      to: "dofus_stuff/cli.py"
      via: "sonde des defauts et de l'aide par `build_parser().parse_args` (API publique, D-14) et capture de la sortie de `_print_db_status` sur une base temporaire"
      pattern: "build_parser"
    - from: "tests/test_docs_base_locale.py"
      to: "dofus_stuff/web/__main__.py"
      via: "sonde du defaut hors-ligne du web et de son texte d'aide par `format_help()` du parseur public"
      pattern: "format_help"
    - from: "docs/base-locale.md"
      to: "docs/sommaire.md"
      via: "la ligne d'index ajoutee dans la table et la derniere ligne de retour `[Retour au sommaire](sommaire.md)`"
      pattern: "sommaire.md"
    - from: "tests/test_docs_parcours.py"
      to: "docs/base-locale.md"
      via: "la reserve `PAGES_INEXISTANTES` videe, qui ne peut plus declarer inexistante une page presente sur disque"
      pattern: "base-locale.md"
  prohibitions:
    - statement: "Ne jamais modifier `dofus_stuff/**` pour aligner la documentation : le code est le referentiel, la page s'y conforme (D-88)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais ecrire, vider, supprimer ni renommer quoi que ce soit sous `.data/` : aucune commande destructrice n'est executee, aucune synchronisation n'est lancee, et la base du depot n'est ouverte qu'en lecture d'octets pour l'empreinte (D-81, D-89)."
      status: resolved
      verification: test
    - statement: "Ne jamais executer `main()` du produit, lancer le solveur, ouvrir un processus, une socket ou une connexion reseau depuis un controle (D-15, D-81)."
      status: resolved
      verification: test
    - statement: "Ne jamais epingler une valeur volatile dans la prose de la page — compteur d'objets, version de jeu, horodatage, age du dernier controle, taille de fichier, chemin absolu du poste (D-73, D-75 ; § 12 de la recherche)."
      status: resolved
      verification: test
    - statement: "Ne jamais presenter une commande destructrice comme une etape d'un parcours recommande ni l'executer : elles existent pour etre reconnues, pas pour etre suivies (D-80, D-81)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais publier, deployer a distance, pousser vers un depot distant ni effectuer d'achat : commits locaux seulement, indexation par chemin explicite, jamais `git add .` (D-91)."
      status: resolved
      verification: judgment
  assumptions:
    - "**Sonde d'arete spec-less : l'orchestrateur n'a PAS execute la sonde deterministe de la section A** (aucun SPEC de phase fourni : `EDGE_ABSENT` / `PROHIB_ABSENT`). La liste d'aretes de ce plan est donc **derivee par le planificateur** des cinq criteres de succes du ROADMAP § Phase 5, des textes de BASE-01 a BASE-03 et de l'inventaire mesure de `05-RESEARCH.md` § Measured inventory. Chaque arete classable a recu un critere confirmable ci-dessus ; deux verifications (invitation de prose, execution JavaScript de `PURGE OUI`) sont ecrites en **backstop** et non en vert silencieux ; les six prohibitions retenues sont au bloc `prohibitions`, aucune n'est ecartee en silence."
    - "Les cinq libelles de l'etat de la base n'ont **aucune constante publique** : ils sont des litteraux de `_print_db_status` (`dofus_stuff/cli.py:214-229`) et de `db_status` (`dofus_stuff/web/routes.py:755-769`). La route d'ancrage retenue est la **sortie capturée** pour la CLI et le **corps rendu** pour le web (mecanismes 4 et 5 du verdict de faisabilite), avec repli `ast` documente si le plan suivant refuse un nom prive : l'import de `_print_db_status` est legitime par le nom que `.claude/CLAUDE.md` § 4.3 prescrit a ce test."
    - "La prose libre de la page n'est pas verifiee par un test : les controles portent sur les libelles, les valeurs et les messages **cites**, et ce plan ne revendique aucune exhaustivite de la redaction (limite ecrite dans la docstring du module, D-85)."
    - statement: "Le contenu de `.doc-agent/`, `doc-agent.toml`, `gsd-auto*.toml` et `.planning/state.json` reste hors du perimetre : aucune suppression, aucune indexation (D-89, D-91)."
      verification: backstop
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 5 (« Un lecteur comprend la base SQLite locale, la fenetre de resynchronisation et les deux comportements hors-ligne, et ne lance jamais une commande destructrice par inadvertance. »), de ses criteres de succes 1, 2 et 5, et des decisions D-68 a D-76 et D-82 a D-84 de `05-CONTEXT.md` :
**En tant que** lecteur qui ne veut pas lire le code, **je veux** comprendre ce que l'outil stocke dans `.data/dofus.sqlite3`, quand il recontacte l'API et ce que « hors-ligne » change sur chaque surface, **afin de** savoir quand je dois passer en ligne et de ne jamais lancer par inadvertance une commande qui detruit mes donnees.

Mode MVP : le `**Goal:**` du ROADMAP n'est pas redige en recit utilisateur. Le cadrage ci-dessus en est **derive**, comme au plan 04-01 de la phase precedente, et non invente : il ne reformule que des criteres de succes et des decisions verrouillees. Aucun retour a `/gsd mvp-phase` n'est demande.

Purpose : c'est **BASE-01** (le fichier, les categories stockees, la fenetre de resynchronisation) et **BASE-02** (les deux defauts hors-ligne et les champs de l'etat de la base) — et c'est aussi le plan qui livre la **cible** que les criteres 4 et 5 exigent ensuite (la page, son index et son module d'ancrage). La page est ecrite **depuis les constantes publiques et le rendu reel** : chaque valeur citee est lue a l'import, dans une aide de parseur rendue, ou dans une sortie capturée, jamais de memoire.

Output : `docs/base-locale.md` (les sections du perimetre BASE-01 et BASE-02), la ligne d'index de `docs/sommaire.md`, la conversion en liens des deux renvois en prose de `docs/parcours-simplifie.md` (fichier declare dans `files_modified` : il entre dans le jeu de chemins explicites du commit, D-91, et il est modifie par la tache 1), la reduction obligatoire de `PAGES_INEXISTANTES` dans `tests/test_docs_parcours.py`, et `tests/test_docs_base_locale.py` (module cree ici, etendu par les plans 05-02 puis 05-03).

Ordonnancement declare : **vague 1**, `depends_on: []` — c'est le premier plan de la phase, et il ouvre la tranche verticale (page + index + reserve de dette + harnais + constantes publiques) dont les deux autres plans etendent le perimetre.

**Ce que ce plan ne fait pas** : il n'ecrit ni les cas non evidents (`db status` qui cree le fichier, `db sync` qui refuse `--offline`, l'ecran de synchronisation web qui contacte l'API hors-ligne) ni les avertissements destructeurs (plan 05-02, criteres 3 et 4) ; il n'ecrit ni le controle des renvois du `README.md` (D-87) ni la mesure d'empreinte de `.data/` (plan 05-03, critere 5) ; il ne modifie **aucun** fichier de `dofus_stuff/**` (D-88), ne relance aucune synchronisation Dofusdude (D-89) et ne touche ni `README.md` ni `GUIDE_WIZARD.md`.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-CONTEXT.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-VALIDATION.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
@.claude/CLAUDE.md
@docs/sommaire.md
@docs/cli.md
@docs/wizard-avance.md
@docs/parcours-simplifie.md
@tests/conftest.py
@tests/test_docs_structure.py
@tests/test_docs_parcours.py
@dofus_stuff/database.py
@dofus_stuff/sync.py
@dofus_stuff/api.py
@dofus_stuff/cli.py
@dofus_stuff/web/__main__.py
@dofus_stuff/web/__init__.py
@dofus_stuff/web/routes.py
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : tranche verticale « la page existe, son index est coherent, la reserve de dette est levee, et les valeurs fondatrices sont ancrees »</name>
  <files>docs/base-locale.md, docs/sommaire.md, docs/parcours-simplifie.md, tests/test_docs_parcours.py, tests/test_docs_base_locale.py</files>
  <reversibility rating="reversible">Creer une page `docs/`, une ligne d'index et un module de test est annulable par un commit de retour ; rien n'est publie, rien n'est deplace hors du depot, `.data/` n'est pas approche.</reversibility>
  <read_first>
    docs/sommaire.md
    docs/cli.md
    docs/wizard-avance.md
    docs/parcours-simplifie.md
    tests/conftest.py
    tests/test_docs_structure.py
    tests/test_docs_parcours.py
    dofus_stuff/database.py
    dofus_stuff/sync.py
    dofus_stuff/api.py
    dofus_stuff/cli.py
    dofus_stuff/web/__main__.py
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
    .claude/CLAUDE.md
  </read_first>
  <action>Ecrire la tranche la plus fine qui traverse **toutes** les couches de la phase — page `docs/`, index du sommaire, reserve de dette du module de parcours, module d'ancrage neuf, constantes publiques du produit — et la rendre verifiable de bout en bout avant toute expansion.

**Page** (`docs/base-locale.md`, nouveau fichier, UTF-8 sans BOM, fins de ligne CRLF comme les pages existantes de `docs/` — convention mesuree sur 20 fichiers) — premiere version **livrable**, pas une ebauche :
- un `H1` exactement `# Base locale`, sans suffixe et sans gras : c'est le libelle d'index exige par D-70, compare apres normalisation accents/casse par `problemes_h1` (`tests/test_docs_structure.py:337-380`) ;
- une phrase d'introduction en francais : la page decrit ce que l'outil stocke localement, quand il recontacte l'API et ce que « hors-ligne » change ; elle dit que les valeurs citees sont lues dans les constantes publiques de `dofus_stuff/database.py` et `dofus_stuff/sync.py` et dans le rendu reel des deux surfaces, et que la surface des commandes reste celle de [la page CLI](cli.md) — lien vers une cible qui existe (D-68, D-37, D-86) ;
- `## Le fichier de la base` : `dofus.sqlite3` et le dossier `.data/` a la racine du depot, lus respectivement dans `DB_NAME` et `DEFAULT_DATA_DIR` (`dofus_stuff/database.py:12-13`) ; les deux tables `meta` et `items` et l'index `idx_items_kind` tels que `Database.open` les cree (`dofus_stuff/database.py:44-61`) ; les deux cles de `meta` `game_version` et `last_checked_at`, lues dans `META_GAME_VERSION` et `META_LAST_CHECKED_AT` ; l'effet du **premier contact** : le dossier puis le fichier sont crees s'ils manquent (comportement developpe au plan 05-02). Aucun chemin absolu de poste, aucune taille de fichier ;
- `## Les catégories stockées` : les **sept** categories de `ITEM_KINDS` — `equipment`, `resources`, `consumables`, `quest`, `cosmetics`, `mounts`, `sets` — chacune entre accents graves, presentees comme les `kind` ecrits en base ; la page ne cite **aucune** categorie que le code ne stocke pas (en particulier `weapons` n'est pas un `kind`), et n'ecrit **aucun** nombre d'objets ;
- `## La fenêtre de re-check de 24 heures` : la **constante nommee** `CHECK_INTERVAL_SECONDS` et son expression `24 * 60 * 60` (`dofus_stuff/sync.py:11`), ce que la fenetre declenche au chargement d'un catalogue, et ce que l'option de forcage change — l'option est presentee **sans recopier son aide coupee par le terminal** (Pitfall 5) ; aucune date, aucune duree calculee, aucun horodatage (D-73) ;
- `## Source de vérité` : un chemin **existant** par ligne, entre accents graves, choisis parmi `dofus_stuff/database.py`, `dofus_stuff/sync.py`, `dofus_stuff/api.py` (D-03, D-69) ; aucun chemin invente, aucune constante privee ;
- derniere ligne non vide, exactement `[Retour au sommaire](sommaire.md)` ; aucun bloc balise `console`, aucun lien externe, aucun jeton de brouillon.

**Index** (`docs/sommaire.md`) — ajouter **une seule** ligne, en fin de table `## Index`, dans la forme des quatre lignes existantes : une ligne `| [Base locale](base-locale.md) | ... |` dont la deuxieme colonne decrit le sujet en une proposition comme les autres. La liste « Parcours conseille » (qui porte deja `5. Base locale` en texte, **sans lien**) n'est pas remaniee, aucune ligne existante n'est deplacee ni reformulee (D-70, D-05).

**Reserve de dette** (`tests/test_docs_parcours.py`) — la dette D-44/D-63 doit etre levee **dans le meme commit** que la page, sans quoi `test_lien_wizard_avance_legitime` rougit des que `docs/base-locale.md` existe (Pitfall 1 : la suite passait 205 tests avant, et rougit sur un seul test qui ne parle pas de la base locale) :
- `PAGES_INEXISTANTES = ("base-locale.md",)` devient un tuple **vide** — plus aucune page de `docs/` n'est en dette apres ce plan ;
- le commentaire au-dessus de la constante (`tests/test_docs_parcours.py:2323-2326`) est **reecrit** : il dit que la dette D-44 est entierement levee (wizard en phase 4, base locale ici), que la constante reste vide comme site d'accueil des deux boucles qui l'iterent, et qu'une entree ne s'y remet que si une page citee en prose n'existe pas ;
- la docstring de `test_lien_wizard_avance_legitime` (`:2870-2897`) et le constat du point 5 (`:2971-2978`) nomment encore la dette restante comme inexistante : ces deux textes sont reecrits pour dire la verite (la reserve est vide, la page cible est livree et indexee) — un commentaire faux est pire qu'un commentaire absent ;
- les deux renvois en prose de `docs/parcours-simplifie.md` vers le sujet « base locale » (l'en-tete, ligne 5, et la section « Ce que cette page ne decrit pas », ligne 256) deviennent des **liens** vers `base-locale.md` : la cible existe desormais (D-86), les mots `base locale` restent dans la phrase, donc `RENVOIS_SANS_LIEN` (`:2314`) reste satisfait et `problemes_liens` resout la cible ;
- `RENVOIS_SANS_LIEN`, `ANCRES_RENVOI_D63`, `LIENS_LEGITIMES_VERS_L_AIGUILLAGE`, les deux boucles qui iterent sur la reserve (`:2494`, `:2627`) et le controle `:2934-2943` ne sont **pas** touches : sur un tuple vide ils ne produisent rien.

**Module** (`tests/test_docs_base_locale.py`, nouveau fichier) — squelette **complet et definitif** de l'outillage ; les plans 05-02 et 05-03 n'auront qu'a l'etendre :
- docstring de module sur le patron de `tests/test_docs_wizard.py:1-19` : le contrat va des constantes publiques et du rendu vers la page, les ecrans sont rendus par le client de test Flask **en processus** sur la fixture `app`, aucun serveur, aucune socket, `main()` jamais execute, rien n'est ecrit sous `.data/` (D-15, D-32, D-81) ; puis les **limites nommees** de D-85 : la garde `ast` ne contraint que le module committe (L-1 de `04-SECURITY.md`), la mesure d'empreinte locale est vraie par construction (L-2, ecrite au plan 05-03), la prose libre de la page n'est pas verifiee, l'execution JavaScript de `PURGE OUI` n'est pas observable, et l'assertion CRLF n'est pas portable hors d'un poste dont `core.autocrlf` vaut `true` (AR-5) ;
- imports : `ast`, `hashlib`, `io`, `re`, `contextlib.redirect_stdout`, `pathlib.Path`, `pytest`, puis les **API publiques** du produit (D-14) : `DB_NAME`, `DEFAULT_DATA_DIR`, `ITEM_KINDS`, `META_GAME_VERSION`, `META_LAST_CHECKED_AT`, `Database` depuis `dofus_stuff.database` ; `CHECK_INTERVAL_SECONDS` et `SYNC_SOURCES` depuis `dofus_stuff.sync` et `dofus_stuff.api` ; `build_parser as parseur_cli` et `_print_db_status` depuis `dofus_stuff.cli` ; `build_parser as parseur_web` depuis `dofus_stuff.web.__main__` ; `create_app` depuis `dofus_stuff.web` pour le cas de la base vide du plan suivant. **Aucun** import de `sqlite3`, `subprocess`, `socket`, `webbrowser`, `urllib`, `requests` (la garde ci-dessous les refuse) ;
- constantes de chemin : `RACINE_DEPOT = Path(__file__).resolve().parents[1]`, `PAGE = "base-locale.md"`, `SOMMAIRE = "sommaire.md"`, `README = "README.md"`, `SOURCE_DATABASE = "dofus_stuff/database.py"`, `SOURCE_SYNC = "dofus_stuff/sync.py"`, `SOURCE_API = "dofus_stuff/api.py"`, `SOURCE_CLI = "dofus_stuff/cli.py"`, `SOURCE_WEB_MAIN = "dofus_stuff/web/__main__.py"`, `SOURCE_ROUTES = "dofus_stuff/web/routes.py"`, `SOURCE_TERMINAL_JS = "dofus_stuff/web/static/js/terminal.js"` ;
- marqueurs du rendu repris **a l'identique** de `tests/test_docs_wizard.py:91-94` (`MARQUEUR_CORPS`, `MARQUEUR_STATUT`, `LIGNE_CORPS`) et lecteurs locaux `_texte_page`, `_lignes_du_corps`, `_statut` : le corps est extrait entre `id="body">` et la ligne de statut, **jamais** la reponse entiere ; une page absente leve un `AssertionError` localisant (D-13), jamais un `FileNotFoundError` brut. Les helpers de `tests/conftest.py` (`docs_dir`, `normalize`, `section`, `sections`, `lignes_de_code`, `lignes_exemple`) sont **utilises**, jamais recopies (D-12) ;
- `TITRES_SECTION_ATTENDUS`, constante de module, initialisee **ici** avec les quatre titres que ce plan cree, dans leur ordre final : `## Le fichier de la base`, `## Les catégories stockées`, `## La fenêtre de re-check de 24 heures`, `## Source de vérité`. Les taches 2 et 3 puis le plan 05-02 insereront leurs titres **juste avant** `## Source de vérité`, qui doit rester le dernier titre epingle de la constante : l'ordre relatif des titres deja crees ne bouge donc jamais, et la suite reste verte a chaque commit ;
- constantes de **motif de morsure**, portees par le module et **jamais** ecrites en clair dans une ligne d'assertion (pytest reproduit la ligne source du `assert`, une valeur en clair y serait trouvee meme sans constat produit — regle posee au plan 03-03, tache 2). Valeurs ASCII, sans apostrophe, chacune incluse dans le constat qui la concerne : `MOTIF_FICHIER = "nom du fichier de la base"`, `MOTIF_CATEGORIES = "categories stockees"`, `MOTIF_FENETRE = "fenetre de re-check"`, `MOTIF_INDEX = "index du sommaire"`, `MOTIF_H1 = "titre de niveau 1"`, `MOTIF_DEFAUTS = "defauts hors-ligne"`, `MOTIF_CHAMPS_CLI = "champs de la ligne de commande"`, `MOTIF_CHAMPS_WEB = "champs de la surface web"`, `MOTIF_CONDITION = "champ conditionnel"`, `MOTIF_VOLATILES = "valeur volatile"`, `MOTIF_GARDE = "garde de cloture du harnais"`, `MOTIF_DATA_DIR = "repertoire de donnees non isole"`, `MOTIF_CONFIRMATION = "confirmation d une action destructive"`, `MOTIF_CRLF = "fins de ligne"` ;
- `MOTIF_VOLATILE = re.compile(r"\d{4,}")` : le motif de **valeur volatile** de la page, un nombre de quatre chiffres ou plus (compteur d'objets, version de jeu, horodatage, taille) — c'est la regle deja appliquee a `docs/parcours-simplifie.md` ;
- garde de cloture **reecrite** (Pitfall 2) : `RACINES_INTERDITES` (`sqlite3`, `subprocess`, `socket`, `multiprocessing`, `ctypes`, `webbrowser`, `urllib`, `requests`, `http`, `ftplib`, `smtplib`), `APPELS_SUPPRESSION` (`remove`, `unlink`, `rmdir`, `rmtree`), `APPEL_PRODUIT = "main"`, `APPELS_SYNCHRO_PRODUIT = ("ensure_up_to_date", "pull_all")` — les deux points d'entree de synchronisation du produit (`dofus_stuff/sync.py:14` et `:74`, noms mesures), par lesquels un controle atteindrait le reseau au travers de `dofus_stuff.api` : la garde refuse tout appel dont le nom — ou le nom d'attribut terminal — figure dans cette liste, ce qui adosse la prohibition « aucune connexion reseau depuis un controle » a un controle reel et non a une intention — plus les deux interdits **propres a cette phase** : `CONFIRMATIONS_INTERDITES = ("O", "Y", "OUI", "YES")` — les quatre valeurs que `dofus_stuff/web/routes.py:810` accepte — et l'interdiction de passer `DEFAULT_DATA_DIR` comme `data_dir`. **`MODULE_BASE_INTERDIT` et son controle ne sont PAS recopies** : `dofus_stuff.database` est la matiere de cette page (constantes publiques + `Database`). Helpers `_imports_du_module` et `_appels_du_module` repris du patron (`ast.walk` de l'arbre entier, imports imbriques compris) et test `test_garde_de_cloture_du_harnais`, qui **s'auto-analyse** par `ast.parse(Path(__file__).read_text(encoding="utf-8"))` — jamais une recherche de chaines, le module citant lui-meme les noms interdits dans ses messages. Le controle de paire lit, pour chaque appel dont un argument nomme `data` porte un dictionnaire litteral de chaines, la valeur de la cle `confirm` et refuse les quatre valeurs de confirmation ; le controle `data_dir` refuse tout appel `Database(...)` ou `create_app(...)` dont l'argument `data_dir` — positionnel ou nomme — n'est pas une expression derivee de `tmp_path` (attendu : un nom dont le texte contient `tmp_path`), constat portant `MOTIF_DATA_DIR` (`repertoire de donnees non isole`) — le plan 05-03 cite cette meme chaine verbatim pour sa morsure `data_dir_reel` ;
- test `test_page_et_index_de_la_base_locale(docs_dir, normalize, section)`, la **tranche de bout en bout** : lit la page par `_texte_page`, exige la ligne d'index dans le sommaire, exige un `H1` unique egal au libelle d'index (compare normalise), exige la ligne de retour en derniere ligne non vide, lit les octets en binaire (CRLF sans BOM), puis confronte les valeurs fondatrices au code : `DB_NAME` present dans la page et egal a la constante importee (constat portant `MOTIF_FICHIER`), `DEFAULT_DATA_DIR.name` present et egal a `.data` (meme constat), les sept `ITEM_KINDS` tous cites par la section des categories et aucun `kind` cite qui ne soit pas dans `ITEM_KINDS` (constat `MOTIF_CATEGORIES`), `tuple(kind for _, _, kind in SYNC_SOURCES)` egal a `ITEM_KINDS` (seconde source, D-14), `CHECK_INTERVAL_SECONDS` nomme par la section de la fenetre et son expression `24 * 60 * 60` citee (constat `MOTIF_FENETRE`), et l'absence de tout nombre de quatre chiffres ou plus dans la page (constat `MOTIF_VOLATILES`). Les constats sont accumules dans `constats: list[str]` et joints a **une seule** assertion `assert not constats` ; chaque constat suit le message de D-13 : `{page} : <ce qui est faux> ; attendu <valeur> <producteur, fichier de code ou ligne>`.

Contraintes de forme : aucun bloc de code balise `console` dans la page, aucun lien externe, aucun jeton de brouillon, octets CRLF sans BOM. Le module n'execute jamais `main()`, n'importe jamais `sqlite3` et n'ecrit rien sous `.data/` ; il n'ouvre que des bases construites sous `tmp_path` par la fixture `app` ou par `Database(data_dir=tmp_path / ...)`.
  </action>
  <acceptance_criteria>
    - `docs/base-locale.md` existe, commence par `# Base locale`, porte `## Le fichier de la base`, `## Les catégories stockées`, `## La fenêtre de re-check de 24 heures` et `## Source de vérité`, et sa derniere ligne non vide est `[Retour au sommaire](sommaire.md)`.
    - La page cite `dofus.sqlite3`, `.data/`, les sept categories de `ITEM_KINDS`, `CHECK_INTERVAL_SECONDS` et `24 * 60 * 60`, et ne contient aucun nombre de quatre chiffres ou plus.
    - `docs/sommaire.md` porte une seule ligne d'index nouvelle, `| [Base locale](base-locale.md) |`, et la liste « Parcours conseille » n'a pas gagne de lien markdown.
    - `tests/test_docs_parcours.py` declare `PAGES_INEXISTANTES = ()`, son commentaire et la docstring de `test_lien_wizard_avance_legitime` ne nomment plus `base-locale.md` comme page inexistante, et `docs/parcours-simplifie.md` lie `base-locale.md` depuis ses deux renvois.
    - `tests/test_docs_base_locale.py` existe, ne recopie aucun helper de `tests/conftest.py`, et `test_garde_de_cloture_du_harnais` passe tout en refusant un import de `socket`, un appel `Database(data_dir=DEFAULT_DATA_DIR)` ajoute au module et un appel portant `data={"confirm": "O"}`.
    - `test_page_et_index_de_la_base_locale` exige l'egalite avec `DB_NAME`, `DEFAULT_DATA_DIR.name`, `ITEM_KINDS`, `SYNC_SOURCES` et `CHECK_INTERVAL_SECONDS`.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py tests/test_docs_structure.py tests/test_docs_parcours.py -q` est vert, ainsi que la suite entiere.
    - `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256 identiques avant et apres la suite complete.
    - La batterie mesure la copie verte sur le module seul (`tests/test_docs_base_locale.py`) avant mutation, puis la suite complete apres : la commande automatisee des morsures dure de l'ordre de 30 a 45 secondes (sept copies, cout assume par D-84).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py tests/test_docs_structure.py tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; mesure() { "$PY" -c "import hashlib,pathlib;p=pathlib.Path('.data/dofus.sqlite3');print(f'{p.stat().st_size}:{p.stat().st_mtime_ns}:{hashlib.sha256(p.read_bytes()).hexdigest()}' if p.exists() else 'base absente')"; }; mesure > "$T/avant.txt"; "$PY" -m pytest -q; statut=$?; mesure > "$T/apres.txt"; if [ "$statut" -ne 0 ]; then echo "SUITE COMPLETE ROUGE (exit $statut)"; exit 1; fi; if diff -q "$T/avant.txt" "$T/apres.txt" >/dev/null; then echo "empreinte .data/ identique autour de la suite complete : $(cat "$T/apres.txt")"; else echo "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE : avant=$(cat "$T/avant.txt") apres=$(cat "$T/apres.txt")"; exit 1; fi</automated>
    <fails_when>exit non-zero, ou la ligne "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE", ou la ligne "SUITE COMPLETE ROUGE", ou une ligne contenant "failed" ou "error"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in fichier_renomme categorie_retiree ligne_index_retiree h1_desaligne import_interdit data_dir_defaut confirmation_interdite; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in fichier_renomme) sed -i 's/DB_NAME = "dofus.sqlite3"/DB_NAME = "dofus.db"/' "$T/dofus_stuff/database.py"; motif='nom du fichier de la base';; categorie_retiree) sed -i '/^    "cosmetics",$/d' "$T/dofus_stuff/database.py"; motif='categories stockees';; ligne_index_retiree) sed -i '/\[Base locale\](base-locale.md)/d' "$T/docs/sommaire.md"; motif='index du sommaire';; h1_desaligne) sed -i 's/^# Base locale$/# Base locale (SQLite)/' "$T/docs/base-locale.md"; motif='titre de niveau 1';; import_interdit) sed -i '1i import socket' "$T/tests/test_docs_base_locale.py"; motif='garde de cloture du harnais';; data_dir_defaut) printf '\n\ndef _temoin_data_dir():\n    return Database(data_dir=DEFAULT_DATA_DIR)\n' >> "$T/tests/test_docs_base_locale.py"; motif='repertoire de donnees non isole';; confirmation_interdite) printf '\n\ndef _temoin_confirmation(client):\n    client.post("/db/sync", data={"confirm": "O"})\n' >> "$T/tests/test_docs_base_locale.py"; motif='confirmation d une action destructive';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 05-01 (TACHE 1) EN ECHEC"; exit 1; fi; echo "morsures 05-01 tache 1 : 7/7 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 05-01 (TACHE 1) EN ECHEC"</fails_when>
  </verify>
  <done>La page existe, porte le libelle d'index `Base locale`, cite le fichier, les sept categories et la fenetre de 24 h depuis les constantes publiques, la reserve de dette est videe dans le meme commit, et la garde de cloture reecrite mord sur un import interdit, sur `DEFAULT_DATA_DIR` comme `data_dir` et sur une confirmation — sept derives epinglees font rougir la suite sur une copie verte avant mutation, et l'empreinte de `.data/` est inchangee.</done>
</task>

<task type="auto">
  <name>Tache 2 : les deux defauts hors-ligne, enonces separement et sondes sur leur propre parseur</name>
  <files>docs/base-locale.md, tests/test_docs_base_locale.py</files>
  <read_first>
    docs/base-locale.md
    tests/test_docs_base_locale.py
    docs/installation.md
    docs/cli.md
    dofus_stuff/cli.py
    dofus_stuff/web/__main__.py
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
  </read_first>
  <action>Ecrire les deux enonces hors-ligne et le controle qui les separe — c'est le risque de confusion central de la phase (D-71) : une phrase generale qui fusionnerait les deux defauts serait fausse sur au moins une surface.

**Page** — deux sections de niveau 2, inserees **juste avant** `## Source de vérité` (la ligne de retour reste la derniere ligne non vide) :
- `## Le mode hors-ligne du web` : l'interface web **demarre hors-ligne par defaut** ; `--offline` y est actif sans rien preciser, et c'est `--no-offline` (ou `--online`) qui autorise le contact API au demarrage ; la phrase d'aide **rendue** par le parseur web est citee telle que le code la porte, et la page dit ce que le mode hors-ligne change reellement au demarrage : la verification de version n'a pas lieu du tout et une base vide est un etat normal (`dofus_stuff/web/__init__.py:110` : `skip_sync`, `Catalog.load(skip_sync=True)` n'appelle pas `ensure_up_to_date`) ;
- `## Le mode hors-ligne de la ligne de commande` : la CLI est **en ligne par defaut**, donc `--offline` y est **requis** pour ne pas contacter l'API ; la phrase d'aide **rendue** par le parseur CLI est citee de meme ; la page dit ce que le mode hors-ligne change sur cette surface — la fenetre de 24 h **est** consultee, puis la synchronisation est refusee sans reseau, et une base vide rend une erreur (le message exact et l'option de forcage sont du ressort du plan 05-02) ;
- les deux sections restent **separees et nommees par leur surface** : aucune phrase ne dit « l'outil est hors-ligne par defaut » sans preciser la surface (D-71).

**Module** :
- `test_defauts_hors_ligne(docs_dir, section, normalize)` : sonde les deux parseurs par leur **API publique** (D-14) — jamais l'introspection privee d'`argparse` — et confronte la page a la mesure :
  - web : `parseur_web().parse_args([]).offline` vaut `True` ; `parse_args(["--no-offline"]).offline` vaut `False` ; `parse_args(["--online"])` porte `offline=True` et `online=True` ; le texte de `parseur_web().format_help()` contient la phrase `Ne pas contacter l'API au démarrage (défaut : oui)`, comparee **apres normalisation des espaces** (`re.sub(r"\s+", " ", ...)`) parce qu'`argparse` coupe l'aide a la largeur du terminal (mesure a 80 colonnes, Pitfall 5) ;
  - CLI : `parseur_cli().parse_args(["db", "status"]).offline` vaut `False` ; l'aide capturee par `parse_args(["--help"])` sous `redirect_stdout` (elle leve `SystemExit(0)`, la capturer avant) contient, apres normalisation des espaces, la phrase `Ne pas contacter l'API (échoue si la base locale est vide)` ;
  - la section web doit citer la phrase d'aide du web, la section CLI celle de la CLI : chaque phrase est exigee **dans sa section**, jamais cherchee dans toute la page — c'est ce qui prouve que chaque defaut est enonce la ou il s'applique ;
  - la section web doit porter `--offline` et une marque de defaut (au moins une de `par defaut`, `defaut`, `actif sans rien preciser`) ; la section CLI doit porter `--offline` et une marque d'exigence (au moins une de `requis`, `obligatoire`, `necessaire`, `exige`) ;
  - la comparaison des phrases d'aide passe par la fixture `normalize` (D-11) ; les jetons de drapeau sont exiges sur la forme exacte `--offline` / `--no-offline` / `--online` ;
  - chaque constat porte `MOTIF_DEFAUTS`, nomme la surface, la valeur mesuree, la valeur attendue et le fichier de code producteur (`SOURCE_WEB_MAIN` ou `SOURCE_CLI`) ;
- `TITRES_SECTION_ATTENDUS` gagne les deux nouveaux titres **avant** `## Source de vérité`, dans l'ordre d'insertion de la page.
  </action>
  <acceptance_criteria>
    - La page porte `## Le mode hors-ligne du web` et `## Le mode hors-ligne de la ligne de commande`, chacune avant `## Source de vérité`, et chacune cite la phrase d'aide rendue par son propre parseur.
    - La section web enonce le defaut hors-ligne du web et la maniere de le desactiver ; la section CLI enonce que `--offline` y est requis ; aucune phrase ne fusionne les deux defauts sans nommer la surface.
    - `test_defauts_hors_ligne` mesure `parse_args([]).offline is True` (web), `parse_args(["db", "status"]).offline is False` (CLI), et lit les deux phrases d'aide apres normalisation des espaces.
    - Inverser le defaut du parseur web (la ligne `default=True,` du parseur web remplacee par `default=False,`), forcer le defaut de `--offline` de la CLI en `store_false` (la ligne qui suit `"--offline",` dans `dofus_stuff/cli.py`, ligne 48 mesuree — jamais le premier `action="store_true"` du fichier, qui appartient a `--force-sync`, ligne 43) ou remplacer la phrase d'aide du web (`(défaut : oui)` -> `(défaut : non)`) rougit le module sur une copie verte avant mutation, avec le motif `defauts hors-ligne` present dans la sortie.
    - La batterie mesure la copie verte sur le module seul (`tests/test_docs_base_locale.py`) avant mutation, puis la suite complete apres : la commande automatisee des morsures dure de l'ordre de 15 a 25 secondes (cout assume par D-84).
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in defaut_web_inverse defaut_cli_force aide_web_remplacee; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif='defauts hors-ligne'; case "$m" in defaut_web_inverse) sed -i 's/        default=True,/        default=False,/' "$T/dofus_stuff/web/__main__.py";; defaut_cli_force) sed -i '/^        "--offline",$/{n;s/action="store_true"/action="store_false"/}' "$T/dofus_stuff/cli.py";; aide_web_remplacee) sed -i "s/(défaut : oui)/(défaut : non)/" "$T/dofus_stuff/web/__main__.py";; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 05-01 (TACHE 2) EN ECHEC"; exit 1; fi; echo "morsures 05-01 tache 2 : 3/3 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 05-01 (TACHE 2) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>Les deux defauts hors-ligne sont enonces separement, chacun dans la section de sa surface, avec la phrase d'aide que son propre parseur rend ; trois derives epinglees (defaut web inverse, defaut CLI force, aide web remplacee) font rougir la suite sur une copie verte avant mutation, et la suite entiere reste verte.</done>
</task>

<task type="auto">
  <name>Tache 3 : les champs de l'etat de la base, surface par surface, avec leur forme exacte</name>
  <files>docs/base-locale.md, tests/test_docs_base_locale.py</files>
  <read_first>
    docs/base-locale.md
    tests/test_docs_base_locale.py
    tests/conftest.py
    dofus_stuff/cli.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/templates/screen.html
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
  </read_first>
  <action>Decrire les champs de l'etat de la base **par leurs noms** et **par surface**, sans aucune valeur volatile, et les ancrer sur la sortie et le rendu reels. C'est le point ou une page peut etre vraie « en general » et fausse sur une surface : les deux surfaces ne portent pas les memes chaines (Pitfall 4).

**Page** — deux sections de niveau 2, inserees **juste avant** `## Source de vérité` :
- `## L'état de la base en ligne de commande` : les libelles produits par `python fetcher.py --offline db status`, dans la **forme exacte de la CLI** — `Fichier :`, `Version jeu :`, `Dernier check :`, `Entrées :` (**accentue**) et `Par catégorie :` suivi de lignes de la forme `  - <categorie> : <nombre>` — puis les deux etats vides que la CLI affiche : `Version jeu : (aucune)` et `Dernier check : (aucun)` ; la page precise que la ligne `Par catégorie :` **n'apparait que si des objets existent** ;
- `## L'état de la base dans l'interface web` : les libelles rendus par l'ecran d'etat, dans la **forme exacte du web** — `FICHIER :`, `VERSION JEU :`, `DERNIER CHECK :`, `ENTREES :` (**sans accent**, ce qui n'est pas une faute de frappe mais la forme du code) et `PAR CATEGORIE :` — puis `VERSION JEU : (aucune)` et `DERNIER CHECK : (AUCUN)` ;
- la page **nomme les deux surfaces** pour chaque jeu de libelles et ne presente aucun nombre d'objets, aucune version de jeu, aucune date ni duree : les valeurs sont produites par la commande et par l'ecran, jamais figees dans la prose (D-75, § 12 de la recherche).

**Module** :
- `test_champs_de_la_ligne_de_commande(docs_dir, section, app, tmp_path)` : capture la sortie reelle de `_print_db_status` sur une base **temporaire** et exige la bijection libelle <-> page :
  - cas **base vide** : `db = Database(data_dir=tmp_path / "vide")`, puis `_print_db_status(db)` sous `redirect_stdout(io.StringIO())`, et `db.close()` dans un `finally` — **obligatoire** : `_print_db_status` appelle `db.stats()` qui ouvre la connexion et la laisse ouverte, et sans `db.close()` le nettoyage du dossier temporaire echoue sur Windows (`PermissionError [WinError 32]`, mesure de la phase de recherche) ;
  - cas **base peuplee** : `Database(data_dir=app.extensions["web_config"]["data_dir"])` — la base que la fixture `app` a deja construite sous `tmp_path`, avec des `equipment` et des `resources` — puis `_print_db_status`, puis `db.close()` dans un `finally` ;
  - exige que chacun des cinq libelles produits soit **cite** par la section et que chacun des cinq libelles cites soit **produit** (les deux sens), la comparaison etant **exacte** — jamais passee par `normalize`, sans quoi l'ecart `Entrées :` / `ENTREES :` deviendrait invisible (Pitfall 4) ;
  - exige la presence de `Par catégorie :` sur la base peuplee et son **absence** sur la base vide : la conditionnalite du champ est prouvee dans les deux etats, pas seulement affirmee (Pitfall 3) ;
  - exige que la section cite l'etat vide `(aucune)` et l'etat vide `(aucun)` tels que la CLI les rend ;
  - chaque constat porte `MOTIF_CHAMPS_CLI` (ou `MOTIF_CONDITION` pour la conditionnalite) et nomme la valeur fautive, la valeur lue a la sortie et `SOURCE_CLI` ;
- `test_champs_de_la_surface_web(docs_dir, section, client, app, catalog, normalize, tmp_path)` : rend l'ecran d'etat par le client de test Flask en processus et compare au **corps rendu**, jamais a la reponse entiere :
  - cas **base peuplee** : `client.get("/db/status")` rend 200 et son corps, extrait par `_lignes_du_corps`, porte les cinq libelles web ;
  - cas **base vide** : une application construite par `create_app(data_dir=tmp_path / "web_vide", offline=True, catalog=catalog, load_catalog=False)` avec `config["TESTING"] = True`, dont le `GET /db/status` rend 200 sur un dossier **absent**, cree le fichier et n'affiche **pas** `PAR CATEGORIE :` ;
  - les libelles sont exiges en **forme exacte** (majuscules et absence d'accent comprises) ; la comparaison des libelles rendus par le web passe par la fixture `normalize` **seulement** la ou une entite HTML est en jeu (l'apostrophe sort en `&#39;` dans d'autres ecrans), jamais pour effacer un ecart d'accent ou de casse entre les deux surfaces ;
  - chaque constat porte `MOTIF_CHAMPS_WEB` (ou `MOTIF_CONDITION`) et nomme la valeur fautive, la valeur rendue et `SOURCE_ROUTES` ;
- `TITRES_SECTION_ATTENDUS` gagne les deux nouveaux titres **avant** `## Source de vérité`, dans l'ordre d'insertion de la page.
  </action>
  <acceptance_criteria>
    - La page porte `## L'état de la base en ligne de commande` et `## L'état de la base dans l'interface web`, chacune avant `## Source de vérité`, et y cite les cinq libelles de sa surface dans la forme exacte du code.
    - La section ligne de commande cite `Entrées :` accentue et les etats vides `(aucune)` et `(aucun)` ; la section web cite `ENTREES :` sans accent et les etats vides `(aucune)` et `(AUCUN)`.
    - `test_champs_de_la_ligne_de_commande` capture la sortie de `_print_db_status` sur une base vide **et** sur la base peuplee de la fixture `app`, ferme chaque base qu'il ouvre, et prouve la presence puis l'absence de `Par catégorie :`.
    - `test_champs_de_la_surface_web` rend `GET /db/status` sur la base peuplee et sur une base vide construite sous `tmp_path` par `create_app`, et prouve la presence puis l'absence de `PAR CATEGORIE :`.
    - Renommer `Entrées :` cote CLI, renommer `ENTREES :` cote web, ou rendre le champ par categorie inconditionnel, rougit le module sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
    - La batterie mesure la copie verte sur le module seul (`tests/test_docs_base_locale.py`) avant mutation, puis la suite complete apres : la commande automatisee des morsures dure de l'ordre de 15 a 25 secondes (cout assume par D-84).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in champ_cli_renomme champ_web_renomme categorie_inconditionnelle; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in champ_cli_renomme) sed -i 's/print(f"Entrées :/print(f"Entrees :/' "$T/dofus_stuff/cli.py"; motif='champs de la ligne de commande';; champ_web_renomme) sed -i 's/"ENTREES : /"TOTAL : /' "$T/dofus_stuff/web/routes.py"; motif='champs de la surface web';; categorie_inconditionnelle) sed -i 's/    if isinstance(by_kind, dict) and by_kind:/    if True:/' "$T/dofus_stuff/web/routes.py"; motif='champ conditionnel';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 05-01 (TACHE 3) EN ECHEC"; exit 1; fi; echo "morsures 05-01 tache 3 : 3/3 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 05-01 (TACHE 3) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>Les champs de l'etat de la base sont decrits par leurs noms, dans la forme exacte de chaque surface, avec leurs etats vides et la conditionnalite du champ par categorie prouvee dans les deux etats ; trois derives epinglees font rougir la suite sur une copie verte avant mutation, et la suite entiere reste verte.</done>
</task>

</tasks>

<!-- planner-discipline-allow: db clear -->
<!-- planner-discipline-allow: PURGE OUI -->

<threat_model>
**Surface** : une page `docs/`, une ligne d'index, deux fichiers de test existants modifies (un commentaire plus vrai, une reserve videe) et un module de test neuf ; le client de test Flask en processus et les parseurs publics servent d'unique sonde. Aucun reseau, aucun secret, aucune dependance ajoutee, aucun fichier de `dofus_stuff/**` modifie (D-88). La menace reelle est **documentaire** — une page qui affirme un libelle ou une valeur que le code ne produit plus — et celle du **harnais** — un test qui ouvrirait la base du depot ou executerait le produit.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Spoofing (documentation qui ment) | `docs/base-locale.md` : nom du fichier, categories, fenetre de 24 h, defauts hors-ligne, champs de l'etat | high | mitigate | Chaque valeur citee est lue a l'import d'une constante publique, dans une aide de parseur rendue ou dans une sortie capturée ; les morsures de la tache 1 (fichier renomme, categorie retiree, `H1` desaligne, index retire) et des taches 2 et 3 (defauts inverses, aide remplacee, libelles renommes, champ rendu inconditionnel) prouvent que la page rougit quand le code change |
| T-05-02 | Tampering | `.data/dofus.sqlite3` | high | mitigate | Aucune ecriture sous `.data/` : la base temporaire vient de `tmp_path` (fixture `app` ou `Database(data_dir=tmp_path / ...)`) ; `DEFAULT_DATA_DIR` n'est jamais passe a `Database` ni a `create_app` (interdit par la garde `ast`) ; l'empreinte `taille:mtime_ns:sha256` est mesuree avant et apres la suite complete |
| T-05-03 | Elevation of privilege | Le harnais : execution du produit, du solveur, d'une synchronisation ou d'une socket | high | mitigate | Garde `ast` reecrite : racines interdites (`sqlite3`, `subprocess`, `socket`, ...), `APPEL_PRODUIT = "main"`, appels de suppression, `data_dir` jamais `DEFAULT_DATA_DIR`, aucune paire de confirmation (`confirm` dans `O`/`Y`/`OUI`/`YES`) ; aucun `POST /db/sync` ni `POST /db/clear` n'est emis par ce module ; les morsures `import_interdit`, `data_dir_defaut` et `confirmation_interdite` prouvent que la garde mord |
| T-05-04 | Repudiation (perte du signal) | `tests/test_docs_parcours.py` : reserve de dette videe sans preuve | medium | mitigate | La reserve est videe **dans le meme commit** que la page ; les deux boucles qui l'iterent et le controle qui la juge ne sont pas touches, donc une reserve qui declarerait encore la page inexistante rougirait ; le commentaire et la docstring qui annoncaient la dette sont reecrits pour rester vrais |
| T-05-05 | Information disclosure | Valeurs volatiles de la base locale epinglees dans la prose | low | mitigate | Motif `\d{4,}` refuse dans la page (compteur, version, horodatage, taille) ; les chemins absolus du poste ne sont pas cites ; `DOFUS_SECRET_KEY` n'est cite dans aucune page (mesure : 0 occurrence dans `docs/` et `README.md`) |
| T-05-06 | Information disclosure | Liens et chemins cites par la page | low | mitigate | Aucun lien externe ; `problemes_liens` (garde existante) resout chaque lien interne, et la garde de la tache 1 exige que chaque chemin entre accents graves du bloc Source de verite existe depuis la racine ; les deux renvois de `docs/parcours-simplifie.md` ne deviennent des liens que parce que la cible existe desormais (D-86) |
| T-05-SC | Tampering | Installations de paquets | low | accept | Cette phase n'ajoute **aucune** dependance : `pyproject.toml` est inchange, aucun `pip install` n'est execute ; la porte de legitimite des paquets n'a aucun sujet ici (aucun `[ASSUMED]`, aucun `[SUS]`, aucun `[SLOP]`) |

**Hors perimetre** : publication, deploiement distant, synchronisation Dofusdude, suppression de donnees, achat. Aucune commande destructrice n'est executee ; `db clear` et `PURGE OUI` sont documentes au plan 05-02, jamais lances.
</threat_model>

<verification>
**Controles automatises** : `tests/test_docs_base_locale.py` (garde de cloture reecrite, page et index, valeurs fondatrices, deux defauts hors-ligne, champs par surface), `tests/test_docs_structure.py` et `tests/test_docs_parcours.py` inchanges dans leurs gardes et verts avec la reserve videe, la suite entiere, l'empreinte de `.data/dofus.sqlite3` autour de la suite, et **treize morsures** sur copie verte avant mutation : fichier de base renomme, categorie retiree, ligne d'index retiree, `H1` desaligne, import de `socket` injecte, `DEFAULT_DATA_DIR` passe comme `data_dir`, confirmation destructive postee, defaut web inverse, defaut CLI force, phrase d'aide du web remplacee, `Entrées :` renomme cote CLI, `ENTREES :` renomme cote web, champ par categorie rendu inconditionnel.

**Non automatisable, nomme comme tel** : la prose libre de la page (l'invitation ou non a lancer une commande destructrice) et le rendu Markdown sur un service distant. Ces deux points sont declares en `backstop` dans `must_haves.truths` et ne sont **pas** revendiques comme verifies. Aucun blocage : rien de cette phase ne depend d'un geste humain, d'un secret ou d'un reseau.

**References de verification de phase** : `05-VALIDATION.md` § Per-Task Verification Map assigne les lignes `fichier_categories`, `fenetre_24h`, `hors_ligne`, `champs_etat`, `sommaire` et `lien_wizard` a ce plan (vague 1) ; `/gsd:verify-work` rejouera la suite entiere et la mesure de non-regression de `.data/`.
</verification>

<success_criteria>
- `docs/base-locale.md` existe, est indexee, et decrit le fichier de la base, les sept categories stockees et la fenetre de re-check de 24 h depuis les constantes publiques du code, les deux defauts hors-ligne separes par surface et les champs de l'etat de la base par surface dans leur forme exacte (criteres de succes 1, 2 et 5 ; BASE-01, BASE-02 ; D-68 a D-76).
- La dette D-44/D-63 est levee dans le meme commit que la page, et les textes qui l'annoncaient sont remis en verite.
- La garde de cloture du nouveau module est **reecrite** : elle interdit les racines de risque, `main`, les suppressions, `DEFAULT_DATA_DIR` comme `data_dir` et la paire de confirmation, et **n'interdit plus** l'import de `dofus_stuff.database` dont la page a besoin (Pitfall 2).
- Treize derives epinglees font rougir la suite sur une copie verte avant mutation ; la suite entiere est verte et `.data/dofus.sqlite3` est intact.
</success_criteria>

<output>
Create `.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-01-SUMMARY.md` when done
</output>

## Artefacts produits par cette phase (inventaire tenu a jour par les trois plans)

| Type | Symbole | Plan |
|------|---------|------|
| page | `docs/base-locale.md` | 05-01 (creation), 05-02 (quatre sections) |
| ligne d'index | la ligne `[Base locale](base-locale.md)` du tableau de `docs/sommaire.md` | 05-01 |
| constante de dette | `PAGES_INEXISTANTES = ()` et ses commentaires remis en verite (`tests/test_docs_parcours.py`) | 05-01 |
| renvois | les deux renvois vers la base locale dans `docs/parcours-simplifie.md` (liens) | 05-01 |
| module de test | `tests/test_docs_base_locale.py` | 05-01 |
| constante de module | `TITRES_SECTION_ATTENDUS` | 05-01 (creation et completion), 05-02 (deux titres de plus) |
| helpers locaux | `_texte_page`, `_lignes_du_corps`, `_statut` | 05-01 |
| helpers locaux | `_routes_declarees` | 05-02 |
| helpers locaux | `renvois_morts`, `_empreinte` (troisieme copie assumee) | 05-03 |
| constantes de garde | `RACINES_INTERDITES`, `APPELS_SUPPRESSION`, `APPEL_PRODUIT`, `APPELS_SYNCHRO_PRODUIT`, `CONFIRMATIONS_INTERDITES`, `MOTIF_GARDE` | 05-01 |
| constantes de motif | `MOTIF_FICHIER`, `MOTIF_CATEGORIES`, `MOTIF_FENETRE`, `MOTIF_INDEX`, `MOTIF_H1`, `MOTIF_DEFAUTS`, `MOTIF_CHAMPS_CLI`, `MOTIF_CHAMPS_WEB`, `MOTIF_CONDITION`, `MOTIF_VOLATILES`, `MOTIF_CRLF`, `MOTIF_DATA_DIR`, `MOTIF_CONFIRMATION` | 05-01 |
| constantes de motif | `MOTIF_CREATION`, `MOTIF_ROUTE`, `MOTIF_REFUS`, `MOTIF_SYNCHRO_WEB`, `MOTIF_DESTRUCTRICES`, `MOTIF_EFFET`, `MOTIF_EXEMPLE` | 05-02 |
| constantes de motif | `MOTIF_README`, `MOTIF_LIEN_D63`, `MOTIF_SECTION`, `MOTIF_SOURCE`, `MOTIF_LIEN_EXTERNE`, `MOTIF_BASE_ABSENTE`, `MOTIF_EMPREINTE_CHANGEE` | 05-03 |
| constantes de contenu | `MOTIF_VOLATILE`, `MARQUES_DESTRUCTRICES`, `MARQUES_BASE`, `MARQUES_SAUVEGARDES`, `MARQUES_HORS_PARCOURS`, `COMMANDE_DESTRUCTRICE`, `JETON_PURGE`, `BALISE_COMMANDE`, `LIMITE_PERIMETRE` | 05-01 (`MOTIF_VOLATILE`), 05-02 (le reste) |
| constantes de contenu | `MOTIF_LIEN_MARKDOWN`, `CIBLES_EXTERNES`, `TITRE_SECTION_RENVOI`, `PAGE_PARCOURS`, `LIEN_RETOUR`, `TITRE_H1`, `BASE_LOCALE` | 05-03 |
| fonctions de test | `test_garde_de_cloture_du_harnais`, `test_page_et_index_de_la_base_locale`, `test_defauts_hors_ligne`, `test_champs_de_la_ligne_de_commande`, `test_champs_de_la_surface_web` | 05-01 |
| fonctions de test | `test_le_premier_contact_cree_la_base`, `test_le_refus_de_la_synchronisation`, `test_la_synchronisation_web_contacte_l_api`, `test_commandes_destructrices` | 05-02 |
| fonctions de test | `test_renvois_du_readme_resolus`, `test_renvoi_base_locale_legitime`, `test_page_close_et_sans_valeur_volatile`, `test_data_locale_non_modifiee_autour_des_rendus` | 05-03 |

## Couverture des sources (ce plan)

| Source | Element | Consomme par |
|--------|---------|--------------|
| ROADMAP § Phase 5 | Critere 1 (fichier, categories stockees, fenetre de 24 h) | tache 1 |
| ROADMAP § Phase 5 | Critere 2 (deux defauts hors-ligne distingues, champs par leurs noms) | taches 2 et 3 |
| ROADMAP § Phase 5 | Critere 5 (chaque nom cite produit par le code, suite verte) | tache 1 (valeurs fondatrices) et taches 2 et 3 (defauts et champs) |
| ROADMAP § Phase 5 | Brief du plan 05-01 (rediger la page et son entree d'index) | tache 1 (tranche verticale, index, reserve), taches 2 et 3 (expansion) |
| REQUIREMENTS | BASE-01, BASE-02 | `requirements` du plan |
| RESEARCH | Inventaire mesure : `DB_NAME`, `DEFAULT_DATA_DIR`, tables et index, cles de `meta`, `ITEM_KINDS` egal aux kinds de `SYNC_SOURCES`, `CHECK_INTERVAL_SECONDS` | tache 1 |
| RESEARCH | § 5 et § 6 (defauts des deux parseurs, phrases d'aide verbatim, ecart reel des deux surfaces hors-ligne) | tache 2 |
| RESEARCH | § 7 (les cinq champs par surface, les trois ecarts mesures d'accentuation et de casse) | tache 3 |
| RESEARCH | Pitfall 1 (`PAGES_INEXISTANTES` rougit des que la page existe), Pitfall 2 (garde `ast` recopiee), Pitfall 3 (champ conditionnel), Pitfall 4 (deux surfaces, deux jeux de chaines), Pitfall 5 (aide coupee a la largeur du terminal) | tache 1 (Pitfall 1 et 2), tache 2 (Pitfall 5), tache 3 (Pitfall 3 et 4) |
| RESEARCH | § Feasibility verdict : mecanismes 1, 2, 3, 4 et 5, et la chose a ne jamais faire (ouvrir ou vider la base reelle) | taches 1 a 3 |
| RESEARCH | § Wave 0 Gaps : `tests/test_docs_base_locale.py`, `docs/base-locale.md`, la ligne d'index, la reduction de la reserve | tache 1 |
| CONTEXT | D-68, D-69, D-70 (page source unique, gabarit, une ligne d'index) | tache 1 |
| CONTEXT | D-71, D-72 (deux defauts nommes par surface, drapeaux et aides lus dans le code) | tache 2 |
| CONTEXT | D-73, D-74 (fenetre depuis la constante, fichier et categories depuis le code) | tache 1 |
| CONTEXT | D-75, D-76 (champs produits par le code, aucun champ non ancrable) | tache 3 |
| CONTEXT | D-82, D-83, D-84, D-85 (module dedie, constats localisants, morsures sur copie verte, limites declarees) | tache 1 (module et garde) et les trois batteries de morsures |
| CONTEXT | D-86 (renvois par lien la ou la cible existe) | tache 1 (les deux renvois de `docs/parcours-simplifie.md`) |
| CONTEXT | D-88, D-89, D-90, D-91 (code non modifie, aucune resynchronisation, conventions des phases 1 a 4, commits locaux) | toutes les taches |

**Limites de la phase, delimitees explicitement (Pitfall 8, A3)** : le critere 4 (« aucune commande destructrice presentee comme une etape ») est applique **au perimetre de cette phase** — la page `docs/base-locale.md` et son module. L'occurrence de `README.md:80`, seule du depot ou une commande destructrice figure dans un bloc de commandes sans avertissement, est **hors du mandat de reecriture** de cette phase (D-87 limite le travail du README a la resolution de ses renvois) ; elle est consignee ici pour la phase 6, proprietaire de la completude. Le plan 05-02 l'enregistre de nouveau dans sa section de couverture.

**Audit de complétude de la phase (les quatre types de sources)** : GOAL — les cinq criteres du ROADMAP sont couverts par les taches de ce plan (1, 2 et 5) et des plans 05-02 (3 et 4) et 05-03 (4 lu sans execution, et 5) ; REQ — `BASE-01` et `BASE-02` ici, `BASE-03` dans les plans 05-02 et 05-03 ; RESEARCH — les treize mesures de l'inventaire, les onze pieges et le verdict de faisabilite sont repartis sur les trois plans, aucun piege n'est laisse sans plan ; CONTEXT — D-68 a D-91 sont reparties sur les trois plans. Exclusions assumees, conformes aux « Deferred Ideas » de `05-CONTEXT.md` : la liste epinglee des pages et la preuve de completude (phase 6), la page de depannage et le glossaire (phase 6), toute resynchronisation Dofusdude (hors perimetre).
