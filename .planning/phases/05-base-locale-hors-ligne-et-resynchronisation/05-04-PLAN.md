---
phase: 05-base-locale-hors-ligne-et-resynchronisation
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - docs/base-locale.md
  - tests/test_docs_base_locale.py
autonomous: true
gap_closure: true
requirements:
  - BASE-01
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Les conditions reelles du declencheur sont lues dans `dofus_stuff/sync.py` et non resumees : `needs_check = force or empty or last_checked is None or (now - last_checked) >= CHECK_INTERVAL_SECONDS` (`sync.py:32`, mesure) precede le premier contact `remote_version = fetch_version(timeout=timeout)` (`sync.py:51`) ; la page enumere donc les quatre situations — fenetre ecoulee, base vide, aucun dernier controle enregistre, `--force-sync` — et n'en presente plus une seule comme le declencheur unique. C'est le critere de succes 1 du ROADMAP (BASE-01) et le gap exact declare par `05-VERIFICATION.md` (lignes 39 et 41)."
    - "Le declencheur est **mesure**, pas seulement cite : six cas joues sur des bases construites sous `tmp_path`, avec `dofus_stuff.sync.fetch_version` et `dofus_stuff.sync.pull_all` remplaces par des doubles compteurs avant l'appel, rendent 0 contact pour une base remplie a fenetre fraiche (retour `skip`/`within_24h`, `sync.py:33-39`), 1 contact pour une fenetre ecoulee (`sync.py:32`), 1 pour une base vide a fenetre fraiche (`empty`, meme ligne), 1 pour une base jamais controlee (`last_checked is None`, meme ligne), 1 pour `--force-sync` (`force`, meme ligne) et 0 pour une base vide en `--offline` (`sync.py:41-43`). Le module exige ces six mesures avant de se prononcer sur la page."
    - "La page nomme chaque condition dont le contact a ete **mesure** : pour chaque cas dont le compteur est superieur a zero, la section « La fenêtre de re-check de 24 heures » porte au moins une marque de son jeu (fenetre ecoulee, base vide, aucun dernier controle, `--force-sync`) ; et pour le cas dont le compteur vaut zero (base remplie, fenetre fraiche, sans `--force-sync`), la section dit que le chargement s'arrete la et qu'aucune requete reseau n'est emise. Mesure du planificateur sur la page livree par 05-03 : la marque « aucun dernier controle » est **absente** de la section — le nouveau controle y est donc rouge avant la tache 1, et la phrase fausse de la verification initiale ne peut plus rester verte."
    - "Aucune marque d'exclusivite de la fenetre ne subsiste dans la section, et cette prohibition est **justifiee par la mesure** : c'est parce qu'un contact a ete mesure sur une base vide a fenetre fraiche que les formes « seulement dans ce cas », « uniquement dans ce cas », « dans ce cas seulement », « la seule situation » et « la seule condition » sont refusees dans cette section. Mesure du planificateur sur la page livree : deux de ces formes y figurent (lignes 39 et 41 de `05-VERIFICATION.md`), le controle y est donc rouge avant la tache 1."
    - "L'affirmation qu'une base absente n'est pas une erreur est bornee et ancre a la mesure : `Catalog.load(offline=True)` sur une base vide leve `RuntimeError(« Base locale vide et --offline : impossible de synchroniser »)` (`sync.py:43`) avec un compteur de contact a 0 ; la section « Le fichier de la base », qui porte cette affirmation, nomme donc la ligne de commande, et la section « Le mode hors-ligne de la ligne de commande » continue de dire qu'une base vide arrete la commande sur une erreur. La contradiction interne relevee entre les lignes 9 et 61 de la verification initiale est fermee des deux cotes."
    - "Le nouveau controle **mord** sur une copie verte avant mutation : reintroduire une clause d'exclusivite dans la section de la fenetre rougit avec le motif `absolu sur le declencheur`, retirer la marque du declencheur « aucun dernier controle » rougit avec le motif `declencheur du controle de version`, et retirer le bornage de l'affirmation de la base absente rougit avec le motif `base absente presentee comme sans erreur`. Les trois copies sont mesurees vertes sur `tests/test_docs_base_locale.py` avant leur mutation, aucune ne se solde par `MUTATION NON DETECTEE`."
    - "Aucun controle existant n'est affaibli et la garde de cloture est **renforcee** : `CIBLES_DATA_DIR` gagne `load`, de sorte que le `data_dir` du nouvel appel `Catalog.load` est verifie comme ceux de `Database` et de `create_app` ; `RACINES_INTERDITES` (dont `socket` et `urllib`), `APPEL_PRODUIT`, `APPELS_SYNCHRO_PRODUIT` et l'interdiction des paires de confirmation restent inchanges, et le module garde ses treize controles verts en plus du nouveau."
    - statement: "La formulation de la prose au-dela des marques epinglees reste un jugement : le controle exige la presence des conditions mesurees et l'absence des marques d'exclusivite de la famille epinglee, il ne lit pas le sens des phrases. Une exclusivite formulee autrement que par ces marques reste hors d'atteinte. Backstop explicite, jamais un vert silencieux (D-85)."
      verification: backstop
    - statement: "Le contact reel de l'API Dofusdude n'est jamais exerce : les deux points d'entree reseau de `dofus_stuff/sync.py` sont remplaces avant l'appel et la garde de cloture du module refuse les racines `socket`, `urllib`, `http` et `requests`. Le controle prouve la **decision** de `sync.py`, pas qu'un appel Dofusdude reussirait. Backstop explicite (D-89, D-85)."
      verification: backstop
    - statement: "La portabilite de l'assertion CRLF hors d'un poste dont `core.autocrlf` vaut `true` reste non prouvee (aucun `.gitattributes` dans le depot, limite AR-5) : elle n'est ni revendiquee ni rouverte par ce plan. Backstop explicite."
      verification: backstop
  artifacts:
    - path: "docs/base-locale.md"
      provides: "Les trois phrases rendues vraies : les quatre conditions du declencheur enumerees dans la section de la fenetre, le cas de non-contact dit pour ce qu'il est, et l'affirmation de la base absente bornee par la limite de la ligne de commande"
      contains: "En dehors de ces quatre situations"
      min_lines: 141
    - path: "tests/test_docs_base_locale.py"
      provides: "Le controle du declencheur (`test_declencheur_du_controle_de_version`), la mesure `_contacts_mesures` sur bases temporaires avec les doubles de reseau, les motifs `MOTIF_DECLENCHEUR`, `MOTIF_ABSOLU_DECLENCHEUR` et `MOTIF_SANS_ERREUR`, les marques des quatre conditions et de la non-erreur bornee, et `CIBLES_DATA_DIR` etendu a `load`"
      contains: "def test_declencheur_du_controle_de_version"
      min_lines: 2249
  key_links:
    - from: "tests/test_docs_base_locale.py"
      to: "dofus_stuff/sync.py"
      via: "mesure des quatre branches de `needs_check` (`sync.py:32`) et de la garde hors-ligne (`sync.py:41-43`) sur des bases temporaires, les deux points d'entree reseau etant remplaces par des doubles compteurs avant tout appel"
      pattern: "fetch_version"
    - from: "docs/base-locale.md"
      to: "dofus_stuff/sync.py"
      via: "la section de la fenetre enumere les conditions dont le contact a ete mesure, et refuse les marques d'exclusivite que la mesure contredit"
      pattern: "--force-sync"
    - from: "tests/test_docs_base_locale.py"
      to: "docs/base-locale.md"
      via: "lecture de la section de la fenetre et de la section du fichier, marques normalisees et constats accumules portant les trois motifs, chacun nomme pour la morsure qui le cherche"
      pattern: "base-locale.md"
  prohibitions:
    - statement: "Ne jamais modifier `dofus_stuff/**` pour faire dire a la page ce qu'elle veut : le code est la reference et la page s'y conforme (D-88, D-19). Le seul fait admis sur `dofus_stuff/**` est la lecture."
      status: resolved
      verification: judgment
    - statement: "Ne jamais ouvrir, lire par SQLite, ecrire ou vider `.data/dofus.sqlite3` : toutes les bases du nouveau controle sont construites sous `tmp_path`, `DEFAULT_DATA_DIR` n'est jamais passe a `Database`, `create_app` ni `Catalog.load`, et la seule operation admise sur le chemin reel est la lecture d'octets pour l'empreinte (D-81, D-89)."
      status: resolved
      verification: test
    - statement: "Ne jamais laisser le controle atteindre le reseau : `dofus_stuff.sync.fetch_version` et `dofus_stuff.sync.pull_all` sont remplaces avant l'appel, la garde de cloture refuse les imports de `socket`, `urllib`, `http`, `requests` et apparentes, et aucune commande de synchronisation n'est jamais executee (D-81, D-89)."
      status: resolved
      verification: test
    - statement: "Ne jamais affaiblir un controle existant pour obtenir le vert : `APPELS_SYNCHRO_PRODUIT`, `RACINES_INTERDITES`, `APPEL_PRODUIT`, `CONFIRMATIONS_INTERDITES`, les marques et les motifs des vagues 1 a 3 restent tels quels ; la garde gagne `load` dans `CIBLES_DATA_DIR`, jamais une exception en moins."
      status: resolved
      verification: judgment
    - statement: "Ne jamais presenter comme prouvee une phrase dont l'ancrage n'existe pas : ce qui ne peut pas etre ancre automatiquement est declare en `backstop` et nomme comme tel (D-85)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais publier, deployer a distance, pousser vers un depot distant ni effectuer d'achat : commits locaux seulement, indexation par chemin explicite, jamais `git add .` (D-91)."
      status: resolved
      verification: judgment
  assumptions:
    - "**Sonde d'arete spec-less : l'orchestrateur n'a PAS execute la sonde deterministe de la section A** (aucun SPEC de phase ; replanification de fermeture de gap). Les aretes de ce plan sont derivees du gap mesure de `05-VERIFICATION.md` (critere 1, BASE-01 : `file:line` 39, 41 et 9), du critere de succes 1 du ROADMAP et des decisions D-73, D-74, D-83, D-85, D-88, D-89 et D-91 ; aucune arete n'est derivee d'une decision produit nouvelle."
    - "Les conditions de declenchement sont **lues par le planificateur dans `dofus_stuff/sync.py`** (`sync.py:11`, `:27-32`, `:33-39`, `:41-43`, `:51`, `:53-56`, `:66`) et **mesurees** sur six cas temporaires par le planificateur lui-meme : base remplie a fenetre fraiche → 0 contact ; fenetre ecoulee → 1 ; base vide a fenetre fraiche → 1 et `pull_all` 1 ; base jamais controlee → 1 ; `--force-sync` a fenetre fraiche → 1 et `pull_all` 1 ; base vide en `--offline` → 0 contact et `RuntimeError`. Les valeurs attendues du controle sont ces mesures, jamais une lecture de la prose de la page."
    - "Le controle mesure le declencheur a travers `Catalog.load(data_dir=..., offline=..., force_sync=..., quiet=True)` (`dofus_stuff/catalog.py:33-52`), la surface que la page decrit sous le nom de « chargement du catalogue », et non par un appel direct a `ensure_up_to_date` : la garde de cloture du module refuse nommement ce dernier (`APPELS_SYNCHRO_PRODUIT`), et cet interdit n'est pas leve. `Catalog.load` traverse la meme decision, avec les deux points d'entree reseau remplaces avant l'appel — mesure du planificateur, aucun socket ouvert, aucune synchronisation executee."
    - "`CIBLES_DATA_DIR` gagne `load` : le nouvel appel passe `data_dir=tmp_path / <nom de cas>`, donc la garde ne produit aucun constat, et la propriete « aucun `data_dir` hors de `tmp_path` » reste vraie **aussi** sur ce nouveau chemin d'appel. C'est un resserrement, jamais un adoucissement ; le module n'avait aucun appel `.load(` avant ce plan (mesure)."
    - "Les trois morsures sont jouees sur une copie en repertoire temporaire, avec des mutations qui **preservent les CR** (lecture et ecriture d'octets par `pathlib`, jamais `sed -i` sur la page : le `sed` de Git-for-Windows convertit le fichier en LF et ferait rougir le controle des fins de ligne pour une raison etrangere a la mutation). Les trois mutations sont ecrites en ASCII dans la commande de verification, les caracteres accentues des ancres etant exprimes par des echappements `\\uXXXX` : aucune question d'encodage du shell sur les ancres francaises."
    - "La copie de morsure n'embarque pas `.data/` : le module y mesure alors 14 tests dont 1 `skip` **nomme** (`base locale du depot absente`) et reste vert avant mutation, comme les batteries des plans 05-01 a 05-03 le mesurent deja (limite declaree du module, jamais un faux vert)."
    - "Douze decisions de contexte portent ce plan : D-73 et D-74 (la fenetre et le fichier documentes depuis le code, sans valeur volatile), D-83 (constats localisants), D-84 (morsures sur copie verte avant mutation), D-85 (limites declarees), D-86 et D-69 (gabarit et renvois), D-88 (code non modifie), D-89 (aucune resynchronisation Dofusdude), D-90 (conventions des phases 1 a 4) et D-91 (commits locaux indexes par chemin)."
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du critere de succes 1 du ROADMAP, de BASE-01 et du gap mesure de `05-VERIFICATION.md` :
**En tant que** porteur du projet qui relit la documentation, **je veux** que la page de la base locale dise exactement quand l'outil repart en ligne — et qu'un controle le voie, **afin de** ne plus jamais dependre d'une phrase que la suite ne lit pas.

Mode MVP : le `**Goal:**` du ROADMAP n'est pas redige en recit utilisateur ; le cadrage est **derive**, comme aux plans 04-01 a 05-03, et ne reformule que des criteres et des decisions verrouillees.

Purpose : fermer le **seul** gap de la phase 5. La verification independante a rendu `gaps_found` : les criteres 2, 3, 4 et 5 sont verifies, le critere 1 echoue parce que la prose de la section « La fenêtre de re-check de 24 heures » affirme deux absolus faux (`docs/base-locale.md:39` et `:41`) contredits par `dofus_stuff/sync.py:32` et mesures par le verificateur : une base **vide** avec une fenetre **fraiche** contacte l'API (`fetch_version` appele, `action` valant `sync`), et `--force-sync` fait de meme. La cause profonde n'est pas la phrase, c'est **le harnais** : la section n'etait controlee que sur la constante, son expression et l'absence de valeur volatile, la semantique du declencheur n'etait couverte par aucun test — une phrase fausse y restait verte. Ce plan repare les deux : la page dit les quatre conditions reelles, et un controle mesure ces conditions sur le code avant d'exiger que la page les nomme.

Output : les trois phrases de `docs/base-locale.md` rendues vraies (section de la fenetre et section du fichier), le controle `test_declencheur_du_controle_de_version` ajoute a `tests/test_docs_base_locale.py` (mesure des six cas, correspondance avec la page, prohibition des marques d'exclusivite, bornage de l'affirmation de la base absente), la garde de cloture renforcee (`CIBLES_DATA_DIR` gagne `load`), et les trois morsures qui prouvent que le nouveau controle mord.

Ordonnancement declare : **vague 4**, `depends_on: [05-03]` — la page doit exister et etre close avant d'etre corrigee, et le module doit contenir ses treize controles avant d'en recevoir un quatorzieme. Les deux taches touchent deux fichiers distincts mais la tache 2 **depend** de la tache 1 : ses morsures rejouent les phrases fautives sur une copie de la page corrigee et exigent que le controle les voie.

**Ce que ce plan ne fait pas** : il ne rouvre **aucun** des criteres 2 a 5 (deja verifies : les deux defauts hors-ligne, les cas non evidents, les commandes destructrices, l'ancrage des noms et la suite verte) ; il n'ajoute aucune section a la page et ne touche ni `docs/sommaire.md`, ni `docs/parcours-simplifie.md`, ni `README.md` ; il ne modifie aucun fichier de `dofus_stuff/**` (D-88) ; il ne lance aucune synchronisation Dofusdude (D-89) ; il n'ajoute aucune dependance et ne publie rien (D-91).
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-VERIFICATION.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-CONTEXT.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-01-PLAN.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-02-PLAN.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-03-PLAN.md
@.claude/CLAUDE.md
@docs/base-locale.md
@tests/test_docs_base_locale.py
@tests/conftest.py
@dofus_stuff/sync.py
@dofus_stuff/catalog.py
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : la page dit les quatre conditions reelles du declencheur, et borne la base absente</name>
  <files>docs/base-locale.md</files>
  <reversibility rating="reversible">Reecrire trois phrases d'une page de documentation est annulable par un commit de retour ; aucune commande du produit n'est executee, aucun fichier de `dofus_stuff/**` n'est touche et aucune base reelle n'est ouverte.</reversibility>
  <read_first>
    docs/base-locale.md
    dofus_stuff/sync.py
    dofus_stuff/catalog.py
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-VERIFICATION.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
    tests/test_docs_base_locale.py
  </read_first>
  <action>Rendre la page vraie en la conformant au code, jamais l'inverse : les trois phrases fautives sont remplacees par ce que `dofus_stuff/sync.py` fait reellement. **Aucun fichier de `dofus_stuff/**` n'est modifie** (D-88) : le code est lu, jamais aligne sur la page.

**Les conditions reelles, lues dans `dofus_stuff/sync.py` (l'oracle, a citer d'apres le fichier et jamais d'apres un resume) :** la porte `if not needs_check:` s'appuie sur `needs_check = force or empty or last_checked is None or (now - last_checked) >= CHECK_INTERVAL_SECONDS` (`sync.py:32`), et elle precede le **premier contact** `remote_version = fetch_version(timeout=timeout)` (`sync.py:51`). Un contact a donc lieu dans **quatre** situations : la fenetre est ecoulee (`now - last_checked >= CHECK_INTERVAL_SECONDS`), la base ne porte aucun objet (`empty`, mesure par `Database.is_empty`), aucun dernier controle n'a ete enregistre (`last_checked is None`), ou `force` est vrai (`--force-sync`). Le seul cas ou le chargement du catalogue s'arrete **avant** tout contact est l'inverse de ces quatre conditions (`sync.py:33-39`, retour `skip` avec `reason` valant `within_24h`). Le mode hors-ligne ne change pas ces conditions : il est examine **apres** la porte de la fenetre (`sync.py:41-50`) et, sur une base vide, il leve `RuntimeError` avec le message `Base locale vide et --offline : impossible de synchroniser` (`sync.py:43`) — aucun contact dans ce cas non plus. Quand le contact a lieu, le comportement reel est : versions identiques, base non vide et pas de `force` → le nouvel instant du controle est note (`sync.py:57-62`) ; versions differentes, ou base vide, ou `force` → tout le catalogue est recupere (`sync.py:66-69`).

**1. Section `## La fenêtre de re-check de 24 heures`** — le paragraphe qui presente la fenetre comme l'unique declencheur et qui affirme qu'aucune requete n'est emise tant qu'elle n'est pas ecoulee est remplace par l'enumeration des quatre conditions reelles, suivie du cas de non-contact. Le paragraphe corrige porte **exactement** ces deux phrases, dans cet ordre : « En ligne, l'outil compare la version du jeu distante à la version locale dans quatre situations : la fenêtre de 24 heures est écoulée, la base locale ne porte encore aucun objet, aucun dernier contrôle n'a été enregistré, ou l'option `--force-sync` a été passée. En dehors de ces quatre situations — une base remplie, un dernier contrôle enregistré et une fenêtre non écoulée —, le chargement du catalogue s'arrête là : aucune requête réseau n'est émise. » Aucune marque d'exclusivite ne subsiste dans la section : ni « seulement », ni « uniquement », ni « dans ce cas seulement », ni « la seule situation », ni « la seule condition » appliques a la fenetre ou au declencheur — la mesure du code les contredit tous, un contact ayant lieu sur une base vide a fenetre fraiche et avec `--force-sync`. Le paragraphe suivant ne decrit plus que la **consequence** de la comparaison (versions identiques → l'instant du controle est note ; versions differentes, ou base vide, ou `--force-sync` → tout le catalogue est recupere) et ne fait plus dependre la comparaison de la seule fenetre ecoulee. Le premier paragraphe de la section (la cle `last_checked_at`, la constante `CHECK_INTERVAL_SECONDS` et son expression `24 * 60 * 60`) et le paragraphe sur `--force-sync` restent inchanges : ils sont exacts.

**2. Section `## Le fichier de la base`** — la phrase qui affirme qu'une base absente n'est pas une erreur est **bornee**, parce que la mesure la contredit sur un parcours precis : en mode hors-ligne, charger le catalogue de la ligne de commande sur une base vide leve `RuntimeError` (`sync.py:43`, `dofus_stuff/cli.py:430-433` imprime l'erreur et sort en 1). La phrase corrigee affirme l'absence d'erreur pour le premier contact et pour l'ecran web, puis nomme la limite de la ligne de commande ; elle contient **exactement** le fragment : « La ligne de commande y ajoute une limite, décrite plus bas : charger le catalogue en mode hors-ligne sur une base vide l'arrête sur une erreur. » Elle ne contredit pas la section `## Le mode hors-ligne de la ligne de commande`, qui decrit deja ce cas et reste inchangee.

**Contraintes de forme (le gabarit est verrouille, D-69, et les controles des vagues 1 a 3 restent armes) :** editer **en place**, a l'interieur des paragraphes existants, sans introduire ni retirer aucun titre de niveau 2 (`TITRES_SECTION_ATTENDUS` reste exactement la liste des douze titres de la page), sans ajouter de saut de ligne, sans aucun lien externe, sans aucun nombre de quatre chiffres ou plus (`24 heures` et `24 * 60 * 60` restent les seules durees, elles sont deja dans la page), et la derniere ligne non vide reste `[Retour au sommaire](sommaire.md)`. Les remplacements ne doivent pas toucher les retours chariot : si l'edition introduit un saut de ligne en LF, le controle de cloture de la page (`test_page_close_et_sans_valeur_volatile`, vague 3) rougit — mesurer alors le compte de fins de ligne et de retours chariot (141 et 141 avant l'edition) et retablir le CRLF sur les lignes touchees.

**Ce que cette tache ne prouve pas encore** : la verite des phrases n'est pas prouvee par sa propre relecture. La tache 2 ajoute le controle qui la mesure ; le planificateur a mesure que ce controle est **rouge** sur la page livree par 05-03, pour trois raisons independantes (la marque du declencheur « aucun dernier controle » est absente de la section, deux marques d'exclusivite y figurent, et la section du fichier affirme l'absence d'erreur sans nommer la ligne de commande). C'est ce defaut que la tache 1 ferme.</action>
  <acceptance_criteria>
    - La section `## La fenêtre de re-check de 24 heures` porte le paragraphe corrige et ses deux phrases, cite toujours `CHECK_INTERVAL_SECONDS`, son expression `24 * 60 * 60` et `--force-sync`, et n'a plus aucune marque d'exclusivite de la fenetre.
    - La section `## Le fichier de la base` borne l'affirmation de la base absente par le fragment « La ligne de commande y ajoute une limite » et ne contredit plus la section de la ligne de commande.
    - La page garde exactement ses douze titres de niveau 2, sa ligne de retour en derniere ligne non vide, 141 ou davantage de lignes CRLF, aucun BOM, aucun lien externe et aucun nombre de quatre chiffres ou plus.
    - Aucun fichier de `dofus_stuff/**` n'est modifie (D-88) : la page se conforme au code lu, jamais l'inverse.
    - Les treize controles existants du module restent verts et la suite entiere reste verte avec l'interpreteur epingle.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; "$PY" -c "import pathlib,re;t=pathlib.Path('docs/base-locale.md').read_bytes().decode('utf-8');f=re.search(r'^## La fen\u00eatre de re-check de 24 heures\r?\n(?P<c>.*?)^## ',t,re.M|re.S);assert f is not None,'section de la fenetre introuvable';c=f.group('c');attendus=('dans quatre situations','la base locale ne porte encore aucun objet','aucun dernier contr\u00f4le n\'a \u00e9t\u00e9 enregistr\u00e9','--force-sync','En dehors de ces quatre situations','aucune requ\u00eate r\u00e9seau n\'est \u00e9mise');manquants=[a for a in attendus if a not in c];assert not manquants,'fragments absents de la section de la fenetre : %r'%manquants;g=re.search(r'^## Le fichier de la base\r?\n(?P<c>.*?)^## ',t,re.M|re.S);assert g is not None,'section du fichier introuvable';assert 'La ligne de commande y ajoute une limite' in g.group('c'),'la limite de la ligne de commande n est pas nommee dans la section du fichier';print('page : les quatre conditions mesurees du declencheur sont enumerees, le cas de non-contact est dit, et la base absente est bornee par la limite de la ligne de commande')"</automated>
    <fails_when>exit non-zero, ou une ligne "AssertionError", "introuvable" ou "fragments absents de la section de la fenetre"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; "$PY" -c "import pathlib,hashlib;b=pathlib.Path('docs/base-locale.md').read_bytes();assert b.count(b'\r\n')==len(b.splitlines()),'fins de ligne non homogenes : %d CR pour %d lignes'%(b.count(b'\r\n'),len(b.splitlines()));assert not b.startswith(b'\xef\xbb\xbf'),'BOM inattendu';print('page : %d lignes, %d CR, aucun BOM'%(len(b.splitlines()),b.count(b'\r\n')))"</automated>
    <fails_when>exit non-zero, ou une ligne "fins de ligne non homogenes" ou "BOM inattendu"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; avant="$(git diff --name-only HEAD -- dofus_stuff)"; "$PY" -m pytest -q; statut=$?; apres="$(git diff --name-only HEAD -- dofus_stuff)"; if [ "$statut" -ne 0 ]; then echo "SUITE COMPLETE ROUGE (exit $statut)"; exit 1; fi; if [ -n "$avant$apres" ]; then echo "CODE MODIFIE (D-88) : $avant$apres"; exit 1; fi; echo "suite complete verte et code de production inchange (D-88)"</automated>
    <fails_when>exit non-zero, ou une ligne "SUITE COMPLETE ROUGE" ou "CODE MODIFIE (D-88)", ou une ligne contenant "failed" ou "error"</fails_when>
  </verify>
  <done>La page enonce les quatre conditions reelles du declencheur lues dans `dofus_stuff/sync.py:32`, dit le cas de non-contact pour ce qu'il est, borne l'affirmation de la base absente par la limite de la ligne de commande mesuree (`sync.py:43`), garde son gabarit et ses octets CRLF ; les treize controles existants et la suite entiere restent verts, et aucun fichier de `dofus_stuff/**` n'est touche.</done>
</task>

<task type="auto">
  <name>Tache 2 : le controle mesure le declencheur reel, exige que la page l'enumere, et mord</name>
  <files>tests/test_docs_base_locale.py</files>
  <read_first>
    tests/test_docs_base_locale.py
    tests/conftest.py
    docs/base-locale.md
    dofus_stuff/sync.py
    dofus_stuff/catalog.py
    dofus_stuff/database.py
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-VERIFICATION.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
  </read_first>
  <action>Ajouter au module d'ancrage le controle qui **mesure** la decision de `dofus_stuff/sync.py` avant de se prononcer sur la page — la section de la fenetre ne doit plus reposer sur la seule constante et son expression.

**Imports (aucun `from __future__ import annotations` : le module documente en tete de fichier pourquoi il ne l'a pas, et le placer apres la docstring tuerait la collection)** : ajouter `import time` et `from dofus_stuff.catalog import Catalog` aux imports existants. Ni `socket`, ni `urllib`, ni aucun module de `RACINES_INTERDITES` : la garde de cloture refuse ces racines.

**Constantes de motif (valeurs ASCII, sans apostrophe, chacune incluse dans le constat qui la concerne — regle des plans 03-03 et 05-03)** : `MOTIF_DECLENCHEUR = "declencheur du controle de version"`, `MOTIF_ABSOLU_DECLENCHEUR = "absolu sur le declencheur"`, `MOTIF_SANS_ERREUR = "base absente presentee comme sans erreur"`. Ces trois chaines sont exactement ce que la batterie de morsures cherche dans la sortie d'echec.

**Constantes de mesure et de contenu** : `VERSION_DISTANTE = "1.2.3.4"` (la version que rend le double de `fetch_version`) ; `MESSAGE_BASE_VIDE = "Base locale vide et --offline : impossible de synchroniser"` (le message reel de `sync.py:43`) ; `CAS_BASE_REMPLIE_FRAICHE = "base_remplie_fenetre_fraiche"`, `CAS_FENETRE_ECOULEE = "base_remplie_fenetre_ecoulee"`, `CAS_BASE_VIDE = "base_vide_fenetre_fraiche"`, `CAS_JAMAIS_CONTROLEE = "base_remplie_jamais_controlee"`, `CAS_FORCE = "base_remplie_fenetre_fraiche_force"`, `CAS_HORS_LIGNE = "base_vide_hors_ligne"` ; `CONTACTS_ATTENDUS` : dictionnaire de ces six noms vers les contacts attendus, mesures par le planificateur sur le code du depot — 0, 1, 1, 1, 1, 0 dans cet ordre (la derniere valeur est le cas hors-ligne, ou la garde de `sync.py:41-43` leve avant tout contact) ; `MARQUES_DECLENCHEURS` : dictionnaire associant a chaque cas **contacte** le jeu de marques dont **au moins une** doit apparaitre dans la section (fenetre ecoulee : `fenetre de 24 heures est ecoulee`, `fenetre est ecoulee`, `fenetre ecoulee` ; base vide : `base locale ne porte encore aucun objet`, `base locale est vide`, `base locale vide`, `base vide` ; jamais controlee : `aucun dernier controle`, `jamais ete controlee` ; force : `--force-sync`) ; `MARQUES_NON_CONTACT = ("s'arrete la", "s arrete la", "aucune requete reseau", "pas de requete reseau")` ; `ABSOLUS_DECLENCHEUR = ("seulement dans ce cas", "uniquement dans ce cas", "dans ce cas seulement", "la seule situation", "la seule condition")` ; `MARQUES_ABSENTE_SANS_ERREUR = ("n'est pas une erreur", "pas une erreur")` ; `MARQUES_LIMITE_CLI = ("ligne de commande", "--offline")` ; `MARQUES_VIDE_HORS_LIGNE = ("vide",)` ; `MARQUES_ERREUR_HORS_LIGNE = ("sur une erreur", "s'arrete", "echoue", "refuse")`. Comparer les marques de la page **apres la fixture `normalize`** (D-11), comme les controles des vagues 1 a 3.

**Helper de mesure `_contacts_mesures(tmp_path, monkeypatch) -> tuple[dict[str, int], str]`** : il rend le nombre de contacts reseau mesure par cas, et le message de l'exception du cas hors-ligne. Pour chaque cas, dans l'ordre de `CONTACTS_ATTENDUS` : construire un dossier `tmp_path / <nom du cas>` ; ouvrir une base sous ce dossier par `Database(data_dir=...)` ; la garnir d'**une** ligne (`replace_kind("equipment", [{"ankama_id": 7, "name": "temoin"}])`, l'entree portant un `ankama_id` entier, sans quoi `replace_kind` n'ecrit rien) et poser `set_meta(META_GAME_VERSION, VERSION_DISTANTE)` pour les cas « remplis » ; poser l'horodatage par `touch_checked_at(when)` — `time.time()` pour un cas a fenetre fraiche, `time.time() - CHECK_INTERVAL_SECONDS` pour la fenetre ecoulee, **rien** pour le cas jamais controle ; fermer la base ; puis installer les doubles par `monkeypatch.setattr("dofus_stuff.sync.fetch_version", double)` et `monkeypatch.setattr("dofus_stuff.sync.pull_all", double)` — les deux points d'entree **importes dans le module `dofus_stuff.sync`**, c'est donc la-bas que le remplacement porte —, chaque double incrementant un journal local et rendant `VERSION_DISTANTE` (pour `fetch_version`) ou un dictionnaire vide (pour `pull_all`) ; appeler ensuite `Catalog.load(data_dir=dossier, offline=<vrai pour le cas hors-ligne>, force_sync=<vrai pour le cas force>, quiet=True)` dans un `try`, compter les appels du journal, et conserver `str(exception)` quand la garde hors-ligne leve. Le `quiet=True` evite la sortie du produit dans le resume pytest. Chaque cas repart d'une base neuve : les cas ne partagent aucun etat.

**Test `test_declencheur_du_controle_de_version(docs_dir, section, normalize, tmp_path, monkeypatch)`**, en cinq blocs numerotes, constats accumules et **une seule** assertion finale :
  1. **la mesure existe et correspond au code** : exiger que la mesure rende exactement `CONTACTS_ATTENDUS` (un contact pour la fenetre ecoulee, la base vide, la base jamais controlee et `--force-sync` ; zero pour la base remplie a fenetre fraiche et pour le cas hors-ligne) et que le cas hors-ligne ait leve en portant `MESSAGE_BASE_VIDE` ; chaque ecart est un constat portant `MOTIF_DECLENCHEUR`, nommant le cas, la valeur mesuree, la valeur attendue et `SOURCE_SYNC:32` (ou `:43` pour le cas hors-ligne) ;
  2. **la page nomme chaque condition mesuree** : pour chaque cas de `MARQUES_DECLENCHEURS`, si la mesure rend au moins un contact, exiger qu'au moins une marque de son jeu apparaisse (normalisee) dans le corps de `## La fenêtre de re-check de 24 heures` ; constat portant `MOTIF_DECLENCHEUR`, nommant la condition, la marque attendue, la mesure (`fetch_version` appele 1 fois) et `SOURCE_SYNC:32` ;
  3. **la page dit aussi le cas de non-contact** : si la mesure du cas `CAS_BASE_REMPLIE_FRAICHE` rend zero contact, exiger qu'au moins une marque de `MARQUES_NON_CONTACT` apparaisse dans la meme section ; constat portant `MOTIF_DECLENCHEUR`, nommant la mesure (zero contact, retour `skip`/`within_24h`, `SOURCE_SYNC:33-39`) ;
  4. **aucune marque d'exclusivite, et la prohibition est justifiee par la mesure** : appliquer la prohibition **seulement si** la mesure du cas `CAS_BASE_VIDE` rend au moins un contact — c'est la mesure qui rend fausse toute affirmation d'exclusivite de la fenetre ; dans ce cas, exiger qu'aucune chaine de `ABSOLUS_DECLENCHEUR` n'apparaisse (normalisee) dans la section ; constat portant `MOTIF_ABSOLU_DECLENCHEUR`, nommant la chaine trouvee, la mesure contradictoire (`fetch_version` appele 1 fois sur une base vide a fenetre fraiche, `SOURCE_SYNC:32`) et le fait que la clause doit etre remplacee par l'enumeration des quatre conditions ;
  5. **l'affirmation de la base absente est bornee des deux cotes** : pour chaque section de la page qui porte une marque de `MARQUES_ABSENTE_SANS_ERREUR` (mesure du planificateur : `## Le fichier de la base`), exiger qu'elle porte au moins une marque de `MARQUES_LIMITE_CLI` ; exiger en outre que le corps de `## Le mode hors-ligne de la ligne de commande` porte au moins une marque de `MARQUES_VIDE_HORS_LIGNE` **et** au moins une marque de `MARQUES_ERREUR_HORS_LIGNE` ; constats portant `MOTIF_SANS_ERREUR`, nommant la section fautive, la mesure (`Catalog.load(offline=True)` sur une base vide leve `MESSAGE_BASE_VIDE` avec zero contact, `SOURCE_SYNC:43`) et ce qui est attendu ;
  - l'assertion finale joint les constats et nomme la page, la section et les fichiers producteurs (`SOURCE_SYNC`, D-83).

**Docstring du test (obligatoire)** : dire la mesure (six cas sur bases temporaires, les deux points d'entree reseau remplaces avant l'appel, `Catalog.load` et non `ensure_up_to_date` parce que la garde de cloture refuse ce dernier par son nom) et **declarer les limites honnetes** (D-85) : les formulations de la page au-dela des marques epinglees ne sont pas lues ; une exclusivite formulee autrement que par les chaines de `ABSOLUS_DECLENCHEUR` reste hors d'atteinte ; le contact reel de l'API Dofusdude n'est jamais exerce et rien de ce controle ne prouve qu'un appel Dofusdude reussirait.

**Garde de cloture renforcee, jamais affaiblie** : ajouter `load` a `CIBLES_DATA_DIR` (qui vaut aujourd'hui `("Database", "create_app")`) et adapter son commentaire — le `data_dir` du nouvel appel `Catalog.load` est desormais verifie comme les autres, et l'appel du module passe `tmp_path / <nom de cas>`, donc aucun constat n'est produit. Ne retirer **aucune** entree de `APPELS_SYNCHRO_PRODUIT`, `RACINES_INTERDITES`, `APPEL_PRODUIT`, `CONFIRMATIONS_INTERDITES` ni d'aucun jeu de marques ou de motifs existant : le module compte treize controles verts avant ce plan et quatorze apres.

**Morsures (la preuve que le controle mord)** : la batterie de verification joue trois mutations sur une copie en repertoire temporaire, chacune sur une copie **mesuree verte** sur le module seul avant sa mutation ; les trois payloads rejouent les formes exactes du defaut mesure par `05-VERIFICATION.md` (l'exclusivite de la fenetre, le declencheur non nomme, l'affirmation non bornee), et chacune doit faire apparaitre le motif nomme de son constat. Toute morsure qui rend `MUTATION NON DETECTEE` signifie que le controle est encore aveugle sur cette forme et doit etre corrige avant de rendre la tache.

Le module n'ecrit rien sous `.data/` : toutes les bases viennent de `tmp_path`, `DEFAULT_DATA_DIR` n'est jamais passe a `Database` ni a `Catalog.load`, et aucune connexion SQLite n'est ouverte sur le chemin reel de la base du depot.</action>
  <acceptance_criteria>
    - `_contacts_mesures` rend exactement les six mesures attendues (0, 1, 1, 1, 1, 0) sur des bases construites sous `tmp_path`, avec `dofus_stuff.sync.fetch_version` et `dofus_stuff.sync.pull_all` remplaces avant l'appel, et le cas hors-ligne leve en portant `Base locale vide et --offline : impossible de synchroniser`.
    - `test_declencheur_du_controle_de_version` est vert sur la page corrigee par la tache 1, et rouge sur la page livree par 05-03 pour au moins trois constats (marque du declencheur jamais controle absente, deux marques d'exclusivite presentes, affirmation de la base absente non bornee).
    - La correspondance est **mesuree vers page** : pour chaque cas dont la mesure rend un contact, la section de la fenetre porte au moins une marque de son jeu ; pour le cas dont la mesure rend zero contact, elle porte une marque de non-contact ; les quatre conditions mesurees sont donc exigees de la page, et aucune ne l'est par simple citation d'une constante.
    - La prohibition des marques d'exclusivite n'est appliquee que parce qu'un contact a ete mesure sur une base vide a fenetre fraiche ; les constats portent `MOTIF_DECLENCHEUR`, `MOTIF_ABSOLU_DECLENCHEUR` ou `MOTIF_SANS_ERREUR`, nomment la valeur fautive, la valeur attendue et le fichier producteur (D-83).
    - `CIBLES_DATA_DIR` contient `load` et aucun autre jeu de la garde n'est affaibli ; le module n'importe aucun module de `RACINES_INTERDITES`, n'appelle ni `main` ni les deux points d'entree de synchronisation, et ne poste aucune confirmation.
    - Les trois morsures sont detectees avec leur motif nomme, sur une copie verte avant mutation, sans jamais toucher l'arbre de travail : aucune ne se solde par `MUTATION NON DETECTEE`.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q` rend 14 controles verts (13 avant ce plan), la suite entiere reste verte, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256 autour de la suite complete.
    - La batterie mesure la copie verte sur le module seul avant chaque mutation, puis la suite complete apres : la commande automatisee des morsures dure de l'ordre de 20 a 30 secondes (cout assume par D-84).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest, ou un compteur de tests different de 14 passed</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in absolu_reintroduit declencheur_omis erreur_sans_borne; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in absolu_reintroduit) (cd "$T" && "$PY" -c "import pathlib;p=pathlib.Path('docs/base-locale.md');s=p.read_bytes().decode('utf-8');i=s.find('CHECK_INTERVAL_SECONDS');assert i>0,'ancre CHECK_INTERVAL_SECONDS absente';j=s.find('\r\n',i);assert j>0,'fin de ligne CRLF absente';p.write_bytes((s[:j+2]+'Quand la fenetre est ecoulee : et seulement dans ce cas, l outil compare la version distante.'+'\r\n'+s[j+2:]).encode('utf-8'))") ; motif='absolu sur le declencheur';; declencheur_omis) (cd "$T" && "$PY" -c "import pathlib;p=pathlib.Path('docs/base-locale.md');s=p.read_bytes().decode('utf-8');a='aucun dernier contr\u00f4le';assert a in s,'ancre du declencheur jamais controle absente';p.write_bytes(s.replace(a,'un contr\u00f4le ancien',1).encode('utf-8'))") ; motif='declencheur du controle de version';; erreur_sans_borne) (cd "$T" && "$PY" -c "import pathlib;p=pathlib.Path('docs/base-locale.md');s=p.read_bytes().decode('utf-8');a='La ligne de commande y ajoute une limite';assert a in s,'ancre du bornage absente';p.write_bytes(s.replace(a,'La commande y ajoute une limite',1).encode('utf-8'))") ; motif='base absente presentee comme sans erreur';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 05-04 (TACHE 2) EN ECHEC"; exit 1; fi; echo "morsures 05-04 tache 2 : 3/3 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION", "ancre" ou "MORSURES DU PLAN 05-04 (TACHE 2) EN ECHEC"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; mesure() { "$PY" -c "import hashlib,pathlib;p=pathlib.Path('.data/dofus.sqlite3');print(f'{p.stat().st_size}:{p.stat().st_mtime_ns}:{hashlib.sha256(p.read_bytes()).hexdigest()}' if p.exists() else 'base absente')"; }; avant="$(git diff --name-only HEAD -- dofus_stuff)"; mesure > "$T/avant.txt"; "$PY" -m pytest -q; statut=$?; mesure > "$T/apres.txt"; apres="$(git diff --name-only HEAD -- dofus_stuff)"; if [ "$statut" -ne 0 ]; then echo "SUITE COMPLETE ROUGE (exit $statut)"; exit 1; fi; if [ -n "$avant$apres" ]; then echo "CODE MODIFIE (D-88) : $avant$apres"; exit 1; fi; if diff -q "$T/avant.txt" "$T/apres.txt" >/dev/null; then echo "empreinte .data/ identique autour de la suite complete : $(cat "$T/apres.txt")"; else echo "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE : avant=$(cat "$T/avant.txt") apres=$(cat "$T/apres.txt")"; exit 1; fi</automated>
    <fails_when>exit non-zero, ou une ligne "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE", "SUITE COMPLETE ROUGE" ou "CODE MODIFIE (D-88)", ou une ligne contenant "failed" ou "error"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; "$PY" -c "import ast,pathlib;src=pathlib.Path('tests/test_docs_base_locale.py').read_text(encoding='utf-8');arbre=ast.parse(src);appeles=set();[appeles.add(n.func.attr if isinstance(n.func,ast.Attribute) else getattr(n.func,'id','')) for n in ast.walk(arbre) if isinstance(n,ast.Call)];interdits=[n for n in ('ensure_up_to_date','pull_all','main','remove','unlink','rmdir','rmtree') if n in appeles];assert not interdits,'appels interdits dans le module : %r'%interdits;assert 'load' in src.split('CIBLES_DATA_DIR = (')[1].split(')')[0],'CIBLES_DATA_DIR ne porte pas load';print('module : aucun appel interdit (ni ensure_up_to_date, ni pull_all, ni main, ni suppression) et CIBLES_DATA_DIR porte load')"</automated>
    <fails_when>exit non-zero, ou une ligne "appels interdits dans le module" ou "CIBLES_DATA_DIR ne porte pas load"</fails_when>
  </verify>
  <done>Le module mesure les six cas du declencheur sur des bases temporaires avec les deux points d'entree reseau remplaces, exige de la page qu'elle nomme chaque condition mesuree, refuse les marques d'exclusivite que la mesure contredit, borne l'affirmation de la base absente des deux cotes, et la garde de cloture est renforcee (`CIBLES_DATA_DIR` porte `load`) ; les trois morsures sont detectees avec leur motif nomme sur une copie verte avant mutation, le module rend 14 controles verts, la suite entiere reste verte et `.data/dofus.sqlite3` n'est pas touche.</done>
</task>

</tasks>

<threat_model>
**Surface** : une page de documentation editee en place, et un module de test qui gagne une mesure sur bases temporaires, trois motifs de morsure et un jeu de marques. Aucun reseau (les deux points d'entree de `dofus_stuff/sync.py` sont remplaces avant l'appel), aucun secret, aucune dependance ajoutee, aucun fichier de `dofus_stuff/**` modifie (D-88). La menace reelle est double : une page qui affirme une chose que le code ne fait pas, et un controle qui ne mesurerait rien tout en restant vert — le defaut meme que la verification de la phase a paye.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Spoofing (documentation qui ment) | `docs/base-locale.md`, section de la fenetre et section du fichier | high | mitigate | Les quatre conditions sont lues dans `sync.py:32` et enumerees dans la page ; le controle **mesure** ce declencheur sur six cas temporaires et exige la correspondance condition par condition ; toute marque d'exclusivite de la fenetre est refusee, et la prohibition est conditionnee a la mesure qui la justifie (morsure `absolu_reintroduit`, motif `absolu sur le declencheur`) |
| T-05-20 | Repudiation (faux vert) | `tests/test_docs_base_locale.py`, nouveau controle | high | mitigate | Le controle est prouve mordant par trois morsures jouees sur une copie **verte avant mutation**, chacune cherchant le motif nomme de son constat et le test vise figurant nommement dans les echecs ; une morsure non detectee fait echouer la batterie. Mesure du planificateur : le controle est rouge sur la page livree par 05-03, pour trois raisons independantes |
| T-05-21 | Tampering | `.data/dofus.sqlite3` | high | mitigate | Toutes les bases du nouveau controle sont construites sous `tmp_path` (`Database(data_dir=tmp_path / ...)`, `Catalog.load(data_dir=tmp_path / ...)`) ; `DEFAULT_DATA_DIR` n'est jamais passe ; la garde de cloture est **renforcee** (`CIBLES_DATA_DIR` gagne `load`) ; l'empreinte `(taille, mtime_ns, sha256)` est mesuree avant et apres la suite complete |
| T-05-22 | Elevation of privilege (contact reseau d'un controle) | `dofus_stuff.sync.fetch_version` et `pull_all` | high | mitigate | Les deux points d'entree importes dans `dofus_stuff.sync` sont remplaces par des doubles compteurs avant tout appel ; aucun import de `socket`, `urllib`, `http`, `requests` ou apparente n'est admis par `RACINES_INTERDITES` ; aucune commande de synchronisation du produit n'est jamais appelee par son nom (`APPELS_SYNCHRO_PRODUIT` reste arme) |
| T-05-23 | Tampering | `dofus_stuff/**` (le code est l'oracle) | high | mitigate | Aucun fichier de `dofus_stuff/**` n'est modifie : la page se conforme au code lu (D-88) ; la verification de chaque tache exige `git diff --name-only HEAD -- dofus_stuff` vide |
| T-05-24 | Denial of service (suite lente) | la mesure des six cas | low | mitigate | Six cas sur des bases d'une ligne ; `pull_all` est un double (aucun telechargement, aucune attente), `quiet=True` supprime la sortie du produit : la duree du module reste de l'ordre de la seconde, celle de la batterie de 20 a 30 secondes (cout assume par D-84) |
| T-05-25 | Information disclosure | constats du module | low | mitigate | Les constats nomment des dossiers relatifs a la racine du depot et les fichiers producteurs (`SOURCE_SYNC`, `dofus_stuff/catalog.py`) ; aucun chemin de poste n'est ecrit dans la page ni dans un constat, et `DOFUS_SECRET_KEY` reste absent de toutes les pages (mesure de la phase) |
| T-05-SC | Tampering | installations de paquets | low | accept | Cette reparation n'ajoute aucune dependance : `pyproject.toml` est inchange, aucun `pip install` n'est execute, les seuls imports ajoutes a `tests/test_docs_base_locale.py` sont `time` (bibliotheque standard) et `dofus_stuff.catalog` (module du depot). La porte de legitimite des paquets n'a aucun sujet ici |

**Hors perimetre** : publication, deploiement distant, synchronisation Dofusdude, suppression de donnees, achat. Les criteres 2 a 5 de la phase, verifies, ne sont pas rouverts.
</threat_model>

<verification>
**Controles automatises** : le controle du declencheur (mesure des six cas sur bases temporaires avec les deux points d'entree reseau remplaces, correspondance avec la page, prohibition des marques d'exclusivite justifiee par la mesure, bornage de l'affirmation de la base absente des deux cotes), la reecriture verifiee de la page (fragments de la section de la fenetre et de la section du fichier, octets CRLF sans BOM), les treize controles existants du module (inchanges), les gardes des phases 1 a 4, la suite entiere avec l'interpreteur epingle, l'empreinte de `.data/dofus.sqlite3` avant et apres la suite, et **trois morsures** sur copie verte avant mutation : clause d'exclusivite reintroduite dans la section de la fenetre, marque du declencheur « aucun dernier controle » retiree, bornage de l'affirmation de la base absente retire.

**Preuve que le controle voit le defaut d'origine (mesure du planificateur, pas une supposition)** : le nouveau controle est **rouge** sur la page livree par 05-03, pour trois constats independants — la marque « aucun dernier controle » est absente de la section de la fenetre (le declencheur `last_checked is None` n'y est nomme nulle part), deux marques d'exclusivite y figurent (mesure : `seulement dans ce cas` et `la seule situation` sont presentes apres normalisation), et la section du fichier affirme l'absence d'erreur sans nommer la ligne de commande. Les trois morsures de la tache 2 rejouent ces trois formes exactes ; chacune doit rendre le motif nomme de son constat.

**Non automatisable, nomme comme tel** : la formulation de la prose au-dela des marques epinglees (le controle ne lit pas le sens des phrases) ; une exclusivite formulee autrement que par les chaines de `ABSOLUS_DECLENCHEUR` ; le contact reel de l'API Dofusdude, jamais exerce (les deux points d'entree reseau sont remplaces avant l'appel) ; la portabilite de l'assertion CRLF hors d'un poste dont `core.autocrlf` vaut `true`. Ces quatre points sont declares en `backstop` dans `must_haves.truths`. Aucun blocage : rien de cette reparation ne depend d'un geste humain, d'un secret ou d'un reseau.

**References de verification de phase** : `/gsd:verify-work` rejouera la suite entiere, la porte de non-regression de `.data/` et la morsure de chaque controle. `05-VERIFICATION.md` est le document que ce plan ferme : le gap du critere 1 (lignes 39, 41 et le constat secondaire de la ligne 9) et son `missing` sont couverts par les deux taches, ligne par ligne.
</verification>

<success_criteria>
- `docs/base-locale.md` enonce les **quatre** conditions reelles du declencheur lues dans `dofus_stuff/sync.py:32` (fenetre ecoulee, base vide, aucun dernier controle enregistre, `--force-sync`), dit le cas de non-contact avec sa bonne portee (base remplie, fenetre fraiche, sans `--force-sync`) et n'affirme plus que la fenetre est l'unique declencheur ni qu'aucune requete n'est emise tant qu'elle n'est pas ecoulee (lignes 39 et 41 de `05-VERIFICATION.md`).
- L'affirmation de la base absente est bornee : la section du fichier nomme la limite de la ligne de commande, mesuree (`sync.py:43`, `Catalog.load(offline=True)` sur une base vide leve et ne contacte rien), et la section de la ligne de commande continue de la dire — la contradiction interne entre les lignes 9 et 61 est fermee des deux cotes.
- Le controle `test_declencheur_du_controle_de_version` mesure le declencheur sur six cas temporaires avant de se prononcer, exige de la page chaque condition dont le contact a ete mesure, refuse les marques d'exclusivite que la mesure contredit (prohibition **conditionnee** a la mesure, jamais une liste de chaines interdites en dur), et exige le bornage de l'affirmation de la base absente. Le module passe de 13 a 14 controles.
- Trois derives epinglees font rougir la suite sur une copie verte avant mutation, chacune avec le motif nomme de son constat ; aucune ne se solde par `MUTATION NON DETECTEE`.
- Aucun controle existant n'est affaibli et la garde de cloture est renforcee (`CIBLES_DATA_DIR` porte `load`) ; aucun fichier de `dofus_stuff/**` n'est modifie (D-88) ; la suite entiere est verte avec `./.venv/Scripts/python.exe -m pytest -q` et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256 avant et apres elle.
- Les criteres 2 a 5 de la phase, verifies par `05-VERIFICATION.md`, ne sont rouverts par aucun controle ni par aucune phrase de la page : la reparation est chirurgicale.
</success_criteria>

<output>
Create `.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-04-SUMMARY.md` when done
</output>

## Artefacts produits par cette phase (inventaire mis a jour par le plan 05-04)

| Type | Symbole | Plan |
|------|---------|------|
| phrases corrigees | les deux phrases de `## La fenêtre de re-check de 24 heures` enoncant les quatre conditions et le cas de non-contact, et la phrase bornee de `## Le fichier de la base` | 05-04 |
| fonction de mesure | `_contacts_mesures(tmp_path, monkeypatch)` (six cas, doubles de `dofus_stuff.sync.fetch_version` et `pull_all`) | 05-04 |
| constantes de motif | `MOTIF_DECLENCHEUR`, `MOTIF_ABSOLU_DECLENCHEUR`, `MOTIF_SANS_ERREUR` | 05-04 |
| constantes de mesure | `VERSION_DISTANTE`, `MESSAGE_BASE_VIDE`, `CAS_BASE_REMPLIE_FRAICHE`, `CAS_FENETRE_ECOULEE`, `CAS_BASE_VIDE`, `CAS_JAMAIS_CONTROLEE`, `CAS_FORCE`, `CAS_HORS_LIGNE`, `CONTACTS_ATTENDUS` | 05-04 |
| constantes de contenu | `MARQUES_DECLENCHEURS`, `MARQUES_NON_CONTACT`, `ABSOLUS_DECLENCHEUR`, `MARQUES_ABSENTE_SANS_ERREUR`, `MARQUES_LIMITE_CLI`, `MARQUES_VIDE_HORS_LIGNE`, `MARQUES_ERREUR_HORS_LIGNE` | 05-04 |
| fonction de test | `test_declencheur_du_controle_de_version` (le module passe de 13 a 14 controles) | 05-04 |
| garde renforcee | `CIBLES_DATA_DIR = ("Database", "create_app", "load")` — le `data_dir` de `Catalog.load` est desormais verifie | 05-04 |

**Inventaire des plans 05-01 a 05-03, inchange** : page `docs/base-locale.md` (creation, sections, cloture), ligne d'index de `docs/sommaire.md`, dette `PAGES_INEXISTANTES = ()` denouee, renvois de `docs/parcours-simplifie.md`, helpers `_texte_page`, `_lignes_du_corps`, `_statut`, `_routes_declarees`, `renvois_morts`, `_empreinte`, les gardes `RACINES_INTERDITES`, `APPELS_SUPPRESSION`, `APPEL_PRODUIT`, `APPELS_SYNCHRO_PRODUIT`, `CONFIRMATIONS_INTERDITES`, `MOTIF_GARDE`, `MOTIF_DATA_DIR` et les motifs de la vague 1 a 3 : **aucun n'est modifie par ce plan**, sauf `CIBLES_DATA_DIR`, qui gagne une entree.

## Couverture des sources (ce plan)

| Source | Element | Consomme par |
|--------|---------|--------------|
| VERIFICATION | Gap du critere 1 : prose fausse lignes 39 et 41, contredite par `dofus_stuff/sync.py:32` ; constat secondaire de la ligne 9 (auto-contradiction avec la ligne 61) ; cause profonde : le module ne voit pas la semantique du declencheur | taches 1 et 2 (les trois `missing` du rapport sont traites un a un) |
| VERIFICATION | `missing` : remplacer l'exclusivite par les conditions reelles, rattacher l'absence de requete a sa vraie condition, borner la phrase de la ligne 9, et le controle minimal du declencheur « recommande » | tache 1 pour les trois phrases, tache 2 pour le controle (les trois cas demandes, plus trois) |
| VERIFICATION | Mesures du verificateur : base vide a fenetre fraiche → `fetch_version` 1, `pull_all` 1, `action=sync` ; `--force-sync` a fenetre fraiche → `fetch_version` 1 | tache 2 (`CONTACTS_ATTENDUS`, blocs 1 et 2) |
| VERIFICATION | Empreinte de la base : `24989696:1788730056843137500:e3793d64cb79…` | portes de non-regression des deux taches |
| ROADMAP § Phase 5 | Critere de succes 1 (le fichier, les categories et la fenetre de re-check decrits conformement au code) | les deux taches |
| REQUIREMENTS | BASE-01 (seul identifiant de cette reparation ; BASE-02 et BASE-03 restent satisfaits par 05-01 a 05-03 et ne sont pas rouverts) | `requirements` du plan |
| RESEARCH | § Measured inventory 4 (la decision `needs_check`, la garde hors-ligne, les quatre sorties mesurees) et § 6 (ce que chaque surface fait hors-ligne) | tache 1 (conditions citees d'apres le code) et tache 2 (cas mesures) |
| RESEARCH | § Measured inventory 9 (le refus de `db sync` et l'ecran web de synchronisation, deja verifies) | hors perimetre : non rouverte |
| CONTEXT | D-73, D-74 (la fenetre et le fichier documentes depuis le code, sans valeur volatile) | tache 1 |
| CONTEXT | D-83, D-84, D-85 (constats localisants, morsures sur copie verte avant mutation, limites declarees) | tache 2 (constats, batterie, docstring du test) |
| CONTEXT | D-88, D-89 (code non modifie, aucune resynchronisation), D-91 (commits locaux indexes par chemin) | les deux taches (contraintes de commit) |
| CONTEXT | D-69, D-86 (gabarit verrouille, renvois), D-90 (conventions des phases 1 a 4) | tache 1 (forme) et tache 2 (fixtures, normalisation, interpreters) |

**Audit de completude de la reparation (les quatre types de sources)** : GOAL — le critere 1 est le seul critere en echec de la phase, il est couvert par les deux taches ; les criteres 2 a 5, verifies, sont explicitement hors perimetre ; REQ — `BASE-01` est le seul identifiant porte par ce plan (les deux autres exigences de la phase sont satisfaites) ; RESEARCH — les sections 4 et 6 de l'inventaire mesure sont consommees, les sections 5, 7, 8, 9, 10 et 11 restent celles des criteres 2 a 4, non rouvertes ; CONTEXT — les decisions applicables (D-69, D-73, D-74, D-83, D-84, D-85, D-86, D-88, D-89, D-90, D-91) sont toutes citees, et les « Deferred Ideas » de `05-CONTEXT.md` (liste epinglee des huit pages, page de depannage, glossaire, preuve de completude : phase 6) restent hors perimetre. Aucune exclusion silencieuse, aucun affaiblissement de controle.

**Prohibitions et responsabilite** : D-85 (limites declarees, quatre backstops), D-88 (le code n'est jamais modifie pour faire passer la page), D-89 (aucune resynchronisation, aucune ecriture sous `.data/`), D-91 (commits locaux, indexation par chemin explicite, aucune publication).
