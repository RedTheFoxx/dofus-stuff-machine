---
phase: 05-base-locale-hors-ligne-et-resynchronisation
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - tests/test_docs_base_locale.py
  - README.md
autonomous: true
requirements:
  - BASE-03
estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Chaque renvoi interne du `README.md` resout depuis la racine du depot (D-87 pris a la lettre) : une fonction **pure** `renvois_morts(texte, racine)` rend un constat par cible interne non resolue, et le controle enumere les liens Markdown du README, ignore les cibles externes (`http://`, `https://`, `mailto:`) et exige qu'aucune cible interne ne reste sans fichier sur disque ; la mesure a un objet, le README portant aujourd'hui un renvoi interne vers `docs/sommaire.md` — critere de succes 5, D-87."
    - "Le detecteur de renvois du README **mord** : le temoin est mesure vert d'abord sur le texte livre, puis la meme fonction appliquee a une copie **en memoire** du README ou la cible du renvoi interne a ete remplacee par un chemin absent doit rendre au moins un constat — la morsure vit sur une chaine, aucun fichier du depot n'est ecrit (D-84, Open Question 4 de la recherche)."
    - "La dette D-44/D-63 n'est pas seulement supprimee, elle est **denouee** : `docs/parcours-simplifie.md` porte au moins deux liens vers `base-locale.md` — un dans sa phrase d'introduction, un dans la section « Ce que cette page ne decrit pas » — et cette section continue de nommer la base locale en clair ; le renvoi en prose devenu lien est donc verifiable, la cible existant desormais (D-63, D-86)."
    - "La page est close sur ses sections : les neuf titres de `TITRES_SECTION_ATTENDUS` apparaissent dans cet ordre, aucun titre de niveau 2 de la page n'est hors de cette liste, et la derniere ligne non vide reste la ligne de retour vers le sommaire (D-69)."
    - "Les octets de la page sont ceux de la convention du depot : autant de retours chariot que de fins de ligne (CRLF sur toutes les lignes), aucun BOM, decodage UTF-8 strict — lus en binaire, jamais sur un texte re-encode ; la limite de portabilite (l'assertion depend de `core.autocrlf=true`, aucun `.gitattributes` n'existe) est ecrite dans la docstring du test, jamais presentee comme portable (D-67, D-69, AR-5, Pitfall 11)."
    - "La page ne fige aucune valeur volatile et n'ouvre aucune porte de sortie : aucun nombre de quatre chiffres ou plus (compteur d'objets, version de jeu, horodatage, taille), aucun lien externe, et chaque chemin entre accents graves du bloc « Source de verite » existe depuis la racine du depot — au moins un chemin cite (D-73, D-75, D-03)."
    - "L'integrite de `.data/dofus.sqlite3` est mesuree autour des rendus du module par la fonction `_empreinte` reprise verbatim (`taille`, `mtime_ns`, `sha256`), avec un `skip` **nomme** quand la base du depot est absente : jamais un faux vert (D-81, D-84, L-2 de `04-SECURITY.md`)."
    - "La suite entiere est verte avec l'interpreteur epingle et l'empreinte de `.data/dofus.sqlite3` est identique avant et apres elle : le compteur de tests est cite tel qu'observe, jamais estime (critere de succes 5, GARD-04)."
    - statement: "L'appreciation de redaction de la page (prose libre, invitation ou non a lancer une commande, lisibilite des avertissements) n'est pas verifiee par un test : les controles portent sur les valeurs, les libelles et les structures **cites**. Backstop explicite, jamais un vert silencieux (D-85)."
      verification: backstop
    - statement: "La portabilite de l'assertion CRLF hors d'un poste dont `core.autocrlf` vaut `true` reste non prouvee (aucun `.gitattributes` dans le depot, limite AR-5). Backstop explicite."
      verification: backstop
  artifacts:
    - path: "tests/test_docs_base_locale.py"
      provides: "Le detecteur de renvois morts du README, le controle du renvoi legitime vers la base locale, le controle de cloture de la page (ordre des sections, octets CRLF sans BOM, valeurs volatiles, liens externes, chemins du bloc Source de verite) et la mesure d'empreinte de `.data/` autour des rendus"
      contains: "def test_renvois_du_readme_resolus"
      min_lines: 620
    - path: "README.md"
      provides: "Le fichier dont chaque renvoi interne est controle ; aucune modification n'est attendue si tous ses renvois resolvent deja"
      contains: "docs/sommaire.md"
      min_lines: 123
  key_links:
    - from: "tests/test_docs_base_locale.py"
      to: "README.md"
      via: "la fonction pure `renvois_morts` appliquee au texte du README, cibles externes ignorees, cibles internes resolues depuis `RACINE_DEPOT` (D-87)"
      pattern: "README.md"
    - from: "tests/test_docs_base_locale.py"
      to: "docs/parcours-simplifie.md"
      via: "le controle que les deux renvois vers `base-locale.md` existent et que la section « Ce que cette page ne decrit pas » nomme toujours la base locale (D-63, D-86)"
      pattern: "base-locale.md"
    - from: "tests/test_docs_base_locale.py"
      to: "docs/base-locale.md"
      via: "lecture des octets et du texte de la page pour l'ordre des sections, l'encodage, les valeurs volatiles, les liens externes et les chemins du bloc Source de verite"
      pattern: "base-locale.md"
    - from: "tests/test_docs_base_locale.py"
      to: ".data/dofus.sqlite3"
      via: "mesure d'empreinte `(taille, mtime_ns, sha256)` avant et apres les rendus du module — lecture d'octets seulement, jamais une ouverture SQLite du chemin reel"
      pattern: "dofus.sqlite3"
  prohibitions:
    - statement: "Ne jamais reecrire `README.md` au-dela de la resolution de ses renvois : si un renvoi ne resout pas, la correction se limite a ce renvoi ; le bloc de commandes du README n'est pas retouche par cette phase (D-87)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais ouvrir, lire par SQLite, ecrire ou vider `.data/dofus.sqlite3` : la seule operation admise est la lecture d'octets pour l'empreinte et sa comparaison a elle-meme (D-81, D-89)."
      status: resolved
      verification: test
    - statement: "Ne jamais laisser une mesure d'integrite passer en silence quand la base du depot est absente : le `skip` doit etre nomme et le module ne revendique jamais avoir mesure ce qu'il n'a pas mesure."
      status: resolved
      verification: test
    - statement: "Ne jamais presenter l'assertion des fins de ligne comme portable : elle depend de la configuration Git du poste (AR-5) et la limite est ecrite la ou l'assertion vit."
      status: resolved
      verification: judgment
    - statement: "Ne jamais revendiquer une exhaustivite de redaction : les controles portent sur ce qui est cite, et la prose libre est declaree hors de leur portee (D-85)."
      status: resolved
      verification: judgment
    - statement: "Ne jamais publier, deployer a distance, pousser vers un depot distant ni effectuer d'achat : commits locaux seulement, indexation par chemin explicite, jamais `git add .` (D-91)."
      status: resolved
      verification: judgment
  assumptions:
    - "**Sonde d'arete spec-less : l'orchestrateur n'a PAS execute la sonde deterministe de la section A** (aucun SPEC de phase). Les aretes de ce plan sont derivees par le planificateur du critere de succes 5 (« chaque nom de champ ou de commande cite par la page est produit par le code, et la suite reste verte avec l'interpreteur epingle »), du texte de BASE-03 et de D-87, enrichies de l'etat initial mesure du README (six liens, un seul interne, qui resout) et du critere 4 du ROADMAP (les controles n'ecrivent pas sous `.data/`, n'executent aucune synchronisation et n'ouvrent aucune connexion reseau). Deux verifications sont ecrites en `backstop` ; les six prohibitions retenues sont au bloc `prohibitions`, aucune n'est ecartee en silence."
    - "`README.md` figure dans `files_modified` parce que la phase doit pouvoir corriger un renvoi s'il ne resolvait pas ; **aucune modification n'est attendue** : la mesure de la recherche ne trouve qu'un renvoi interne, vers `docs/sommaire.md`, et il resout. Si le fichier n'est pas modifie, il ne doit pas etre commite pour autant : l'indexation se fait par chemin explicite, et un fichier inchange n'entre pas dans le commit (D-91)."
    - "Le controle D-87 vit dans le **module neuf** et non dans `tests/test_docs_structure.py` : les gardes de la phase 1 restent inchangees (elles verifient un seul renvoi du README, vers le sommaire) et le critere « les renvois du README resolvent » est plus general (Open Question 4 de la recherche). Le resolveur de liens de `docs/` de `tests/test_docs_structure.py` est scope a `docs/` et n'est pas elargi ni importe : la fonction du module resout depuis la racine du depot, et la raison de cette duplication locale du **motif** est ecrite dans sa docstring (D-12 interdit de dupliquer un helper de `tests/conftest.py`, dont celui-ci ne fait pas partie)."
    - "La mesure d'empreinte `_empreinte` est la **troisieme copie assumee** du meme helper (`tests/test_docs_parcours.py:2557+`, `tests/test_docs_wizard.py:2289+`) : la convention du depot est de la reprendre verbatim, jamais de la factoriser, et sa limite (vraie par construction, L-2) est ecrite dans la docstring (Pitfall 10)."
    - "La prose libre de la page n'est pas verifiee : les controles portent sur les valeurs, les libelles et les structures **cites**, et ce plan ne revendique aucune exhaustivite de la redaction (D-85)."
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 5, de son critere de succes 5 et de la decision D-87 de `05-CONTEXT.md` :
**En tant que** porteur du projet qui relit la documentation apres coup, **je veux** que la page de la base locale soit close (sections epinglees, octets conformes, aucune valeur volatile, aucun lien mort) et que les renvois du `README.md` comme ceux de la page voisine soient reellement resolus, **afin de** pouvoir faire confiance a une suite verte : elle doit rougir le jour ou la page ou un renvoi derive.

Mode MVP : le `**Goal:**` du ROADMAP n'est pas redige en recit utilisateur ; le cadrage est **derive**, comme au plan 04-01, et ne reformule que des criteres et des decisions verrouillees.

Purpose : c'est le critere 5 du ROADMAP — « chaque nom de champ ou de commande cite par la page est produit par le code, et la suite reste verte avec l'interpreteur epingle » — et le controle de completude que D-87 confie a cette phase pour les renvois du `README.md`. C'est aussi le plan qui **denoue** la dette D-44 : apres lui, plus aucune page de `docs/` n'est en dette, et la preuve n'est pas une suppression de controle mais un renvoi desormais legitime.

Output : les quatre controles de cloture ajoutes a `tests/test_docs_base_locale.py` (renvois du README, renvoi legitime vers la base locale, cloture de la page, integrite de `.data/` autour des rendus), plus l'inventaire des artefacts de la phase et la porte de fin de phase (suite entiere verte, empreinte identique).

Ordonnancement declare : **vague 3**, `depends_on: [05-02]` — la page doit etre complete avant d'etre close, et le module contient les rendus que la mesure d'empreinte encadre ; les plans 05-02 et 05-03 touchent le meme module de test, donc aucun parallelisme n'est possible.

**Ce que ce plan ne fait pas** : il n'ajoute aucune section a la page (la page est complete apres 05-02), ne modifie aucun fichier de `dofus_stuff/**` (D-88), ne reecrit pas `README.md` au-dela d'un renvoi qui ne resoudrait pas (D-87), et ne touche ni la liste epinglee des huit pages ni la preuve de completude de la phase 6.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-CONTEXT.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-VALIDATION.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-01-PLAN.md
@.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-02-PLAN.md
@.claude/CLAUDE.md
@README.md
@docs/base-locale.md
@docs/parcours-simplifie.md
@docs/sommaire.md
@tests/test_docs_base_locale.py
@tests/test_docs_parcours.py
@tests/test_docs_wizard.py
@tests/test_docs_structure.py
@tests/conftest.py
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : tranche verticale « chaque renvoi du README resout, et la dette D-44 est denouee »</name>
  <files>tests/test_docs_base_locale.py, README.md</files>
  <reversibility rating="reversible">Ajouter deux controles a un module de test est annulable par un commit de retour ; aucune correction du README n'est attendue, et rien n'est publie ni ecrit hors du depot.</reversibility>
  <read_first>
    README.md
    tests/test_docs_base_locale.py
    tests/test_docs_structure.py
    tests/test_docs_parcours.py
    tests/test_docs_wizard.py
    docs/parcours-simplifie.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-RESEARCH.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
  </read_first>
  <action>Ecrire le controle de resolution des renvois du `README.md` (D-87) et le controle symetrique de la dette denouee (D-63), tous deux adosses a une fonction **pure** — c'est la tranche de bout en bout de ce plan, elle traverse le README, la page voisine et le disque.

**Module** :
- constantes : `MOTIF_LIEN_MARKDOWN = re.compile(r"\[(?P<libelle>[^\]]*)\]\((?P<cible>[^)]+)\)")` (le motif deja employe par `tests/test_docs_wizard.py:2475`), `CIBLES_EXTERNES = ("http://", "https://", "mailto:")`, `MOTIF_README = "renvoi du README"`, `MOTIF_LIEN_D63 = "renvoi en prose sans lien vers la page de la base locale"`, `TITRE_SECTION_RENVOI = "### Ce que cette page ne décrit pas"` (le titre reel du fichier), `PAGE_PARCOURS = "parcours-simplifie.md"` ;
- fonction **pure** `renvois_morts(texte: str, racine: Path) -> list[str]` : elle ne lit aucun fichier et n'ouvre aucune connexion ; pour chaque lien Markdown du texte, elle ignore les cibles externes, rejette les cibles qui portent une ancre, un chemin absolu, un antislash ou le schema de fichier local (memes interdits que la garde de structure de `docs/`), et rend un constat pour chaque cible interne dont le fichier n'existe pas sous `racine`. Chaque constat nomme le fichier controle, la cible fautive et la racine ; la docstring dit **pourquoi** le motif du resolveur est local (D-12 ne porte que sur les helpers de `tests/conftest.py`, et le resolveur de `tests/test_docs_structure.py` est scope a `docs/`) et declare la limite honnete : seules les cibles de liens Markdown sont vues, un chemin cite en prose ne l'est pas ;
- test `test_renvois_du_readme_resolus(docs_dir, normalize)` : lire `README.md`, appliquer `renvois_morts` et exiger qu'aucun constat ne soit rendu ; exiger que la **mesure ait un objet** — au moins un lien interne dans le README — sans quoi un detecteur qui ne voit rien passerait pour un detecteur qui valide ; puis **mesurer la morsure dans le meme test** (patron « temoin vert puis mutation » de `tests/test_docs_wizard.py:2666-2720`) : le temoin est vert sur le texte livre, et la fonction appliquee a une **copie en memoire** du README ou la cible du renvoi interne est remplacee par un chemin absent doit rendre au moins un constat. Aucun fichier du depot n'est ecrit : la mutation vit sur une chaine ;
- test `test_renvoi_base_locale_legitime(docs_dir, normalize)` : lire `docs/parcours-simplifie.md` et exiger (a) au moins **deux** occurrences du lien vers `base-locale.md`, (b) dont une dans la section ouverte par le titre `TITRE_SECTION_RENVOI` (texte pris du titre jusqu'au prochain titre de niveau 2), (c) que cette section continue de nommer la base locale en clair (le mot est present apres normalisation, D-63 : le renvoi en prose devient un lien sans perdre son sens), (d) que la cible existe sur disque ; constat portant `MOTIF_LIEN_D63`. La docstring declare la limite : ce controle dit ce que les deux renvois nommes sont devenus, rien de la prose du reste de la page.

Si — et seulement si — un renvoi interne du `README.md` ne resolvait pas, corriger **ce renvoi** et rien d'autre : le bloc de commandes du README (`README.md` lignes 77-81, dont la ligne portant la commande destructrice) est **hors du mandat de cette phase** (D-87) et reste tel quel.
  </action>
  <acceptance_criteria>
    - `renvois_morts(texte, racine)` est une fonction pure, rend une liste de constats, ignore les cibles externes et nomme le fichier, la cible et la racine.
    - `test_renvois_du_readme_resolus` est vert sur le README livre, exige au moins un lien interne, et exige qu'une copie en memoire dont la cible interne est remplacee par un chemin absent produise au moins un constat.
    - `test_renvoi_base_locale_legitime` exige au moins deux liens vers `base-locale.md` dans `docs/parcours-simplifie.md`, dont un dans la section des renvois, et que cette section nomme toujours la base locale.
    - `README.md` n'est modifie que si un renvoi ne resolvait pas ; en l'etat mesure, il n'y a rien a corriger et le fichier reste inchange.
    - Retirer le renvoi interne du README, ou retirer les liens vers `base-locale.md` de la page voisine, rougit le module sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q` est vert, la suite entiere reste verte, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256 autour de la suite complete.
    - La batterie mesure la copie verte sur le module seul (`tests/test_docs_base_locale.py`) avant mutation, puis la suite complete apres : la commande automatisee des morsures dure de l'ordre de 10 a 20 secondes (cout assume par D-84).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py tests/test_docs_parcours.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume des gardes existantes</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in lien_readme_retire renvoi_base_locale_retire; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in lien_readme_retire) sed -i 's|(docs/sommaire.md)|(docs/absent.md)|' "$T/README.md"; motif='renvoi du README';; renvoi_base_locale_retire) sed -i 's|(base-locale.md)|(base-locale-absent.md)|g' "$T/docs/parcours-simplifie.md"; motif='renvoi en prose sans lien vers la page de la base locale';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 05-03 (TACHE 1) EN ECHEC"; exit 1; fi; echo "morsures 05-03 tache 1 : 2/2 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 05-03 (TACHE 1) EN ECHEC"</fails_when>
  </verify>
  <done>Chaque renvoi interne du README resout et le detecteur est prouve mordant sur une copie en memoire ; les deux renvois vers la base locale existent dans la page voisine et la dette D-44 est denouee, pas seulement supprimee ; deux derives epinglees font rougir la suite sur une copie verte avant mutation.</done>
</task>

<task type="auto">
  <name>Tache 2 : la page est close — ordre des sections, octets CRLF sans BOM, aucune valeur volatile, aucun lien externe, chemins du bloc Source de verite</name>
  <files>tests/test_docs_base_locale.py</files>
  <read_first>
    docs/base-locale.md
    docs/sommaire.md
    tests/test_docs_base_locale.py
    tests/test_docs_structure.py
    tests/test_docs_wizard.py
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
  </read_first>
  <action>Ecrire le controle de cloture de la page complete — c'est la derniere ecriture de la page, donc le seul moment ou les regles d'ensemble s'appliquent a l'objet fini.

**Module** :
- constantes de motif : `MOTIF_SECTION = "section attendue de la page"`, `MOTIF_SOURCE = "chemin du bloc source de verite"`, `MOTIF_LIEN_EXTERNE = "lien externe"` ; les constantes `MOTIF_CRLF` et `MOTIF_VOLATILES` viennent de la tache 1 du plan 05-01 et sont reutilisees telles quelles ; `LIEN_RETOUR = "[Retour au sommaire](sommaire.md)"` ; `TITRE_H1 = "# Base locale"` ;
- test `test_page_close_et_sans_valeur_volatile(docs_dir, section, normalize)` : la page est lue par le lecteur local `_texte_page(docs_dir)` livre par la tache 1 du plan 05-01 (qui porte `PAGE = "base-locale.md"`) et ses octets par `(docs_dir / PAGE).read_bytes()` — **aucune fixture `page` n'existe ni ne doit etre supposee** : `tests/conftest.py` expose exactement `catalog`, `app`, `client`, `docs_dir`, `normalize`, `lignes_de_code`, `lignes_exemple`, `sections` et `section` (mesure), et le plan 05-01 ne declare aucune fixture supplementaire ;
  - **octets de la page (D-67, D-69, AR-5, Pitfall 11)** : lire le fichier en binaire ; exiger aucun BOM, decodage UTF-8 strict, et autant de retours chariot que de fins de ligne — la convention du depot est CRLF sur toutes les lignes ; la docstring du test dit que cette assertion depend de la configuration Git du poste (`core.autocrlf=true`, aucun `.gitattributes` dans le depot) et **n'est pas presentee comme portable** ; constat portant `MOTIF_CRLF` ;
  - **structure** : exiger un seul titre de niveau 1 et qu'il vaille `TITRE_H1` ; extraire les titres de niveau 2 de la page ; exiger que la suite des titres presents appartenant a `TITRES_SECTION_ATTENDUS` soit **egale a la constante, dans l'ordre**, et qu'**aucun** titre de niveau 2 de la page ne soit hors de cette liste (un titre inattendu est une derive de structure, pas une liberte de redaction) ; la correspondance est exigee **dans les deux sens** — aucun titre attendu ne manque et aucun titre de la page n'est hors liste, quel que soit le nombre d'entrees de la constante a ce stade ; exiger que la derniere ligne non vide soit `LIEN_RETOUR` ; constat portant `MOTIF_SECTION` ;
  - **aucune valeur volatile (D-73, D-75)** : exiger que `MOTIF_VOLATILE` ne trouve rien dans le texte de la page — compteur d'objets, version de jeu, horodatage ou taille figee ; le controle court ici sur la **page complete**, apres les sections des plans 05-01 et 05-02 ; constat portant `MOTIF_VOLATILES` ;
  - **aucune porte de sortie** : exiger qu'aucun lien externe n'existe sur la page (aucun lien dont la cible porte un schema `http`, `https` ou `mailto`, et aucune URL en clair) ; constat portant `MOTIF_LIEN_EXTERNE` ;
  - **bloc « Source de verite »** : extraire le corps de la section `## Source de vérité` par la fixture `section` ; y collecter les jetons entre accents graves qui ressemblent a un chemin (presence d'une barre oblique ou suffixe `.py`/`.md`/`.js`/`.json`) ; exiger qu'au moins un chemin soit cite, et que **chacun** existe depuis `RACINE_DEPOT` ; constat portant `MOTIF_SOURCE` et nommant le chemin fautif, la racine et la page ;
  - chaque constat est accumule et joint a **une seule** assertion, et nomme la page, la valeur fautive et la valeur attendue ;
- la docstring du test declare ce qu'il **ne** verifie pas : la prose libre, la formulation des avertissements et l'appreciation de lisibilite restent hors de portee d'un controle (D-85).
  </action>
  <acceptance_criteria>
    - `test_page_close_et_sans_valeur_volatile` est vert sur la page livres, et ses controles portent sur le fichier **lu en binaire** pour les octets et sur le texte pour le reste.
    - La suite des titres de niveau 2 de la page appartenant a `TITRES_SECTION_ATTENDUS` egale la constante dans l'ordre, et la page n'a aucun titre de niveau 2 hors de cette liste.
    - La page ne contient aucun nombre de quatre chiffres ou plus, aucun lien externe, et chaque chemin du bloc Source de verite existe depuis la racine du depot ; au moins un chemin y est cite.
    - Ajouter une valeur volatile, retirer un titre attendu, inventer un chemin dans le bloc Source de verite, ajouter un lien externe, ou convertir la page en fins de ligne LF, rougit le module sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
    - La batterie mesure la copie verte sur le module seul (`tests/test_docs_base_locale.py`) avant mutation, puis la suite complete apres : la commande automatisee des morsures dure de l'ordre de 25 a 35 secondes (cinq copies, cout assume par D-84).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_base_locale.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in valeur_volatile_ajoutee section_retiree chemin_source_invente lien_externe_ajoute page_en_lf; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in valeur_volatile_ajoutee) printf '\nLe catalogue contient 18288 objets au 2026-02-13T09:15:00Z.\n' >> "$T/docs/base-locale.md"; motif='valeur volatile';; section_retiree) sed -i '/^## Les commandes destructrices$/d' "$T/docs/base-locale.md"; motif='section attendue de la page';; chemin_source_invente) sed -i 's|^\[Retour au sommaire\]|`dofus_stuff/inexistant.py`\n[Retour au sommaire]|' "$T/docs/base-locale.md"; motif='chemin du bloc source de verite';; lien_externe_ajoute) printf '\nVoir [la documentation officielle](https://exemple.test/page).\n' >> "$T/docs/base-locale.md"; motif='lien externe';; page_en_lf) sed -i 's/\r$//' "$T/docs/base-locale.md"; motif='fins de ligne';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 05-03 (TACHE 2) EN ECHEC"; exit 1; fi; echo "morsures 05-03 tache 2 : 5/5 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 05-03 (TACHE 2) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
  </verify>
  <done>La page est close : sections epinglees dans l'ordre et sans titre inattendu, octets CRLF sans BOM, aucune valeur volatile, aucun lien externe, chemins du bloc Source de verite existants ; cinq derives epinglees font rougir la suite sur une copie verte avant mutation, et la suite entiere est verte.</done>
</task>

<task type="auto">
  <name>Tache 3 : la base du depot n'est pas touchee par les controles, et la porte de fin de phase</name>
  <files>tests/test_docs_base_locale.py</files>
  <read_first>
    tests/test_docs_base_locale.py
    tests/test_docs_parcours.py
    tests/test_docs_wizard.py
    tests/conftest.py
    dofus_stuff/database.py
    dofus_stuff/web/routes.py
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-PATTERNS.md
    .planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-VALIDATION.md
  </read_first>
  <action>Fermer la phase par la mesure d'integrite et la porte de non-regression.

**Module** :
- reprendre **verbatim** la fonction `_empreinte(path)` de `tests/test_docs_parcours.py:2557+` (aussi presente dans `tests/test_docs_wizard.py:2289+`) : elle rend `(taille, mtime_ns, sha256)` ; c'est la **troisieme copie assumee** du meme helper, la convention du depot etant de la reprendre et non de la factoriser (Pitfall 10) ; sa docstring declare la limite **L-2** deja admise en phase 4 : c'est une mesure locale, vraie par construction, et le module ne pretend pas prouver autre chose que l'absence d'ecriture pendant ses propres rendus ;
- constantes : `BASE_LOCALE = (".data", "dofus.sqlite3")`, `MOTIF_BASE_ABSENTE = "base locale du depot absente"`, `MOTIF_EMPREINTE_CHANGEE = "empreinte de la base locale modifiee"`, et les chemins d'ecran rendus par le module, reunis en une constante nommee (l'etat de la base, l'ecran de synchronisation, l'ecran de vidage et les sauvegardes) ;
- test `test_data_locale_non_modifiee_autour_des_rendus(app)` : construire le chemin de la base du depot depuis `RACINE_DEPOT` ; si le fichier est absent, `pytest.skip(MOTIF_BASE_ABSENTE)` — un `skip` **nomme**, jamais un vert silencieux ; mesurer l'empreinte **avant**, rendre les ecrans du module par le client de l'application (etat, statut, synchronisation en lecture, ecran de vidage en lecture, sauvegardes — aucun `POST`, D-81), mesurer l'empreinte **apres**, et exiger l'egalite des deux mesures ; un ecart est un constat qui nomme les deux empreintes et `MOTIF_EMPREINTE_CHANGEE` ;
- la docstring du test dit ce qu'il ne prouve pas : que la suite entiere n'ecrit pas sous `.data/` (c'est la porte de fin de phase qui le mesure, autour de la suite complete) et que l'execution du JavaScript de l'ecran des sauvegardes reste hors d'atteinte.
  </action>
  <acceptance_criteria>
    - `_empreinte` est repris verbatim (rend `(taille, mtime_ns, sha256)`) et sa docstring declare la limite L-2.
    - `test_data_locale_non_modifiee_autour_des_rendus` mesure avant et apres les rendus du module et exige l'egalite ; quand `.data/dofus.sqlite3` est absent, il **saute** avec un motif nomme, jamais en silence.
    - Le module n'ouvre aucune connexion SQLite vers le chemin reel de la base et n'emet aucun `POST` : les seules operations sur le fichier reel sont des lectures d'octets pour l'empreinte.
    - Retirer le dossier `.data/` sur une copie fait **sauter** le controle avec son motif nomme et laisse la suite verte ; ajouter au module un appel qui construit une base sur la racine du depot fait rougir la garde de cloture avec le motif `MOTIF_DATA_DIR`, dont la valeur est **`repertoire de donnees non isole`** (la chaine epinglee par le plan 05-01, citee ici verbatim : la morsure et la constante sont une seule et meme chaine).
    - La suite entiere est verte avec `./.venv/Scripts/python.exe -m pytest -q`, son compteur de tests est releve tel qu'observe, et l'empreinte de `.data/dofus.sqlite3` est identique avant et apres elle.
    - Les deux batteries de la tache mesurent la copie verte sur le module seul (`tests/test_docs_base_locale.py`) avant mutation ou avant la copie sans `.data/` : la commande automatisee du skip nomme et celle de la morsure `data_dir_reel` durent chacune de l'ordre de 5 a 15 secondes (cout assume par D-84).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume de la suite complete</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; rm -rf "${T:?}/.data"; out="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q -rs 2>&1)"; statut=$?; if [ "$statut" -ne 0 ]; then echo "SKIP NON PROPRE : sortie non nulle ($statut) quand .data/ est absent"; printf '%s\n' "$out" | tail -20; exit 1; fi; if printf '%s' "$out" | grep -qF 'base locale du depot absente' && printf '%s' "$out" | grep -qE '[0-9]+ skipped'; then echo "skip nomme present quand .data/ est absent : $(printf '%s' "$out" | grep -oE '[0-9]+ skipped' | head -1)"; else echo "SKIP SILENCIEUX : motif \"base locale du depot absente\" absent, ou aucune ligne skipped"; printf '%s\n' "$out" | tail -20; exit 1; fi</automated>
    <fails_when>exit non-zero, ou une ligne "SKIP SILENCIEUX" ou "SKIP NON PROPRE"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_base_locale.py -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION (data_dir_reel) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; exit 1; fi; printf '\n\ndef _temoin_data_dir_reel():\n    return Database(data_dir=RACINE_DEPOT)\n' >> "$T/tests/test_docs_base_locale.py"; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF 'repertoire de donnees non isole'; then echo "mutation detectee (data_dir_reel), motif \"repertoire de donnees non isole\""; else echo "MUTATION NON DETECTEE (data_dir_reel) : motif \"repertoire de donnees non isole\" absent de la sortie"; printf '%s\n' "$out" | tail -30; exit 1; fi</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE" ou "COPIE ROUGE AVANT MUTATION"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; mesure() { "$PY" -c "import hashlib,pathlib;p=pathlib.Path('.data/dofus.sqlite3');print(f'{p.stat().st_size}:{p.stat().st_mtime_ns}:{hashlib.sha256(p.read_bytes()).hexdigest()}' if p.exists() else 'base absente')"; }; mesure > "$T/avant.txt"; "$PY" -m pytest -q; statut=$?; mesure > "$T/apres.txt"; if [ "$statut" -ne 0 ]; then echo "SUITE COMPLETE ROUGE (exit $statut)"; exit 1; fi; if diff -q "$T/avant.txt" "$T/apres.txt" >/dev/null; then echo "empreinte .data/ identique autour de la suite complete : $(cat "$T/apres.txt")"; else echo "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE : avant=$(cat "$T/avant.txt") apres=$(cat "$T/apres.txt")"; exit 1; fi</automated>
    <fails_when>exit non-zero, ou la ligne "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE", ou la ligne "SUITE COMPLETE ROUGE", ou une ligne contenant "failed" ou "error"</fails_when>
  </verify>
  <done>Les controles de la phase n'ecrivent pas sous `.data/` — mesure d'empreinte autour des rendus du module et autour de la suite complete — et l'absence de la base du depot produit un `skip` nomme, jamais un faux vert ; la phase est verte avec l'interpreteur epingle.</done>
</task>

</tasks>

<threat_model>
**Surface** : un module de test de plus de quatre controles, une fonction pure de resolution de chemins, et un acces **en lecture** a `.data/dofus.sqlite3` pour la mesure d'empreinte. Aucun reseau, aucun secret, aucune dependance, aucun fichier de `dofus_stuff/**` modifie (D-88). La menace reelle est celle d'un controle qui toucherait la base du depot, ou d'un controle qui ne mesurerait rien tout en restant vert.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Tampering | `.data/dofus.sqlite3` : ecriture ou ouverture par un controle | high | mitigate | La seule operation admise sur le chemin reel est la lecture d'octets pour `_empreinte` ; la garde de cloture du plan 05-01 refuse `DEFAULT_DATA_DIR` et toute construction hors `tmp_path` (`MOTIF_DATA_DIR`, morsure `data_dir_reel`) ; l'empreinte est mesuree avant et apres les rendus du module et apres la suite complete (taches 1 et 3) |
| T-05-14 | Repudiation (faux vert) | Une mesure d'integrite qui passe en silence | high | mitigate | Un `skip` **nomme** (`MOTIF_BASE_ABSENTE`) quand la base est absente, verifie par la morsure `base_absente` (copie sans `.data/`, sortie verte et ligne `skipped` portant le motif) ; le compteur de tests est releve tel qu'observe |
| T-05-15 | Spoofing (documentation qui ment) | Cloture de la page (octets, valeurs, chemins) | medium | mitigate | Les octets sont lus en binaire, les chemins du bloc Source de verite sont resolus sur disque, les valeurs volatiles sont refusees ; cinq morsures (valeur volatile, section retiree, chemin invente, lien externe, page en LF) prouvent que le controle mord sur une copie verte avant mutation |
| T-05-16 | Tampering | Un renvoi du README corrige par opportunite | medium | mitigate | Le mandat est ecrit : aucune modification du README n'est attendue ; si un renvoi ne resout pas, seule la cible fautive est corrigee, le bloc de commandes reste tel quel (D-87) ; la morsure `lien_readme_retire` prouve que la correction est bien mesuree, et non supprimee du controle |
| T-05-17 | Denial of service (suite lente) | La lecture de `.data/` (25 Mo) pour l'empreinte | low | mitigate | Une seule lecture d'octets par mesure, deux mesures autour des rendus et deux autour de la suite complete ; aucune ouverture SQLite, aucun parcours de dossier |
| T-05-18 | Information disclosure | Chemins absolus de poste ou secrets dans les constats | low | mitigate | Les constats nomment des chemins relatifs a la racine du depot ; `DOFUS_SECRET_KEY` reste absent de toutes les pages (mesure : 0 occurrence) |
| T-05-SC | Tampering | Installations de paquets | low | accept | Cette phase n'ajoute aucune dependance : `pyproject.toml` est inchange, aucun `pip install` n'est execute ; la porte de legitimite des paquets n'a aucun sujet ici |

**Hors perimetre** : publication, deploiement distant, synchronisation Dofusdude, suppression de donnees, achat.
</threat_model>

<verification>
**Controles automatises** : les quatre controles de cloture du module (`renvois_morts` et sa morsure en memoire, renvoi legitime vers la base locale, cloture de la page, integrite autour des rendus), les gardes existantes inchangees (`test_docs_structure.py`, `test_docs_parcours.py`, `test_docs_wizard.py`), la suite entiere avec l'interpreteur epingle, l'empreinte de `.data/dofus.sqlite3` avant et apres la suite, et **neuf morsures** sur copie verte avant mutation : renvoi du README retire, renvois vers la base locale retires, valeur volatile ajoutee, section retiree, chemin invente dans le bloc Source de verite, lien externe ajoute, page convertie en LF, `.data/` absent (skip nomme), appel construisant une base sur la racine du depot.

**Non automatisable, nomme comme tel** : la prose libre et la lisibilite des avertissements ; la portabilite de l'assertion CRLF hors d'un poste dont `git config core.autocrlf` vaut `true` ; l'execution du JavaScript de l'ecran des sauvegardes. Ces trois points sont declares en `backstop` dans `must_haves.truths`. Aucun blocage : rien de cette phase ne depend d'un geste humain, d'un secret ou d'un reseau.

**References de verification de phase** : `05-VALIDATION.md` § Per-Task Verification Map assigne les lignes `readme_renvois`, `d63_renvoi`, `cloture_page` et `data_locale` a ce plan (vague 3) ; `/gsd:verify-work` rejouera la suite entiere, la porte de non-regression de `.data/` et la morsure de chaque controle.
</verification>

<success_criteria>
- Les renvois internes du `README.md` resolvent tous, la dette D-44 est denouee par deux renvois legitimes vers `docs/base-locale.md`, et la page est close : sections epinglees dans l'ordre, octets CRLF sans BOM, aucune valeur volatile, aucun lien externe, chemins du bloc Source de verite existants (critere 5 ; BASE-03 ; D-87, D-63, D-85).
- Aucun controle de la phase n'ecrit sous `.data/`, n'ouvre une connexion SQLite vers le chemin reel, ni n'emet un `POST` : les mesures sont des lectures d'octets, et l'absence de la base produit un `skip` nomme.
- Neuf derives epinglees font rougir la suite sur une copie verte avant mutation, la morsure du detecteur de renvois vit sur une copie en memoire, et la suite entiere est verte avec `./.venv/Scripts/python.exe -m pytest -q`.
</success_criteria>

<output>
Create `.planning/phases/05-base-locale-hors-ligne-et-resynchronisation/05-03-SUMMARY.md` when done
</output>

## Artefacts produits par cette phase (inventaire tenu a jour par les trois plans)

| Type | Symbole | Plan |
|------|---------|------|
| page | `docs/base-locale.md` | 05-01 (creation), 05-02 (quatre sections) |
| ligne d'index | la ligne `[Base locale](base-locale.md)` du tableau de `docs/sommaire.md` | 05-01 |
| constante de dette | `PAGES_INEXISTANTES = ()` et ses commentaires remis en verite (`tests/test_docs_parcours.py`) | 05-01 |
| renvois | les deux renvois vers la base locale dans `docs/parcours-simplifie.md` (liens) | 05-01 |
| module de test | `tests/test_docs_base_locale.py` | 05-01 |
| constante de module | `TITRES_SECTION_ATTENDUS` | 05-01 (creation et completion), 05-02 (deux titres de plus) |
| helpers locaux | `_texte_page`, `_lignes_du_corps`, `_statut` | 05-01 |
| helpers locaux | `_routes_declarees` | 05-02 |
| helpers locaux | `renvois_morts`, `_empreinte` (troisieme copie assumee) | 05-03 |
| constantes de garde | `RACINES_INTERDITES`, `APPELS_SUPPRESSION`, `APPEL_PRODUIT`, `APPELS_SYNCHRO_PRODUIT`, `CONFIRMATIONS_INTERDITES`, `MOTIF_GARDE` | 05-01 |
| constantes de motif | `MOTIF_FICHIER`, `MOTIF_CATEGORIES`, `MOTIF_FENETRE`, `MOTIF_INDEX`, `MOTIF_H1`, `MOTIF_DEFAUTS`, `MOTIF_CHAMPS_CLI`, `MOTIF_CHAMPS_WEB`, `MOTIF_CONDITION`, `MOTIF_VOLATILES`, `MOTIF_CRLF`, `MOTIF_DATA_DIR`, `MOTIF_CONFIRMATION` | 05-01 |
| constantes de motif | `MOTIF_CREATION`, `MOTIF_ROUTE`, `MOTIF_REFUS`, `MOTIF_SYNCHRO_WEB`, `MOTIF_DESTRUCTRICES`, `MOTIF_EFFET`, `MOTIF_EXEMPLE` | 05-02 |
| constantes de motif | `MOTIF_README`, `MOTIF_LIEN_D63`, `MOTIF_SECTION`, `MOTIF_SOURCE`, `MOTIF_LIEN_EXTERNE`, `MOTIF_BASE_ABSENTE`, `MOTIF_EMPREINTE_CHANGEE` | 05-03 |
| constantes de contenu | `MOTIF_VOLATILE`, `MARQUES_DESTRUCTRICES`, `MARQUES_BASE`, `MARQUES_SAUVEGARDES`, `MARQUES_HORS_PARCOURS`, `COMMANDE_DESTRUCTRICE`, `JETON_PURGE`, `BALISE_COMMANDE`, `LIMITE_PERIMETRE` | 05-01 (`MOTIF_VOLATILE`), 05-02 (le reste) |
| constantes de contenu | `MOTIF_LIEN_MARKDOWN`, `CIBLES_EXTERNES`, `TITRE_SECTION_RENVOI`, `PAGE_PARCOURS`, `LIEN_RETOUR`, `TITRE_H1`, `BASE_LOCALE` | 05-03 |
| fonctions de test | `test_garde_de_cloture_du_harnais`, `test_page_et_index_de_la_base_locale`, `test_defauts_hors_ligne`, `test_champs_de_la_ligne_de_commande`, `test_champs_de_la_surface_web` | 05-01 |
| fonctions de test | `test_le_premier_contact_cree_la_base`, `test_le_refus_de_la_synchronisation`, `test_la_synchronisation_web_contacte_l_api`, `test_commandes_destructrices` | 05-02 |
| fonctions de test | `test_renvois_du_readme_resolus`, `test_renvoi_base_locale_legitime`, `test_page_close_et_sans_valeur_volatile`, `test_data_locale_non_modifiee_autour_des_rendus` | 05-03 |

## Couverture des sources (ce plan)

| Source | Element | Consomme par |
|--------|---------|--------------|
| ROADMAP § Phase 5 | Critere 5 (« chaque nom de champ ou de commande cite par la page est produit par le code, et la suite reste verte avec l'interpreteur epingle ») | taches 1 et 2, et la porte de fin de phase de la tache 3 |
| ROADMAP § Phase 5 | Critere 4, volet « les controles n'ecrivent pas sous `.data/` » | tache 3 (mesure d'empreinte) |
| ROADMAP § Phase 5 | Brief du plan 05-03 (controles d'ancrage) | les trois taches |
| REQUIREMENTS | BASE-03 (la phase porte les trois exigences ; ce plan **termine** la verification de BASE-03 : les valeurs citees par la page sont prouvees produites par le code et les ancres sont completes, les commandes destructrices elles-memes etant traitees par le plan 05-02) | `requirements` du plan |
| RESEARCH | Etat initial du `README.md` (six liens, un seul interne, vers `docs/sommaire.md`, qui resout) et Open Question 4 (le controle du critere D-87 vit dans le module neuf, les gardes de la phase 1 ne sont pas elargies) | tache 1 |
| RESEARCH | `tests/test_docs_structure.py::problemes_liens` (resolveur scope a `docs/`, interdits du schema de fichier local et des chemins absolus) | tache 1 (motif du resolveur local) |
| RESEARCH | Patron `_empreinte` (`tests/test_docs_parcours.py:2557+`, `tests/test_docs_wizard.py:2289+`) et `test_data_locale_non_modifiee_autour_des_rendus` (`tests/test_docs_wizard.py`), limite L-2 (`04-SECURITY.md`) | tache 3 |
| RESEARCH | Patron « temoin vert puis mutation » (`tests/test_docs_wizard.py:2666-2720`), morsures sur copie verte (D-84) | taches 1 et 2 |
| RESEARCH | Pitfall 10 (la fonction d'empreinte est copiee, jamais factorisee), Pitfall 11 (l'assertion CRLF depend de `core.autocrlf`, aucun `.gitattributes`) | taches 2 et 3 |
| RESEARCH | Limites de ce que la phase ne peut pas prouver (prose libre, portabilite de l'assertion CRLF, execution du JavaScript) | taches 1, 2 et 3 (backstops declares) |
| CONTEXT | D-87 (les renvois du `README.md` resolvent ; le mandat du README est cette resolution, rien de plus) | tache 1 |
| CONTEXT | D-63, D-44, D-86 (la dette est denouee par un renvoi legitime, jamais par une suppression de controle) | tache 1 |
| CONTEXT | D-67, D-69 (octets CRLF sans BOM, page close, ligne de retour) | tache 2 |
| CONTEXT | D-73, D-75 (aucune valeur volatile, aucune aide de parseur recopiee) | tache 2 |
| CONTEXT | D-03, D-12, D-17 (aucun lien externe, helpers de `conftest.py` non dupliques, une page par sujet a son proprietaire) | taches 1 et 2 |
| CONTEXT | D-81, D-83, D-84, D-85 (integrite mesuree, constats localisants, morsures, limites declarees) | taches 1, 2 et 3 |
| CONTEXT | D-88, D-89, D-91 (code non modifie, aucune resynchronisation, commits locaux indexes par chemin) | les trois taches (contraintes de commit) |

**Audit de complétude de la phase (les quatre types de sources)** : GOAL — le critere 5 et le volet integrite du critere 4 sont couverts par ce plan, les criteres 1 a 3 par les plans 05-01 et 05-02 ; REQ — `BASE-03` ici et en 05-02, `BASE-01` et `BASE-02` en 05-01 ; RESEARCH — les renvois du README, le patron d'empreinte, le patron de morsure, les pieges 10 et 11 et les limites sont consommes ici, les sections 9 et 10, l'inventaire destructeur, les pieges 1 a 8 et le verdict de faisabilite par les plans 05-01 et 05-02 ; CONTEXT — D-87, D-63, D-67, D-69, D-73 et D-75 sont l'objet de ce plan, D-68 a D-76 et D-82 a D-86 le socle du plan 05-01, D-76 a D-81 les cas non evidents du plan 05-02. Exclusions assumees, conformes aux « Deferred Ideas » de `05-CONTEXT.md` : liste epinglee des huit pages et preuve de completude du corpus (phase 6), page de depannage (phase 6), glossaire (phase 6), reecriture du bloc de commandes du `README.md` (hors mandat, D-87), toute resynchronisation Dofusdude (hors perimetre).

