---
phase: 01-socle-documentaire-installation-et-harnais-v-rifiable
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - docs/installation.md
  - tests/test_docs_structure.py
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "docs/installation.md mene de Python 3.11+ au premier lancement CLI en citant la commande reelle pip install -e \".[dev]\" puis la verification .venv/Scripts/python.exe -m pytest -q (INST-01, D-07)."
    - "Le premier contact CLI enseigne est hors-ligne (python fetcher.py --offline db status) : aucune resynchronisation Dofusdude n'est declenchee par la lecture de la page (INST-01, T-01-06)."
    - "La page annonce le mode de demarrage reel de l'interface web (hors-ligne par defaut, desactivable) et l'adresse par defaut http://127.0.0.1:5000, sans recommander une autre adresse d'ecoute (INST-02, T-01-02)."
    - "Le pilotage clavier est decrit AVANT la commande de lancement web, avec le champ de saisie, F3, F7, F8, ESC, PageUp et PageDown (INST-03, D-09)."
    - "Une erreur reellement rencontree est expliquee : python fetcher.py version echoue sur une base vide avec le message du produit, et une option globale placee apres la sous-commande est refusee (D-02)."
    - "Aucun gestionnaire de paquets autre que pip n'est documente (D-08) et aucune commande destructrice n'apparait dans la page (T-01-05)."
  artifacts:
    - path: "docs/installation.md"
      provides: "Page d'installation complete au gabarit D-01 : chemin minimal, clavier, lancement web, erreurs frequentes"
      contains: "## Pilotage clavier"
      min_lines: 60
    - path: "tests/test_docs_structure.py"
      provides: "Controle d'ordre clavier avant lancement et garde des commandes destructrices"
      contains: "def test_pilotage_clavier_avant_lancement("
  key_links:
    - from: "docs/installation.md"
      to: "dofus_stuff/web/static/js/terminal.js"
      via: "touches F3, ESC, F7, F8, PageUp, PageDown et champ de saisie decrits depuis le gestionnaire keydown reel"
      pattern: "PageDown"
    - from: "docs/installation.md"
      to: "dofus_stuff/web/__main__.py"
      via: "commande de lancement web, mode hors-ligne par defaut et adresse par defaut"
      pattern: "127\\.0\\.0\\.1:5000"
    - from: "tests/test_docs_structure.py"
      to: "docs/installation.md"
      via: "position de la section clavier comparee a la position de la commande de lancement"
      pattern: "## Pilotage clavier"
  prohibitions:
    - statement: "Aucun gestionnaire de paquets autre que pip (ni uv, ni pipx, ni poetry) n'est documente comme procedure d'installation (D-08)."
      status: resolved
      verification: test
      reason: "test_documented_entry_options_are_documented et test_no_destructive_command_in_installation verrouillent la surface citee ; toute procedure inventee serait un ecart visible a la relecture du diff."
    - statement: "Aucune adresse d'ecoute autre que le defaut 127.0.0.1 n'est recommandee : documenter une exposition reseau d'un serveur de developpement sans authentification est un constat de severite haute, bloquant selon security_block_on."
      status: resolved
      verification: test
      reason: "Le seul controle qui nomme l'adresse derive l'attendu des defauts reels du parseur ; une recommandation contraire ferait diverger la page du test d'ancrage."
    - statement: "Le drapeau --debug n'entre pas dans le chemin minimal d'installation : au plus une mention « reserve au developpement » (T-01-03)."
      status: resolved
      verification: judgment
      reason: "La page decrit le chemin minimal D-07 ; le seul autre drapeau cite est celui du mode de synchronisation."
    - statement: "Aucune commande destructrice (db clear, PURGE) n'apparait dans docs/installation.md (T-01-05)."
      status: resolved
      verification: test
      reason: "test_no_destructive_command_in_installation asserte l'absence du jeton destructeur et la presence du seul premier contact autorise, --offline db status."
    - statement: "Aucun secret ni valeur de configuration n'est recopie : la page nomme au plus des variables d'environnement, jamais leur valeur (T-01-01)."
      status: resolved
      verification: test
      reason: "test_documents_are_utf8_and_not_drafts (plan 01-03) refuse les jetons de brouillon ; la page ne cite aucune valeur par defaut du produit."
    - statement: "Aucune modification sous dofus_stuff/ et aucune resynchronisation Dofusdude : la page ne fait que refleter le code."
      status: resolved
      verification: test
      reason: "Le premier contact CLI documente est hors-ligne ; aucun test de la phase n'ouvre la base ni le reseau (D-15)."
  assumptions:
    - "INST-01 — hypothese signalee (bordure non classable par la sonde d'exhaustivite, ligne unresolved) : la suite d'installation est tenue pour suffisante si la page cite le plancher de version, la commande d'installation, la commande de verification et un premier contact CLI analysable. Ce qui pourrait echapper silencieusement : une installation neuve sur une machine sans Python 3.11+ ou sans acces au reseau, que la page annonce comme prerequis mais qu'aucun test de ce depot (Windows, environnement deja installe) ne peut rejouer. La preuve automatisee s'arrete a la conformite des valeurs citees avec pyproject.toml et le parseur reel."
    - "INST-02 — hypothese signalee (ligne unresolved) : le besoin « savoir dans quel mode et sur quelle adresse elle demarre » est tenu pour satisfait par la citation du defaut hors-ligne et de l'adresse derivee du parseur. Ce qui pourrait echapper silencieusement : un lecteur qui surcharge les variables d'environnement documentees (DOFUS_OFFLINE, DOFUS_DATA_DIR) et n'obtient donc pas le comportement annonce — hors perimetre de la phase 1, qui ne documente aucune surcharge."
    - "INST-03 — hypothese signalee (ligne unresolved) : « comprendre le pilotage clavier avant de lancer » est tenu pour satisfait par l'ordre des sections de la page, verifie mecaniquement. Ce qui pourrait echapper silencieusement : un lecteur qui arrive directement sur la section de lancement par un lien ou une recherche plein texte, ou une touche citee mais rendue inoperante par un changement de JS que seul le rendu reel revelerait ; l'ancrage des libelles d'ecran appartient aux phases 3 a 5."
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 1 dans `.planning/ROADMAP.md`, de son critere de succes 3 et des decisions D-01, D-02, D-03, D-07, D-08 et D-09 de `01-CONTEXT.md` :
**En tant que** lecteur qui installe l'outil pour la premiere fois, **je veux** suivre un chemin minimal pas-a-pas jusqu'au premier lancement CLI puis web, **afin de** utiliser l'outil sans lire le code ni deviner le pilotage de l'interface.

Remplacer le squelette livre par le plan 01-01 par la page complete : prerequis, installation, verification, premier contact CLI hors-ligne, pilotage clavier decrit avant le lancement web, lancement web avec son mode et son adresse reels, puis les erreurs reellement rencontrees (D-02). Les deux gardes de la page qui l'exposent au plus gros risque (ordre clavier, commande destructrice) sont ecrites dans le meme plan.

Purpose : satisfaire les criteres de succes 3 et 4 de la phase, et rendre la page auto-suffisante pour un lecteur qui n'a jamais vu le depot. La page est ecrite depuis le code reel : `.venv` et aptitudes mesurees dans `01-RESEARCH.md` § References reelles pour `docs/installation.md`.

Output : `docs/installation.md` complet (gabarit D-01, bloc « Source de verite ») et deux nouveaux tests de structure : `test_pilotage_clavier_avant_lancement`, `test_no_destructive_command_in_installation`.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-RESEARCH.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-PATTERNS.md
@docs/installation.md
@tests/test_docs_structure.py
@pyproject.toml
@dofus_stuff/web/__main__.py
</context>

<tasks>

<task type="auto">
  <name>Tache 1 : chemin minimal d'installation, verification et premier contact CLI</name>
  <files>docs/installation.md</files>
  <read_first>
    docs/installation.md
    pyproject.toml
    dofus_stuff/cli.py
    dofus_stuff/sync.py
    README.md
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-RESEARCH.md
  </read_first>
  <action>Etendre `docs/installation.md` en conservant le H1 `# Installation` (libelle impose par l'index du sommaire, SOMM-03), la phrase d'introduction et la ligne finale de retour vers `sommaire.md` (D-01). Ordre des sections en phase 1 : `## Prerequis`, `## Installation`, `## Verification`, `## Premier lancement`, puis les sections ajoutees par la tache 2, puis `## Source de verite` et la ligne de retour.

Contenu attendu :
- `## Prerequis` : Python 3.11+ est le plancher declare par `pyproject.toml` (`requires-python = ">=3.11"`), et l'extra `dev` porte `pytest` ; la phrase doit rendre ces deux valeurs verifiables dans le fichier cite.
- `## Installation` : creation de l'environnement virtuel puis la commande exacte `pip install -e ".[dev]"`, presentee comme la seule procedure (D-08 : ni `uv`, ni `pipx`, ni `poetry`).
- `## Verification` : `.venv/Scripts/python.exe -m pytest -q` est la commande de verification, avec la precision que le nombre de tests affiche varie a chaque phase et ne doit pas etre cite comme une valeur de reference.
- `## Premier lancement` : `python fetcher.py --offline db status` comme premier contact hors-ligne (aucune resynchronisation Dofusdude), puis la mention que `python fetcher.py version` necessite une base peuplee.
- `## Erreurs frequentes` (D-02, uniquement des erreurs reellement rencontrees) : (a) `python fetcher.py version` sur une base vide echoue avec le message du produit `Base locale vide et --offline : impossible de synchroniser` (source `dofus_stuff/sync.py`) ; (b) une option globale placee apres la sous-commande, du type `db status --offline`, est refusee par le parseur avec le message `unrecognized arguments: --offline` (source `dofus_stuff/cli.py`) ; (c) l'ordre correct est rappele : l'option globale precede la sous-commande.
- `## Source de verite` : conserver les chemins reels `pyproject.toml`, `fetcher.py`, `dofus_stuff/cli.py` (D-03) ; ces chemins sont ancres par le plan 01-04.

Ecrire les commandes dans des blocs de code markdown, en francais pour la prose, identifiants et chemins de code en anglais.</action>
  <acceptance_criteria>
    - `docs/installation.md` contient les chaines `pip install -e ".[dev]"`, `.venv/Scripts/python.exe -m pytest -q` et `python fetcher.py --offline db status` verbatim.
    - La page cite le plancher `3.11` et les mots `dev` et `pytest` dans la section des prerequis.
    - La page nomme au moins une erreur reellement rencontree avec le message produit `Base locale vide et --offline : impossible de synchroniser` et l'ordre option globale avant sous-commande.
    - La page ne cite aucun gestionnaire de paquets autre que `pip` et n'ecrit aucun compteur de tests comme valeur de reference.
    - Le H1 reste `# Installation` et la ligne de retour vers `sommaire.md` est conservee (D-01).
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'pip install -e "\.\[dev\]"' docs/installation.md && grep -n '\.venv/Scripts/python.exe -m pytest -q' docs/installation.md && grep -n 'python fetcher.py --offline db status' docs/installation.md</automated>
    <fails_when>exit non-zero, ou le motif manquant ne produit aucune ligne imprimee (aucun numero de ligne affiche pour l'un des trois motifs)</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
  </verify>
  <done>La page mene un lecteur neuf de Python 3.11+ au premier contact CLI hors-ligne, avec les erreurs reellement rencontrees, en conservant le H1 et la ligne de retour.</done>
</task>

<task type="auto">
  <name>Tache 2 : clavier avant lancement, mode et adresse web, et leurs deux gardes</name>
  <files>docs/installation.md, tests/test_docs_structure.py</files>
  <read_first>
    docs/installation.md
    tests/test_docs_structure.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/static/js/terminal.js
    dofus_stuff/web/__main__.py
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-RESEARCH.md
  </read_first>
  <action>1. Dans `docs/installation.md`, inserer deux sections dans cet ordre strict (D-09), entre `## Premier lancement` et `## Erreurs frequentes` :
   - `## Pilotage clavier` : decrire le champ de saisie (on tape la reponse dans le champ puis on valide par la touche Entree (nom d'evenement `Enter`)), `F3` (quitter l'interface, libelle `Quitter`, seule touche visible sur le menu principal), `F7` et `F8` (navigation precedent / suivant, libelles `Precedent`, `Page prec`, `Suivant`, `Page suiv`), `ESC` (retour, nom d'evenement `Escape`), `PageUp` et `PageDown` (pagination de la page precedente et suivante). Citer comme sources `dofus_stuff/web/routes.py` et `dofus_stuff/web/static/js/terminal.js`.
   - `## Lancement de l'interface web` : la commande `python -m dofus_stuff.web` (elle n'apparait que dans cette section), l'interface demarre hors-ligne par defaut (`--offline` actif par defaut, desactivable par `--no-offline` ou `--online`), l'adresse par defaut `http://127.0.0.1:5000`, et rien d'autre comme adresse d'ecoute. Mentionner `Ctrl+C` pour arreter le serveur. Le drapeau `--debug` n'apparait pas dans ce chemin, ou seulement accompagne d'une mention « reserve au developpement ».
   Etendre `## Source de verite` avec les deux chemins cites ci-dessus s'ils n'y figurent pas encore (D-03).

2. Dans `tests/test_docs_structure.py`, ajouter deux tests utilisant les helpers et fixtures existants (`docs_dir`, `_pages`, `LINK`), avec messages localisants (D-13) :
   - `test_pilotage_clavier_avant_lancement(docs_dir)` : lire la page avec `encoding="utf-8"` ; extraire la section `## Pilotage clavier` jusqu'au titre de niveau 2 suivant ; exiger que la position de ce titre soit inferieure a la position de la chaine `python -m dofus_stuff.web` ; exiger que chacune des touches `F3`, `F7`, `F8`, `ESC`, `PageUp`, `PageDown` apparaisse dans la section clavier. Message d'echec citant `docs/installation.md`, la touche ou l'ordre attendu, et la source `dofus_stuff/web/static/js/terminal.js`.
   - `test_no_destructive_command_in_installation(docs_dir)` : exiger la presence de `--offline db status` et l'absence du jeton de commande destructrice de `dofus_stuff/cli.py` (`db` suivi de `clear`) ainsi que du jeton `PURGE` ; message citant `docs/installation.md`, le jeton trouve et la source du jeton (`dofus_stuff/cli.py` pour `clear`, `dofus_stuff/web/routes.py` pour `PURGE OUI`).</action>
  <acceptance_criteria>
    - La section `## Pilotage clavier` precede la section `## Lancement de l'interface web`, qui precede `## Erreurs frequentes`, et la chaine `python -m dofus_stuff.web` n'apparait qu'apres le titre clavier.
    - Les sept elements du pilotage sont documentes : champ de saisie, `F3`, `F7`, `F8`, `ESC`, `PageUp`, `PageDown`.
    - La page cite `http://127.0.0.1:5000`, declare le demarrage hors-ligne par defaut et ne recommande aucune autre adresse d'ecoute.
    - `test_pilotage_clavier_avant_lancement` et `test_no_destructive_command_in_installation` passent, et leurs messages d'echec citent la page, le libelle ou le jeton attendu et le fichier source (D-13).
    - Les tests precedents du module restent verts.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py::test_pilotage_clavier_avant_lancement tests/test_docs_structure.py::test_no_destructive_command_in_installation -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
    <automated>grep -n 'http://127.0.0.1:5000' docs/installation.md && grep -n 'python -m dofus_stuff.web' docs/installation.md</automated>
    <fails_when>exit non-zero, ou aucun numero de ligne imprime pour l'un des deux motifs</fails_when>
  </verify>
  <done>Le clavier est decrit avant le lancement web, l'adresse et le mode par defaut sont ceux du code, et les deux gardes correspondantes sont vertes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redacteur -> lecteur debutant | La page d'installation est la premiere surface suivie aveuglement : une commande inventee ou une adresse permissive y est executee telle quelle. |
| Page -> produit fige | Toute valeur citee (version, mode, adresse, touche, message d'erreur) doit venir de `pyproject.toml`, de `dofus_stuff/web/__main__.py`, de `dofus_stuff/web/static/js/terminal.js` ou de `dofus_stuff/cli.py`. |
| Documentation -> quota Dofusdude | Une procedure qui declenche une synchronisation surprend le lecteur et consomme un quota distant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Information Disclosure / Elevation of Privilege | `docs/installation.md` § Lancement de l'interface web | high | mitigate | Seule l'adresse par defaut du parseur est documentee ; aucune recommandation d'exposition reseau n'est ecrite, et le controle d'ancrage du plan 01-04 derive l'adresse attendue des defauts reels du parseur. |
| T-01-03 | Elevation of Privilege | `docs/installation.md` § Lancement | high | mitigate | Le mode debogage reste hors du chemin minimal (D-07) ; au plus une mention « reserve au developpement », et aucun appel a `main()` n'est ecrit dans un exemple executable. |
| T-01-05 | Tampering / Denial of Service | `docs/installation.md` | high | mitigate | `test_no_destructive_command_in_installation` refuse le jeton destructeur et `PURGE` dans la page ; le seul contact CLI enseigne est `--offline db status`. |
| T-01-06 | Denial of Service (quota) | `docs/installation.md` § Premier lancement | medium | mitigate | Le premier contact CLI documente est hors-ligne ; aucune instruction de la page ne declenche de resynchronisation. |
| T-01-01 | Information Disclosure | `docs/installation.md` § Prerequis et erreurs | low | mitigate | La page ne cite aucune valeur de configuration par defaut ; seuls des noms de fichiers et de commandes apparaissent. |
</threat_model>

<verification>
- `./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q` : module vert apres chaque ajout.
- `./.venv/Scripts/python.exe -m pytest -q` : suite complete verte (les 136 tests existants restent verts).
- `./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py::test_mutation_detecte_les_trois_derives -q` n'est attendu qu'apres le plan 01-03 (le test de mutation appartient a ce plan).
- Verification de non-intrusion : horodatage de `.data/dofus.sqlite3` inchange apres la suite ; aucun fichier de `dofus_stuff/` modifie (`git status --porcelain`).
</verification>

<success_criteria>
- Le critere de succes 3 de la phase est vrai : la page mene de Python 3.11+ au premier lancement CLI et web avec l'adresse par defaut, le pilotage clavier etant decrit avant le lancement.
- Les exigences INST-01, INST-02 et INST-03 sont couvertes par du contenu verifiable, pas par de la prose non controlee.
- Les deux interdits de redaction mesures par la recherche (adresse d'ecoute permissive, commande destructrice) sont refusables par un test.
</success_criteria>

## Artifacts this phase produces

Plan 01-02 :

- `docs/installation.md` : sections `## Prerequis`, `## Installation`, `## Verification`, `## Premier lancement`, `## Pilotage clavier`, `## Lancement de l'interface web`, `## Erreurs frequentes`, `## Source de verite`.
- `tests/test_docs_structure.py` : `test_pilotage_clavier_avant_lancement(docs_dir)`, `test_no_destructive_command_in_installation(docs_dir)`.

<output>
Create `.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-02-SUMMARY.md` when done.
</output>
