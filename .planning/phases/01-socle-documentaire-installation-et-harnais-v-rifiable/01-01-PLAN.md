---
phase: 01-socle-documentaire-installation-et-harnais-v-rifiable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/sommaire.md
  - docs/installation.md
  - tests/conftest.py
  - tests/test_docs_structure.py
autonomous: true
requirements:
  - SOMM-01
  - SOMM-02
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Depuis README.md, un lien markdown unique mene a docs/sommaire.md et la cible existe sur disque (SOMM-01, D-10)."
    - "Les cibles de liens de docs/sommaire.md forment exactement l'ensemble des pages presentes sous docs/ : en phase 1, une seule entree d'index, installation.md, et aucune autre cible de lien — ni ligne de retour vers ../README.md, ni lien dans l'introduction ou dans le parcours conseille (SOMM-02, D-04, D-05, D-06)."
    - "Le parcours guide du sommaire est ordonne en texte simple (sept themes) et l'index thematique ne porte que des cibles existantes (D-04, D-05)."
    - "docs/installation.md existe avec un H1 unique egal au libelle d'index et une ligne de retour vers sommaire.md (SOMM-03 amorce, D-01)."
    - "tests/conftest.py expose la normalisation de D-11 et les fixtures docs_dir / normalize sans modifier les fixtures existantes (D-11, D-12)."
    - "tests/test_docs_structure.py refuse une cible de lien morte, une cible hors racine du depot, une ancre #, un lien absolu, et une page non listee dans le sommaire (SOMM-02, T-01-04)."
  artifacts:
    - path: "docs/sommaire.md"
      provides: "Point d'entree unique de la documentation : parcours guide puis index thematique (D-04, D-05)"
      contains: "## "
      min_lines: 15
    - path: "docs/installation.md"
      provides: "Squelette au gabarit leger D-01, complete par le plan 01-02"
      contains: "# Installation"
    - path: "tests/conftest.py"
      provides: "Helper de normalisation D-11 et fixtures de documentation D-12"
      exports: ["docs_dir", "normalize"]
    - path: "tests/test_docs_structure.py"
      provides: "Premiers invariants documentaires, executes sur l'arbre livre"
      contains: "def problemes_index("
    - path: "README.md"
      provides: "Section Documentation utilisateur a lien unique (SOMM-01, D-10)"
      contains: "## Documentation utilisateur"
  key_links:
    - from: "README.md"
      to: "docs/sommaire.md"
      via: "lien markdown unique de la section Documentation utilisateur (D-10)"
      pattern: "docs/sommaire\\.md"
    - from: "docs/sommaire.md"
      to: "docs/installation.md"
      via: "ligne du tableau d'index thematique"
      pattern: "\\]\\(installation\\.md\\)"
    - from: "tests/test_docs_structure.py"
      to: "tests/conftest.py"
      via: "fixtures docs_dir et normalize injectees par pytest (D-12)"
      pattern: "docs_dir: Path"
  prohibitions:
    - statement: "Aucune modification sous dofus_stuff/ ni dans pyproject.toml : le produit est fige par la phase."
      status: resolved
      verification: judgment
      reason: "files_modified de ce plan ne contient que README.md, docs/ et tests/."
    - statement: "Aucune nouvelle dependance : les tests s'appuient sur la bibliotheque standard et sur pytest deja installe."
      status: resolved
      verification: test
      reason: "tests/test_docs_structure.py n'importe que re, pathlib et shutil ; aucune ligne ajoutee a pyproject.toml."
    - statement: "Aucune ecriture sous .data/ et aucun acces reseau depuis le harnais (D-15)."
      status: resolved
      verification: test
      reason: "Les invariants lisent des fichiers Markdown ; les fixtures existantes gardent data_dir=tmp_path, offline=True et load_catalog=False."
    - statement: "README.md ne recoit aucun second lien vers docs/ et la ligne existante vers GUIDE_WIZARD.md reste inchangee (phase 4)."
      status: resolved
      verification: test
      reason: "test_readme_links_to_sommaire compte les liens vers docs/sommaire.md et exige un lien unique."
    - statement: "Aucune publication, aucun deploiement distant, aucun achat, aucune suppression de donnees (pas de db clear, pas de drop SQLite)."
      status: resolved
      verification: judgment
      reason: "Aucune commande de ce plan n'ecrit hors du depot ni n'interroge un service distant."
  assumptions:
    - "SOMM-01 — hypothese signalee (bordure non classable par la sonde d'exhaustivite, ligne unresolved) : le besoin est considere satisfait par un lien markdown resolvable depuis README.md. Ce qui pourrait echapper silencieusement : un lecteur qui n'ouvre jamais le README, ou un lecteur a qui le lien est rendu en texte non cliquable. Aucun controle de cette phase ne peut l'observer ; le lien resolvable est la seule preuve automatisable."
    - "SOMM-02 — hypothese signalee (bordure non classable, ligne unresolved) : l'egalite d'ensembles sommaire <-> docs/**/*.md est tenue pour equivalente a « chaque page est listee, et reciproquement ». Ce qui pourrait echapper silencieusement : une page ajoutee par une phase ulterieure et oubliee dans l'index, non observable ici puisqu'une seule page existe en phase 1 (D-05, D-06) ; l'egalite d'ensembles reste le signal qui fera echouer la suite."
---

<objective>
**Cadrage derive (non une decision produit nouvelle)** — repris du `**Goal:**` de la phase 1 dans `.planning/ROADMAP.md`, de ses criteres de succes 1 a 3 et des decisions D-01, D-04, D-05, D-06, D-10, D-11 et D-12 de `01-CONTEXT.md` :
**En tant que** lecteur qui n'a jamais vu le projet, **je veux** trouver la documentation depuis le `README.md` et atteindre une page d'installation reelle, **afin de** installer l'outil et le lancer sans lire le code.

Poser la tranche verticale la plus fine de la phase : la chaine documentaire `README.md` -> `docs/sommaire.md` -> `docs/installation.md` existe sur disque, et les trois premiers invariants qui la gardent sont executes par `pytest` dans le meme plan (D-06). Le gabarit de page applique des ce plan est celui de D-01 : un H1 unique, une phrase d'introduction, des sections courtes, un bloc « Source de verite » et une ligne de retour vers le sommaire.

Purpose : c'est le squelette marcheur du milestone documentaire. Une seule page est livree (`installation.md`), donc l'exhaustivite bidirectionnelle du sommaire reste un vrai signal pendant les cinq phases suivantes (D-05, D-06). Constituer le harnais des maintenant evite de livrer deux pages non gardees ; le mode de defaillance « documentation qui ment » est mesure dans `.planning/research/PITFALLS.md` (aucun des 136 tests existants ne lit un fichier de documentation).

Output : `docs/sommaire.md`, un `docs/installation.md` squelette conforme a D-01, la section « Documentation utilisateur » de `README.md` (lien unique, D-10), et les six premiers tests de `tests/test_docs_structure.py` alimentes par les nouvelles fixtures de `tests/conftest.py`.

Ecart assume au decoupage indicatif du ROADMAP : la decomposition verticale imposee par le mode tracer (tranche fine de bout en bout livree avec son premier garde-fou) fait que le createur de `tests/conftest.py` et des premiers tests de `tests/test_docs_structure.py` est ce plan, et non le plan 01-03, qui complete le module. Le rattachement des exigences reste celui du contrat de validation de la phase (SOMM-01 et SOMM-02 ici).
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-RESEARCH.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-PATTERNS.md
@README.md
@tests/conftest.py
@pyproject.toml
</context>

<tasks>

<task type="tracer">
  <name>Tache 1 : chaine documentaire de bout en bout README.md -> docs/sommaire.md -> docs/installation.md</name>
  <files>README.md, docs/sommaire.md, docs/installation.md</files>
  <read_first>
    README.md
    docs/sommaire.md
    docs/installation.md
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-RESEARCH.md
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-PATTERNS.md
  </read_first>
  <action>Ouvrir les trois maillons de la chaine, en une seule passe, avec du contenu reel (pas de page vide).

1. `README.md` : inserer une section de niveau 2 intitulee exactement `## Documentation utilisateur` immediatement avant la ligne `## Base locale Dofus`. La section contient une phrase et un lien markdown unique vers `docs/sommaire.md` (cible relative `docs/sommaire.md`, libelle en francais, par exemple `Sommaire de la documentation`) — un seul lien, aucun lien direct vers une page individuelle (D-10). Ne pas modifier, deplacer ni supprimer la ligne existante `**Guide detaille :** [GUIDE_WIZARD.md](GUIDE_WIZARD.md)` : sa correction appartient a la phase 4.

2. `docs/sommaire.md` (nouveau fichier, encodage UTF-8, fins de ligne libres) : H1 `# Sommaire de la documentation`, une phrase d'introduction, puis deux sections de niveau 2 — `## Parcours conseille` : liste numerotee en texte simple des sept themes dans l'ordre `Installation`, `Parcours simplifie`, `Wizard avance`, `CLI`, `Base locale`, `Depannage`, `Glossaire`, sans aucun lien markdown (une cible absente ferait rougir le controle de liens, D-06) ; `## Index` : tableau markdown a une seule ligne de donnees, de la forme `| Page | Sujet |` puis `| [Installation](installation.md) | Installer l'outil, verifier, lancer CLI et web |`. Le libelle d'index est exactement `Installation` : c'est lui que le H1 de `docs/installation.md` doit reprendre (SOMM-03, amorce ici). **Aucune autre cible de lien que les entrees du tableau d'index** : la page ne porte aucune ligne de retour vers `../README.md`, aucun lien dans sa phrase d'introduction et aucun lien dans le parcours conseille. C'est la condition d'application de l'invariant de la tache 2 (egalite d'ensembles entre les cibles de liens du sommaire et les noms de pages presentes sous `docs/`) : une cible `../README.md` ne resout pas sous `docs/`, donc `test_sommaire_lists_every_document` serait rouge sur l'arbre livre et l'etape 2 du test de mutation du plan 01-03 (« l'arbre livre est sain avant mutation ») serait rouge par construction. Le cadre est explicite : le critere de succes 2 du ROADMAP exige que le sommaire « liste exactement les pages `docs/**/*.md` presentes », le prototype de `01-RESEARCH.md` § Pattern 1 lit *toutes* les cibles de liens du sommaire, et D-06 interdit toute exception et toute tolerance pour cible absente. Le retour du lecteur vers `README.md` n'est donc pas porte par le sommaire : l'aller est le lien unique du `README.md` (D-10), et la ligne de retour de D-01 decrit une page de contenu qui revient *vers* `sommaire.md` — le sommaire lui-meme est hors de cette regle, ce que l'invariant `problemes_retour_sommaire` du plan 01-03 encode deja en exemptant `sommaire.md`.

3. `docs/installation.md` (nouveau fichier) : squelette au gabarit leger D-01, volontairement partiel — le contenu complet arrive dans le plan 01-02. H1 `# Installation`, une phrase d'introduction, puis les sections `## Prerequis` (Python 3.11+), `## Installation` (commande `pip install -e ".[dev]"`), `## Verification` (`.venv/Scripts/python.exe -m pytest -q`), `## Premier lancement` (CLI, `python fetcher.py --offline db status`), un bloc `## Source de verite` listant les chemins reels `pyproject.toml`, `fetcher.py`, `dofus_stuff/cli.py`, et la ligne finale de retour `[Retour au sommaire](sommaire.md)`. Aucun front-matter, aucun bloc de metadonnees d'en-tete (D-01).

La chaine est verifiee de bout en bout par une sonde qui resout chaque maillon sur disque depuis la racine du depot.</action>
  <acceptance_criteria>
    - `README.md` contient `## Documentation utilisateur` place avant `## Base locale Dofus`, et contient la cible `docs/sommaire.md` un nombre de fois egal a 1 (D-10).
    - `docs/sommaire.md` contient un H1 `# Sommaire de la documentation`, la section `## Parcours conseille` avec les sept themes dans l'ordre impose par D-04, et la section `## Index` avec le libelle `Installation` cible `installation.md`.
    - Les cibles de liens markdown de `docs/sommaire.md` forment exactement l'ensemble `{installation.md}` : aucune cible hors de `docs/` (en particulier aucune ligne de retour vers `../README.md`), et aucun lien dans la phrase d'introduction ni dans le parcours conseille (SOMM-02, D-06, critere de succes 2).
    - `docs/installation.md` commence par `# Installation`, porte les cinq sections listees, le bloc `## Source de verite` avec `pyproject.toml`, `fetcher.py` et `dofus_stuff/cli.py`, et la ligne de retour vers `sommaire.md`.
    - La sonde de bout en bout resout les deux maillons et imprime `chaine README -> docs/sommaire.md -> docs/installation.md resolue` (D-15 : aucun reseau, aucune ecriture sous `.data/`, aucune execution de `main()`).
    - La ligne `**Guide detaille :** [GUIDE_WIZARD.md](GUIDE_WIZARD.md)` de `README.md` est inchangee.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -c "import re; from pathlib import Path; r=Path('README.md').read_text(encoding='utf-8'); cible='docs/sommaire.md'; assert cible in r and Path(cible).exists(), f'README.md ne mene pas a {cible}'; s=Path(cible).read_text(encoding='utf-8'); assert 'installation.md' in s, 'docs/sommaire.md ne liste pas installation.md'; cibles=set(re.findall(r'\]\(([^)\s]+)\)', s)); assert cibles == {'installation.md'}, f'cibles de liens du sommaire : {sorted(cibles)} ; attendu exactement installation.md, aucune cible hors de docs/ comme ../README.md (SOMM-02, D-06)'; p=Path('docs/installation.md'); t=p.read_text(encoding='utf-8'); assert t.startswith('# '), 'docs/installation.md sans H1 en tete'; assert '(sommaire.md)' in t, 'docs/installation.md sans ligne de retour'; print('chaine README -> docs/sommaire.md -> docs/installation.md resolue')"</automated>
    <fails_when>exit non-zero avec AssertionError, dont le message nomme le maillon casse (par exemple « README.md ne mene pas a docs/sommaire.md »)</fails_when>
  </verify>
  <done>Les trois fichiers existent, la chaine README -> sommaire -> installation est resolue par la sonde, et le commit ne contient que ces trois chemins.</done>
</task>

<task type="auto">
  <name>Tache 2 : fixtures et helper de normalisation, puis six invariants de la chaine</name>
  <files>tests/conftest.py, tests/test_docs_structure.py</files>
  <read_first>
    tests/conftest.py
    tests/test_docs_structure.py
    tests/test_screens.py
    docs/sommaire.md
    docs/installation.md
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-RESEARCH.md
    .planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-PATTERNS.md
  </read_first>
  <action>Etendre le harnais pour que la chaine soit gardee par le juge `pytest` (D-11, D-12), sans toucher aux fixtures existantes.

1. `tests/conftest.py` (fichier existant) : a la fin du fichier, apres la fixture `client`, ajouter les imports `html`, `re`, `unicodedata` et `from pathlib import Path`, la fonction de module `_normalize(text: str) -> str` implementant D-11 (decodage des entites HTML, decomposition `unicodedata.normalize("NFKD", ...)`, retrait des marques combinantes, reduction de tout `\s+` en une espace, `strip()`, minuscules), puis deux fixtures de portee session avec docstrings en francais : `docs_dir` renvoyant `Path(__file__).resolve().parents[1] / "docs"` (jamais `Path("docs")`, Pitfall du repertoire courant) et `normalize` renvoyant `_normalize` (expose le helper sans import inter-modules). Les fixtures `catalog`, `app`, `client` et `_sample_items` ne changent pas.

2. `tests/test_docs_structure.py` (nouveau module, docstring francaise, n'importe pas le produit : bibliotheque standard seulement) : definir les deux motifs `LINK = re.compile(r"\[[^\]]*\]\((?P<target>[^)\s]+)\)")` et `H1 = re.compile(r"^#\s+(?P<title>.+?)\s*$", re.MULTILINE)`, puis les fonctions pures au niveau module, parametrees par `docs_dir: Path` et renvoyant `list[str]` de problemes formates selon D-13 (liste vide = conforme) :
   - `_pages(docs_dir)` : `sorted(docs_dir.rglob("*.md"))`, liste vide si le dossier manque, sans jamais lever d'exception ;
   - `problemes_index(docs_dir)` et `problemes_liens(docs_dir)` renvoient le probleme lisible `docs/sommaire.md : absent` quand le sommaire n'existe pas, au lieu de lever `FileNotFoundError` (D-13) ; `problemes_liens(docs_dir)` parcourt ensuite chaque page et chaque cible de lien ; ignorer `http://`, `https://`, `mailto:` et les cibles commencant par `#` ; signaler une cible absolue, une cible contenant un antislash, une cible portant le schema `file://`, une cible qui resout hors de la racine du depot (`docs_dir.parent`), et une cible qui n'existe pas ; message avec la page, la cible et la racine attendue ;
   - puis `problemes_index(docs_dir)` : egalite d'ensembles entre les cibles du sommaire et les noms de pages presentes hors `sommaire.md` ; message `docs/sommaire.md : cible listee absente sur disque : X` et message `X : page non listee dans docs/sommaire.md` (SOMM-02, D-06).

3. Les six tests du module, noms de fonctions en anglais, docstrings en francais, chaque assertion portant un message localisant (page concernee + libelle ou cible attendue + fichier de code ou de reference, D-13) : `test_docs_directory_has_sommaire`, `test_sommaire_lists_every_document` (problemes d'index vides), `test_sommaire_links_resolve`, `test_all_relative_links_resolve` (problemes de liens vides sur tout `docs/`), `test_no_anchor_or_absolute_links` (aucune ancre `#`, aucune cible absolue, aucun antislash, aucun `file://`), `test_readme_links_to_sommaire` (lire `README.md` avec `encoding="utf-8"`, compter les liens dont la cible est `docs/sommaire.md` via le motif `LINK`, exiger un compte egal a 1 et l'existence de la cible, message citant `README.md` et `docs/sommaire.md`).</action>
  <acceptance_criteria>
    - `tests/conftest.py` expose `_normalize`, `docs_dir` et `normalize`, et les fixtures `catalog`, `app`, `client` sont byte-identiques a l'etat anterieur.
    - `tests/test_docs_structure.py` n'importe aucun module de `dofus_stuff` (aucun import produit), et definit les helpers `_pages`, `problemes_liens`, `problemes_index` au niveau module.
    - Les six tests du module passent sur l'arbre livre : l'execution du module seul n'affiche ni "failed" ni "error".
    - Chaque message d'echec potentiel contient la page, la cible ou le libelle attendu et le fichier de reference (D-13).
    - Aucune ecriture sous `.data/` : le module ne lit que des fichiers Markdown (D-15).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le resume pytest</fails_when>
  </verify>
  <done>Le module de structure passe, les fixtures de documentation sont en place et le commit separe le harnais des trois fichiers de documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redacteur -> `docs/` | Une cible de lien ecrite a la main franchit cette frontiere : elle peut sortir du depot, viser un fichier absent ou porter une ancre non resolvable. |
| Depot -> lecteur (`README.md`) | Le point d'entree expose au lecteur : un second lien ou un lien mort y casse la promesse SOMM-01. |
| Harnais -> systeme | Les tests lisent le disque ; toute ecriture ou tout acces reseau franchirait une frontiere non autorisee (D-15). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `README.md`, `docs/sommaire.md` | low | mitigate | Les deux pages ne contiennent que des liens et des libelles : aucun nom de variable d'environnement, aucune valeur de configuration, aucun secret recopie. |
| T-01-04 | Tampering | `problemes_liens` / `problemes_index` dans `tests/test_docs_structure.py` | medium | mitigate | Le helper resout chaque cible avec `Path.resolve()` et refuse les cibles absolues, les ancres `#`, les antislashs, `file://` et tout chemin sortant de `docs_dir.parent`. |
| T-01-08 | Information Disclosure | `tests/test_docs_structure.py`, `tests/conftest.py` | medium | mitigate | Le module n'importe pas le produit et n'ouvre aucune socket ; les fixtures de ce plan ne construisent ni `app` ni `Catalog`. Execution de reference strictement locale (D-15). |
</threat_model>

<verification>
- `./.venv/Scripts/python.exe -m pytest tests/test_docs_structure.py -q` : module vert.
- `./.venv/Scripts/python.exe -m pytest -q` : suite complete verte (aucune regression sur les 136 tests existants).
- `git status --porcelain` apres commit : seuls `README.md`, `docs/sommaire.md`, `docs/installation.md`, `tests/conftest.py`, `tests/test_docs_structure.py` apparaissent comme modifies ou nouveaux par ce plan.
- Horodatage de `.data/dofus.sqlite3` inchange apres execution de la suite.
</verification>

<success_criteria>
- Le critere de succes 1 de la phase est vrai : `README.md` porte une section « Documentation utilisateur » dont la cible `docs/sommaire.md` existe sur disque.
- Le critere de succes 2 est vrai dans sa version de phase 1 : le sommaire liste exactement les pages presentes (une seule) et chaque page porte un H1 egal a son libelle d'index.
- La chaine est gardee par des tests et non par relecture humaine : un lien mort, une page non listee ou une cible hors depot font echouer la suite.
</success_criteria>

## Artifacts this phase produces

Plan 01-01 (tranche verticale de la phase) :

- `README.md` : nouvelle section `## Documentation utilisateur`.
- `docs/sommaire.md` : nouveaux chemins `docs/` et point d'entree unique.
- `docs/installation.md` : page au gabarit D-01 (completee au plan 01-02).
- `tests/conftest.py` : `_normalize(text: str) -> str`, fixtures `docs_dir`, `normalize`.
- `tests/test_docs_structure.py` : motifs `LINK`, `H1` ; helpers de module `_pages(docs_dir)`, `problemes_liens(docs_dir)`, `problemes_index(docs_dir)` ; tests `test_docs_directory_has_sommaire`, `test_sommaire_lists_every_document`, `test_sommaire_links_resolve`, `test_all_relative_links_resolve`, `test_no_anchor_or_absolute_links`, `test_readme_links_to_sommaire`.

<output>
Create `.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-01-SUMMARY.md` when done.
</output>
