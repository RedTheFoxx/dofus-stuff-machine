---
phase: 04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - docs/wizard-avance.md
  - tests/test_docs_wizard.py
autonomous: true
requirements:
  - WIZ-02
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Le chemin d'arrivée réel est **mesuré pas à pas** et cité par la page : `POST /` avec `selection=4` redirige vers `/optimize`, `GET /optimize` redirige vers `/optimize/quick/classe`, puis les trois questions s'enchaînent (`classe`, `elements`, `niveau`) et `POST /optimize/quick/niveau` avec `AVANCE` redirige vers `/optimize/wizard/recap` — donc l'arrivée est le **récapitulatif**, pas les slots (D-51) ; la page dit ce chemin, et casser `AVANCE` dans `dofus_stuff/web/routes.py:948` fait rougir le module (constat portant le jeton `arrivee du wizard`) — critère 3 du ROADMAP, WIZ-02."
    - "Les **identifiants d'écrans** sont lus au rendu et cités par la page : chaque étape porte `OPT-W<n>` dans sa ligne d'en-tête (`dofus_stuff/web/routes.py:1159` et `:1176`), le sous-écran d'édition porte `OPT-WED`, et la page associe chaque étape à son identifiant ; décaler le calcul de l'index (`WIZARD_STEPS.index(step) + 1`) fait rougir le module (constat portant le jeton `identifiant d'ecran`)."
    - "Les **couples touche/libellé** sont exigés **par étape**, extrémités comprises : l'étape 1 rend `Page prec` et `Suivant`, les étapes 2 à 8 rendent `Precedent` et `Suivant`, l'étape 9 rend `Precedent` et `Page suiv`, et `ESC`/`Retour` est rendu sur les neuf — chaque couple est lu au rendu (`dofus_stuff/web/routes.py:137-141`) et cité par la page dans la ligne de son étape ; renommer `Precedent` dans le code fait rougir le module (constat portant le jeton `couple touche/libelle`)."
    - "Les **commandes du récapitulatif** sont prouvées par le rendu et par l'action : les libellés `GO = LANCER`, `RESET = REINITIALISER`, `1-8 = RETOUR ECRAN` et `SAVES = STUFFS SAUVEGARDES` sont lus dans le corps du récapitulatif (`dofus_stuff/web/optimize_wizard.py:262-276`) et cités par la page ; `RESET` redirige vers `/optimize/wizard/slots`, `SAVES` vers `/saves`, chaque chiffre `n` de `1` à `8` vers `/optimize/wizard/<WIZARD_STEPS[n-1]>`, et toute autre saisie est refusée avec la liste `GO | RESET | SAVES | 1-8` ; **`GO` n'est jamais posté** (il exécute le solveur, `dofus_stuff/web/routes.py:1114-1116`) — le libellé est cité depuis le rendu. Renommer le libellé de `SAVES` ou décaler la table des chiffres fait rougir le module (jetons `commande du recapitulatif` et `commande numerique`)."
    - "L'**exemple guidé** migré (D-55) est réancré au rendu : ses trois éditions sont rejouées sur des clients neufs — le niveau par l'option `1`, la ligne `4` des caractéristiques avec `300 0 0 1`, la ligne `1` de l'écran PA / PM / PO avec `6 0 11 5` — et les lignes rendues (`NIVEAU`, `Intelligence B=300 P=0 C=0 W=1`, `PA B=6 E=0 C=11 W=5`) sont citées par la page ; la variante « cible PA » **survit** parce que le code la porte (`with_exo`, format `BASE EXO CIBLE POIDS`), et la faire disparaître du rendu fait rougir le module (constat portant le jeton `exemple guide`). L'exemple passe par le chemin d'arrivée réel, ne met jamais une commande destructive dans un parcours recommandé et ne promet aucune validation que le code n'applique pas."
    - "L'affirmation de `docs/parcours-simplifie.md:140` reste **vraie** après cette phase (D-64) : le contrôle des couples `Precedent`/`Suivant` est **scopé aux étapes intermédiaires 2 à 8**, jamais aux neuf, et la page porte la précision par étape au lieu de généraliser ; la ligne 140 elle-même n'est pas modifiée."
    - "L'empreinte de la base locale est mesurée autour des rendus du module : `taille`, `mtime_ns` et SHA-256 de `.data/dofus.sqlite3` relevés avant et après les rendus sont identiques, et le test de module **saute explicitement** (`pytest.skip` nommant la raison) si la base est absente au lieu de rendre un faux vert (patron de `tests/test_docs_parcours.py:2456-2464`)."
  artifacts:
    - path: "docs/wizard-avance.md"
      provides: "Les sections « Arriver au wizard » (chemin réel et identifiants d'écrans), « Touches et commandes » (couples par étape, commandes du récapitulatif) et « Exemple guidé » (exemple migré et réancré)"
      contains: "## Arriver au wizard"
      min_lines: 180
    - path: "tests/test_docs_wizard.py"
      provides: "Les contrôles WIZ-02 : écrans et chemin d'arrivée, couples de touches par étape, commandes du récapitulatif, exemple guidé réancré, empreinte `.data/` autour des rendus"
      contains: "def test_touches_et_commandes_par_etape"
      min_lines: 380
  key_links:
    - from: "tests/test_docs_wizard.py"
      to: "dofus_stuff/web/routes.py"
      via: "chaîne de redirections du client de test Flask (menu, `/optimize`, les trois questions, `AVANCE`), lecture des en-têtes d'écran (`OPT-W<n>`) et des couples de touches rendus"
      pattern: "/optimize/quick/niveau"
    - from: "tests/test_docs_wizard.py"
      to: "dofus_stuff/web/optimize_wizard.py"
      via: "lecture des libellés du corps du récapitulatif et application des éditions de l'exemple guidé (option, caractéristique, PA / PM / PO) par `POST` sur le client de test"
      pattern: "SAVES = STUFFS SAUVEGARDES"
    - from: "tests/test_docs_wizard.py"
      to: ".data/dofus.sqlite3"
      via: "empreinte (taille, `mtime_ns`, SHA-256) mesurée avant et après les rendus du module, jamais une écriture ; saut explicite si la base est absente"
      pattern: "dofus.sqlite3"
    - from: "docs/wizard-avance.md"
      to: "docs/parcours-simplifie.md"
      via: "cohérence de l'affirmation de la ligne 140 : la page du wizard porte la vérité par étape, la page du parcours simplifié reste inchangée (D-63, D-64)"
      pattern: "Precedent"
  prohibitions:
    - statement: "Ne jamais poster `GO` sans patcher l'étape de calcul : `GO` au récapitulatif exécute réellement le solveur et redirige vers le résultat ; le libellé se cite depuis le rendu, et son exécution déjà couverte reste celle de `tests/test_web.py` (patché)."
    - statement: "Ne jamais présenter `db clear` ni aucune commande destructrice comme une étape d'un parcours recommandé, et ne jamais l'exécuter (D-22, D-23, D-61) : l'exemple guidé migré n'en contient aucune."
    - statement: "Ne jamais modifier `dofus_stuff/**` pour que la documentation reste vraie : la page et le test s'alignent sur le rendu, jamais l'inverse (D-66)."
    - statement: "Ne jamais écrire ni supprimer quoi que ce soit sous `.data/` ou `.doc-agent/`, et ne jamais exécuter `main()` (D-15, D-66)."
    - statement: "Ne jamais publier, déployer à distance, pousser vers un dépôt distant ni effectuer d'achat ; les commits restent locaux et `git add` se fait par chemin explicite (D-66)."
  assumptions:
    - "**Sonde d'arêtes — WIZ-02 : la ligne est `unclassified`, elle RESTE `unresolved`.** Hypothèse signalée explicitement (jamais résolue automatiquement ni écartée) : la sonde n'a pas su classer cette exigence, donc **aucune exhaustivité de la surface des touches n'est revendiquée**. Ce que ce plan prouve est nommé : les couples `F7`/`F8`/`ESC` des neuf étapes, lus au rendu, et les quatre commandes du récapitulatif ; une touche non rendue hors de ces couples resterait hors du contrôle, et le module l'écrit."
    - "L'exécution réelle de `GO` (calcul puis redirection) n'est **pas** rejouée ici : elle est déjà couverte, patchée, par `tests/test_web.py` (`test_optimize_wizard_go_mocked`), que le module cite au lieu de la dupliquer (D-12). Ce plan prouve le **libellé rendu** et l'**action des autres commandes**, pas l'exécution du solveur."
    - "La barre de touches du récapitulatif rend `Page suiv` alors qu'aucune pagination n'existe (`f8_url is None`, conséquence directe de `dofus_stuff/web/routes.py:137-141`) : la page le dit comme une particularité mesurée, et le contrôle l'exige tel quel plutôt que de le lisser (D-19)."
    - "L'exemple guidé est **migré, jamais perdu** (D-49/D-55) : son texte d'origine reste consultable dans la copie figée créée par le plan 04-04 et dans l'historique git ; le plan 04-03 le retire de `GUIDE_WIZARD.md` en sachant où il vit désormais."
---

<objective>
**Cadrage dérivé (non une décision produit nouvelle)** — repris du `**Goal:**` de la phase 4, de son critère de succès 3 et des décisions D-51, D-55 et D-64 de `04-CONTEXT.md` :
**En tant que** joueur qui a réglé le wizard, **je veux** savoir comment on y arrive réellement, quelles touches agissent sur chaque écran et quelles commandes le récapitulatif accepte, **afin de** naviguer et de lancer le calcul sans tâtonner et sans croire à une arrivée « directe » qui n'existe pas.

Purpose : c'est **WIZ-02** — « un lecteur connaît les commandes et les touches réellement actives du wizard » — et la section du chemin d'arrivée est précisément l'affirmation fausse que le critère 5 doit résorber : `GUIDE_WIZARD.md` annonce une arrivée directe dans le wizard, alors que `optimize_entry` redirige vers les trois questions et que l'atterrissage est le récapitulatif (D-51). Ce plan écrit la vérité **mesurée** dans la page et l'ancre par le rendu.

Output : les sections `## Arriver au wizard`, `## Touches et commandes` et `## Exemple guidé` de `docs/wizard-avance.md`, et les contrôles correspondants dans `tests/test_docs_wizard.py` (module créé par le plan 04-01, étendu ici ; la constante `TITRES_SECTION_ATTENDUS` y est complétée **tâche par tâche**, en accord avec les sections réellement créées : huit titres à la fin de la tâche 1, neuf à la fin de la tâche 2 — aucune tâche ne se termine avec un titre épinglé absent de la page).

Ordonnancement déclaré : **vague 2**, `depends_on: [04-01]` — même page, même module : les plans de cette phase ne s'exécutent pas en parallèle. Ce plan vient **avant** le plan 04-04 (vague 3) pour que le détecteur de renvois obsolètes (créé en vague 3, plan 04-04) porte sur le module déjà complet, et **avant** le plan 04-03 (vague 4) qui produit le vert du critère 5.

**Ce que ce plan ne fait pas** : il ne réduit pas `GUIDE_WIZARD.md` et ne redirige pas `README.md` (plan 04-03) ; il n'écrit pas le détecteur de renvois obsolètes (plan 04-04) ; il ne modifie ni `docs/parcours-simplifie.md` (le lien D-63 appartient au plan 04-03) ni aucun fichier de `dofus_stuff/**` (D-66) ; il ne rejoue pas l'exécution du solveur (couvert ailleurs, patché).
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-CONTEXT.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-PATTERNS.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-VALIDATION.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-01-PLAN.md
@docs/wizard-avance.md
@docs/parcours-simplifie.md
@GUIDE_WIZARD.md
@tests/conftest.py
@tests/test_docs_wizard.py
@tests/test_docs_parcours.py
@tests/test_web.py
@dofus_stuff/web/routes.py
@dofus_stuff/web/optimize_wizard.py
@dofus_stuff/web/screens.py
</context>


<tasks>

<task type="auto">
  <name>Tâche 1 : le chemin d'arrivée réel, les identifiants d'écrans et l'exemple guidé migré</name>
  <files>docs/wizard-avance.md, tests/test_docs_wizard.py</files>
  <reversibility rating="reversible">Le texte de l'exemple migré reste consultable dans la copie figée du plan 04-04 et dans l'historique git ; une correction de rédaction ne détruit rien.</reversibility>
  <read_first>
    docs/wizard-avance.md
    tests/test_docs_wizard.py
    GUIDE_WIZARD.md
    dofus_stuff/web/routes.py
    dofus_stuff/web/optimize_wizard.py
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-PATTERNS.md
  </read_first>
  <action>Tranche verticale de ce plan : écrire le chemin d'arrivée **mesuré**, les identifiants d'écrans et l'exemple guidé migré, et les ancrer sur le rendu.

**Page** — deux sections, plus la mise à jour de la constante de titres du module :
- nouvelle section `## Arriver au wizard`, placée **avant** `## Les 9 étapes du wizard` : depuis le menu principal, la saisie `4` ouvre l'optimisation (libellé rendu `4. OPTIMISATION DE STUFF`, lu au rendu de `GET /`) et le parcours passe par `/optimize`, qui redirige vers les **trois questions** (`classe`, `elements`, `niveau`) ; `AVANCE` (reconnu quelle que soit la casse) ouvre alors le wizard et **atterrit sur le récapitulatif** — pas sur les slots (D-51, l'affirmation fausse à résorber). Décrire aussi l'identifiant d'écran : chaque écran du wizard porte `OPT-W1` à `OPT-W9` dans sa ligne d'en-tête, dans l'ordre des étapes, l'écran atteint par un numéro étant `OPT-WED`. Ajouter la mesure d'un écran inconnu : une étape qui n'existe pas rend le message `ECRAN WIZARD INCONNU` et ramène au menu. La page **ne promet aucune validation** que le code n'applique pas (le niveau du wizard n'est pas borné, D-19) et ne recopie aucune commande de `fetcher.py` (D-37).
- nouvelle section `## Exemple guidé`, placée **après** `## Interdire, forcer, retirer un objet` : la tâche 2 insérera `## Touches et commandes` **juste avant** elle, sans déplacer son contenu, de sorte que les sections de la page et la constante `TITRES_SECTION_ATTENDUS` restent en accord à la fin de **chaque** tâche. Contenu : l'exemple du guide migré et **corrigé** (D-55/D-49) — le parcours passe par le menu `4` et les trois questions, puis : l'option de niveau se règle par `1` puis la valeur ; les caractéristiques s'éditent par le **numéro de ligne** (l'Intelligence est la ligne `4`) avec les quatre valeurs `300 0 0 1`, ce qui rend la ligne `Intelligence B=300 P=0 C=0 W=1` ; la cible de PA se règle sur l'écran PA / PM / PO avec `6 0 11 5`, ce qui rend `PA B=6 E=0 C=11 W=5` ; le lancement se fait par `GO`, dont le libellé rendu est `GO = LANCER`, et le résultat remplace l'écran. La justification « base 200 + parchemins 100 » du guide est **présentée comme un choix du lecteur**, jamais comme une contrainte du code (le wizard n'édite pas les parchemins) ; la variante « cible PA » **survit** parce que le code la porte ; aucun `db clear`, aucune commande destructive n'apparaît dans l'exemple.
- dans le module, **compléter** `TITRES_SECTION_ATTENDUS` avec les **deux titres que cette tâche crée**, à leurs positions finales : `## Arriver au wizard` en tête et `## Exemple guidé` immédiatement avant `## Source de vérité`. La constante compte alors **huit** titres et reste en accord avec les sections de la page à la fin de la tâche — jamais un titre épinglé sans sa section, sinon la morsure du contrôle de forme serait indiscernable d'une dérive. Le neuvième titre (`## Touches et commandes`) appartient à la tâche 2, qui le crée **et** complète la constante dans le même mouvement.

**Module** :
- `test_ecrans_et_arrivee_du_wizard(docs_dir, app, normalize, section)` : (1) pour chaque étape, `GET /optimize/wizard/<etape>` sur un client neuf répond 200 et sa ligne d'en-tête porte `OPT-W<index+1>` **et** le titre rendu, et la page cite ces couples ; (2) `GET /optimize/wizard/inconnu` redirige vers le menu et le message `ECRAN WIZARD INCONNU` est rendu à l'écran suivant ; (3) la chaîne d'arrivée est rejouée **pas à pas** sur un seul client — `POST /` avec `selection=4` donne `Location` `/optimize`, `GET /optimize` donne `Location` `/optimize/quick/classe`, puis les trois `POST` des questions, puis `POST /optimize/quick/niveau` avec `AVANCE` donne `Location` `/optimize/wizard/recap` — et la page cite le menu, les trois questions, `AVANCE` et l'atterrissage sur le récapitulatif. Les constats portent les constantes `MOTIF_ARRIVEE` et `MOTIF_IDENTIFIANT_ECRAN` (valeurs ASCII, jamais écrites en clair dans la ligne d'assertion).
- `test_exemple_guide_ancre_au_rendu(docs_dir, app, normalize, section)` : rejoue les trois éditions de l'exemple, chacune sur un **client neuf** (un `POST` accepté réécrit l'état de session) — l'option de niveau par `POST /optimize/wizard/options` avec le numéro puis la valeur, la caractéristique par `POST /optimize/wizard/caracs` avec `4`, puis la valeur, et la cible de PA par `POST /optimize/wizard/papmpo` avec `1`, puis la valeur — et exige que chaque ligne rendue après édition soit **citée par la section** de l'exemple ; exige aussi que la section cite `AVANCE`, `RECAPITULATIF` et le libellé `GO = LANCER` lu dans le corps du récapitulatif, et que le message de refus du format soit celui du code pour la ligne éditée. Les constats portent la constante `MOTIF_EXEMPLE`. **Aucun `POST` de `GO`** : la saisie n'est jamais postée, seulement citée depuis le rendu.
  </action>
  <acceptance_criteria>
    - La page porte `## Arriver au wizard` (avant `## Les 9 étapes du wizard`) et `## Exemple guidé` (dernière section avant `## Source de vérité`) ; `TITRES_SECTION_ATTENDUS` liste **huit** titres en accord avec ces sections, dans l'ordre de la page, et le neuvième (`## Touches et commandes`, avec sa section) est ajouté en tâche 2.
    - Le chemin d'arrivée est rejoué pas à pas : `selection=4` vers `/optimize`, `GET /optimize` vers `/optimize/quick/classe`, les trois questions, `AVANCE` vers `/optimize/wizard/recap` ; la page cite ce chemin et l'atterrissage sur le récapitulatif.
    - Les neuf en-têtes d'écran portent `OPT-W1` à `OPT-W9` et la page cite l'identifiant de chaque étape ; un écran inconnu rend `ECRAN WIZARD INCONNU`.
    - L'exemple guidé est rejoué sur des clients neufs et les lignes rendues (`NIVEAU`, `Intelligence B=300 P=0 C=0 W=1`, `PA B=6 E=0 C=11 W=5`) sont citées par la section ; la variante « cible PA » est présente.
    - Aucun test de ce plan ne poste `GO` ; le libellé `GO = LANCER` est cité depuis le rendu du récapitulatif.
    - Casser `AVANCE` dans `dofus_stuff/web/routes.py:948`, décaler l'index des identifiants d'écran, ou rendre le format sans exo sur l'écran PA / PM / PO, rougit le module sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_wizard.py -q` est vert, et la suite entière reste verte.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_wizard.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in avance_casse identifiant_ecran exemple_pa; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in avance_casse) sed -i 's/cmd.upper() == "AVANCE"/cmd.upper() == "AVANCER"/' "$T/dofus_stuff/web/routes.py"; motif='arrivee du wizard';; identifiant_ecran) sed -i 's/WIZARD_STEPS.index(step) + 1/WIZARD_STEPS.index(step) + 2/' "$T/dofus_stuff/web/routes.py"; motif='identifiant d'ecran';; exemple_pa) sed -i 's/with_exo=(step == "papmpo")/with_exo=(step == "caracs")/' "$T/dofus_stuff/web/routes.py"; motif='exemple guide';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 04-02 (TACHE 1) EN ECHEC"; exit 1; fi; echo "morsures 04-02 tache 1 : 3/3 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 04-02 (TACHE 1) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>Le chemin d'arrivée réel, les identifiants d'écrans et l'exemple guidé migré sont écrits et ancrés au rendu : l'arrivée est mesurée sur le récapitulatif, les neuf en-têtes sont cités, l'exemple est rejoué sur trois éditions réelles, et trois dérives épinglées font rougir la suite sur une copie verte avant mutation.</done>
</task>


<task type="auto">
  <name>Tâche 2 : les couples de touches par étape, les commandes du récapitulatif et la garde d'empreinte</name>
  <files>docs/wizard-avance.md, tests/test_docs_wizard.py</files>
  <read_first>
    docs/wizard-avance.md
    tests/test_docs_wizard.py
    docs/parcours-simplifie.md
    docs/installation.md
    dofus_stuff/web/routes.py
    dofus_stuff/web/optimize_wizard.py
    tests/test_docs_parcours.py
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
  </read_first>
  <action>Écrire la section des touches et commandes **par étape**, la prouver par le rendu et par l'action, et poser la garde d'empreinte de la base locale.

**Page** — nouvelle section `## Touches et commandes`, placée après `## Interdire, forcer, retirer un objet` et avant `## Exemple guidé` :
- un tableau **par étape** des couples touche/libellé réellement rendus : l'étape 1 (`SLOTS ET FILTRES`) porte `Page prec` et `Suivant`, les étapes 2 à 8 portent `Precedent` et `Suivant`, l'étape 9 (`RECAPITULATIF`) porte `Precedent` et `Page suiv` ; `ESC` porte `Retour` sur les neuf. La section dit **explicitement** que c'est la barre de touches du wizard, et que la barre du résultat affiche d'autres libellés : l'affirmation de `docs/parcours-simplifie.md:140` reste ainsi vraie, et la généralisation « le wizard affiche `Precedent` et `Suivant` » n'est pas écrite (D-64, Pitfall 2 de la recherche) ;
- la particularité mesurée du récapitulatif : il rend `Page suiv` alors qu'il n'a **aucune pagination** (`f8_url is None`, conséquence directe de `dofus_stuff/web/routes.py:137-141`) — la page le dit tel quel, sans le lisser (D-19) ;
- les commandes du récapitulatif, avec leurs libellés rendus : `GO` lance le calcul et le résultat remplace l'écran ; `RESET` remet le wizard à zéro et revient au premier écran ; `SAVES` ouvre les stuffs sauvegardés ; les chiffres `1` à `8` reviennent à l'écran correspondant ; toute autre saisie est refusée, et la ligne de statut porte alors la liste `GO | RESET | SAVES | 1-8`. La phrase `SAVES = STUFFS SAUVEGARDES` du corps est citée entre accents graves ; les écrans de sauvegarde eux-mêmes restent décrits par `docs/parcours-simplifie.md` (D-38/D-49) : **renvoi, pas recopie** ;
- un renvoi, sous forme de **lien**, vers `docs/parcours-simplifie.md` pour lire le résultat et sauvegarder/export (D-56/D-43), sans recopier ces sections ;
- compléter `TITRES_SECTION_ATTENDUS` avec le **neuvième** titre, `## Touches et commandes`, inséré **avant** `## Exemple guidé` : la constante reste en accord exact avec les sections de la page, dans l'ordre, à la fin de la tâche — la tâche 1 l'a laissée à huit titres pour cette raison.

**Module** :
- `test_touches_et_commandes_par_etape(docs_dir, app, normalize, section)` : pour chacune des neuf étapes, rend l'écran sur un client neuf, lit les couples via `_touches`, et exige que la page **cite le couple de cette étape** — le constat nomme l'étape, la touche, le libellé cité, le libellé rendu et `SOURCE_ROUTES` (constante `MOTIF_TOUCHE`) ; exige en outre que `ESC`/`Retour` soit rendu sur les neuf. Les libellés `Precedent`/`Suivant` ne sont exigés que pour les étapes **2 à 8** (assertion scopée : un contrôle qui les exigerait sur les neuf contredirait le rendu des extrémités et l'affirmation de la ligne 140).
- puis, sur des clients neufs : `POST /optimize/wizard/recap` avec `RESET` redirige vers `/optimize/wizard/slots` ; avec `SAVES` vers `/saves` ; pour chaque chiffre `n` de `1` à `8`, la redirection vise `/optimize/wizard/<WIZARD_STEPS[n-1]>` (la table est lue au code, jamais recopiée — constante `MOTIF_COMMANDE_NUMERIQUE`) ; et une saisie hors liste est refusée avec la liste `GO | RESET | SAVES | 1-8` rendue dans la ligne de statut. Les quatre libellés du corps (`GO = LANCER`, `RESET = REINITIALISER`, `1-8 = RETOUR ECRAN`, `SAVES = STUFFS SAUVEGARDES`) sont lus au rendu et exigés dans la page (constante `MOTIF_COMMANDE_RECAP`). **`GO` n'est jamais posté** : seule sa citation au rendu est contrôlée.
- `_empreinte(chemin)` et `test_data_locale_non_modifiee_autour_des_rendus(docs_dir, app)` : empreinte `(taille, mtime_ns, sha256)` de `.data/dofus.sqlite3` relevée avant puis après un rendu d'étape, exigée identique ; si la base est absente, `pytest.skip` **nommant la raison** (jamais un faux vert), comme le fait déjà `tests/test_docs_parcours.py:2557+`.
  </action>
  <acceptance_criteria>
    - La page porte `## Touches et commandes` avec les couples par étape : `Page prec`/`Suivant` à l'étape 1, `Precedent`/`Suivant` aux étapes 2 à 8, `Precedent`/`Page suiv` à l'étape 9, `ESC`/`Retour` sur les neuf.
    - La page porte `## Touches et commandes` **entre** `## Interdire, forcer, retirer un objet` et `## Exemple guidé`, et `TITRES_SECTION_ATTENDUS` compte les **neuf** titres dans l'ordre de la page.
    - `test_touches_et_commandes_par_etape` lit les couples au rendu des neuf étapes et les compare à ceux cités par la page ; les libellés `Precedent`/`Suivant` ne sont exigés que pour les étapes 2 à 8.
    - `RESET` redirige vers `/optimize/wizard/slots`, `SAVES` vers `/saves`, chaque chiffre de `1` à `8` vers l'étape correspondante lue dans `WIZARD_STEPS`, et une saisie hors liste est refusée avec `GO | RESET | SAVES | 1-8`.
    - Aucun test ne poste `GO` ; la page cite `GO = LANCER`, `RESET = REINITIALISER`, `1-8 = RETOUR ECRAN` et `SAVES = STUFFS SAUVEGARDES`.
    - `test_data_locale_non_modifiee_autour_des_rendus` compare l'empreinte avant et après les rendus, et saute explicitement si la base est absente.
    - Renommer `Precedent` dans `dofus_stuff/web/routes.py:138`, renommer le libellé de `SAVES` dans le corps du récapitulatif, ou décaler la table des chiffres (`WIZARD_STEPS[n - 1]`), rougit le module sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_wizard.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in libelle_f7 libelle_saves commande_numerique; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in libelle_f7) sed -i 's/f7_label = "Precedent" if f7_url else "Page prec"/f7_label = "Precedente" if f7_url else "Page prec"/' "$T/dofus_stuff/web/routes.py"; motif='couple touche/libelle';; libelle_saves) sed -i 's/SAVES = STUFFS SAUVEGARDES/SAVES = SAUVEGARDES/' "$T/dofus_stuff/web/optimize_wizard.py"; motif='commande du recapitulatif';; commande_numerique) sed -i 's/step=WIZARD_STEPS\[n - 1\]/step=WIZARD_STEPS[-n]/' "$T/dofus_stuff/web/routes.py"; motif='commande numerique';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 04-02 (TACHE 2) EN ECHEC"; exit 1; fi; echo "morsures 04-02 tache 2 : 3/3 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 04-02 (TACHE 2) EN ECHEC"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; mesure() { "$PY" -c "import hashlib,pathlib;p=pathlib.Path('.data/dofus.sqlite3');print(f'{p.stat().st_size}:{p.stat().st_mtime_ns}:{hashlib.sha256(p.read_bytes()).hexdigest()}' if p.exists() else 'base absente')"; }; mesure > "$T/avant.txt"; "$PY" -m pytest -q; statut=$?; mesure > "$T/apres.txt"; if [ "$statut" -ne 0 ]; then echo "SUITE COMPLETE ROUGE (exit $statut)"; exit 1; fi; if diff -q "$T/avant.txt" "$T/apres.txt" >/dev/null; then echo "empreinte .data/ identique autour de la suite complete : $(cat "$T/apres.txt")"; else echo "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE : avant=$(cat "$T/avant.txt") apres=$(cat "$T/apres.txt")"; exit 1; fi</automated>
    <fails_when>exit non-zero, ou la ligne "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE", ou la ligne "SUITE COMPLETE ROUGE", ou une ligne contenant "failed" ou "error"</fails_when>
  </verify>
  <done>Les couples de touches sont cités par étape et prouvés au rendu, les quatre commandes du récapitulatif sont citées et trois d'entre elles prouvées par l'action, l'exemple guidé et le renvoi vers le parcours simplifié sont en place, l'empreinte de `.data/` est mesurée autour des rendus, et trois dérives épinglées font rougir la suite sur une copie verte avant mutation.</done>
</task>

</tasks>


<threat_model>
**Surface** : deux sections de la page `docs/wizard-avance.md`, le module `tests/test_docs_wizard.py`, le client de test Flask en processus et la base locale en **lecture seule**. Aucune dépendance ajoutée, aucun fichier de `dofus_stuff/**` modifié (D-66).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Spoofing (documentation qui ment) | `docs/wizard-avance.md` : chemin d'arrivée, identifiants d'écrans, couples de touches, commandes, exemple guidé | high | mitigate | Chaque affirmation est lue au rendu (chaîne de redirections, en-têtes `OPT-W<n>`, couples touche/libellé, corps du récapitulatif) ; six morsures prouvent que le module rougit quand le code change (`AVANCE` cassé, index d'identifiant décalé, format d'édition déplacé, libellé `Precedent` renommé, libellé de `SAVES` renommé, table des chiffres décalée) |
| T-04-07 | Elevation of privilege | Exécution du solveur par un `POST` de `GO` au récapitulatif | high | mitigate | Aucun `GO` n'est posté : le libellé est cité depuis le rendu ; la garde `ast` du plan 04-01 refuse tout appel dont l'argument `data` porte la paire `"cmd": "GO"` ; l'exécution réelle reste couverte, patchée, par `tests/test_web.py` |
| T-04-08 | Tampering | `.data/dofus.sqlite3` | medium | mitigate | Les éditions de l'exemple et les commandes sont postées sur la fixture `app`, dont la base vit sous `tmp_path` ; `test_data_locale_non_modifiee_autour_des_rendus` compare l'empreinte avant/après, et saute explicitement si la base est absente ; la tâche 2 mesure aussi l'empreinte autour de la suite complète |
| T-04-09 | Denial of service (du harnais) | Boucle de rendu des 9 étapes × plusieurs tests | low | accept | Le coût mesuré est de quelques secondes pour la suite entière (baseline 186 tests en 3,65 s) ; aucune boucle non bornée, aucun réseau, aucun serveur |
| T-04-10 | Repudiation | Traçabilité du rouge et du vert du critère 5 | low | mitigate | Le rouge est produit par le plan 04-04 (vague 3) sur `GUIDE_WIZARD.md` encore obsolète et consigné dans son `SUMMARY.md` ; le vert est produit par le plan 04-03 (vague 4) et consigné de même ; aucune des deux observations n'est une validation humaine |
| T-04-SC | Tampering | Installations de paquets | low | accept | Aucune dépendance ajoutée : `pyproject.toml` inchangé, aucun `pip install` exécuté (aucun `[ASSUMED]`, aucun `[SUS]`, aucun `[SLOP]`) |

**Hors périmètre** : publication, déploiement distant, synchronisation Dofusdude, suppression de données, achat. L'exemple guidé ne contient aucune commande destructive (D-61).
</threat_model>

<verification>
**Contrôles automatisés** : `tests/test_docs_wizard.py` (écrans et arrivée, couples de touches par étape, commandes du récapitulatif, exemple guidé réancré, empreinte `.data/`), les modules des phases 1 à 3 inchangés et verts, la suite entière, l'empreinte de `.data/dofus.sqlite3` avant et après la suite, et **six morsures** sur copie verte avant mutation : `AVANCE` cassé, index des identifiants d'écrans décalé, format d'édition déplacé hors de l'écran PA / PM / PO, libellé `Precedent` renommé, libellé de `SAVES` renommé, table des chiffres du récapitulatif décalée.

**Non automatisable, nommé comme tel** : l'exécution réelle du solveur après `GO` (déjà couverte, patchée, par `tests/test_web.py`) et la lisibilité de l'exemple pour un lecteur humain. Aucun blocage : rien ne dépend d'un geste humain, d'un secret ou d'un réseau ; aucun `checkpoint` n'est requis (`workflow.human_verify_mode` vaut `end-of-phase`).

**Références de vérification de phase** : `04-VALIDATION.md` § Per-Task Verification Map assigne les lignes `touches` et `arrivee` à ce plan (vague 2) ; `/gsd:verify-work` rejouera la suite entière, l'empreinte `.data/` et les deux états du critère 5.
</verification>

<success_criteria>
- Le chemin d'arrivée réel est décrit et mesuré, l'atterrissage sur le récapitulatif est énoncé, et l'affirmation d'une arrivée « directe » n'apparaît nulle part dans la page (D-51, critère 3 du ROADMAP).
- Les identifiants d'écrans, les couples de touches par étape et les commandes du récapitulatif sont cités depuis le rendu ; `GO` est cité mais jamais posté (WIZ-02).
- L'exemple guidé est migré sans perte, corrigé là où le guide contredisait le code, et ses trois éditions sont rejouées sur le rendu ; la variante « cible PA » survit (D-55).
- La ligne 140 de `docs/parcours-simplifie.md` reste vraie : le contrôle des libellés `Precedent`/`Suivant` est scopé aux étapes intermédiaires (D-64).
- Six dérives épinglées font rougir la suite sur une copie verte avant mutation ; la suite entière est verte et `.data/dofus.sqlite3` est intact.
</success_criteria>

## Couverture des sources (ce plan)

| Source | Élément | Consommé par |
|--------|---------|--------------|
| ROADMAP § Phase 4 | Critère 3 (touches et commandes réellement actives, dont `GO`, `RESET`, `SAVES`, `1`–`8`) | tâche 2 |
| ROADMAP § Phase 4 | Critère 5, volet « arrivée directe » (l'affirmation fausse à résorber) | tâche 1 (la page dit le chemin réel) |
| ROADMAP § Phase 4 | Brief du plan 04-02 (contrôles depuis le rendu : étapes, commandes, touches, identifiants d'écrans) | tâches 1 et 2 |
| REQUIREMENTS | WIZ-02 | `requirements` du plan |
| RESEARCH | § « Chemin d'arrivée réel — mesuré pas à pas » | tâche 1 |
| RESEARCH | § « Récapitulatif, touches et commandes — mesurés » | tâche 2 |
| RESEARCH | § « L'exemple guidé à migrer — vérifié contre le code » | tâche 1 |
| RESEARCH | Pitfall 2 (la ligne 140 n'est vraie qu'à moitié), Pitfall 6 (ne pas confondre wizard et résultat) | tâche 2 (assertion scopée, aucune revendication universelle) |
| CONTEXT | D-51 (chemin réel : menu `4`, trois questions, `AVANCE`) | tâche 1 |
| CONTEXT | D-55 (exemple guidé migré et réancré, variante PA conservée si le code la porte) | tâche 1 |
| CONTEXT | D-64 (la ligne 140 reste vraie ; les libellés par étape vivent dans la page du wizard) | tâche 2 |
| CONTEXT | D-38, D-43, D-49 (lire le résultat et sauvegarder restent dans `parcours-simplifie.md` : renvoi, pas recopie) | tâche 2 |
| CONTEXT | D-12, D-13, D-14, D-15 (helpers partagés, message localisant, API publique, interdits d'exécution) | tâches 1 et 2 |
| CONTEXT | D-66 et D-67 (aucune modification de `dofus_stuff/**`, documentation en français) | toutes les tâches |

## Output

Create `.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-02-SUMMARY.md` when done

