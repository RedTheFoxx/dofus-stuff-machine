---
phase: 04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard
plan: 03
type: execute
wave: 4
depends_on:
  - 04-04
  - 04-02
files_modified:
  - GUIDE_WIZARD.md
  - README.md
  - docs/parcours-simplifie.md
  - tests/test_docs_parcours.py
  - tests/test_docs_wizard.py
autonomous: true
requirements:
  - WIZ-03
estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`GUIDE_WIZARD.md` est réduit à l'**aiguillage court** de D-46 : un `H1`, l'avis de déplacement, l'**arborescence de menus corrigée**, un lien vers `docs/wizard-avance.md` **et** un lien vers `docs/sommaire.md` — et rien d'autre ; le fichier ne dépasse pas 20 lignes (baseline mesurée : 330 lignes), et tout le reste est supprimé (ni « Lire le résultat », ni exemple guidé, ni table des touches rédupliquée)."
    - "L'arborescence est **lue au code** (D-47) : le numéro de l'optimisation est **4**, pas 3 ; les libellés rendus sont `RECHERCHE D'OBJETS`/`LISTE DES EQUIPEMENTS`/`LISTE DES PANOPLIES`/`OPTIMISATION DE STUFF`/`SYSTEME` (la forme abrégée `4. OPTIMISATION`, `3. PANOPLIES`, `5. SYSTEME` est celle que D-47 prescrit) ; la phrase « tapez `3` puis Entrée pour ouvrir l'optimisation » **disparaît** ; et le détecteur de renvois obsolètes ne signale plus rien sur ce fichier."
    - "La **preuve de la correction est le vert lui-même** : `test_aiguillage_sans_renvoi_obsolete` passe (zéro constat sur le fichier livré) alors que `test_copie_figee_signalee_par_le_detecteur` continue d'exiger les **trois constats** sur la copie figée — la morsure ne s'éteint pas en même temps que le vert (D-59a/D-59b). Réintroduire la phrase d'arrivée directe dans le fichier livré fait rougir le test livré : le vert vient bien d'une correction, pas d'un détecteur affaibli."
    - "`README.md` perd sa ligne `**Guide détaillé :** [GUIDE_WIZARD.md](GUIDE_WIZARD.md)` (~ligne 63, emplacement vérifié au fichier réel avant édition, D-19), **sans texte de remplacement** (D-62) : le fichier ne contient plus aucune mention de `GUIDE_WIZARD`, n'ajoute aucun renvoi, ni en prose ni en lien, vers `docs/wizard-avance.md`, et garde son **lien unique** vers `docs/sommaire.md` (D-10/D-29) — compter deux liens vers le sommaire affaiblirait le contrôle de structure et créerait une seconde formulation du même renvoi (D-17)."
    - "La dette D-44 est **levée dans le même commit que sa cible** (D-63) : les deux renvois **en prose sans lien** de `docs/parcours-simplifie.md` (~lignes 5 et 256) deviennent de vrais liens vers `docs/wizard-avance.md`, et `PAGES_INEXISTANTES` (`tests/test_docs_parcours.py:2231`) perd l'entrée `wizard-avance.md` — un seul commit, `git add` par chemin explicite — **aucun autre énoncé** de cette page n'étant modifié (la ligne ~140 et son affirmation `Precedent`/`Suivant` restent intactes, D-64)."
    - "La réserve ne peut pas mentir : une entrée de `PAGES_INEXISTANTES` qui correspond à une page **existante** est un échec — c'est ce qui rend la réduction obligatoire, vérifiable et inversible (remettre `wizard-avance.md` dans la réserve fait rougir la suite). Les phrases exigées par `RENVOIS_SANS_LIEN` (`réglages avancés`, `base locale`) restent présentes : `base-locale.md` n'existe toujours pas, et la dette restante reste visible."
    - "Le lien de l'aiguillage est un **renvoi légitime** au sens de D-60, prouvé par un contrôle qui vit à côté de son modèle : `test_lien_wizard_avance_legitime` dans `tests/test_docs_parcours.py` (module qui porte déjà les deux contrôles de `PAGES_INEXISTANTES`) exige que toute cible de lien de `GUIDE_WIZARD.md` vers `docs/` soit **déclarée** dans une liste légitime **et existe sur disque** — un lien non déclaré ou mort est un échec."
    - "À la fin de ce plan, la suite entière est **verte** (critère 5, état vert) et le dépôt reste propre côté données : `.data/dofus.sqlite3` garde taille et `mtime_ns`, les tests de structure de `docs/` (index, `H1`, retour, encodage, liens) sont verts **sans exception**, et le module de la phase 3 ne se dégrade pas."
  artifacts:
    - path: "GUIDE_WIZARD.md"
      provides: "L'aiguillage corrigé : titre, avis de déplacement, arborescence de menus lue au code, lien vers la page unique et lien vers le sommaire"
      contains: "docs/wizard-avance.md"
      min_lines: 8
    - path: "README.md"
      provides: "Le fichier sans la ligne « Guide détaillé », conservant son lien unique vers le sommaire"
      contains: "docs/sommaire.md"
      min_lines: 60
    - path: "docs/parcours-simplifie.md"
      provides: "La page maîtrisée de la phase 3 avec ses deux renvois devenus de vrais liens (D-63), sans autre modification"
      contains: "](wizard-avance.md)"
      min_lines: 260
    - path: "tests/test_docs_parcours.py"
      provides: "Le contrôle du renvoi légitime, la réserve réduite et la garde interdisant qu'une page existante soit déclarée inexistante"
      contains: "def test_lien_wizard_avance_legitime"
      min_lines: 2740
    - path: "tests/test_docs_wizard.py"
      provides: "Le contrôle de l'aiguillage et du README (forme courte, liens D-46/D-48, retrait D-62) et le vert du critère 5"
      contains: "def test_aiguillage_et_readme"
      min_lines: 500
  key_links:
    - from: "GUIDE_WIZARD.md"
      to: "docs/wizard-avance.md"
      via: "le lien de migration D-46 et le second lien de navigation vers `docs/sommaire.md` (D-48) ; les cibles existent sur disque et sont déclarées légitimes"
      pattern: "wizard-avance.md"
    - from: "docs/parcours-simplifie.md"
      to: "docs/wizard-avance.md"
      via: "les deux renvois en prose des lignes ~5 et ~256 convertis en vrais liens, dans le même commit que la réduction de `PAGES_INEXISTANTES` (D-63)"
      pattern: "wizard-avance.md"
    - from: "tests/test_docs_parcours.py"
      to: "tests/test_docs_parcours.py:2231"
      via: "`PAGES_INEXISTANTES` réduit à la seule page réellement inexistante, et la garde qui refuse toute page déclarée inexistante qui existe"
      pattern: "PAGES_INEXISTANTES"
    - from: "tests/test_docs_wizard.py"
      to: "GUIDE_WIZARD.md"
      via: "`test_aiguillage_sans_renvoi_obsolete` (créé en 04-04) devient vert ici : c'est le vert du critère 5, obtenu par la correction du fichier"
      pattern: "GUIDE_WIZARD"
    - from: "GUIDE_WIZARD.md"
      to: "dofus_stuff/web/routes.py"
      via: "l'arborescence corrigée (`menu_post`, `dofus_stuff/web/routes.py:217-222`) : `4` branche l'optimisation, `3` les panoplies, `5` le système"
      pattern: "menu_post"
  prohibitions:
    - statement: "Ne jamais exécuter `db clear` ni aucune commande destructrice, et ne jamais la présenter comme une étape d'un parcours recommandé : la suppression des sections du guide ne doit pas laisser un `db clear` orphelin dans un parcours (D-22, D-23, D-61)."
    - statement: "Ne jamais écrire ni supprimer quoi que ce soit sous `.data/` ou `.doc-agent/` (D-15, D-66)."
    - statement: "Ne jamais supprimer le contenu retiré sans destination : l'exemple guidé vit dans la page du wizard (D-55) et l'original reste consultable dans la copie figée de `tests/` et dans l'historique git (D-49)."
    - statement: "Ne jamais affaiblir le détecteur ni la copie figée pour obtenir le vert : la morsure doit survivre à la correction, sinon la preuve du critère 5 est détruite (D-59)."
    - statement: "Ne jamais modifier `dofus_stuff/**` pour aligner la documentation (D-66), ni publier, déployer à distance, pousser vers un dépôt distant ou effectuer d'achat ; les commits restent locaux, `git add` par chemin explicite, jamais `git add .`."
  assumptions:
    - "**Sonde d'arêtes — WIZ-03 : la ligne est `unclassified`, elle RESTE `unresolved`.** Hypothèse signalée, jamais résolue automatiquement. Ce plan produit le **vert** de l'état rouge observé au plan 04-04, et l'énoncé produit reste borné : « le détecteur ne signale rien sur l'aiguillage corrigé », jamais « plus aucun renvoi obsolète n'existe » (trois formes nommées, aucune exhaustivité revendiquée)."
    - "La mention de `docs/installation.md` (~ligne 69 : `F7`/`F8` n'apparaissent qu'à un écran paginé ou à une étape du wizard) **reste vraie** : les étapes intermédiaires du wizard rendent bien les couples `F7`/`F8` (contrôlé pas à pas par le plan 04-02), et l'aiguillage réduit ne la contredit pas. Aucun test nouveau n'est ajouté pour cette mention : son ancrage est celui de 04-02, et le fichier n'est pas modifié."
    - "Le libellé exact du `H1` de l'aiguillage et le texte de l'avis de déplacement relèvent de **Claude's Discretion** (`04-CONTEXT.md`) : ce plan n'impose que la forme courte, l'arborescence lue au code, les deux liens et l'absence d'énoncé de contenu. Le contrôle ne fige donc **pas** la formulation, seulement ces invariants."
    - "Le déplacement du contenu migré est vérifié **par destination, pas par reliquat** : le contrôle exige que l'aiguillage ne porte plus les formats ni la syntaxe d'items, et que la page cible les porte (contrôles des plans 04-01/04-02). Aucun contrôle ne compare texte à texte l'ancien et le nouveau : cette comparaison serait une validation humaine déguisée."
---

<objective>
**Cadrage dérivé (non une décision produit nouvelle)** — repris du `**Goal:**` de la phase 4, de son critère de succès 4 et de ses critères 5 (état vert), des décisions D-46, D-47, D-48, D-49, D-62, D-63 et D-64 de `04-CONTEXT.md` :
**En tant que** joueur qui tombe sur l'ancien guide, **je veux** être aiguillé en quelques lignes vers la page qui décrit vraiment le wizard, avec le bon numéro de menu, **afin de** ne plus perdre de temps sur un document périmé, et **en tant que** mainteneur, je veux que la dette de renvoi soit soldée là où la cible vient d'être créée.

Purpose : c'est la **seconde moitié de WIZ-03** et le **vert du critère 5** : le plan 04-04 a écrit le détecteur et observé le **rouge** sur `GUIDE_WIZARD.md` encore obsolète ; ce plan corrige le fichier et obtient le **vert**, dans la même phase. Il solde aussi la dette de renvoi D-44 en convertissant les deux renvois en prose de `docs/parcours-simplifie.md` en vrais liens, **dans le même commit que la cible** (D-63), et retire la réserve correspondante.

Output : `GUIDE_WIZARD.md` réduit à son aiguillage corrigé, `README.md` débarrassé de son pointeur produit, les deux liens D-63 de `docs/parcours-simplifie.md` avec la réserve réduite, et les contrôles du renvoi légitime (`tests/test_docs_parcours.py`) et de l'aiguillage (`tests/test_docs_wizard.py`).

Ordonnancement déclaré : **vague 4**, `depends_on: [04-04, 04-02]` — après le plan 04-04, sans quoi il n'y aurait ni rouge à convertir ni détecteur à faire passer au vert, et après le plan 04-02 pour que la page cible et son ancrage soient complets. Écart déclaré avec le brouillon `04-VALIDATION.md` (qui annonçait « plan 03, vague 2 ») : **ÉCR-6**, déjà consigné dans `04-01-PLAN.md`.

**Ce que ce plan ne fait pas** : il n'ajoute aucune section à `docs/wizard-avance.md` (plans 04-01 et 04-02), ne touche pas un mot de la ligne ~140 de `docs/parcours-simplifie.md` (D-64), ne modifie pas `docs/installation.md`, n'ajoute aucun renvoi dans `README.md` (D-62 est une suppression sèche), ne réécrit pas la copie figée du plan 04-04 et ne modifie aucun fichier de `dofus_stuff/**` (D-66).
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-CONTEXT.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-PATTERNS.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-VALIDATION.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-04-PLAN.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-CONTEXT.md
@GUIDE_WIZARD.md
@README.md
@docs/wizard-avance.md
@docs/parcours-simplifie.md
@docs/sommaire.md
@docs/installation.md
@tests/test_docs_wizard.py
@tests/test_docs_parcours.py
@tests/test_docs_structure.py
@dofus_stuff/web/routes.py
</context>


<tasks>

<task type="auto">
  <name>Tâche 1 : réduire `GUIDE_WIZARD.md` à l'aiguillage corrigé, nettoyer `README.md`, obtenir le vert du critère 5</name>
  <files>GUIDE_WIZARD.md, README.md, tests/test_docs_wizard.py</files>
  <reversibility rating="reversible">Fichiers suivis par git : la réduction du guide et le retrait de la ligne du README sont restaurables par un commit de retour ; le contenu retiré est par ailleurs conservé dans la copie figée du plan 04-04 et dans la page cible.</reversibility>
  <read_first>
    GUIDE_WIZARD.md
    README.md
    docs/wizard-avance.md
    docs/sommaire.md
    tests/test_docs_wizard.py
    dofus_stuff/web/routes.py
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-PATTERNS.md
  </read_first>
  <action>Réduire le guide à son aiguillage et obtenir le vert, **par correction du fichier, jamais par affaiblissement du contrôle**.

**`GUIDE_WIZARD.md`** — réécrire le fichier pour qu'il ne contienne **que** (D-46/D-48) :
- un `H1` (libellé au choix de l'exécutant : Claude's Discretion) ;
- l'**avis de déplacement** : le document ne décrit plus le wizard, la description vit désormais dans la page unique, avec le **lien** vers `docs/wizard-avance.md` ;
- l'**arborescence de menus corrigée**, lue au code (`dofus_stuff/web/routes.py:217-222`, `menu_post`) : `4` branche l'optimisation (libellé rendu `OPTIMISATION DE STUFF`, forme courte `4. OPTIMISATION`), `3` les panoplies, `5` le système, aux libellés réellement rendus ; la phrase « tapez `3` puis Entrée pour ouvrir l'optimisation » **disparaît** ; si une invitation est conservée, elle porte le numéro **4** (mesuré par le plan 04-04) ;
- le **second lien de navigation** vers `docs/sommaire.md` (D-48) ;
- **rien d'autre** : ni section « Lire le résultat » ni « Stuffs sauvegardés » (propriété de `docs/parcours-simplifie.md`, D-38/D-49), ni table des touches, ni formats des quatre nombres, ni syntaxe d'items, ni exemple guidé (migré par le plan 04-02, D-55), ni mention de `db clear`. Le fichier ne dépasse pas 20 lignes (baseline 330) et garde les fins de ligne CRLF du dépôt.

**`README.md`** — vérifier l'emplacement réel de la ligne `**Guide détaillé :** [GUIDE_WIZARD.md](GUIDE_WIZARD.md)` (~ligne 63, D-19) puis la **supprimer sans texte de remplacement** (D-62) : le fichier ne mentionne plus `GUIDE_WIZARD`, n'ajoute aucun renvoi vers `docs/wizard-avance.md`, et conserve son **unique** lien vers `docs/sommaire.md` (ligne 7, D-10/D-29). Aucune autre ligne n'est touchée.

**Module** (`tests/test_docs_wizard.py`) — ajouter `test_aiguillage_et_readme(docs_dir, normalize)` :
- sur `GUIDE_WIZARD.md` : le fichier existe, tient en **au plus 20 lignes**, porte un seul `H1`, un lien markdown dont la cible est `docs/wizard-avance.md` et un lien dont la cible est `docs/sommaire.md`, et **ne porte plus aucun énoncé de contenu** — mesuré en exigeant l'absence du message de refus de la syntaxe d'items, l'absence des deux formats d'édition des quatre nombres, l'absence des lignes d'options du solveur et l'absence de la phrase d'arrivée directe. La valeur ASCII du constat est fixée **ici**, et le contrôle l'emploie telle quelle : `MOTIF_AIGUILLAGE_BRUYANT` = `aiguillage qui decrit encore le wizard`, porté par chacun de ces constats d'énoncé de contenu restant ;
- sur `README.md` : aucune occurrence de `GUIDE_WIZARD`, aucune occurrence de `wizard-avance`, et exactement **un** lien markdown vers `docs/sommaire.md`. Valeur ASCII fixée **ici** : `MOTIF_LIEN_PRODUIT` = `renvoi produit vers un fichier non indexe`, porté par le constat d'une mention produit restante dans le README ou d'un nombre de liens vers le sommaire différent de un ;
- sur le lien de l'aiguillage : un lien markdown dont la cible est `docs/wizard-avance.md` **et** un lien dont la cible est `docs/sommaire.md` sont exigés, et chaque cible existe depuis la racine du dépôt. Valeur ASCII fixée **ici** : `MOTIF_LIEN_AIGUILLAGE` = `aiguillage sans lien vers la page`, porté par un constat de lien manquant (vers `docs/wizard-avance.md` ou vers `docs/sommaire.md`) ou de cible absente ;
- les constats suivent D-13 (fichier, valeur attendue, source) et chaque motif est porté par une constante, jamais écrit en clair dans la ligne d'assertion.

Enfin, **vérifier le vert et le consigner** : `test_aiguillage_sans_renvoi_obsolete` (plan 04-04) doit passer, `test_copie_figee_signalee_par_le_detecteur` doit **toujours** exiger ses trois constats sur la copie figée, et la suite entière doit être verte. Consigner dans `04-03-SUMMARY.md` la transition rouge → vert avec ses deux commandes, sans revendiquer aucune exhaustivité de détection.
  </action>
  <acceptance_criteria>
    - `GUIDE_WIZARD.md` fait au plus 20 lignes, porte un seul `H1`, l'avis de déplacement, l'arborescence corrigée (`4` = optimisation, jamais `3`), un lien vers `docs/wizard-avance.md` et un lien vers `docs/sommaire.md`, et aucun énoncé de contenu (ni formats, ni syntaxe d'items, ni options, ni exemple guidé, ni `db clear`).
    - `README.md` ne contient plus aucune mention de `GUIDE_WIZARD`, n'ajoute aucun renvoi vers `docs/wizard-avance.md`, et garde exactement un lien markdown vers `docs/sommaire.md`.
    - `test_aiguillage_sans_renvoi_obsolete` est **vert** (zéro constat sur le fichier livré) tandis que `test_copie_figee_signalee_par_le_detecteur` exige toujours les trois constats sur la copie figée.
    - `test_aiguillage_et_readme` passe et ses motifs (`MOTIF_AIGUILLAGE_BRUYANT`, `MOTIF_LIEN_PRODUIT`, `MOTIF_LIEN_AIGUILLAGE`) sont portés par des constantes dont la valeur ASCII est celle fixée par l'action : `aiguillage qui decrit encore le wizard`, `renvoi produit vers un fichier non indexe`, `aiguillage sans lien vers la page`.
    - Retirer le lien vers la page unique, réintroduire la phrase d'arrivée directe, remettre la ligne « Guide détaillé » dans `README.md` ou remettre un énoncé de contenu dans l'aiguillage font rougir la suite sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest -q` est **vert** (le critère 5 passe de rouge à vert) et `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé de la suite complète</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in aiguillage_sans_lien renvoi_direct_reintroduit readme_revert aiguillage_bruyant; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in aiguillage_sans_lien) sed -i '/wizard-avance\.md/d' "$T/GUIDE_WIZARD.md"; motif='aiguillage sans lien vers la page';; renvoi_direct_reintroduit) printf 'Vous arrivez directement dans le wizard (premier ecran : slots et filtres).\n' >> "$T/GUIDE_WIZARD.md"; motif='renvoi obsolete (c)';; readme_revert) printf '\n**Guide détaillé :** [GUIDE_WIZARD.md](GUIDE_WIZARD.md)\n' >> "$T/README.md"; motif='renvoi produit vers un fichier non indexe';; aiguillage_bruyant) printf '\nLe refus unique est le message exact du code (SYNTAXE : +ID | -ID | !ID | CLEAR) et BASE EXO CIBLE POIDS sur l ecran PA / PM / PO.\n' >> "$T/GUIDE_WIZARD.md"; motif='aiguillage qui decrit encore le wizard';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 04-03 (TACHE 1) EN ECHEC"; exit 1; fi; echo "morsures 04-03 tache 1 : 4/4 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 04-03 (TACHE 1) EN ECHEC"</fails_when>
  </verify>
  <done>L'aiguillage est réduit et corrigé, le README ne porte plus son pointeur produit, le détecteur ne signale plus rien sur le fichier livré tandis que la copie figée reste signalée, la suite est verte, et quatre dérives épinglées font rougir la suite sur une copie verte avant mutation.</done>
</task>

<task type="auto">
  <name>Tâche 2 : lever la dette D-63 et réduire la réserve dans le même commit</name>
  <files>docs/parcours-simplifie.md, tests/test_docs_parcours.py</files>
  <read_first>
    docs/parcours-simplifie.md
    tests/test_docs_parcours.py
    docs/wizard-avance.md
    docs/sommaire.md
    GUIDE_WIZARD.md
    .planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-CONTEXT.md
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
  </read_first>
  <action>Solde la dette de renvoi D-44 en un seul changement, et prouve-le.

**`docs/parcours-simplifie.md`** — convertir **les deux renvois en prose sans lien** (lignes ~5 et ~256, emplacements vérifiés au fichier réel avant édition, D-19) en **vrais liens markdown** vers `wizard-avance.md` : le sujet du wizard avancé est désormais décrit par cette page, et le lien le dit. Contraintes strictes :
- **aucun autre énoncé** de la page n'est modifié : la ligne ~140 (`Precedent`/`Suivant`) et son affirmation restent **intactes** (D-64), l'énoncé de niveau C de la phase 3 reste intact ;
- les phrases exigées par `RENVOIS_SANS_LIEN` (`réglages avancés`, `base locale`) **restent présentes** : `base-locale.md` n'existe toujours pas, donc la partie « base locale » garde sa formulation au futur et la dette restante reste visible ;
- la ligne de retour au sommaire et les invariants de forme (CRLF sans BOM) ne bougent pas.

**`tests/test_docs_parcours.py`** — dans le **même commit** que le lien (D-63) :
- réduire `PAGES_INEXISTANTES` (ligne ~2231) à la seule page encore inexistante (`("base-locale.md",)`) et mettre à jour son commentaire : la réserve ne contient plus que ce qui n'existe pas ;
- ajouter la liste légitime des cibles de l'aiguillage, par exemple `LIENS_LEGITIMES_VERS_L_AIGUILLAGE = ("docs/wizard-avance.md", "docs/sommaire.md")` ;
- ajouter `test_lien_wizard_avance_legitime(docs_dir)` qui exige, avec des constats localisants et des constantes de motif (jamais de valeur en clair dans la ligne d'assertion) :
  1. les deux renvois de `docs/parcours-simplifie.md` portent un lien dont la cible est `wizard-avance.md` — constat `MOTIF_LIEN_D63` (« renvoi en prose sans lien vers la page du wizard ») si l'un des deux redevient de la prose ;
  2. **aucune** entrée de `PAGES_INEXISTANTES` ne correspond à une page qui existe sur disque — constat `MOTIF_PAGES_INEXISTANTES` (« page declaree inexistante alors qu'elle existe ») : c'est cette garde qui rend la réduction obligatoire et inversible ;
  3. chaque cible de lien markdown de `GUIDE_WIZARD.md` vers `docs/` est déclarée dans la liste légitime — constat `MOTIF_LIEN_NON_DECLARE`, valeur ASCII `lien non declare` (fixée ici, comme les trois autres) ;
  4. chaque cible déclarée **existe** depuis la racine du dépôt — constat `MOTIF_LIEN_MORT` (« lien vers une page inexistante ») ;
  5. les phrases de `RENVOIS_SANS_LIEN` sont toujours présentes, et `docs/wizard-avance.md` est bien cité par `docs/sommaire.md` (la cible est indexée, donc l'aiguillage mène à un point d'entrée réel).

**Commit** : un seul commit local portant à la fois le lien de `docs/parcours-simplifie.md`, la réduction de la réserve et le nouveau contrôle ; `git add` par chemin explicite, jamais `git add .`. Vérifier ensuite que la suite entière reste **verte** et que le module de la phase 3 (17 tests) ne se dégrade pas.
  </action>
  <acceptance_criteria>
    - Les deux renvois de `docs/parcours-simplifie.md` (~lignes 5 et 256) portent un lien markdown vers `wizard-avance.md`, sans qu'aucun autre énoncé de la page (dont la ligne ~140) soit modifié.
    - `PAGES_INEXISTANTES` ne contient plus que `base-locale.md`, et son commentaire le dit ; les phrases `RENVOIS_SANS_LIEN` sont toujours présentes.
    - `test_lien_wizard_avance_legitime` exige les cinq invariants, avec les constantes et leurs valeurs ASCII fixées par l'action : `MOTIF_LIEN_D63` (`renvoi en prose sans lien vers la page du wizard`), `MOTIF_PAGES_INEXISTANTES` (`page declaree inexistante alors qu'elle existe`), `MOTIF_LIEN_NON_DECLARE` (`lien non declare`), `MOTIF_LIEN_MORT` (`lien vers une page inexistante`).
    - Le lien, la réduction et le contrôle sont dans **un seul** commit local, `git add` par chemin explicite.
    - Retirer l'un des deux liens, remettre `wizard-avance.md` dans la réserve, casser une cible de l'aiguillage ou déclarer un lien non déclaré font rougir la suite sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert (la suite entière, critère 5 vert inclus) et `.data/dofus.sqlite3` garde taille et `mtime_ns`.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé de la suite complète</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in lien_d63_retire pages_inexistantes_non_reduite lien_mort_dans_aiguillage lien_non_declare; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in lien_d63_retire) sed -i '0,/](wizard-avance\.md)/s/](wizard-avance\.md)//' "$T/docs/parcours-simplifie.md"; motif='renvoi en prose sans lien';; pages_inexistantes_non_reduite) printf '\nPAGES_INEXISTANTES = ("wizard-avance.md", "base-locale.md")\n' >> "$T/tests/test_docs_parcours.py"; motif='page declaree inexistante alors qu'"'"'elle existe';; lien_mort_dans_aiguillage) sed -i 's#(docs/wizard-avance\.md)#(docs/wizard-avance-manquant.md)#' "$T/GUIDE_WIZARD.md"; motif='lien vers une page inexistante';; lien_non_declare) printf '\nVoir aussi [la page CLI](docs/cli.md).\n' >> "$T/GUIDE_WIZARD.md"; motif='lien non declare';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 04-03 (TACHE 2) EN ECHEC"; exit 1; fi; echo "morsures 04-03 tache 2 : 4/4 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 04-03 (TACHE 2) EN ECHEC"</fails_when>
    <automated>noms=""; if ! noms="$(git log -1 --name-only)"; then echo "ECHEC : lecture du dernier commit impossible (git log a echoue, aucun verdict de commit)"; exit 1; fi; if [ -z "$noms" ]; then echo "ECHEC : le dernier commit ne liste aucun fichier (rien n'a ete committe)"; exit 1; fi; T="$(mktemp -d)"; printf '%s\n' "$noms" > "$T/noms.txt"; if grep -qF 'tests/test_docs_parcours.py' "$T/noms.txt" && grep -qF 'docs/parcours-simplifie.md' "$T/noms.txt"; then echo "lien D-63 et reduction de la reserve dans le meme commit : $(git log -1 --pretty=%h)"; else echo "ECHEC : le lien et la reduction de la reserve ne sont pas dans le meme commit"; exit 1; fi</automated>
    <fails_when>exit non-zero, ou la ligne "ECHEC : le lien et la reduction de la reserve ne sont pas dans le meme commit"</fails_when>
  </verify>
  <done>Les deux renvois en prose sont devenus de vrais liens vers la page unique, la réserve `PAGES_INEXISTANTES` est réduite à la seule page réellement inexistante dans le même commit, le contrôle du renvoi légitime exige déclaration et existence, la page de la phase 3 ne se dégrade pas, quatre dérives épinglées font rougir la suite sur une copie verte avant mutation et la suite entière est verte.</done>
</task>

</tasks>


<threat_model>
**Surface** : trois fichiers de documentation à la racine et dans `docs/`, deux modules de test, des commits **locaux**. Aucune dépendance ajoutée, aucun fichier de `dofus_stuff/**` modifié (D-66), aucune suppression de fichier, aucun réseau.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Tampering (preuve détruite par le vert) | Le vert du critère 5 pourrait venir d'un détecteur affaibli ou d'une copie figée neutralisée | high | mitigate | `test_copie_figee_signalee_par_le_detecteur` reste exigeant (trois constats) tandis que le test livré passe ; la mutation `renvoi_direct_reintroduit` prouve que le fichier livré rougit si l'affirmation obsolète revient ; l'énoncé produit est borné aux trois formes (D-58/D-59) |
| T-04-18 | Spoofing (aiguillage qui ment encore) | L'arborescence corrigée pourrait garder le mauvais numéro de menu | high | mitigate | Le contrôle exige le numéro **4** pour l'optimisation ; le détecteur du plan 04-04 juge l'arborescence au regard des libellés **rendus** ; le libellé `4. SYSTEME` (en gras et entre accents graves dans le fichier d'origine) est l'une des occurrences détectées avant correction |
| T-04-19 | Tampering (dette de renvoi à moitié soldée) | Un lien D-63 sans réduction de la réserve, ou l'inverse | medium | mitigate | La garde « aucune entrée de `PAGES_INEXISTANTES` ne correspond à une page qui existe » rend la réduction obligatoire ; la mutation `pages_inexistantes_non_reduite` le prouve ; le contrôle de commit exige les deux fichiers dans le **même** commit ; les phrases `RENVOIS_SANS_LIEN` restent présentes, donc la dette restante (`base-locale.md`) reste visible |
| T-04-20 | Tampering (lien mort ou non déclaré) | Le lien de l'aiguillage pourrait viser une page absente ou en cacher d'autres | medium | mitigate | `test_lien_wizard_avance_legitime` exige que chaque cible vers `docs/` soit déclarée **et** existe sur disque ; les mutations `lien_mort_dans_aiguillage` et `lien_non_declare` le prouvent |
| T-04-21 | Denial of service (contrôle de structure affaibli) | Deux liens vers le sommaire dans le même fichier affaibliraient la garde de structure (D-10/D-29) | low | mitigate | Le contrôle compte exactement un lien vers `docs/sommaire.md` dans `README.md` et interdit toute nouvelle formulation du même renvoi (D-17) |
| T-04-22 | Information disclosure (fichiers non indexés) | Un renvoi vers un fichier absent de l'index `docs/` | low | mitigate | L'aiguillage pointe `docs/wizard-avance.md`, cité par `docs/sommaire.md` (contrôle de provenance) ; le contrôle d'exhaustivité bidirectionnelle du sommaire reste la garde d'index |
| T-04-SC | Tampering | Installations de paquets | low | accept | Aucune dépendance ajoutée : `pyproject.toml` inchangé, aucun `pip install` exécuté (aucun `[ASSUMED]`, aucun `[SUS]`, aucun `[SLOP]`) |

**Hors périmètre** : publication, déploiement distant, synchronisation Dofusdude, suppression de données, achat. Aucune commande destructive ; `db clear` ne réapparaît nulle part et n'est jamais exécuté (D-22/D-23/D-61).
</threat_model>

<verification>
**Contrôles automatisés** : `test_aiguillage_et_readme` (forme courte, `H1`, liens D-46/D-48, absence d'énoncé de contenu, retrait D-62, lien unique du sommaire), `test_lien_wizard_avance_legitime` (deux liens D-63, réserve réduite, réserve qui ne peut pas mentir, cibles déclarées et existantes, phrases `RENVOIS_SANS_LIEN` présentes), le **vert** de `test_aiguillage_sans_renvoi_obsolete` avec `test_copie_figee_signalee_par_le_detecteur` toujours exigeant ses trois constats, la suite entière verte (critère 5 vert), les tests de structure de `docs/` verts sans exception, le contrôle de commit (lien + réserve dans le même commit), l'empreinte de `.data/dofus.sqlite3`, et **huit morsures** sur copie verte avant mutation : lien de l'aiguillage retiré, phrase d'arrivée directe réintroduite, ligne « Guide détaillé » remise dans le README, énoncé de contenu remis dans l'aiguillage, lien D-63 retiré, page existante remise dans la réserve, cible de l'aiguillage cassée, lien non déclaré ajouté.

**Non automatisable, nommé comme tel** : la formulation exacte de l'avis de déplacement et du `H1` (Claude's Discretion) et l'appréciation rédactionnelle du lecteur. Aucun blocage : rien ne dépend d'un geste humain, d'un secret ou d'un réseau ; aucun `checkpoint` n'est requis.

**Références de vérification de phase** : `04-VALIDATION.md` § Per-Task Verification Map assigne les lignes `aiguillage`, `readme` et `lien_d63` à ce plan (vague 4) ; `/gsd:verify-work` rejouera la suite entière, la transition rouge → vert et la mesure `.data/`.
</verification>

<success_criteria>
- `GUIDE_WIZARD.md` est un aiguillage de moins de 20 lignes, avec l'arborescence lue au code (numéro 4 pour l'optimisation), un lien vers la page unique et un lien vers le sommaire, et aucun énoncé de contenu (critère 4 du ROADMAP, D-46/D-47/D-48).
- Le détecteur ne signale plus rien sur le fichier livré et signale toujours les trois formes sur la copie figée : le critère 5 passe **de rouge à vert** dans la même phase, sans geste humain (D-59a/D-59b).
- `README.md` perd son pointeur produit sans texte de remplacement et garde son lien unique vers le sommaire (D-62/D-10/D-29).
- La dette D-44 est levée : les deux renvois en prose deviennent de vrais liens, la réserve est réduite **dans le même commit**, et la réserve ne peut pas déclarer inexistante une page qui existe (D-63).
- La ligne ~140 de `docs/parcours-simplifie.md` reste intacte et vraie (D-64) ; la suite entière est verte et `.data/dofus.sqlite3` est intact.
</success_criteria>

## Couverture des sources (ce plan)

| Source | Élément | Consommé par |
|--------|---------|--------------|
| ROADMAP § Phase 4 | Critère 4 (réduire `GUIDE_WIZARD.md` à un aiguillage corrigé, rediriger `README.md`) | tâche 1 |
| ROADMAP § Phase 4 | Critère 5 (état **vert** : le contrôle des renvois obsolètes passe après correction) | tâche 1 (transition rouge → vert) |
| ROADMAP § Phase 4 | Brief du plan 04-03 (réduire l'aiguillage, rediriger le README) | tâche 1 |
| REQUIREMENTS | WIZ-03 | `requirements` du plan |
| RESEARCH | § « Le contenu du guide à migrer, jamais à perdre » et § « Occurrences secondaires » | tâche 1 (destinations du contenu retiré) |
| RESEARCH | § « La preuve du critère 5 » (ordre rouge → vert) et les formes à détecter | tâche 1 (le vert doit venir de la correction, pas d'un détecteur affaibli) |
| CONTEXT | D-46 (aiguillage ~15 lignes : `H1`, avis, arborescence corrigée, deux liens) | tâche 1 |
| CONTEXT | D-47 (arborescence lue au code : `4. OPTIMISATION`, `3. PANOPLIES`, `5. SYSTEME`) | tâche 1 |
| CONTEXT | D-48 (deux liens de navigation, aucun énoncé de contenu) | tâche 1 |
| CONTEXT | D-49 (contenu migré, jamais perdu ; « lire le résultat » reste à `parcours-simplifie.md`) | tâche 1 |
| CONTEXT | D-62 (suppression sèche de la ligne « Guide détaillé », lien unique du sommaire conservé) | tâche 1 |
| CONTEXT | D-63 (deux renvois en prose → vrais liens, **dans le même commit que la cible**) | tâche 2 |
| CONTEXT | D-64 (la ligne ~140 reste vraie et intacte) | tâche 2 |
| CONTEXT | D-60 (le lien de l'aiguillage est un renvoi légitime, non signalé) | tâche 2 (`test_lien_wizard_avance_legitime`) |
| CONTEXT | D-10/D-29 (README à lien unique), D-17 (une seule source par énoncé) | tâche 1 |
| CONTEXT | D-19 (emplacement vérifié au fichier réel), D-13/D-14/D-15 (conventions de test) | tâches 1 et 2 |
| CONTEXT | D-66 et D-67 (aucune modification de `dofus_stuff/**`, commits locaux, documentation en français) | toutes les tâches |
| CONTEXT § Canonical Refs | `docs/installation.md` § ~69 (cohérence de la mention `F7`/`F8`) | hypothèse consignée (ancrage par le plan 04-02, fichier non modifié) |

## Output

Create `.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-03-SUMMARY.md` when done

