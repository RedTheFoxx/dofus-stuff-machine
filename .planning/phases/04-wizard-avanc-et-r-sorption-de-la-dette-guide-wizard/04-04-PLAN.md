---
phase: 04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - tests/fixtures/guide-wizard-obsolete.md
  - tests/test_docs_wizard.py
autonomous: true
requirements:
  - WIZ-03
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "La fonction pure `renvois_obsoletes(texte, faits)` est écrite d'abord, avec **trois prédicats ciblés** et rien d'autre (D-58) : (a) **renvoi au menu** — une phrase qui invite à taper un numéro pour ouvrir l'**optimisation** sans que ce numéro soit celui du menu réellement branché, ou un jeton `N. LIBELLE` dont le libellé ne **désigne pas** le menu de ce numéro (aucun mot significatif en commun avec le libellé réellement rendu) ; (b) **renvoi de filtre inversé** — une mention de `F6`/`F7` dans une phrase qui porte aussi `distance`/`melee`/`mêlée`, quand le libellé rendu de cette touche n'y apparaît pas ; (c) **renvoi à l'arrivée** — une phrase qui affirme une arrivée directe dans le wizard. Elle reçoit ses **attentes** (`faits`) **lues au rendu** (D-58), jamais recopiées, et rend une liste de constats : un constat nomme la forme, la valeur fautive, la valeur attendue **et le fichier de code qui produit cette attente**, porté par `faits` — la fonction reste pure, les producteurs lui arrivent par son argument (D-13, repris par D-65)."
    - "Les attentes sont mesurées : `faits` porte les libellés de menu lus au rendu de `GET /` et le numéro dont le `POST /` redirige vers `/optimize` (mesuré, pas supposé), le couple `F6`/`F7` lu au rendu de la page 2 de l'écran des slots **et** corroboré par `TYPE_FILTER_KEYS[5]`/`[6]` de `dofus_stuff/model/solver_spec.py:42-53` (D-53, D-58), et l'étape d'atterrissage de la chaîne mesurée en 04-02 (`recap`). Le détecteur est donc utilisable contre **n'importe quel** texte ; c'est un outil, pas un chien de garde d'un seul fichier."
    - "La **copie figée** `tests/fixtures/guide-wizard-obsolete.md` existe dans `tests/` et est signalée par le **même** détecteur : au moins trois constats, couvrant les trois formes nommées, dont un qui nomme le libellé réellement rendu du menu de l'optimisation et un qui nomme le libellé réellement rendu de `F7` (D-59b) — le rouge devient **relançable** et survit à la correction du fichier livré."
    - "Les **renvois légitimes ne sont pas signalés** (D-60) : la copie figée du détecteur ne rend aucun constat sur (i) le texte d'aiguillage corrigé (arbre de menu aux libellés rendus, complété d'un `tapez <n> pour ouvrir l'optimisation` correct, et les deux liens vers la page du wizard avancé et la page CLI), (ii) une phrase portant `F7` **et** son libellé rendu, (iii) une phrase **négative** (« le parcours ne vous amène pas directement dans le wizard »), (iv) un renvoi en prose ordinaire. La portée de la forme (a) est bornée et écrite : elle juge les jetons `N. LIBELLE` contre les libellés de **premier niveau** mesurés au rendu de l'écran d'accueil, si bien qu'un renvoi vers un **sous-menu** portant le même numéro reste hors de la surface comparée et serait rapporté comme forme (a) — précision nommée plutôt que passée sous silence (D-26). Un détecteur qui crie au loup sur ces textes serait pire que pas de détecteur."
    - "`GUIDE_WIZARD.md` **encore obsolète** est observé **ROUGE** par le même détecteur, avec les **trois formes nommées** dans les constats, et le fichier **n'est pas modifié par ce plan** : `git diff --quiet -- GUIDE_WIZARD.md` est vrai, et `tests/fixtures/guide-wizard-obsolete.md` n'est pas `GUIDE_WIZARD.md` (le test lit explicitement la copie figée, jamais le fichier livré). C'est le critère 5 dans son état rouge, sans geste humain ni jugement."
    - "La fenêtre rouge est **bornée** : sélectionné par son nom, seul `test_aiguillage_sans_renvoi_obsolete` échoue ; le reste du module, et le reste de la suite, restent verts (le nombre d'échecs de la suite complète est exactement `1`, et le nom du test qui échoue est celui-là). Le module l'annonce en docstring afin qu'un exécutant ne le prenne pas pour une régression."
    - "La **limite honnête** est écrite dans le module et dans son message : le détecteur couvre trois formes nommées, il ne revendique **aucune exhaustivité**, et toute autre forme qui prétendrait l'être est explicitement hors contrat (même arbitrage que D-26). L'énoncé produit est exactement « le détecteur ne signale rien sur l'aiguillage corrigé », jamais « plus aucun renvoi obsolète n'existe »."
  artifacts:
    - path: "tests/fixtures/guide-wizard-obsolete.md"
      provides: "La copie figée et partielle des extraits obsolètes du guide couvert par le wizard avancé : la phrase d'arrivée directe, le bloc de l'arbre de menu, la phrase des filtres, et les deux occurrences secondaires du numéro du menu d'optimisation ; toujours signalée par le détecteur, donc le rouge reste relançable après la correction"
      contains: "OPTIMISATION DE STUFF"
      min_lines: 18
    - path: "tests/test_docs_wizard.py"
      provides: "Le détecteur `renvois_obsoletes`, son lecteur d'attentes `_faits_du_rendu` (les faits **et** le fichier de code qui produit chacun), son lecteur de copie figée `_lire_fixture`, les constantes de motif des trois formes, et les trois tests correspondants"
      contains: "def renvois_obsoletes"
      min_lines: 470
  key_links:
    - from: "tests/test_docs_wizard.py"
      to: "tests/fixtures/guide-wizard-obsolete.md"
      via: "`_lire_fixture` lit la copie figée en UTF-8 explicite et lève une `AssertionError` localisante si elle manque ; le détecteur y trouve les trois formes"
      pattern: "fixtures"
    - from: "tests/test_docs_wizard.py"
      to: "dofus_stuff/web/routes.py"
      via: "`_faits_du_rendu` mesure le menu (`GET /`), le numéro branché sur l'optimisation (redirection du `POST /`) et l'étape d'atterrissage de la chaîne des trois questions"
      pattern: "/optimize/quick/niveau"
    - from: "tests/test_docs_wizard.py"
      to: "dofus_stuff/model/solver_spec.py"
      via: "corroboration du couple `F6`/`F7` par `TYPE_FILTER_KEYS[5]` et `[6]` et `TYPE_FILTER_LABELS`, en plus du rendu (D-53)"
      pattern: "TYPE_FILTER_KEYS"
    - from: "tests/test_docs_wizard.py"
      to: "GUIDE_WIZARD.md"
      via: "`test_aiguillage_sans_renvoi_obsolete` applique le détecteur au fichier livré, sans le modifier : le plan 04-03 le rendra vert"
      pattern: "GUIDE_WIZARD"
  prohibitions:
    - statement: "Ne jamais exécuter `db clear` ni laisser croire qu'une resynchronisation Dofusdude est nécessaire : la base locale reste en lecture seule et le mode hors-ligne est le mode normal (D-22, D-23, D-61)."
    - statement: "Ne jamais écrire ni supprimer quoi que ce soit sous `.data/` ou `.doc-agent/` : les attentes du détecteur se mesurent sur la fixture `app`, dont la base vit sous `tmp_path` (D-15, D-66)."
    - statement: "Ne jamais faire passer la copie figée pour de la documentation livrée : elle vit sous `tests/`, s'annonce dans son en-tête HTML comme une pièce de test, et la page `docs/` et le sommaire ne la citent jamais (D-59)."
    - statement: "Ne jamais faire passer l'énoncé produit pour une exhaustivité : le détecteur ne revendique que trois formes nommées, et le mot « exhaustif » ne doit apparaître que pour dire qu'il ne l'est pas (D-58, arbitrage D-26)."
    - statement: "Ne jamais modifier `dofus_stuff/**` pour faire taire un constat du détecteur : le code est le référentiel, l'outil s'y conforme (D-66)."
    - statement: "Ne jamais publier, déployer à distance, pousser vers un dépôt distant ni effectuer d'achat ; les commits restent locaux et `git add` se fait par chemin explicite (D-66)."
  assumptions:
    - "**Sonde d'arêtes — WIZ-03 : la ligne est `unclassified`, elle RESTE `unresolved`.** Hypothèse signalée explicitement, jamais résolue automatiquement : la sonde n'a pas su classer cette exigence. Ce que ce plan prouve est donc nommé sans prétendre au-delà — le détecteur signale **trois formes nommées** (renvoi au menu, renvoi de filtre inversé, renvoi à l'arrivée) et ne revendique **aucune exhaustivité** ; une quatrième forme resterait hors du contrat (D-58, même limite assumée que D-26)."
    - "La copie figée est **partielle et annoncée comme telle** : elle ne contient pas les 330 lignes du guide, seulement les extraits couverts par le wizard avancé (l'en-tête HTML le dit). Ce choix garde la pièce lisible et suffisante : chaque forme du détecteur y est présente au moins une fois."
    - "**Ce plan laisse volontairement la suite ROUGE sur un seul test** : `test_aiguillage_sans_renvoi_obsolete` échoue jusqu'à ce que le plan 04-03 (vague 4) corrige l'aiguillage. Sortir `GUIDE_WIZARD.md` de son état obsolète ici détruirait la preuve du critère 5 (D-59a). Le `SUMMARY.md` de ce plan doit le dire explicitement, pour qu'aucun lecteur ne prenne ce rouge pour une régression ; c'est un **résultat attendu**, pas un blocage."
    - "La forme (a) s'applique **aussi** aux occurrences secondaires du guide (ligne 50 et ligne 328, où `4. SYSTEME` désigne le mauvais menu) : la copie figée les inclut, ce qui prouve que le détecteur n'est pas un outil à une seule phrase. Les deux lignes du fichier livré seront corrigées par le plan 04-03."
    - "**Portée du détecteur, nommée** : il est appliqué à `GUIDE_WIZARD.md` — le fichier que le périmètre WIZ-03 et `.planning/PROJECT.md` désignent — et à la copie figée. Le tableau du § 4.2 de `.claude/CLAUDE.md` décrit un `test_no_obsolete_menu_references` qui porterait sur `README.md`, `GUIDE_WIZARD.md` **et** `docs/**` : vérifié au dépôt, ce test **n'existe pas** dans `tests/test_docs_structure.py` (les 14 tests du module, comptés au fichier, ne le portent pas), donc la portée multi-fichiers n'est ni un contrôle existant à satisfaire ni une garde à affaiblir ; elle est laissée hors contrat ici, `README.md` restant couvert par le contrôle de forme du plan 04-03 (`test_aiguillage_et_readme`)."
---

<objective>
**Cadrage dérivé (non une décision produit nouvelle)** — repris du `**Goal:**` de la phase 4, de son critère de succès 5 et des décisions D-58, D-59 et D-60 de `04-CONTEXT.md` :
**En tant que** mainteneur du dépôt, **je veux** un contrôle automatique qui cesse de valider en silence dès qu'un document renvoie à un menu, un filtre ou une arrivée qui n'existent plus, **afin que** la documentation ne puisse plus pourrir à l'insu de personne.

Purpose : c'est **WIZ-03** — « plus aucun renvoi obsolète ne subsiste vers les menus, filtres ou la navigation supprimés » — et c'est l'**état rouge** du critère 5 : le plan 04-03 corrigera l'aiguillage et obtiendra le vert. L'ORDRE est donc une contrainte de preuve, pas une préférence : le détecteur doit être écrit **et exécuté contre le fichier encore obsolète** avant que ce fichier ne change, sinon la preuve n'existe pas.

Output : la copie figée `tests/fixtures/guide-wizard-obsolete.md`, la fonction pure `renvois_obsoletes` et son lecteur d'attentes dans `tests/test_docs_wizard.py`, et **l'observation rouge consignée** dont le plan 04-03 produira le vert.

**Décision tranchée (l'arbitrage annoncé comme non résolu par la recherche)** : la mesure A' — le détecteur doit distinguer un renvoi légitime d'un renvoi obsolète, et « un renvoi légitime est un lien markdown vers une page existante » ne tient pas, car l'arbre de menu et la phrase de l'arrivée **ne sont pas des liens** et ne peuvent pas le devenir sans devenir faux. La mesure retenue est **B′** : l'outil **collecte les faits du jeu courant** — les libellés de menu rendus par l'écran d'accueil, le numéro de menu qui mène réellement à l'optimisation (mesuré en postant les numéros et en lisant la redirection), le couple `F6`/`F7` rendu et corroboré par les constantes publiques, l'étape d'atterrissage réelle — puis **juge une phrase à l'aune de ces faits**. Un renvoi est légitime quand il **correspond aux faits**, et non quand il est cliquable : c'est ce qui permet au texte corrigé du plan 04-03 de survivre intact.

Ordonnancement déclaré : **vague 3**, `depends_on: [04-02]` — même module, même page : exécution séquentielle. Ce plan vient **après** la construction de la page et **avant** le plan 04-03 (vague 4), parce que le détecteur a besoin du module complet et du fichier encore obsolète.

**Ce que ce plan ne fait pas** : il ne corrige **pas** `GUIDE_WIZARD.md` (plan 04-03), ne touche ni `README.md` ni `docs/parcours-simplifie.md` (plan 04-03), n'ajoute aucune section à `docs/wizard-avance.md`, ne modifie aucun fichier de `dofus_stuff/**` (D-66) et n'introduit aucune dépendance.
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-CONTEXT.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-PATTERNS.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-VALIDATION.md
@.planning/phases/03-parcours-simplifi-document-depuis-le-rendu-r-el/03-04-PLAN.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-02-PLAN.md
@docs/wizard-avance.md
@docs/parcours-simplifie.md
@docs/cli.md
@docs/sommaire.md
@GUIDE_WIZARD.md
@tests/conftest.py
@tests/test_docs_wizard.py
@tests/test_docs_parcours.py
@dofus_stuff/web/routes.py
@dofus_stuff/web/optimize_wizard.py
@dofus_stuff/model/solver_spec.py
</context>


<tasks>

<task type="auto">
  <name>Tâche 1 : le détecteur à trois formes, ses attentes mesurées et la copie figée qui le rend relançable</name>
  <files>tests/fixtures/guide-wizard-obsolete.md, tests/test_docs_wizard.py</files>
  <read_first>
    tests/test_docs_wizard.py
    tests/test_docs_parcours.py
    GUIDE_WIZARD.md
    docs/wizard-avance.md
    dofus_stuff/web/routes.py
    dofus_stuff/model/solver_spec.py
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-PATTERNS.md
  </read_first>
  <action>Écrire l'outil **avant** de l'appliquer, puis la pièce qui garde sa morsure.

**Copie figée** (`tests/fixtures/guide-wizard-obsolete.md`, nouveau fichier, UTF-8 sans BOM, fins de ligne **CRLF** comme les fichiers mesurés du dépôt — aucun contrôle de la phase ne porte sur les octets de fin de ligne de cette pièce, et `_lire_fixture` la lit en mode texte : la déclaration suit la convention mesurée au lieu d'en introduire une seconde ; c'est une pièce technique, pas une page `docs/`) : reproduire **verbatim** les extraits du guide couverts par le wizard avancé, copiés depuis `GUIDE_WIZARD.md` (jamais réécrits de mémoire) — l'arbre du menu principal avec ses cinq lignes, la phrase d'arrivée directe dans le wizard, la phrase des couples de filtres `F6`/`F7`, et la mention de `4. SYSTEME` hors de l'arbre. Le fichier s'ouvre sur un commentaire HTML qui annonce qu'il s'agit d'un extrait figé pour test, que ce **n'est pas** de la documentation, et qu'il ne doit pas être indexé ni cité par `docs/`.

**Module** — quatre ajouts, dans `tests/test_docs_wizard.py` :
1. Constantes de motif des trois formes, valeurs ASCII : `MOTIF_RENVOI_MENU` = `renvoi obsolete (a)`, `MOTIF_RENVOI_FILTRE` = `renvoi obsolete (b)`, `MOTIF_RENVOI_ARRIVEE` = `renvoi obsolete (c)`.
2. `_lire_fixture()` : lit `RACINE_DEPOT / "tests" / "fixtures" / "guide-wizard-obsolete.md"` en UTF-8 explicite et lève une `AssertionError` localisante (chemin + raison) si elle manque — jamais un `skip` silencieux.
3. `renvois_obsoletes(texte: str, faits: dict) -> list[str]`, **fonction pure** (aucun accès fichier, aucun client, aucune constante de projet à l'intérieur) avec exactement trois prédicats privés, chacun préfixant ses constats de son motif et nommant la valeur fautive, puis la valeur attendue lue dans `faits`, puis le **fichier de code producteur** de cette attente, porté par la clé `faits["source_*"]` de son groupe (D-13, repris par D-65) :
   - `_renvois_au_menu(lignes, faits)` — forme (a) : sur chaque ligne, deux prises. D'abord une **invitation** : une ligne contenant `tapez` ou `saisissez`, un numéro entre accents graves, et une racine d'« optimisation » ; si ce numéro n'est pas celui de `faits["menu_optimisation"]`, constat. Ensuite un **jeton de menu** `(\d)\. ([A-Z][A-Z' /-]{2,})` cherché **n'importe où dans la ligne** — les trois occurrences réelles du fichier sont en tête de ligne (l'arbre), en gras (`**4. SYSTEME**`) et entre accents graves, donc une prise limitée au début de ligne en manquerait deux sur trois. Un jeton est fautif si son numéro n'existe pas dans `faits["menus"]`, ou s'il ne **désigne pas** le menu de ce numéro : le libellé lu doit partager **au moins un mot significatif** (longueur ≥ 4, hors mots-outils `LISTE`, `DES`, `DE`, `LA`, `LE`, `LES`) avec le libellé réellement rendu de ce numéro. Cette règle d'appartenance, et non l'égalité stricte, est **requise par D-47** : l'arborescence corrigée que la phase 3 exige écrit `4. OPTIMISATION` et `3. PANOPLIES`, formes abrégées des libellés rendus `OPTIMISATION DE STUFF` et `LISTE DES PANOPLIES` ; l'égalité stricte déclarerait fautif l'aiguillage corrigé et rendrait le vert du critère 5 inatteignable. Elle attrape pourtant les trois inversions réelles : `3. OPTIMISATION DE STUFF` (aucun mot commun avec `LISTE DES PANOPLIES`), `4. SYSTEME` et `4. GESTION DE LA BASE` (aucun mot commun avec `OPTIMISATION DE STUFF`). Le constat nomme le numéro, le libellé lu, le libellé rendu et `faits["source_menu"]`.
   - `_renvois_de_filtre(lignes, faits)` — forme (b) : une ligne qui mentionne `F6` ou `F7` **et** l'un des mots `distance`, `melee` ou `mêlée` ; pour chaque touche mentionnée, le libellé attendu vient de `faits["filtres"]` (`F6` → libellé rendu du filtre d'index 5, `F7` → index 6) ; si le libellé attendu n'apparaît **pas** dans la ligne, constat nommant la touche, le libellé attendu, la ligne et `faits["source_filtres"]`.
   - `_renvois_a_l_arrivee(lignes, faits)` — forme (c) : une ligne qui porte à la fois une racine d'« arriv »/« arrivez »/« atterriss », `wizard` ou `assistant`, et une marque d'immédiateté (`direct`, `directement`, `sans passer`, `sans etape`), **sans** marque de négation (`pas`, `jamais`, `n'` ni `ne `) ; si l'étape d'atterrissage lue dans `faits["arrivee"]` est `recap`, constat nommant l'affirmation, l'étape réelle et `faits["source_arrivee"]`.
   La docstring de `renvois_obsoletes` écrit la **limite honnête** : le détecteur couvre ces trois formes nommées, ne revendique aucune exhaustivité, et l'énoncé produit est « le détecteur ne signale rien sur l'aiguillage corrigé », jamais « plus aucun renvoi obsolète n'existe ». Elle y ajoute la **limite de précision de la forme (a)** : un jeton `N. LIBELLE` est jugé contre les libellés de **premier niveau** mesurés au rendu de `GET /`, si bien qu'un renvoi vers un **sous-menu** portant le même numéro est hors de la surface comparée et serait rapporté comme forme (a) — précision nommée plutôt que passée sous silence (D-26), la portée de la forme (a) étant les renvois au menu principal.
4. `_faits_du_rendu(app, normalize) -> dict`, qui mesure au lieu de supposer : sur un client **neuf**, `GET /` donne les libellés de menu (`faits["menus"]` = numéro → libellé normalisé par la fixture `normalize`) ; sur un client neuf par candidat, `POST /` avec `selection=<n>` révèle par la redirection le numéro branché sur `/optimize` (`faits["menu_optimisation"]`) — jamais un numéro supposé ; la page 2 de `GET /optimize/wizard/slots` donne les libellés de filtres (`faits["filtres"]`), corroborés par `TYPE_FILTER_KEYS[5]`/`[6]` et `TYPE_FILTER_LABELS` de `dofus_stuff/model/solver_spec.py` ; et `faits["arrivee"]` reçoit le dernier segment de la chaîne d'arrivée mesurée (`recap`). Chaque groupe porte **en plus le chemin du fichier de code qui produit ses attentes** — les constantes du module déjà en place, `SOURCE_ROUTES` pour le menu et l'arrivée, `SOURCE_SPEC` pour `TYPE_FILTER_KEYS` — sous les clés `faits["source_menu"]`, `faits["source_filtres"]` et `faits["source_arrivee"]` : c'est ce qui permet à chaque constat de nommer son producteur (D-13, repris par D-65) sans que `renvois_obsoletes` n'ait à lire un fichier ni à citer une constante de projet. Les deux lecteurs sont inutilisés par un test qui ne les appelle pas : `_faits_du_rendu` est appelé par les trois tests ci-dessous, rien d'autre.

**Tests** :
- `test_copie_figee_signalee_par_le_detecteur(docs_dir, app, normalize)` : `constats = renvois_obsoletes(_lire_fixture(), _faits_du_rendu(app, normalize))`. Exiger : au moins trois constats ; chacun des trois motifs présent au moins une fois ; au moins un constat de forme (a) nomme le libellé du menu d'optimisation **rendu** ; au moins un constat de forme (b) nomme le libellé **rendu** de `F7`. Un seul `assert not constats` n'est pas utilisé ici : le test exige la **présence** des trois formes, et son message rappelle que ce fichier est la preuve relançable du critère 5 (D-59).
- `test_renvois_legitimes_non_signales(docs_dir, app, normalize)` : quatre textes témoins écrits en constantes du module (le texte corrigé type — arbre de menu **aux formes que D-47 exige** (`4. OPTIMISATION`, `3. PANOPLIES`, `5. SYSTEME`) complété d'une invitation correcte à taper le numéro du menu d'optimisation **mesuré**, et les deux liens markdown vers `wizard-avance.md` et `cli.md` —, une phrase portant `F7` **et** son libellé rendu, une phrase négative sur l'arrivée, un renvoi en prose ordinaire). Exiger `renvois_obsoletes(texte, faits) == []` pour **chacun**, avec un constat nommant le témoin fautif. Le premier témoin est aussi la preuve que la forme (a) **accepte les abréviations de D-47** : sans cette tolérance, l'aiguillage corrigé serait déclaré fautif et le vert du critère 5 deviendrait inatteignable. C'est la garde anti-faux-positif de D-60 : un détecteur qui crie au loup sur le texte corrigé serait pire que pas de détecteur.

Contraintes : aucun accès réseau, aucun fichier sous `.data/` lu autrement qu'en empreinte, aucune écriture hors de `tests/` ; `renvois_obsoletes` reste pure et sans état.
  </action>
  <acceptance_criteria>
    - `tests/fixtures/guide-wizard-obsolete.md` existe, s'annonce comme pièce de test, et contient l'arbre du menu, la phrase d'arrivée directe, la phrase des filtres et la mention hors arbre du mauvais numéro de menu.
    - `renvois_obsoletes` est une fonction pure à trois prédicats, chaque constat préfixé par son motif ASCII, et sa docstring écrit la limite honnête (trois formes nommées, précision de la forme (a) bornée aux libellés de premier niveau, aucune exhaustivité).
    - Chaque constat nomme la forme, la valeur fautive, la valeur attendue lue au rendu **et le fichier de code producteur** porté par `faits` (D-13, repris par D-65) ; `renvois_obsoletes` reste pure — aucun accès fichier, aucune constante de projet dans son corps.
    - `_faits_du_rendu` mesure les libellés de menu, le numéro branché sur `/optimize`, le couple `F6`/`F7` (rendu **et** corroboré par `TYPE_FILTER_KEYS`) et l'étape d'atterrissage, et porte les producteurs `faits["source_menu"]`, `faits["source_filtres"]`, `faits["source_arrivee"]`.
    - `test_copie_figee_signalee_par_le_detecteur` exige au moins trois constats couvrant les trois formes, dont un nommant le libellé rendu du menu d'optimisation et un nommant le libellé rendu de `F7`.
    - `test_renvois_legitimes_non_signales` rend zéro constat sur les quatre témoins, y compris le texte corrigé type (arbre aux formes `4. OPTIMISATION` / `3. PANOPLIES` que D-47 exige) et la phrase négative ; sans cette tolérance, le vert du critère 5 serait inatteignable.
    - Retirer l'une des trois formes de la copie figée fait rougir le test de la copie figée, avec le motif de la forme retirée dans la sortie (copie verte avant mutation).
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_wizard.py -q -k "copie_figee or renvois_legitimes"</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in fixture_sans_arrivee fixture_sans_filtre fixture_sans_menu; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest tests/test_docs_wizard.py -q -k "copie_figee" 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in fixture_sans_arrivee) sed -i '/directement dans le wizard/d' "$T/tests/fixtures/guide-wizard-obsolete.md"; motif='renvoi obsolete (c)';; fixture_sans_filtre) sed -i 's/F7/F5/g' "$T/tests/fixtures/guide-wizard-obsolete.md"; motif='renvoi obsolete (b)';; fixture_sans_menu) sed -i '/optimisation/Id' "$T/tests/fixtures/guide-wizard-obsolete.md"; sed -i 's/4\. SYSTEME/4. OPTIMISATION DE STUFF/g' "$T/tests/fixtures/guide-wizard-obsolete.md"; motif='renvoi obsolete (a)';; esac; out="$(cd "$T" && "$PY" -m pytest tests/test_docs_wizard.py -q -k "copie_figee" 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 04-04 (TACHE 1) EN ECHEC"; exit 1; fi; echo "morsures 04-04 tache 1 : 3/3 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 04-04 (TACHE 1) EN ECHEC"</fails_when>
  </verify>
  <done>Le détecteur pur à trois formes existe, ses attentes sont mesurées au rendu, la copie figée est signalée par le même détecteur avec les trois motifs, les quatre témoins légitimes ne sont pas signalés, et retirer l'une des trois formes de la copie figée fait rougir le test (copie verte avant mutation).</done>
</task>

<task type="auto">
  <name>Tâche 2 : appliquer le détecteur au fichier encore obsolète et consigner le rouge</name>
  <files>tests/test_docs_wizard.py</files>
  <read_first>
    tests/test_docs_wizard.py
    GUIDE_WIZARD.md
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-04-PLAN.md
  </read_first>
  <action>Appliquer l'outil au fichier **livré**, encore obsolète, et rendre le rouge **explicite et borné**.

- Ajouter `test_aiguillage_sans_renvoi_obsolete(docs_dir, app, normalize)`, qui lit `GUIDE_WIZARD.md` et exige `renvois_obsoletes(texte, _faits_du_rendu(app, normalize))` **vide**, avec une seule assertion et le message de D-13 : le message nomme **la page contrôlée** (`GUIDE_WIZARD.md`, le fichier sous test) et relaie chaque constat du détecteur, lequel nomme la forme, la valeur fautive, la valeur attendue lue au rendu **et le fichier de code producteur** porté par `faits` (`dofus_stuff/web/routes.py` pour le menu et l'arrivée, `dofus_stuff/model/solver_spec.py` pour le couple `F6`/`F7`) — un constat sans producteur est un défaut de forme à corriger, pas un détail. La docstring du test **et** le message final portent la limite honnête : ce contrôle porte sur trois formes nommées, il ne revendique aucune exhaustivité ; l'énoncé exact est « le détecteur ne signale rien sur l'aiguillage corrigé ».
- Enrichir la docstring de **module** d'une ligne qui annonce l'état attendu pendant la phase : jusqu'à la correction de l'aiguillage, ce test est **ROUGE** ; c'est la preuve du critère 5, pas une régression, et le fichier livré n'est jamais corrigé par ce plan.
- **Exécuter et consigner le rouge** : lancer le test seul et vérifier que la sortie porte les **trois motifs** de forme (a), (b) et (c), ainsi que le libellé de menu réellement rendu de l'optimisation. Vérifier ensuite que `GUIDE_WIZARD.md` est **intact** (`git diff --quiet -- GUIDE_WIZARD.md`), que le reste du module passe (`-k "not renvoi_obsolete"`), et que la suite complète compte **exactement un** échec, portant le nom de ce test.
- **Consigner le résultat** dans `04-04-SUMMARY.md` : les trois constats observés, la commande exacte, et la phrase qui dit que le vert appartient au plan 04-03 (vague 4). Aucune observation humaine n'est revendiquée : tout est sortie de commande.
  </action>
  <acceptance_criteria>
    - `test_aiguillage_sans_renvoi_obsolete` existe, lit `GUIDE_WIZARD.md`, et son message nomme la page contrôlée, la valeur attendue et le fichier de code producteur de chaque constat relayé (D-13, repris par D-65) ; il porte la limite honnête des trois formes.
    - La docstring de module annonce que ce test est rouge jusqu'à la correction de l'aiguillage (preuve du critère 5, pas une régression).
    - L'exécution du test seul est **rouge** et sa sortie porte les trois motifs `renvoi obsolete (a)`, `(b)`, `(c)` et le libellé de menu réellement rendu de l'optimisation.
    - `git diff --quiet -- GUIDE_WIZARD.md` est vrai après cette tâche (le fichier livré n'est pas modifié) et `tests/fixtures/guide-wizard-obsolete.md` est bien la pièce lue par le test de la copie figée.
    - `-k "not renvoi_obsolete"` est vert, et la suite complète compte exactement un échec, nommément `test_aiguillage_sans_renvoi_obsolete`.
    - Le rouge observé est consigné dans `04-04-SUMMARY.md` avec sa commande et ses trois constats.
  </acceptance_criteria>
  <verify>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; out="$("$PY" -m pytest tests/test_docs_wizard.py -q -k "renvoi_obsolete" 2>&1)"; if printf '%s' "$out" | grep -qE '1 (failed|error)' && printf '%s' "$out" | grep -qF 'renvoi obsolete (a)' && printf '%s' "$out" | grep -qF 'renvoi obsolete (b)' && printf '%s' "$out" | grep -qF 'renvoi obsolete (c)'; then echo "ROUGE OBSERVE : les trois formes nommees sont signalees sur GUIDE_WIZARD.md encore obsolète"; printf '%s\n' "$out" | grep -F 'renvoi obsolete' | head -6; else echo "ECHEC : le rouge attendu sur les trois formes n'a pas ete observe"; printf '%s\n' "$out" | tail -40; exit 1; fi</automated>
    <fails_when>exit non-zero, ou l'absence de l'une des lignes "ROUGE OBSERVE", "renvoi obsolete (a)", "renvoi obsolete (b)", "renvoi obsolete (c)" dans la sortie</fails_when>
    <automated>if git diff --quiet -- GUIDE_WIZARD.md; then echo "GUIDE_WIZARD.md inchange par le plan 04-04 : le rouge est reel"; else echo "ECHEC : GUIDE_WIZARD.md a ete modifie par le plan 04-04 (le rouge du critere 5 est detruit)"; exit 1; fi</automated>
    <fails_when>exit non-zero, ou la ligne "ECHEC : GUIDE_WIZARD.md a ete modifie"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; "$PY" -m pytest tests/test_docs_wizard.py -q -k "not renvoi_obsolete"; statut=$?; if [ "$statut" -ne 0 ]; then echo "ECHEC : le reste du module n'est pas vert (fenetre rouge non bornee)"; exit 1; fi; out="$("$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '^1 (failed|error)|, 1 failed' && printf '%s' "$out" | grep -qF 'test_aiguillage_sans_renvoi_obsolete' && ! printf '%s' "$out" | grep -qE '[2-9] failed'; then echo "fenetre rouge bornee : exactement un echec, test_aiguillage_sans_renvoi_obsolete"; else echo "ECHEC : la fenetre rouge n'est pas bornee a un seul test"; printf '%s\n' "$out" | tail -40; exit 1; fi</automated>
    <fails_when>exit non-zero, ou une ligne "ECHEC :", ou une sortie de suite qui n'annonce pas exactement un échec portant le nom du test</fails_when>
  </verify>
  <done>Le détecteur est appliqué au fichier encore obsolète, le rouge est observé et consigné avec ses trois formes nommées, `GUIDE_WIZARD.md` reste intact, le reste du module est vert et la suite complète n'échoue que sur ce seul test — l'état rouge du critère 5 est prouvé machine, sans geste humain.</done>
</task>

</tasks>


<threat_model>
**Surface** : un fichier de fixture sous `tests/`, une fonction pure et trois tests dans `tests/test_docs_wizard.py`, le client de test Flask en processus (pour collecter les faits) et la base locale en **lecture seule**. Aucune dépendance ajoutée, aucun fichier de `dofus_stuff/**` modifié (D-66), **aucune** modification de `GUIDE_WIZARD.md` (le plan 04-03 s'en charge).

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Spoofing (faux positif du détecteur) | `renvois_obsoletes` : un contrôle qui crie au loup rendrait le harnais nuisible | high | mitigate | `test_renvois_legitimes_non_signales` exige zéro constat sur quatre témoins — dont le **texte corrigé type** (arbre de menu aux libellés rendus, invitation correcte à taper le numéro du menu d'optimisation mesuré, les deux liens) et une phrase **négative** sur l'arrivée ; chaque témoin est une constante du module, exécutée à chaque suite |
| T-04-12 | Spoofing (attentes recopiées) | `faits` recopiés à la main au lieu d'être mesurés | high | mitigate | `_faits_du_rendu` lit les libellés de menu au rendu, découvre le numéro branché sur `/optimize` **par la redirection du `POST`** (jamais un numéro supposé), lit le couple `F6`/`F7` au rendu et le corrobore par `TYPE_FILTER_KEYS` (D-53/D-58) ; aucune attente littérale de menu, de filtre ou d'arrivée n'existe dans le module |
| T-04-13 | Tampering (preuve détruite) | Corriger `GUIDE_WIZARD.md` dans ce plan supprimerait le rouge du critère 5 | high | mitigate | `git diff --quiet -- GUIDE_WIZARD.md` est vérifié après la tâche 2 ; la docstring de module annonce le rouge attendu ; la copie figée rend le rouge **relançable** après la correction (D-59a/D-59b) |
| T-04-14 | Tampering | `.data/dofus.sqlite3` pendant la collecte des faits | medium | mitigate | La collecte se fait sur la fixture `app` (base sous `tmp_path`) ; la tâche 1 n'écrit rien sous `.data/` ; l'empreinte reste mesurée par les plans 04-01 et 04-02 et par `/gsd:verify-work` |
| T-04-15 | Information disclosure | La copie figée pourrait passer pour de la documentation ou être indexée | low | mitigate | La copie vit sous `tests/`, s'ouvre sur un commentaire HTML qui annonce sa nature, n'est citée par aucune page `docs/`, et le contrôle d'exhaustivité bidirectionnelle du sommaire (`tests/test_docs_structure.py`) reste la garde de l'index |
| T-04-16 | Denial of service (du harnais) | Collecte des faits par `POST` sur plusieurs clients | low | accept | Une poignée de requêtes en processus, quelques dizaines de millisecondes ; aucune boucle non bornée, aucun socket, aucun serveur |
| T-04-SC | Tampering | Installations de paquets | low | accept | Aucune dépendance ajoutée : `pyproject.toml` inchangé, aucun `pip install` exécuté (aucun `[ASSUMED]`, aucun `[SUS]`, aucun `[SLOP]`) |

**Hors périmètre** : publication, déploiement distant, synchronisation Dofusdude, suppression de données, achat. Aucune commande destructive ; le détecteur ne lit que des fichiers du dépôt et la fixture de test.
</threat_model>

<verification>
**Contrôles automatisés** : le test de la copie figée (trois formes présentes), le test des témoins légitimes (zéro faux positif), le test de l'aiguillage livré (**rouge observé ici**, vert au plan 04-03), le reste du module vert, la suite complète avec **exactement un** échec nommé, `git diff --quiet -- GUIDE_WIZARD.md`, et **trois morsures** de la copie figée sur copie verte avant mutation (arrivée retirée, mention de `F7` remplacée, mauvais numéro de menu corrigé).

**État final attendu de cette vague : la suite est ROUGE sur un seul test, volontairement.** C'est le critère 5 dans son état rouge (D-59a) ; le plan 04-03 (vague 4) produit le vert. Cet état est explicite, mesuré et borné — jamais un blocage, jamais une régression, et il est consigné dans `04-04-SUMMARY.md`.

**Non automatisable, nommé comme tel** : la couverture de formes de renvoi non nommées (le détecteur en couvre trois et le dit) ; aucune validation humaine n'est requise ni revendiquée. Aucun blocage : rien ne dépend d'un geste humain, d'un secret ou d'un réseau.

**Références de vérification de phase** : `04-VALIDATION.md` § Per-Task Verification Map assigne les lignes `detecteur` et `copie_figee` à ce plan (vague 3) ; `/gsd:verify-work` rejouera la copie figée, le rouge borné et, après 04-03, le vert.
</verification>

<success_criteria>
- `renvois_obsoletes` est un outil pur, à trois formes nommées, dont les attentes sont **mesurées** au rendu et non recopiées (D-58).
- La copie figée est signalée par le même détecteur, donc le rouge du critère 5 est relançable après la correction du fichier livré (D-59b).
- Aucun renvoi légitime n'est signalé : texte corrigé type, phrase portant `F7` **et** son libellé rendu, phrase négative, renvoi en prose (D-60).
- `GUIDE_WIZARD.md` encore obsolète est observé **rouge** avec les trois formes, le fichier n'est pas modifié par ce plan, et la suite ne compte qu'un seul échec, ce test (WIZ-03, critère 5 état rouge).
- Trois morsures de la copie figée sont détectées sur une copie verte avant mutation ; la limite d'inexhaustivité est écrite dans le module.
</success_criteria>

## Couverture des sources (ce plan)

| Source | Élément | Consommé par |
|--------|---------|--------------|
| ROADMAP § Phase 4 | Critère 5 (le contrôle des renvois obsolètes, **observé**) | tâches 1 et 2 (écriture puis observation rouge bornée) |
| ROADMAP § Phase 4 | Brief du plan 04-04 (ajouter le contrôle des renvois obsolètes, observé) | tâches 1 et 2 |
| REQUIREMENTS | WIZ-03 | `requirements` du plan |
| RESEARCH | § « Ce que le détecteur doit distinguer » et les trois formes exactes (arrivée, menu, filtre) | tâche 1 (prédicats) |
| RESEARCH | § « La phrase qui rend la mesure A' fausse » (l'arbre de menu et la phrase d'arrivée ne sont pas des liens) | `<objective>` (mesure B′ retenue) et tâche 1 (`_faits_du_rendu`) |
| RESEARCH | § « Renvois légitimes à ne pas signaler » (trois textes témoins) | tâche 1 (garde anti-faux-positif) |
| RESEARCH | § « Occurrences secondaires du guide » (lignes 50 et 328) | tâche 1 (copie figée et prédicat de jeton de menu) |
| RESEARCH | Wave 0 Gaps : `tests/fixtures/` inexistant | tâche 1 (création de la copie figée) |
| CONTEXT | D-58 (trois formes ciblées, attentes lues au rendu, aucune exhaustivité revendiquée) | tâche 1 (prédicats et docstring) et tâche 2 (message) |
| CONTEXT | D-59 (a : rouge **avant** la correction, b : copie figée relançable, c : la copie vit sous `tests/` et n'est pas indexée) | tâche 1 (fixture) et tâche 2 (rouge borné) |
| CONTEXT | D-60 (les renvois légitimes ne sont pas signalés) | tâche 1 (test des témoins) |
| CONTEXT | D-53, D-54 (couple `F6`/`F7` adossé au rendu et aux constantes publiques) | tâche 1 (`faits["filtres"]`) |
| CONTEXT | D-13, D-14, D-15 (message localisant, API publique, interdits d'exécution) | tâches 1 et 2 |
| CONTEXT | D-26 (limite assumée plutôt que fausse exhaustivité) | `<objective>` et docstring du détecteur |
| CONTEXT | D-66 et D-67 (aucune modification de `dofus_stuff/**`, documentation en français) | toutes les tâches |

## Output

Create `.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-04-SUMMARY.md` when done

