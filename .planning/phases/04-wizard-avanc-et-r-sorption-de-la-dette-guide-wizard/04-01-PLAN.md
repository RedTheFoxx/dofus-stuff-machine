---
phase: 04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/wizard-avance.md
  - docs/sommaire.md
  - tests/test_docs_wizard.py
autonomous: true
requirements:
  - WIZ-01
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Les 9 étapes sont citées dans l'ordre exact de `WIZARD_STEPS` (`dofus_stuff/web/optimize_wizard.py:23-33`) avec les titres lus au rendu de chacun des 9 écrans (`STEP_TITLES`, `dofus_stuff/web/optimize_wizard.py:35-45`) : le test parcourt `enumerate(WIZARD_STEPS, start=1)`, exige que le titre rendu par `GET /optimize/wizard/<etape>` soit cité par la page, et que la suite des titres cités respecte l'ordre du code (constat portant le jeton `ordre de WIZARD_STEPS`) — **arête `ordering` de la sonde WIZ-01, critère confirmable** (D-50)."
    - "Les 11 slots et les 10 filtres sont cités avec **leur propre numéro** : l'ensemble des libellés de slots cités par la page est exactement celui rendu par la page 1 de `GET /optimize/wizard/slots`, et chaque couple `F<n>`/libellé cité est égal au couple lu au rendu (page 1 et page 2 de la même URL), donc `F6` = `ARMES DISTANCE` et `F7` = `ARMES MELEE` ; permuter les deux valeurs de `TYPE_FILTER_LABELS` (`dofus_stuff/web/optimize_wizard.py:111-121`) fait rougir le module — **arête `adjacency` de la sonde WIZ-01, critère confirmable** (D-53)."
    - "Les deux formats d'édition ne sont **jamais fusionnés** : la page associe `BASE POINTS CIBLE POIDS` à l'écran des caractéristiques et `BASE EXO CIBLE POIDS` à l'écran PA / PM / PO, chacun lu au rendu du sous-écran d'édition réellement atteint (`pgm=OPT-WED`) ; permuter les deux messages de refus dans `dofus_stuff/web/optimize_wizard.py:375-392` fait rougir le module — **seconde arête `adjacency` de la sonde WIZ-01, critère confirmable** (D-54)."
    - "L'état vide des listes d'objets est cité tel que le rendu le porte (`(aucun)`) et vérifié **dans les deux états** : un client neuf rend `(aucun)` pour `INTERDITS` et pour `FORCES`, et après un `POST` `CLEAR` les deux listes reviennent à `(aucun)` ; remplacer le littéral d'état vide dans `dofus_stuff/web/optimize_wizard.py:240-260` fait rougir le module — **arête `empty` de la sonde WIZ-01, critère confirmable** (D-54)."
    - "Les 11 options de l'écran `OPTIONS SOLVEUR` sont citées (numéro et libellé) et le nombre d'options est **dérivé du rendu**, jamais d'une table locale privée : le test extrait les lignes numérotées du corps rendu par `GET /optimize/wizard/options` (`body_options`, `dofus_stuff/web/optimize_wizard.py:198-215`) et exige l'égalité d'ensembles avec ce que la page cite (D-14, critère 1 du ROADMAP)."
    - "La syntaxe d'items est citée telle que le code l'applique, sans reformulation : `+ID` = AJOUTER INTERDIT (ce **n'est pas** un refus), `-ID` = AJOUTER FORCE, `!ID` = RETIRER, `CLEAR` (et `clear`) = VIDER ; le refus unique est le message exact rendu par le code (`ValueError` portant `Syntaxe : +ID | -ID | !ID | CLEAR`, `dofus_stuff/web/optimize_wizard.py:424`) et un identifiant **sans préfixe** est refusé — les deux états sont postés et observés (D-19, D-54, critère 2 du ROADMAP)."
    - "La page suit le gabarit D-01/D-56 : un seul `H1`, égal au libellé d'index après normalisation (D-11), un bloc `## Source de vérité` dont chaque chemin entre accents graves existe depuis la racine du dépôt, la dernière ligne non vide `[Retour au sommaire](sommaire.md)`, des fins de ligne CRLF sans BOM, aucun lien externe et aucun bloc de commandes."
    - "`docs/sommaire.md` croît d'**exactement une** cible et les gardes existantes restent vertes sans exception : exhaustivité bidirectionnelle (`problemes_index`), `H1` (`problemes_h1`), retour au sommaire (`problemes_retour_sommaire`), encodage (`problemes_encodage`) ; créer la page sans la ligne d'index rougit, et ajouter la ligne sans créer la page rougit aussi (D-06, D-57)."
  artifacts:
    - path: "docs/wizard-avance.md"
      provides: "La page unique du flux avancé : les 9 étapes dans l'ordre du code, les slots et filtres, les 11 options, les formats d'édition des quatre nombres, la syntaxe d'items, le bloc Source de vérité et la ligne de retour"
      contains: "## Les 9 étapes du wizard"
      min_lines: 130
    - path: "docs/sommaire.md"
      provides: "La ligne d'index « Wizard avancé » dans la table `## Index`, sans autre modification"
      contains: "| [Wizard avancé](wizard-avance.md) |"
      min_lines: 22
    - path: "tests/test_docs_wizard.py"
      provides: "Le module d'ancrage : marqueurs et lecteurs du rendu, garde de clôture, contrôles des étapes, des slots et filtres, des options, des formats, des items et de la conformité de la page"
      contains: "def test_page_et_index_du_wizard"
      min_lines: 240
  key_links:
    - from: "tests/test_docs_wizard.py"
      to: "dofus_stuff/web/optimize_wizard.py"
      via: "lecture des constantes publiques `WIZARD_STEPS`, `STEP_TITLES`, `TYPE_FILTER_LABELS` et rendu des 9 écrans par le client de test Flask, jamais d'introspection privée (D-14)"
      pattern: "STEP_TITLES"
    - from: "tests/test_docs_wizard.py"
      to: "dofus_stuff/model/solver_spec.py"
      via: "lecture de `TYPE_FILTER_KEYS` (index 5 et 6) comme seconde source du couple `F6`/`F7`, et de `SLOT_GROUPS` pour l'ordre des 11 emplacements"
      pattern: "TYPE_FILTER_KEYS"
    - from: "docs/wizard-avance.md"
      to: "docs/sommaire.md"
      via: "la ligne d'index ajoutée dans la table et la dernière ligne de retour `[Retour au sommaire](sommaire.md)`"
      pattern: "sommaire.md"
    - from: "tests/test_docs_wizard.py"
      to: "dofus_stuff/web/templates/screen.html"
      via: "marqueurs HTML du rendu (corps, ligne de statut, ligne de corps, libellé de saisie, couple touche/libellé) repris de la phase 3 (04-PATTERNS.md § 2)"
      pattern: "row status"
  prohibitions:
    - statement: "Ne jamais inventer de sémantique que le code ne porte pas : la page décrit ce que le rendu et les constantes publiques portent, jamais une intention supposée, et un refus est cité mot pour mot (D-19)."
    - statement: "Ne jamais présenter `db clear` ni aucune commande destructrice comme une étape d'un parcours recommandé, et ne jamais l'exécuter (D-22, D-23, D-61)."
    - statement: "Ne jamais écrire ni supprimer quoi que ce soit sous `.data/` ou `.doc-agent/`, et ne jamais exécuter `main()` ni lancer le solveur pour observer autre chose que ce que la phase mesure (D-15, D-66)."
    - statement: "Ne jamais modifier `dofus_stuff/**` pour aligner la documentation sur elle-même : le code est le référentiel, la page s'y conforme (D-66)."
    - statement: "Ne jamais publier, déployer à distance, pousser vers un dépôt distant ni effectuer d'achat : les commits restent locaux, `git add` par chemin explicite, jamais `git add .` (D-66)."
    - statement: "Ne jamais faire passer la copie figée prévue par le plan 04-04 pour une page de documentation livrée : elle est un artefact de `tests/`, annoncé comme tel (D-59)."
  assumptions:
    - "**Sonde d'arêtes — WIZ-01 : les 3 lignes classables sont RÉSOLUES** (`ordering`, `adjacency` ×2, `empty`) et chacune est écrite comme critère confirmable dans `truths` ci-dessus. Aucun marqueur `backstop` n'est nécessaire dans ce plan, et aucun item n'est écarté en silence : les 5 lignes sondées se répartissent en 3 écrites ici plus 2 signalées comme hypothèses (WIZ-02 dans le plan 04-02, WIZ-03 dans le plan 04-04)."
    - "Les 11 options **n'ont aucune constante publique** : `body_options` écrit onze lignes littérales et la table d'application est locale à `apply_options_input` (`04-RESEARCH.md` § Pitfall 3, `04-PATTERNS.md` § « Ce qui n'a pas d'analogue »). Décision enregistrée : l'ancrage se fait au **rendu**, ce qui prouve que la page et l'écran ne divergent pas, mais ne prétend pas à une constante de comptage."
    - "La prose libre de la page (phrases d'explication) n'est pas vérifiée par un test : les contrôles portent sur les libellés, les nombres et les messages **cités**, et ce plan ne revendique aucune exhaustivité de la rédaction (limite nommée, même arbitrage que D-26)."
    - "Le regroupement des 9 étapes en sous-sections est laissé au rédacteur (Claude's Discretion, `04-CONTEXT.md`) : le contrôle lit les titres de niveau 2 épinglés et un helper local `_sous_section` pour les `H3`, jamais un helper promu dans `tests/conftest.py` (D-12)."
---

<objective>
**Cadrage dérivé (non une décision produit nouvelle)** — repris du `**Goal:**` de la phase 4 (« Le flux avancé est décrit dans une source unique alignée sur le code »), de ses critères de succès 1 et 2, et des décisions D-50, D-51, D-53, D-54, D-56 et D-57 de `04-CONTEXT.md` :
**En tant que** joueur qui veut un stuff sur mesure, **je veux** une seule page qui décrive le wizard avancé étape par étape, avec les vrais libellés, les vrais formats d'édition et la vraie syntaxe d'items, **afin de** régler le solveur sans lire le code et sans tomber sur une affirmation périmée.

Purpose : c'est **WIZ-01** — « un lecteur peut parcourir les étapes du wizard, éditer les formats de caractéristiques et utiliser la syntaxe d'items interdits/forcés » — et la création de la **cible unique** que les critères 4 et 5 de la phase exigent ensuite (l'aiguillage et le détecteur pointent cette page). La page est écrite **depuis le rendu réel** (D-32/D-36, méthode de la phase 3) : chaque libellé cité est lu au rendu ou dans une constante publique, jamais de mémoire.

Output : `docs/wizard-avance.md` (page complète du périmètre WIZ-01), la ligne d'index de `docs/sommaire.md` (D-57) et `tests/test_docs_wizard.py` (module d'ancrage créé ici, complété par le plan 04-02 puis par le plan 04-04).

Ordonnancement déclaré : **vague 1**, `depends_on: []` — c'est le premier plan de la phase, et il ouvre la tranche verticale (page + index + harnais + rendu) dont les trois autres plans étendent le périmètre.

**Ce que ce plan ne fait pas** : il n'écrit ni la section du chemin d'arrivée, ni celle des touches et commandes, ni l'exemple guidé (plan 04-02, D-51/D-55) ; il ne touche ni `GUIDE_WIZARD.md`, ni `README.md`, ni `docs/parcours-simplifie.md` (plan 04-03) ; il n'écrit aucun détecteur de renvois obsolètes (plan 04-04) ; il ne documente **aucune** commande de `fetcher.py` (D-37 : la surface de commandes reste dans `docs/cli.md`) et il ne modifie aucun fichier de `dofus_stuff/**` (D-66).
</objective>

<execution_context>
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/workflows/execute-plan.md
@C:/Users/Red/Documents/Projets/gsd-core-automated/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-CONTEXT.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-PATTERNS.md
@.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-VALIDATION.md
@.planning/phases/01-socle-documentaire-installation-et-harnais-v-rifiable/01-CONTEXT.md
@docs/sommaire.md
@docs/parcours-simplifie.md
@docs/cli.md
@tests/conftest.py
@tests/test_docs_parcours.py
@tests/test_docs_structure.py
@dofus_stuff/web/optimize_wizard.py
@dofus_stuff/model/solver_spec.py
@dofus_stuff/web/routes.py
@dofus_stuff/web/templates/screen.html
</context>


<tasks>

<task type="tracer">
  <name>Tâche 1 : tranche verticale « la page existe, son index est cohérent, son ancrage au rendu mord »</name>
  <files>docs/wizard-avance.md, docs/sommaire.md, tests/test_docs_wizard.py</files>
  <reversibility rating="reversible">Créer une page `docs/` et sa ligne d'index est annulable par un commit de retour ; rien n'est publié ni déplacé hors du dépôt.</reversibility>
  <read_first>
    docs/sommaire.md
    docs/parcours-simplifie.md
    docs/cli.md
    tests/conftest.py
    tests/test_docs_structure.py
    tests/test_docs_parcours.py
    dofus_stuff/web/optimize_wizard.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/templates/screen.html
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-PATTERNS.md
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
  </read_first>
  <action>Écrire la tranche la plus fine qui traverse **toutes** les couches de la phase — page, index, harnais, rendu — et la rendre vérifiable de bout en bout avant toute expansion.

**Page** (`docs/wizard-avance.md`, nouveau fichier, UTF-8 sans BOM, fins de ligne CRLF comme les quatre pages existantes) — première version **livrable**, pas une ébauche :
- un `H1` exactement `# Wizard avancé` (le libellé d'index, sans apostrophe ni précision entre parenthèses : `problemes_h1` compare après normalisation D-11, Pitfall 4 de la recherche) ;
- une phrase d'introduction : cette page décrit le parcours avancé de l'interface web, étape par étape, depuis le rendu réel de l'outil ; la surface des commandes n'est pas recopiée ici, elle appartient à la page CLI (D-52, D-37), et les libellés, formats et touches sont cités tels que le code les applique ;
- la section `## Les 9 étapes du wizard` : une ligne par étape, numérotée de 1 à 9, portant le **titre réellement rendu** et le mot-clé de l'étape entre accents graves, dans l'ordre du code — `SLOTS ET FILTRES` (`slots`), `OPTIONS SOLVEUR` (`options`), `CARACTERISTIQUES` (`caracs`), `PA / PM / PO` (`papmpo`), `RESISTANCES` (`resistances`), `DOMMAGES` (`damages`), `DIVERS` (`misc`), `ITEMS INTERDITS / FORCES` (`items`), `RECAPITULATIF` (`recap`). D-50 fixe cet ordre ; les titres sont relus dans `STEP_TITLES` et confrontés au rendu ;
- le bloc `## Source de vérité` : un chemin **existant** par ligne, entre accents graves, choisis parmi `dofus_stuff/web/optimize_wizard.py`, `dofus_stuff/web/routes.py`, `dofus_stuff/model/solver_spec.py`, `dofus_stuff/web/screens.py`, `dofus_stuff/web/templates/screen.html` (D-03, D-56) ; aucun chemin inventé, aucune constante privée ;
- la dernière ligne non vide, exactement `[Retour au sommaire](sommaire.md)`.

**Index** (`docs/sommaire.md`) — ajouter **une seule** ligne, dans la table `## Index`, en respectant la casse et la forme des trois lignes existantes : `| [Wizard avancé](wizard-avance.md) | Les 9 étapes du wizard, ses filtres, ses formats et ses touches |`. La liste « Parcours conseillé » reste **sans lien** markdown (elle porte déjà « 3. Wizard avancé » en texte) : D-05/D-57 et Pitfall 4. Aucun autre énoncé du sommaire n'est modifié.

**Module** (`tests/test_docs_wizard.py`, nouveau fichier) — squelette **complet et définitif** de l'outillage du module ; les plans 04-02 et 04-04 n'auront qu'à l'étendre :
- docstring de module sur le patron de `tests/test_docs_parcours.py:1-11` : le contrat va du rendu vers la page, les écrans sont rendus par le client de test Flask en processus sur la fixture `app`, aucun serveur, aucun socket, `main()` jamais exécuté, rien n'est écrit sous `.data/` (D-15/D-32) ;
- imports : `ast`, `re`, `pathlib.Path`, `pytest` — **aucun** import de `msgpack`, `base64`, `hashlib`, `sqlite3`, `subprocess`, `socket`, `webbrowser` (la garde de clôture ci-dessous les refuse) ; constantes `RACINE_DEPOT`, `PAGE`, `SOMMAIRE`, `GUIDE_WIZARD`, `README`, `SOURCE_ROUTES`, `SOURCE_WIZARD`, `SOURCE_SPEC`, `SOURCE_SCREENS`, `SOURCE_TEMPLATE` ;
- marqueurs du rendu repris **à l'identique** de `tests/test_docs_parcours.py:100-106` (`MARQUEUR_CORPS`, `MARQUEUR_STATUT`, `LIGNE_CORPS`, `LIBELLE_SAISIE`, `TOUCHE_BARRE`) et lecteurs locaux `_texte_page`, `_lignes_du_corps`, `_statut`, `_touches`, `_sous_section` — les sections de la page sont extraites par la fixture `section` du `tests/conftest.py`, appelée selon le patron de `tests/test_docs_parcours.py:339` (`section(texte, titre, PAGE)`), jamais par un extracteur réinventé ; la docstring de `_sous_section` dit pourquoi il reste local (le helper partagé ne connaît que les `H2`, D-12) ;
- `TITRES_SECTION_ATTENDUS`, constante de module, initialisée **ici** avec les six titres que ce plan crée, dans l'ordre final : `## Les 9 étapes du wizard`, `## Slots et filtres`, `## Les 11 options du solveur`, `## Les quatre nombres d'une ligne`, `## Interdire, forcer, retirer un objet`, `## Source de vérité`. Le plan 04-02 **complétera** cette constante avec `## Arriver au wizard`, `## Touches et commandes` et `## Exemple guidé`, aux positions finales ;
- constantes de garde de clôture reprises de `tests/test_docs_parcours.py:245-263` (`RACINES_INTERDITES`, `MODULE_BASE_INTERDIT`, `APPELS_SUPPRESSION`, `APPEL_PRODUIT`) et test `test_garde_de_cloture_du_harnais` : analyse `ast` de `Path(__file__)` — aucun import des racines interdites, aucun import de `dofus_stuff.database`, aucun appel de nom `main`, aucun appel dont un argument nommé `data` porte la paire `"cmd": "GO"` ; le message d'échec rappelle pourquoi (poster `GO` exécute le solveur, `dofus_stuff/web/routes.py:1114-1116`) ;
- test `test_page_et_index_du_wizard(docs_dir, app, normalize)`, la **tranche de bout en bout** : lit la page par `_texte_page`, exige la ligne d'index dans le sommaire, exige un `H1` unique égal au libellé d'index, puis rend **chacun** des 9 écrans (`GET /optimize/wizard/<etape>` sur un client neuf, sans état de session) et exige que le titre rendu de l'écran soit cité par la page (normalisé par la fixture `normalize`, D-11). Les constats sont accumulés dans `constats: list[str]` et joints à **une seule** assertion `assert not constats` ; chaque constat suit le message de D-13 : `{page} : <ce qui est faux> ; attendu <valeur> <producteur, fichier de code ou ligne>`.

Contraintes de forme : aucun bloc de code balisé `console` dans la page, aucun lien externe, aucun jeton de brouillon, encodage CRLF sans BOM. Le module n'exécute jamais `main()`, n'importe jamais `sqlite3` et n'écrit rien sous `.data/`.
  </action>
  <acceptance_criteria>
    - `docs/wizard-avance.md` existe, commence par `# Wizard avancé`, contient `## Les 9 étapes du wizard` avec les 9 titres rendus et `## Source de vérité`, et sa dernière ligne non vide est `[Retour au sommaire](sommaire.md)`.
    - `docs/sommaire.md` contient la ligne `| [Wizard avancé](wizard-avance.md) | Les 9 étapes du wizard, ses filtres, ses formats et ses touches |` dans la table `## Index`, et la liste « Parcours conseillé » n'a pas gagné de lien markdown.
    - `tests/test_docs_wizard.py` existe, ne reprend aucun helper de `tests/conftest.py`, et `test_garde_de_cloture_du_harnais` passe.
    - `test_page_et_index_du_wizard` rend les 9 écrans du wizard et exige les 9 titres cités par la page ; renommer une valeur de `STEP_TITLES` rougit le module.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_wizard.py tests/test_docs_structure.py -q` est vert, ainsi que la suite entière.
    - `.data/dofus.sqlite3` garde taille, `mtime_ns` et SHA-256 identiques avant et après la suite complète.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_wizard.py tests/test_docs_structure.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; T="$(mktemp -d)"; mesure() { "$PY" -c "import hashlib,pathlib;p=pathlib.Path('.data/dofus.sqlite3');print(f'{p.stat().st_size}:{p.stat().st_mtime_ns}:{hashlib.sha256(p.read_bytes()).hexdigest()}' if p.exists() else 'base absente')"; }; mesure > "$T/avant.txt"; "$PY" -m pytest -q; statut=$?; mesure > "$T/apres.txt"; if [ "$statut" -ne 0 ]; then echo "SUITE COMPLETE ROUGE (exit $statut)"; exit 1; fi; if diff -q "$T/avant.txt" "$T/apres.txt" >/dev/null; then echo "empreinte .data/ identique autour de la suite complete : $(cat "$T/apres.txt")"; else echo "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE : avant=$(cat "$T/avant.txt") apres=$(cat "$T/apres.txt")"; exit 1; fi</automated>
    <fails_when>exit non-zero, ou la ligne "EMPREINTE .data/ MODIFIEE PENDANT LA SUITE", ou la ligne "SUITE COMPLETE ROUGE", ou une ligne contenant "failed" ou "error"</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in titre_etape index_retire h1_desaligne; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in titre_etape) sed -i 's/"resistances": "RESISTANCES"/"resistances": "RESISTANCES ELEM"/' "$T/dofus_stuff/web/optimize_wizard.py"; motif='RESISTANCES ELEM';; index_retire) sed -i '/\[Wizard avancé\](wizard-avance.md)/d' "$T/docs/sommaire.md"; motif='page non listee dans docs/sommaire.md';; h1_desaligne) sed -i 's/^# Wizard avancé/# Wizard avancé (avance)/' "$T/docs/wizard-avance.md"; motif='different du libelle d'index';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 04-01 (TACHE 1) EN ECHEC"; exit 1; fi; echo "morsures 04-01 tache 1 : 3/3 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 04-01 (TACHE 1) EN ECHEC"</fails_when>
  </verify>
  <done>La page existe, porte son libellé d'index, cite les 9 titres réellement rendus, et le module d'ancrage la contrôle de bout en bout ; trois dérives épinglées (titre d'étape renommé, ligne d'index retirée, `H1` désaligné) font rougir la suite sur une copie verte avant mutation, et l'empreinte de `.data/` est inchangée.</done>
</task>


<task type="auto">
  <name>Tâche 2 : les 9 étapes dans l'ordre du code, les slots et les filtres</name>
  <files>docs/wizard-avance.md, tests/test_docs_wizard.py</files>
  <read_first>
    docs/wizard-avance.md
    tests/test_docs_wizard.py
    dofus_stuff/web/optimize_wizard.py
    dofus_stuff/model/solver_spec.py
    dofus_stuff/web/routes.py
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-PATTERNS.md
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
  </read_first>
  <action>Compléter la section des étapes et ajouter la section des slots et filtres, avec leurs deux contrôles.

**Page** :
- `## Les 9 étapes du wizard` gagne, pour chaque étape, une proposition décrivant **ce que l'écran fait** (les slots et filtres se togglent par numéro et par `F<n>` ; les options s'éditent par numéro ; les écrans de statistiques s'éditent par numéro de ligne et affichent `N=EDIT` ; l'écran des items applique la syntaxe `+ID`/`-ID`/`!ID`/`CLEAR` ; le récapitulatif rassemble et offre `GO`, `RESET`, `SAVES` et `1`–`8`). Les libellés restent ceux du rendu.
- nouvelle section `## Slots et filtres` : les **11 emplacements** avec leur numéro et leur libellé rendu (`AMULETTE`, `ANNEAUX`, `CEINTURE`, `BOTTES`, `COIFFE`, `CAPE`, `ARME`, `BOUCLIER`, `DOFUS/TROPHEES`, `FAMILIER/MONTURE`, `PRYSMARADITE`), les **10 filtres** avec leur touche et leur libellé rendu (dont `F6` = `ARMES DISTANCE` et `F7` = `ARMES MELEE`, énoncé explicitement), les deux invites rendues `SLOTS (N=TOGGLE) :` et `FILTRES TYPES (F+N) :`, la ligne `N=TOGGLE SLOT  FN=TOGGLE FILTRE`, et le comportement mesuré : un numéro `1`–`11` bascule l'emplacement, `F<n>` avec `1 ≤ n ≤ 10` bascule le filtre, une entrée vide passe à l'étape suivante, une autre saisie rend `SAISIE INVALIDE`, `F11` rend `FILTRE INVALIDE`, désactiver le dernier emplacement actif rend `AU MOINS UN SLOT REQUIS`, et `f7` fonctionne comme `F7` (la casse est ignorée). La page décrit `ON`/`OFF` **en prose** et ne cite **jamais** la forme entre crochets produite par `_on_off` : l'espace avant le croissant fermant ne survit pas à la normalisation D-11 (Pitfall 5).

**Module** :
- `test_etapes_dans_l_ordre_du_code(docs_dir, app, normalize)` : lit les titres rendus dans l'ordre de `enumerate(WIZARD_STEPS, start=1)`, exige que la page les cite **dans cet ordre**, et que le nombre d'étapes soit celui de `WIZARD_STEPS` (relu au code, jamais écrit de mémoire). Constat portant la constante de module `MOTIF_ORDRE_ETAPES` (valeur ASCII, jamais écrite en clair dans la ligne d'assertion : pytest reproduit cette ligne dans sa sortie, et une valeur écrite en clair rendrait la morsure non discriminante — leçon du plan 03-03, tâche 2).
- `test_slots_et_filtres_ancres_au_rendu(docs_dir, app, normalize, section)` : rend `GET /optimize/wizard/slots` puis la même URL avec `?page=2` (le corps dépasse 18 lignes, `BODY_LINES`, `dofus_stuff/web/screens.py:10`), extrait du corps les lignes `N. [ON ] LIBELLE` et `F<n>. [ON ] LIBELLE`, et exige (a) l'égalité d'ensembles entre les libellés d'emplacements rendus et ceux cités par la section, (b) pour chaque filtre, le couple `(F<n>, libellé)` rendu **cité à l'identique** par la section, (c) la seconde source du couple `F6`/`F7` : `TYPE_FILTER_KEYS[5]` et `[6]` (`dofus_stuff/model/solver_spec.py:42-53`) appariés aux libellés de `TYPE_FILTER_LABELS`. Un libellé de la section absent du rendu, ou un couple permuté, est un constat nommant la touche, le libellé cité, le libellé rendu et `SOURCE_WIZARD`.
  </action>
  <acceptance_criteria>
    - La page porte `## Slots et filtres`, cite les 11 libellés d'emplacements rendus et les 10 couples `F<n>`/libellé rendus, et énonce que `F6` = `ARMES DISTANCE` et `F7` = `ARMES MELEE`.
    - `test_etapes_dans_l_ordre_du_code` exige la séquence des 9 titres dans l'ordre de `WIZARD_STEPS`, et son constat porte la constante `MOTIF_ORDRE_ETAPES`.
    - `test_slots_et_filtres_ancres_au_rendu` rend la page 1 **et** la page 2 de l'écran `slots` et compare les couples extraits du rendu à ceux de la page.
    - Permuter les deux valeurs `ARMES DISTANCE`/`ARMES MELEE` de `TYPE_FILTER_LABELS`, retirer l'emplacement `PRYSMARADITE` de `SLOT_GROUP_LABELS`, ou permuter deux entrées de `WIZARD_STEPS`, rougit le module sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest tests/test_docs_wizard.py -q` est vert, et la suite entière reste verte.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_wizard.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in filtre_permute slot_retire etapes_permutees; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in filtre_permute) sed -i 's/"arme_distance": "ARMES DISTANCE"/"arme_distance": "ARMES MELEE"/' "$T/dofus_stuff/web/optimize_wizard.py"; motif='ARMES MELEE';; slot_retire) sed -i '/"prysma": "PRYSMARADITE",/d' "$T/dofus_stuff/web/optimize_wizard.py"; motif='PRYSMARADITE';; etapes_permutees) sed -i 's/    "caracs",/    "papmpo_TMP",/' "$T/dofus_stuff/web/optimize_wizard.py"; sed -i 's/    "papmpo",/    "caracs",/' "$T/dofus_stuff/web/optimize_wizard.py"; sed -i 's/    "papmpo_TMP",/    "papmpo",/' "$T/dofus_stuff/web/optimize_wizard.py"; motif='ordre de WIZARD_STEPS';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 04-01 (TACHE 2) EN ECHEC"; exit 1; fi; echo "morsures 04-01 tache 2 : 3/3 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 04-01 (TACHE 2) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>Les 9 étapes sont citées dans l'ordre du code et les emplacements et filtres sont cités avec leur propre numéro, `F6`/`F7` compris ; trois dérives épinglées (libellés de filtres permutés, emplacement retiré, étapes permutées) font rougir la suite sur une copie verte avant mutation.</done>
</task>


<task type="auto">
  <name>Tâche 3 : les 11 options, les formats d'édition, la syntaxe d'items et la conformité de la page</name>
  <files>docs/wizard-avance.md, tests/test_docs_wizard.py</files>
  <read_first>
    docs/wizard-avance.md
    tests/test_docs_wizard.py
    dofus_stuff/web/optimize_wizard.py
    dofus_stuff/web/routes.py
    dofus_stuff/web/templates/screen.html
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-RESEARCH.md
    .planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-PATTERNS.md
  </read_first>
  <action>Terminer le périmètre WIZ-01 de la page et ajouter les contrôles qui l'ancrent, conformité de forme comprise.

**Page** :
- `## Les 11 options du solveur` : les onze options avec leur numéro et leur libellé rendus (`1. NIVEAU`, `2. JET`, `3. DUREE (S)`, `4. SEED`, `5. TOP-K`, `6. CP-SAT`, `7. STOP SI CIBLES`, `8. AUTO POINTS`, `9. ALLOW POWER`, `10. ALLOW DOMMAGES`, `11. ALLOW DOM CRIT`), l'invite `OPTIONS (N=EDIT) :` et `N=CHOISIR OPTION`, puis la distinction mesurée : `1`–`5` ouvrent un **sous-écran d'édition** (en-tête `OPT-WED`, libellé `VAL :`, `maxlength=20`, statut `VALEUR ENREGISTREE`), `6`–`11` basculent **immédiatement** avec le statut `OPTION MISE A JOUR` ; les refus réels (`OPTION INVALIDE` hors liste, `SAISIR UN NUMERO D'OPTION` si la saisie n'est pas numérique) ; les valeurs acceptées (`jet_mode` parmi `min`/`average`/`max` en ignorant la casse, sinon `JET = MIN|AVERAGE|MAX` ; `seed` vide, `-`, `none` ou `aucun` rend `(aucun)` ; `top_k` a un plancher à 1). La page ne présente **pas** un niveau négatif ni une durée négative comme des entrées normales : le code ne les borne pas, mais en faire un mode d'emploi serait trompeur (D-19).
- `## Les quatre nombres d'une ligne` : les quatre valeurs `B`/`P`/`C`/`W`, la forme sans exo `BASE POINTS CIBLE POIDS` (écrans des caractéristiques) et la forme avec exo `BASE EXO CIBLE POIDS` (écran PA / PM / PO), **jamais fusionnées** ; exactement quatre valeurs séparées par des espaces **ou** des virgules (les deux sont acceptés) ; toute autre quantité rend le message de refus du format concerné ; une valeur non numérique remonte le message brut du convertisseur ; une entrée vide **annule** l'édition ; une saisie valide rend `CARAC ENREGISTREE`. Décrire la mécanique d'édition : taper le **numéro** de la ligne (statut `SAISIR LE NUMERO DE LA LIGNE` si la saisie n'est pas numérique, `NUMERO INVALIDE` hors liste), l'écran de liste affichant `N=EDIT`.
- `## Interdire, forcer, retirer un objet` : les quatre lignes de syntaxe affichées par l'écran, puis la réalité mesurée — `+ID` ajoute aux **interdits**, `-ID` ajoute aux **forcés**, `!ID` retire des deux listes, `CLEAR` (et `clear`) vide les deux ; un identifiant ajouté à une liste est retiré de l'autre ; les espaces autour de la saisie sont acceptés ; le **seul** refus est le message exact du code (la forme `SYNTAXE : +ID | -ID | !ID | CLEAR`), levé quand la saisie n'est ni `CLEAR`/`clear`, ni un préfixe `+`/`-`/`!` suivi de chiffres — un identifiant **sans préfixe** est donc refusé alors que `+ID` ne l'est pas (D-19) ; l'affichage est tronqué au-delà de 8 entrées avec une ligne d'ellipse ; les listes vides rendent l'état vide `(aucun)`.

**Module** :
- `test_options_solveur_ancres_au_rendu(docs_dir, app, normalize, section)` : extrait du corps rendu de `GET /optimize/wizard/options` les lignes numérotées, exige l'égalité d'ensembles avec les couples numéro/libellé cités par la section, et **compte** les options depuis le rendu (Pitfall 3 : aucune table locale privée n'est lue, D-14). Le sous-écran d'édition s'atteint en deux temps (`POST` du numéro, puis `GET`), comme le code le fait.
- `test_formats_d_edition_et_refus_reels(docs_dir, app, normalize, section)` : sur un client neuf, `POST /optimize/wizard/caracs` avec `cmd=4` puis `GET` rend le sous-écran d'édition dont le corps porte le format des caractéristiques ; `POST /optimize/wizard/papmpo` avec `cmd=1` puis `GET` rend celui dont le corps porte le format avec exo ; exige que la page cite **chacun avec son écran** ; puis poste une saisie à trois valeurs sur le sous-écran des caractéristiques et exige le message de refus du format dans la ligne de statut, et exige ce même message cité par la page. Les deux messages sont lus au rendu, jamais recopiés dans le test.
- `test_syntaxe_d_items_et_etat_vide(docs_dir, app, normalize, section)` : sur des clients neufs, poste successivement les formes `+`/`-`/`!` avec un identifiant, `CLEAR`, puis une saisie **sans préfixe**, et exige à chaque fois le corps et le statut réellement rendus (dont l'ajout à une liste qui retire de l'autre, et l'état vide `(aucun)` pour les deux listes sur un client neuf **et** après `CLEAR`) ; puis exige que la page cite les quatre lignes de syntaxe, le message de refus unique et l'état vide. **Aucune** saisie `GO` n'est postée : elle lancerait le solveur.
- `test_page_sans_derive_ni_chemin_invente(docs_dir, normalize)` : un seul `H1` ; les titres de `TITRES_SECTION_ATTENDUS` présents **dans cet ordre** ; dernière ligne non vide exactement `[Retour au sommaire](sommaire.md)` ; aucun lien externe ; aucun bloc de commandes balisé `console` ; toutes les fins de ligne en CRLF et aucun BOM — assertion sur les **octets lus en binaire**, reprise du patron déjà en place de `tests/test_docs_parcours.py` (« Octets en CRLF sans BOM », ~ligne 2533) : la ligne « ne pas asserter sur les octets de fin de ligne » de `.claude/CLAUDE.md` § 10 vise la portabilité Windows, et l'arbitrage retenu suit le précédent exécuté, parce qu'une conversion en LF a déjà été prise en défaut sur ces pages (T-21) ; l'assertion ne porte que sur la page de `docs/`, jamais sur une fixture ni sur `GUIDE_WIZARD.md` ; chaque chemin cité entre accents graves existe depuis `RACINE_DEPOT` et au moins un chemin est cité (patron de `tests/test_docs_parcours.py:1187-1219`).

Tous les constats restent accumulés et joints à **une** assertion par test, dans la forme de D-13 ; les jetons de morsure sont portés par des constantes de module et jamais écrits en clair dans la ligne d'assertion.
  </action>
  <acceptance_criteria>
    - La page porte `## Les 11 options du solveur`, `## Les quatre nombres d'une ligne` et `## Interdire, forcer, retirer un objet`, et cite les onze options rendues, les deux formats et le message de refus unique d'items.
    - `test_options_solveur_ancres_au_rendu` dérive le nombre d'options du rendu et exige l'égalité d'ensembles avec la page.
    - `test_formats_d_edition_et_refus_reels` associe le format sans exo à l'écran des caractéristiques et le format avec exo à l'écran PA / PM / PO, et exige que le message de refus rendu soit cité par la page.
    - `test_syntaxe_d_items_et_etat_vide` poste les formes `+`/`-`/`!`, `CLEAR` et une saisie sans préfixe, exige l'état vide `(aucun)` dans les deux listes sur un client neuf **et** après `CLEAR`, et n'exécute jamais le solveur.
    - `test_page_sans_derive_ni_chemin_invente` passe : `H1` unique, ordre des titres épinglés respecté, ligne de retour exacte, aucun lien externe, aucun bloc `console`, CRLF sans BOM, chemins cités existants et au moins un chemin cité.
    - Permuter les deux messages de format, remplacer le verbe de `+ID`, remplacer le littéral d'état vide, ou renommer une option, rougit le module sur une copie verte avant mutation.
    - `./.venv/Scripts/python.exe -m pytest -q` est vert, et `.data/dofus.sqlite3` garde taille et `mtime_ns`.
  </acceptance_criteria>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_docs_wizard.py -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé pytest</fails_when>
    <automated>PY="$PWD/.venv/Scripts/python.exe"; ok=1; for m in formats_permutes items_verbe etat_vide option_renommee; do T="$(mktemp -d)"; cp -r docs tests dofus_stuff fetcher.py pyproject.toml GUIDE_WIZARD.md README.md "$T"/; avant="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$avant" | grep -qE '[0-9]+ (failed|error)'; then echo "COPIE ROUGE AVANT MUTATION ($m) : la morsure ne serait pas discriminante"; printf '%s\n' "$avant" | tail -15; ok=0; continue; fi; motif=''; case "$m" in formats_permutes) sed -i 's/Format : BASE POINTS CIBLE POIDS/Format : BASE EXO CIBLE POIDS/' "$T/dofus_stuff/web/optimize_wizard.py"; motif='BASE EXO CIBLE POIDS';; items_verbe) sed -i 's/  +ID   AJOUTER INTERDIT/  +ID   AJOUTER FORCE/' "$T/dofus_stuff/web/optimize_wizard.py"; motif='AJOUTER INTERDIT';; etat_vide) sed -i "s/or '(aucun)'/or '(vide)'/g" "$T/dofus_stuff/web/optimize_wizard.py"; motif='aucun';; option_renommee) sed -i 's/11. ALLOW DOM CRIT/11. ALLOW CRIT D/' "$T/dofus_stuff/web/optimize_wizard.py"; motif='ALLOW DOM CRIT';; esac; out="$(cd "$T" && "$PY" -m pytest -q 2>&1)"; if printf '%s' "$out" | grep -qE '[0-9]+ (failed|error)' && printf '%s' "$out" | grep -qF "$motif"; then echo "mutation detectee ($m), motif \"$motif\""; else echo "MUTATION NON DETECTEE ($m) : motif \"$motif\" absent de la sortie"; printf '%s\n' "$out" | tail -30; ok=0; fi; done; if [ "$ok" -ne 1 ]; then echo "MORSURES DU PLAN 04-01 (TACHE 3) EN ECHEC"; exit 1; fi; echo "morsures 04-01 tache 3 : 4/4 detectees (copie verte avant chaque mutation)"</automated>
    <fails_when>exit non-zero, ou une ligne "MUTATION NON DETECTEE", "COPIE ROUGE AVANT MUTATION" ou "MORSURES DU PLAN 04-01 (TACHE 3) EN ECHEC"</fails_when>
    <automated>./.venv/Scripts/python.exe -m pytest -q</automated>
    <fails_when>exit non-zero, ou une ligne contenant "failed" ou "error" dans le résumé de la suite complète</fails_when>
  </verify>
  <done>Le périmètre WIZ-01 est écrit : les onze options, les deux formats d'édition jamais fusionnés, la syntaxe d'items avec son unique refus et l'état vide, et la conformité de forme de la page ; quatre dérives épinglées font rougir la suite sur une copie verte avant mutation, et la suite entière reste verte.</done>
</task>

</tasks>


<threat_model>
**Surface** : une page `docs/`, une ligne d'index, un module de test et le client de test Flask en processus. Aucun réseau, aucun secret, aucune dépendance ajoutée, aucun fichier de `dofus_stuff/**` modifié (D-66). La menace réelle est **documentaire** — une page qui affirme un libellé que le code ne rend plus — et **celle du harnais** — un test qui exécute le produit.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing (documentation qui ment) | `docs/wizard-avance.md` : titres d'étapes, slots, filtres, options, formats, syntaxe d'items | high | mitigate | Chaque libellé cité est lu au rendu (client de test Flask en processus, D-32) ou dans une constante publique (D-14) ; les morsures du plan prouvent que la page rougit quand le code change (titre renommé, filtres permutés, emplacement retiré, étapes permutées, formats permutés, verbe d'items, état vide, option renommée) |
| T-04-02 | Tampering | `.data/dofus.sqlite3` | medium | mitigate | La fixture `app` construit sa propre base sous `tmp_path` ; aucune écriture sous `.data/` ; mesure `taille:mtime_ns:sha256` avant et après la suite complète (tâche 1) |
| T-04-03 | Elevation of privilege | Le harnais : exécution du produit ou du solveur | high | mitigate | Garde `ast` sur le module (racines interdites, `MODULE_BASE_INTERDIT`, `APPEL_PRODUIT`, refus d'un appel dont l'argument `data` porte la paire `"cmd": "GO"`) ; aucun `GO` n'est posté dans cette phase |
| T-04-04 | Information disclosure | Liens et chemins cités par la page | low | mitigate | Aucun lien externe ; le contrôle exige que chaque chemin entre accents graves existe depuis la racine du dépôt et qu'au moins un soit cité (D-03) ; `problemes_liens` reste la garde de structure |
| T-04-05 | Repudiation | Traçabilité des décisions et des morsures | low | mitigate | Les constats citent la page, la valeur attendue et le fichier de code producteur (D-13) ; chaque plan et sa sortie de morsures sont consignés dans son `SUMMARY.md` |
| T-04-SC | Tampering | Installations de paquets | low | accept | Cette phase n'ajoute **aucune** dépendance : `pyproject.toml` est inchangé, aucun `pip install` n'est exécuté ; la porte de légitimité des paquets n'a donc aucun sujet ici (aucun `[ASSUMED]`, aucun `[SUS]`, aucun `[SLOP]`) |

**Hors périmètre** : publication, déploiement distant, synchronisation Dofusdude, suppression de données, achat. Aucune commande destructive n'est employée ; `db clear` n'apparaît dans aucun parcours recommandé et n'est jamais exécuté (D-22/D-23/D-61).
</threat_model>

<verification>
**Contrôles automatisés** : `tests/test_docs_wizard.py` (étapes, slots et filtres, options, formats, items, état vide, conformité de la page, garde de clôture), `tests/test_docs_structure.py` (index, `H1`, retour, encodage) inchangé et vert, la suite entière, l'empreinte de `.data/dofus.sqlite3` autour de la suite, et **dix morsures** sur copie verte avant mutation : titre d'étape renommé, ligne d'index retirée, `H1` désaligné, libellés de filtres permutés, emplacement retiré, étapes permutées, formats permutés, verbe de `+ID` modifié, état vide remplacé, option renommée.

**Non automatisable, nommé comme tel** : la prose libre de la page et le rendu Markdown sur GitHub. Ce plan ne revendique donc aucune exhaustivité rédactionnelle ; les critères portent sur les libellés, les nombres et les messages **cités**. Aucun blocage : rien ne dépend d'un geste humain, d'un secret ou d'un réseau.

**Références de vérification de phase** : `04-VALIDATION.md` § Per-Task Verification Map assigne les lignes `etapes`, `filtres`, `options`, `formats` et `items` à ce plan (vague 1) ; `/gsd:verify-work` rejouera la suite entière et la mesure de non-régression `.data/`.
</verification>

<success_criteria>
- `docs/wizard-avance.md` existe, est indexée, et décrit les 9 étapes dans l'ordre du code, les 11 emplacements, les 10 filtres (`F6` = armes distance, `F7` = armes mêlée), les 11 options, les deux formats d'édition et la syntaxe d'items — chaque libellé retrouvé dans le rendu (critères 1 et 2 du ROADMAP, D-50 à D-54, D-56, D-57).
- Les trois arêtes classables de la sonde WIZ-01 (`ordering`, `adjacency` ×2, `empty`) sont couvertes par des critères confirmables ; les deux lignes non classées sont signalées comme hypothèses dans les plans 04-02 et 04-04.
- Le module ne duplique aucun helper partagé (D-12), n'exécute jamais le produit et ne peut pas poster `GO` (D-15).
- Dix dérives épinglées font rougir la suite sur une copie verte avant mutation ; la suite entière est verte et `.data/dofus.sqlite3` est intact.
</success_criteria>

## Couverture des sources (ce plan)

| Source | Élément | Consommé par |
|--------|---------|--------------|
| ROADMAP § Phase 4 | Critère 1 (9 étapes, slots, filtres `F1`–`F10`, 11 options) | tâche 2 (étapes, slots, filtres) et tâche 3 (options) |
| ROADMAP § Phase 4 | Critère 2 (formats des quatre nombres, syntaxe d'items) | tâche 3 |
| ROADMAP § Phase 4 | Brief du plan 04-01 (rédiger la page + son entrée d'index) | tâche 1 (tranche verticale et index), tâches 2 et 3 (expansion) |
| REQUIREMENTS | WIZ-01 | `requirements` du plan |
| RESEARCH | Inventaire du rendu mesuré (9 étapes, 11 options, 11 slots, 10 filtres, formats, items) | tâches 2 et 3 (attentes lues au rendu) |
| RESEARCH | Pitfall 3 (les options n'ont pas de constante publique) et Pitfall 5 (`[ON ]` ne se normalise pas) | tâche 2 (invite en prose) et tâche 3 (comptage au rendu) |
| RESEARCH | Wave 0 Gaps : `tests/test_docs_wizard.py` | tâche 1 (création du module) |
| CONTEXT | D-50 (ordre et titres des 9 étapes) | tâche 1 (liste) et tâche 2 (contrôle d'ordre) |
| CONTEXT | D-53 (`F6`/`F7` adossés à `TYPE_FILTER_KEYS` et `TYPE_FILTER_LABELS`) | tâche 2 |
| CONTEXT | D-54 (formats, syntaxe d'items, touches citées) | tâche 3 |
| CONTEXT | D-56 (gabarit D-01 : `H1`, bloc Source de vérité, ligne de retour) | tâche 1 (création) et tâche 3 (contrôle de conformité) |
| CONTEXT | D-57 (entrée d'index « Wizard avancé ») | tâche 1 |
| CONTEXT | D-12, D-13, D-14, D-15 (helpers partagés, message localisant, API publique, interdits d'exécution) | tâches 1 à 3 |
| CONTEXT | D-19 (aucune sémantique inventée) | tâche 3 (options non bornées, `+ID` n'est pas un refus) |
| CONTEXT | D-66 et D-67 (aucune modification de `dofus_stuff/**`, documentation en français) | toutes les tâches |

**Audit de complétude de la phase** : les quatre types de sources sont audités sur l'ensemble des quatre plans — GOAL (les 5 critères du ROADMAP), REQ (`WIZ-01`, `WIZ-02`, `WIZ-03`), RESEARCH (les faits mesurés et les huit pièges), CONTEXT (D-46 à D-67). Aucun item n'est laissé sans plan : `WIZ-02` va au plan 04-02, `WIZ-03` aux plans 04-04 et 04-03, le critère 5 au plan 04-04 (rouge) puis 04-03 (vert). Les exclusions sont celles de `04-CONTEXT.md` § Deferred Ideas : `docs/base-locale.md` et son renvoi en prose (phase 5), liste épinglée des 8 pages du sommaire (phase 6), `glossaire.md` et `depannage.md` (phases suivantes).

**Écart assumé, enregistré ici (patron ÉCR-1…ÉCR-5 de la phase 3)** — *ÉCR-6* : le plan 04-03 s'exécute en **vague 4**, après le plan 04-04 (vague 3), alors que le brouillon `04-VALIDATION.md` annonçait « plan 03, vague 2 ». Motif mesuré : le détecteur doit être **exécuté contre `GUIDE_WIZARD.md` encore obsolète** pour que le rouge du critère 5 soit réel, et la correction qui produit le vert vient ensuite (D-59a). *ÉCR-7* : la ligne « Sommaire » de la carte de vérification de `04-VALIDATION.md` est attribuée au plan 03 ; la croissance d'index appartient en réalité à **04-01, vague 1** — c'est là que l'entrée est créée et que les gardes de structure sont exercées —, et le plan 04-03 (vague 4) ne fait que constater que ces gardes restent vertes après la réduction de l'aiguillage. Aucun autre écart à `04-CONTEXT.md` n'est introduit : D-47 est honorée en lisant la numérotation et les libellés **au code** (`dofus_stuff/web/routes.py:217-222`) — l'aiguillage peut employer les formes courtes que D-47 prescrit (`4. OPTIMISATION`, `3. PANOPLIES`, `5. SYSTEME`), que le détecteur accepte par appartenance de mots, tandis que la page `docs/wizard-avance.md` cite les **libellés rendus complets** lus à l'écran (plan 04-01), conformément à D-19 et D-32/D-36.

## Artefacts produits par cette phase (inventaire tenu à jour par les quatre plans)

| Type | Symbole | Plan |
|------|---------|------|
| page | `docs/wizard-avance.md` | 04-01 |
| module de test | `tests/test_docs_wizard.py` | 04-01 |
| fixture | `tests/fixtures/guide-wizard-obsolete.md` | 04-04 |
| constante de module | `TITRES_SECTION_ATTENDUS` (`tests/test_docs_wizard.py`) | 04-01, complétée en 04-02 |
| helpers locaux | `_texte_page`, `_lignes_du_corps`, `_statut`, `_touches`, `_sous_section` | 04-01 |
| helpers locaux | `_empreinte` | 04-02 |
| helpers locaux | `_faits_du_rendu`, `renvois_obsoletes`, `_lire_fixture` | 04-04 |
| constantes de morsure et de garde | `MOTIF_ORDRE_ETAPES`, `RACINES_INTERDITES`, `MODULE_BASE_INTERDIT`, `APPELS_SUPPRESSION`, `APPEL_PRODUIT` | 04-01 |
| constantes de morsure | `MOTIF_ARRIVEE`, `MOTIF_IDENTIFIANT_ECRAN`, `MOTIF_EXEMPLE`, `MOTIF_TOUCHE`, `MOTIF_COMMANDE_RECAP`, `MOTIF_COMMANDE_NUMERIQUE` | 04-02 |
| constantes de détection | `MOTIF_RENVOI_MENU`, `MOTIF_RENVOI_FILTRE`, `MOTIF_RENVOI_ARRIVEE` (valeurs ASCII `renvoi obsolete (a)`, `(b)`, `(c)`) | 04-04 |
| constantes de morsure | `MOTIF_LIEN_AIGUILLAGE`, `MOTIF_AIGUILLAGE_BRUYANT`, `MOTIF_LIEN_PRODUIT` (dans `tests/test_docs_wizard.py`) et `MOTIF_LIEN_D63`, `MOTIF_PAGES_INEXISTANTES`, `MOTIF_LIEN_NON_DECLARE`, `MOTIF_LIEN_MORT` (dans `tests/test_docs_parcours.py`) | 04-03 |
| fonctions de test | `test_garde_de_cloture_du_harnais`, `test_page_et_index_du_wizard`, `test_etapes_dans_l_ordre_du_code`, `test_slots_et_filtres_ancres_au_rendu`, `test_options_solveur_ancres_au_rendu`, `test_formats_d_edition_et_refus_reels`, `test_syntaxe_d_items_et_etat_vide`, `test_page_sans_derive_ni_chemin_invente` | 04-01 |
| fonctions de test | `test_ecrans_et_arrivee_du_wizard`, `test_touches_et_commandes_par_etape`, `test_exemple_guide_ancre_au_rendu`, `test_data_locale_non_modifiee_autour_des_rendus` | 04-02 |
| fonctions de test | `test_copie_figee_signalee_par_le_detecteur`, `test_renvois_legitimes_non_signales`, `test_aiguillage_sans_renvoi_obsolete` | 04-04 |
| fonctions de test | `test_aiguillage_et_readme` (dans `tests/test_docs_wizard.py`), `test_lien_wizard_avance_legitime` (dans `tests/test_docs_parcours.py`) | 04-03 |

## Output

Create `.planning/phases/04-wizard-avanc-et-r-sorption-de-la-dette-guide-wizard/04-01-SUMMARY.md` when done

